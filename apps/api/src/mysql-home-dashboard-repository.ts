import type{Pool,RowDataPacket}from'mysql2/promise';import type{HomeDashboardItem}from'@scoutops/contracts';import type{HomeDashboardRepository}from'./home-dashboard-service.js';
export class MySqlHomeDashboardRepository implements HomeDashboardRepository{constructor(private readonly pool:Pool){}async list(input:Parameters<HomeDashboardRepository['list']>[0]){if(!input.capabilities.length)return[];const[rows]=await this.pool.query<RowDataPacket[]>("SELECT id,kind,title,reason,route,priority,owner_label,due_at,source_count,observed_at,severity,source_version FROM home_dashboard_items WHERE organization_id=? AND workspace_id=? AND required_capability IN (?) AND route LIKE '/%' AND route NOT LIKE '//%' AND ((kind='health' AND audience_user_id=?) OR (kind<>'health' AND (audience_user_id IS NULL OR audience_user_id=?))) ORDER BY FIELD(kind,'action','change','follow','health'),FIELD(priority,'overdue','blocking','high_risk','high_value','normal'),observed_at DESC,id DESC LIMIT 80",[input.organizationId,input.workspaceId,input.capabilities,input.actorId,input.actorId]);return rows.map(row=>({id:String(row.id),kind:String(row.kind),title:String(row.title),reason:String(row.reason),route:String(row.route),priority:row.priority===null?null:String(row.priority),owner_label:row.owner_label===null?null:String(row.owner_label),due_at:row.due_at===null?null:new Date(row.due_at).toISOString(),source_count:row.source_count===null?null:Number(row.source_count),observed_at:new Date(row.observed_at).toISOString(),severity:String(row.severity),source_version:Number(row.source_version)}as HomeDashboardItem));}}
