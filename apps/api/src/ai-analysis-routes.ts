import type{FastifyInstance,FastifyRequest}from'fastify';import type{LocalAuthService}from'@scoutops/auth';import type{AuthorizationService,Capability}from'@scoutops/authorization';import{sessionToken}from'./auth-routes.js';import{ApiError,requireIdempotencyKey}from'./api-foundation.js';import type{AiAnalysisService}from'./ai-analysis-service.js';
export interface AiAnalysisRouteOptions{service:AiAnalysisService;authorization:AuthorizationService;auth:LocalAuthService;secureCookie:boolean;webOrigin:string}const ids=(r:FastifyRequest)=>({requestId:String(r.headers['x-request-id']),traceId:String(r.headers['x-trace-id'])}),env=(data:unknown,r:FastifyRequest)=>({data,request_id:ids(r).requestId,trace_id:ids(r).traceId});
export function registerAiAnalysisRoutes(app:FastifyInstance,o:AiAnalysisRouteOptions){const scope=async(r:FastifyRequest,capability:Capability)=>{const a=await o.auth.authenticate(sessionToken(r,o.secureCookie)),x=await o.authorization.resolveSession(a.user.id,a.session.id);await o.authorization.authorize({actorId:a.user.id,organizationId:x.context.organization_id,workspaceId:x.context.workspace_id,capability,surface:'api',...ids(r)});return{organizationId:x.context.organization_id,workspaceId:x.context.workspace_id,actorId:a.user.id};},write=async(r:FastifyRequest)=>{if(r.headers.origin!==o.webOrigin)throw new ApiError(403,'origin_forbidden','请求来源不允许。','从 ScoutOps 页面重试。');return{...await scope(r,'opportunity:decide'),...ids(r),idempotencyKey:requireIdempotencyKey(r)};};app.get('/api/v1/opportunities/:id/ai-analyses',async r=>env(await o.service.list({...await scope(r,'opportunity:read'),opportunityId:(r.params as any).id}),r));app.post('/api/v1/opportunities/:id/ai-analyses',async(r,reply)=>{const x=await o.service.queue({...await write(r),opportunityId:(r.params as any).id,value:r.body});reply.code(202);return env(x,r);});app.post('/api/v1/ai-analyses/:id/reviews',async(r,reply)=>{const x=await o.service.review({...await write(r),resultId:(r.params as any).id,value:r.body});reply.code(201);return env(x,r);});}
