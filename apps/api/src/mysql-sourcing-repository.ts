import{randomUUID}from'node:crypto';import type{Pool,PoolConnection,RowDataPacket}from'mysql2/promise';import{SourcingServiceError,type SourcingContext,type SourcingRepository}from'./sourcing-service.js';import{loadErpSourcingReference}from'./erp-sourcing-reference.js';
const parse=<T>(v:unknown):T=>typeof v==='string'?JSON.parse(v):v as T,iso=(v:unknown)=>(v instanceof Date?v:new Date(String(v))).toISOString();
export class MySqlSourcingRepository implements SourcingRepository{constructor(private readonly pool:Pool){}
 async list(i:any){const[rows]=await this.pool.query<RowDataPacket[]>('SELECT * FROM sourcing_searches WHERE organization_id=? AND workspace_id=? ORDER BY created_at DESC,id DESC',[i.organizationId,i.workspaceId]);return rows.map(r=>({id:String(r.id),input_type:r.input_type,input_ref:String(r.input_ref),status:r.status,candidate_count:Number(r.candidate_count),missing_fields:parse(r.missing_fields_json),created_at:iso(r.created_at)}));}
 async detail(i:any){const[searches]=await this.pool.query<RowDataPacket[]>('SELECT * FROM sourcing_searches WHERE id=? AND organization_id=? AND workspace_id=?',[i.searchId,i.organizationId,i.workspaceId]);if(!searches[0])throw new SourcingServiceError('sourcing_search_not_found',404,'刷新找货列表。');const[candidates]=await this.pool.query<RowDataPacket[]>('SELECT c.*,q.id quote_id,q.quote_version,q.specification quote_specification,q.lead_time_days quote_lead_time_days,q.location quote_location,q.confidence_value quote_confidence,q.stability_status,q.risk_level FROM sourcing_candidates c LEFT JOIN supplier_quotes q ON q.candidate_id=c.id AND q.is_current=1 WHERE c.search_id=? ORDER BY c.created_at,c.id',[i.searchId]),erpReference=searches[0].input_type==='opportunity'?await loadErpSourcingReference(this.pool,{organizationId:i.organizationId,workspaceId:i.workspaceId,opportunityId:String(searches[0].input_ref)}):null;return{...this.search(searches[0]),candidates:candidates.map(r=>this.candidate(r)),erp_reference:erpReference};}
 async listComparisons(i:any){const[rows]=await this.pool.query<RowDataPacket[]>('SELECT * FROM sourcing_comparisons WHERE organization_id=? AND workspace_id=? ORDER BY created_at DESC',[i.organizationId,i.workspaceId]);const result=[];for(const row of rows){const ids=parse<string[]>(row.quote_ids_json),[quotes]=await this.pool.query<RowDataPacket[]>('SELECT * FROM supplier_quotes WHERE organization_id=? AND workspace_id=? AND id IN (?)',[i.organizationId,i.workspaceId,ids]);result.push({id:String(row.id),name:String(row.name),quotes:quotes.map(r=>this.quote(r)),created_at:iso(row.created_at)});}return result;}
 async createSearch(i:any){const old=await this.operation(i);if(old)return old;const c=await this.pool.getConnection(),now=new Date();try{await c.beginTransaction();const[tasks]=await c.query<RowDataPacket[]>("SELECT id,status FROM collection_tasks WHERE id=? AND organization_id=? AND workspace_id=? AND status IN ('succeeded','succeeded_empty','completed_with_warnings') FOR UPDATE",[i.value.collection_task_id,i.organizationId,i.workspaceId]);if(!tasks[0])throw new SourcingServiceError('sourcing_collection_task_not_ready',409,'选择当前工作区已完成的供应链采集任务。');await c.query("INSERT INTO sourcing_searches (id,organization_id,workspace_id,collection_task_id,input_type,input_ref,status,candidate_count,missing_fields_json,request_id,trace_id,created_by,created_at,updated_at) VALUES (?,?,?,?,?,?,'queued',0,'[]',?,?,?,?,?)",[i.id,i.organizationId,i.workspaceId,i.value.collection_task_id,i.value.input_type,i.value.input_ref,i.requestId,i.traceId,i.actorId,now,now]);const jobId=randomUUID();await c.query("INSERT INTO sourcing_projection_jobs (id,organization_id,workspace_id,search_id,status,attempt_count,available_at,request_id,trace_id,created_at,updated_at) VALUES (?,?,?,?,'queued',0,?,?,?,?,?)",[jobId,i.organizationId,i.workspaceId,i.id,now,i.requestId,i.traceId,now,now]);const result={id:i.id,status:'queued',job_id:jobId};await this.record(c,i,'sourcing.search.queued',i.id,result,now);await this.save(c,i,i.id,result,now);await c.commit();return result;}catch(e){await c.rollback();throw e;}finally{c.release();}}
 async confirmQuote(i:any){const old=await this.operation(i);if(old)return old;const c=await this.pool.getConnection(),now=new Date();try{await c.beginTransaction();const[rows]=await c.query<RowDataPacket[]>('SELECT * FROM sourcing_candidates WHERE id=? AND organization_id=? AND workspace_id=? FOR UPDATE',[i.value.candidate_id,i.organizationId,i.workspaceId]);const r=rows[0];if(!r)throw new SourcingServiceError('sourcing_candidate_not_found',404,'刷新候选列表。');const[versions]=await c.query<RowDataPacket[]>('SELECT COALESCE(MAX(quote_version),0)+1 version FROM supplier_quotes WHERE candidate_id=?',[r.id]),version=Number(versions[0]?.version??1);await c.query('UPDATE supplier_quotes SET is_current=0 WHERE candidate_id=? AND is_current=1',[r.id]);await c.query("INSERT INTO supplier_quotes (id,organization_id,workspace_id,candidate_id,quote_version,is_current,supplier_name,product_title,specification,moq,quoted_price,currency,lead_time_days,location,original_url,observed_at,evidence_id,confidence_value,stability_status,risk_level,confirmed_by,request_id,trace_id,created_at) VALUES (?,?,?,?,?,1,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)",[i.id,i.organizationId,i.workspaceId,r.id,version,r.supplier_name,r.product_title,i.value.specification,r.moq,r.quoted_price,r.currency,i.value.lead_time_days,i.value.location,r.original_url,i.value.observed_at,i.value.evidence_id,i.value.confidence_value,i.value.stability_status,i.value.risk_level,i.actorId,i.requestId,i.traceId,now]);await c.query("UPDATE sourcing_candidates SET specification=?,lead_time_days=?,location=?,confidence_value=?,status='ready',missing_fields_json='[]' WHERE id=?",[i.value.specification,i.value.lead_time_days,i.value.location,i.value.confidence_value,r.id]);const result={id:i.id,candidate_id:String(r.id),quote_version:version,status:'confirmed'};await this.record(c,i,'supplier_quote.confirmed',i.id,result,now);await this.save(c,i,i.id,result,now);await c.commit();return result;}catch(e){await c.rollback();throw e;}finally{c.release();}}
 async createComparison(i:any){const old=await this.operation(i);if(old)return old;const c=await this.pool.getConnection(),now=new Date();try{await c.beginTransaction();const[rows]=await c.query<RowDataPacket[]>('SELECT id FROM supplier_quotes WHERE organization_id=? AND workspace_id=? AND is_current=1 AND id IN (?)',[i.organizationId,i.workspaceId,i.value.quote_ids]);if(rows.length!==i.value.quote_ids.length)throw new SourcingServiceError('sourcing_quote_scope_invalid',409,'只比较当前工作区的现行报价。');await c.query('INSERT INTO sourcing_comparisons (id,organization_id,workspace_id,name,quote_ids_json,created_by,request_id,trace_id,created_at) VALUES (?,?,?,?,?,?,?,?,?)',[i.id,i.organizationId,i.workspaceId,i.value.name,JSON.stringify(i.value.quote_ids),i.actorId,i.requestId,i.traceId,now]);const result={id:i.id,name:i.value.name,quote_count:rows.length};await this.record(c,i,'sourcing.comparison.created',i.id,result,now);await this.save(c,i,i.id,result,now);await c.commit();return result;}catch(e){await c.rollback();throw e;}finally{c.release();}}
 async createPurchaseTask(i:any){const old=await this.operation(i);if(old)return old;const c=await this.pool.getConnection(),now=new Date();try{await c.beginTransaction();const[rows]=await c.query<RowDataPacket[]>('SELECT moq FROM supplier_quotes WHERE id=? AND organization_id=? AND workspace_id=? AND is_current=1',[i.quoteId,i.organizationId,i.workspaceId]);if(!rows[0])throw new SourcingServiceError('supplier_quote_not_found',404,'刷新报价列表。');if(i.quantity<Number(rows[0].moq))throw new SourcingServiceError('purchase_quantity_below_moq',409,'采购数量不得低于 MOQ。');await c.query("INSERT INTO sourcing_purchase_tasks (id,organization_id,workspace_id,quote_id,quantity,reason,status,created_by,request_id,trace_id,created_at,updated_at) VALUES (?,?,?,?,?,?,'queued',?,?,?,?,?)",[i.id,i.organizationId,i.workspaceId,i.quoteId,i.quantity,i.reason,i.actorId,i.requestId,i.traceId,now,now]);const result={id:i.id,status:'queued',quote_id:i.quoteId,quantity:i.quantity};await this.record(c,i,'sourcing.purchase_task.queued',i.id,result,now);await this.save(c,i,i.id,result,now);await c.commit();return result;}catch(e){await c.rollback();throw e;}finally{c.release();}}
 private search(r:RowDataPacket){return{id:String(r.id),input_type:r.input_type,input_ref:String(r.input_ref),status:r.status,candidate_count:Number(r.candidate_count),missing_fields:parse(r.missing_fields_json),created_at:iso(r.created_at)};}private candidate(r:RowDataPacket){return{id:String(r.id),supplier_name:String(r.supplier_name),product_title:String(r.product_title),specification:r.quote_specification??r.specification,moq:Number(r.moq),quoted_price:Number(r.quoted_price),currency:String(r.currency),lead_time_days:r.quote_lead_time_days==null?null:Number(r.quote_lead_time_days),location:r.quote_location??r.location,original_url:String(r.original_url),observed_at:iso(r.observed_at),evidence_id:String(r.raw_evidence_id),confidence_value:r.quote_confidence==null?null:Number(r.quote_confidence),status:r.status,missing_fields:parse(r.missing_fields_json),quote:r.quote_id?{id:String(r.quote_id),version:Number(r.quote_version),stability_status:r.stability_status,risk_level:r.risk_level}:null};}private quote(r:RowDataPacket){return{id:String(r.id),supplier_name:String(r.supplier_name),product_title:String(r.product_title),specification:String(r.specification),moq:Number(r.moq),quoted_price:Number(r.quoted_price),currency:String(r.currency),lead_time_days:Number(r.lead_time_days),location:String(r.location),confidence_value:Number(r.confidence_value),stability_status:r.stability_status,risk_level:r.risk_level,evidence_id:String(r.evidence_id)};}
 private async operation(i:SourcingContext&{route:string}){const[rows]=await this.pool.query<RowDataPacket[]>('SELECT result_json FROM sourcing_operations WHERE actor_id=? AND route=? AND idempotency_key=?',[i.actorId,i.route,i.idempotencyKey]);return rows[0]?parse(rows[0].result_json):null;}private save(c:PoolConnection,i:SourcingContext&{route:string},id:string,result:unknown,now:Date){return c.query('INSERT INTO sourcing_operations (id,actor_id,route,idempotency_key,resource_id,result_json,created_at) VALUES (?,?,?,?,?,?,?)',[randomUUID(),i.actorId,i.route,i.idempotencyKey,id,JSON.stringify(result),now]);}private async record(c:PoolConnection,i:SourcingContext,type:string,id:string,payload:unknown,now:Date){const event=randomUUID();await c.query('INSERT INTO sourcing_events (id,organization_id,workspace_id,event_type,resource_id,actor_id,payload_json,request_id,trace_id,created_at) VALUES (?,?,?,?,?,?,?,?,?,?)',[event,i.organizationId,i.workspaceId,type,id,i.actorId,JSON.stringify(payload),i.requestId,i.traceId,now]);await c.query("INSERT INTO sourcing_outbox (id,organization_id,workspace_id,event_type,resource_id,payload_json,status,available_at,created_at) VALUES (?,?,?,?,?,?,'queued',?,?)",[event,i.organizationId,i.workspaceId,type,id,JSON.stringify(payload),now,now]);}}
