import type{FastifyInstance,FastifyRequest}from"fastify";import type{LocalAuthService}from"@scoutops/auth";import type{AuthorizationService}from"@scoutops/authorization";import{sessionToken}from"./auth-routes.js";import type{SecurityOperationsService}from"./security-operations-service.js";export interface SecurityOperationsRouteOptions{service:SecurityOperationsService;authorization:AuthorizationService;auth:LocalAuthService;secureCookie:boolean}export function registerSecurityOperationsRoutes(app:FastifyInstance,o:SecurityOperationsRouteOptions){app.get("/api/v1/platform/security/operations",async(r:FastifyRequest,reply)=>{const requestId=String(r.headers["x-request-id"]),traceId=String(r.headers["x-trace-id"]),a=await o.auth.authenticate(sessionToken(r,o.secureCookie));await o.authorization.authorize({actorId:a.user.id,capability:"platform:secure",surface:"api",requestId,traceId});reply.header("cache-control","private, no-store");return{data:await o.service.read({actorId:a.user.id,window:(r.query as any).window,requestId,traceId}),request_id:requestId,trace_id:traceId};});}
