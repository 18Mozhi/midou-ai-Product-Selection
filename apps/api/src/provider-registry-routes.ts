import type{FastifyInstance,FastifyRequest}from'fastify';import type{LocalAuthService}from'@scoutops/auth';import type{AuthorizationService}from'@scoutops/authorization';import type{ProviderDefinitionInput}from'@scoutops/contracts';import type{ProviderRegistryService}from'./provider-registry-service.js';import{sessionToken}from'./auth-routes.js';import{ApiError,requireIdempotencyKey}from'./api-foundation.js';
export interface ProviderRegistryRouteOptions{service:ProviderRegistryService;authorization:AuthorizationService;auth:LocalAuthService;secureCookie:boolean;webOrigin:string;}const ids=(r:FastifyRequest)=>({requestId:r.headers['x-request-id']!.toString(),traceId:r.headers['x-trace-id']!.toString()}),envelope=(data:unknown,r:FastifyRequest)=>({data,request_id:ids(r).requestId,trace_id:ids(r).traceId});export function registerProviderRegistryRoutes(app:FastifyInstance,o:ProviderRegistryRouteOptions){const actor=async(r:FastifyRequest)=>{const a=await o.auth.authenticate(sessionToken(r,o.secureCookie));await o.authorization.authorize({actorId:a.user.id,capability:'provider:configure',surface:'api',...ids(r)});return a.user.id;},write=async(r:FastifyRequest)=>{if(r.headers.origin!==o.webOrigin)throw new ApiError(403,'origin_forbidden','请求来源不允许。','从 ScoutOps 页面重试。');return actor(r);};app.get('/api/v1/platform/providers',async(r,reply)=>{await actor(r);reply.header('cache-control','private, no-store');return envelope(await o.service.list(),r);});app.post('/api/v1/platform/providers',async(r,reply)=>{const actorId=await write(r),data=await o.service.create(r.body as ProviderDefinitionInput,{actorId,idempotencyKey:requireIdempotencyKey(r),...ids(r)});reply.code(201);return envelope(data,r);});app.put('/api/v1/platform/providers/:providerId',async(r)=>{const actorId=await write(r),body=r.body as ProviderDefinitionInput&{expected_version:number},providerId=(r.params as{providerId:string}).providerId;const{expected_version,...value}=body;return envelope(await o.service.update(providerId,value,expected_version,{actorId,idempotencyKey:requireIdempotencyKey(r),...ids(r)}),r);});}
