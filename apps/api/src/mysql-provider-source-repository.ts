import { randomUUID } from 'node:crypto';
import type { Pool, PoolConnection, RowDataPacket } from 'mysql2/promise';
import type { ProviderSourceReplay, ProviderSourceRepository, ProvisionedSource } from './provider-source-service.js';
import { ProviderSourceServiceError } from './provider-source-service.js';
const iso=(value:unknown)=>new Date(value as string|Date).toISOString();
const provisioned=(row:RowDataPacket):ProvisionedSource=>({id:String(row.id),code:String(row.code),status:row.status,version:Number(row.version),schedule_minutes:Number(row.schedule_minutes),timeout_ms:Number(row.timeout_ms),retry_limit:Number(row.retry_limit),updated_at:iso(row.updated_at)});
const replay=(row:RowDataPacket):ProviderSourceReplay=>({id:String(row.id),task_id:String(row.task_id),provider_id:String(row.provider_id),source_code:String(row.source_code),status:String(row.status),item_count:Number(row.item_count),error_code:row.error_code==null?null:String(row.error_code),request_id:String(row.request_id),trace_id:String(row.trace_id),created_at:iso(row.created_at),updated_at:iso(row.updated_at)});
export class MySqlProviderSourceRepository implements ProviderSourceRepository{
  constructor(private readonly pool:Pool){}
  async listProvisioned(codes:string[]){if(!codes.length)return[];const[rows]=await this.pool.query<RowDataPacket[]>(`SELECT id,code,status,version,schedule_minutes,timeout_ms,retry_limit,updated_at FROM providers WHERE code IN (${codes.map(()=>'?').join(',')})`,codes);return rows.map(provisioned);}
  async syncCatalog(input:Parameters<ProviderSourceRepository['syncCatalog']>[0]){const c=await this.pool.getConnection();try{await c.beginTransaction();const[actors]=await c.query<RowDataPacket[]>("SELECT pra.user_id FROM platform_role_assignments pra JOIN users u ON u.id=pra.user_id WHERE pra.role_code='platform_super_admin' AND u.status='active' ORDER BY pra.created_at LIMIT 1 FOR UPDATE"),actorId=actors[0]?.user_id?String(actors[0].user_id):null;if(!actorId){await c.rollback();return{inserted:0,updated:0,automatic_enabled:0,status:'waiting_for_platform_admin' as const};}let inserted=0,updated=0,automatic=0;for(const d of input.definitions){const[rows]=await c.query<RowDataPacket[]>('SELECT id,status,version FROM providers WHERE code=? FOR UPDATE',[d.code]);const current=rows[0];if(current){await c.query('UPDATE providers SET name=?,target_url=?,access_mode=?,markets_json=?,languages_json=?,fields_json=?,dedupe_key=?,failure_rules_json=?,parser_version=?,healthcheck_url=?,owner_label=?,updated_at=? WHERE id=?',[d.name,d.target_url,d.access_mode,JSON.stringify(d.markets),JSON.stringify(d.languages),JSON.stringify(d.fields),d.dedupe_key,JSON.stringify(d.failure_rules),d.parser_version,d.healthcheck_url,d.owner_label,input.now,current.id]);updated+=1;if(current.status==='enabled'&&d.availability==='automatic')automatic+=1;continue;}const id=randomUUID(),status=d.status;await c.query('INSERT INTO providers (id,code,name,target_url,access_mode,markets_json,languages_json,fields_json,schedule_minutes,concurrency_limit,timeout_ms,retry_limit,circuit_failure_threshold,dedupe_key,retention_days,failure_rules_json,parser_version,healthcheck_url,owner_label,status,version,created_by,updated_by,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,1,?,?,?,?)',[id,d.code,d.name,d.target_url,d.access_mode,JSON.stringify(d.markets),JSON.stringify(d.languages),JSON.stringify(d.fields),d.schedule_minutes,d.concurrency_limit,d.timeout_ms,d.retry_limit,d.circuit_failure_threshold,d.dedupe_key,d.retention_days,JSON.stringify(d.failure_rules),d.parser_version,d.healthcheck_url,d.owner_label,status,actorId,actorId,input.now,input.now]);await c.query('INSERT INTO provider_versions (id,provider_id,version,snapshot_json,actor_id,action,request_id,trace_id,created_at) VALUES (?,?,?,?,?,?,?,?,?)',[randomUUID(),id,1,JSON.stringify({...d,id,status,version:1}),actorId,'created','automatic-source-catalog','automatic-source-catalog',input.now]);inserted+=1;if(status==='enabled'&&d.availability==='automatic')automatic+=1;}await c.commit();return{inserted,updated,automatic_enabled:automatic,status:'synced' as const};}catch(error){await c.rollback();throw error;}finally{c.release();}}
  async provision(input:Parameters<ProviderSourceRepository['provision']>[0]){const c=await this.pool.getConnection();try{await c.beginTransaction();const replayed=await this.operationProvider(c,input.actorId,input.route,input.idempotencyKey);if(replayed){await c.commit();return replayed;}const[existing]=await c.query<RowDataPacket[]>('SELECT id,code,status,version,updated_at FROM providers WHERE code=? FOR UPDATE',[input.definition.code]);if(existing[0])throw new ProviderSourceServiceError('provider_source_already_provisioned',409,'来源已经自动登记，请直接查看状态或手动刷新。');const d=input.definition,status=d.status;await c.query('INSERT INTO providers (id,code,name,target_url,access_mode,markets_json,languages_json,fields_json,schedule_minutes,concurrency_limit,timeout_ms,retry_limit,circuit_failure_threshold,dedupe_key,retention_days,failure_rules_json,parser_version,healthcheck_url,owner_label,status,version,created_by,updated_by,created_at,updated_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,1,?,?,?,?)',[input.providerId,d.code,d.name,d.target_url,d.access_mode,JSON.stringify(d.markets),JSON.stringify(d.languages),JSON.stringify(d.fields),d.schedule_minutes,d.concurrency_limit,d.timeout_ms,d.retry_limit,d.circuit_failure_threshold,d.dedupe_key,d.retention_days,JSON.stringify(d.failure_rules),d.parser_version,d.healthcheck_url,d.owner_label,status,input.actorId,input.actorId,input.now,input.now]);const result:ProvisionedSource={id:input.providerId,code:d.code,status,version:1,schedule_minutes:d.schedule_minutes,timeout_ms:d.timeout_ms,retry_limit:d.retry_limit,updated_at:input.now.toISOString()};await c.query('INSERT INTO provider_versions (id,provider_id,version,snapshot_json,actor_id,action,request_id,trace_id,created_at) VALUES (?,?,?,?,?,?,?,?,?)',[randomUUID(),input.providerId,1,JSON.stringify({...d,id:input.providerId,status,version:1}),input.actorId,'created',input.requestId,input.traceId,input.now]);await c.query('INSERT INTO provider_source_operations (id,actor_id,route,idempotency_key,provider_id,replay_run_id,created_at) VALUES (?,?,?,?,?,NULL,?)',[randomUUID(),input.actorId,input.route,input.idempotencyKey,input.providerId,input.now]);await c.commit();return result;}catch(error){await c.rollback();if(error instanceof ProviderSourceServiceError)throw error;if((error as{code?:string}).code==='ER_DUP_ENTRY')throw new ProviderSourceServiceError('provider_source_conflict',409,'刷新目录后重试。');throw error;}finally{c.release();}}
  async replay(input:Parameters<ProviderSourceRepository['replay']>[0]){const c=await this.pool.getConnection();try{await c.beginTransaction();const replayed=await this.operationReplay(c,input.actorId,input.route,input.idempotencyKey);if(replayed){await c.commit();return replayed;}const[providers]=await c.query<RowDataPacket[]>('SELECT * FROM providers WHERE id=? FOR UPDATE',[input.providerId]),provider=providers[0];if(!provider)throw new ProviderSourceServiceError('provider_not_found',404,'刷新首批来源目录。');if(!['google_news_search','manual_product_supply_csv'].includes(String(provider.code)))throw new ProviderSourceServiceError('provider_source_not_builtin',409,'仅首批来源可从此入口运行。');if(provider.status!=='enabled')throw new ProviderSourceServiceError('provider_source_disabled',409,'先在来源定义页完成所有者复核并显式启用。');if((provider.code==='google_news_search')!==('query'in input.target))throw new ProviderSourceServiceError('provider_source_target_mismatch',400,'目标内容与来源类型不匹配。');const[scope]=await c.query<RowDataPacket[]>('SELECT w.id FROM workspaces w JOIN organizations o ON o.id=w.organization_id WHERE w.id=? AND w.organization_id=? AND w.status=\'active\' AND o.status=\'active\' FOR UPDATE',[input.workspaceId,input.organizationId]);if(!scope[0])throw new ProviderSourceServiceError('provider_source_scope_inactive',409,'选择活动组织与工作区。');await c.query("INSERT INTO collection_tasks (id,organization_id,workspace_id,status,coverage_status,priority,scheduled_at,available_at,leased_at,lease_owner,lease_token_hash,lease_expires_at,started_at,finished_at,attempt_count,successful_subquery_count,failed_subquery_count,blocked_subquery_count,available_result_count,missing_fields_json,last_error_code,rate_limit_reset_at,replay_of_task_id,replay_reason,request_id,trace_id,version,created_by,created_at,updated_at) VALUES (?,?,?,'scheduled',NULL,'normal',?,?,NULL,NULL,NULL,NULL,NULL,NULL,0,0,0,0,0,'[]',NULL,NULL,NULL,NULL,?,?,1,?,?,?)",[input.taskId,input.organizationId,input.workspaceId,input.now,input.now,input.requestId,input.traceId,input.actorId,input.now,input.now]);await c.query("INSERT INTO collection_subqueries (id,task_id,organization_id,workspace_id,provider_id,ordinal,target_json,is_required,status,available_result_count,missing_fields_json,error_code,retryable,started_at,finished_at,version,created_at,updated_at) VALUES (?,?,?,?,?,1,?,1,'pending',0,'[]',NULL,0,NULL,NULL,1,?,?)",[input.subqueryId,input.taskId,input.organizationId,input.workspaceId,input.providerId,JSON.stringify(input.target),input.now,input.now]);await c.query("INSERT INTO provider_source_replay_runs (id,organization_id,workspace_id,provider_id,source_code,task_id,input_sha256,status,item_count,error_code,request_id,trace_id,created_by,created_at,updated_at) VALUES (?,?,?,?,?,?,?,'scheduled',0,NULL,?,?,?,?,?)",[input.runId,input.organizationId,input.workspaceId,input.providerId,provider.code,input.taskId,input.inputSha256,input.requestId,input.traceId,input.actorId,input.now,input.now]);await c.query("INSERT INTO collection_task_events (id,task_id,organization_id,workspace_id,event_type,from_status,to_status,actor_type,actor_id,request_id,trace_id,metadata_json,occurred_at) VALUES (?,?,?,?,? ,NULL,'scheduled','user',?,?,?,?,?)",[randomUUID(),input.taskId,input.organizationId,input.workspaceId,'collection.task.scheduled',input.actorId,input.requestId,input.traceId,JSON.stringify({source_code:provider.code,replay_run_id:input.runId}),input.now]);await c.query("INSERT INTO collection_task_outbox (id,task_id,organization_id,workspace_id,event_type,payload_json,status,attempt_count,available_at,lease_owner,lease_expires_at,request_id,trace_id,created_at,updated_at) VALUES (?,?,?,?,?,?,'queued',0,?,NULL,NULL,?,?,?,?)",[randomUUID(),input.taskId,input.organizationId,input.workspaceId,'collection.task.scheduled',JSON.stringify({task_id:input.taskId,source_code:provider.code}),input.now,input.requestId,input.traceId,input.now,input.now]);await c.query('INSERT INTO provider_source_operations (id,actor_id,route,idempotency_key,provider_id,replay_run_id,created_at) VALUES (?,?,?,?,?,?,?)',[randomUUID(),input.actorId,input.route,input.idempotencyKey,input.providerId,input.runId,input.now]);const result:ProviderSourceReplay={id:input.runId,task_id:input.taskId,provider_id:input.providerId,source_code:String(provider.code),status:'scheduled',item_count:0,error_code:null,request_id:input.requestId,trace_id:input.traceId,created_at:input.now.toISOString(),updated_at:input.now.toISOString()};await c.commit();return result;}catch(error){await c.rollback();if(error instanceof ProviderSourceServiceError)throw error;if((error as{code?:string}).code==='ER_DUP_ENTRY')throw new ProviderSourceServiceError('provider_source_replay_conflict',409,'使用相同 Idempotency-Key 可读取原结果；否则刷新后重试。');throw error;}finally{c.release();}}
  async refresh(input:Parameters<ProviderSourceRepository['refresh']>[0]){const c=await this.pool.getConnection();try{await c.beginTransaction();const[previous]=await c.query<RowDataPacket[]>('SELECT task_id,source_count FROM provider_refresh_operations WHERE actor_id=? AND route=? AND idempotency_key=? LIMIT 1',[input.actorId,input.route,input.idempotencyKey]);if(previous[0]){await c.commit();return{task_id:String(previous[0].task_id),source_count:Number(previous[0].source_count),status:'scheduled' as const};}const[scope]=await c.query<RowDataPacket[]>('SELECT w.id FROM workspaces w JOIN organizations o ON o.id=w.organization_id WHERE w.id=? AND w.organization_id=? AND w.status=\'active\' AND o.status=\'active\' FOR UPDATE',[input.workspaceId,input.organizationId]);if(!scope[0])throw new ProviderSourceServiceError('provider_source_scope_inactive',409,'重新选择活动组织与工作区。');const[providers]=await c.query<RowDataPacket[]>("SELECT id,code FROM providers WHERE status='enabled' AND parser_version IN ('google-news-fixed-rss-v1','syndication-feed-v1','structured-public-page-v1') ORDER BY updated_at DESC,code LIMIT 100 FOR UPDATE");if(!providers.length)throw new ProviderSourceServiceError('provider_source_automatic_empty',409,'平台管理员先同步并启用自动热点来源。');const taskId=randomUUID();await c.query("INSERT INTO collection_tasks (id,organization_id,workspace_id,status,coverage_status,priority,scheduled_at,available_at,attempt_count,successful_subquery_count,failed_subquery_count,blocked_subquery_count,available_result_count,missing_fields_json,request_id,trace_id,version,created_by,created_at,updated_at) VALUES (?,?,?,'scheduled',NULL,'high',?,?,0,0,0,0,0,'[]',?,?,1,?,?,?)",[taskId,input.organizationId,input.workspaceId,input.now,input.now,input.requestId,input.traceId,input.actorId,input.now,input.now]);for(let index=0;index<providers.length;index++){const provider=providers[index]!;await c.query("INSERT INTO collection_subqueries (id,task_id,organization_id,workspace_id,provider_id,ordinal,target_json,is_required,status,available_result_count,missing_fields_json,error_code,retryable,version,created_at,updated_at) VALUES (?,?,?,?,?,?,?,0,'pending',0,'[]',NULL,0,1,?,?)",[randomUUID(),taskId,input.organizationId,input.workspaceId,provider.id,index+1,'{}',input.now,input.now]);}await c.query("INSERT INTO collection_task_events (id,task_id,organization_id,workspace_id,event_type,from_status,to_status,actor_type,actor_id,request_id,trace_id,metadata_json,occurred_at) VALUES (?,?,?,?,?,NULL,'scheduled','user',?,?,?,?,?)",[randomUUID(),taskId,input.organizationId,input.workspaceId,'hotspot.refresh.scheduled',input.actorId,input.requestId,input.traceId,JSON.stringify({source_count:providers.length}),input.now]);await c.query("INSERT INTO collection_task_outbox (id,task_id,organization_id,workspace_id,event_type,payload_json,status,attempt_count,available_at,request_id,trace_id,created_at,updated_at) VALUES (?,?,?,?,?,?,'queued',0,?,?,?,?,?)",[randomUUID(),taskId,input.organizationId,input.workspaceId,'hotspot.refresh.scheduled',JSON.stringify({task_id:taskId,source_count:providers.length}),input.now,input.requestId,input.traceId,input.now,input.now]);await c.query('INSERT INTO provider_refresh_operations (id,actor_id,route,idempotency_key,task_id,source_count,created_at) VALUES (?,?,?,?,?,?,?)',[randomUUID(),input.actorId,input.route,input.idempotencyKey,taskId,providers.length,input.now]);await c.commit();return{task_id:taskId,source_count:providers.length,status:'scheduled' as const};}catch(error){await c.rollback();if(error instanceof ProviderSourceServiceError)throw error;if((error as{code?:string}).code==='ER_DUP_ENTRY')throw new ProviderSourceServiceError('provider_refresh_conflict',409,'刷新页面后查看已创建的热点采集任务。');throw error;}finally{c.release();}}
  async updateConfiguration(input:Parameters<ProviderSourceRepository['updateConfiguration']>[0]){const c=await this.pool.getConnection();try{await c.beginTransaction();const replayed=await this.operationProvider(c,input.actorId,input.route,input.idempotencyKey);if(replayed){await c.commit();return replayed;}const[rows]=await c.query<RowDataPacket[]>('SELECT * FROM providers WHERE id=? FOR UPDATE',[input.providerId]),current=rows[0];if(!current)throw new ProviderSourceServiceError('provider_not_found',404,'刷新来源目录后重试。');if(Number(current.version)!==input.expectedVersion)throw new ProviderSourceServiceError('provider_version_conflict',409,'来源已被其他管理员修改，刷新后重试。');const nextVersion=Number(current.version)+1;await c.query('UPDATE providers SET schedule_minutes=?,timeout_ms=?,retry_limit=?,status=?,version=?,updated_by=?,updated_at=? WHERE id=?',[input.scheduleMinutes,input.timeoutMs,input.retryLimit,input.status,nextVersion,input.actorId,input.now,input.providerId]);const snapshot={...current,schedule_minutes:input.scheduleMinutes,timeout_ms:input.timeoutMs,retry_limit:input.retryLimit,status:input.status,version:nextVersion,updated_by:input.actorId,updated_at:input.now};await c.query('INSERT INTO provider_versions (id,provider_id,version,snapshot_json,actor_id,action,request_id,trace_id,created_at) VALUES (?,?,?,?,?,?,?,?,?)',[randomUUID(),input.providerId,nextVersion,JSON.stringify(snapshot),input.actorId,'configuration_updated',input.requestId,input.traceId,input.now]);await c.query('INSERT INTO provider_source_operations (id,actor_id,route,idempotency_key,provider_id,replay_run_id,created_at) VALUES (?,?,?,?,?,NULL,?)',[randomUUID(),input.actorId,input.route,input.idempotencyKey,input.providerId,input.now]);await c.query("INSERT INTO platform_audit_events(id,organization_id,workspace_id,actor_id,action,resource_type,resource_id,outcome,request_id,trace_id,metadata,occurred_at,schema_version) VALUES(?,NULL,NULL,?,'provider.configuration.updated','provider',?,'succeeded',?,?,?,?,1)",[randomUUID(),input.actorId,input.providerId,input.requestId,input.traceId,JSON.stringify({reason:input.reason,from_version:input.expectedVersion,to_version:nextVersion,schedule_minutes:input.scheduleMinutes,timeout_ms:input.timeoutMs,retry_limit:input.retryLimit,status:input.status}),input.now]);await c.commit();return{id:String(current.id),code:String(current.code),status:input.status,version:nextVersion,schedule_minutes:input.scheduleMinutes,timeout_ms:input.timeoutMs,retry_limit:input.retryLimit,updated_at:input.now.toISOString()};}catch(error){await c.rollback();if(error instanceof ProviderSourceServiceError)throw error;if((error as{code?:string}).code==='ER_DUP_ENTRY')throw new ProviderSourceServiceError('provider_configuration_conflict',409,'刷新来源版本后重试。');throw error;}finally{c.release();}}
  private async operationProvider(c:PoolConnection,actorId:string,route:string,key:string){const[rows]=await c.query<RowDataPacket[]>('SELECT p.id,p.code,p.status,p.version,p.schedule_minutes,p.timeout_ms,p.retry_limit,p.updated_at FROM provider_source_operations o JOIN providers p ON p.id=o.provider_id WHERE o.actor_id=? AND o.route=? AND o.idempotency_key=? LIMIT 1',[actorId,route,key]);return rows[0]?provisioned(rows[0]):null;}
  private async operationReplay(c:PoolConnection,actorId:string,route:string,key:string){const[rows]=await c.query<RowDataPacket[]>('SELECT r.* FROM provider_source_operations o JOIN provider_source_replay_runs r ON r.id=o.replay_run_id WHERE o.actor_id=? AND o.route=? AND o.idempotency_key=? LIMIT 1',[actorId,route,key]);return rows[0]?replay(rows[0]):null;}
}
