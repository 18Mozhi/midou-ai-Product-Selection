import type{HomeDashboardItem,HomeDashboardSummary}from'@scoutops/contracts';
export interface HomeDashboardRepository{list(input:{organizationId:string;workspaceId:string;actorId:string;capabilities:string[]}):Promise<HomeDashboardItem[]>;}
const limits={action:5,change:6,follow:6,health:3}as const;
export class HomeDashboardService{constructor(private readonly repository:HomeDashboardRepository,private readonly now:()=>Date=()=>new Date()){}async get(input:{organizationId:string;workspaceId:string;actorId:string;capabilities:string[]}):Promise<HomeDashboardSummary>{const rows=await this.repository.list(input),groups={actions:[]as HomeDashboardItem[],changes:[]as HomeDashboardItem[],follows:[]as HomeDashboardItem[],health:[]as HomeDashboardItem[]};for(const item of rows){const key=item.kind==='action'?'actions':item.kind==='change'?'changes':item.kind==='follow'?'follows':'health';if(groups[key].length<limits[item.kind])groups[key].push(item);}return{...groups,scope:{organization_id:input.organizationId,workspace_id:input.workspaceId},generated_at:this.now().toISOString()};}}
