import type{FastifyInstance,FastifyRequest}from"fastify";
import type{LocalAuthService}from"@scoutops/auth";
import type{AuthorizationService}from"@scoutops/authorization";
import{sessionToken}from"./auth-routes.js";
import{ApiError,requireIdempotencyKey}from"./api-foundation.js";
import type{PlatformDashboardService}from"./platform-dashboard-service.js";
export interface PlatformDashboardRouteOptions{service:PlatformDashboardService;authorization:AuthorizationService;auth:LocalAuthService;secureCookie:boolean;webOrigin:string;}
export function registerPlatformDashboardRoutes(app:FastifyInstance,o:PlatformDashboardRouteOptions){const context=async(r:FastifyRequest)=>{const requestId=String(r.headers["x-request-id"]),traceId=String(r.headers["x-trace-id"]),a=await o.auth.authenticate(sessionToken(r,o.secureCookie));await o.authorization.authorize({actorId:a.user.id,capability:"platform:operate",surface:"api",requestId,traceId});return{actorId:a.user.id,requestId,traceId};};app.get("/api/v1/platform/dashboard",async(r:FastifyRequest,reply)=>{const c=await context(r);reply.header("cache-control","private, no-store");return{data:await o.service.read({...c,window:(r.query as any)?.window}),request_id:c.requestId,trace_id:c.traceId};});app.get("/api/v1/platform/management",async(r:FastifyRequest,reply)=>{const c=await context(r),query=r.query as any;reply.header("cache-control","private, no-store");return{data:await o.service.management({...c,domain:query?.domain,query:query?.query,status:query?.status}),request_id:c.requestId,trace_id:c.traceId};});app.patch("/api/v1/platform/management/content/:topicId",async(r:FastifyRequest)=>{if(r.headers.origin!==o.webOrigin)throw new ApiError(403,"origin_forbidden","请求来源不允许。","从 ai选品 页面重试。");const c=await context(r);return{data:await o.service.moderateTrend((r.params as any).topicId,r.body,{...c,idempotencyKey:requireIdempotencyKey(r)}),request_id:c.requestId,trace_id:c.traceId};});}
