import { randomUUID } from 'node:crypto';
import { relative, resolve, sep } from 'node:path';
import { rm } from 'node:fs/promises';
import type { Pool, PoolConnection, RowDataPacket } from 'mysql2/promise';
import type { OrganizationId, WorkspaceId } from '@scoutops/contracts';
import { buildScopedFilePath, writeScopedFile } from '@scoutops/storage';
import { evaluateQualityMetric, validateEvidenceInput, type EvidencePersistInput, type QualityMetricCode } from '@scoutops/data-quality';

export class EvidencePersistenceError extends Error{constructor(readonly code:string){super(code);this.name='EvidencePersistenceError';}}
export class MySqlEvidencePersistence{constructor(private readonly pool:Pool,private readonly evidenceRoot:string,private readonly maxRawBytes:number,private readonly now:()=>Date=()=>new Date()){}
  async persist(input:EvidencePersistInput){const value=validateEvidenceInput(input,this.maxRawBytes);const[existing]=await this.pool.query<RowDataPacket[]>('SELECT e.id evidence_id,e.content_sha256,n.id record_id FROM raw_evidence e JOIN normalized_records n ON n.raw_evidence_id=e.id AND n.status=\'active\' WHERE e.organization_id=? AND e.workspace_id=? AND e.provider_id=? AND e.dedupe_key=? LIMIT 1',[value.organizationId,value.workspaceId,value.providerId,value.dedupeKey]);if(existing[0]){if(String(existing[0].content_sha256)!==value.contentHash)throw new EvidencePersistenceError('evidence_dedupe_conflict');return{evidence_id:String(existing[0].evidence_id),normalized_record_id:String(existing[0].record_id),deduplicated:true};}const evidenceId=randomUUID(),fileAssetId=randomUUID(),recordId=randomUUID(),fileInput={organization_id:value.organizationId as OrganizationId,workspace_id:value.workspaceId as WorkspaceId,category:'evidence' as const,resource_id:evidenceId,filename:`${evidenceId}.bin`},target=buildScopedFilePath(this.evidenceRoot,fileInput);await writeScopedFile(this.evidenceRoot,fileInput,value.content);const c=await this.pool.getConnection(),now=this.now();try{await c.beginTransaction();const[scopeRows]=await c.query<RowDataPacket[]>('SELECT p.retention_days FROM collection_tasks t JOIN collection_subqueries q ON q.task_id=t.id JOIN providers p ON p.id=q.provider_id WHERE t.id=? AND q.id=? AND t.organization_id=? AND t.workspace_id=? AND q.organization_id=? AND q.workspace_id=? AND q.provider_id=? FOR UPDATE',[value.taskId,value.subqueryId,value.organizationId,value.workspaceId,value.organizationId,value.workspaceId,value.providerId]);const scope=scopeRows[0];if(!scope)throw new EvidencePersistenceError('evidence_scope_or_task_invalid');const retentionUntil=new Date(value.capturedAt.getTime()+Number(scope.retention_days)*86400000),relativePath=relative(resolve(this.evidenceRoot),target).split(sep).join('/');await c.query("INSERT INTO file_assets (id,organization_id,workspace_id,category,relative_path,content_sha256,size_bytes,status,created_by,created_at,updated_at) VALUES (?,?,?,'evidence',?,?,?,'active',?,?,?)",[fileAssetId,value.organizationId,value.workspaceId,relativePath,value.contentHash,value.content.byteLength,value.actorId,now,now]);await c.query("INSERT INTO raw_evidence (id,organization_id,workspace_id,collection_task_id,collection_subquery_id,provider_id,file_asset_id,source_url,canonical_url,dedupe_key,content_sha256,content_type,size_bytes,captured_at,parser_version,adapter_version,retention_until,status,request_id,trace_id,created_by,created_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,'active',?,?,?,?)",[evidenceId,value.organizationId,value.workspaceId,value.taskId,value.subqueryId,value.providerId,fileAssetId,value.sourceUrl,value.canonicalUrl,value.dedupeKey,value.contentHash,value.contentType,value.content.byteLength,value.capturedAt,value.parserVersion,value.adapterVersion,retentionUntil,value.requestId,value.traceId,value.actorId,now]);await c.query("INSERT INTO normalized_records (id,organization_id,workspace_id,provider_id,raw_evidence_id,record_key,schema_version,record_version,payload_json,supersedes_record_id,correction_reason,status,request_id,trace_id,created_by,created_at) VALUES (?,?,?,?,?,?,?,1,?,NULL,NULL,'active',?,?,?,?)",[recordId,value.organizationId,value.workspaceId,value.providerId,evidenceId,value.recordKey,value.recordSchemaVersion,JSON.stringify(value.normalizedPayload),value.requestId,value.traceId,value.actorId,now]);for(const item of value.provenance)await c.query('INSERT INTO field_provenance (id,organization_id,workspace_id,normalized_record_id,raw_evidence_id,field_path,source_path,transform_version,source_value_sha256,created_at) VALUES (?,?,?,?,?,?,?,?,?,?)',[randomUUID(),value.organizationId,value.workspaceId,recordId,evidenceId,item.fieldPath,item.sourcePath,item.transformVersion,item.sourceValueHash,now]);await this.event(c,value,'evidence.persisted','raw_evidence',evidenceId,{normalized_record_id:recordId,content_sha256:value.contentHash},now);await this.outbox(c,value,'evidence.persisted','raw_evidence',evidenceId,{evidence_id:evidenceId,normalized_record_id:recordId},now);await c.query('INSERT INTO evidence_data_operations (id,actor_id,route,idempotency_key,resource_id,result_json,created_at) VALUES (?,?,?,?,?,?,?)',[randomUUID(),value.actorId,'worker:evidence.persist',value.dedupeKey,evidenceId,JSON.stringify({evidence_id:evidenceId,normalized_record_id:recordId}),now]);await c.commit();return{evidence_id:evidenceId,normalized_record_id:recordId,deduplicated:false};}catch(error){await c.rollback();await rm(target,{force:true});if((error as{code?:string}).code==='ER_DUP_ENTRY'){const[rows]=await this.pool.query<RowDataPacket[]>('SELECT e.id evidence_id,e.content_sha256,n.id record_id FROM raw_evidence e JOIN normalized_records n ON n.raw_evidence_id=e.id AND n.status=\'active\' WHERE e.organization_id=? AND e.workspace_id=? AND e.provider_id=? AND e.dedupe_key=? LIMIT 1',[value.organizationId,value.workspaceId,value.providerId,value.dedupeKey]);if(rows[0]){if(String(rows[0].content_sha256)!==value.contentHash)throw new EvidencePersistenceError('evidence_dedupe_conflict');return{evidence_id:String(rows[0].evidence_id),normalized_record_id:String(rows[0].record_id),deduplicated:true};}}throw error;}finally{c.release();}}
  async reconcile(input:{organizationId:string;workspaceId:string;providerId:string;parserVersion:string;market:string;windowStartedAt:Date;windowEndedAt:Date;metrics:Array<{code:QualityMetricCode;numerator:number;denominator:number}>;actorId:string;requestId:string;traceId:string}){if(!/^[A-Za-z0-9._-]{1,40}$/.test(input.market)||input.windowEndedAt<=input.windowStartedAt||!input.metrics.length)throw new EvidencePersistenceError('reconciliation_input_invalid');const metrics=input.metrics.map(item=>evaluateQualityMetric(item.code,item.numerator,item.denominator)),status=metrics.some(item=>item.status==='failed')?'failed':metrics.some(item=>item.status==='insufficient_sample')?'insufficient_sample':'passed',runId=randomUUID(),now=this.now(),sampleCount=Math.max(...metrics.map(item=>item.denominator)),c=await this.pool.getConnection();try{await c.beginTransaction();await c.query('INSERT INTO reconciliation_runs (id,organization_id,workspace_id,provider_id,parser_version,market,window_started_at,window_ended_at,sample_count,metrics_json,status,request_id,trace_id,created_by,created_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)',[runId,input.organizationId,input.workspaceId,input.providerId,input.parserVersion,input.market,input.windowStartedAt,input.windowEndedAt,sampleCount,JSON.stringify(metrics),status,input.requestId,input.traceId,input.actorId,now]);for(const metric of metrics.filter(item=>item.status!=='passed')){const issueId=randomUUID();await c.query("INSERT INTO data_quality_issues (id,organization_id,workspace_id,provider_id,reconciliation_run_id,raw_evidence_id,normalized_record_id,metric_code,field_path,severity,status,actual_value,threshold_value,details_json,resolved_by,resolution_reason,resolved_at,request_id,trace_id,version,created_at,updated_at) VALUES (?,?,?,?,?,NULL,NULL,?,NULL,?,'open',?,?,?,NULL,NULL,NULL,?,?,1,?,?)",[issueId,input.organizationId,input.workspaceId,input.providerId,runId,metric.code,metric.status==='failed'?'critical':'warning',metric.value,metric.threshold,JSON.stringify({direction:metric.direction,numerator:metric.numerator,denominator:metric.denominator,minimum_sample:metric.minimumSample}),input.requestId,input.traceId,now,now]);await this.event(c,input,'data_quality.issue.opened','data_quality_issue',issueId,{metric_code:metric.code,status:metric.status},now);await this.outbox(c,input,'data_quality.issue.opened','data_quality_issue',issueId,{issue_id:issueId,metric_code:metric.code},now);}await this.event(c,input,'data_quality.reconciled','reconciliation_run',runId,{status,sample_count:sampleCount},now);await c.commit();return{id:runId,status,sample_count:sampleCount,metrics};}catch(error){await c.rollback();throw error;}finally{c.release();}}
  private event(c:PoolConnection,input:{organizationId:string;workspaceId:string;actorId:string;requestId:string;traceId:string},eventType:string,resourceType:string,resourceId:string,payload:unknown,now:Date){return c.query("INSERT INTO evidence_data_events (id,organization_id,workspace_id,event_type,resource_type,resource_id,actor_type,actor_id,request_id,trace_id,payload_json,occurred_at) VALUES (?,?,?,?,?,?,'worker',?,?,?,?,?)",[randomUUID(),input.organizationId,input.workspaceId,eventType,resourceType,resourceId,input.actorId,input.requestId,input.traceId,JSON.stringify(payload),now]);}
  private outbox(c:PoolConnection,input:{organizationId:string;workspaceId:string;requestId:string;traceId:string},eventType:string,resourceType:string,resourceId:string,payload:unknown,now:Date){return c.query("INSERT INTO evidence_data_outbox (id,organization_id,workspace_id,event_type,resource_type,resource_id,payload_json,status,attempt_count,available_at,request_id,trace_id,created_at,updated_at) VALUES (?,?,?,?,?,?,?,'queued',0,?,?,?,?,?)",[randomUUID(),input.organizationId,input.workspaceId,eventType,resourceType,resourceId,JSON.stringify(payload),now,input.requestId,input.traceId,now,now]);}
}
