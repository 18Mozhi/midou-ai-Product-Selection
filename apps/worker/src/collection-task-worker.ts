import{createHash,randomUUID}from'node:crypto';import type{Pool,PoolConnection,RowDataPacket}from'mysql2/promise';import type{OrganizationId,WorkspaceId}from'@scoutops/contracts';import{assertTaskTransition,classifyCollectionFailure,retryAvailableAt,summarizeCoverage,type CollectionErrorCode,type SubqueryOutcome}from'@scoutops/collection-tasks';import{ScopedRedisStore}from'@scoutops/redis';
const hash=(token:string)=>createHash('sha256').update('scoutops:collection-task-lease:v1\0').update(token).digest('hex');const token=()=>randomUUID()+randomUUID();
export interface ClaimedCollectionTask{id:string;organizationId:string;workspaceId:string;attemptCount:number;requestId:string;traceId:string;leaseToken:string;subqueries:Array<{id:string;providerId:string;ordinal:number;required:boolean;target:Record<string,unknown>}>;}
export class CollectionExecutionError extends Error{constructor(readonly code:CollectionErrorCode,readonly rateLimitResetAt?:Date){super(code);this.name='CollectionExecutionError';}}
export interface CollectionTaskExecutor{execute(task:ClaimedCollectionTask,heartbeat:()=>Promise<void>):Promise<Array<SubqueryOutcome&{id:string}>>;}
export interface CollectionQueueCoordinator{signal(task:{id:string;organizationId:string;workspaceId:string;requestId:string;traceId:string}):Promise<void>;acquire(task:ClaimedCollectionTask,ttlSeconds:number):Promise<boolean>;release(task:ClaimedCollectionTask):Promise<void>;}
export class ScopedRedisCollectionCoordinator implements CollectionQueueCoordinator{constructor(private readonly store:ScopedRedisStore){}private scope(task:{organizationId:string;workspaceId:string}){return{organization_id:task.organizationId as OrganizationId,workspace_id:task.workspaceId as WorkspaceId};}async signal(task:{id:string;organizationId:string;workspaceId:string;requestId:string;traceId:string}){await this.store.writeJson({...this.scope(task),purpose:'queue',resource:'collection-ready',identifiers:[task.id]},{task_id:task.id,request_id:task.requestId,trace_id:task.traceId});}acquire(task:ClaimedCollectionTask,ttlSeconds:number){return this.store.acquireLease({...this.scope(task),resource:'collection-task',identifiers:[task.id]},task.leaseToken,ttlSeconds);}async release(task:ClaimedCollectionTask){await this.store.releaseLease({...this.scope(task),resource:'collection-task',identifiers:[task.id]},task.leaseToken);}}
export class MySqlCollectionTaskWorkerRepository{constructor(private readonly pool:Pool,private readonly random:()=>number=Math.random,private readonly taskId?:string){}async recoverExpired(now:Date,actorId='collection-scheduler'){const c=await this.pool.getConnection();let recovered=0;try{await c.beginTransaction();const sql="SELECT * FROM collection_tasks WHERE status IN ('leased','running') AND lease_expires_at<=?"+(this.taskId?' AND id=?':'')+' FOR UPDATE',[rows]=await c.query<RowDataPacket[]>(sql,this.taskId?[now,this.taskId]:[now]);for(const row of rows){const classification=classifyCollectionFailure('timeout',Number(row.attempt_count)),next=classification.status==='retry_scheduled'?retryAvailableAt(Number(row.attempt_count),now,this.random()):now;await c.query('UPDATE collection_tasks SET status=?,available_at=?,lease_owner=NULL,lease_token_hash=NULL,lease_expires_at=NULL,last_error_code=?,finished_at=?,version=version+1,updated_at=? WHERE id=?',[classification.status,next,'timeout',classification.status==='retry_scheduled'?null:now,now,row.id]);await c.query('UPDATE collection_task_attempts SET status=?,error_code=?,finished_at=? WHERE task_id=? AND attempt_number=?',[classification.status,'timeout',now,row.id,row.attempt_count]);if(classification.status==='dead_letter')await this.dead(c,row,'timeout',now);await this.event(c,row,row.status,classification.status,actorId,'system',{error_code:'timeout',lease_recovered:true},now);await this.outbox(c,row,`collection.task.${classification.status}`,{task_id:row.id,error_code:'timeout'},now);recovered+=1;}await c.commit();return recovered;}catch(error){await c.rollback();throw error;}finally{c.release();}}
async queueReady(now:Date,actorId='collection-scheduler'){const c=await this.pool.getConnection();try{await c.beginTransaction();const sql="SELECT * FROM collection_tasks WHERE status IN ('scheduled','retry_scheduled','rate_limited') AND available_at<=?"+(this.taskId?' AND id=?':'')+" ORDER BY FIELD(priority,'critical','high','normal','low'),available_at,id LIMIT 1 FOR UPDATE",[rows]=await c.query<RowDataPacket[]>(sql,this.taskId?[now,this.taskId]:[now]);const row=rows[0];if(!row){await c.commit();return null;}assertTaskTransition(row.status,'queued');await c.query("UPDATE collection_tasks SET status='queued',version=version+1,updated_at=? WHERE id=?",[now,row.id]);await this.event(c,row,row.status,'queued',actorId,'system',{},now);await this.outbox(c,row,'collection.task.queued',{task_id:row.id},now);await c.commit();return{id:String(row.id),organizationId:String(row.organization_id),workspaceId:String(row.workspace_id),requestId:String(row.request_id),traceId:String(row.trace_id)};}catch(error){await c.rollback();throw error;}finally{c.release();}}
async claim(workerId:string,now:Date,leaseSeconds:number):Promise<ClaimedCollectionTask|null>{const c=await this.pool.getConnection(),leaseToken=token();try{await c.beginTransaction();const sql="SELECT * FROM collection_tasks WHERE status='queued'"+(this.taskId?' AND id=?':'')+" ORDER BY FIELD(priority,'critical','high','normal','low'),available_at,id LIMIT 1 FOR UPDATE",[rows]=await c.query<RowDataPacket[]>(sql,this.taskId?[this.taskId]:[]);const row=rows[0];if(!row){await c.commit();return null;}assertTaskTransition('queued','leased');const attempt=Number(row.attempt_count)+1,expires=new Date(now.getTime()+leaseSeconds*1000);if(attempt>4)throw new Error('collection_attempt_overflow');await c.query("UPDATE collection_tasks SET status='leased',leased_at=?,lease_owner=?,lease_token_hash=UNHEX(?),lease_expires_at=?,attempt_count=?,version=version+1,updated_at=? WHERE id=?",[now,workerId,hash(leaseToken),expires,attempt,now,row.id]);const attemptId=randomUUID();await c.query("INSERT INTO collection_task_attempts (id,task_id,attempt_number,worker_id,status,error_code,leased_at,lease_expires_at,started_at,finished_at,request_id,trace_id,created_at) VALUES (?,?,?,?, 'leased',NULL,?,?,NULL,NULL,?,?,?)",[attemptId,row.id,attempt,workerId,now,expires,row.request_id,row.trace_id,now]);await this.event(c,row,'queued','leased',workerId,'worker',{attempt_number:attempt},now);await this.outbox(c,row,'collection.task.leased',{task_id:row.id,attempt_number:attempt},now);const[subqueries]=await c.query<RowDataPacket[]>('SELECT id,provider_id,ordinal,is_required,target_json FROM collection_subqueries WHERE task_id=? ORDER BY ordinal',[row.id]);await c.commit();return{id:String(row.id),organizationId:String(row.organization_id),workspaceId:String(row.workspace_id),attemptCount:attempt,requestId:String(row.request_id),traceId:String(row.trace_id),leaseToken,subqueries:subqueries.map(item=>({id:String(item.id),providerId:String(item.provider_id),ordinal:Number(item.ordinal),required:Boolean(item.is_required),target:typeof item.target_json==='string'?JSON.parse(item.target_json):item.target_json}))};}catch(error){await c.rollback();throw error;}finally{c.release();}}
async start(task:ClaimedCollectionTask,now:Date){const[result]=await this.pool.query<any>("UPDATE collection_tasks SET status='running',started_at=COALESCE(started_at,?),version=version+1,updated_at=? WHERE id=? AND status='leased' AND lease_token_hash=UNHEX(?)",[now,now,task.id,hash(task.leaseToken)]);if(result.affectedRows!==1)throw new Error('collection_task_lease_invalid');await this.pool.query("UPDATE collection_task_attempts SET status='running',started_at=? WHERE task_id=? AND attempt_number=?",[now,task.id,task.attemptCount]);}
async releaseCoordinationConflict(task:ClaimedCollectionTask,now:Date){const c=await this.pool.getConnection();try{await c.beginTransaction();const[rows]=await c.query<RowDataPacket[]>("SELECT * FROM collection_tasks WHERE id=? AND status='leased' AND lease_token_hash=UNHEX(?) FOR UPDATE",[task.id,hash(task.leaseToken)]);const row=rows[0];if(!row)throw new Error('collection_task_lease_invalid');const classification=classifyCollectionFailure('timeout',task.attemptCount),available=classification.status==='retry_scheduled'?retryAvailableAt(task.attemptCount,now,this.random()):now;assertTaskTransition('leased',classification.status);await c.query('UPDATE collection_tasks SET status=?,available_at=?,last_error_code=?,lease_owner=NULL,lease_token_hash=NULL,lease_expires_at=NULL,finished_at=?,version=version+1,updated_at=? WHERE id=?',[classification.status,available,'timeout',classification.status==='dead_letter'?now:null,now,task.id]);await c.query('UPDATE collection_task_attempts SET status=?,error_code=?,finished_at=? WHERE task_id=? AND attempt_number=?',[classification.status,'timeout',now,task.id,task.attemptCount]);if(classification.status==='dead_letter')await this.dead(c,row,'timeout',now);await this.event(c,row,'leased',classification.status,row.lease_owner,'worker',{error_code:'timeout',coordination_conflict:true},now);await this.outbox(c,row,`collection.task.${classification.status}`,{task_id:task.id,error_code:'timeout',coordination_conflict:true},now);await c.commit();return classification;}catch(error){await c.rollback();throw error;}finally{c.release();}}
async heartbeat(task:ClaimedCollectionTask,now:Date,leaseSeconds:number){const[result]=await this.pool.query<any>("UPDATE collection_tasks SET lease_expires_at=?,updated_at=? WHERE id=? AND status IN ('leased','running','parsing','validating') AND lease_token_hash=UNHEX(?)",[new Date(now.getTime()+leaseSeconds*1000),now,task.id,hash(task.leaseToken)]);if(result.affectedRows!==1)throw new Error('collection_task_lease_invalid');}
async complete(task:ClaimedCollectionTask,outcomes:Array<SubqueryOutcome&{id:string}>,now:Date){if(outcomes.length!==task.subqueries.length||new Set(outcomes.map(item=>item.id)).size!==outcomes.length||outcomes.some(item=>!task.subqueries.some(query=>query.id===item.id)))throw new Error('collection_subquery_result_mismatch');const summary=summarizeCoverage(outcomes),c=await this.pool.getConnection();try{await c.beginTransaction();const row=await this.lock(c,task);for(const stage of['parsing','validating','persisted']as const){assertTaskTransition(row.status,stage);await c.query('UPDATE collection_tasks SET status=?,version=version+1,updated_at=? WHERE id=?',[stage,now,task.id]);await this.event(c,row,row.status,stage,row.lease_owner,'worker',{},now);row.status=stage;}for(const outcome of outcomes)await c.query('UPDATE collection_subqueries SET status=?,available_result_count=?,missing_fields_json=?,error_code=?,retryable=0,started_at=COALESCE(started_at,?),finished_at=?,version=version+1,updated_at=? WHERE id=? AND task_id=?',[outcome.status,outcome.availableResultCount,JSON.stringify(outcome.missingFields),outcome.errorCode,now,now,now,outcome.id,task.id]);assertTaskTransition('persisted',summary.terminalStatus);await c.query('UPDATE collection_tasks SET status=?,coverage_status=?,successful_subquery_count=?,failed_subquery_count=?,blocked_subquery_count=?,available_result_count=?,missing_fields_json=?,last_error_code=NULL,lease_owner=NULL,lease_token_hash=NULL,lease_expires_at=NULL,finished_at=?,version=version+1,updated_at=? WHERE id=?',[summary.terminalStatus,summary.coverageStatus,summary.successfulSubqueryCount,summary.failedSubqueryCount,summary.blockedSubqueryCount,summary.availableResultCount,JSON.stringify(summary.missingFields),now,now,task.id]);await c.query("UPDATE collection_task_attempts SET status='succeeded',finished_at=? WHERE task_id=? AND attempt_number=?",[now,task.id,task.attemptCount]);await this.event(c,row,'persisted',summary.terminalStatus,row.lease_owner,'worker',{coverage_status:summary.coverageStatus,available_result_count:summary.availableResultCount},now);await this.outbox(c,row,`collection.task.${summary.terminalStatus}`,{task_id:task.id,coverage_status:summary.coverageStatus},now);await c.commit();return summary;}catch(error){await c.rollback();throw error;}finally{c.release();}}
async fail(task:ClaimedCollectionTask,error:CollectionExecutionError,now:Date){const classification=classifyCollectionFailure(error.code,task.attemptCount),c=await this.pool.getConnection();try{await c.beginTransaction();const row=await this.lock(c,task);let available=now;if(classification.status==='retry_scheduled')available=retryAvailableAt(task.attemptCount,now,this.random());else if(classification.status==='rate_limited'){if(!error.rateLimitResetAt||error.rateLimitResetAt<=now)throw new Error('collection_rate_limit_reset_required');available=error.rateLimitResetAt;}assertTaskTransition(row.status,classification.status);await c.query('UPDATE collection_tasks SET status=?,available_at=?,rate_limit_reset_at=?,last_error_code=?,lease_owner=NULL,lease_token_hash=NULL,lease_expires_at=NULL,finished_at=?,version=version+1,updated_at=? WHERE id=?',[classification.status,available,classification.status==='rate_limited'?available:null,error.code,classification.status==='retry_scheduled'||classification.status==='rate_limited'?null:now,now,task.id]);await c.query('UPDATE collection_task_attempts SET status=?,error_code=?,finished_at=? WHERE task_id=? AND attempt_number=?',[classification.status,error.code,now,task.id,task.attemptCount]);if(classification.status==='dead_letter')await this.dead(c,row,error.code,now);await this.event(c,row,row.status,classification.status,row.lease_owner,'worker',{error_code:error.code,retryable:classification.retryable},now);await this.outbox(c,row,`collection.task.${classification.status}`,{task_id:task.id,error_code:error.code},now);await c.commit();return classification;}catch(caught){await c.rollback();throw caught;}finally{c.release();}}
private async lock(c:PoolConnection,task:ClaimedCollectionTask){const[rows]=await c.query<RowDataPacket[]>("SELECT * FROM collection_tasks WHERE id=? AND status='running' AND lease_token_hash=UNHEX(?) FOR UPDATE",[task.id,hash(task.leaseToken)]);if(!rows[0])throw new Error('collection_task_lease_invalid');return rows[0];}private event(c:PoolConnection,row:RowDataPacket,from:string|null,to:string,actorId:string,actorType:string,metadata:unknown,now:Date){return c.query('INSERT INTO collection_task_events (id,task_id,organization_id,workspace_id,event_type,from_status,to_status,actor_type,actor_id,request_id,trace_id,metadata_json,occurred_at) VALUES (?,?,?,?,?,?,?,?,?,?,?,?,?)',[randomUUID(),row.id,row.organization_id,row.workspace_id,`collection.task.${to}`,from,to,actorType,actorId,row.request_id,row.trace_id,JSON.stringify(metadata),now]);}private outbox(c:PoolConnection,row:RowDataPacket,eventType:string,payload:unknown,now:Date){return c.query("INSERT INTO collection_task_outbox (id,task_id,organization_id,workspace_id,event_type,payload_json,status,attempt_count,available_at,lease_owner,lease_expires_at,request_id,trace_id,created_at,updated_at) VALUES (?,?,?,?,?,?,'queued',0,?,NULL,NULL,?,?,?,?)",[randomUUID(),row.id,row.organization_id,row.workspace_id,eventType,JSON.stringify(payload),now,row.request_id,row.trace_id,now,now]);}private async dead(c:PoolConnection,row:RowDataPacket,errorCode:string,now:Date){const[attempts]=await c.query<RowDataPacket[]>('SELECT id FROM collection_task_attempts WHERE task_id=? AND attempt_number=?',[row.id,row.attempt_count]);await c.query("INSERT INTO collection_dead_letters (id,task_id,attempt_id,organization_id,workspace_id,error_code,failure_summary_json,status,replayed_by,replay_reason,replayed_at,request_id,trace_id,created_at) VALUES (?,?,?,?,?,?,?,'open',NULL,NULL,NULL,?,?,?) ON DUPLICATE KEY UPDATE error_code=VALUES(error_code),failure_summary_json=VALUES(failure_summary_json),request_id=VALUES(request_id),trace_id=VALUES(trace_id)",[randomUUID(),row.id,attempts[0]!.id,row.organization_id,row.workspace_id,errorCode,JSON.stringify({attempt_count:Number(row.attempt_count),error_code:errorCode}),row.request_id,row.trace_id,now]);}}
export async function processCollectionTaskOnce(input:{repository:MySqlCollectionTaskWorkerRepository;coordinator:CollectionQueueCoordinator;executor:CollectionTaskExecutor;workerId:string;leaseSeconds:number;now?:()=>Date}){const clock=input.now??(()=>new Date()),now=clock();const recovered=await input.repository.recoverExpired(now);const queued=await input.repository.queueReady(now);if(queued)await input.coordinator.signal(queued);const claimed=await input.repository.claim(input.workerId,now,input.leaseSeconds);if(!claimed)return{status:'idle' as const,recovered};if(!await input.coordinator.acquire(claimed,input.leaseSeconds)){const result=await input.repository.releaseCoordinationConflict(claimed,now);return{status:result.status,task_id:claimed.id,error_code:'timeout' as const,recovered};}try{await input.repository.start(claimed,now);try{const outcomes=await input.executor.execute(claimed,()=>input.repository.heartbeat(claimed,clock(),input.leaseSeconds));const summary=await input.repository.complete(claimed,outcomes,clock());return{status:summary.terminalStatus,task_id:claimed.id,coverage_status:summary.coverageStatus,recovered};}catch(error){const failure=error instanceof CollectionExecutionError?error:new CollectionExecutionError('network_error');const result=await input.repository.fail(claimed,failure,clock());return{status:result.status,task_id:claimed.id,error_code:failure.code,recovered};}}finally{await input.coordinator.release(claimed);}}
