import assert from "node:assert/strict";
import { createHash } from "node:crypto";
import { mkdir, readFile, writeFile } from "node:fs/promises";
import { createServer as reservePort } from "node:net";
import path from "node:path";
import { chromium } from "playwright";
import { createServer } from "vite";
import { resetPasswordPagePlugin, resetPasswordPageSources } from "./lib/reset-password-page-preview.mjs";

const args=process.argv.slice(2),smoke=process.env.P06_SMOKE==="1";
assert.ok(args.length===0||(args.length===2&&args[0]==="--capture-review"&&/^r[1-9]\d*$/.test(args[1])));
const widths=process.env.P06_VIEWPORT?[Number(process.env.P06_VIEWPORT)]:smoke?[390]:[1440,390],motions=process.env.P06_MOTION?[process.env.P06_MOTION]:smoke?["reduce"]:["reduce","no-preference"],output=args.length?path.resolve(`output/playwright/p06-page-composition-${args[1]}`):null;
if(output)await mkdir(output,{recursive:true});const previous=output?await readFile(path.join(output,"manifest.json"),"utf8").then(JSON.parse).catch(()=>null):null;
const probe=reservePort();await new Promise(r=>probe.listen(0,"127.0.0.1",r));const port=probe.address().port;await new Promise(r=>probe.close(r));
const server=await createServer({configFile:path.resolve("apps/web/vite.config.ts"),logLevel:"error",define:{"import.meta.env.VITE_API_BASE_URL":JSON.stringify("/api/v1")},plugins:[resetPasswordPagePlugin()],server:{host:"127.0.0.1",port,strictPort:true,proxy:{},hmr:false,open:false}});
const hash=v=>createHash("sha256").update(v).digest("hex"),images=[],results=[];let browser;
try{await server.listen();browser=await chromium.launch();const origin=`http://127.0.0.1:${port}`;console.log(`P06 actual Vue review ${origin}`);
for(const width of widths)for(const motion of motions){const context=await browser.newContext({viewport:{width,height:width===390?844:1000},locale:"zh-CN",reducedMotion:motion}),errors=[],unexpected=[],writes=[];let checks=0;const check=(a,e,l)=>{assert.deepEqual(a,e,`${width}/${motion}: ${l}`);checks++},capture=async(page,name)=>{await page.evaluate(()=>new Promise(r=>{scrollTo(0,0);requestAnimationFrame(()=>{scrollTo(0,0);r()})}));const bytes=await page.screenshot({animations:"disabled"}),file=`${width}-${name}.png`;await writeFile(path.join(output,file),bytes);images.push({file,sha256:hash(bytes),pixelWidth:bytes.readUInt32BE(16),pixelHeight:bytes.readUInt32BE(20)})};
try{context.on("page",p=>p.on("pageerror",e=>errors.push(e.message)));await context.route("**/*",async route=>{const req=route.request(),url=new URL(req.url());if(url.origin!==origin)return route.abort();if(!url.pathname.startsWith("/api/"))return route.continue();if(req.method()!=="POST"||url.pathname!=="/api/v1/auth/password-reset/confirm"){unexpected.push(`${req.method()} ${url.pathname}`);return route.abort()}const body=req.postDataJSON();writes.push(body);if(body.token==="p06-local-success")return route.fulfill({status:204});if(body.token==="p06-local-failure")return route.fulfill({status:503,json:{error:{code:"password_reset_unavailable",message:"重置服务暂不可用。",action_hint:"请稍后重新申请链接。"},request_id:"p06-failure",trace_id:"p06-failure"}});return route.fulfill({status:400,json:{error:{code:"password_reset_invalid",message:"链接无效或已过期。",action_hint:"请重新申请重置链接。"},request_id:"p06-invalid",trace_id:"p06-invalid"}})});
const empty=await context.newPage();await empty.goto(origin+"/reset-password",{waitUntil:"domcontentloaded"});const emptyRoot=empty.locator(".identity-page--review");await emptyRoot.waitFor();check(await empty.getByLabel("新密码").count(),1,"single new password field");check(await emptyRoot.locator(".p06-top small").getByText(/token 不展示/).count(),1,"token boundary visible");check(await empty.evaluate(()=>document.documentElement.scrollWidth<=innerWidth+1),true,"no-token no overflow");if(output&&motion==="reduce")await capture(empty,"no-token");await empty.getByLabel("新密码").fill("safe-password-123");const emptyButton=empty.getByRole("button",{name:"更新密码",exact:true});await emptyButton.focus();check(await emptyButton.evaluate(n=>getComputedStyle(n).outlineWidth),"3px","focus visible");await emptyButton.click();await emptyRoot.getByText("链接无效或已过期。",{exact:true}).waitFor();check(await emptyRoot.getAttribute("data-state"),"error","empty token rejected by server");
const expired=await context.newPage();await expired.goto(origin+"/reset-password?token=p06-local-expired&state=expired",{waitUntil:"domcontentloaded"});const expiredRoot=expired.locator(".identity-page--review");await expiredRoot.locator(".p06-warning").getByText("链接已过期",{exact:true}).waitFor();check(await expired.getByLabel("新密码").count(),1,"query expiry preserves real form");check(writes,[{token:"",new_password:"safe-password-123"}],"query expiry makes no post");if(output&&motion==="reduce")await capture(expired,"expired");
const success=await context.newPage();await success.goto(origin+"/reset-password?token=p06-local-success",{waitUntil:"domcontentloaded"});const successRoot=success.locator(".identity-page--review");await success.getByLabel("新密码").fill("safe-password-123");await success.getByRole("button",{name:"更新密码",exact:true}).click();await successRoot.getByText("密码已更新，请重新登录。",{exact:true}).waitFor();check(await successRoot.getByRole("button",{name:"返回登录",exact:true}).count(),1,"success requires explicit login return");if(output&&motion==="reduce")await capture(success,"success");
const failure=await context.newPage();await failure.goto(origin+"/reset-password?token=p06-local-failure",{waitUntil:"domcontentloaded"});const failureRoot=failure.locator(".identity-page--review");await failure.getByLabel("新密码").fill("safe-password-123");await failure.getByRole("button",{name:"更新密码",exact:true}).click();await failureRoot.getByText("重置服务暂不可用。",{exact:true}).waitFor();check(await failureRoot.getByText(/关联编号：p06-failure/).count(),1,"failure request id shown");if(output&&motion==="reduce")await capture(failure,"failure");check(writes,[{token:"",new_password:"safe-password-123"},{token:"p06-local-success",new_password:"safe-password-123"},{token:"p06-local-failure",new_password:"safe-password-123"}],"only expected local request bodies");check(unexpected,[],"no unexpected api");check(errors,[],"no page errors");results.push({width,motion,checks,writes:writes.length});console.log(JSON.stringify({width,motion,checks,writes:writes.length}))}finally{await context.close()}}
const sources=new Set(resetPasswordPageSources);for(const m of server.moduleGraph.idToModuleMap.values()){const f=m.file&&path.relative(process.cwd(),m.file).replaceAll("\\","/");if(f&&!f.startsWith("..")&&!f.includes("node_modules")&&/\.(vue|ts|css|json)$/.test(f))sources.add(f)}const allImages=[...(previous?.images??[]),...images].filter((x,i,a)=>a.findLastIndex(y=>y.file===x.file)===i).sort((a,b)=>a.file.localeCompare(b.file)),allResults=[...(previous?.results??[]),...results].filter((x,i,a)=>a.findLastIndex(y=>y.width===x.width&&y.motion===x.motion)===i).sort((a,b)=>a.width-b.width||a.motion.localeCompare(b.motion));if(output)await writeFile(path.join(output,"manifest.json"),JSON.stringify({page:"P06",revision:args[1],capturedAt:new Date().toISOString(),scope:"local actual Vue C review; synthetic token placeholders are intercepted and never rendered",sources:Object.fromEntries(await Promise.all([...sources].sort().map(async f=>[f,hash(await readFile(f))]))),images:allImages,results:allResults},null,2)+"\n");console.log(JSON.stringify({groups:allResults.length,checks:allResults.reduce((s,x)=>s+x.checks,0),writes:allResults.reduce((s,x)=>s+x.writes,0),images:allImages.length,sources:sources.size,port}))}finally{await browser?.close();await server.close()}
