import assert from "node:assert/strict";import path from "node:path";
export const emailVerificationReviewCss="design-plans/ui-phase-2-2026-09-07/implementation/email-verification-page-preview.css";export const emailVerificationPageSources=[emailVerificationReviewCss,"scripts/lib/email-verification-page-preview.mjs","apps/web/src/components/LocalIdentity.vue"];
export function previewEmailVerificationPage(input){const source=input.replaceAll("\r\n","\n"),start=source.indexOf("<template>"),end=source.lastIndexOf("</template>");assert.ok(start>0&&end>start,"P05 template bounds");const next=`<template><main class="identity-page identity-page--review" :data-mode="mode" :data-state="requestState"><header class="p05-top"><RouterLink to="/"><span>ScoutOps</span></RouterLink><b>邮箱验证</b><small>一次性链接 · token 不展示</small></header><section class="p05-hero"><p>EMAIL VERIFICATION</p><h1>{{ params.get('token') ? requestState === 'success' ? '邮箱验证完成' : ['error','rate_limited','blocked','expired'].includes(requestState) ? '验证未完成' : '正在验证邮箱' : '检查验证邮件' }}</h1><span>{{ params.get('token') ? '系统会自动核验链接；请不要重复提交或分享链接。' : '打开注册邮件中的一次性验证链接后，系统会自动完成核验。' }}</span></section><section class="p05-workspace" aria-live="polite"><div v-if="requestState === 'success' && message" class="p05-notice p05-success"><b>验证完成</b><span>{{ message }}</span></div><div v-else-if="['error','rate_limited','blocked'].includes(requestState)" class="p05-notice p05-error"><b>{{ requestState === 'rate_limited' ? '请求过于频繁' : requestState === 'blocked' ? '身份服务暂不可用' : '验证未完成' }}</b><span>{{ message }}</span><small v-if="actionHint">{{ actionHint }}</small><code v-if="requestId">关联编号：{{ requestId }}</code></div><div v-else class="p05-notice"><b>{{ params.get('token') ? '正在核验链接' : '尚未提供验证链接' }}</b><span>{{ params.get('token') ? '此操作由页面自动执行，无需额外按钮。' : '未找到链接材料时不会发送确认请求。' }}</span></div><section class="p05-next"><b>{{ requestState === 'success' ? '下一步：返回登录' : '需要帮助？' }}</b><p>{{ requestState === 'success' ? '邮箱验证不会自动登录；请返回登录后进入允许的工作区。' : '链接失效或请求失败时，请按实际提示重新申请；本页不提供虚构的重发操作。' }}</p><button class="p05-primary" type="button" @click="switchMode('login')">返回登录</button></section></section><footer>不显示 token · 不推断邮箱或账号状态 · 不自动进入业务</footer></main></template>`;return source.slice(0,start)+next+source.slice(end+"</template>".length)}
export function emailVerificationPagePlugin(){return {name:"p05-actual-vue-review",enforce:"pre",transform(s,id){if(id.replaceAll("\\","/")===path.resolve("apps/web/src/components/LocalIdentity.vue").replaceAll("\\","/"))return {code:previewEmailVerificationPage(s),map:null}},transformIndexHtml(h){return h.replace("<body>",'<body class="p05-review">').replace("</head>",`<link rel="stylesheet" href="/@fs/${path.resolve(emailVerificationReviewCss).replaceAll("\\","/")}"></head>`)}}}
