import assert from "node:assert/strict";
import path from "node:path";

export const taskReviewCss = "design-plans/ui-phase-2-2026-09-07/implementation/task-page-preview.css";
const replaceTemplate=(source,template,label)=>{const normalized=source.replaceAll("\r\n","\n"),start=normalized.indexOf("<template>"),end=normalized.lastIndexOf("</template>");assert.ok(start>0&&end>start,`${label} template bounds`);return normalized.slice(0,start)+template+normalized.slice(end+"</template>".length)};

export function previewTaskWorkspace(source){return replaceTemplate(source,`<template><section class="task-workspace p13-workspace"><section v-if="state==='loading'" class="p13-state" role="status"><b>正在读取我的工作队列</b><span>任务、汇总和成员目录会按当前工作区分别读取。</span></section><section v-else-if="['error','not_found','forbidden','expired','rate_limited'].includes(state)" class="p13-state" aria-live="assertive"><b>{{state==='expired'?'登录已失效':state==='forbidden'?'当前没有任务访问权限':state==='rate_limited'?'请求过于频繁':'任务服务暂不可用'}}</b><span>{{notice||'请重新读取当前工作区任务。'}}</span><code v-if="requestId">关联编号：{{requestId}}</code><button type="button" @click="load">重新加载</button></section><template v-else><header class="p13-hero"><div><p>PERSONAL QUEUE / 今日工作</p><h2>先决定下一项需要推进的工作</h2><span>这里是当前工作区中分配给你的全部任务，不等同于仅今天到期。</span></div><div class="p13-hero-count"><b>{{summary.todo+summary.in_progress+summary.paused}}</b><span>进行中或待处理</span></div></header><nav class="p13-actions" aria-label="今日工作主要操作"><button v-if="canCreate" class="p13-primary" type="button" @click="showCreate=true">新建任务</button><a href="#p13-queue">查看任务队列</a><button type="button" @click="resetFilters">重置筛选</button></nav><p v-if="notice" class="p13-notice" role="status">{{notice}}<code v-if="requestId">关联编号：{{requestId}}</code></p><section class="p13-focus" :data-tone="focusSummary.tone"><div><p>{{focusSummary.eyebrow}}</p><h3>{{focusSummary.title}}</h3><span>{{focusSummary.detail}}</span></div><dl><div><dt>待处理</dt><dd>{{summary.todo}}</dd></div><div><dt>进行中</dt><dd>{{summary.in_progress}}</dd></div><div><dt>已暂停</dt><dd>{{summary.paused}}</dd></div><div><dt>已逾期</dt><dd>{{summary.overdue}}</dd></div></dl></section><p v-if="!canUpdate" class="p13-readonly">当前角色仅可查看任务事实；选择、批量操作和删除入口已按权限隐藏。</p><TaskBatchActions v-if="canUpdate" :open="showBatchImpact" :action="batchAction" :targets="batchTargets" :eligible="batchEligible" :members="memberOptions" :reason="batchReason" :due-at="batchDueAt" :assignee-id="batchAssigneeId" :busy="busy" :can-assign="canAssign" @start="previewBatch" @close="showBatchImpact=false" @confirm="confirmBatch" @update:reason="batchReason=$event" @update:due-at="batchDueAt=$event" @update:assignee-id="batchAssigneeId=$event"/><section id="p13-queue" class="p13-queue"><header><div><p>我的任务队列</p><h3>按状态筛选，再打开需要处理的任务</h3></div><span>共 {{total}} 项</span></header><TaskListPanel :tasks="visible" :summary="summary" :selected-ids="selectedIds" :status="status" :query="query" :sort="sort" :can-create="canCreate" :can-update="canUpdate" :route-full-path="route.fullPath" :label="label" :phase="phase" :time="time" @update:selected-ids="selectedIds=$event" @status="setStatus" @apply-filters="applyFilters" @reset-filters="resetFilters" @create="showCreate=true" @remove="askRemove"/></section><nav v-if="total>pageSize" class="task-pagination" aria-label="任务分页"><button type="button" :disabled="page<=1" @click="setPage(page-1)">上一页</button><span>第 {{page}} / {{pageCount}} 页 · 共 {{total}} 项</span><button type="button" :disabled="page>=pageCount" @click="setPage(page+1)">下一页</button></nav><details class="p13-truth"><summary>查看数据说明</summary><div><span>状态汇总不随当前搜索或分页改变。</span><span>负责人目录不可用时，转交入口会保留但需稍后重试。</span></div></details></template><dialog ref="createDialogElement" class="p13-task-dialog" :aria-label="editing?'编辑任务':'新建任务'" @cancel="handleCreateCancel"><form @submit.prevent="create"><p>创建任务</p><h3>{{editing?'编辑任务':'把需要跟进的事写成任务'}}</h3><span>未指定负责人时分配给当前用户；期限为空时明确显示“未设置”。</span><label>标题<input v-model="form.title" required maxlength="200" /></label><label>说明<textarea v-model="form.description" maxlength="5000"></textarea></label><label>优先级<select v-model="form.priority"><option value="low">低</option><option value="normal">普通</option><option value="high">高</option><option value="critical">紧急</option></select></label><label>截止时间（可选）<input v-model="form.due_at" type="datetime-local" /></label><footer><button type="button" :disabled="busy" @click="closeTaskEditor">取消</button><button class="p13-primary" :disabled="busy">{{busy?'正在提交…':editing?'保存修改':'创建任务'}}</button></footer></form></dialog><dialog ref="deleteDialogElement" class="p13-task-dialog" aria-label="删除任务" @cancel="handleDeleteCancel"><form @submit.prevent="removeTask"><p>危险操作</p><h3>删除任务</h3><span>任务将不再显示，但既有审计记录会保留。</span><label>删除原因<textarea v-model="deleteReason" required maxlength="500" placeholder="请填写删除原因"></textarea></label><footer><button type="button" :disabled="busy" @click="closeDeleteDialog">取消</button><button class="p13-danger" type="submit" :disabled="busy">{{busy?'正在删除…':'确认删除'}}</button></footer></form></dialog></section></template>`,"P13 workspace")}

export function taskPagePlugin(){const workspace=path.resolve("apps/web/src/components/TaskWorkspace.vue").replaceAll("\\","/");return{name:"p13-actual-vue-review",enforce:"pre",transform(source,id){if(id.replaceAll("\\","/")===workspace)return{code:previewTaskWorkspace(source),map:null}},transformIndexHtml(html){return html.replace("<body>",'<body class="p13-review">').replace("</head>",`<link rel="stylesheet" href="/@fs/${path.resolve(taskReviewCss).replaceAll("\\","/")}"></head>`)}}}
