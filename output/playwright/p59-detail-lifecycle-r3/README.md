# P59 跨宽度焦点返回 r3 · 区域待审

[打开两张实际视口图](index.html) · [修复、验证与剩余门槛](../../../design-plans/ui-phase-2-2026-09-07/P59-DETAIL-FOCUS-BATCH21.md)

从390打开详情转1440、从760打开详情转761，Esc关闭后返回当前记录列表。蓝色轮廓是程序化后备焦点，不是列表选中或批量操作状态；下一次Tab继续进入可见列表控件。

图片来自实际App及本地合成数据，截图前滚动使完整外侧轮廓可见，不修改焦点/样式。仅确认轮廓区域，不代表整页、全部调用方、真机/读屏或真实权限验收。4组580项检查、167来源与2图原始指纹写入manifest，保留捕获HEAD d64a4fc4和工作区身份。

默认 `node scripts/verify-security-detail-preview.mjs --lifecycle` 不写文件。新图使用 `--capture-lifecycle r4` 等尚不存在的版本目录；不覆盖历史。r2是采图前失败留下的空目录，清理被工具策略拒绝，未提交。
