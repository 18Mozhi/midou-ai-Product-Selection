# P57 C 响应式边界与窄桌面阅读 r3

2026-09-15，基于e938f40d后的实际Vue审核宿主。4张视口PNG、192份加载来源指纹，840/841px×两种动效偏好，共4环境112项定向检查通过。窄桌面上下阅读布局已请求用户审核，待答复；旧390/1440首屏结构与手机筛选待审各自保持，不扩大审批。

## 图与修复

- [840px首屏](P57-840-responsive-first-viewport.png)
- [841px首屏](P57-841-responsive-first-viewport.png)
- [840px底部键盘焦点](P57-840-responsive-bottom-keyboard.png)
- [841px上下阅读组合](P57-841-responsive-bottom-keyboard.png)

841px全局侧栏出现后，原250px最小目录挤窄正文，实际阅读宽287px、状态两行。仅在P57审核CSS添加命名inline-size容器：视口至少761px且消息容器至多640px时，目录三列在上、阅读在下。未改变原760px阅读窗、840px导航规则、按钮事件、数据/权限或生产源码；宽屏和手机样式不据此重新设计。640px是本提案内部布局阈值，调整位置为implementation/platform-notification-shell-preview.css，调整后必须另版捕获，不覆盖旧包。

## 验证范围

原18项组合检查每环境保留，新增10项：840打开菜单→841返回可见品牌、841菜单持焦→840返回品牌、390/1440往返后恢复原宽度/URL/唯一菜单、阅读宽度至少300px与Range文本单行、编辑窗在两个方向切宽后仍保留输入与焦点、新建草稿/消息下一页/快照追踪三个键盘目标的中心及两角命中、滚回首屏后顶栏位置/可见性/透明度及品牌命中。三点取样不是全部像素无遮挡，也未覆盖所有按钮、缩放、触控/软键盘/读屏。

原390/1440 C组合72项与非提案App56项回归通过；2项新CLI/容器边界测试和3项实际SFC/业务源码保持测试通过。全部为原E2E数据与本地允许GET；编辑仅填充并关闭，没有保存/发布/取消写请求。真实账号、后端、权限、数据和生产验收未覆盖；全73页目标继续。

## 使用、来源与清理

`node scripts/verify-platform-notification-app.mjs --shell-preview --responsive`默认无文件；加`--capture-review`仅首次排他创建固定本r3目录，已存在就失败。--responsive单独使用、重复或未知标志在服务启动前拒绝。旧两种运行模式保持。

当前4图片和192来源哈希逐一复核；来源范围是模块与本地CSS导入，不是整个构建输入穷举。r1为狭窄阅读诊断，r2为首屏截屏稳定性诊断，均原样保留且不作为当前匹配/批准。旧composition-r2驱动与CSS指纹因本轮演进已不全匹配当前，原图与待审身份保留。

没有生产源码/API/OpenAPI/环境/依赖/权限/数据库或部署变化，不需重启；新变换沿用真实SFC编译与浏览器验证，没有新生产字节不重复Web构建。新增均为永久驱动、测试或审核图；无一次性临时文件，全部本轮服务/浏览器已关闭，旧受限清理项不动。
