import { routes } from "./catalog.generated.mjs";
import { routeSpec, dialogVariants, direction } from "./design-data.mjs";

const slug=(value)=>value==="/"?"root":value.replace(/^\//,"").replaceAll("/","__").replaceAll(":","").replace(/[()*]/g,"").replaceAll(".","-");
const shellLabel={member:"成员",organization_admin:"组织治理",platform_admin:"平台运营",account:"个人",null:"公开"};
const cards=routes.map((route,index)=>{const spec=routeSpec(route),name=`${String(index+1).padStart(2,"0")}-${slug(route.path)}.png`;return `<article class="route-card" data-search="${route.title} ${route.path} ${route.shell||"public"}" data-shell="${route.shell||"public"}"><header class="route-card-head"><span class="route-no">${String(index+1).padStart(2,"0")}</span><div><h3>${route.title}</h3><code>${route.path}</code></div><span class="shell-tag">${shellLabel[route.shell]||"公开"}</span></header><div class="thumbs"><a class="thumb" href="screens/desktop/${name}" target="_blank"><img src="screens/desktop/${name}" alt="${route.title}桌面设计稿"><span>1440</span></a><a class="thumb mobile" href="screens/mobile/${name}" target="_blank"><img src="screens/mobile/${name}" alt="${route.title}移动设计稿"><span>390</span></a></div><div class="route-focus">焦点：${spec.focal}<br/>布局：${spec.layout}</div></article>`}).join("");
const boardItems=[["design-system.png","视觉语言与令牌"],["button-system.png","按钮全部状态"],["state-system.png","界面状态系统"],...dialogVariants.map(([id,,title])=>[`dialog-${id}.png`,`弹窗 · ${title}`])];
document.querySelector("#app").innerHTML=`<header class="review-head"><div class="review-brand"><span class="review-seal">选</span>SCOUTOPS</div><span class="review-title">全新 UI / 布局 / 样式 / 交互审核台</span><span class="review-meta">${direction.version}</span></header><main class="review-wrap"><section class="hero"><div class="hero-no">V2</div><div><p class="review-meta">SIGNAL LEDGER / 信号账页</p><h1>旧视觉归零，<br/>从决定重新开始。</h1><p>这不是旧界面的换色或微调。新版以横向模块索引、编辑式数据账页、决策签章与行动卷轴重新组织 73 条真实路由，桌面与 390px 移动稿逐页对应。</p></div><aside class="hero-note"><b>本轮审核重点</b><br/>先审整体风格与布局结构，再审页面、按钮、弹窗和状态。审核通过前不修改生产 Vue 页面。</aside></section><section class="summary-strip"><div><span>真实路由</span><strong>${routes.length}</strong></div><div><span>桌面稿</span><strong>${routes.length}</strong></div><div><span>移动稿</span><strong>${routes.length}</strong></div><div><span>系统板</span><strong>${boardItems.length}</strong></div><div><span>旧版复用</span><strong>0%</strong></div></section><section class="review-controls"><input id="search" placeholder="搜索页面名或路由"><select id="shell"><option value="all">全部角色</option><option value="member">成员</option><option value="organization_admin">组织治理</option><option value="platform_admin">平台运营</option><option value="account">个人</option><option value="public">公开</option></select><select id="density"><option value="all">全部审核稿</option><option value="desktop">桌面优先</option><option value="mobile">移动优先</option></select><button id="reset">清除筛选</button></section><header class="section-title"><h2>逐页审核</h2><span id="count">${routes.length} / ${routes.length} ROUTES</span></header><section class="route-grid">${cards}</section><header class="section-title"><h2>组件、状态与弹窗</h2><span>${boardItems.length} BOARDS</span></header><section class="boards">${boardItems.map(([file,label])=>`<a class="board-link" href="boards/${file}" target="_blank"><img src="boards/${file}" alt="${label}"><strong>${label}</strong></a>`).join("")}</section><header class="section-title"><h2>可执行设计文档</h2><span>DOCS</span></header><nav class="doc-links"><a href="README.md">审核说明</a><a href="FULL-RECOMMENDATIONS.md">全套优化建议</a><a href="page-matrix.md">逐页矩阵</a><a href="button-inventory.md">按钮与弹窗清单</a></nav></main>`;
const search=document.querySelector("#search"),shell=document.querySelector("#shell"),count=document.querySelector("#count"),cardsEls=[...document.querySelectorAll(".route-card")];
function filter(){const q=search.value.trim().toLowerCase(),s=shell.value;let n=0;for(const card of cardsEls){const visible=(!q||card.dataset.search.toLowerCase().includes(q))&&(s==="all"||card.dataset.shell===s);card.classList.toggle("hidden",!visible);if(visible)n++;}count.textContent=`${n} / ${routes.length} ROUTES`;}
search.addEventListener("input",filter);shell.addEventListener("change",filter);document.querySelector("#reset").addEventListener("click",()=>{search.value="";shell.value="all";filter();});
