# P59 五类详情 C 整合 · 批次20

[当前 r2 图册](../../output/playwright/p59-detail-composition-r2/index.html)：15 张正式区域图。手机五种详情各有折叠与技术展开图，桌面五种详情保留原生表内展开。当前请求确认已展示的会话、令牌、审计手机区域与桌面审计技术区；其他图及完整页面不自动获得批准。

## 范围与依据

依据总纲 M06-04、Feature Map、实际 SecurityOperationsCenter 的五组 `#detail` 与桌面 `details`，及实际 ResponsiveDataView。采用 fixing-accessibility 检查初始焦点、Tab 循环、背景 inert、Esc/关闭按钮/遮罩关闭的返回；采用 frontend-design 落实蓝色标题、白色字段、浅灰技术信息、蓝色焦点。手机保留右侧抽屉与20px遮罩入口，桌面保持非模态原生展开，不强制两端使用相同弹窗。

基线实际计算色值显示：原抽屉底色 `rgb(248,245,233)`、焦点 `rgb(23,24,19)`，标题区透明，仍带旧风格。新审核 CSS 只作用于 P59 C 宿主的 Teleport 抽屉及桌面技术详情；不修改公共组件、业务 script、五类字段、权限、事件或 API。原生技术展开标记保留；抽屉标题吸顶，阅读底部技术内容时仍可关闭。

首轮桌面断言错误地统计了壳层的非模态导航 dialog；读取真实 DOM 确认后，将断言限定到 `.responsive-data-view__overlay [role="dialog"]`。这不是产品弹窗故障，不因此修改产品。

## 验证

- `node --test tests/unit/security-detail-preview.test.mjs tests/unit/platform-security-page-preview.test.mjs tests/unit/security-read-lifecycle.test.mjs`：19 项相关回归通过。新增3项覆盖 CSS-only 插件、作用域/焦点与非法参数边界；桌面样式调整后重跑新增3项通过。
- `node scripts/verify-security-detail-preview.mjs --baseline`：未注入详情 CSS 的实际 App，390/760/761/1440、reduce/no-preference 共8组592项，确认既有关闭与焦点合同。
- `node scripts/verify-security-detail-preview.mjs --capture-review r2`：同8组752项通过，其中手机129项/组、桌面59项/组。检查原字段顺序、技术展开/收起、初始焦点、正反Tab、三种关闭及焦点返回、背景恢复、末尾字段可达、横向溢出和样式。
- 每轮8组共32次本地 GET，详情交互不额外请求、无写请求、无页面错误或未知/外部请求。这里只证明模拟数据条件下的前端行为，不证明真实权限、审计或后台脱敏。
- r2 15图/166份来源原始字节 SHA-256，manifest 固定捕获 HEAD ebfce8a0 与工作区指纹；不把捕获 HEAD 当作干净源码证明。r1同15图为诊断前稿（692项），桌面字体/缩进由r2替代，保留原图与原 manifest，不回写历史指纹。
- 收口复核：r2全部15图与166来源匹配，r1全部原图匹配但CSS/驱动两项来源已演进；批次19原布局r2的17图/164来源仍匹配。新增4个代码/样式文件格式检查、73路由/153文档及运行说明一致性通过。6个本批验证端口均无监听，两个实际产品组件无工作区差异。

手机“technical”图是滚动到末尾字段后的真实视口，吸顶标题之下可能显示上方字段的剩余部分；不把一张图宣称为全部字段同时可见。桌面图是原生展开所在列表区域截图，并不代表导航/整页已审。

## 使用、清理与发布

默认 `node scripts/verify-security-detail-preview.mjs` 不写文件；`--baseline` 不加载详情样式且不允许采图；`--capture-review rN` 仅允许首次创建正整数版本目录，例如新版本 `r3`，拒绝覆盖已有包、未知参数或外部模式。均复用现有依赖，自动本地临时端口，不使用生产服务、外部数据或新环境变量。浏览器与Vite服务在 finally 关闭。

图册可直接打开，不需长期开发服务或重启。新代码、测试及两个正式版本图包是永久交付；没有创建验证专用临时文件或日志。此前其他批次清理受限的目录保持不动。

没有生产 import/API/OpenAPI/字段/权限/数据库/环境/依赖变更，无需更新运行配置或重启。生产构建输入未变，复用批次18的 Web 构建/类型与253资源预算证据；审核 CSS 通过本次实际 Vite 编译。后续采用审核稿进入产品、部署与真实验收另行推进，不宣称本批已部署。

## 剩余门槛

P59 整体布局和本批详情区域仍待审；详情打开时跨断点/离页/刷新、全主题与密度、长字段边界、多页分页、列设置、读屏、真机软键盘/触控和真实权限/审计仍未在本批覆盖。原73路由完整实施目标不缩小，局部视觉与前端检查不能代替生产签收。
