# P47 空态当前回放与历史图证衔接

2026-09-13；main / af239b08，开始时518项既有变更、暂存区为空。全73页目标不缩减；
本批只改审核验证工具和测试，不改运行中页面、CSS、API、权限或已有批准。

## 原因与证据边界

手机空态3项首测1过2失败：原逆向只认识51c0ba的手机实施版本，当前24a3b2还包含已验证
的分页焦点保护；原图证也固定51c0ba。核对179源后只有P47组件不同，不能改原哈希掩盖。
原96张图和32个精确比较保持，390两种空态仍与用户批准原图逐字节一致。

测试先通过已有 `beforeAdapterPaginationFocus` 完整逆向，再核对手机增量；未知分页修改
仍拒绝，当前源不变。图证固定完整manifest `38752bf4…`，仅P47组件按完整逆向关联，
其余178来源读当前原文件。原script/style、桌面文案、七项手机增量、五规则色值断言保留。

原C装配图证174来源中只有P47组件和palette发生后续变化；完整manifest `dd435c67…`
固定不改。使用已有pre-refresh历史解析，保留20PNG、原动作/六模型/条件/事实、样式、
焦点、请求和缓存返回全部原断言。明确这两包是捕获时证据，不称当前源码截图或全页批准。

## 当前真实 Vue 回放

新增 `scripts/verify-provider-adapter-empty-mobile-current.mjs`，不接受任何参数：

```powershell
node scripts/verify-provider-adapter-empty-mobile-current.mjs
```

`--capture`及未知参数在启动浏览器前拒绝，旧图永不被本入口覆盖。复用完整原手机驱动
7dce5403…及其原基础驱动db56f1e9…，保持实际交互、数据、断言、清理原文；只插入：
精确历史链检查、current-only模式、关闭capture、新工具来源与简洁结果输出。
历史baseline/review仍被计算以验证版本链，但不传给当前Vite插件；当前模式插件为空，
实际App/Router/NavigationShell/ProviderAdapterCenter/P46均使用原文件。

无障碍技能用于核对可访问名称、隐藏重复文案、44px动作与焦点；Playwright沿用项目驱动。
当前390/760/761/1440四宽度 × 空目录/搜索无结果/组合筛选无结果，**12组216检查通过**：

- 每页一次来源读取；清除全部筛选后回到真实样例结果，搜索框获得焦点，工具栏重置保留自身焦点。
- 手机版新文案和桌面原文各自唯一可见、隐藏文案不进入可访问名称；没有横向溢出。
- “登记来源”实际到达P46，只读取本地空目录，不登记真实来源。
- 28次本地GET，零请求体/写入/意外外网/页面运行错误；零新截图。
- 182个加载来源计数、当前组件SHA及5个驱动指纹见
  [机器结果](P47-EMPTY-CURRENT-REPLAY-RESULT.json)。182只是计数，不冒充完整输出的源指纹清单。

首轮工具启动失败：外层导入扫描误匹配内层正则；第二轮发现内层data URL不能解析裸包。
两次均在浏览器启动前退出。最终固定保留内层正则、在实际入口解析两层明确导入并使用封闭
映射；新增回归测试覆盖这两个错误、未知/重复锚点、驱动漂移和禁止capture。未改原驱动。

## 验证与交付门

最小10项通过；补充导入回归后，最终7个相关测试文件**47项全部通过**，包括分页15项、
历史解析5项、旧空态/刷新图证与当前工具断言。实际浏览器回放216检查独立通过。
本批没有生产或共享实现变更，完整风险验证为上述关联集合与真实当前App回放；未重复全库，
不把上轮1499项1352过147失败的记录重新计算成新总数，发布门保持未通过。

`verify:docs`（73路由/153必需文件）、`verify:runtime-docs`、本批格式、diff空白检查通过。

使用时直接运行新无参数命令。旧capture驱动只对应原图版本，不用于当前重拍；若以后修改
真实页面，应另建版本/补当前验证，不改历史清单、哈希或批准来制造一致。

API/OpenAPI、配置、环境变量、数据库、依赖、安全和权限均不涉及，不需要生产重启。
没有提交或部署；commit hash不适用（全库门尚未通过，工作树包含既有混合变更）。
本轮未创建临时脚本/截图/日志文件，永久新增驱动、测试与结果记录为交付物。
Vite/Chromium已退出，56695无监听；两次启动失败未创建服务。旧两处清理限制仍保持：

- `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p47-c-e2e-temp`
- `C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`

P47已批准空态/原C区域不扩大；全局新壳、P16横向组合等仍待具体审核，全部73页和生产验收继续。
