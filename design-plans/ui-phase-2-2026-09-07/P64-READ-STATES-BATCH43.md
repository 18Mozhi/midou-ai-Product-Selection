# P64 读取状态与可访问区域 · 批次43

起点main/6025c198、554项在途变化、索引为空；保留原工作树。批次42有具体Vue组合，本批补加载/读取失败/权限清除与恢复，不默认批准既有待审区域、P62离页行为或API覆盖测试修复。

## 产品改动

BackupRecoveryCenter的首次加载/首次失败、保留快照的刷新提示增加aria-labelledby与aria-busy，原b标题改h3并局部设18px/零外边距；前两种状态由原v-if/v-else-if互斥渲染，复用同一标题ID而不同时存在。审核转换器对应提升为h2，配合审核h1保持层级。依照fixing-accessibility技能提供原生命名区域和标题，不将根live或aria属性当作完整读屏验收。

业务script、文案、15秒超时、客户端[0,150,400]安全重试、状态判定、清除/保留条件、刷新禁用、原请求和共享组件均不改；没有新增页面状态或判断“已恢复”的算法。

## 验证链

新语义合同先失败后通过，7项审核合同加原M07-04的9项共16通过。实际Vue读取驱动 `node scripts/verify-backup-read-states.mjs` 使用四组1440/390与减少/正常动效；原生15秒等待分别覆盖首次和保留快照超时，不改时钟或回调。

每组21个本地GET，共84个：首次超时1；首次503/429各3次安全尝试，403/401各1；恢复blocked1；保留快照超时1；保留503/429各3；403清除1；恢复blocked1；401清除1；恢复empty1。无POST、真实下载、外部请求或浏览器异常。模拟权限响应不是服务真实鉴权或角色矩阵验收。

先完成200项语义/状态/请求检查，再加入每组14个区域的可见控件命中检查，最终256项通过。证明首次无快照、缺失资产不造数据；保留普通失败/超时不替换结论，401/403移除结论和资产；原登录链接仍为/login（不跟随）；成功empty不是error。forbidden只在错误区域不显示重试，顶部刷新仍按原实现保留，不能声称全页没有读取入口。

第一轮图中手机固定导航挡住部分下方内容。新增滚动居中及elementFromPoint核验后发现页面已有足够滚动空间，不需要产品布局加留白；只修正取景，未隐藏底部导航或缩短页面。初版命中筛选仅看getClientRects，将原生details折叠内的复制按钮也纳入，造成四组保留超时误报；改为浏览器checkVisibility后仍逐一核对可见按钮/链接/summary的中心命中，不放宽真实遮挡要求。

原默认/详情/Tab/返焦组合97项回归通过，前端类型检查/构建和253项资源预算通过；构建来自混合工作树，不是干净发布包。AES-GCM自测为既有测试内部临时合成文件，自动finally清理，不是真实恢复。

## 图与边界

`output/playwright/p64-read-states-r2`为当前28图：每端首次加载/超时/503/429/403/401、blocked恢复、保留读取中顶部按钮、保留超时/503/429、清除后的403/401及empty恢复。index.html与manifest.json绑定165来源和28PNG；截图只在减少动效两组保存，正常动效有检查但不额外出图。

r1/28图保留为有遮挡的捕获对照，不作为当前待审区域。批次42组合r3仍是其捕获时的31图证据，不覆写旧manifest；本批组件/审核转换器变化使其部分来源与当前不同，原97项已重新运行。

本批只覆盖上述读取状态。尚未完成真实权限、完整读屏、焦点连续性、离页/保活/迟到响应、请求编号归属/首次失败技术编号、复制消费者、全部列/主题/密度，以及stale/verified完整实际Vue状态及真实宝塔恢复演练。局部通过不扩大为整页或全站完成。

## 使用与交付

沿用原刷新/重新核验入口，无新调节项。验证命令默认无文件，`--capture-review rN`只新建独占版本目录。API/OpenAPI/权限/审计/数据库/后端/Worker/Python/插件/.env/依赖/配置未变，生产者/消费者接口联动不适用。未来只需发布前端静态包，无Node/Python重启，本轮未部署。

浏览器/Vite全部finally关闭，无新增散落临时脚本/日志/下载；加密自测临时目录自动清理并核查无残留。正式脚本、测试、两版图册是永久交付。既有清理受阻目录不触碰。

原API覆盖计数失败输入未变、用户处理决定仍待答；未重复失败测试，未擅自修改，未暂存/未提交，commit hash不适用。P62离页选择仍独立待确认；完整73页目标保留。
