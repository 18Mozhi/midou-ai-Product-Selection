# P36 轮换与撤销原因窗 · 实际 Vue 的 C 审核组合

从干净 `main/136fec21` 继续。已有手机筛选批准不扩大到原因窗；创建、明文、列表此前问题未答，不默认通过。

[80 张四宽度图册](../../output/playwright/p36-reason-vue-preview/index.html) · [手机撤销组合](../../output/playwright/p36-reason-vue-preview/390-revoke-confirm-focus.png) · [手机轮换初始](../../output/playwright/p36-reason-vue-preview/390-rotate-default.png) · [证据](../../output/playwright/p36-reason-vue-preview/evidence.json)

## 设计和审图边界

使用 `frontend-design` 延续已选择的 C 方向：蓝色目标标题、白色影响说明和原因输入，浅底独立操作区；手机取消与确认等宽，桌面右对齐。普通控件16px、标题22px、帮助14px，按钮至少44px。原因/底部按钮用蓝色键盘焦点，蓝标题上的关闭采用白色焦点；撤销的启用确认按钮红色，禁用统一灰色。

本次仅请求审核 **390px 撤销原因窗、已填有效原因且确认获得键盘焦点的组合**。不是轮换窗、所有状态、字段上限、真实权限、实际撤销或生产批准。

## 原行为保持

- 原 `OrganizationAdminCenter`、`OrganizationTokenPanel`、`AuditedReasonDialog` 和两项组合函数完整加载，模板/脚本不转换。仅审核宿主导入 `implementation/token-reason-preview.css`，所有规则限定 `.p36-reason-preview #app`；不会进入生产或其他页面。
- 标题/说明/按钮文字来自原组件，包括“确认提交”、旧令牌失效和不可恢复提示。根据原标题的撤销前缀仅设置审核 CSS 颜色，不推导业务动作、不新增 props。
- 原原因初始为空，trim后至少2字符；没有前端 maxlength，不将旧提案500字符上限擅自落地。501字符只证明输入保留，不表示后端接受。
- 原 `useAuditedReason.finish` 在确认时立即清请求/关闭原因窗，父页面才继续 POST；本批不创建弹窗“提交中/失败”假状态，也不触发 POST。提交、幂等、版本冲突、失败恢复、审计和持久化仍需另验。
- 短屏保留原生滚动，不使用覆盖输入区的悬浮底栏。长目标标题可换行，分别滚到头部和底部截图，不宣称二者同时可见。

## 验证

390 / 760 / 761 / 1440 四宽度，常规高1000、短屏高520，**408项检查、80 PNG、38项源指纹**。每动作十种图：默认、无效输入、确认/取消/关闭焦点、确认悬停/按下、长原因、短屏长目标头部/底部。

原8条E2E测试记录、固定审核时钟2026-08-26；补充长目标为合成克隆，不代表真实名称上限。每宽度仅4个拦截 GET，零写入/外部请求/浏览器错误/storage，系统剪贴板调用禁止。

核对原目标、打开后自动输入焦点、禁用灰色、无效trim与有效启用、44px热区、两端Tab循环、三入口关闭及焦点归还、重开清空、真实按下、蓝/白焦点色、短屏底部按钮在弹窗内完整可达。每图检查横向边界和三点可见遮挡；窄屏鼠标悬停不等于手机触摸。未验证真实键盘弹起、200%缩放、全App/主题密度或操作系统剪贴板。

最初回放发现旧 `#app` 规则压过审核减少动态设置，产生启用时灰色过渡帧。将所有审核规则在宿主内明确到 `#app` 后修正，未改全局样式；最终80图完整重拍。短屏底部截图明确滚到底并检查按钮不被弹窗边界裁切。

## 使用及收尾

```powershell
node scripts/verify-ui-phase2-token-reason-preview.mjs
node --test tests/unit/ui-phase2-token-reason-preview.test.mjs
# 需要更新本批交付图时添加 --capture；普通复测不改图。
```

生产 Vue/CSS、后端、OpenAPI、数据库、环境、依赖、权限均未改；无配置调节或重启要求，未部署。80PNG、index/evidence与永久验证器是本次交付物，非待删除临时文件。中间同名截图已被成功回放覆盖；Vite及浏览器 finally 关闭，审核使用本机5173端口，不留服务器。

已有待答局部、原因提交生命周期、全73页实际C和真实权限/宝塔交付继续，不因本批测试通过称全项目完成。

最终门禁：3项新增定向测试及690项相关回归通过；73路由/153必需文档、格式与diff空白检查通过，原102图包/15069PNG审计无漂移。新80图独立登记，不增加批准页面计数。5173监听数为0，无新增验证临时文件残留；生产代码/配置/依赖未变，不重复此前有效构建。
