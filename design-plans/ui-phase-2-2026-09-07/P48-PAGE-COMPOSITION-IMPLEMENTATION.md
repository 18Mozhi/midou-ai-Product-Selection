# P48 来源频道主目录实施记录

2026-09-23，将经审核通过的来源频道主页面 C 方向迁移到生产 Vue：桌面保持蓝色来源身份与连续事实指标、白色筛选区和按用途分组的目录台账；手机始终显示搜索，次级筛选可折叠，来源事实与原操作在页内详情中单列呈现。目录和筛选拆为 `ProviderSourceDirectory`、`ProviderSourceFilters`，API、URL 查询、分页、排序、筛选、匿名测试、配置、版本、样本、兼容性及 1688 验收事件继续由 `ProviderSourceCenter` 编排。手机详情进入后将焦点移至详情返回控件，返回后恢复原来源入口焦点。

本批保留现有接口、字段、请求/响应、权限、业务和持久化合同；不新增独立来源登记或刷新写入动作。状态反馈区分首次读取和保留旧目录的手动刷新，内部写入后的目录重读不冒充手动刷新反馈。

验证：`npm run typecheck:web`；`node scripts/run-playwright-projects.mjs`，桌面 Chromium 12 passed / 1 skipped、390px mobile 13 passed；覆盖主目录、筛选、页内详情与焦点归还，以及配置、样本、兼容矩阵、版本和匿名测试的既有流程。API 写入由 E2E 本地拦截；不代表生产权限、真实配置写入、烟测、固定样本审批、MySQL 或生命周期验收。最终发布前按发布门禁重新运行。

范围边界：本提交只完成来源频道主目录、筛选和页内详情；配置/版本、固定样本、兼容矩阵四个对话框及其完整交互视觉仍保留现有实现，后续独立完成。SC48、PR-G01 和全 73 页计划继续开放。
