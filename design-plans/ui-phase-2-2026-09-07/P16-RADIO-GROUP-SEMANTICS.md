# P16 单选组语义补齐

日期：2026-09-14。局部实现已落地；关联历史图证的当前来源门尚未闭合，未提交、未部署，不代表整页或全73页完成。

## 接续：114张控件图的历史校验与当前重放

无参数 `node scripts/verify-ui-phase2-journey-vue.mjs` 已通过当前双端170项检查、90个控件状态实例，零新截图。10类控件保留原生焦点、悬停、按下及适用的禁用/处理中检查；创建与保存失败、草稿保留、五门限制、重试请求与原请求体断言未修改。本地拦截包括模拟失败的POST，不是“零HTTP写请求”，但没有真实业务写入。

运行前独立校验旧manifest完整SHA256（4cb80b9e75aee2d4cb355ca8897a2e0960e66c82b398f720ef10cf1b4ac532ac）、20份登记来源及114张原PNG。SelectionJourney、NavigationShell、主控件验证器绑定af239b08b69f7d97cd0372f9840a70009eeef0c7原文；signal-ledger.css绑定0c5c48ee4959d24238af106ae6d2610a48aa9b9c；onboarding-navigation.css绑定6e7dfd93d245e06d5a549584ec14e34f425c8a5f，其余15来源仍检查当前完整哈希。Git原文不进入浏览器，当前App仍读取工作树；旧图没有重拍或更新批准。当前运行比较状态覆盖并执行原断言，不宣称与旧图逐像素一致或覆盖全部动态依赖。

新增3项永久历史校验测试，与字段历史/模式、成员组合、单选语义、写入生命周期、颜色和P34查询共34项通过。字段历史包因主控件验证器本轮变化，增加该文件的af239来源绑定，现为6份Git原文与17份当前来源；23来源/24图固定检查仍通过。下节5+18属于上一轮记录。

本轮只改验证器、历史校验及测试/说明，不改产品Vue/CSS、API、权限、配置、依赖或生产，无需重启。没有新临时截图/日志/脚本；全部页面验收、其他图包来源门与跨页提交范围仍待，不将本包通过外推。commit hash不适用，未部署。

## 接续：24张字段图的历史校验入口恢复

当前无参数入口已经通过：

```text
node scripts/verify-ui-phase2-journey-fields.mjs
```

先由独立历史校验模块检查固定manifest、23份登记来源和24张原PNG；再启动当前App运行双端三主题ID两密度的96项字段观察，最后仍以deepEqual逐项比较原checks。此次96条当前结果与原记录完全一致，零业务写入、零新图。输出screenshots=24指读取校验的原图数量，不是本次生成24张图，也不代表当前像素截图与旧图逐像素相同。

旧字段包有5份源码与当前不同，完整原文分别从以下Git提交读取；其余18份继续核对当前文件完整哈希。

| 历史源码                                                                       | 精确提交                                 |
| ------------------------------------------------------------------------------ | ---------------------------------------- |
| SelectionJourney.vue、NavigationShell.vue、verify-ui-phase2-journey-fields.mjs | af239b08b69f7d97cd0372f9840a70009eeef0c7 |
| signal-ledger.css                                                              | 0c5c48ee4959d24238af106ae6d2610a48aa9b9c |
| styles/onboarding-navigation.css                                               | 6e7dfd93d245e06d5a549584ec14e34f425c8a5f |

manifest SHA256固定为6b6cf0cdb58a7cc36c9d0553998a7bde4b1f1d10cfba1b3517121d63bedfc74e；任何来源、图片、检查或边界元数据改动均拒绝。新增永久测试先确认原JSON按原格式往返字节相同，再改各类内容验证拒绝，避免只因测试重新序列化格式不同而出现假阴性。历史校验模块不向浏览器提供Git原文；当前sourceHashes仍直接读取工作树，新增模块也纳入当前登记输入。

本次新增3项历史测试，与模式、组合、语义、生命周期、颜色及P34查询共31项通过；强化负例后该3项复测通过。旧字段目录26文件整体指纹仍为9ec314432a7c726ae917b00e8b7a4340845d675d93198490de7e2e9e199f1ee3。前述--current仍仅运行当前矩阵、不执行历史校验；下文此前“无参数直接比较全部当前来源”的描述只保留为修改前记录，本节取代该入口说明。

本轮只变更字段验证及其测试/说明，不改产品Vue/CSS、API、权限、依赖、配置或生产。无需生产重启，5175服务和浏览器已关闭，无新增临时截图/脚本/日志，原图与用户批准不变。只关闭24张字段包的来源/观察对照；114张原控件包等其它来源门尚未因此关闭，跨页提交范围仍待确认，commit hash 不适用，未部署。

## 接续：成员组合r3历史来源与当前重放

本次关闭的是 shell-journey-vue-c-r3 这一包的来源失配，不是所有P16/P34旧图包或全库门禁。下文上一轮的“共享组合4项3过1失败”保留为当时结果。

逐项核对旧包166份登记来源，仅3份与当前不同：OrganizationAdminCenter.vue、OrganizationApprovalPanel.vue、SelectionJourney.vue。三份原文都能从完整提交 af239b08b69f7d97cd0372f9840a70009eeef0c7 读取，且各自完整SHA256与旧manifest一致。因此只在 tests/unit/ui-phase2-shell-journey-vue.test.mjs 的历史图证测试中，将这3条路径绑定该Git快照；其余163份继续与当前文件逐字节哈希比较，不引入通用“哈希不同就跳过”逻辑。

旧manifest本身新增固定SHA256检查：57ce771050ae29a19f19d6563095995489b6774168c8b75fbf1bf4c560ddf168。原48张PNG的哈希/尺寸、6组242项历史检查记录、请求与五门断言全部保留。历史测试不把Git源码送入浏览器或生产；缺失Git对象或任何预期不符都会失败。

当前实现另运行现有、未修改的验证器：

```text
node scripts/verify-ui-phase2-shell-journey-vue-c.mjs
```

无参数本来就执行当前源码全组合且不写图，无需增加模式。实际 baseline 为390/1440，review 为390/840/841/1440，六组视口高度均1000。真实App/Router/NavigationShell/SelectionJourney、本地拦截读取下242项通过，包括五门逐一缺失时禁止采纳、菜单关闭回焦、保留当前决策草稿、底部操作可达、无意外网络/运行错误及零业务写入。C壳和横向阶段仍是审核预览，不是生产默认布局；无参数未执行仅在截图模式中的局部裁剪遮挡检查，不用本次零截图运行冒充新视觉对照。

本包4项单测由3过1失败变为4项通过；与P16字段模式、模板语义、生命周期、颜色及P34当前查询共28项通过。此次当前重放输出166份登记来源，但不把“数量相同”当成全应用动态依赖完全相同的证明。原50文件（48PNG、manifest、索引）前后聚合字节指纹均为6af82ea0d53294afdae1d2129911675460b8c790c8a4964e95da6eb52b76e508。

只修订测试的历史来源归属及交付说明，不改产品、验证器、图、批准、接口、权限或配置。浏览器及49646/49678两端口已关闭，无新临时文件；没有部署或重启生产。P16单独字段/其它历史包的来源策略、全站门和原跨页检查点确认仍待处理；本测试及其既有依赖尚未提交，不擅自合并跨页文件，commit hash 不适用。

## 接续：当前源码字段矩阵通过

当前字段验证已从上一轮的手机冒烟扩展为完整双端矩阵。仍未关闭共享组合的历史来源门，也不将原图包改称当前版本证据。

在仓库根目录运行：

```text
node scripts/verify-ui-phase2-journey-fields.mjs --current
```

此模式运行实际 App/SelectionJourney 的现有字段流程，1440/390 × deep-ocean/aurora-purple/cloud-white × standard/compact，共12组；三主题指兼容偏好ID环境，并非新增三套C配色。读取由本地夹具拦截，任何业务写入或非预期网络请求使验证失败。

- 96项原字段观察通过，保留字段边界/文字/占位对比、44px触区、字号、原生输入校验、单选键盘切换、焦点、密度和采纳禁用检查。
- 新增36项单选组检查通过：每组环境验证输入类型、候选结果、决策方式各唯一命名组，原生radio数量/name正确且未添加tabindex。用实际浏览器角色定位验证，不只依赖SSR；不代替真实屏幕阅读器播报验收。
- 15项相关单元测试通过，包括新增参数模式测试3项。未知参数、重复参数、组合参数在启动服务前拒绝。
- 控制台输出 mode=current、screenshots=0、businessWrites=0、historicalPacketValidated=false；本次登记输入聚合指纹为 ba52d0f05c467369866232a4520ef758a4caf28f94f36f55626a340c7779ef7c。输入清单沿用原P16包的登记范围并增加字段模式模块，不声称覆盖完整应用的全部动态依赖。
- 旧字段包26个文件（24PNG、manifest、索引）前后字节指纹均为9ec314432a7c726ae917b00e8b7a4340845d675d93198490de7e2e9e199f1ee3，未增加、删除或改写旧图包内容。

模式边界：无参数仍严格比较旧sourceHashes、原PNG及原checks，当前源与旧源有差异时继续失败；--smoke 保持手机双密度冒烟；--capture 是原有写图模式，本轮未使用；--current 只重放当前全矩阵、不写文件、不把旧证据验证记为通过。四种模式互斥，不能用 --current 与 --capture 组合覆盖旧图。

本轮只改字段验证器、增加模式解析模块与永久测试并更新说明；产品模板和CSS沿用前轮，不新增业务、API、配置、环境变量、权限、依赖或数据库变更。无需生产重启，未部署；本地浏览器/5175服务已关闭，无新临时文件。旧壳层组合证据及跨页检查点仍未闭合，commit hash 不适用。后续继续当前完整组合与其它控件状态，不能将本矩阵外推为P16整页或全73页通过。

## 修改与依据

用户已批准 P16 C-r2 整体布局并要求继续细化交互。实际 SelectionJourney.vue 的输入类型已有带名称的 radiogroup；候选结果和决策方式的容器只有 CSS class，原生 radio 具有各自 name，但没有向辅助技术暴露这两组的组级名称。

仅给 selection-candidate-grid 与 selection-actions 原容器分别增加 role="radiogroup" 和 aria-label="候选结果" / "决策方式"。不新增包装元素、tabindex 或自定义方向键处理。原 radio、label、name、v-model、禁用条件、提交处理与来源链接保持不变；不修改视觉布局、CSS、业务规则、五项质量门、API、权限、数据库、配置或依赖。

使用 ui-skills-root 选择 fixing-accessibility，按“分组具有可访问名称、保留原生键盘行为、最小局部修改”的要求执行。字段原生约束与本次组级语义不同，不据此声称所有错误关联、读屏及动态反馈已验收。

## 验证结果

- 新增永久测试 tests/unit/ui-phase2-journey-radio-semantics.test.mjs，编译真实 Vue 模板、以明确的合成展示状态执行 Vue SSR，再解析输出。验证原输入类型组、两个新增命名组及原生 radio 数量/name、禁用采纳、默认观察和外链属性。它不执行业务 setup/HTTP，也不等于读屏测试。
- 测试工具初次将 HTML void 元素按通用模板解析，产生解析错误；改用现有 Vue DOM HTML 解析器并显式提供未展示的状态组件后，基线为2通过/1失败。失败准确显示缺少“候选结果/决策方式”两个组；两处源码修复后3项通过。
- 新测试与既有写入生命周期、颜色令牌测试共12项通过。实际 Vue 字段冒烟 node scripts/verify-ui-phase2-journey-fields.mjs --smoke：手机390、两密度共16项观察通过，零截图、零业务写入；覆盖既有原生单选键盘切换、输入约束和字段几何，不宣称本轮完成桌面/三主题全矩阵。
- npm run typecheck:web 通过。实际差异仅两行模板属性；script 和 CSS 未修改。
- 关联 shell-journey Vue 测试4项中3通过/1失败：首个失败为已有 OrganizationAdminCenter.vue owner-path 修改与旧截图来源不一致，不是本轮造成的该父组件差异。
- 文档门通过（153项必需文件，73路由/60受保护/6角色）。本次源码、新测试、功能地图及本报告/页面规格/当前计划的格式通过；PROGRESS.md 全文件格式检查发现原有末尾 P47 标题前缺一空行，本次新增段落不涉及该位置，保留未修改，不报告整份格式门通过。

## 历史证据边界

本轮另核对了 p16-c-r2-review、p16-c-r2-fields-review、shell-journey-vue-c-r3、p34-route-lifecycle-vue-c-r1。四包均登记了 SelectionJourney.vue，因此两行语义增量也使这些包不能再被称作“完全匹配当前所有源码”的证据。

- 旧源码 SHA256（统一 LF）：107a289ca488e7dfa6e7b6a2d1fa50b740294e5223fd3ab316653c3faf89685f。
- 新源码 SHA256（统一 LF）：08e6c860cb5ebfec8f8a7999c9dcd32d6aaa8918b9e48c172507afff17965573。

原 PNG、manifest 和用户批准均未改。P34 图册仍可作为其捕获版本的视觉审核入口，但前轮“202份来源均匹配”的结果只描述前轮状态，不能移用到本轮工作树。源码图登记也不证明 P16 在 P34 场景中被执行。后续应独立保留历史来源与当前重放证据，不覆盖旧哈希或自动批准新版本。

## 交付与后续

当前失败门未闭合，不自动提交；原跨页检查点范围仍待确认，commit hash 不适用。没有生产更新或重启；后续正式发布仍走宝塔固定流程，不能以本次冒烟替代生产验收。

本轮无新临时截图、脚本、日志或服务待交接；复用现有依赖，Vite产生的依赖缓存不删除。测试浏览器与5175服务已退出，永久测试和本文保留。下一步是分离受影响旧包的版本证明并完成当前组合回归；不重复请求原布局批准，不将组级名称补齐等同于所有控件、弹窗或全站重构完成。
