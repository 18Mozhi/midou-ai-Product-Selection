# P44 页面主体整页编排 · 实际Vue审核 r2

后续增量：[1200/1201px断点与焦点补充](P44-PAGE-BOUNDARY-VUE-REVIEW.md)。本文件下述“未测精确断点”是原批次范围；新补充独立验证并记录焦点修订，原72图/清单/审核状态不变。

2026-09-11，基于生产源码1b8f9ff0。本轮是页面主体整体设计复核，不是新生产接入，不包含NavigationShell全局导航。已批准局部的目录、比较控件、角色资料和同角色结果仍按各自原PNG/批准记录限定，不能把局部批准累加成整页通过。

## 整体方案

统一浅灰蓝页面背景；标题与创建/刷新动作位于上方；1440px左侧为平台汇总和对象导航，右侧先处理可授权账号，再比较角色能力。1200px以下管理信息区移至目录上方，手机为单列。目录保留已授予和未授予平台角色账号，全局管理员数不随筛选改变。

角色比较采用蓝色说明栏、白色筛选与角色资料、浅灰数量提示和逐项事实结果；完整保留6项默认差异。390px整页高3659px，没有新增默认折叠或隐藏能力条目；另交首屏图供核对层级。对移动页长度的进一步交互调整不在本轮自动实施。

UI技能用于统一整页层级、留白和颜色，并消除已接入局部负边距与整页无内距容器叠加的问题。只在审核宿主添加高限定的拼接样式；没有修改生产CSS，也没有覆盖原批准PNG。

## 完整图册

- [72图入口](../../output/playwright/p44-page-assembly-vue-preview/index.html)
- [1440px整页](../../output/playwright/p44-page-assembly-vue-preview/1440-default.png) / [首屏](../../output/playwright/p44-page-assembly-vue-preview/1440-default-top.png)
- [390px整页](../../output/playwright/p44-page-assembly-vue-preview/390-default.png) / [首屏](../../output/playwright/p44-page-assembly-vue-preview/390-default-top.png)

390/760各19张，761/1440各17张：加载、完整默认、首屏、目录局部、筛选、筛选后的无角色账号、筛选空、同角色0/3、比较搜索空、详情入口、长邮箱多角色、角色刷新失败、账号刷新失败、首次读取失败、无账号、空角色目录；手机另含原预览和技术详情。所有新组合图均待审，不把同名状态视为再次获得批准。

## 真实来源与保护

审核宿主读取当前父/账号记录/角色比较及账号详情Vue；完整script保持不变，原事件、模型绑定、插值与控件合同逐项相等。父组件仅在审核变换时移除一次已实现的静态目录header，再由目录布局插入同文案，避免重复标题；此静态header的v-if不涉及任何控件或业务表达式。详情沿用原已存在审核分区助手，不改生产详情。

44源LF指纹、两个审核变换指纹、72张PNG指纹和精确文件清单均保留。没有新增路由，本页不是完整App验证。宿主/拼接CSS/变换助手为独立新文件，原审核助手、原图与全部生产源码保持。

沿用原账户/角色/买家详情样例。无角色第二条从既有user样例显式派生，长邮箱/空列表为明确测试变体；原全局摘要不变，缺失membership.organization_id不补造。四宽度每次19个拦截GET（10 accounts、8 roles、1 detail），无真实账号或真实权限结论。比较6/0/3、查询与重置不写URL或新增请求；目录筛选精确query/status、保留无关URL参数；移动预览→技术详情→账号详情及原生模态、创建默认运营角色/取消不请求均检查。

原角色读取失败后仍保留旧矩阵且没有独立过期说明，审核如实呈现，不靠新样式宣称数据已更新。创建仅检查默认/取消，不覆盖全部按钮、写请求、原因窗、真实授权、审计或生命周期。

## 验证与复验

```powershell
node scripts/verify-ui-phase2-admin-page-assembly-preview.mjs
node --test tests/unit/ui-phase2-admin-page-assembly-preview.test.mjs
```

默认只复验；有意重制本批正式图时追加--capture。使用现有依赖与自动空闲端口，finally关闭浏览器/Vite。第一次布局断言误把761px视作左右分栏，已按既有1200px断点修正并重跑全部宽度；生产未因此改动。未声称测试过1200/1201精确断点或全主题密度。

302项浏览器检查、5项新定向测试通过；修正历史指纹断言后8项定向与820项完整相关UI回归通过，73路由/153文件文档门禁、格式与diff空白检查通过。整合回归发现旧组织证据测试仍以fd9b68b4为审计actual，而已提交清单记录本次目录接入后的b6b04ced；仅将该断言接续已登记adminDirectoryRevision的精确before/after，未知修订和全部旧业务检查不放宽。生产源码、依赖、配置、API/OpenAPI、数据库、权限及部署脚本不变；上一轮生产构建仍对应当前生产源码，因此不重复构建。实际审核模板已经由Vite编译和浏览器运行验证。

## 待审核与收尾边界

请审核1440/390默认页面主体的整体编排：上方操作、管理信息区位置、目录优先级与下方比较区的关系。确认仅锁定该整体编排，不扩大为每个按钮/弹窗/状态、NavigationShell、完整App或生产验收。此前单独发出的差异前三项、分组和搜索空结果等未答审核仍待，不推定答复。

本轮未部署、未重启。全73页重新设计/实现、全控件状态审图、真实业务/权限、完整App与宝塔交付继续；没有降低目标。72PNG及图册/清单为正式交付保留，无一次性临时文件；失败回放的同名图由成功全量回放覆盖，清单排除额外产物。跟踪56032、56136、56670三个端口，结束前复核无监听，原文件未删除。
