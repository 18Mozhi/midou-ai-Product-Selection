# 组织概览 C · 实际 Vue 审核 r2

2026-09-13。仅为待审核提案，不扩大 P47 两种空态或其他已批准区域的范围。

## 交付与依据

- [审核图册](../../output/playwright/shell-org-vue-c-r2/index.html)、[机器证据](../../output/playwright/shell-org-vue-c-r2/evidence.json)：6 组、114 检查、20 PNG、174 个当前来源指纹。
- 实际 App / Router / NavigationShell / OrganizationAdminCenter；依据总纲 4.1、Feature Map M02-03/M06-01、现有组件与 M06-01 E2E 样例。
- 精确提取原组织 guard、summary、profile、workspaces，不合并成员/平台能力。9 个组织菜单来自原权限投影；数据为本地样例，不是真实组织数据。
- 沿用 C 蓝白方向：蓝色目录与组织身份区、六项概要、只读资料/编辑表单分区；1440 并排、841/840 堆叠、390 手机单列。
- r2 统一正文与标题字体，移除手机身份荧光边线和底栏旧分隔；蓝色标题读取已有组织名称，避免重复治理标题。没有添加不存在的快捷入口。
- 顶部“未命名组织·默认工作区”是旧导航样例缺少名称后的真实回退，不能当作上下文名称联动已验证；不编造导航字段或修改 API。

## 变更边界

新增审核驱动、fixture 提取器、CSS、永久测试和图证。组织组件仅在 Vite 审核宿主中替换一处标题表达式；script、表单事件、验证规则、API 请求、原生字段约束完整保留。生产文件未编辑。

本地网络仅 42 GET、无请求体、零写入、零意外请求、零运行异常。主题偏好 GET 显式返回 500，沿用既有访问状态审核约定；不是主题偏好成功读取或跨端同步验证。未提交“保存并审计”，也未验证真实保存失败/成功、其他组织路由、全部角色/主题、权限或生产。

## 验证

1. fixture 权限/名称关系、标题唯一逆向、Vue 编译及 script 不变定向检查通过。首轮 CSS 断言因格式化换行失败，改为仅归一化空白后通过，没有放宽选择器范围。
2. r2 `--smoke`：390/1440 共 49 检查通过。
3. r2 捕获：baseline 390/1440；review 390/840/841/1440。六项事实、9 菜单、首屏布局、无横向溢出、44px 字段/按钮、Tab 到相邻字段、原因必填、菜单搜索恢复、抽屉 Tab/Escape/返回焦点、一次额外刷新读取通过。
4. 当前组织 3 项 + 平台壳 4 项 + 成员/P16 4 项 + 访问状态 5 项 + M02-03 9 项，共 25/25；图片、174 来源 SHA 与当前文件核验通过。
5. 人工查看桌面、390 手机概览与完整表单、841 边界截图。截图仅证明展示区域；没有完成全站无障碍或真实保存验收。
6. route artifacts 73 路由、文档门 153 必需文件、runtime-docs、Prettier 与 `git diff --check` 通过；后者仅有工作区原有 LF/CRLF 提示。暂存区为空。

本轮未重跑全库。最近全库仍是 1499 项 / 1352 通过 / 147 失败的历史快照，不能从定向通过推算新全库数量。完整 73 页和发布门仍未完成。

## 回放与版本

`node scripts/verify-ui-phase2-shell-org-vue-c.mjs --smoke`：双端无图。

`node scripts/verify-ui-phase2-shell-org-vue-c.mjs`：六组无图。

`--capture` 只用于独占新版本目录；已有 r2 将拒绝覆盖。r1 的 20 张图和原 manifest 留作迭代历史，其来源指纹对应修改前的驱动/CSS，不再声称绑定当前文件。r2 是当前审核包；未来修改必须新版本出图。

前端设计技能用于蓝白分区、字体和旧视觉残留清理；无障碍技能用于原生字段、44px 点击区和焦点约束。复用项目现有 Playwright/Vite 驱动方式，无依赖安装。

## 收尾

所有本轮 Vite/Chromium 均 finally 关闭。使用过端口 58321、58545、58641、58670、59180、59243、59268；收尾核对监听数量为 0。未修改生产服务，无重启要求，无部署。

本轮没有一次性临时文件；r1/r2 两组图册共 40 张图是永久审核/迭代交付。此前两个清理受阻目录仍保留，本轮未绕过删除限制：

- `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p47-c-e2e-temp`
- `C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`

起点 main / af239b08b69f7d97cd0372f9840a70009eeef0c7，523 条已有工作区改动。混合工作区与全库失败门未解除，仅追加当前审核文件和文档，不暂存其他改动，不提交。commit hash 不适用。

待确认：r2 所展示的组织标题、六项概要、资料/表单分区；不代表整页所有状态、真实写入、其他组织页或生产验收。
