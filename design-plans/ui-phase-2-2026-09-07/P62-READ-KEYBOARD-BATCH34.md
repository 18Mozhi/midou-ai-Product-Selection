# P62 读取按钮焦点连续性 · 批次34

基线main/32cae5a4，529项原在途变化，暂存区为空。沿用P62/M06-02范围，使用fixing-accessibility约束原生按钮、等待标记、焦点归属；不把批次33或自动续行当作视觉批准。

## 改动与边界

真实Vue四组复现：点击首次错误区域的“重新加载”，loading移除该DOM，原持焦节点不再连接。现在保留已有重试按钮直至这次读取结束；初次打开页面的loading仍不凭空增加重试入口。等待标签为“正在读取…”。仅“刷新日志”和“重新加载”改用aria-disabled/aria-busy，原load单飞继续阻止Enter/Space/点击重复请求，两按钮可保留焦点并允许Tab离开。导出、检索和重置的原native disabled不改。

有记录的成功结果会移除错误/空态区域；在改变state前，只将仍由重试按钮持有的焦点交给常驻刷新按钮。核对当前路径、双方连接、inert、目标可见与实际焦点归属；用户移走焦点、切页、目标隐藏/失效时不交接。先preventScroll聚焦；目标越界或中心被遮挡才滚动，聚焦被拒绝不滚动。失败/超时或仍为空结果时保留原重试节点，不执行成功交接。

只新增局部UI引用与等待可见标记，原API/请求字段/200条/排序/快照/15秒/安全重试/sequence/卸载取消不变。无权限、原因、导出范围、SQL、环境、依赖或持久化格式变更。C审核CSS把两按钮aria-disabled映射为原灰色禁用外观，排除禁用hover；生产有局部灰色状态，C整页仍不是生产布局。

## 验证与审核

原8项读取测试补12项，覆盖已有按钮等待可见、单飞、成功焦点交接、已移焦/异页/断开/inert/隐藏的拒绝、越界/遮挡滚动及拒绝聚焦不滚动。实际Vue脚本执行与编译，加6项组合、10项M06-02合同，共36项通过。

实际App/原客户端，1440×1000与390×844，各reduce/no-preference；四组270检查、68次本地拦截GET。每组11次逻辑读取、17次GET，包含三个503各三次安全重试、三个真实15秒超时及一个受控延迟恢复。保留原读取文案/数据/空结果全部检查；补等待按钮同节点、焦点与Tab往返、防重复、超时不需主动补焦、成功交接、用户转到桌面搜索/手机筛选窗后迟到成功不抢焦点及筛选原关闭返焦。未做真实权限或导出，未知/外部请求、写入、下载和浏览器错误为零。

[读取反馈r4图册](../../output/playwright/p62-read-feedback-r4/index.html)22张双端图、168来源，捕获HEAD32cae5a4加本批工作树。含原18个区域的新状态及等待刷新/成功返焦四张视口图；当前申请手机等待重试按钮区域审核，未获批前所有新图保持待审。r2/r3保留为截图范围对照：按钮组和无内边距标题区都会裁掉外置焦点轮廓，r4对这两种状态改拍整个视口；历史图不因此获得批准。原r1清单/18图不重写；本次组件、审核CSS、读取验证器三项来源改变，历史包不是当前全量来源验证。

默认页原77项回归、前端类型/完整构建与253资源预算通过。构建基于当前混合工作树，不当作干净生产发布证据。文档和格式检查在收尾核对；不以局部浏览器键盘通过宣称完整读屏、真机、路由生命周期或73页完成。

## 使用与收尾

`node scripts/verify-log-read-feedback.mjs`默认无文件；`--capture-review rN`独占新目录，当前r4、下次r5。布局默认验证仍用`node scripts/verify-log-page-preview.mjs`。无需新增配置；只需未来前端静态包发布，无Node/Python重启、数据库迁移，本轮未部署。

生产改动只有PlatformLogCenter；配套OpenAPI/后端/Worker/Python/插件无接口消费变化，不修改。查询与在途响应归属、KeepAlive、快照/失败/导出requestId拆分、401/403/429、长数据/列菜单、完整导出原因与真实CSV仍待完成。

没有新建一次性文件、日志或下载；当前r4与截图范围对照r2/r3各22图、清单、说明及永久脚本/测试为交付物。所有验证浏览器/Vite在finally关闭；旧清理受阻材料不触碰，只提交本批文件和共享文档增量，其他529项变化保留。
