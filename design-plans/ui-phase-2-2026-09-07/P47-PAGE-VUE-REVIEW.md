# P47 采集程序 · 当前 Vue 页面审核

2026-09-11，基线 main / b7a7de9e。由此前离线PROVIDER-ADAPTERS-C-r1推进到实际前端路由。新稿待审，不是生产接入、真实健康检查或全73页完成。

## 实施范围

使用frontend-design技能，以“异常定位 → 检查探针 → 判断恢复入口”为页面顺序。保留真实App/NavigationShell/KeepAlive/ProviderRuntimeSurface入口，仅在本地审核宿主用独立SFC替换ProviderAdapterCenter的展示模板，并加载有审核标记且页面挂载条件的CSS。原生产组件及完整script不改，不改后台和数据库。

- 桌面将原八列事实重组为五组：程序与来源、健康探针、24小时实际采集、暂停与恢复、操作。原动态事实逐项保留，不新增桌面详情入口，不把离线稿的额外按钮假装成已有实现。
- 全目录统计独立于筛选；搜索/重置常显，五个选择控件放入原生details，默认折叠，不保存新偏好。DOM顺序按搜索→重置→更多筛选排列，避免焦点与视觉位置不一致。
- 手机压缩重复标题和统计说明，内部导航横向滚动；这些来源规则仍在页面说明与运行边界保留。首条来源显示健康、运行类别、样本量和暂停状态，不能将健康探针等同采集成功。761–840px混合布局同样使用紧凑头部。
- 现有状态组件primary本来只调用load，审核稿明确标为“重新读取状态”，隐藏没有消费者的secondary，未增加登录或授权动作。
- 已批准的P46样式和待审桌面图不变；P47有自己的审核标记，不在生产导入。

## 图包与事实边界

[72张实际Vue审核图](../../output/playwright/p47-adapters-vue/index.html)

390/760/761/1440px，高1000px。包括完整默认页与首屏、筛选展开、无匹配、待登记、搜索焦点、刷新失败保留快照、45项目录首页和末页、零数值样例、检查中/拒绝、首次空/403/500、加载与恢复。手机另有原详情和技术详情图；这4张详情图仅用于确认原入口仍工作，**没有完成详情C重构**，不应与新页面稿混为一项批准。

数据来自现有M03-03测试：两条默认记录保持；原45条目录样例缺少运行字段，本批明确用同一测试中的base补齐，属于审核样例而非真实API返回。另有显式合成的1个运行样本、0%成功率、0ms延迟/P95，验证零值不变成缺失。全部API在本地拦截，健康POST返回409拒绝，不访问真实来源、不持久化。

## 验证

四宽度130项显式浏览器检查、72PNG、167原始源指纹；源指纹数不是页面覆盖数。六筛选复位且不发请求，20/20/5分页和查询后页码回1，原详情Escape回入口，探针按钮禁用及无body请求，刷新失败保留数据和首次失败重读均有实际回放。

新增首屏检查要求首条记录在固定底栏上方至少露出44px区域；不等同整条记录或所有操作均在首屏。此前761px此项失败，审核CSS随后扩展紧凑头部到840px，没有放松测试。横向溢出检查针对根文档；761px桌面诊断表允许在其容器内横向滚动，不能宣称所有列无需滚动。

四项静态/证据合同证明：生产与审核完整脚本相同；所有原事件、六模型、条件和插值事实保留（以词法token区分格式变化与字符串内容）；所有CSS有标记与挂载范围、生产不导入；72张PNG及当前原始源完整绑定。没有把一次页面检查当作全部按钮/无障碍验收。

复现：`node scripts/verify-ui-phase2-provider-adapters-vue.mjs --capture`；定向合同：`node --test tests/unit/ui-phase2-provider-adapters-vue.test.mjs`。复用现有依赖，浏览器/Vite在finally关闭。本轮没有生产代码/依赖/config变化，不重复上一批已通过的生产构建。

## 未完成与审核范围

本批请审核所展示桌面五组诊断与手机紧凑目录的整体编排；全部控件状态、桌面列工具、详情C布局、三主题两密度、键盘全流程、读屏器、200%缩放和触摸设备仍待。

PR-G03中的旧GET覆盖新probe、路由离开迟到结果、详情对象消失与未知探针结果仍按原实现，未修复。移动检查反馈仍在页面背景，不能由存在role=status就推定打开详情时可感知。恢复深链、真实provider:configure鉴权、幂等、外发隔离和数据库状态未验；未运行真实健康检查。

没有生产API/配置/env/数据库/依赖变更，OpenAPI/.env.example/后端/Python不适用，不部署、不重启。正式72PNG与HTML/JSON为交付物保留，不删除既有111张离线图；没有一次性文件。最终测试、进程与Git清理结果在本批收尾记录。

## 本批收尾记录

新增定向合同4/4、相关provider合同67/67通过；文档检查153个必需文件、73条路由通过，代码风格和diff空白检查通过。浏览器130项检查通过，52次GET与4次无body健康POST全部本地拦截，POST均以409拒绝样例结束，没有真实外发或持久化。

审核主机与浏览器已关闭；本批使用的56537、56695、56961、57281、58017、58163端口均无监听，也无残留审核脚本进程。保留72张PNG、index.html、evidence.json共74个正式交付文件；没有需删除的一次性文件。当前仅提交本批审核组件、样式、永久测试、文档及图包，生产文件不变。布局审核等待用户答复；详情重构、完整交互、真实运行与全部73页交付仍未完成。
