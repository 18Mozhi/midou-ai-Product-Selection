# P37 组织审计 · 实际 Vue 整页 C 组合

从干净 `main/e13842c2` 继续。已有P36/P37局部问题未答，不扩大批准；当前实际P37脚本已有复制归属保护，不能把早期规格的旧问题描述当作未修复现状。本批保留当前脚本，不重新修改复制或父读取逻辑。

[76 张图册](../../output/playwright/p37-page-vue-preview/index.html) · [桌面完整图](../../output/playwright/p37-page-vue-preview/1440-default.png) · [手机完整图](../../output/playwright/p37-page-vue-preview/390-default.png) · [证据](../../output/playwright/p37-page-vue-preview/evidence.json)

## 设计及本次审图

使用 `frontend-design` 将页面重组为桌面左侧300px蓝色查询区、右侧紧凑已加载数量及时间线/详情。1250px以下查询区移到上方，900px以下列表/详情纵向排列；手机字段单列、数量两列。列表保留局部滚动，详情不使用原吸顶，长追踪号在自身区域横向滚动，不撑宽整页。

页内搜索在查询表单内部以白色小区单独说明；其余三个基础精确字段和四个高级追踪/时间字段保持原生控件。所有输入仍属于原form，未改变Enter提交行为。详情默认复制文案明确为“复制请求 ID / 复制追踪 ID”，成功/失败仍用原反馈；只有标签改变，调用参数不变。未知结果用中性色，成功/失败/已阻止颜色仍辅以原文字。

本次只请求审核**整页内容布局**，不含全局导航、全部控件状态、完整无障碍、真实数据/权限或生产验收。原页面无业务弹窗，不新增编辑、删除、导出或重放按钮。

## 实现与行为保持

`scripts/lib/ui-phase2-audit-page-preview.mjs` 用Vue AST定位原overview/metrics/form/grid/ledger/correlation，增加工作区和结果列包装，拆出原页内搜索label并保留在form中。父模板/脚本不转换，子script完整保留；所有原指令、字段表达式、按钮与原生details/summary不变，仅两个默认复制字符串明确字段。

`implementation/audit-page-preview.css` 仅审核宿主加载，所有覆盖限定p37-page-preview。没有修改生产源或旧P37图包，没有新增依赖、服务、API、权限或字段。

## 验证与样例

390 / 760 / 761 / 1440 四宽度，**216项浏览器检查、76PNG、39项源指纹**。每宽度19图：加载、默认完整/首屏、失败/已阻止选择、技术详情、请求复制成功/追踪复制失败、追加等待/末尾、页内无匹配、日期错误、服务端筛选、系统折叠/展开/搜索、真空、首次403/500。

- 从原E2E初始化表达式提取55条记录；系统模式另用原40连接+10业务样例，不把两套数据拼成95条。UI显示50/55及已加载分类数，不宣称全库数量。
- 每宽度仅8次GET，全部为当前组织audit-events，没有organization:manage摘要请求或写入。首批limit50、追加游标为第50个夹具ID；这是测试游标，不是生产编码。
- 日期倒序在本地拒绝且不GET；七项精确条件trim/ISO与请求参数核对通过。夹具只实现原五项字符串精确过滤，不将请求格式检查冒称服务端时间范围语义验证。
- 页内搜索、系统折叠/展开不请求新数据；精确应用和重置调用实际父函数。追加等待禁用查询按钮；50到55后不再出现更多按钮。
- 原metadata敏感键例子被脱敏，合成probe值未出现在页面；请求/追踪复制参数精确，模拟剪贴板拒绝时显示原失败反馈。不是任意秘密值检测、端到端脱敏或真实OS剪贴板验证。
- 每图检查水平溢出，默认五个主要区域逐一滚动并进行三点可见遮挡检查；错误隐藏审计内容，页面没有可见业务dialog。列表采用局部滚动，完整截图不平铺所有50行。
- 初轮验证器用严格label定位含option文本的select超时，已改为实际唯一select定位；非接口/产品故障，最终76图已重拍。

原listitem按钮语义、完整键盘/焦点/屏幕阅读器、200%缩放、三主题/密度、历史导航/缓存/组织切换、实际SQL双表归并/游标/权限仍未验；本次不宣称全页功能或生产验收完成。

## 使用与收尾

收尾验证：本批4项定向测试及701项相关回归全部通过；文档门禁、格式检查、diff空白检查通过。交付审计73路由/102图包，源与PNG漂移均为0，但全页完成度仍未证实。62771/62856端口均无监听；生产代码、配置和依赖未变化，本批不重复运行生产构建。

```powershell
node scripts/verify-ui-phase2-audit-page-preview.mjs
node --test tests/unit/ui-phase2-audit-page-preview.test.mjs
# 更新本批正式图片时才加 --capture；普通复测不改图。
```

生产Vue/CSS、API/OpenAPI、后端、数据库、环境、依赖、权限未改；无需配置调整或重启，未部署。76PNG/index/evidence与永久测试为交付物，中间同名图覆盖为最终结果；Vite/浏览器finally关闭，无额外临时文件。整页布局与其他前问分别待审，全73页实际C/完整壳层/真实验收/宝塔交付继续。
