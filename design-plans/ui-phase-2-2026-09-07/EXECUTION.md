# 第二阶段执行手册与审核安排

日期：2026-09-08；配套 [PLAN.md](PLAN.md) 1.24 与 [73条逐页矩阵](PAGES.md)。本文件是实施安排，不是完成或发布记录。当前main/ea9cd59、73份事实规格齐全；唯一当前任务卡见第3.16节。先F04源码/语义与动态覆盖收口，F00方向获审后优先F05代表闭环，再完成全部W02–W09。B3b/B3c不再领取，规格、局部回归和历史部署不抵扣正式设计、全量验证或用户审核。

## 1. 本轮目标与真实起点

目标：重新设计全站页面结构、视觉语言、按钮与弹窗，交付可逐页审阅的完整图包，并在设计确认后落实到真实 Vue、测试及宝塔生产环境。保留既有功能、权限和数据合同，不保留旧 UI 的默认豁免。

1.6核对时的历史起点（当前基线与任务以PLAN 2.12及本文件3.16为准）：

- 路由目录为 73 条，包含跳转、内部开发页和 fallback，不等于 73 个正常生产业务页。
- 已提交的盘点工具发现 1377 个控件/事件候选、100 个弹窗定义或调用候选；这些数字尚非去重后的业务动作或弹窗变体分母。
- PROGRESS 已记录 W00 静态盘点、五类代表页的局部 Vue 基线、W01审核工具和 A/B 方向图；已有19份页面规格，任务方向12图、其他代表方向及按钮板76图。仍缺54份规格，运行时分母、最终逐页设计、完整实现及全站验收尚未完成。
- 核对基线为 main / 181ab16，已有未跟踪机会候选草稿，本轮不修改、暂存或认定其完成。任务缓存修复e1f7a92及证据刷新4e5e8f5已提交，不重复安排为待实现；54个目录外候选、任务71项、评分25项、身份38项、账号/首页51项、趋势60项有已提交局部语义记录，但不能直接相加充当全站去重分母。P14新增8项合同测试与旧8项桌面/移动各16项通过记录来自PROGRESS，不是本轮重测或全站设计通过；产品源码变化后按PLAN 2.2刷新相关证据。
- 本次只规划，没有重新连接生产环境，线上是否仍与历史部署一致不能从本地 HEAD 推断。

执行人负责设计与编码，验收职责负责证据复核，用户负责风格选择及最终签收。同一实施者可以承担验收职责，但应明确属于自检，不声称独立第三方审核。默认不创建代理任务。

## 2. 工作包、依赖与交付节奏

| 工作包 | 内容与逐页范围 | 预计有效工作日 | 交付与退出条件 |
| --- | --- | --- | --- |
| W00 | 73 路由运行时盘点、候选归并、旧新映射 | 1–2 | 稳定 actionId/dialogId；角色和状态入口已追踪；G0 通过 |
| W01 | 新视觉方向、五类代表构图、公共控件、审核机制 | 3–5 | 用户选定方向；代表页闭环跑通；完成设计规范和组件合同 |
| W02 | 账号入口、核心业务与异常入口，19 项 | 2–4 | P01–P12、P14–P18、P72–P73 逐页规格、图、实现、包级验收 |
| W03 | 今日工作、竞品、供应链、任务、审批、通知、自动化、报表，11 项 | 4–6 | P13、P19–P28，保留真实业务关联并完成连续链验证 |
| W04 | 组织治理，9 项 | 2–4 | P29–P37，所有子页独立设计及权限回归 |
| W05 | 平台账号与权限，8 项 | 2–3 | P38–P45，六角色对应允许/拒绝路径 |
| W06 | 来源、凭证与采集，8 项 | 3–5 | P46–P53，受阻与恢复真实，外部执行边界清楚 |
| W07 | 数据、质量、运营、安全、开放平台，8 项 | 2–4 | P54–P60、P63，不把停用能力设计为可用 |
| W08 | 系统状态、日志、恢复、发布、拓扑与容量，10 项 | 2–4 | P61–P62、P64–P71，单机边界与危险动作逐项验收 |
| W09 | 全站整合、发布、生产验证与用户签收 | 2–3 | G0–G5 全部有结论；同版本证据、清理、回退材料齐全 |

完整顺序为W00→W01→W02→W03→W04→W05→W06→W07→W08→W09。W00未全站冻结前可以研究W01原型，并对合同已核实、设计已获审的代表页做隔离实现与验证；不据此跳过G0或部署。W01的代表页不抵扣后续工作包的最终验收，事实盘点与用户审图按3.16交错推进。

合计 23–40 个有效工作日，仅作为初始估算，不承诺交付日期，不含等待用户审核、凭证及发布窗口。新增结构差异方案的工作量先在 W01 实测，再修订剩余估算；不能为了守住区间削减逐页、异常状态或真实验证。审核与返工耗时单独记录。

每包交付顺序固定：规格 → 设计与意见 → 实现 → 定向验证 → 文档/清单同步 → 精确提交。默认 W09 统一发布，本次计划不触发发布。

## 3. 工作包任务定义

以下是完整工作包的任务合同，其中已产出部分按 PLAN 2.12 和 PROGRESS 复用，不重复从零制作。已有图稿不等于该任务退出条件全部通过；从本次起的剩余执行顺序见3.16。

| 任务 | 读取入口 | 操作及产物 | 验收条件 |
| --- | --- | --- | --- |
| W00-01 | route-catalog、App.vue、PAGES、coverage | 按 routePath/mode 核对 73 条入口、共享消费者、会话及六角色 | 路由无漏项；动态 URL 使用合法隔离 ID；内部项独立分类 |
| W00-02 | actions、dialogs、真实 handler、api-client 调用 | 打开菜单、详情、批量态及各类弹窗；将源码候选归并到语义 ID | 每个候选有保留/合并/内部/废弃等理由；写入与跳转分开 |
| W00-03 | 现有 E2E、角色与接口合同 | 为动作建立 caseId、角色、前置数据、断言、副作用及清理目标 | 阻塞与未执行不算 passed；Mock 不作为真实后端通过 |
| W01-01 | 当前截图、真实功能清单、蓝图视觉章节 | 为同一列表与详情制作至少两种结构明显不同的新方案，每种含桌面/移动对照 | 对比导航组织、首屏重心、列表密度、详情结构，不只比较颜色；用户选定方向 |
| W01-02 | TaskWorkspace、ScoreRuleConsole、OrganizationAdminCenter、PlatformManagementCenter | 将选定方向扩展到任务列表 P23、详情 P24、规则 P17、权限矩阵 P31、异常恢复 P61 | 五类场景各有桌面/移动图、字段出处与关键异常态；避免一种模板覆盖全站 |
| W01-03 | 共享按钮、菜单、筛选、表单、useModalDialog 及消费者 | 制作控件六态板、确认/原因/长表单弹窗变体；确定布局、令牌与动效规范 | 44px 热区、焦点链、失败恢复、重复提交保护；同步当前设计规范后再批量应用 |
| W01-04 | 现有 review.html 与独立意见导出 | 增加设计/实现图对照、版本与页/动作/弹窗意见关联 | 用户可逐项查看和导出；缺图不能通过；旧意见不能因重生成丢失 |
| W01-05 | 已审代表页、对应 E2E | 在真实 Vue 中完成一个页面及弹窗的设计→实现→交互→证据链 | 本地真实接口与隔离合同数据证据分别标记，设计差异已复核，用户可审阅 |

完成上述任务后更新实际工时、分母、风险及 W02 的精确领任务清单。后续页面入口、设计重点、测试入口均按 PAGES，不临时漏掉组织、平台或运维页面。

### 3.1 从当前状态开始的剩余任务

R编号仅为实施顺序，不新增产品阶段，也不是73页以外的新范围。实施者负责产出和自检，用户负责视觉方向及逐页签收。

| 顺序 | 输入与具体动作 | 必须留下的交付 | 退出条件及阻塞处理 |
| --- | --- | --- | --- |
| R01：W00冻结 | 复核73条实际路由和共享mode；复用54项目录外语义记录并补剩余运行边界；逐页遍历角色、动态菜单、空/错误/批量态，追handler和API | 每个候选的业务归属、稳定actionId/dialogId、适用状态、caseId、清理目标；人工记录与生成物分开 | 无无主候选；分母及排除理由可复核；不能因旧704项源码对应完成而跳过语义复核 |
| R02：W01方向审定 | 展示现有任务及其他代表页A/B图；逐项比较导航、首屏、列表详情关系、按钮/弹窗 | 用户选择A、B或要求重做的明确记录，关联图版本；两案都不通过时列差异并重做 | 未回复不选默认项；等待期间可继续R01及不依赖风格的合同记录 |
| R03：W01设计规范 | 将获审方向统一到五类代表；补长内容、异常、多主题/密度；按实际消费者设计公共组件 | 五页完整规格与图、按钮六态、弹窗各变体；更新 docs/frontend-design-system.md、docs/ui-page-spec.md 与蓝图相关视觉章节 | 新布局不混用旧纸色/朱砂/零圆角等强制规范；仅更新视觉规定，保留主题ID和偏好存储合同 |
| R04：W01实现闭环 | 选已审P23/P24列表到详情及进度弹窗，沿TaskWorkspace及现有E2E实现；共享变更检查全部消费者 | 真实Vue图、请求/返回断言、错误恢复及焦点证据、与设计逐项差异、自检记录 | 隔离夹具与真实后端分别标记；代表实现可验证后才批量迁移；其余业务动作继续在W03全验 |
| R05：W02–W08逐包交付 | 依下表子批，每页完成十项规格→用户审图→Vue→定向与受影响回归 | 每页独立规格、最终设计/实现对照、全部动作/弹窗结果、意见闭环及精确提交 | 每个子批有可审核完整页面，不跨包遗留未解释动作；共享变更使旧证据失效时重验 |
| R06：W09收口 | 汇总G0–G3，同版本完整回归；完成发布预检，再经宝塔部署和生产验证 | ACCEPTANCE、生产SHA/图索引、隔离残留0、恢复材料及用户最终意见 | G4技术验收与G5用户签收分开；无凭证/授权/隔离目标时列具体缺项，不伪造通过 |

### 3.2 每包内部按这些子批交付

子批依次推进；每批图交用户审阅后才实现该批。已有通过的共享规范可复用，但不能省略业务页单独审核。合计19+11+9+8+8+8+10=73条。

| 包 | 子批顺序 | 本包必须跑通的链 |
| --- | --- | --- |
| W02 | P01–P09身份/入驻 → P10–P12偏好/个人/首页 → P14–P18趋势/机会/规则 → P72–P73内部/404 | 登录→合法范围→首页→机会→详情；无会话、无权、失效和返回路径 |
| W03 | P13/P23–P26工作/任务/审批/通知 → P19–P22竞品/供应/利润规则 → P27–P28自动化/报表 | 任务→审批→通知→关联对象；供应事实→利润；报表→真实下载 |
| W04 | P29–P31概览/成员/权限 → P32–P34工作区/团队/模板 → P35–P37数据/令牌/审计 | 组织切换→管理对象→取消/提交→列表刷新→审计；无跨组织残留 |
| W05 | P38–P39平台入口 → P40–P42组织列表/创建/详情 → P43–P45用户/管理员/权限 | 列表→创建/详情→返回；不同平台角色的允许和拒绝 |
| W06 | P46–P49来源/程序/启用检查 → P50凭证 → P51–P53任务/总览/浏览器运行 | 缺条件→受阻→查看原因→已有恢复入口；真实第三方调用另审执行边界 |
| W07 | P54–P55数据/质量 → P56–P58内容/通知/配额 → P59–P60/P63安全/开放/接口证据 | 列表→事实详情→允许操作→异步反馈；未启用及未验证能力不伪装成功 |
| W08 | P61–P62状态/日志 → P66–P71拓扑/资源/调度/容量 → P64–P65恢复/发布 | 异常→日志/依赖→诊断→受限恢复入口；高影响操作默认验证到确认/取消 |

第一轮实施检查点为R01清单归并与R02方向意见，不承诺以一天完成全部盘点。R03/R04结束时用实测耗时重估剩余子批；每次进度更新列出完成ID、未完成ID、验证版本和需要用户处理的事项，不只报百分比。

### 3.3 获准实施后的首批执行卡

下表只安排下一步，不表示本次已经执行。以语义动作和调用链为单位交付，避免不断增加同一原型的截图而没有推进新页面。

| 顺序 | 精确范围与工作 | 交付及最小验证 | 完成后转向 |
| --- | --- | --- | --- |
| S00 | 已完成：e1f7a92提交任务缓存读取修复，4e5e8f5刷新证据；下一轮只检查当时工作树及来源是否变化 | 复用绑定源码的已有验证；相关源或测试环境变化才定向复测 | 直接进入S01/S03；不重复修复或无条件重采历史图 |
| S01 | 读取当前清单、source-scope-review及相关源哈希；复用已有54项解释，列出仍未运行的边界 | 只读运行 inventory --check 和 source-scope verifier；过期记录才重做 | 按业务入口继续R01，不重复已有效的源码盘点 |
| S02 | 复用已提交P13/P23/P24规格及71候选/13弹窗局部合同；对照当前TaskWorkspace和拆分组件补剩余状态 | 不重建已有记录；重点核实缓存读取与迟到响应修复证据、弹窗内错误可达、提交关闭竞态及未覆盖权限/真实后端；共享夹具保持事实口径 | 已验证项目登记后补齐其他路由族；不把18项历史隔离测试视为三页全验 |
| S03 | 先补P15/P18，再补P16，复用已有P14/P17；随后按W02–W08共享入口族补其他51份规格及动态菜单、全局壳层、共享确认调用方 | 每族有候选→语义ID→调用方→caseId映射；已有19份逐页复用并补缺，不重写为新成果；无法执行有原因及解除条件 | 全73项合并检查，冻结G0；不能用抽样冒充全量，三页未交付前剩余仍为54份 |
| S04 | 向用户展示现有五类代表A/B，说明导航、首屏、详情和弹窗差异 | 记录用户选定方向或重做意见及图版本；未回复不代选 | 用户选定后R03，否则继续与风格无关的合同工作 |
| S05 | 获审方向→正式令牌/共享组件合同→P23/P24与进度弹窗Vue闭环 | 设计/实现双视口对照、请求及焦点断言、失败恢复、受影响消费者回归 | R04通过后按W02–W08逐包实施，不把代表页算成全站完成 |

S04可在S01–S03期间提交审核，不要求用户等待全量盘点才看方向。每次报告列明新增完成的页面/动作ID、实际验证层级、下一批范围和需用户处理事项。已有效的检查不重复执行；若源、夹具或共享样式改变，只重验受影响证据及必要整体验证。

### 3.4 下一批可直接领取的工作卡（1.6）

以下是后续实施安排，本轮不执行。先完成一个页面族的规格与合同，再交付可审核成果；不能只增加截图数量而不减少缺失页面或未验动作。

| 卡片 | 输入/范围 | 具体交付 | 最小验收与下一步 |
| --- | --- | --- | --- |
| N01 趋势 P14 | 规格与局部合同已提交181ab16；读取P14、trend-contract-review与PROGRESS | 复用60候选、18绑定、弹窗变体及8项新增测试；不重建已交付规格 | 源未变化不重复运行已有效16项双视口回归；TR08–TR12未验/待修项进入3.6，最终设计仍待审核 |
| N02 机会列表/详情 P15/P18 | `/opportunities`及动态详情，共用OpportunityWorkspace及其实际子组件；机会/评分/利润/AI域；现有m04-02-opportunities测试；先核对已有未跟踪候选草稿 | 两份独立十项规格、完成可追溯的动作/弹窗合同；创建、批量、详情、签认及成本/证据/AI按调用方归属；草稿引用缺失或未核实项不能算交付 | 分别核对返回、刷新、无权/不存在、成功后同步；补准确请求、取消零写入、失败保留与焦点测试。通过后规格为21份、待补52份；不把P17配置和机会评分混为一项 |
| N03 创建流程 P16 | `/opportunities/start`，SelectionJourney及真实journey路由/服务 | P16十项规格；输入→创建→进度→结果→人工决定→关联对象的状态表；恢复和重新开始的真实边界 | 核对重复提交、失败/空结果、离开/返回、已有本地记录恢复；测试入口先按实际用例定位，不编造测试文件或服务字段。通过后规格22份、待补51份 |
| N04 该族验证与移交 | N02–N03新增工作及现有P14/P17；不依赖风格的合同可先做 | 三份新规格与两份复用规格、候选/动作/弹窗映射、必要永久回归用例、未验清单、精确提交；设计方向未通过则明确图仍待交付 | 最小用例通过后执行受影响模块桌面→移动回归和文档门；不无条件重跑未变化的P14/P17。规格累计22份、尚缺51份，用户通过数不自动增加 |
| N05 同步方向审核 | 已有[任务A/B](design/task-directions.html)及[规则/权限/恢复代表A/B](design/representative-design-review.html) | 展示旧版差异、两案导航与首屏/详情结构；记录用户通过、修改或全部重做及图版本 | A/B只是候选，用户可否定两案；选定后进入R03/R04，否则继续源码合同但不实施未审视觉 |

N04只验证本批必要行为，不自动修复全部旧缺陷。影响当前正确性的缺口按真实合同最小修复；需改变权限、业务规则、数据库或外部接口时先停在明确问题上请求决定。后续按3.2逐包领取；W02还需P72/P73规格，W03–W08继续其缺失页面，已有P13/P23/P24/P31/P61不重建但仍须最终设计与全验。

每批交接固定列：本批新增交付ID / 未完ID及原因 / 代码与证据版本 / 有效测试 / 需要用户审核的具体图 / 下一批。登记“计划、源码核对、本地隔离通过、真实后端通过、生产通过、用户通过”六种不同证据，不把一种替代另一种。

### 3.5 设计决策与返工边界

- 审核先回答结构问题：采用哪种导航、首页首屏重点、列表与详情关系、移动主操作位置；通过后再确定字体、配色、圆角、阴影与动效。用户要求的是全面重设计，不能默认继承旧纸色、朱砂、宋体标题或横向账页布局。
- 保留既有主题ID、偏好存储、权限和业务动作；视觉方案可以彻底重做，但不借重设计增删业务能力。没有真实字段支持的图表、指标或确认影响不进入正式图。
- 用户指出某页仍有旧风格时，记录具体结构差异并返工该页；只有共享令牌或组件确实受影响时才扩展复核范围，不无条件重做所有图。
- 每子批交付应同时包含主页面、实际菜单/按钮、全部适用弹窗及错误恢复，不先做完所有正常态再无限延期补交互。无用户意见时保留待审核状态，不自动通过或自行上线。

### 3.6 已知缺口的实施优先级与收口

以下依据已提交合同和PROGRESS安排复现、修复、复测，不把源码疑点直接说成已运行确认的缺陷。属于本阶段页面可用性和合同稳定性范围；改变业务、安全或持久化规则时仍需精确说明并确认。

| 优先级 / 领取时机 | 范围与依据 | 具体执行及退出证据 |
| --- | --- | --- |
| 第一优先：该页编码前 | 各页权限、范围切换、缓存及迟到响应；身份I01–I12、账号/首页A06–A12和任务合同未验项 | 先复现并记录请求/当前页面身份；只有证实需要才最小修复。验证旧请求不能覆盖新范围、无权页面不能写入、未知/失败不能假装为空或成功。用户数据或权限错误未解决不进入该页验收通过 |
| 第二优先：页面与弹窗同批交付 | 趋势TR08–TR12、评分及机会调用方中的焦点、字段报错、原因保留、关闭/重开、重复提交 | 每个变体验证可访问名称、初焦点、Tab循环、Escape/取消归还、错误可达、重试准确请求；忙碌态及迟到结果按既有业务结果设计，不擅自把关闭解释为取消服务端任务。已有测试未覆盖的交互补永久用例 |
| 第三优先：本包验收前 | 73页的适用状态、三主题/两密度、四视口及长内容 | 按页出设计/实现对照；逐项处理溢出、遮挡、颜色之外的状态表达、键盘路径及reduced-motion；复用组件不替代调用方检查 |
| 发布门：W09前 | 真实后端/RBAC、同版本生产、迁移记录、可执行恢复和隔离数据清理 | 本地隔离测试不能顶替这些项；依第7节在真实允许范围采证，缺凭证或维护窗口记录具体条件，不提前标记生产通过 |

发现一个缺口时，在对应既有合同保留稳定caseId、当前结论、负责人、解除条件、最后验证SHA及证据；未复现、已复现待修、局部通过、真实后端通过、生产通过分别登记。每个工作包退出前检查其所有未关闭项，不能只统计新增测试数。无证据证明修复的旧问题不能被下一批进度覆盖。

第一次审核交付不等待全站图完成：N05向用户展示可打开的五类代表A/B对照，明确每案导航、列表/详情、首屏和弹窗差异，以及仍缺的状态图。用户可以要求两案全部重做。通过方向后先完成P23/P24及进度弹窗的整套新设计与真实Vue对照，再依3.2分包提交全部页面、按钮、弹窗。既有原型只作为候选，不预设用户接受其配色或构图。

### 3.7 1.8工作卡（历史，当前领取按3.8）

每卡由实施者产出与自检，用户负责设计/业务决定及签收。默认串行执行，不创建其他代理或任务。E01/E03/E04不依赖风格决定，可推进事实盘点；E02已交付规格，最终视觉仍归W02。E05尽早提交用户审核，E06须有明确批准记录。其余工程约束、图包和发布要求仍按本手册第2–7节，不因下表只列近期工作而缩小W00–W09范围。

| 卡片 | 输入和具体实施 | 必须交付 | 退出标准 / 依赖 |
| --- | --- | --- | --- |
| E01 当前源证据收尾 | 核对060e0b5、5个既有生成清单、state-recovery合同及图源；f55942d的旧源证据收尾已经完成 | 当前源候选差异、复用/失效证据索引、实际必要重采与校验；单独提交或明确归属待处理记录；临时目录交接 | inventory --check、source-scope及受影响图库校验通过；源指纹变化必须说明图的有效性，禁止仅改哈希。未变独立原型可以复用；此卡不增加完成页面 |
| E02 已交付，不重复领取 | 060e0b5已交付P72/P73独立规格、合同测试及共享遮罩焦点修复 | 复用已提交规格及测试，剩余状态/调用方/生产边界移入对应工作包 | 当前24份规格、缺49；最终图、生产不可达及用户审核仍未完成，不把规格交付等同页面签收 |
| E03 W03缺失8页 | 依次P19/P20竞品及监控规则→P21/P22供应及费用规则→P25/P26审批及通知→P27/P28自动化及报表；复用P13/P23/P24 | 每个二页小批各交独立规格、共享入口的动作/弹窗变体合同、必要永久定向测试及未验项 | 规格24→26→28→30→32、缺41；区分读/写/下载/外部副作用。以CompetitorMonitor及m04-05真实入口起步，不修改采集或规则业务来配合草图 |
| E04 W04–W08缺失41页 | W04补P29/P30/P32–P37八页；W05八页；W06八页；W07八页；W08补P62/P64–P71九页；复用P31/P61 | 按3.2子批交付41份规格与语义映射，合并全站动作/弹窗分母及明确排除项 | 规格32→40→48→56→64→73、缺0；1377/100仅候选基线，不能强制总数不变；无孤立动作、错误继承角色或无依据字段才可冻结G0 |
| E05 方向与规则审核 | 展示任务A/B、其他代表A/B；独立列出J07采纳门冲突 | 绑定图版本的用户意见；J07绑定真实合同的单独决定 | 可选A/B、改稿或重做；视觉认可不等于授权修改业务。未答复保留pending，可继续E02–E04 |
| E06 首个完整新界面闭环 | E05视觉通过后，先统一令牌/导航/按钮/弹窗规范，再做P23/P24与进度弹窗；读取全部共享消费者 | 新设计图、Vue对照、按钮六态、弹窗各态、准确请求与焦点/错误恢复证据 | 两视口真实交互通过、用户审图意见关闭、共享消费者无回归；三主题/两密度与断点规则明确。不能以CSS研究图抵扣 |
| E07 全站正式交付 | 沿W02–W08子批执行，每页十项规格→最终图审核→Vue→定向及包级验证→提交；J08/J09/J10按真实合同保留未完项 | 73条逐页审核卡、至少146最终设计槽位和146Vue槽位，全部业务弹窗变体与控件板另计 | G1/G2/G3按页通过后汇总；进入W09前所有必验项有证据，业务冲突未解决的相关链路不能通过；按第7节发布、生产复验及G5签收 |

每次领取前检查工作树、当前源码、既有测试和进程，只做当前卡直接相关内容。每卡交付时记录完成ID、剩余ID、具体证据层级、实际耗时与下一卡；缺49份规格不表示只剩49页工作，已有24页仍须最终设计、实现、全验与用户签收。初始23–40有效工作日不是剩余工期；E06完成后按实际“规格/设计/实现/状态回归/审图返工”分项耗时，结合剩余页面复杂度重估。用户等待、凭证和维护窗口另列，不算开发工作日，不承诺未经验证的日历上线日期。

#### 第一轮实施交付与防止停留在盘点的规则

1. 首次实施交接必须同时说明E01证据状态、展示E05可打开的候选方案、领取E03首个P19/P20小批。若需要重采，列准确原因及受影响范围，不重复制作源与样式均未变的88张独立研究图。
2. P19/P20规格写清实际列表与规则模式、搜索/详情、创建三步表单、采集状态、暂停/恢复、原因删除、阈值表单及验证任务的归属；先按真实可见条件确认，不由计划补造规则编辑/删除入口。相邻小批沿PAGES同法逐项领取。
3. 用户审核方向时，集中对比导航、首屏重点、列表与详情关系、弹窗承载方式及移动主操作；颜色/字体仅是其中一部分。用户可以要求A/B全部重做，计划获准不等于任一方案获准。
4. 每个正式交付小批同时含主页面、所有实际菜单/按钮、适用弹窗和失败恢复图；不把异常态集中拖到W09。图路径缺失、按钮无合同或用户意见未关闭，该页不晋级。
5. 进度报告只列新增页ID/合同ID、实际证据层、当前阻塞及下一批；不因已有局部测试、采图数量或提交次数增长推算总完成百分比。规格归零后，尚有正式图、Vue、真实后端、生产和签收工作。

J07决策若要求改变业务，实施前必须追完整前端→路由→服务→持久化合同，提出准确影响与测试，不直接在视觉提交里混改。其他未确定业务问题同样处理。用户已给出的单次迁移许可不扩展为下一次任意迁移或危险操作授权。

### 3.8 1.9工作卡（历史，当前按3.9领取）

仅使用当前仓库事实；不重新开始已完成的E01证据收尾、P19/P20规格或P72/P73规格。本轮只是计划，以下动作在实施轮执行。实施者负责产出和自检，用户负责设计选择、业务决定及签收；默认一个执行者串行，不启动其他代理。

| 卡片 | 入口 / 实施内容 | 交付与退出标准 |
| --- | --- | --- |
| F00 方向审定 | 复用[任务候选](design/task-directions.html)与[规则/权限/恢复候选](design/representative-design-review.html)，集中对照导航、首屏、列表/详情、弹窗和移动主行动；已有有效研究图不重复采集 | 用户绑定图版本选择、修改或要求全部重做。未答复仍pending；J07单独列问题，不能把视觉批准当业务授权。F01–F04可继续；F05正式视觉必须等待明确意见 |
| F01 供应与成本 P21/P22 | 先按feature-map定位SourcingWorkspace、SourcingComparisonPanel、SourcingWorkspaceDialogs、SourcingCostConfirmationPanel、CostRuleConsole及其真实API消费者；复用m04-06-sourcing、m04-04-profit测试入口 | 两份独立十项规格；供应搜索→报价→比较→采购与成本提交→复核、费用规则版本→审批→生效/回退分别映射。核实可见角色、事实/试算/生效边界；按钮取消、准确请求、失败保留、防重复和焦点按真实合同补测试，不改变MOQ、算法或审批规则。26→28份 |
| F02 审批与通知 P25/P26 | 按PAGES定位任务审批与通知真实组件、handler及既有测试；区分任务内审批与组织审批、通知状态与任务状态 | 两份规格、共享入口归属和弹窗合同；测试筛选/返回、允许与拒绝、原因/失败重试、关联对象跳转及现有状态同步。不为设计新增通知发送或批准规则。28→30份 |
| F03 自动化与报表 P27/P28 | 按PAGES追踪规则创建/暂停/恢复、执行记录与报表读取/导出/下载/过期处理；先读真实服务和测试，再确定呈现 | 两份规格、全部现有动作/弹窗及副作用映射；失败/空/禁用/导出处理中与过期恢复有证据；不把统计缺失显示为0，不执行真实外发或采集来凑测试。30→32份 |
| F04 组织、平台与运维剩余41份 | 依次W04补P29/P30/P32–P37八份；W05 P38–P45八份；W06 P46–P53八份；W07 P54–P60/P63八份；W08 P62/P64–P71九份。复用P31/P61 | 各子批按3.2和PAGES真实入口领任务；32→40→48→56→64→73。每页字段/动作/弹窗/角色/状态有归属；全站运行时语义归并、排除原因和测试映射核对后才冻结G0，不能拿1377/100候选数代替 |
| F05 获审方向的首个闭环 | F00通过后，将方向落实到五类代表及令牌；先交P23/P24和进度弹窗正式双视口图、六态按钮与失败恢复，再实现真实Vue | 本页设计意见关闭；真实输入/请求/返回、焦点/键盘、忙碌防重、失败恢复及共享消费者回归通过；图分别标design与vue-isolated。三主题、两密度、断点与动效规则确定；记录实测工时，不重做无变化的独立研究图 |
| F06 全站逐页交付 | 沿W02–W08，以一个共享入口族或2–4页为小批，规格→新图审核→Vue→定向及包级验证→文档同步→精确提交。F04后续规格可与获审页面交付交错安排 | 每批主页面、实际菜单/按钮、全部适用弹窗与错误恢复同批交付；已有26份规格也须走完整链。J08/J09/J10、CP-G01–CP-G07及旧合同缺项逐一关账；不只增加测试或图数。G1/G2/G3按页通过，再汇总全站 |
| F07 真实验收、发布、签收 | 全站G0–G3达标后按第6–7节核对真实后端、六角色、范围切换和核心链；线上base SHA、迁移白名单执行记录、恢复材料、生产窗口、发布归属均核实后使用既有宝塔部署器 | 生产版本/健康及全部适用路由证据同SHA；高影响动作有允许范围或明确未覆盖说明，隔离数据清理有结果；G4后交用户逐页签收G5。缺少真实验收或用户意见不能宣布第二阶段完成 |

#### 第一批操作顺序与检查点

1. 读取当前AGENTS、Feature Map与F01直接相关蓝图；记录branch/HEAD/status，核对依赖和4101/5173端口，不安装已有依赖、不停止未知服务。
2. F00确认是否已有新的审图意见，复用其记录；没有新意见不机械重复提问或把默认选项记通过。先完成F01的真实字段/角色/状态盘点，再补两份规格及必要永久回归；本次不要求重新制作A/B图。
3. F01先跑一个直接覆盖关键合同的最小用例，再按受影响模块桌面→移动验证；源码改变则补类型/构建/组件边界和相关消费者，只有文档改变不重跑全站业务测试。
4. 同步有变化的Feature Map、API/配置/文档；没有运行契约变化时明确OpenAPI/.env不适用。核对临时产物及进程，精确暂存本批文件，验证通过后提交。
5. 交接列本批Pxx与caseId、实际证据层级、未完项、commit、图的待审状态和下一批。F01成功后的规格应为28/73、剩45份，用户通过数不自动变化；F02接续，方向通过则安排F05，不等待全47份规格补完。

#### 缺口、估算和审图约束

- 已提交竞品合同的CP-G01–CP-G06涵盖焦点、阈值/币种、快照窗口、缓存/异步归属、提交竞态和删除反馈；CP-G07包括最终设计与真实验收。作为P19/P20进入实现的检查输入，不把待验证疑点直接定为已复现缺陷，也不借修复扩大规则。
- 正式图不能继承旧布局作为默认答案；每页说明旧问题、新信息顺序、主次按钮和桌面/移动差异。已有Vue截图只是旧版基线，注入CSS研究图不算最终实现。全套至少146个设计槽位+146个Vue槽位，弹窗、按钮板和异常态另计，生产146份路由视口证据按PLAN第7节的环境口径处理。
- 当前26/73是规格计数，不是阶段完成率。初始23–40有效工作日不作为剩余承诺；F05后用实际分项工时重估，按页面复杂度分别估算，并单列审核返工、真实后端、回归和生产窗口。进度只更新PROGRESS/审核记录，不为了每两个新规格再次重编完整计划。
- J07需要用户决定统一五项质量门还是维持独立采纳规则；具体规则变化需另追调用链并确认实现范围。正式风格待审和J07是两个不同依赖，互不替代。

### 3.9 1.11工作卡（历史，当前按3.10领取）

本轮只制定计划。完整W00–W09范围仍按第2节及PLAN第5节执行；3.8卡片的合同要求继续保留，以下更新先后顺序、输入和退出条件。实施者负责交付与自检，用户负责方向、业务边界和签收，默认单执行者串行。

| 顺序 / 卡片 | 真实入口与操作 | 必须交付 / 退出条件 |
| --- | --- | --- |
| R00 增量证据维护 | 3aaf061的4b83588来源收尾已完成，不重新领取；从fa09f39的AutomationRuleCenter预览变更及后续稳定源检查实际影响 | 按需要刷新清单、候选映射和受影响证据；未采证部分明确来源，检查不过不能标全站通过。未变独立A/B图复用；此项不增加正式页面完成数 |
| F00 方向审核 | 3.8中的两个候选审核入口；对比导航、首屏、列表/详情、弹窗、移动主行动及旧版结构 | 用户对具体版本选择、修改或全部重做；意见关联版本和页面。无答复仍pending；不反复机械提问。F04可继续，F05等待明确方向 |
| F02/F03 已交付规格，不重复领取 | 4b83588的P25/P26与fa09f39的P27/P28、各族contract-review及PROGRESS | 复用四份规格和有效局部用例；AN-G、AR/RP-G等未验项进入F06。正式图、全行为、真实后端、生产及用户签收没有因此通过 |
| F04 其余41份 | W04 P29/P30/P32–P37；W05 P38–P45；W06 P46–P53；W07 P54–P60/P63；W08 P62/P64–P71。复用P31/P61 | 8/8/8/8/9份无遗漏，每页追入口、字段、角色、handler/API及全部适用状态；与动态菜单/共享调用方语义归并后才冻结G0。32→40→48→56→64→73 |
| F05 代表闭环 | F00通过后按3.8先做P23/P24及进度弹窗的正式双视口图→用户意见→真实Vue，并检查全部共享消费者 | 正式令牌、三主题/两密度、断点、按钮六态、弹窗键盘/焦点/失败恢复；可运行页面与已审图一致、定向回归通过。记录分项工时重估剩余工作，不能将代表页通过推为全站通过 |
| F06 全站逐页交付 | W02–W08，每批2–4页或一个共享入口族；已有32份规格同样进入完整新设计链 | 主页面、菜单/按钮、弹窗变体、适用异常态同时交图和实现；J08–J10、CP-G、SC-G、AN-G、AR/RP-G及其他合同缺项逐项复现/关闭或明确阻塞；G1/G2/G3按页判定后全站汇总 |
| F07 发布与签收 | 全站G0–G3通过后，执行第6–7节的真实后端/六角色/范围切换、线上base、迁移白名单、恢复材料与归属检查，再由既有宝塔部署器发布 | 同SHA生产健康、路由与动作证据；高影响动作的最终执行有对应授权或明确未覆盖，隔离数据无残留；G4通过后用户逐页签收G5。无签收不得称第二阶段完成 |

#### 下一实施轮的直接操作

1. 读取AGENTS、Feature Map组织管理条目和蓝图4.1；记录branch/HEAD/status，确认没有他人混合改动。不重做F01–F03规格；R00仅登记实际增量失效范围，在稳定源节点更新。
2. 核对F00意见；没有新意见仍可推进F04组织治理。依次P29/P30（复用P31）→P32/P33/P34→P35/P36/P37，补八份十项规格及独立动作/弹窗合同；每小批明确交付，不把共享入口算一个页面。
3. 读取OrganizationAdminCenter及对应Member/Workspace/Team/Approval/Data/Token/Audit子组件，追organization-admin-routes/service/repository及审计链，复用m06-01-organization-admin。尤其P34依据蓝图实现基线只读治理，不能按PAGES旧概念描述添加模板编辑/发布；其余旧矩阵拟议动作也逐项追真实handler，不据文案造接口。验证当前组织/能力、准确原因和版本、取消零写入、失败保留、令牌明文退出及脱敏。先复现再必要修复；最小用例通过后组织模块桌面→移动串行验证，共享变更再覆盖实际消费者。启动前检查4101/5173及健康，复用依赖，不停止未知服务。
4. 若改产品源，同步相关地图/运行文档、类型/构建/组件边界与消费者回归，提交稳定源码后再刷新受影响证据；若只有文档，检查路线矩阵、路径、命令及文档门，不重跑无变化的业务测试。任何API/配置/迁移变化均先检查授权边界。
5. 检查并清理本批自建临时文件与进程；策略拒绝的清理不得绕过，记录精确路径。精确暂存、验证后提交。交接标Pxx/caseId、已交与未验、commit、审核状态和下一批；组织八份交付后为40份规格、余33份，不等于40页重构完成。随后领取平台账号P38–P45；若F00有明确方向意见，优先插入F05可审核的新图→Vue闭环。

本计划没有新增或调整API、路由、参数、环境变量、数据库或依赖，不需要OpenAPI/.env/Feature Map同步、迁移或重启。本轮未创建临时文件和测试服务，历史14批目录不动；全局图与清单的增量刷新属于实施工作，不在本次文档更新中伪造验证结果。

### 3.10 1.12接续卡（历史，当前按3.11领取）

本轮只改计划。下列操作在进入实施任务后执行，默认单执行者；未经用户明确要求不启动子代理。先接续在途批，不覆盖或重复新建现有八份规格；以下“退出条件”是要求，不是已经通过的结果。

| 卡片 / 顺序 | 具体操作及输入 | 退出条件与下一步 |
| --- | --- | --- |
| H00 在途归属核对 | 记录当前branch/HEAD/status，对照PLAN 2.6的14个路径及完整diff。读组织合同、八份规格、父组件、组织E2E、地图与运行文档；查既有测试记录，不将用例存在视为通过 | 明确每项来源与待验项。文件混有其他任务改动且无法分离时，先询问提交范围；不执行stash、reset或清理未知文件 |
| H01 组织批收尾 | 补合同末节真实源码指纹，逐项核对108候选/54绑定映射与八页十项规格；复用已有效的定向结果，无有效证据才重测。随后组织模块桌面→移动串行；令牌明文取消/离开/迟到、角色冲突、只读入口、轮换/撤销分别核验 | 类型/构建/组件边界、相关文档与代码门通过；PROGRESS记录通过/失败/跳过及真实未覆盖。只清理本批可确认归属的临时对象；精确提交后才称40份规格已交付。OG-G01–OG-G06不因局部通过自动关闭 |
| R01 增量证据 | 从已提交4b83588来源检查fa09f39及后续组织稳定提交的实际影响；按生成器维护清单，核对受影响候选/矩阵/来源，必要时真实重新采图 | 源码与证据版本匹配；无变化独立A/B图复用。不得只改hash或全局计数冒充采证；G0仍须运行时语义归并 |
| F00 方向决定 | 提交两个已有候选审核入口及旧版对照；审导航、首屏、列表/详情、操作层级、弹窗、移动结构，允许否定全部方案 | 用户意见关联具体图版本。无意见仅阻塞正式新视觉；可继续无争议合同盘点。J07业务决定单独记录 |
| F04 剩余规格 | W05 P38–P45八页 → W06 P46–P53八页 → W07 P54–P60/P63八页 → W08 P62/P64–P71九页；复用P31/P61。按PAGES入口及Feature Map追真实组件和API | 40→48→56→64→73；每页独立十项规格，全部真实按钮/弹窗/状态有归属。旧稿没有对应handler的操作不设计成已可用；全站G0归并冻结不靠静态数量凑数 |
| F05 新视觉代表闭环 | F00通过后优先P23/P24及进度弹窗正式桌面/移动图→用户意见→真实Vue；同步五类代表结构、设计令牌、共享按钮六态和弹窗合同 | 页面与已审图一致，焦点、失败恢复、防重、范围归属及共享消费者回归通过。记录实际设计/返工/实现/验证工时，重新估算剩余工作；不等33份规格全部写完 |
| F06 全站完成 | 按第2节W02–W08范围，以2–4页或一个共享入口族为批次，执行第4节任务卡；已交付规格的页面也须全新设计与实现 | 每批交正式图、真实Vue图、动作/弹窗/异常态、测试和审核意见。既有J/CP/SC/AN/AR/RP/OG等合同缺项逐项关闭或明确阻塞；不能只交规格和测试而无新布局 |
| F07 发布签收 | 全站G0–G3通过后按第6–7节验证真实后端/六角色/范围与业务连续链，再核对线上SHA、发布归属、迁移白名单、恢复材料和维护窗口 | 既有宝塔部署器发布，同SHA生产证据与隔离数据清理通过后交用户逐页签收。用户未审核、不具备真实执行条件的高影响动作明确列未完成，不能自动视为通过 |

#### H01验证执行卡

启动前检查4101/5173和服务健康；不停止未知进程，不重复安装依赖。以下测试输出目录仅在实施时创建并登记归属。本轮计划不创建这些目录，不启动测试服务。

```text
npx --offline --no-install playwright test tests/e2e/m06-01-organization-admin.spec.ts --project=desktop-chromium --output=output/playwright/ui-phase2-organization-governance-20260908/final-desktop
npx --offline --no-install playwright test tests/e2e/m06-01-organization-admin.spec.ts --project=mobile-390 --output=output/playwright/ui-phase2-organization-governance-20260908/final-mobile
npm run build:web
node --test tests/unit/frontend-component-boundary.test.mjs
npm run verify:frontend-budget
npm run verify:docs
npm run verify:runtime-docs
npm run verify:static-analysis
npm run format:check
git diff --check
```

先确认定向验证有效，再运行以上模块与配套门。已有文件/运行状态未变且证据完整时复用结果；失败先定位并最小复测，不先跑全站。既有用例若跳过须记录用例名、原因和替代验证，不称零遗漏。组织局部修复不要求重跑无关Node/Python业务，但真实后端与全站G3仍是F07前置，不以本地夹具代替。

在途临时根目录是`output/playwright/ui-phase2-organization-governance-20260908`；处理前解析到本仓库绝对路径、确认原任务归属及无链接跳转。只处理已登记且可安全删除的临时产物，保留永久规格/测试/审核图。历史14批目录和被策略拒绝的清理不重试、不绕过。发布工作树必须干净，单独提交计划不代表产品变更已收尾。

#### 每批提交给用户的固定审核包

每批报告列pageId与路由，提供旧版/正式新图/真实实现对照、实际按钮清单、每个弹窗变体和错误恢复图、测试环境与结果、未覆盖项、提交SHA；意见按同一pageId/actionId/dialogId闭环。图缺失或尚未审核维持pending，不用测试数量抵扣。控件通用六态板可共享，但每个业务按钮的可见、可达和副作用要独立验收。

#### 计划本轮验证与边界

本轮只检查73路由与W02–W08分包无遗漏重复、32已提交/8在途/33缺失的计数、引用文件与npm脚本存在、计划差异及文档门。`verify:plans`检查原P00–P08产品计划，不单独证明本UI第二阶段计划完整；因此另做本计划矩阵和链接检查。不修改API/配置/数据库/依赖或图稿，不需OpenAPI/.env更新、迁移、重启或部署。在途14个文件以修改前后hash核对保持原样，计划两文件单独提交。

1.12本轮实际检查结果：73个Pxx唯一、W02–W08范围合计73且无重复；磁盘40份规格、HEAD32份；两份计划11处本地Markdown链接存在，所有引用npm脚本存在。`npm run verify:docs`通过（73路由、60受保护、6角色、153必需文件），计划两文件`git diff --check`通过。本轮没有执行组织业务测试、构建、真实后端或生产验证，以上检查只证明计划引用及结构，不证明在途实现正确。

### 3.11 1.14实施卡（历史，当前按3.12领取）

本轮只制定计划，以下均为下一实施任务的操作。保留第2节完整W00–W09范围，不把剩余33份规格当作最终目标。先查看PROGRESS和Git，已经完成且证据仍有效的步骤不重复执行。

| 顺序 / 任务 | 输入与操作 | 产物及退出标准 |
| --- | --- | --- |
| 1 / F00 新结构审图 | 复用现有审核台；旧A/B已展示但尚无选案，不把再次发链接当新成果。以P23/P24、P17、P31、P61为代表，补足真正不同的构图；可先用W05事实清楚的页面提供未批准结构样稿 | 同任务的旧/新桌面和移动结构可比较，明确导航、首屏、列表/详情、操作和移动转换差异；用户可通过、改稿或全部否定。意见绑定图版本，不默认采用A/B；有意见即优先步骤4 |
| 2 / F04-A W05事实合同 | 按下面四个入口族补P38–P45，复用H02合同与有效回归；先复现PA-D01–PA-D03相关未验项，不把已修好的GET问题再列待实现 | 八份独立十项规格、局部语义动作/弹窗/输入合同、实际源码与测试索引；40→48。没有新设计或真实后端证据的项保持待验，不按文档数算重构完成 |
| 3 / F04-B 与R01 后续事实及证据 | W06八页→W07八页→W08九页；按Feature Map追入口及真实调用链。稳定产品提交后核对4b83588以来自动化、组织及账号辅助模块的增量证据 | 规格48→56→64→73；运行时语义归并后冻结G0；生成证据的源码/样式/夹具及图哈希一致。不只改hash，不补造旧矩阵中的分页、保存或业务指标 |
| 4 / F05 代表页闭环 | 方向意见明确后，先P23/P24和实际进度弹窗：正式图→用户意见→Vue实现→定向测试；同步其他代表构图、令牌、按钮六态和弹窗合同 | 正式图/实现对照可审核；焦点、错误恢复、防重复、范围归属及共享消费者验证通过。记录实际工时重估剩余批次，不等全部规格完成才启动 |
| 5 / F06 全站逐批交付 | 按W02–W08，以2–4页或一个共享入口族重复第4节任务卡；已有40份规格的页面同样重设计 | 每批同时交主页面双视口图、全部真实按钮/弹窗/适用异常态、Vue、测试和意见闭环；不以局部修复或文档交付代替新布局 |
| 6 / F07 真实验证与发布 | G0–G3通过后检查真实后端、六角色、跨范围与连续业务链；按第7节预检线上SHA、迁移白名单、恢复材料、维护窗口与授权范围 | 宝塔固定目录发布；同SHA生产证据、健康及隔离数据清理通过后逐页签收G4/G5。未执行高影响动作、未审核页面如实保留，不自动通过 |

#### F04-A四个可实施入口族

| 小批 | 文件与真实调用链入口 | 必交付与重点验收 |
| --- | --- | --- |
| A1：P38/P39 | PlatformDashboard、PlatformAccountCenter；沿Feature Map的概览与账号API入口读取真实路由/服务/仓储 | 平台概览与账号概览两份规格；指标范围、null与0、刷新失败旧快照、不同角色跳转；逐项辨别纯读取及读取附带审计，不凭GET声称数据库零写入 |
| A2：P40/P41/P42 | PlatformOrganizationRecords、OrganizationCreationWizard、PlatformOrganizationDetailDialog、父入口；platform-account-routes/service/repository | 列表、两步创建、组织详情三份规格；直达/返回/筛选、校验、创建失败、编辑及状态原因窗。按真实API核实分页与详情来源，不按旧矩阵假定存在分页或独立详情GET |
| A3：P43/P44 | PlatformUserRecords、PlatformAdminRecords、PlatformAccountDialogs、PlatformUserDetailDialog、PlatformUserMembershipForm、use-platform-user-detail | 用户与管理员两份规格；身份/角色/组织关系、创建、密码、会话、赋撤权及原因变体；复用H02，另验证写完成后关闭/切人/离开窗口归属。不得自动重放写入或改变自我保护规则 |
| A4：P45 | PlatformRoleComparison与父入口；真实角色目录GET和固定能力目录 | 权限比较一份规格；双角色选择、差异筛选、URL历史与空/失败；只按现有能力设计，不添加无契约的保存或权限编辑按钮；同时复核P44共享消费者 |

上述组件位于`apps/web/src/components/`，详情辅助模块位于`apps/web/src/use-platform-user-detail.ts`；账号后端位于`apps/api/src/platform-account-routes.ts`、`platform-account-service.ts`、`mysql-platform-account-repository.ts`。新增八份规格目标为`page-specs/P38.md`至`P45.md`，全族合同建议保存为`platform-account-contract-review.md`，复用已有`platform-account-detail-contract-review.md`，不覆盖其历史证据。

每个小批按“源事实→动作/弹窗变体→现有测试缺口→必要复现→规格及退出记录”交付。仅规格改动做链接/ID/源映射检查，不重复跑未改变的业务套件；若有必要产品修复，先定向实例，再跑`tests/e2e/m06-01-platform-accounts.spec.ts`或`m06-02-platform-dashboard.spec.ts`的受影响模块，桌面与mobile-390分别验证。共享壳层追加`m02-03-navigation-shell.spec.ts`及全部消费者；随后执行PLAN第7节对应类型、构建、边界、文档和格式门。所有测试串行，检查4101/5173健康后复用或由既有测试配置启动；不重复安装依赖。

H02在5192de2已完成，其UI2-PA01/PA02五个读取时序实例及模块双视口各21项通过来自PROGRESS历史记录。本次没有重跑；后续仅在相关源码、测试或环境变化使其失效时复测。读取保护不等于写完成回调、所有密码表单或真实后端权限均通过。

账号历史临时目录为`D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\ui-phase2-platform-accounts-20260908`（上批记录24文件）；组织历史目录为`D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\ui-phase2-organization-governance-20260908`（上批记录43文件）。连同更早批次共16批已交接材料，本轮未创建或清理它们。下一实施轮单独登记新增产物，所有既有被拒绝的删除不重试、不绕过；不能把留下历史材料写成清理完成。

每次交付向用户说明三种状态：本批事实合同/局部修复是否完成、正式新设计是否已审核、真实环境/生产是否已验证。每批最后提交固定审核包（3.10所列格式仍适用），下一步优先处理审核意见和阻塞缺陷，再领取新页。J07质量门业务决定独立于视觉意见；只有相应链路等待决定，不阻塞其他明确范围。

本次计划校验范围：73路由与W02–W08分包一致且无重漏、HEAD及磁盘40份规格、33份缺口、计划本地链接/npm脚本、W05文件与测试入口、文档门和diff检查。不修改产品、API、环境、数据库、依赖、Feature Map、PAGES、全局源指纹或图稿，不需OpenAPI/.env同步、重启或部署；计划两文件独立提交，不代替产品验证。

1.13历史检查：73个Pxx与路由目录逐条一致，无遗漏重复；W02–W08数量依次19/11/9/8/8/8/10；HEAD和磁盘均40份规格、缺33份。两计划11处本地Markdown链接和15个引用npm脚本均存在。文档门通过（73路由、60受保护、6角色、153必需文件），计划diff检查通过。此段保留历史边界，不表示1.14已重新运行产品测试或检查生产。

1.14本轮实际核验：73条路由逐项匹配真实目录、41个组件入口存在，W02–W08分包19/11/9/8/8/8/10无重漏；HEAD与磁盘均40份规格，缺33份。两份计划11处本地链接、15个npm脚本及W05组件/三个E2E文件入口有效；coverage仍为分母未冻结、用户通过0。`npm run verify:docs`通过（73路由、60受保护、6角色、153必需文件），diff检查通过。本轮仅更新两份计划，未重跑产品构建、业务测试、采图或生产验证。需求拆解技能用于工作卡与退出标准，frontend-design技能用于结构差异及先审图要求，不替用户选定风格。没有新增临时产物、服务或浏览器进程；历史临时材料未触碰，无新清理、配置、迁移、重启或发布事项。

### 3.12 1.15实施卡（历史，当前按3.13领取）

本次任务是制定计划，下表是后续实施安排，不是已经执行。沿用W00–W09全量范围和第4–7节的状态、审核、测试、发布规则；先核对当时Git和PROGRESS，避免重复已交付工作。所有实施默认单执行者，不自动创建子代理。

| 卡 / 依赖 | 实际入口与动作 | 永久产物、退出条件及下一卡 |
| --- | --- | --- |
| F00-C / 立即可审 | 打开[现有C研究稿](design/account-direction-c/index.html)及其README，核对evidence.json版本；接收通过/修改/全部否定意见 | 意见关联具体图版本及结构变化。未变化时只运行`node scripts/verify-ui-phase2-account-direction.mjs --check`复验，不重采18图；未获审可继续A1–B3，但不得自动套用C。获审转F05 |
| A1 / 事实线起点 | P38 PlatformDashboard、P39 PlatformAccountCenter；沿Feature Map定位dashboard/accounts的后端路由、服务、仓储与现有测试 | 新增`page-specs/P38.md`、`P39.md`；记录指标范围、null/0、刷新保留旧快照、权限跳转及读取审计；不因GET判定零数据库副作用。规格41→43 |
| A2 / A1共享入口已清楚 | P40–P42父入口、PlatformOrganizationRecords、OrganizationCreationWizard、PlatformOrganizationDetailDialog | 三份规格，列表→两步创建→详情→返回/筛选的完整合同；核实最多200条概览的详情来源、创建返回字段、编辑/状态原因窗，不造分页或独立详情GET。规格43→46 |
| A3 / 复用已交付P43 | P44 PlatformAdminRecords及P43已有四组件合同、use-platform-user-detail、父入口写入回调 | 新增P44规格；复核管理员候选、赋撤权、密码/会话/组织关系；保留P43的33直接候选和9输入事实，不当作全族分母。规格46→47；PA-D01按下述时序独立复现 |
| A4 / A3共享角色目录 | P45 PlatformRoleComparison与父入口、真实角色目录；连同P44嵌入消费者核对 | 新增P45规格；双角色、差异、搜索、重置、URL前进后退和角色目录失败；只读比较不设计保存权限。规格47→48 |
| A5 / A1–A4 | ResponsiveDataView、ResponsiveFilterDrawer、TableViewControls、TechnicalDetails、useModalDialog及W05所有调用方 | 新增`platform-account-contract-review.md`汇总八页父/子入口、共享控件、菜单、弹窗定义/调用/变体、输入与未验项；交叉链接P43及H02已有合同。每个候选有语义归属，共享定义去重但调用方变体不遗漏 |
| B1 / W05事实交付 | W06 P46–P50 ProviderRuntimeSurface，P51–P53 CollectionRuntimeSurface；按真实子组件/API逐页追踪 | 八份规格、来源/凭证/门禁与任务/浏览器的动作弹窗表和验证清单，48→56；账号/凭证和第三方操作需隔离，不改变来源启用规则 |
| B2 / B1 | W07 P54 PlatformDataCenter、P55 PlatformGovernanceCenter、P56/P57/P63 PlatformManagementCenter、P58 CommercialOperationsCenter、P59 SecurityOperationsCenter、P60 OpenPlatformCenter | 八份规格和消费者合同，56→64；按数据/运营/安全/开放平台拆2–4页小批；停用能力、权限、审计和外部发送范围明确，不虚构可用动作 |
| B3 / B2 | W08 P62 PlatformLogCenter、P64 BackupRecoveryCenter、P65 ReleaseRolloutCenter、P66 RuntimeTopologyCenter、P67 RedisResilienceCenter、P68 MySqlResilienceCenter、P69 FileResilienceCenter、P70 CrawlerSchedulerCenter、P71 CapacityBoundaryCenter | 九份规格，复用P61，64→73；按日志/恢复发布、资源韧性、调度容量拆批。只核对现有单机能力，真实恢复/删除/发布不在盘点中执行 |
| R01 / 稳定产品源；可随小批推进 | 4b83588之后AutomationRuleCenter、OrganizationAdminCenter、账号详情辅助模块及实际diff；PAGES、baseline/actions/dialogs/coverage和关联Vue证据 | 纠偏旧矩阵并重新生成受影响清单、实际采证；A/B/C独立研究源未变可复用。冻结G0前动态动作/角色/变体全有结论；不得只换hash或把候选数当行为数 |
| F05 / F00方向获审，相关规格已核实 | P23/P24 TaskWorkspace及真实进度弹窗，扩展P17规则/P31权限/P61恢复和三壳层导航代表设计；共享令牌/控件 | 正式桌面移动图先审再实现Vue；每个真实动作有测试和证据。先完成一个代表闭环，不等待B1–B3全部完成；补控件六态、主题/密度及审核台，记录实耗重估余量 |
| F06 / F05底座可用 | 按W02→W08的PAGES分包，2–4页或一个共享入口族，已有41份规格页同样重构 | 每小批交正式图、Vue对照、动作/弹窗/状态、测试和用户意见；共享组件变更复核全部消费者；每包精确提交，不以规格数量抵扣页面完成 |
| F07 / G0–G3及发布条件齐全 | 按本手册第7节逐项核对线上SHA、迁移执行记录、发布区间所有权、隔离数据、维护窗口及恢复材料 | 既有宝塔部署→同SHA生产路由/核心链/动作证据→清理→用户签收；G4/G5未经核验不通过，缺少任何发布前置项不发版 |

组件入口均在`apps/web/src/components/`；辅助模块在`apps/web/src/`；后端入口以Feature Map定位，不从研究稿推导接口。新合同/规格采用现有目录，不另建第二套计划或修改全局设置。八页完成规格只是W05事实线退出，正式设计、Vue和生产验收仍按F05–F07执行。

#### PA-D01与共享交互的实施验收卡

1. 在隔离Vue用例挂起甲账号的状态/平台角色/会话或组织关系写请求，关闭窗口、打开乙、导航共享入口或离开KeepAlive页面，再释放成功/失败；分别检查当前窗口身份、反馈归属、旧窗是否复活及发出请求次数。先复现，不将H02的GET保护误认为写入已解决。
2. 如果现有契约下证实错误，实施最小归属保护并加永久回归：请求目标/请求体保持原语义，不自动重放写入；后台已成功的操作不能因关闭窗被表示成撤销。没有证据不预先宣称缺陷或修改业务规则。
3. A5逐调用方检查名称、Tab/Shift+Tab、Escape、初始焦点、焦点归还、嵌套模态、长表单、字段错误关联、提交中重复触发和失败重试。原型焦点通过不能证明生产useModalDialog通过；视觉44px检查不替代键盘/辅助技术验证。
4. PA-D02/03覆盖列表与角色目录失败、URL前进后退、筛选外组织详情、关闭密码表单、切换壳层/卸载和真实权限拒绝。先记录实际行为与契约，涉及新的持久化、权限或API决定时暂停该项并明确需要用户选择。
5. 规格仅引用真实字段；例如缺失计数、暂无样本和0必须区分。截图可以用明确标注的合成数据，禁止把演示反馈当作账号创建/角色变更或真实审计通过。

#### 验证顺序、审阅包与停止点

- 文档批：内联核对Pxx唯一、实际路由/组件/文件存在、十项规格结构、候选/输入对应及本地链接，再运行`npm run verify:docs`和`git diff --check`；不重跑未变化的产品测试。
- W05产品改动批：先现有`tests/e2e/m06-01-platform-accounts.spec.ts`或`m06-02-platform-dashboard.spec.ts`的新增/受影响实例，再分别执行desktop-chromium、mobile-390所属套件；共享导航追加`m02-03-navigation-shell.spec.ts`和相关消费者。随后按变更执行typecheck/build:web、组件边界、frontend-budget、static-analysis、docs/runtime-docs及format门。按序复用依赖、端口和有效测试，不并发污染夹具。
- F05/F06每页审核包：旧参考（注明来源）→正式新图双视口→真实Vue双视口→按钮六态及调用差异→全部适用弹窗/菜单/异常态→源码版本与caseId→失败/未测项→用户意见。缺图和未回复不得标通过；驳回后保留原版本及意见，修订同一ID。
- W09：重读`verify:functional`聚合命令，运行全站门并补未包含的预算/运行文档/真实性门；真实后端、六角色、跨范围、生产写入和截图分环境留证。预检命令不算生产验收，恢复材料不足或有未授权迁移时停止发布。
- 收尾：每批记录临时文件精确路径及新进程，清理自建临时物后检查Git；永久图稿/规格/测试保留。历史16批未清理材料不重试删除；本次没有新增临时目录或服务。代码/合同变化时同步相关map/docs/config；纯计划修订不动运行配置。

需审的决定只有具体视觉/交互版本、J07业务规则和届时发布/迁移范围，分别记录；普通局部实现细节自主处理。若用户否定C，返工结构研究而非换色后直接应用；若无审图意见，继续事实线并如实说明正式设计待审，不把整个阶段宣称完成。

1.15本轮实际检查：73个唯一Pxx及路径与真实路由目录、PAGES逐条匹配，41个组件入口存在；W02–W08为19/11/9/8/8/8/10条，无重漏。HEAD与磁盘均41份规格，缺32份，分别W05七份、W06八份、W07八份、W08九份；两计划17处本地链接、15个npm脚本及W05三份E2E入口有效。C研究只读校验通过18张图及源/图哈希，approval仍pending；没有重新采图。文档门通过73路由/60受保护/6角色/153必需文件，diff检查通过。

本轮仅修订两份计划；没有修改产品、API、数据库、依赖、环境变量、Feature Map、PAGES、全局清单或审核记录，因此不运行未变化的构建/业务E2E，也无需配置/OpenAPI同步、迁移、部署或重启。需求拆解技能用于完整工作卡及退出标准，无障碍技能用于逐控件/弹窗的验收要求，并非本轮实际无障碍测试通过。没有创建临时文件、截图、日志、服务或浏览器进程；旧16批材料未触碰。计划文件为永久交付，提交时只暂存这两份文件。

### 3.13 1.16实施卡（历史，当前按3.14领取）

下面各卡为后续执行任务，不表示本轮已实施。A1–A5事实交付已由8503d74完成，不重复补七份规格；其正式设计、行为、真实权限与生产验收仍未完成。默认单执行者，当前已有6个在途文件不纳入本轮计划提交。

| 卡 / 前置 | 要做的事与文件入口 | 必须交付 / 退出条件 | 失败或缺条件时 |
| --- | --- | --- | --- |
| S0 在途收口 / 实施前 | 核对PlatformAccountCenter.vue、use-platform-user-detail.ts、m06-01-platform-accounts.spec.ts及三份相关文档的归属和实际diff；读PA-D01增量 | 四类用户详情写入归属修复的定向及完整桌面/移动结果、build:web/预算/边界门、文档与32源合同一致性；源变更影响C manifest时实际复验/采证；精确清理本批临时物后单独提交 | 归属混杂且不可分先问；验证失败只修本卡。没有实际结果不把在途代码记完成，不把四类写入保护称全部PA-D01解决 |
| S1 剩余交互 / S0明确 | PA-D01/02/03：密码、创建、组织资料/状态、共享原因窗生命周期、列表与角色目录刷新、直接深链及真实拒绝；按W05合同逐场景拆用例 | 每个疑点有复现或未复现证据；确认缺陷最小修复并补永久回归；权限/API/持久化保持现有合同 | 前端隔离测试不代替真实MySQL、鉴权和审计。需要业务决定暂停该项，其他卡继续 |
| F00 方向审核 / 可立即准备 | 复用现有版本及来源说明，核对图源是否过期；审导航、首屏、任务详情、规则、权限、恢复和移动结构 | 用户针对具体版本通过/修改/全部否定；获审后转F05。未变研究素材不重复生成计进度 | 未获审保持pending，可推进B1–B3与无争议验证；不能自动选C |
| B1 / W05事实已交付 | P46–P50 ProviderRuntimeSurface及真实子组件，P51–P53 CollectionRuntimeSurface；追API/服务/状态机 | 八份十项规格，动作/输入/模态变体合同、角色与凭证/第三方操作边界；规格48→56 | 不执行真实凭证、来源启用或第三方采集凑覆盖，不补造API |
| B2 / B1 | P54数据、P55治理、P56/P57/P63管理入口、P58商业运营、P59安全、P60开放平台 | 八份规格与共享消费者表，56→64；明确只读、禁用、审计、外发及真实字段 | 未支持能力诚实展示，不因新设计启用停用功能 |
| B3 / B2 | P62日志、P64恢复、P65发布、P66拓扑、P67 Redis、P68 MySQL、P69文件、P70调度、P71容量；复用P61 | 九份规格，64→73；日志/恢复发布、资源韧性、调度容量分组；危险动作隔离说明 | 不在盘点阶段执行恢复、删除或发布；不宣称高可用或已验证容量 |
| R01 / 各相关产品源稳定 | PAGES、baseline/actions/dialogs/coverage及变更影响的真实Vue图；沿现有生成器和合同核对 | 动态菜单、共享调用、角色/状态变体均有语义ID和caseId；旧候选映射无漏项；实际采证后冻结G0 | 静态数不作语义分母，只改hash不能通过；旧证据保持其历史来源 |
| F05 / 方向通过且代表事实已清楚 | P23/P24任务列表/详情与进度弹窗先闭环；补P17规则、P31权限、P61恢复及三壳层；App/NavigationShell/AccountShell和共享控件 | 正式图→用户审核→Vue→隔离与真实合同测试→对照审核；按钮六态、主题密度、焦点与动效规范可复用；记录实耗以重估排期 | 方向驳回回到结构设计；共享回归失败先修底座。不等待B1–B3全部完成才做代表页 |
| F06 / F05底座通过 | W02→W03→W04→W05→W06→W07→W08，每次2–4页或一个明确共享入口族；按PAGES追真实入口 | 每页规格/正式图/Vue/全动作和弹窗/状态/测试/用户意见齐套；每小批单独提交，共享改动覆盖全部消费者 | 缺图、缺用例、未审核单独标记，不以其他页通过抵扣。所有48份已有规格页仍需正式重构 |
| F07 / 全站G0–G3通过 | 依第7节发布预检、真实后端/六角色、同SHA宝塔上线与复验 | ACCEPTANCE、生产证据、精确清理、回退与重启记录；逐页用户签收G5 | 不符合发布窗口、隔离条件、迁移许可或恢复能力就不部署；关键异常停止晋级并按已验证方案恢复 |

#### 首批S0的验证与非目标

先阅读已有测试结果及当前源码，证据仍有效且源未变化的定向用例不重复跑；缺少或已过期时先运行PA03/PA04对应最小实例。随后按顺序执行以下现有入口，测试产物使用执行时确认归属的临时目录，并记录准确路径：

```text
npx --no-install playwright test tests/e2e/m06-01-platform-accounts.spec.ts --project=desktop-chromium --workers=1
npx --no-install playwright test tests/e2e/m06-01-platform-accounts.spec.ts --project=mobile-390 --workers=1
npm run build:web
node --test tests/unit/frontend-component-boundary.test.mjs
npm run verify:frontend-budget
node scripts/verify-ui-phase2-platform-account-contract.mjs
node scripts/verify-ui-phase2-account-direction.mjs --check
npm run verify:docs
npm run verify:runtime-docs
npm run verify:static-analysis
npm run format:check
git diff --check
```

C研究的产品源hash若已过期，先复核事实与演示语义；确需更新时用现有`node scripts/verify-ui-phase2-account-direction.mjs --capture`实际生成证据后再check，不能手改hash冒充重采。S0只关闭已验证的四类写入反馈归属，不自动修复密码/组织/共享原因窗关闭，不改变请求字段、后端成功事实，不取消或重放写入；其余列S1。本轮制定计划未运行这些产品命令，不把此前部分结果冒充完整套件通过。

#### B1–B3及F05/F06的统一交付卡

- 输入：真实路由与组件、字段/API/权限来源、上一个有效源码版本、已批准设计版本或明确待审状态。
- 动作：逐页十项规格；静态候选逐一归属并运行时补动态入口；按业务任务设计结构与全套状态；绑定pageId/actionId/dialogId/caseId。
- 输出：规格及合同、桌面1440/移动390正式图和Vue图、按钮六态、弹窗各业务变体及适用异常态、测试记录、用户意见、未覆盖清单。768/1024及断点邻界、三主题两密度、键盘/200%缩放按PLAN补验。
- 退出：文档批核对页ID/路由/十节/链接/源码及`verify:docs`；代码批先定向再完整所属包与共享消费者，执行build/预算/static/docs/runtime/format等相关门；失败先定位复测，不提前提交。
- 收尾：同步直接相关Feature Map/运维文档；API/SQL/.env无变更则明确不适用。只暂存本批文件，清理自建临时物/进程，保留正式交付图与永久测试；已存在且归属不明材料不删。
- 交接：报告页数与动作/弹窗分母、实际通过与未测项、图路径、commit、无需重启或具体发布要求、下一卡。全套图和用户签收必须单列，不能用事实规格数量抵扣。

#### 发布卡与计划本轮边界

F07执行前读取`verify:functional`当前聚合内容，补其未包含的前端预算、运行文档、E2E真实性等门；真实后端、隔离组织和六角色跨范围验收与mock图分开。发布前实际获取线上SHA/迁移账本、核对完整区间所有权和干净工作树，确认宝塔停止Node窗口与可恢复材料；执行现有部署脚本后复核公开SHA、live/ready/available、核心链和逐页证据。全局/外发/危险动作只在明确授权且可隔离时最终执行，否则如实列未执行限制。当前计划不触发生产连接、迁移或重启。

本轮只更新两份计划文件，保留开始时6个代码/测试/文档改动；不刷新旧清单、图源或用户审核状态。需求拆解技能用于任务入口、依赖、退出证据；frontend-design用于新结构与逐页差异要求，不代表本轮生成新设计图。

1.16实际计划验证：内存检查确认73个唯一页面ID、路径与真实路由目录及PAGES一致，所有组件入口存在；W02–W08分别19/11/9/8/8/8/10，无重漏；磁盘48份规格、缺25份；两计划17处本地链接、15个npm脚本及引用脚本路径有效。`npm run verify:docs`通过73路由、60受保护、6角色及153必需文档门；`git diff --check`通过。未运行产品构建/E2E、旧图hash验证或生产检查，不将其记为本轮通过。本轮没有创建临时文件、截图、日志或后台进程；两份计划是永久交付，旧临时材料及在途代码均未清理或提交。

### 3.14 1.18实施卡（历史，当前按3.15领取）

默认单实施者；本轮只更新计划。下列范围与PLAN 2.10一起使用，保留第4–7节的全套图、验证与部署合同。S0已由55a7439、B1a已由ed4e8ed提交，不重新领取；历史测试结果仅引用PROGRESS，若相关源未变不重复跑。下一实施轮先交可视方向审核材料，不能把23份事实规格全部写完作为第一次审图的前提。

| 卡 / 前置 | 真实入口与范围 | 必须交付 / 退出条件 |
| --- | --- | --- |
| B1a / 已交付事实与局部修复 | P46/P47；ProviderRuntimeSurface、ProviderRegistry、ProviderAdapterCenter | ed4e8ed已提交两页规格、合同、完整清除与永久测试；余下正式设计和PR-G项随对应页关闭，不重复48→50 |
| B1b / B1a，当前事实入口 | P48/P49/P50；ProviderSourceCenter、Alibaba1688AcceptanceCenter、CredentialAssetCenter | 三份十项规格；配置/回滚/样本/启用门/凭证逐实际handler列副作用和取消路径；API有登记能力不等于页面有按钮；不操作真实来源或凭证，50→53 |
| B1c / B1b | P51/P52/P53；CollectionRuntimeSurface及其实际子组件；m03-04-playwright-crawler、m03-05-collection-tasks、m06-03-collection-console | 三份规格，任务/汇总/浏览器状态、深链、重放/恢复合同与测试映射；总览不能复制列表规格，53→56 |
| B2a / B1事实收口 | P54/P55；PlatformDataCenter、PlatformGovernanceCenter | 数据与质量两份规格；范围、字段、治理操作、证据缺失、返回状态独立记录，56→58 |
| B2b / B2a | P56/P57/P63；PlatformManagementCenter及真实内容/通知/ApiCoverageDashboard子入口 | 三份规格；外发与审计证据边界；共享入口连同P61回归，58→61 |
| B2c / B2b | P58/P59/P60；CommercialOperationsCenter、SecurityOperationsCenter、OpenPlatformCenter | 三份规格；有效额度、安全处置、密钥/外发事实，不启用停用能力，61→64 |
| B3a / B2 | P62/P64/P65；PlatformLogCenter、BackupRecoveryCenter、ReleaseRolloutCenter | 三份规格；筛选/日志与恢复/发布分开，危险最终执行必须隔离，不做生产故障注入，64→67 |
| B3b / B3a | P66–P69；RuntimeTopologyCenter、RedisResilienceCenter、MySqlResilienceCenter、FileResilienceCenter | 四份规格；观察时间、未知/过期/失败、资源单位、真实单机边界，67→71 |
| B3c / B3b | P70/P71；CrawlerSchedulerCenter、CapacityBoundaryCenter；复核已有P61交叉入口 | 两份新规格；调度与容量真实状态和操作，不新增并发/重试/阈值，71→73 |
| S1 / 对应页面实施前 | W05合同PA-D剩余项：密码、创建、组织资料/状态、共享原因窗、列表/角色刷新及真实拒绝 | 拆成可复现小场景；缺陷最小修复/永久回归，未复现保留证据。不得把S0扩大为全部PA-D已完成 |
| F00 / 立即可准备 | 现有A/B/C研究入口及图源；三壳层、任务、规则、权限、恢复与移动结构 | 用户对具体版本给出通过/修改/全部否定。审图不等于业务规则决定，未回复不通过 |
| R01 / 相关源稳定 | PAGES、现有清单生成器、各族合同与Vue采证程序 | 源码→候选→语义动作/弹窗变体→角色状态→caseId全映射；实际重采失效证据；无漏项才冻结G0，不仅更新hash |
| F05 / 方向通过且代表事实清楚 | 先P23/P24及进度弹窗，再P17/P31/P61和真实壳层共享组件 | 正式图获审→Vue→最小/消费者测试→对照审核；令牌、三主题两密度、六态、焦点与动效规范；记录实耗重估后续工期 |
| F06 / F05底座通过 | W02–W08逐包，每批2–4页或一个明确共享入口族 | 每页十项规格、全套正式图/实现图、动作/弹窗/适用状态、测试和意见闭环，同批精确提交；50份已有规格页同样必须重设计 |
| F07 / 全站G0–G3及发布条件具备 | 第7节宝塔预检/发布/恢复；真实后端与六角色验收、审核台 | 同SHA构建、生产身份/健康/路由/允许动作/残留与回退证据；G5逐页用户签收。技术通过不能代替用户签收 |

#### B1b直接执行步骤：三页独立合同，不把配置保存当启用完成

1. 从Feature Map的providerSources、credentialAssets追到三页及共享弹窗、API路由/服务、真实权限与测试；先检查Git与运行状态。复用P46/P47合同，不把共享入口视为同一页面。
2. P48逐项追踪筛选/分页/URL、配置保存、公开来源烟测启用、版本回滚、解析样本创建/回放/双人复核、兼容矩阵及跨页链接。把读操作、持久化写入和可能外发探针分开；每个弹窗记录字段、版本锁、错误归属和返回焦点。
3. P49核对三个启用门、解析覆盖证据、组织/工作区与查询输入、创建验收任务和后续刷新。区分“任务已提交”“检查通过”“来源已启用”，不凭前端成功提示补造终态或自动轮询。
4. P50核对资产创建/轮换/撤销、运行档案、登录导入、浏览器助手、兼容矩阵和指定来源深链。只用隔离合成材料验证；记录明文、文件读取和助手回调在关闭/切换/离开后的生命周期，以及资产保存后档案失败的真实恢复路径，不读取真实Cookie。
5. 输出P48/P49/P50各十项规格及可追溯合同；候选、输入、弹窗、权限和测试逐项映射。疑点先复现，只有确认属于当前交付的缺陷才最小修复并加永久测试；业务、安全或持久化规则不明时停止对应改动，其他明确工作继续。
6. 先最小定向测试，再受影响模块桌面/移动回归；共享组件变更补消费者，产品变更补构建/预算/边界及必要文档门。若只交事实文档，不无变化重跑业务测试。核对来源hash与链接，清理本批临时物，按文件提交；规格50→53不等于三页新设计完成。

以下为后续实施时的现有命令入口，本轮未执行。输出目录在执行时选明确临时根并登记；启动前检查现有服务，桌面/移动串行：

```text
npx --no-install playwright test tests/e2e/m03-07-provider-sources.spec.ts --project=desktop-chromium --workers=1
npx --no-install playwright test tests/e2e/m03-07-provider-sources.spec.ts --project=mobile-390 --workers=1
npx --no-install playwright test tests/e2e/m03-02-credential-assets.spec.ts --project=desktop-chromium --workers=1
npx --no-install playwright test tests/e2e/m03-02-credential-assets.spec.ts --project=mobile-390 --workers=1
npm run build:web
node --test tests/unit/frontend-component-boundary.test.mjs
npm run verify:frontend-budget
npm run verify:docs
npm run verify:runtime-docs
npm run verify:static-analysis
npm run format:check
git diff --check
```

#### 完整图包、排期与停止点

每张正式页面图绑定pageId、视口、状态、版本与事实来源；每个业务动作/弹窗变体绑定actionId/dialogId/caseId。最少146份正式页面设计视口与146份真实Vue视口，额外详情、菜单、六态、表单错误/提交中/失败/成功、危险确认和恢复过程另计。三主题两密度及断点遵循PLAN，不用原型或静态扫描抵扣实际覆盖。审核台必须能从页面打开对应图、按钮和弹窗，不让用户在散落文件中自行找全。

F00方向审核与B1事实线同轮安排，不等待全部规格；获审后优先F05。未获审只阻塞正式视觉实现，不阻塞可安全核对的事实线。当前不承诺剩余天数；F05完成后按每类页面实测工时、返工、共享回归和真实验收重估，每批交付后更新剩余工作，等待审核/凭证/维护窗口独立列出。

发布前须重新读取部署脚本、线上SHA、迁移账本和所有权范围。默认W09统一发布；现有deploy-baota.py可能停止Node并执行白名单迁移，不添加虚构参数，不使用历史“允许”授权未知迁移。任何API/持久化/权限/业务规则冲突单独确认；J07仍独立待决。生产高影响动作没有明确隔离目标和授权时不得最终执行，也不记生产执行通过。

本轮只调整PLAN/EXECUTION，未改产品、未运行业务回归、未生成图或更新用户审批；无需OpenAPI/.env/Feature Map同步，无新配置、迁移、部署或重启。本轮没有创建临时文件、截图、日志或服务；历史18批临时材料按PROGRESS登记仍保留，不重试被拒绝的清理，不借旧测试宣称本轮产品验证通过。

1.17历史计划校验基线为48份规格和2草稿，不再代表当前状态。1.18只读内存校验通过：73个唯一页面ID与真实路由一致、41个组件入口存在、50份规格已跟踪提交、八个子批3/3/2/3/3/3/4/2恰好覆盖23份缺失规格且无重漏；六角色、19处本地链接、15个npm脚本及22处显式命令入口有效。`npm run verify:docs`通过73路由/60受保护/6角色及153份必需文档，`git diff --check`通过。coverage仍为分母未冻结、用户通过0；这些结果只证明计划结构和引用有效，不代表产品测试、新图或生产通过。

### 3.15 1.20实施卡（历史，当前按3.16领取）

本轮只交计划，未执行下列产品步骤。总体包定义见第2节及PLAN 2.11；既有逐页矩阵仍覆盖73项。3.14中的B1c–B3c、S1、R01、F05–F07文件与测试入口继续有效；以下卡明确当前状态和领取顺序，不重复F00制作、B1b规格或既有修复。已提交B1b结果以a728f81及PROGRESS为准，未在本次重跑。

| 顺序 / 卡 | 输入与实施动作 | 交付与退出证据 |
| --- | --- | --- |
| 0 / 现场核对 | main/a728f81起点；采集任务/总览两份E2E diff及既有red终态；核对Git、端口和归属 | 明确本批文件边界；先区分产品与测试错误，不覆盖或把未验证改动混进其他批提交 |
| 1 / F00待审 | 打开[新结构审核台](direction-review.html)，按[DIRECTION-REVIEW](DIRECTION-REVIEW.md)展示F00-1.18-r1及图源；若来源变化先检查影响 | 用户对导航、首屏、列表/详情、移动结构给出版本绑定意见；未回复保持待审，C仅用户目录，不伪装全站第三案 |
| 2 / B1c接续 | P51/P52/P53，CollectionRuntimeSurface及子组件；列表、总览与浏览器运行分别追踪；按下方直接执行卡收尾 | 三份独立规格和重放/恢复/深链合同；必要修复、红绿和双视口模块证据、清理及独立提交后53→56 |
| 3 / B2a→B2b→B2c | P54/P55 → P56/P57/P63 → P58/P59/P60；各自真实页面与测试入口沿3.14 | 每小批独立事实合同和必要验证；不新增业务动作，56→58→61→64 |
| 4 / B3a→B3b→B3c | P62/P64/P65 → P66–P69 → P70/P71；交叉复核已有P61 | 日志、发布恢复、资源与调度容量事实及副作用边界；64→67→71→73 |
| 随源变化 / R01 | 各批源码稳定后核对旧清单、候选语义归属、动态菜单、消费者和图源 | 仅实际重采后刷新受影响证据；源未变复用；全站无漏项后冻结G0 |
| 方向通过即优先 / F05 | 先任务P23/P24及进度弹窗，再P17/P31/P61和三壳层/个人中心；读正式设计相关技能及真实合同 | 用户审正式图→Vue→定向与消费者测试→实现对照；同步设计规范，记录实耗以重估剩余工期 |
| F05后 / F06与S1 | W02–W08全73项，按2–4页或共享入口族滚动；已有规格页同样重构；S1按平台剩余缺口逐场景处理 | 每页规格、正式桌面/移动图、菜单/按钮/弹窗/异常态、Vue、回归、意见闭环及提交；不以规格文件数当设计完成率 |
| 全站门具备 / F07 | G0–G3、真实后端/六角色链、第7节发布前执行卡及当次授权 | 宝塔同SHA部署、生产路由/允许动作、恢复及残留清理、用户签收；缺一项保留明确未完成状态 |

#### B1c首批直接执行卡

1. 读取CollectionRuntimeSurface、CollectionTaskCenter、CollectionOperationsConsole、CollectionRuntimeCenter，沿Feature Map追到collection-task、collection-console、crawler-runtime的真实路由、service和repository。分别记录权限、查询范围、分页、深链、历史返回、重放/租约回收及外部副作用；来源总览不能复用任务列表的统计口径。
2. 接续两份在途E2E而非重写。已读取既有运行终态：UI2-CL共6项，4失败/2通过。CL51迟到成功响应在返回后重新出现详情；CL52来源1/8项出现错误空态；9项失败是桌面/移动同名节点导致定位器不唯一。迟到失败用例本次通过，不据此声称完整时序已可靠覆盖，也不能把四个失败都算产品缺陷。
3. 先修正测试对实际可见桌面/移动区域的定位，删除未用测试变量；复用有效red材料，源/用例改变后再定向复现。只在证据支持下修复详情读取归属和来源空态渲染；不改任务状态机、查询范围、重放幂等、批处理语义或生产租约。补迟到响应终态、前进/后退、焦点与来源0/1/8/9展开边界的可靠断言。
4. 定向绿测后，串行执行受影响采集任务/总览模块的desktop-chromium、mobile-390；如修改浏览器运行页或共享入口，再执行m03-04-playwright-crawler及实际消费者。只有规格变化且既有测试仍有效的页面不重复跑。隔离Vue结果不冒称真实采集、MySQL或生产恢复通过。
5. 交付P51/P52/P53三份十项规格和本批独立合同，逐项关联实际动作、字段绑定、弹窗变体、角色、状态与用例；来源hash按最终源码记录。遗留读写生命周期、焦点、权限与业务冲突按可验证场景列出，未验不注销。完成后规格53→56，不改变正式风格审批或全站门状态。
6. 有产品改动时执行build:web、组件边界、预算、静态/格式/文档门；相关UI行为说明同步runbook和Feature Map，API/.env/SQL无变化则注明不适用。核实临时路径和进程后安全清理，仅暂存本批精确文件；未绿的产品修复不提交。

```text
npx --no-install playwright test tests/e2e/m03-05-collection-tasks.spec.ts tests/e2e/m06-03-collection-console.spec.ts --project=desktop-chromium --grep UI2-CL --workers=1
npx --no-install playwright test tests/e2e/m03-05-collection-tasks.spec.ts tests/e2e/m06-03-collection-console.spec.ts --project=desktop-chromium --workers=1
npx --no-install playwright test tests/e2e/m03-05-collection-tasks.spec.ts tests/e2e/m06-03-collection-console.spec.ts --project=mobile-390 --workers=1
```

执行时添加实际登记的`--output`目录，启动前检查端口和健康状态，不停止未知进程。B1c既有失败材料位于`output/playwright/ui-phase2-collection-20260908/red`，本轮仅读取终态与错误记录，没有重跑、修测试或改产品。后续实施保存失败→修复→最终通过的准确结论，再按安全规则清理本批临时材料；旧18批及B1b清理记录继续以PROGRESS为准，不重试已被拒绝的旧清理。

#### 逐批审核与发布闸门

- 每页任务卡按第4节填写，必须交桌面/移动正式图和实现图、所有实际动作/弹窗及适用状态。每个动作记录名称、权限、条件、反馈与测试；每个弹窗验初始焦点、正反Tab、Escape、取消、错误关联、处理中防重和关闭后焦点归还，不能只做一张打开态图。
- 用户意见绑定pageId/actionId/dialogId、具体版本和图源；每个小批处理完意见再报告结论。共享组件变化使相关消费者待复核，不能沿用失效通过记录；不删除失败项来缩小分母。
- 默认W09统一发布；部署器、线上SHA、迁移账本、恢复材料、维护窗口和隔离目标必须当次核对。新UI不默认需要迁移，但现有部署流程可能停Node并执行白名单迁移，不承诺零停机或全自动回退。
- F00方向、J07/PR-G01业务规则、当次生产高影响操作分别确认；只阻塞对应事项。没有方向意见可以继续事实合同，但不能擅自给正式稿盖章；没有生产条件可以交本地证据，但不能称阶段完成。

#### 1.20本轮交付边界

仅更新PLAN/EXECUTION两份永久计划。需求拆解技能用于输入、依赖、产物和退出证据；UI目录选择frontend-design用于结构差异、代表页验证和风格审批要求，不触发产品实现或替用户定案。未修改两份在途E2E及生产源码，不更新全局证据hash和审批状态；无API/OpenAPI、SQL、权限、依赖、配置、Feature Map变更，无需.env同步、部署、迁移或重启。无新增临时文件、截图、服务或浏览器，既有材料全部保留。

本版只读内存校验通过：73个稳定ID与实际路由逐项一致，41个入口组件存在，六角色；53份已提交规格加七批3/2/3/3/3/4/2共20份待交任务无重叠且无缺页；七业务包19/11/9/8/8/8/10合计73。24处本地链接、15个npm脚本引用和7个E2E文件路径有效；verify:docs通过73路由/60受保护/6角色及153份必需文档，git diff --check通过；两份在途E2E内容hash与本轮起点相同。这里只验证计划一致性，不把既有4项红测、正式设计、真实后端或生产状态写为通过。本轮未新建临时产物或服务；既有测试句柄已终态，检查4101/5173/5181无监听，既有输出目录保留待原批次收尾。

### 3.16 当前完整实施卡（1.24）

当前基线main/ea9cd59；73份事实规格已提交，B3b/B3c不再领取。G0未冻结，正式方向未审。本轮只修订计划，三个既有未跟踪检查器文件保留、不编辑或暂存。实施时重新核对当时Git、源和证据，不沿用本版SHA冒充实时基线。

| 卡 / 依赖 | 输入、直接任务和交付 | 退出条件 |
| --- | --- | --- |
| R00 / 首先 | AGENTS→Feature Map→相关蓝图→路由/真实装配链；Git及证据指纹 | 登记本批页面、共享消费者、文件归属、有效旧证据与临时路径 |
| F04a / R00后 | 73份规格、局部合同、source-scope记录、当前Vue/TS及既有清单工具；做源码对账 | 每项有当前归属或精确差异；历史签名不误报新缺陷，候选重复不当作业务重复 |
| F04b / F04a后 | 按壳层账号→成员业务→组织平台→采集运营→运维入口族补动态菜单、共享确认、角色/状态 | actionId/dialogId、条件、handler/API、caseId、证据类型及未验原因齐全；全部有去向后冻结G0 |
| F00 / 可与事实工作交错 | [方向审核台](direction-review.html)与[DIRECTION-REVIEW](DIRECTION-REVIEW.md)，F00-1.18-r1 | 用户明确选定/修改/全部否定；未回复pending，P43的C不算全站方向 |
| F05a / 方向获审 | P23/P24任务列表、详情和进度弹窗，真实合同及获审结构 | 正式双视口图先审，再Vue实现、交互/视觉测试、差异和意见闭环 |
| F05b / F05a后 | P17规则、P31权限、P61异常恢复、三壳层/个人中心、共享控件 | 获审规范、主题/密度/响应式、共享消费者及焦点/错误合同可复用；记录实耗 |
| F06 / F05后 | W02→W08全73条，每批2–4页或一个共享入口族 | 每页规格+正式图+Vue+全部适用动作/弹窗/状态+测试+用户意见，不能只交文档 |
| F07 / 全站G0–G3具备 | 真实后端六角色、发布区间、迁移账本、维护窗口和恢复材料 | 宝塔同SHA生产复验、清理、完整审核包；G5用户签收后才称全阶段完成 |

#### 第一实施批：F04a对账步骤

1. 对照config/route-catalog.json、PAGES、page-specs及coverage核对73个稳定Pxx、真实入口和规格链接。读取baseline/actions/dialogs、局部contract-review及source-scope记录，区分计划目标、源事实、隔离测试和历史生产证据。
2. 先读scripts/build-ui-phase2-inventory.mjs和scripts/lib/ui-phase2-inventory.mjs的解析及写入边界，复用已有Vue/TS能力；初次对账不执行默认写模式，不刷新历史图或通过计数。三个未跟踪audit-ui-phase2-contracts相关文件是待审草稿，不列为已通过门。
3. 保留历史表、旧签名列和新表来源，逐条分出当前对应、行号移动、内容/handler改变、缺少稳定记录及源缺失；不按最近行号猜身份，同签名不证明API/权限/行为未变。
4. 沿App、NavigationShell/AccountShell、surface和composable追共享菜单/快捷入口、响应式重复渲染、ConfirmDialog/原因框调用点。共享视觉实例不等于一个业务动作，各调用方保留角色、对象、条件和结果。
5. 交付源对账报告及未映射清单，关联页、合同、消费者、历史sourceInventoryIds和后续动作。若完善检查器，先确认归属，再验证只读默认、输入校验、历史/现行不混算、失败不提升覆盖、可重复输出和永久单测。
6. 验收先最小解析/归并测试，再73页对应、引用和文档/格式门。文档-only不重跑产品构建；工具变化运行直接单测及既有清单单测。检查器退出码0不等于G0完成，源码对账也不替代F04b运行时验证。

#### 第二实施批：F04b冻结范围

每条动作登记稳定ID、Pxx/路由、位置、名称层级、真实角色/capability、可见/禁用条件、handler、现有API合同、输入输出、副作用、成功/失败表现、caseId和证据。逐项遍历默认、空/无结果、筛选、选中/批量、分页、详情、窄屏菜单、无权、过期、失败/重试等适用入口；不存在的功能写不适用和依据，不按旧图补造。

每个模态及业务变体登记触发对象、字段校验、提交中、取消、Escape、焦点循环/归还、防重、失败重试、成功后读取失败及草稿去留；披露、非模态详情与真正模态分开。共享组件测试不替代调用方验收。动态对象按稳定业务ID关联，行号只辅助定位。

各入口族独立核对后再全站归并：73条路由、所有历史候选去向、动态补项、共享消费者、适用角色/状态与caseId无孤项才冻结G0；新增发现扩大清单，未执行不能从分母移除。冻结仅确认验收范围，不使G1–G5通过。静态、隔离Vue、真实后端、生产和审核分别统计。

真实写入只用已授权隔离对象；缺目标、凭证或明确边界时记录所需条件与清理方式。有效既有测试可复用，但不将隔离响应证明提升为真实MySQL/生产通过。

#### F05/F06每个小批执行模板

1. 领取2–4个Pxx或共享入口族，读取对应page-spec、未验合同ID、真实Vue和PAGES测试入口，明确全部字段、动作和消费者。
2. 交独立桌面/移动构图，说明导航分组、首屏重点、列表/详情、主次动作、弹窗顺序、移动结构与旧版差异；字体、色彩、间距、圆角、层级和动效统一采用获审新规范，不默认继承旧账页。
3. 同批交按钮默认/hover/focus/pressed/disabled/loading六态、菜单、弹窗所有变体及适用错误/空/加载/受阻/恢复图。视觉控件板可复用，但每个真实业务动作仍独立审。
4. 用户意见绑定页/动作/弹窗ID及版本；未审不进入该页正式编码，等待时可处理其他已审页或事实工作。修改保留旧稿和意见，不自动通过。
5. 在现有Vue3真实装配链实现；共享壳层/控件与业务页分提交，旧样式移除前核对消费者，不留永久CSS覆盖补丁层。保持字段、请求、权限、排序、持久化、主题兼容ID及已有偏好，不新增依赖或服务。
6. 先最小用例，失败修复复测后再受影响模块桌面/移动及共享消费者；按风险执行build:web、组件边界、体积、静态、docs/runtime-docs、格式门。全站收口按PLAN第7节和本文件第6节，不重复未变化的有效验证。
7. 采同状态设计与真实Vue对照，记录角色/视口/数据类型/源与构建SHA/时间/caseId/hash；设计、隔离Vue、真实后端及生产证据分开，不只更新hash冒充重采。
8. 交完成ID、待改/未验ID、图包、命令结果、用户意见、下一卡、精确提交与清理记录。主要流程、权限或数据错误不得放行，本地通过不等于生产或用户通过。

无障碍随批验证名称/标签、键盘/可见焦点、模态首焦点/限制/归还、字段错误关联、状态反馈、非颜色提示、reduced-motion、长中文、200%缩放、390/768/1024/1440及适用断点邻界、移动键盘遮挡。共享底座覆盖三主题/两密度并复核消费者；截图及尺寸检查不等于完整读屏合规。

最低为73×2正式设计视口、73×2真实Vue视口，弹窗/菜单/过程/异常图另计；W09另有146份按路由类型分类的生产视口证据，internal只验不可达。审核台按页、包、角色、动作、弹窗及未完成项筛选，显示旧参考→新设计→Vue→差异/意见，不把研究图冒称线上效果。

#### 未关闭项、工期与上线门

J07采纳质量门与PR-G01来源启用/烟测冲突单独请用户决定；DG/PN/CS/OP/RS/SC等沿原ID逐页复现和关闭，不能因规格齐全或局部测试通过注销。P63数量断言对照当前目录；备份路径、Python运行说明及容量证据差异在相关工作包或发布前核清，涉及安全/持久化规则时单独确认。

F05后按简单页、复杂工作台、多角色治理/运维实测设计+实现+验证+返工耗时估算剩余量；共享底座、全站回归、上线及审核/凭证等待分别列。初始23–40有效工作日不作为当前剩余承诺。默认单人串行，自检明确标识，用户负责设计与最终签收。

F07按第7节核对站点29、固定frontend/backend/python/config/runtime/backups目录、宝塔Node/Python身份、线上SHA及发布所有权；读取当时部署器迁移白名单与生产账本，确认维护窗口和恢复材料。不新增current/releases、后台服务或参数，不承诺零停机/数据库自动回退。发布仍用python scripts/deploy-baota.py，必要重启由宝塔执行，Nginx仅配置改变且语法验证后reload。

线上核对version/live/ready/available、六角色允许/拒绝、核心连续链及授权逐项动作。危险最终操作缺隔离目标/授权则保留未执行；失败停止晋级、保留证据并恢复稳定包，复验才继续。最终ACCEPTANCE列分母/通过/未验、图与意见、生产SHA、测试结果、重启恢复、对象/文件清理及用户签收；未签收不称全阶段完成。

#### 1.24本轮边界与验证

仅修订PLAN/EXECUTION两份永久文档。需求拆解技能用于工作卡/依赖/交付/退出条件，无障碍技能用于按钮、字段、菜单和弹窗的逐项验收。未修改产品、API、权限、SQL、配置、依赖、Feature Map、生成清单、审核记录及三个既有未跟踪检查器；无需OpenAPI/.env同步，无迁移、部署、重启或新调节项。

本轮未创建临时文件、图、服务或浏览器；PROGRESS记录的26批历史材料未动、不宣称已清理。只读内存校验通过：73条路由与73份规格逐项匹配、82处规格链接有效，当前计划实际七包19/11/9/8/8/8/10无遗漏无重复覆盖73项，41个入口组件存在；两份计划13个不同本地链接、15个npm脚本引用及9个测试文件引用有效。首轮校验误要求规格路由必须包在反引号中，在P62纯文本路径处失败；核对真实内容后改用完整路径token精确匹配，通过全部73项，未修改规格或放宽路由值。

verify:docs通过（73路由、60受保护、6角色、153份必需文件），verify:runtime-docs与git diff --check通过；G0仍未冻结、用户批准0。未运行无变化的产品构建、业务E2E或生产验收；本轮未运行原P00–P08的verify:plans，不把它当作本UI阶段覆盖证明。以上验证只证明计划范围、引用和文档一致，不证明新图、实现、业务或生产通过。

## 4. 每页任务卡及状态规则

每页沿用固定 Pxx，不因改标题或调整构图换 ID。每张任务卡必须包含 PLAN 第 4 节的十项规格，并增加以下执行字段：

| 字段 | 记录要求 |
| --- | --- |
| 归属 | pageId、workPackage、route、surface、共享消费者 |
| 输入与依据 | 角色、范围、真实字段/API、基线 SHA、现有测试 |
| 设计决策 | 主任务、旧问题、新构图、保留功能、删除/合并的视觉入口及理由 |
| 工作范围 | 文件/组件、actionIds、dialogIds、状态与响应式变化 |
| 验收 | caseIds、预期请求和结果、图路径、环境、敏感材料处理 |
| 状态与责任 | 实施者、自检/独立复核说明、用户意见版本、阻塞及解除条件 |

动作记录必须表达“触发 → 前置条件 → 输入 → 实际 handler/API → 成功/失败 → 原页同步”；既有 API 内容只引用或按源码记录，不能在计划中发明请求字段。

状态顺序：待盘点 → 规格完成 → 设计待审核 → 设计通过 → 实现完成 → 本地验收通过 → 生产验证通过 → 用户验收通过。进入下一状态必须有对应证据。若用户要求修改，保留原意见和旧图版本，返工该项；关联组件改变时，同时将受影响消费者标记待复核。

生成器管理 baseline/actions/dialogs/coverage/review-data；人工规格、审核意见与验证记录独立保存，不直接覆写生成物中的状态。后续若扩展聚合器，必须测试重生成保留人工结论、版本失效和缺图失败关闭。

## 5. 全套图与用户审核方式

最低交付量：73 条路由 × 桌面/移动 = 146 个最终设计图槽位；另有 146 个真实 Vue 实现证据槽位。业务弹窗全部变体、按钮状态板、异常状态和过程图另计，不能以凑足 146 张结束。跳转、fallback 与内部页按各自语义留图及 URL/响应证据，不冒充普通业务页。

每个页面审核卡集中展示：旧版参考 → 新设计 → Vue 实现 → 本页变化 → 按钮/弹窗 → 未完成或未验证项。按页、工作包、角色、失败项筛选。设计稿可以使用合成或隔离数据，但必须标注；AI 概念图若采用也不算接口、权限或真实运行证据。

审核分三次，不要求用户一次看完全部材料：

1. 方向审核：W01 比较新构图，选定全站视觉方向；未回复不是通过。
2. 分包审核：W02–W08 每包给出全页、按钮和弹窗图，并逐条处理意见；不能只展示首页。
3. 总体验收：W09 对照真实生产版本完成全站与连续业务链审阅，记录用户签收。

审核意见至少关联目标 ID、图/源码版本、通过或修改、原因、时间与复核证据。已有审核台可记录并导出批注，但完整图对照和正式通过流程仍属 W01 待完善，不把当前工具误称最终审核系统。

## 6. 验证责任与失败处理

六角色沿用 config/route-catalog.json：member、selection_manager、organization_admin、platform_operations_admin、platform_security_admin、platform_super_admin。按实际 capability 验允许和拒绝两条路径；不让超级管理员代验其他角色权限。

验证层级必须分开：

- 设计/静态：图、规格、字段溯源、控件状态与布局审阅。
- Vue + 隔离合同数据：状态、响应式、请求内容、焦点和交互；不得称真实数据库验证。
- 真实后端 + 隔离数据：事务、幂等、权限、数据范围、错误与恢复链。
- 生产：同版本路由、真实允许链路、必要隔离写入及残留清理；危险全局操作按 PLAN 限制。

每页默认 1440×1000、390×844，补 768/1024、适用断点邻界、200% 缩放和长文本；共享组件另覆盖三主题和两种密度。加载、空、错误、无权限、过期、受阻、恢复等状态按适用性逐项记录，不能全写不适用。

先最小验证，失败即修复复测；通过后验证全部受影响消费者。W09 执行现有 verify:functional 聚合门，并补其未包含的 frontend-budget、runtime-docs、e2e-realism 等 PLAN 所列相关门。已读到的聚合脚本包含代码风格、静态分析、构建、Node/Python 测试、桌面/移动 E2E、文档、计划、发布矩阵和安全门；实施时若脚本变化须重读。按序执行，避免临时夹具污染格式检查。

阻塞发布的问题包括：权限越界、字段或业务合同变化、数据错误、主要操作不可达、重复写入、关键状态缺失、敏感信息泄漏、版本不一致。纯视觉差异也必须记录并修正至用户认可；例外只能由用户明确接受，不能由实施者自动降级。

## 7. 发布、回退与交接

发布前核对本次授权范围、干净工作树、发布区间所有权、通过的测试及恢复材料，再调用既有 `python scripts/deploy-baota.py`。新布局默认不需要数据库迁移；出现迁移需求先拆出说明影响并确认，不能借此前一次迁移授权无限扩展。

生产沿用 `/www/wwwroot/ai选品` 和宝塔既有对象。前端静态文件发布不需要自行创建或重启开发服务；Node/Python 是否重启按实际运行包及配置变更判断并由宝塔执行，Nginx 仅在配置变化且语法检查通过后 reload。不能借 UI 项目重建服务架构。

### 发布前执行卡

1. 读取当时 deploy-baota.py 和 service-manifest：确认站点29、Node/Python对象及固定目录一致；不启动新的生产服务。
2. 读取线上当前SHA和迁移执行记录，与本地发布区间、迁移白名单逐项比较。当前部署器会停止Node并调用迁移器，纯UI改动也不能承诺零停机；若有待执行且未授权的迁移，停止发布并提交精确影响说明。
3. 通过发布所有权门、同版本构建及G0–G3；保留旧运行包、受限配置及原SHA，确认恢复路径。现有脚本的公开验证失败不保证自动恢复，缺少可执行恢复方案时不能发布。
4. 确认维护窗口和隔离身份/数据范围后执行现有部署命令；记录实际停止、启动、健康检查和版本结果。不要把 `--initialize-layout` 用作日常更新命令，不添加不存在的部署参数。
5. 核验公开版本、live/ready/available、六角色路由及核心链；只执行有授权且可精确清理的生产写入。发现版本错配、关键不可用、权限越界或数据异常立即停止晋级，通过既有宝塔流程恢复并复验。
6. 图包/测试文件不上传生产静态目录，生产敏感材料留境内受限目录；清理本次隔离对象和临时包，用户签收后按已确认保留策略处理恢复材料，不擅自删除已有备份。

上线后核对公开构建身份、健康、六角色路由、核心链与逐项证据。失败即停止晋级，保留诊断材料，按已验证的恢复方案恢复已知稳定运行包；不能宣称数据库会随前端自动回退。备份保留到生产核验通过，敏感生产图和恢复材料留在中国境内受限目录。

最终 ACCEPTANCE 应列出：页面/动作/弹窗/状态分母及通过数、未执行原因、用户意见闭环、生产 SHA、测试命令结果、重启和恢复记录、精确清理结果、保留图包位置与提交 hash。用户未签收时只能称技术交付，不称第二阶段全部完成。

## 8. 1.3历史计划修订的交付边界

本次1.3只修订 PLAN/EXECUTION；保留已有73页矩阵及全部草稿，不修改产品 Vue/CSS、测试代码、API、权限、数据库、配置、依赖或生产服务。没有运行实际业务写入测试。没有新增运行时合同，OpenAPI、.env及Feature Map无需变更。不创建临时测试文件或启动服务，文档是永久交付物。此前PROGRESS记录的系统临时目录清理未完成属于既有交接项，本次不把它误写成已清理，也不删除旧文件。

下一步是审核本计划的范围与节奏；获准实施后从R01及已有代表方案的R02审核开始。本轮不代替用户选定视觉稿，也不启动新的实现或部署。

### 1.1历史计划验证记录

只读内联校验通过：73 个页面 ID 无遗漏和重复，路径逐项匹配当前路由顺序，41 个不同入口组件全部存在，6 个验收角色来自当前目录，文档链接及引用命令存在，工作包页数合计 73、初始工作日区间合计 23–40。`npm run verify:docs` 通过（73 路由、60 受保护路由、6 角色，153 份必需文档）。这仅证明计划与当前仓库结构一致，不代表产品功能、全套图片、生产部署或用户验收通过。本轮不重复运行业务构建或 E2E。

### 1.2历史计划验证记录

重新执行只读内联校验：73个页面ID唯一且路由与实际目录对应、入口组件存在、两份计划内相对链接和npm命令有效、7个业务工作包合计73条，全部通过。`npm run verify:docs`通过，仍为73路由、60受保护路由、6角色、153份必需文档。本次没有重跑产品构建、业务E2E或生产验收，因为只修改计划文字；已有原型与脚本未提交改动不由上述文档检查证明。PAGES及生成器未改，不为计划版本号重生成清单或图片。无新增临时产物、服务、迁移、部署或重启要求。

### 1.4历史计划验证与交接

本次只更新PLAN/EXECUTION，基线为d2d7eff且已有未提交任务页相关修改，未代为编辑或暂存这些文件。只读内联校验确认PAGES的73个ID唯一且路径均匹配当前route-catalog、角色6个、页面规格实际6份；两份计划的相对链接及npm命令有效。文档门通过（73路由、60受保护路由、6角色、153份必需文档）。这些结果只证明计划引用及范围，不证明工作树中的产品修复、截图、线上状态或用户审批。

未重跑产品构建或业务E2E；未生成图片、启动测试服务、安装依赖或部署，无重启和迁移要求。UI技能目录查询无返回后已停止，使用本地需求拆解与设计技能制定范围和设计审核规则；未完成专用技能目录流程不等于计划受阻。本轮无新增临时测试产物，既有临时目录不动。下一步获准实施后按S00起步，方向选择仍须用户明确审核，不据历史“允许”代选新稿。

### 1.5历史计划修订边界

按用户“制定第二阶段的完整可实施计划”修订，仅更新PLAN/EXECUTION；保留W00–W09、73条路由、全部按钮/弹窗/适用状态、完整图包、Vue、测试、宝塔发布和用户签收的目标。核对47707c1干净起点及18份实际规格，纠正旧计划仍列6份、S00尚未提交的过期状态，增加N01–N05可领取工作卡。需求拆解技能用于输入/产物/退出条件；无障碍技能用于名称、键盘、焦点、字段错误和状态反馈验收，不触发代码修改。

本轮不改变产品代码、CSS、API、权限、数据库、依赖、配置、Feature Map、PAGES或生成清单；无运行时合同变更，OpenAPI与.env不适用，不部署或重启。旧矩阵纠偏安排在相关图源刷新时同步，不以修改哈希代替重新采证。没有创建临时测试文件、图片、服务器或浏览器；PROGRESS已记录的六个历史临时目录未触碰，不宣称已清理。本文为永久交付物。

只读内联检查已通过：73个ID唯一并按ID映射当前路由、组件均存在，18份规格/55份待补，审核计数仍为0，两份计划相对链接和npm命令有效，W02–W08合计73项。首轮校验误按文档出现顺序比较路由，因PAGES按工作包分组在P12后进入P14而失败；改为按稳定ID排序再逐项核对后通过，未修改矩阵或放宽路由比对。`npm run verify:docs`通过（73路由、60受保护路由、6角色、153份必需文档）。只验证计划一致性，不代表最终图、业务回归、线上状态或用户审核通过。

### 1.6本次计划修订边界

本次仅修订PLAN/EXECUTION：核实181ab16起点、19份规格及54份缺项；N01改为复用已有交付，N02/N03/N04明确19→21→22的产出顺序；新增3.6缺口优先级、关闭证据和首次审图交付要求。需求拆解技能用于可领取任务和退出条件；UI技能目录选择无障碍技能，用于名称、键盘、焦点及错误恢复的验收计划，不在本轮实施修复。

只读内联校验通过：73条路由与ID、41个入口组件、19份规格、6个角色、7包合计73项，以及两份计划链接和npm命令均有效；文档门通过（153份必需文档）。本轮未重跑业务测试、构建、图片采集或生产验收；无运行代码和配置变化，不需OpenAPI/.env/Feature Map配套更新，无迁移、发布及重启。验证不代表用户选定了A/B方向。

无新增临时产物或进程需要清理。开始时已有的opportunity-candidate-map.md保留、不暂存、不计为两页规格完成；PROGRESS记录的七批历史临时目录未动，不宣称已清理。下一步审核本计划及候选风格；获准实施后按N02→N03→N04推进，N05审图与合同工作同步，不因历史“允许”自行批准新稿。

### 1.7计划修订与校验

本次只更新PLAN/EXECUTION，保持全部W00–W09目标。实际核对22份规格，新增E01–E07近期执行卡；只读校验确认73个稳定ID及路径逐项匹配route-catalog、入口组件存在、两份计划链接有效、E02–E04恰好无遗漏无重复覆盖51份缺失规格。文档门通过：73路由、60受保护路由、6角色、153份必需文档。首轮提取路由的内联表达式受PowerShell反引号影响，未据其结果宣称路由通过；改用逐行拆分和字符码后逐项断言通过，没有修改路由或放宽比较。

技能使用：需求拆解用于输入/产物/退出条件，UI技能目录选择无障碍约束，用于控件名称、键盘、弹窗焦点和错误恢复验收。未实施产品修复、新视觉、采图、生产验收或部署，未新增依赖、配置和运行时合同，OpenAPI/.env/Feature Map无需本轮配套修改，无迁移或重启要求。本轮无新临时文件或服务；既有58项未提交变更及PROGRESS所记历史临时目录均保留，不代为提交或宣称清理完成。两份计划为永久交付物。

### 1.8计划修订与校验

本次仅更新PLAN/EXECUTION。以main/060e0b5、当前文件和PROGRESS为依据，更新为24份规格、缺49份；标明E02已交付，拆分E03四个二页小批及E04五个组织/平台批次，要求尽早提交方向审核。W00–W09全范围、每页十项规格、全部动作/弹窗/适用状态、正式图与Vue对照、宝塔发布及用户签收要求保持；原型、局部测试和历史部署不当作全站完成证据。

只读内联校验通过：73个ID与真实路由逐项一致、41个入口组件存在、24份规格名单正确、七个业务包无遗漏无重复覆盖73项、6个验收角色、9处相对链接及19次npm脚本引用有效。npm run verify:docs通过（73路由、60受保护、6角色、153份必需文档）；git diff --check通过。仅验证计划一致性，未运行产品构建、业务E2E或生产验收。

需求拆解技能用于工作卡、输入产物和退出标准；UI技能目录选择无障碍技能，用于名称、键盘、焦点、错误关联和状态反馈验收。不改Vue/CSS、API、权限、SQL、配置、依赖、Feature Map、PAGES或生成器，OpenAPI/.env无需同步；无迁移、部署、重启或调节项。开始时已有5个生成清单修改全部保留、不暂存；未新增临时测试文件、图片、服务或浏览器，历史临时目录未触碰、不宣称已清理。两份计划为永久交付物；本次制定计划不构成用户选定风格或授权业务规则改变。

### 1.10计划修订与校验

本次从main/a78d22698fedebc5030d6bb17e1a91e1b450ebbc干净工作树开始，仅更新PLAN/EXECUTION。按实际规格和PROGRESS纠正当前起点：F01已交付，28份规格、剩45份；增加R00源证据收尾，下一规格小批为F02审批/通知。保留完整W00–W09和所有逐页图、动作/弹窗/状态、实现、验收、发布与用户签收，不将已有局部合同回归或研究图视为全新设计通过。

只读内联检查最终通过：73个稳定ID唯一且对应route-catalog真实路径，41个组件入口存在，28份规格与45份剩余任务构成无重复的73项；待补七批为2/2/8/8/8/8/9。六角色、11处相对链接、21次npm命令引用以及F02三个E2E入口均有效。首次检查误按PAGES按工作包排列的物理行序比较目录，出现/trends与/work不等；改为稳定Pxx排序后通过，未改路由或矩阵。npm run verify:docs通过（73路由、60受保护、6角色、153份必需文件），git diff --check通过。上述仅证明计划引用和范围，不证明当前图片、业务运行或生产完成。

本次没有重跑产品构建、业务E2E、图采集或生产检查，没有更新全局证据SHA、用户审核记录和产品文件；无新增临时文件、图片、服务或浏览器进程。旧13批临时目录保留，不宣称已清理；两份计划为永久交付物。需求拆解技能用于可领取工作卡和验收依赖，frontend-design技能用于明确结构重设计与代表稿先审的标准；没有运行跨模型或独立第三方评审。文档无需迁移、部署、配置调整或重启。

### 1.11计划修订与校验

2026-09-08从main/fa09f39干净起点，仅更新PLAN/EXECUTION。核实32份规格、剩41份，F02/F03与一次R00已有提交，不重复安排；明确F04组织治理八页的真实入口、只读审批边界和分批验收，再接平台/采集/数据/运维。全部73路由的新设计、实际按钮与弹窗、适用状态、Vue、真实后端、宝塔部署及用户签收仍是完整阶段范围。正式方向未选定，J07独立待决，不能把本次计划修订视为这些决定或发布授权。

只读内联校验通过：73个稳定ID唯一并对应实际路由、41个组件入口存在、32份规格与8/8/8/8/9份待补任务无重复覆盖73项，6角色、11处相对链接、22次npm命令引用及组织E2E入口有效。coverage仍为分母未冻结、用户批准0。npm run verify:docs通过：73路由、60受保护、6角色、153份必需文档。验证只证明计划结构、范围和引用，不证明最终图、产品行为、线上状态或用户验收。

需求拆解技能用于执行卡、依赖和退出证据；无障碍技能用于逐个按钮/弹窗的名称、键盘、焦点和错误恢复验收标准。本轮没有产品、API、权限、配置、SQL、依赖、Feature Map或PAGES变更，无OpenAPI/.env配套更新、调节项、部署、迁移及重启要求。不重复运行未变化的产品构建/业务E2E，不生成图片或更新证据SHA。未创建临时产物、测试服务或浏览器进程，无新清理项；旧14批目录不动。两份文档为永久计划交付物。

### 1.9计划修订与校验（历史）

本次从main/69d7b219e8adc9eabbb1639b024b1cf123696a7f干净工作树开始，只更新PLAN/EXECUTION；核实26份规格和47份缺项，将E01证据收尾与P19/P20首批标为已交付，并增加F00–F07当前工作卡。保留全部W00–W09、73条逐页矩阵、每个按钮/弹窗/状态、正式图→Vue→真实验收→宝塔发布→用户签收，不以局部测试替代全阶段完成。第一实施小批是P21/P22；方向审核与事实盘点交错，获审代表闭环不必等待47份规格全部补齐。

只读内联检查通过：73个ID及路径逐项匹配真实route-catalog，41个入口组件存在，26份规格名单准确，剩余八小批2/2/2/8/8/8/8/9恰覆盖47份且无重复，W02–W08为19/11/9/8/8/8/10共73项；6角色、11处相对链接、20次npm命令引用和F01组件/测试入口有效。inventory --check通过；npm run verify:docs通过（73路由、60受保护路由、6角色、153份必需文件），git diff --check通过。这里只验证计划与引用，没有重跑产品构建、业务E2E、图片采集或生产验收，未提高任何用户审核状态。

需求拆解技能用于工作卡输入/交付/依赖/退出条件；UI技能目录选择无障碍技能用于按钮名称、键盘、弹窗焦点、字段错误与状态反馈的验收计划。未改Vue/CSS、API、权限、SQL、依赖、配置、Feature Map、PAGES或生成清单；OpenAPI/.env不适用，无迁移、部署、重启或新增调节项。本轮没有创建临时文件、截图、服务或浏览器；历史12批临时目录未触碰，未宣称清理。两份计划是永久交付文档。
