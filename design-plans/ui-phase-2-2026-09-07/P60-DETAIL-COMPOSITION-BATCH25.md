# P60 三类详情与状态 · 批次25

审核入口：[r2实际Vue 24图](../../output/playwright/p60-detail-composition-r2/index.html)。本次只请求手机账号详情“权限与限额 / 访问状态”两组事实的标题、字段顺序、留白和分隔线意见；技术区、按钮和真实操作不随之通过。上一批创建窗的待审范围保持不变。

## 设计与实现

按 frontend-design 延续C方向，将实际详情的原字段重新分为账号“权限与限额 / 访问状态”、回调“目标与事件 / 状态与时间”、投递“投递结果 / 调度信息”。蓝色身份标题、白色事实、浅灰可展开技术区和独立操作区形成清晰层级；撤销账号沿用原danger类，审核视觉为红色描边。桌面保持原技术披露入口，不凭空增加详情操作。

详情变换在上一批实际页面审核变换之后单独启用。三类dt、插值、v-if条件、点击处理与顺序逐项相等，业务script不再改动；桌面与列表摘要slot逐字不变。只新增分组容器与标题，不增加风险评分、探测结果、原始请求/响应内容、scope或权限含义。生产源码、公共详情组件和原确认组件均未改。

按 fixing-accessibility 检查原详情的背景inert、首焦点、真实Tab首尾、技术区Space展开/收起、确认叠层暂停和取消还焦。技术summary恢复原生list-item标记，并展示蓝色键盘焦点。确认操作仍使用原门槛：只有撤销账号须勾选影响确认；不能因为按钮危险性增加未授权业务门。

## 样例与覆盖

12样例从原m06-05 E2E提取并局部派生：Client active/expired/revoked/rotated，Webhook active/disabled，投递dead_letter/queued/leased/retry_scheduled/succeeded，另有长名称/长URL。已过期样例使用过去到期时间；摘要状态数与派生状态同步，但这些均为合成数据，不是生产统计。队列样例保持0次和空响应“—”，成功样例保留204，不把排队等同成功。

四宽度1440/768/390/320，两动效reduce/no-preference，共8组1556项，96次本地模拟GET。390视口高844，其余高1000；不是所有设备尺寸或真机。桌面检查原技术披露；移动检查完整字段与条件、无横溢出、长内容、可用动作、详情暂停/恢复及最终还焦。每个手机/动效组执行9次行确认后取消，涵盖7种原行操作，轮换回调与重放跨状态重复覆盖，不重复计作新业务。确认均取消，不执行POST/PATCH，不生成密钥、不发送回调。每组首次载入12个独立样例，所有未知/外部请求阻断并断言不存在，无浏览器异常。

原默认页134项/6次GET回归通过。新增8项测试连同上一批7项和既有M06-05五项，共20项；覆盖脚本/事实/处理保持、Vue编译、锚点变化失败关闭、样例不变性、状态边界及CLI非法/混合模式拒绝。只改审核模块与验证驱动，生产构建沿用未变源码的既有结果；实际审核Vue由编译与Vite宿主直接验证，不冒充生产集成。

## 出图与来源

r2为24张原始PNG、169份捕获来源，HEAD bf7217f4加本批工作树。内容含三张桌面技术区、手机三类事实区、12种技术/状态区以及六张主要行确认；内部滚动截图只展示当时视口，不把标题覆盖经过区或底部未显示字段算成完整单图验收。

首次r1出图在一个键盘焦点颜色读数处失败，未完成的20张检查图已清理。一次错误使用了“页面所有dialog数量”，把桌面导航dialog算入详情，已改为只核对详情抽屉；另将手机程序化focus改为实际Tab，避免混淆输入方式。此后无输出与r2捕获均通过。不能仅凭复跑通过断言偶发读数的根因已证明。

r2捕获后，验证helper增加至多3秒等待真实activeElement、focus-visible和蓝色轮廓到位，超时仍失败，不修改产品或放宽预期；再跑全部1556项通过。当前168份来源仍匹配，唯一差异是 `scripts/lib/open-detail-verification.mjs` 的上述等待；24图哈希一致，不重写旧manifest。上一批页面r2的26图保持不动，来源中仅共用验证脚本因新增详情模式改变，不能宣称旧166份来源均与当前一致。

## 使用与收尾

- `node scripts/verify-open-page-preview.mjs --details`：八组详情验证，无文件输出。
- `node scripts/verify-open-page-preview.mjs --capture-details rN`：相同检查并创建新图包，拒绝已存在目录、不安全版本及混合模式；下一次用r3，不覆盖r2。
- 原无参数与 `--capture-review rN` 仍为页面/创建134项，详情CSS和分组不混入该模式。

同步P60规格、Feature Map、计划、审核索引和运行说明。API/OpenAPI、数据库、插件、Python、后端/Worker、密钥、环境与依赖均不涉及且未改；不需要部署、迁移或重启。文档/运行说明/路由门与5个代码文件格式检查通过。8个本批验证端口无监听，浏览器与服务均由finally关闭。

r1检查产物已删除，r2是永久审核交付；没有新日志、下载或一次性脚本，其他批次遗留目录不动。更多宽度/缩放、完整读屏/真机、跨页与移除生命周期、独立行原因、密钥保存清理、写失败/读归属及真实权限/发送/生产仍待完成。完整73页目标不缩减为当前图包。
