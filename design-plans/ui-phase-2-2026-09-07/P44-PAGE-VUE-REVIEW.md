# P44 可授权账号目录 · 实际 Vue C 组合审核

后续用户已[批准手机目录局部](P44-MOBILE-DIRECTORY-APPROVAL.md)；下文“待审”是制图时状态，不扩为整包或整页批准。

2026-09-11，起点 main/05c5ebe0，工作树干净。使用 ui-skills-root 选择 frontend-design，并用 Playwright 复用仓库既有 Vue/Vite 浏览器验证方式；不安装依赖、不操作真实账号。

[四宽度68图](../../output/playwright/p44-page-vue-preview/index.html) · [精确清单](../../output/playwright/p44-page-vue-preview/evidence.json) · [手机目录组合](../../output/playwright/p44-page-vue-preview/390-directory.png) · [桌面整体](../../output/playwright/p44-page-vue-preview/1440-default.png)

## 本批改变

- 基于C方向的蓝色全局汇总、白色任务目录：可授权账号与全局管理员数明确区分，邮箱/状态帮助靠近字段，桌面三列呈现身份、平台角色与详情入口。手机目录转上下排列，保留原两步预览到详情流程。
- 新建管理员仍为主要动作；创建组织、刷新和三对象导航保留。角色比较放在目录之后，本批保留其原组件内部布局，只核对组合位置及读取/筛选接线，不能称其全部C细节已实现。
- 仅审核宿主应用模板组合和独立CSS；实际父级、管理员记录、详情与角色比较脚本不替换。AST测试逐一比对所有原指令、表达式和原生控件属性；仅新帮助关联与展示容器属于视觉调整。
- 人工初看发现桌面详情按钮换行，已在本批尚未交付稿修正操作列宽/按钮不换行后重拍。原P43/P44离线256图、历史真实P43图和已批区域均未覆盖。

## 事实与测试数据

`PlatformAdminRecords`来自`/platform-admin/admins`共享父级；真实后端管理员查询包含未授权账号。原E2E overview、三角色目录和buyer详情保留。为验证无角色账号，明确将既有users[0]派生为admins中的第二条测试行，roles=[]、granted_at=null；长邮箱/三角色/空数据另列合成变体，不能充当真实权限或平台统计。

全局summary.platform_admins一直为原样例2。邮箱buyer筛选后列表只有1条且没有平台角色，全局仍为2，证明两者不是同一计数。账号请求只用原query/status；角色比较的6项差异→同角色0项→全部3项、无结果与重置不改URL、不新增请求。

## 覆盖与限制

390/760/761/1440共282项浏览器检查、68PNG、39源指纹。每宽度19个拦截GET（账号10、角色8、详情1），零POST：首次等待/失败、筛选/清空、无账号、长邮箱/多角色、移动预览/完整UUID、真实详情入口和Escape、创建管理员默认运营角色及取消、角色空和后台失败保留、账号后台失败保留。

现有角色刷新失败会保留旧矩阵，页面只显示“账号记录仍可继续使用”一类提示；内部rolesError中的旧快照说明没有在本页单独显示。已按实际状态记证，不把矩阵可见说成已刷新成功，也不擅自重定义失败后的保留/隐藏规则。本批不是比较错误区、所有创建/改密/原因窗或真实授权验收。

未覆盖完整App/KeepAlive/浏览器历史、三主题两密度、200%缩放/完整无障碍、真实MySQL权限与审计、写后刷新、全部按钮状态。实际Vue不等于实际后端，截图数量不等于整页完成；P44尚未获用户视觉批准，全73页实施与宝塔交付继续。

## 使用、验证与收尾

```powershell
node scripts/verify-ui-phase2-admin-page-preview.mjs
node --test tests/unit/ui-phase2-admin-page-preview.test.mjs
# 正式重拍使用 --capture；保留历史图包，不将测试样例用于真实账号操作。
```

无需调参。生产源、API/OpenAPI、路由、配置/环境变量、依赖、数据库、权限和后端/Python/插件均不改，无部署或生产重启。正式68PNG、图册和清单保留，无一次性临时文件；浏览器/宿主由finally关闭，最终端口与提交结果在本轮收尾核对。

收尾：5项定向与792项相关UI回归通过；73路由/153必需文档检查、格式及diff检查通过。全局既有设计包来源与PNG漂移为0（其102包/15069PNG口径不包含本批实际Vue图，不冒充全站完成）。本批生产/依赖未改，复用上一提交已通过的构建结果，不重复构建。首轮测试可访问名称精确匹配超时已按实际label帮助文本修正并复测；56237、56599、56698、56922四次本轮验证端口均无监听。
