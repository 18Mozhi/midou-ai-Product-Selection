# P47 手机空态实施后的刷新焦点回归

当前从main/af239b08、441项已有工作区变更、空暂存继续。两种手机空态区域已获批准
并实施，本轮只恢复当前源码的刷新交互验证，不再改已批准视觉或扩大批准范围。
使用UI技能的无障碍焦点约束；本地UI CLI未安装，不安装依赖，复用项目现有浏览器驱动。

## 改动与边界

新增无参数入口`node scripts/verify-provider-adapter-refresh-focus-current.mjs`，严格固定
原驱动完整SHA `ed261c30178dc65d251d87ffe4d4a4b01b446dc3da321f8bd63fc9da79ea5975`。
实际手机源码通过原完整逆向函数接续手机前版/刷新前版，仅作来源验证；浏览器只运行
未转换的当前App，不渲染before、不注入提案。原所有交互、请求、焦点、清理断言逐字保留。
强制capture=false；传入任何参数立即失败，不能通过本入口覆盖历史审核图片。
原刷新驱动、两份逆向辅助文件、旧图包和用户批准记录全部不变。

## 当前实测

[原始结构化结果](P47-REFRESH-FOCUS-CURRENT-RESULT.json)：390/760/761/1440四宽度，
各成功/409失败，共8组152检查、177加载来源、0图片。完整当前Vue SHA为
`51c0ba1f86fa1179fcb0d25b9cb327ef8a38f34b0cef53d5a916f0ef2f92a2ee`。

每组实际Enter、空格、鼠标各触发一次刷新；等待时按钮禁用、标题持焦点且无强制滚动，
两种键盘触发有可见焦点。空格触发后Tab跳过禁用按钮到原返回链接，再主动移到搜索框，
响应不得抢焦点。成功/失败都保留搜索与1条筛选结果，反馈和请求次数符合原断言。
合计40条本地无body GET：8导航、8初始读取、24显式刷新；0 POST、0浏览器异常或意外传输。
这些不是新状态截图，不代表待审焦点框、失败提案、真实权限或生产验收已经通过。

## 验证与交付

新增5项定向测试通过，覆盖不截图/仅current、完整场景与清理保留、实际源码逆向边界、
未知驱动变更拒绝及两个错误参数在浏览器启动前失败。最终四份相关测试共14项通过，
包含既有读取/反馈适配器保护，以及实际手机实施包的完整源码/96图/样式边界核对。
文档153文件/73路由/60受保护/6角色、运行说明一致性通过。结果JSON首次格式检查未过，
仅按现有Prettier规范排版后，四份相关代码/JSON格式复查通过，数据和浏览器结果未改。
git diff --check通过（已有文件的LF/CRLF提示不是检查失败）；最终446项工作区变更，
暂存仍为空。未改运行代码，未重复完整全库或构建，不推算全局剩余失败数。
实际浏览器已退出，端口59020已另行核对无监听，无本轮临时文件、日志或后台进程。
新增验证脚本、永久测试、结果与本记录是正式交付物；没有创建新截图，不需删除旧图。

不改生产Vue/CSS、API/OpenAPI、权限、数据库、依赖、环境变量或运行配置；无需生产重启。
只新增本地无参数验证入口，运行说明和Feature Map同步。不重复未受影响的生产构建。
最近全库快照仍为手机实施阶段1423项1240通过/183失败；本轮不推算当前失败数、不放宽
历史图证门禁，不把局部通过写成全站完成。未提交或部署，commit hash不适用。

两处历史清理受策略阻止的目录仍保留，本轮不绕过重试：
`D:/项目工程文件/vue/curson/工具/智能选品/output/playwright/p47-c-e2e-temp`；
`C:/Users/23136/AppData/Local/Temp/scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`。
全73页/其他状态实施、既有待审项、全库门禁和真实部署验收继续保持未完成。
