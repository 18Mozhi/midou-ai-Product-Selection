# P35 父级 C 读取状态设计 · 待审

从main/f3544bb9继续。此批是独立HTML设计，不是实际Vue、生产截图或整页批准。手机导出详情“尚未生成/0行”的批准按原记录保留，不转授本批。

[276张图册](../../output/playwright/p35-parent-c-review/index.html) · [交互稿](design/org-data-parent-c/index.html) · [源与图片证据](../../output/playwright/p35-parent-c-review/evidence.json)

## 设计与范围

frontend-design技能用于延续已选C方向：蓝色范围栏、中文无衬线、白色提示区域、留白和就近恢复按钮。权限说明采用温和语气，但P34措辞批准不等于P35批准。Playwright使用现有依赖；UI Skills CLI本地缺失，使用已安装技能回退，未下载或安装工具。

工作区比较/导出履历 × 1440/390 × 17场景：首次读取中、已读取、后台刷新中，以及首次/后台七类失败500、503、409、429、401、403、请求中断。每种失败的可见性均与前批实际App矩阵中两个请求端点的结果核对。

| 状态 | 设计处理 | 不作出的承诺 |
| --- | --- | --- |
| 首次读取中 | 白色读取区、静态装饰线，不显示列表或数量 | 不伪造进度或完成时间 |
| 首次失败 | 白色说明、未展示说明、折叠追踪、重新加载 | 不把失败解释为空记录 |
| 后台读取中 | 蓝色轻提示，保留旧内容，禁用重复刷新 | 不更新未返回的事实 |
| 普通后台失败 | 温和提示条，保留旧内容、筛选及分页 | 不将旧观测时间标为最新 |
| 后台401/403 | 替换数据区，温和说明与恢复入口 | 不声称清除内存或真实权限已验证 |

409仍只是现有客户端分类器合成场景，不新增接口合同。请求中断不等同真实断网/超时。追踪编号明确标“示例”。重新加载只记录既有summary/data两项GET意图并进入读取状态，不发送HTTP、不模拟成功；后续结果用稿外审核工具选择。

复用原C子页面及样例事实，没有合并新字段适配稿，也没修改已批准的实际Vue导出详情。移动区域后恢复输入焦点和选区；通过原视图按钮回调重绘，避免重置分页。审核视图选择器与原导航同步。

## 图片与验证

276PNG = 68组合 + 60刷新焦点 + 36重新加载焦点 + 56追踪焦点 + 56追踪展开。悬停/按下有原生检查，但本包没有单独图片，不宣称全部按钮六态图齐全。390宽度鼠标检查不是触屏悬停验证。

最小手机双视图192检查，完整1272检查。验证数据、筛选、双分页、输入焦点保留；禁用不重复；恢复仅两个GET意图；零业务弹窗、HTTP/外部请求、页面错误。两个新单测固定17场景/276图、来源/图片哈希、精确文件清单与批准边界，登记关联另有变异检查。

```powershell
node scripts/verify-ui-phase2-org-data-parent-c.mjs --smoke
node scripts/verify-ui-phase2-org-data-parent-c.mjs --capture
node scripts/verify-ui-phase2-org-data-parent-c.mjs
node --test tests/unit/ui-phase2-org-data-parent-c.test.mjs tests/unit/ui-phase2-org-data-review.test.mjs
node scripts/build-ui-phase2-org-data-review.mjs --check
```

--capture写永久图与清单；无参数核对源/图哈希并重跑，不重写图片。本地打开交互稿index.html，用页外审核工具选视图/状态，无生产可调参数或env要求。

## 审核与交付边界

本轮仅询问手机首次500失败白区（说明、追踪、重新加载蓝色焦点），待用户回答；不包含顶部、其他状态、实际Vue或生产验收。生成/等待重试详情与筛选区域的旧问题保持原状态，不重复索取批准。

未改生产Vue/CSS、后端/Worker/Python、API/OpenAPI、权限、数据库/迁移、重试、env、依赖或部署。旧94/192/140/24及实际父级128PNG、批准和清单不变。全73页重构、未审区域、实际父级C实施、真实后端/权限/生命周期和生产验收继续待办。

276PNG及index/evidence共278输出文件是永久交付。无新临时夹具、下载、日志或服务，浏览器/上下文finally关闭，file URL无监听端口。不清理其他任务历史材料。未部署，无当前重启要求；生产源码未变，不重复构建或旧1624检查，只验证其源与图指纹仍有效。

收尾：完整无参数复验1272检查通过；21项P35定向单测及全部ui-phase2单测通过。正式登记只读核验、153文档/73路由、运行说明、格式门与git diff --check通过。查看桌面正常页、手机首次失败/权限/读取中/后台保留实图；原始交付与生产源未改，无新临时产物需要删除。全站正式审核仍36/73页、整页批准0，不因本包增加批准计数。
