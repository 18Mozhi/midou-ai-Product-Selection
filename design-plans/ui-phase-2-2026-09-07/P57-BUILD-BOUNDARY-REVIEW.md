# P57 构建边界验证

本轮在 `main/af239b08b69f7d97cd0372f9840a70009eeef0c7` 的 P57 未提交实现上，
调整 NavigationShell 动态组件目录。P57 六个内部组件随所属页面导入，
编辑器和消息工作台保留独立分块；完整源差异仅增加六个排除项和一行说明。

## 当前证据

- 类型检查通过；最终 Vite 构建通过，440 个模块。
- 全部 252 个构建文件通过原有前端体积门禁。NavigationShell 为 50,425 字节，
  PlatformManagementCenter 为 47,903 字节，均低于原 51,200 字节限制。
- 与 HEAD 比较，去除动态目录声明后源文本完全一致；模板、权限、路由分派、
  KeepAlive 和页面参数未改。35 个加载入口均存在，60 个三类壳层路由均有组件映射。
- M02-03 导航与 P57 通知定向单元测试 16/16 通过。
- 开发模式浏览器回归 37 通过、1 跳过；相同用例在最终静态构建上再次运行，
  37 通过、1 跳过。跳过项是手机重复的旧保存回执测试，桌面用例通过。
- 覆盖双端通知正文、编辑受众、创建并发布、空态、权限提示、读取失败保留快照、
  取消冲突、独立分页；导航覆盖三壳层手机四入口加更多、键盘打开、拒绝恢复、
  平台入口隔离及无刷新切页。API 使用现有测试样例，不构成真实服务验收。
- 路由产物、文档、静态分析、格式及差异空白检查通过。

构建回归复用 `tests/e2e/platform-message-management.spec.ts` 和
`tests/e2e/m02-03-navigation-shell.spec.ts`，以 Vite preview 提供最终 `apps/web/dist`。
临时配置与测试结果文件已清理，测试服务已停止；保留项目正常构建产物。

## 审核与发布边界

本轮没有改变页面视觉、用户批准状态、API、数据库、环境变量、依赖或权限。
外壳 C 布局、P57 整体审核及生产验收仍待完成，未部署。
未来发布需要本地构建、既有宝塔上传和浏览器刷新；本项不要求后端服务重启。

引用 NavigationShell 全文哈希的历史证据包必须重新复核。
不能用本轮模板一致性检查覆盖其他页面原有的失败或以替换哈希恢复“通过”。
本轮重新运行完整 `npm run test:unit`：1,291 项中 1,108 通过、183 失败。
失败包含现有可读性/样式规则、平台管理源码断言，以及多个页面和共享外壳的历史
源码/图片证据绑定。具体失败仍需逐项追踪，不能一概归为历史噪声。
根据 AGENTS.md 的验证失败不自动提交规则，本轮未提交；全站交付未完成。
