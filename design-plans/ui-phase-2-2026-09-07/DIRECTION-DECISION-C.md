# 用户方向决策 · C

2026-09-08，本任务用户明确回复“C”。记录人为本任务助手，决策人为用户。

- 选择：C「清晰目录 / 目录与范围分区」。
- 对应审核包：F00-1.18-r1，指纹 d0dbcf0700567dbb2614d8aeaacb00802991bceda295ff5052e30bf03af9e063。
- 选择依据图：[P43 C 用户目录](design/account-direction-c/1440-directory.png)，SHA256 为 05e03f67eba8bc7382533b796cfdf7497f8c753b38fe8a8f0e6e64659906785d。
- 授权范围：按 C 方向进入 F05，扩展任务列表、详情和进度弹窗正式图稿；不是仅把 C 留在用户管理页。
- 不代表：全部页面细节获审、所有图已批准、生产代码已实现、允许部署或新增业务能力。
- 旧研究证据中的 pending-user-review 是采集时状态，保留历史；本记录是后续真实选择，不通过改旧图片或旧数据文件制造批准。

## C 方向执行规范

1. 桌面：紧凑白色身份栏、蓝色范围/主行动侧区、白色主要内容面。侧区解释当前对象范围，不放与主列表统计口径不同的假总数。
2. 配色：主蓝 #254a9c、深蓝 #193b80、正文 #202c3d、辅助文字 #58677b、底色 #edf1f6、边界 #dbe1e9。颜色表达主次，状态同时有文字。
3. 排版：中文界面无衬线字体，正文和控件16px、辅助13px起；标题不沿用旧衬线账页大编号。圆角采用8/12/20px层级，不能用零圆角旧视觉覆盖。
4. 列表：主体按真实字段排列，筛选和结果分开；细节不藏进大块装饰区，危险动作收进独立菜单。不同任务页面按其流程构图，不机械复制用户表格。
5. 详情：范围/任务事实与内容/活动分区，主操作就近；保留独立详情 URL 和安全返回，不默认改成路由消失的模态。
6. 移动：范围侧区转换为紧凑顶区，列表信息纵向排列；表单保持44px触控、可滚动和可见操作区，不按比例缩小桌面。
7. 动效：只用于状态切换、控件反馈；支持减少动态效果。轻量CSS优先，不新增依赖。

## 当前第一批

[任务 C 正式提案 r1](design/task-direction-c/index.html)：P23列表、P24详情及进度表单的设计提案，复用仓库隔离样本、不连接API。具体稿件仍待审，不是生产Vue。首次覆盖及未覆盖项见同目录README，不能按方向获审自动通过页面。
