# 共享详情缓存证据阶段修正

## 本批边界

全73页 C 重设计目标继续。本批仅修正直接相关验证代码与记录，不改变 Vue、业务、权限、
API、数据库、依赖或生产配置；未新增/覆盖审核图，未扩大用户批准范围。
采用 ui-skills-root 与 fixing-accessibility，分别定位最小 UI 验证范围和缓存退出/焦点状态；
本地 UI Skills CLI 不存在，未安装依赖。既有399项工作树变更保留，开始时暂存区为空。

## 精确图证链

旧 `detail-lifecycle-capture-review.json` 只描述缓存修复时的26包/3511图/37个精确差异。
之后093d643b（P50通用写入归属）更新了独立焦点包的一张图和3个源码指纹，
但没有更新旧阶段差异表。不能因此说旧阶段图表错误，也不能把整个后续源码变化当成已审核。

单图 `output/playwright/responsive-data-view-focus/390-close-focus.png`：

- 缓存修复阶段54ec47364b3422b1d49a6eea18f070229af56a59：ed7b8531…。
- 后续P50阶段093d643b789cc887edd4762c8612dd3c8904560a：ce9b7f6c…，与当前磁盘及其清单相同。
- 两图均390×900，实际全图测量2个像素不同，最大通道差3，边界[321,37,323,37]。
- 后续图恰好与更早3023a030基线的该图相同，不表示中间阶段从未存在。

新增 `ui-detail-lifecycle-image-stage.mjs` 固定这一个路径、两个完整提交、两个完整SHA256，
并实际解码测量全部像素。旧生成器先检查当前磁盘图与当前清单一致，再核对明确后续版本，
仅在构造旧阶段差异表时返回原阶段字节。旧JSON与所有图片原样保留；其余3510图仍按原流程验证。
未知当前图、用旧图冒充后续图、错误历史字节均拒绝；其他路径完全没有此例外。没有像素容差。

## 源码证据与当前行为分开

原单测把“修复时只增加deactivation hook、20调用者不变”错误地持续套到后来全部页面重设计。
现改为从固定54ec4736提交列出当时20个调用者，与3023a030比较；共享组件自身还按b9e635a3…
固定指纹验证。保留原来的源代码差分约束，不再要求今天的页面撤销后续已实施变更。

两包原始 App 缓存证据（baseline/current目录）均与54ec4736完整清单比较，各30个源码指纹
逐项绑定该提交；截图仍校验当前保存的原始文件。名称明确标为归档证据，不能冒充现在的页面截图。
原baselineReplay、请求列表、四宽度观察、图片数量与批准边界不变。

旧图证测试末尾还把107个预览HTML/CSS/JS的“拍摄时未变”当作“今后永远不变”。
实际只读检查发现其中9个后续脚本已变化，分属browser-runtime、collection-overview和
data-records方向；本批不覆盖它们，也不宣称旧截图已证明这些后续修改。固定拍摄阶段的
107个来源与3023a030逐项比较全部一致，该历史范围断言现也明确使用拍摄阶段文件。

当前源码继续进入真实脚本VM的缓存清理断言，不读取历史代码。新增4种内存反例验证：
删除退出钩子、留下selectedKey、留下trigger、遗漏releaseBackground，均必须被同一行为检查拒绝。
上一轮实际当前Vue72项交互与实际App4宽度回归输入未变，本批不重跑或宣称重拍。

## 验证记录

- 新图证阶段单测5/5通过。
- 当前正反例与历史源码/清单阶段定向4/4通过，18.30秒，无取消/跳过。
- 原图证生成器实际完整校验通过：26包、3511图、37差异、最大像素数101、最大通道差13。
  这里是旧表全部差异的最大值，不是新增单图的2/3，也不是允许未来图片变化的阈值。
- 历史107个预览源码逐项检查，mismatch=0。
- 第一批17项在源码阶段修正前执行，11通过/6失败；其中最后一项是像素生成校验通过后，
  又命中后续预览脚本不变断言。没有将这一失败隐藏或直接覆盖历史JSON。
- 最小验证通过后完成242个文件的完整Node单测：1397项，1230通过、167失败、0取消/跳过，
  209.91秒，退出1。当前生命周期6项及新图证5项均通过；独立焦点文件仍3项失败，不豁免。
- 完整167个失败名称保存在 [完整结果](DETAIL-LIFECYCLE-UNIT-RESULT.json)，本轮未截断。
  上一轮1388/1218/170的失败名称曾被截断，因此不声称逐名证明无新增失败；本轮新增总数9
  包括上一轮新增3项适配器、本轮5项图证和1项生命周期反例测试。
- 文档及运行文档门通过（73路由、60保护、6角色、153必需文档）；全库中的真实工作树格式门
  通过，git diff --check通过。没有因单测总失败而改写其他门禁或撤销后续页面实现。

## 使用与交付边界

继续使用 `node scripts/build-ui-phase2-detail-lifecycle-capture-review.mjs` 校验旧阶段图证，
无需参数变化。不要用 `--write` 覆盖历史结果来掩盖后续修改。当前交互仍使用独立的
`node scripts/verify-responsive-data-view-current.mjs`；历史图证通过不是当前视觉或生产验收。

无新运行时选项/环境变量/OpenAPI/接口消费方或后端变化，不涉及Node/Worker/Python重启。
本批新增脚本与测试是永久回归交付；没有新临时图片、日志或浏览器服务。测试自有临时夹具与
进程在收尾核实。全库格式测试自建的 `C:\Users\23136\AppData\Local\Temp\scoutops-code-style-b3XDhO`
已自动删除，Test-Path=False；父子测试进程12368/62996均退出，暂存区为空。
本批没有页面图与原差异JSON的Git变更。不提交/部署，commit hash不适用；原P58待确认规则
仍待定，不重复询问。后续继续处理独立共享焦点/模态的历史与当前证据，不把其他167失败统称无害。

以前因工具策略无法删除的两处不绕过重试，仍待手动清理：

- `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p47-c-e2e-temp`。
- `C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`。
