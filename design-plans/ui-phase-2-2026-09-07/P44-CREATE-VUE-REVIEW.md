# P44 创建管理员 · 实际Vue C组合审核

2026-09-11，P44-CREATE-ADMIN-VUE-r1，基于cd040baf当前源码。继续全73页实施计划，本批只交创建管理员实际Vue审核稿及验证，不将已批准的手机角色资料、其他目录区域或P43创建稿转授为本弹窗批准。

## 图册与构图

[四宽度106图册](../../output/playwright/p44-create-admin-preview/index.html) · [手机上部](../../output/playwright/p44-create-admin-preview/390-org-admin.png) / [手机下部](../../output/playwright/p44-create-admin-preview/390-org-admin-bottom.png) · [桌面上部](../../output/playwright/p44-create-admin-preview/1440-member.png) / [桌面下部](../../output/playwright/p44-create-admin-preview/1440-member-bottom.png) · [短屏底部](../../output/playwright/p44-create-admin-preview/390-short-screen-bottom.png)

按frontend-design技能延续已选C方向：桌面左侧260px蓝色启用说明，右侧白色账号身份/权限归属；760px及以下上下排列，字段与按钮48px、控件16px。长表单在原生dialog内滚动，操作随内容排列，不增加固定遮挡底栏、关闭按钮或确认步骤。桌面844px高视口下部分五字段表单也需少量滚动，提供上下部图，不把裁切图宣称为全部字段同时可见。

本页真实入口是“新建管理员”，标题由原组件计算为“新建平台管理员”，默认platform_operations_admin。平台选择仍保留普通用户和三种平台管理员，组织角色仍只有member/organization_admin两种创建选项。新增就近说明：“从管理员页打开时默认运营管理员；选择普通用户则不授予平台角色。”该说明有aria-describedby关联，不限制原选项或代替服务端授权。

复用P43共用创建组件的C分区与样式，是同一真实表单的设计一致性；通过新增P44助手加入本页说明，并从当前P44管理员路由、当前创建归属保护和真实管理员按钮重新挂载、提交和截图。没有复用或改名P43 PNG，也没有修改旧P43助手/CSS或原P44审核图。

## 状态与实际合同

390/760/761/1440四宽度；390为28图（含568px短屏两张），其余各26图。默认上下部、邮箱/密码/平台角色/组织/组织角色/取消/确认的七类焦点、三平台角色与普通用户、加入组织两种组织角色、五字段底部、必填/邮箱格式/密码长度无效、确认悬停/按下/等待、失败上下部、重新打开及短屏。原生系统校验气泡、原生下拉展开和移动软键盘不在PNG内，不伪造成就地红色字段错误。

393项浏览器检查；每宽度10GET（5 accounts、5 roles）和3POST均本地拦截。既有E2E样例直接抽取，停用组织为明确合成变体，原管理员记录/全局摘要不变。三个POST分别验证：运营管理员+组织管理员关系、默认运营管理员+空组织、明确选择普通用户+空组织。五字段、空值转null、Idempotency-Key、无新增reason均保持；密码只在本地核对已知合成值，证据只保存核对标记。

等待期间仅确认按钮禁用，输入和取消仍可用；失败保留五字段；取消/Escape回到实际管理员入口；重开清空身份字段并恢复运营管理员默认；成功样例触发原账号及角色重读、关闭和父成功通知。成功后的列表仍返回原测试数据，不能宣称真实新增账号、角色已授予或数据库已持久化。安全/超级管理员选项仅核对选择状态，没有以此替代对应真实创建权限测试。

追加焦点断言时发现程序focus()不足以证明取消按钮的键盘样式；仅修正验证步骤，改用从组织角色按真实Tab到取消，再Tab到确认。最终七类焦点都检查focus-visible、3px蓝色实线；没有为通过测试修改生产组件或浏览器行为。初轮同名图由最终完整回放覆盖，清单严格限制106PNG。

## 验证与边界

50项当前源码LF指纹、两份审核变换指纹、每图PNG指纹与精确文件清单。新增4项定向测试及原整页5项合同共9项通过，完整script、每一条事件/指令/模型、原生控件及后两个密码/原因dialog源文本不变。所有当前验收直接读取工作树源码，不通过历史替换来掩盖漂移。

当前创建归属保护随真实父组件加载，但本批不是完整App/KeepAlive、跨路由在途回执专项复验；不能仅靠393项视觉/样例检查宣称全生命周期通过。关闭后的密码暂留内存、写后重读失败反馈策略均未改变；没有真实SQL/事务/审计、MFA、密码哈希或角色允许/拒绝验收。整页、全部弹窗、三主题两密度、200%原生缩放等继续待完成。

```powershell
node scripts/verify-ui-phase2-admin-create-preview.mjs
node --test tests/unit/ui-phase2-admin-create-preview.test.mjs tests/unit/ui-phase2-admin-page-assembly-preview.test.mjs
```

默认只复验。仅有意重制本批正式图时使用--capture，不覆盖P43或原P44图包。复用现有依赖与空闲本地端口，finally关闭Vite/浏览器。

827项完整相关UI回归通过；73路由/60受保护路由/6角色、153文件文档门禁、格式与diff空白检查通过。没有生产代码/CSS导入、路由、API/OpenAPI、配置、.env、依赖、后端、Python、插件、数据库或权限变更，相关生产运行配套不适用；无需生产重启，本批未部署。当前生产源码和上次构建输入未改变，不重复生产构建；审核Vue/CSS已通过Vite编译与浏览器运行。

106PNG、图册和证据为正式交付保留，无一次性临时文件；59773、59966、60018端口均已核查无监听。前次整页/焦点及其他未答问题继续待审，本批仅请求手机创建窗上下组合布局审核；不自动接入生产或宣称全73页完成。
