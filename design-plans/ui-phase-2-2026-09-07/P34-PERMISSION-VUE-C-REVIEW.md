# P34 温和权限提示 · 实际 Vue C 组合 r1

日期：2026-09-13。状态：本地实际 Vue 审核预览，新增区域组合待审。沿用用户已批准的
四处温和措辞，不将措辞批准扩大为本组合、真实权限或生产通过。

后续用户已批准[手机后台403展开追踪/重读焦点这一张白区组合](P34-PERMISSION-VUE-C-APPROVAL.md)。
只更新该区域审核记录；采集时pending保留，不将桌面、80图整包、顶部或生产标为通过。

## 改动范围

在当前 C 父级预览上，仅为 `view === 'approvals' && state === 'forbidden'` 增加展示分支。
白色区域包含已批小标题/标题/说明/数据提示；原 `notice` 与 `requestId` 原值保存在原生
可展开详情内；蓝色“重新加载”仍调用原 `load()`。

新CSS仅在审批父级实际包含该权限区域时生效，隐藏旁边原来的重复错误状态区，使可访问
的“重新加载”只有一个。原父级脚本、错误分类、API、权限、重试、原子读取和恢复查询不改。
后台403可能仍持有内存旧数据，因此不以 `!data` 作为权限提示条件；审批子内容始终按原
状态机撤下，不把权限拒绝画成“没有记录”。其他页面/状态与已批500/429提示分支保持。

这是内存组合，不修改生产Vue；原整页r4、父级r3和措辞批准图片均保留，不覆盖或重写哈希。
新增按钮仅转发既有读取动作，不新增业务动作、写入或权限入口。

## 证据与图片

[正式清单](../../output/playwright/p34-permission-vue-c-r1/evidence.json)：186个源哈希、
56组故障/恢复、926项浏览器检查、80张完整视口PNG。两宽1440×1000与390×844；
336次父级两个端点的本地样例GET，零业务写入/意外请求/页面运行错误。336不是全网络请求数。

沿用完整父级矩阵：summary/approvals、首次/后台、500/503/409/429/401/403/网络中断。
全部原检查保留；八组403额外验证：四文案中的主标题/说明/数据提示、唯一可访问重读入口、
原重复区隐藏、追踪初始关闭、Enter展开、实际错误/编号保留、Tab抵达可见无遮挡的按钮，
展开/聚焦不发请求，Enter走原恢复读取。小标题另由静态原文绑定测试确认。

| 图片 | 范围 |
| --- | --- |
| [桌面首次拒绝](../../output/playwright/p34-permission-vue-c-r1/initial-approvals-permission-forbidden-1440.png) | 新白色权限区域，追踪折叠 |
| [手机后台拒绝](../../output/playwright/p34-permission-vue-c-r1/background-approvals-permission-forbidden-390.png) | 原内容已撤下，不宣称空目录 |
| [手机展开与焦点](../../output/playwright/p34-permission-vue-c-r1/background-approvals-permission-forbidden-reload-focus-390.png) | 实际错误和编号，蓝色重读焦点 |

64张原矩阵场景图加八组403的展开/重读焦点各一张，共80张。取景为实际完整视口，按
反馈区域滚动；焦点图保留原生Tab滚动位置，不在截图前重新滚走焦点。不是整张长页截图，
也不证明全部断点/主题/软键盘/完整焦点生命周期。偏好仍是已有deep-ocean样例。
原错误出现“隔离…”字样是测试故障消息；不是正式产品文案。编号为本地样例追踪。
409沿用合成客户端分类器场景，不增加API契约。未验证真实后端、SQL、RBAC、真实超时、
范围切换/卸载、并发权限变化或生产环境。

## 单测与不变项

新增6项与原父级/整页12项合计18/18通过。覆盖父级全部script不变、模板可逆/编译、
四处已批措辞来源、原错误插值/事件、新样式范围、所有旧检查保留、参数拒绝、当前源/80图
绑定，以及原父r3的64图和措辞批准对象字节不变。
历史6项共享源哈希失败不因这批新证据自动转绿；未改旧测试期望，也未宣称旧像素比较被
当前矩阵替代。全73页、完整控件与生产门禁继续，不依据此局部工作宣布任务完成。

## 运行、配置与交接

`node scripts/verify-ui-phase2-org-approvals-permission-vue.mjs`：完整矩阵、不写图。
`--smoke`：390宽12组；`--capture`：写独立r1目录，已有目录在浏览器启动前拒绝覆盖。
仅一个参数，未知/重复/组合参数拒绝。固定原驱动及父级r3组合源码哈希，变更需先核对。
复用已有依赖、本机随机端口、全API拦截，finally关闭浏览器/Vite。
没有生产API/OpenAPI/环境/依赖/数据库/安全规则修改；无需部署、迁移或重启。
工作树混有前序阶段改动，旧关联门禁未通过；不暂存、不提交、不部署，commit hash不适用。

本轮80张图为正式审核交付，无临时文件或失败中间图产生；未重试任何此前被策略拒绝的
清理操作。浏览器和测试服务均已关闭。具体区域组合等待用户审核，其他待答问题不替换。

### 收尾与后续状态核对

P34全部14个关联测试文件共57项，51通过、6失败；失败仍是既有历史共享源哈希断言，
本轮未修改它们。格式、153项文档门禁、73路由/60保护路由/6角色产物和运行文档检查通过。
本轮端口61988/62144已确认无监听，暂存为空，无临时产物需要清理。

接续查看本包 `initial-approvals-session-expired-390.png`，确认401仍有旧红色提示条、
重复错误说明、橙色重新加载按钮，不能算C完成。对应真实源码是父级默认notice分支与
`org-admin-state` 的expired分支。下一状态应保留原401分类/撤下内容/GET重读合同，重做
这两个显示区域；不得仅因提示“登录已失效”擅自新增登录跳转或改变恢复操作。
后台普通失败保留旧内容的反馈也仍需C重设计；本次通过不扩展到这些状态。
