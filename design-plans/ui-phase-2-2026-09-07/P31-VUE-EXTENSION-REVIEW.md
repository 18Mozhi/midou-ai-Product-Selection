# P31 延期字段组合 · 真实 Vue 落地

2026-09-10，从干净main/e89cf8fc继续。依据P31-EXTEND-COMPOSITION-APPROVAL.md的手机组合批准及真实ResourceGrantService.extend规则；桌面适配不自动视为已获批准。

[真实页面图册](../../output/playwright/p31-approved-controls-review/index.html) · [手机延期错误组合](../../output/playwright/p31-approved-controls-review/390-extension-not-later.png) · [源码与图指纹](../../output/playwright/p31-approved-controls-review/evidence.json)

## 落地内容

OrganizationRolePanel延期内联表单增加关联帮助、原因字数、日期aria-invalid和常驻aria-live错误节点；只有存在不晚于原期限的有效输入时显示就地错误，修正后自动清除。空值仍由原生required处理，不编造新的必填/原因长度规则。

日期输入min取现有min与原授权期限的下一可选分钟较大者。datetime-local原生默认步长为一分钟，遇到整分钟或含秒的原期限都严格向下一可选分钟推进；原期限缺失/无效时退回既有min，不猜期限。既有后端已拒绝新期限小于或等于旧期限，这次只是把同一规则前置到输入提示和原生校验，后端依然最终校验当前状态、权限、期限与expected_version。

局部CSS使用已有roles-tokens增加字段语义色，采用白色表单面、中文无衬线、蓝色字段与焦点、红色错误、底部右对齐操作；只影响延期form。创建表单、共享原因窗、父级PATCH/版本/trim/30天校验、refresh watcher及草稿归属不变。原始期限、最大期限和默认“现在+7天”不被重定义为“原期限+7天”。

## 验证与证据

复用verify-ui-phase2-roles-vue-controls.mjs：--smoke为手机27检查；--capture为双端54检查/22PNG，其中原控件与相邻页面12张，新延期组合10张；无参数核对源图指纹并重新执行。新组合是默认、未延长、修正、等待、失败。错误图先关闭原生校验气泡，保留就地错误，不让瞬态气泡遮住字段。

实际Vue在1440/390均验证等于/早于原期限不发写请求，下一可选分钟清错并发送一次原有PATCH。断言expected_version=1、trim后的原因、ISO期限不变；等待时字段仍可编辑、按钮禁用，隔离409后保留后续原因草稿且无假成功。PATCH被浏览器夹具截获，不接触真实接口或数据库；不能当作生产授权与审计通过。P30撤销邀请样式、P31撤销取消返焦继续回归。

两项新增源码回归覆盖下一分钟、含秒、无效原期限和错误恢复；P31与主题定向29项通过。字段说明改变延期form的文本指纹，签名由64b97b8b5fe9de71更新为201e6ad7077e4318，emit表达式及业务事件未改；真实source hash及两份语义合同同步，不绕过漂移检查。

旧48/378/202图包按新Vue来源复验并重新采集，数量/设计代码/数据不变；部分原生focus/pressed/日期选择截图字节更新，不能宣称所有旧PNG字节不变。获批手机延期组合SHA256仍为a1f7d4f1589a722f2cbef181b8fef5bcd9df2bb77f3350eb60dbe6a42df2fd11，批准不扩展到别图。

## 操作及剩余边界

最终完整单测640/640通过；静态390文件、前端构建407模块及251资产预算、153文档、路由/运行文档与生成器检查通过。临时夹具与下载目录已确认不存在，临时服务端口无监听；22张正式审核图保留。首次完整测试的合同旧行号失败已修正后全量复验，无未解决测试失败。

用户按原入口填写原因和新到期时间；不晚于旧期限时先修正再提交，不需新配置。等待中继续编辑的既有能力保留，未擅自修复尚未确认的刷新/写后草稿归属问题。

无新依赖、环境变量、API字段、路由、权限或迁移；OpenAPI、后端/Worker/Python及部署器不涉及。未部署、不需当前重启，后续仍走完整宝塔发布门禁。临时本机服务/浏览器自动关闭，正式图和证据保留。整页C布局、桌面具体批准、创建/撤销其他组合及全部页面与生产验收仍待。
