# P71 实际 Vue C 组合 · 批次68

P71 原有 CAPACITY-C-r1 是离线稿。本批新增的 Vite 转换用于审核：保持 `CapacityBoundaryCenter.vue` 的 script、GET、15秒单飞、权限、快照、签认写入和原有确认弹窗，使用原组件 DTO 将通过档位/停止事实、性能参考、归档恢复、资源绝对值与告警处置分区。对应真实 Vue 页面已随当前发布版本部署；本地审核不打开或提交签认。

`tests/unit/capacity-page-preview.test.mjs`通过；实际 Vue 在1440/390和两种动效下共144项检查、52次本地 GET，无外部请求、写请求、下载或页面错误。r1图册为16PNG/171来源指纹，适用于默认布局、读取状态与焦点审核，不代表真实容量测量、签认、恢复、权限、审计或生产。资源以原始绝对值显示，不使用8192/262144显示封顶绘制成容量水位；返回的规划用户数和通过档位也不表述成并发承诺。

批68复核既有读取语义：503保留上次成功容量事实并显示“刷新未完成”；403清空受保护事实，只显示“没有平台运维权限”和重新核验入口。真实 Tab 可到达顶部“刷新实测事实”与保留快照区“重新核验”，两者均呈现3px蓝色轮廓。签认按钮、确认词、处理函数和固定写入合同均保持原样，但本地审核未打开确认窗或发送 POST。

无需新增配置；当前发布已完成宝塔部署、Node/Python 重启和公共健康检查。用户已确认设计审核通过，仍需核验告警详情、签认弹窗、操作结果和真实容量/恢复证据。
