# P47 C 配色集中化与实际组件复核

后续进度见 [颜色角色收口复核](COLOR-ROLES-CLOSURE-REVIEW.md)：当前 P47 图包为 174 个源文件，包含递归导入依赖；全局颜色门禁已通过。下文保留本批次当时的 170 源图包和像素比较结论，不将其套用为后续重拍的逐像素结论。

2026-09-12。在现有已批准局部 C 构图上整理颜色，不重新设计布局、不扩大审核结论。

## 改动及边界

`design/provider-adapter-tokens.css` 集中保存 37 个 P47 颜色角色：原页面 17 个加上
页面状态、详情和反馈原有的 20 个颜色值。保留原 `html:has(body #app .adapter-center--c)`
条件，供页面、Teleport 详情和反馈继承。三份 C CSS 明确导入调色板；P46 不匹配。
原 4 个保留但当前未使用的角色一并原样迁移，未借此删除旧变量或改变遮罩透明度。

原始 Vue 脚本、六个模型、所有事件、条件与显示事实均未修改。现有样式合同单测仍逐条
比较审核稿的选择器和声明，新增颜色变量解析及断点祖先比较，原 17 个变量逐值对照，
不是用更新源码哈希代替样式等价验证。调色板只允许精确活动页面条件下的变量声明。

API、权限、探针规则、数据库、配置和依赖不变。OpenAPI 及后端/Worker/Python 消费方
不涉及此纯样式整理；没有执行真实健康检查或生产写入。

## 实际验证

- 调色板与 P47 原有运行/模板/样式/证据/交互合同：10/10 单测通过。
- 原实际 Vue 整合运行器：36 组、430 项检查，通过 P46/P47 样式隔离、缓存往返、
  初读/刷新/检查成功或拒绝、同来源反馈及详情 Tab/Escape/返回焦点。
- 原 M03-03 桌面和手机功能：16/16 通过。
- [20 张当前实际组件图](../../output/playwright/p47-c-integration/index.html)及
  [机器证据](../../output/playwright/p47-c-integration/evidence.json)已重新生成。
  170 个当前源指纹明确包含新调色板。PostCSS 会把 import 折入所有者，不能仅凭
  Vite module graph 自动收集；运行器因此显式登记该文件。
- 图证据的源集合相比旧包新增两个已存在的内容组件及调色板，移除三个通知内部组件，
  来源是此前 NavigationShell 懒加载边界调整，本轮未修改组件派发器。
- 测试时钟现在固定 Date 为 `2026-09-11T05:00:00Z`，计时器继续运行。此前只 install
  时钟会随测试耗时增加“最近刷新”秒数，不能把该波动当成设计变化。生产时间逻辑未改。
- 与 HEAD 旧 20 图逐像素比较：14 张完全一致；4 张仅最近刷新末位秒数变化，
  范围为 x356–363/y419–428，分别 70/70/58/70 像素；另两张分别有 62 和 9 个边缘
  像素差异，最大通道差均为 2（顶部工具边缘及记录卡角部）。没有把这些 6 张写成零差异。
- 关联平台页双端回归 72 通过、2 个桌面已覆盖的手机重复用例跳过；81 份 Vue 证据
  的图片、当前源码/CSS、测试、辅助程序指纹及无页面横向溢出均已核对。
  初次有两张桌面图与上轮 SHA 不同，定向重拍 2/2 通过后，P54 溯源图恢复相同，
  P57 新建全员编辑图仍不同；本轮未宣称这张图逐像素稳定，也未因此提升待审状态。
- 前端类型检查、构建、原 252 文件体积预算和静态分析通过。

全站颜色检查已越过 P47 三份 CSS，但仍失败于 P46 `provider-approved-feedback.css`；
其他颜色声明、旧源码与历史图证据门禁继续处理。本批不是全站通过或生产验收。

## 使用、审核与收尾

以后调节 P47 配色只编辑该调色板，并复跑上面的整合运行器及单测。
界面审核范围保持原样；已批准的默认布局、手机详情分区、成功/拒绝反馈和两空态区域
没有自动扩大为全部交互、焦点修复、真实权限/探针或整页批准。

未来仍需按既有宝塔流程本地构建、上传前端并刷新浏览器；本次无需任何生产服务重启。
未提交、未部署，commit hash 不适用。正式图册和 JSON 保留；测试运行器关闭 Vite/浏览器，
临时 E2E last-run 文件在收尾删除，不删除既有正式测试及审核资料。
