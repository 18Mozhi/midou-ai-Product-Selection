# P11 实际 Vue C 组合 · 批次83

本批新增审核用 Vite 转换，同时保留 `AccountShell.vue` 的 query 分区导航与 `PersonalCenter.vue` 的 profile-first 读取、资料/偏好写入、密码入口、会话撤销及资产链接。审核层重构为蓝色账号边界、六项分区目录、资料与分区事实区；不保留旧账页色、圆角卡片、渐变或营销式布局。

实际 Vue 在 1440/390 与两种动效偏好下通过 36 项检查，生成 12 个本地拦截写入：资料 PATCH 精确包含七个允许字段和 `expected_version`、不包含邮箱；偏好 PUT 精确包含五个开关和版本；会话撤销只请求当前会话 ID 并在成功后从本地列表移除。r1 图册覆盖资料、保存反馈、权限、安全、资产、通知及资料读取失败的双端 14 PNG/5 个受控来源指纹。

无需重启、部署或配置变更。转换与图册仅供用户审核，未改生产账号组件、API、会话或个人资料。密码真实写入、通知邮件拒绝、分区读取失败保留、busy/重复提交、读屏与生产验收仍待完成。
