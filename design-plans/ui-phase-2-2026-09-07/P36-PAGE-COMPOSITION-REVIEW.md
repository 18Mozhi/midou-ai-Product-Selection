# P36 整页 C 组合 · 实际 Vue 审核稿

从干净 `main/2454db5f` 继续。将创建、明文、列表与原因窗局部审核稿接成该路由的完整内容。写后读取失败反馈、离页原因草稿两项选择未答，保持原行为，不因整页出图而默认批准。

[44 张图册](../../output/playwright/p36-page-vue-preview/index.html) · [桌面完整图](../../output/playwright/p36-page-vue-preview/1440-default.png) · [手机完整图](../../output/playwright/p36-page-vue-preview/390-default.png) · [证据](../../output/playwright/p36-page-vue-preview/evidence.json)

## 整体设计与本次审核

使用 `frontend-design`，采用**路由标题/刷新 → 蓝色用途说明 → 数量概览 → 筛选/记录 → 安全说明 → 创建表单**的顺序。明文出现时仍独立放在数量概览与管理区之间，不混入行或通知。桌面内容最大1280px，数量五列；手机数量三列并把最后一项扩展到两列，保留所有文字和数量。

初版手机首屏被说明和数量占满，完整图检查后把详细安全边界与“令牌不是成员账号”说明移到创建表单之前；没有删除或隐藏内容。管理区在DOM和视觉上均先于安全/创建，键盘顺序不靠CSS order假重排。所有表单仍常驻，不新加折叠/抽屉或业务入口。

刷新、顶部说明、数量、安全区、通知和首次读取失败区域采用蓝/白/灰C样式；旧网格和黄色底板已在审核宿主移除。桌面筛选四字段/帮助文本/底部重置采用白色区域，761–1100px为两列，宽屏四列；**原已批手机筛选节点与专用CSS未改**，不声称其在新整页容器里像素完全相同。

本次请求确认**桌面与手机完整内容的整体顺序和布局**，不是所有按钮/局部、错误政策、字段上限、真实写入/权限或生产验收。首屏图不能代替完整图；完整默认图不包含应用导航壳层。

## 实现方式

`scripts/lib/ui-phase2-token-page-preview.mjs` 顺序组合既有 create/secret/list 三个审核转换，再用Vue AST重排唯一 workbench 并移动原安全说明节点。父组件脚本和模板不转换；子组件完整script、所有原按钮/输入/select/summary、字段表达式和指令保持，仅保留已有明文审核新增的data-copy-state。原两项安全说明各出现一次。

宿主导入五份审核CSS（create/secret/list/reason/page）；新的整页样式只作用 `.p36-page-preview`。既有四份局部CSS与图片不修改，生产源不import这些文件。创建及列表区域保持各自已交付结构；桌面筛选新增视觉样式属于本次待审内容，不借用手机批准。

## 验证与未覆盖项

390 / 760 / 761 / 1440 四宽度，**164项浏览器检查、44PNG、46项源指纹**。每宽度11图：首次读取中、默认完整图、默认首屏、第二页、无结果、创建scope必选错误、明文出现、明文清除、轮换原因窗、撤销原因窗、首次读取500。弹窗与首屏为viewport图，其余为完整路由内容图。

- 原E2E八记录与固定审核时钟2026-08-26；创建新增记录/secret是明确的合成夹具，不是当前生产数量或真实凭据。
- 每宽度6 GET + 1创建POST。分页6/2与URL、搜索/重置、scope必选不发请求、精确创建body/幂等键、成功清草稿/重读数量9/明文完整显示/保存后清除均通过。
- 轮换/撤销原原因窗使用真实modal，Escape取消并归还触发焦点；本批不提交这两种写入，不改变此前诊断问题。
- 每图检查页面横向溢出，默认七个主要区域逐一滚动并做三点遮挡检查；原生按钮/字段未丢失或重复。旧网格背景最初覆盖新底色，已修正审核选择器；最终44图完整重拍，未保留失败布局为最终证据。
- 请求全部拦截，零外部/意外写入、浏览器错误或storage；禁止OS剪贴板。不是全App/导航壳层/真实认证、权限、MySQL、审计、缩放/所有主题密度或生产集成证明。桌面新增筛选样式的全部控件状态尚需细化。

## 使用及收尾

```powershell
node scripts/verify-ui-phase2-token-page-preview.mjs
node --test tests/unit/ui-phase2-token-page-preview.test.mjs
# 需要更新本批正式图时加 --capture；普通复测不改图。
```

生产Vue/CSS、API/OpenAPI、后端、数据库、环境、依赖和权限均未改；不需要配置调整或重启，未部署。44PNG/index/evidence和永久测试为正式交付物；中间同名图已被最终回放覆盖，无额外临时文件，Vite/浏览器finally关闭。

整页布局新待审；局部待答和两项行为规则不转授批准。全73页实际C接入、完整壳层/真实验收及宝塔交付仍继续。

最终门禁：4项新增定向测试与697项相关回归通过；73路由/153必需文档、格式和diff空白检查通过，原102图包/15069PNG审计无漂移。新44图独立登记，不增加批准页面数量。审核端口61293/61333/61487/61664均已关闭，无验证临时文件残留；生产代码/配置/依赖未变，不重复此前有效构建。
