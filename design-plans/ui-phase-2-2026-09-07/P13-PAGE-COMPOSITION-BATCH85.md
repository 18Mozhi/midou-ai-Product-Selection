# P13 实际 Vue C 组合 · 批次85

本批新增审核用 Vite 转换，保留 `NavigationShell.vue` 的成员守卫、导航与路由行为，以及 `TaskWorkspace.vue`、`TaskListPanel.vue`、`TaskBatchActions.vue` 的本人列表读取、汇总、成员目录、创建、选择、分页与逐项批量操作。审核层重构为蓝白个人行动台，明确“今日工作”是本人任务而非仅今天到期；不保留旧账页标题、米色背景、圆角卡片或营销式布局。

实际 Vue 在 1440/390 与两种动效偏好下通过 28 项检查，产生 8 次本地拦截写入：新建任务 POST 精确发送标题、空说明、普通优先级与空期限；批量暂停只向符合资格的进行中任务 POST 当前版本和原因。r1 图册覆盖默认、创建成功、批量暂停和读取失败的双端 8 PNG/6 个受控来源指纹。

无需重启、部署或配置变更。转换和图册仅供用户审核，未改生产任务组件、共享导航、API、任务状态机、权限、版本锁或审计。真实会话/RBAC、成员目录失败、批量部分失败/重复提交、迟到响应、读屏和生产验收仍待完成。
