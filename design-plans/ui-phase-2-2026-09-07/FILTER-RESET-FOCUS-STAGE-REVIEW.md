# 筛选重置焦点：当前行为与历史证据

## 本批范围

全73页 C 重设计目标继续。只修改 `tests/unit/responsive-filter-reset-focus.test.mjs` 与本批记录，
不改生产Vue、视觉布局、原驱动、历史转换器、API、权限、环境变量、数据库或依赖。
没有生成或覆盖图片，也未扩大用户批准范围。采用UI路由及无障碍技能检查重置、关闭、焦点返回；
本地UI Skills CLI不存在，未安装依赖；浏览器复用项目已有Playwright驱动。
开始为main/af239b08、410项未提交变更、暂存区为空。

## 依据与修正

原测试同时把后来含外观、颜色、useId/ARIA关联及动态效果修正的当前组件，要求等于重置修复时
的a566080f…完整源码。它还通过后续P44/P43历史转换器串接旧图证，导致当前依赖被误用为旧截图依据。

现以固定01af02620bdbb751cc846e6a02081d4a0b735784读取重置修复阶段；旧对照组件来自既有
07c492963f56e266864143cfadaa1e6f6ef5627d。两包完整清单与01af0262一致，各34个来源逐一核对；
baseline包只有该筛选组件按07c4929验证，其余33个来源仍按真实拍摄提交验证。
保留原模板/样式差分、76/80条观察、每包8张图、全部目录清单与文件哈希检查。

历史转换器本身未改，也没有添加当前源码例外；新测试明确确认它拒绝当前后续版本以及未知文本。
当前行为测试改为直接读取原始Vue，避免经过任何历史转换；两种修复焦点路径及十种不抢焦点边界
全部保留。新增四种内存反例，删除disabled判断、await后的open/overlay判断或新焦点保护，
必须被同一个不抢焦点行为断言拒绝。反例只存在内存，没有更改生产源码。

## 实际验证

- 完整相关单测文件15/15通过，13.72秒，0取消/跳过；包含两项原历史失败和新增反例。
- `node scripts/verify-ui-phase2-filter-reset-focus.mjs`：当前实际Vue/账号父组件，4宽度
  （390/760/761/1440）、80检查通过，37个实际加载来源，0图片。
- 同一命令加 `--baseline`：只替换指定旧筛选组件，其余仍为本轮实际源码，4宽度76检查通过，
  37个实际加载来源，0图片。它是本轮旧缺陷对照，不是重新拍摄原34源历史整包。
- 每个模式、每个宽度仅4次拦截的本地账号GET（两种操作各读取及重置一次）；共32次样例GET，
  0业务写入，未知请求被拒绝。验证点击/键盘重置、焦点回关闭按钮、Escape关闭/回触发器、
  URL保留/清除筛选，以及文本输入、外部焦点、新原生弹窗、卸载等边界。
- 浏览器只覆盖默认外观、responsive模式、reduced-motion；不冒充其余外观/强制dialog模式、
  全部调用页面、完整无障碍、真实账号权限或生产验收。

本批仅改隔离测试，未改其他测试输入、运行时或公共验证库，因此以完整15项相关单测和两套
真实浏览器矩阵作为完整相关验证，不重复运行243文件全库。上一轮1403项/1239过/164失败
仍是上一轮快照；不据此推算新的全库失败数量，也不把局部通过称作整体发布门通过。

## 使用与收尾

上面两条既有命令可复查；不要加 `--capture` 覆盖历史包。无配置调节或生产重启需求。
两次浏览器/宿主均退出，59689/59999端口已无监听。没有新临时文件、图片、日志或夹具；
本批记录是永久交付。历史16张图及其清单不变。文档门（73路由、60受保护路由、6角色、153份
必需文档）、运行文档门、格式门及diff空白检查均通过；收尾工作区412项变更，暂存区仍为空。
整体验收未通过，不提交或部署，commit hash不适用。后续继续定位其他页面来源与图证失败，
不把它们统称为无害历史问题；原P58待审和未知创建结果规则仍待定。

以前被工具策略阻止删除的两处不绕过重试，仍待手动清理：

- `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p47-c-e2e-temp`。
- `C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`。
