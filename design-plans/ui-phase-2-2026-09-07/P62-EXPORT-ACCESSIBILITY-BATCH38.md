# P62 导出上限与等待焦点 · 批次38

起点main/6025c198，539项在途变化，索引为空。继续已授权实施，保留批次36/37及原529项工作；自动继续不视为P62视觉或既有API覆盖测试修复批准。

## 实现

依据服务端PlatformDashboardService.exportLogs现有去首尾空白后2–300字符校验，P62向共享AuditedReasonDialog传入已有maximumLength=300，复用原生maxlength和已有字数帮助。不改共享组件、服务端、原因最小长度、默认原因、trim或请求字段。浏览器普通输入最多300 UTF-16单元，与当前JS length计量一致；不把原生输入限制当安全校验，也不宣称程序化绕过已被前端拦截，服务端仍独立校验。

P62导出按钮在实际导出中使用aria-disabled/aria-busy并保持原生可聚焦，灰色等待外观和文字保留。只在读取且尚未导出时沿用原生disabled；导出进行中不因刷新状态将按钮移出焦点。exportCsv入口拦截exporting/refreshing，原因返回后再次检查exporting，防止另一个已开始操作被重复提交。请求完成后原节点恢复可用，未增加强制focus调用，用户已移焦时不会抢回。

使用fixing-accessibility技能核对等待语义和字段帮助。实际看r3发现新增上限span被共享label grid拆成三行，仅P62 scoped深层label/textarea改为连续文字加8px输入间距；其他页面及共享组件文件不改。r4已修正连续说明。

## 验证

新增3项实际Vue脚本单飞测试先全部失败：在途再次调用发出两次、两个已解析原因发出两次、读取busy仍发出一次。增加前后guard后3项通过，加原P62/M06-02平台面板共57项通过。两个同时解析原因是测试桩直接覆盖异步防御路径，不宣称原生UI会打开两个原因窗。

更新永久`node scripts/verify-log-export-reason.mjs`，默认不写文件；`--capture-review rN`独占图册目录。实际App/原客户端/共享弹窗、本地E2E样例，1440和390宽度、两种动效共272项：尝试输入301字时实际只接受300字、帮助计数300/300、原最少两字/取消/Tab循环、长度说明没有三行grid拆分；提交后焦点回到同一等待按钮，灰色/aria-busy/焦点可见，原生click与Enter/Space不重开窗或再发请求；失败后仍持焦，第二次明确提交期间主动移到刷新按钮，失败后不抢焦点。4次本地GET、8次本地拦截400的POST，零真实下载/写审计/外部请求。颜色断言等待原有过渡结束，不修改产品动效或加速计时。

另复跑追踪144、默认77、读取键盘270（真实15秒）与生命周期140项；完整前端类型/构建和253资源预算。最后仅P62原因标签CSS调整后重跑272项和构建，读取/路由逻辑未再变动，不重复这些已有效检查。构建基于混合工作树，不作为干净生产发布包证据。

## 图册与审核

最新`output/playwright/p62-export-reason-r4`：双端14PNG、index.html、manifest.json和README.md；168捕获来源/14图指纹，HEAD6025c198加当时工作树。每端默认、确认焦点、最少字数禁用、300字输入、等待视口、失败块、失败后重开共7图。r3同样14图为长度标签分行问题的修正前对照，原清单不回写；r2手机按钮视觉申请仍只对应原图，不扩大为新增字数说明或完整交互批准。

本次可审核手机连续长度说明、已输入计数与输入区的组合；等待视口只证明该按钮的灰色/焦点外观，不包含其余导航筛选或完整页。实际操作基于模拟数据，不是生产日志/真实导出或真机、软键盘、完整读屏验收。

## 未改变、使用与剩余

仅P62生产组件、P62审核CSS和直接测试/文档变更。OpenAPI/外部参数、后端/Worker/Python/插件、数据库、.env/配置/依赖、权限、CSV内容/200条范围/审计/幂等键策略不变，无新增消费者或配置联动。使用原导出按钮和原因输入即可；未来前端静态包发布生效，无Node/Python重启需求，本轮未部署。

没有冻结导出范围，没有在原因等待或请求离页时增加取消/响应归属，也未拆读写共用message。旧快照对应条件、真实权限、服务端CSV/幂等/审计、成功下载后的焦点、全73页实施与部署验收仍未完成。此次防重仅是一个请求进行中及原因返回后检查，不表示完整导出生命周期已验收。

既有API覆盖测试223/225计数失败的输入未变化，仍待用户处理决定；不重复运行相同失败测试，不擅自调整无关测试。按验证失败不自动提交规则，继续未暂存/未提交，commit hash不适用。

永久测试/脚本和r3/r4图册为交付物保留；无一次性脚本、下载、日志或临时图片。所有Vite/浏览器由finally关闭，项目必需构建产物保留。既有清理受阻目录不触碰。
