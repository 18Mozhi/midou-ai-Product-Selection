# 统一审核台补齐 P57 / P58 · 批次 15

状态：审核工具接续与既有材料归档；不代表新页面批准、完整实现或生产发布。全 73 页目标继续。

## 本次交付

统一入口为 [逐页审核工作台](review.html)。搜索 P57 / P58，在“真实 Vue 对照”查看新结构及指定反馈图，也可以展开来源差异、打开原图和图包记录。P57 整体组合 r2、窄桌面 r3，以及 P58 双工作区 r1、读取反馈 r1 共 4 组 37 图接入。原 10 组 506 图不移除；合计 14 组 543 图，登记 34 张就地预览，覆盖 P34 / P44 / P46 / P47 / P57 / P58 六页的补充材料。不是新增 543 张图，也不以图数计算完整页数。

按 2026-09-14 用户授权接管既有 UI 在途改动，将审核台所需的已有补充索引、展示、样式与测试正式纳入；没有替换用户批注存储键、导出字段、决定选项或批准记录。记录意见的身份仍是页面/材料 ID/原 manifest SHA；缺少用户明确确认时不记为批准。`frontend-design` 的界面语义核对用于区分“对照图”和“已实施/已验收”，不把审核稿误称生产结果。

## 指纹与历史边界

新增显式清单格式：既有 evidence/sourceHashes 使用 LF 标准化源码摘要；新 manifest/sources/images 使用原始字节摘要。格式必须显式选择，不猜字段或静默回退；清单路径仅限 evidence.json / manifest.json，所有读取必须留在仓库内。PNG 原始字节始终核对，未知图片、重复 ID、越界路径或错误摘要拒绝通过。

索引生成时发现 13 组材料存在来源差异，P58 读取反馈的 11 项选定来源一致。P57 两包分别 3/1 项，P58 双工作区 4 项，P34 两包各 6 项，P44 四包各 1 项，P46 两包 11/3 项，P47 两包 11/10 项。差异在审核台逐文件显示，既不偷偷重绑定旧图，也不自动撤销过去局部批准。P58 读取包仅检查 11 个选定来源，不宣称完整依赖图。

14 包共有 578 个直接图片/清单/图册/报告依赖。当前接管其中此前未跟踪的 202 个文件及有在途版本的 149 个文件；新文件约 8.81 MB。它们均是既有正式 UI 图证，不是本次生成的临时图片。此次未重绘、删除或改写这些图包；Git 保存先前已跟踪版本。图包报告内更早的驱动、源码、历史链接仍按各自批次记录，不宣称本次归档使每一个历史执行环境均可重建。

## 验证结果

- 15 项材料索引单测通过：原始 PNG、版本身份、缺失/变更来源、目录边界、两种源码哈希规则、错误格式拒绝，以及 14 包 543 图/34 预览与生成数据一致。
- 使用项目既有浏览器验证入口，无采图参数：1440×1000 和 390×844 均验证 73 路由列表、筛选、六页补充材料及来源差异、全部 34 预览解码、版本化批注、Escape 回焦、保存重载、导出、历史意见和横向溢出。两个视口均错误 0。
- `node scripts/build-ui-phase2-recent-review-materials.mjs --check` 核对本地当前工作区索引。它是来源变化检查，不保证在源码不同的旧 checkout 上仍显示一致；不同版本必须先检查差异再更新索引。
- 这是审核工具验证，不是重跑所有产品动作、MySQL、权限、网络故障、生产或全站验收。此前完整产品套件仍有未收口项，不能以本批绿色检查替代。

## 使用、运维与收尾

用浏览器打开 review.html → 搜索页面 ID → 展开指定图 → “记录这组图的意见” → 导出意见。意见应注明具体图与区域；正式批准仍由用户明确确认，不用“继续”替代。

维护：`node scripts/build-ui-phase2-recent-review-materials.mjs --check`；检查来源差异后才用 `--write` 更新索引。回归：`node --test tests/unit/ui-phase2-recent-review-materials.test.mjs`、`node scripts/verify-ui-phase2-review.mjs`。默认回归不写/覆盖截图，本次未使用 capture 参数。

没有新增生产服务、依赖、环境变量、API/OpenAPI、数据库或权限变更；不部署、不重启，无生产调节项。浏览器与临时 57645 服务已关闭；4 份隔离导出通过浏览器下载删除接口清理，工具临时目录也已不存在。本轮没有新建审核截图或残留临时文件；此前受策略限制的目录不重试删除。无关产品源码与其他在途改动留在工作区。
