# P58 读取反馈 · 批次 14

状态：代码文案已修正，C 反馈区域待审；未部署，不是 P58 或全 73 页完成。

## 改动与依据

实际 `CommercialOperationsCenter.load` 首次超时也提示“已保留上次成功数据”。新增直接提取实际 load 函数的回归测试，修改前首次分支失败、其他两项通过。修改后依据原 `loadedOnce` 区分“尚未取得数据”和“已保留上次成功数据”。通用读错误标题改为“暂时无法读取配额数据”，不再把超时/权限等错误一律说成字段或组织范围无效。只调整文案，15 秒超时、AbortController、sequence、状态分类、快照保留和 GET 请求不变。

使用 `frontend-design` 沿用 C 方向的白色错误区、蓝色操作/焦点和浅蓝保留数据提示；仅审核转换把首次 notice 合入原错误区，并把原请求编号披露移入该区，避免双重反馈。已有快照时仍在独立 notice 中提示。此模板/CSS 未写入生产页面，原三个弹窗、业务行为、接口、权限、配置和数据库未改。OpenAPI 不涉及变更。

## 验证

- 3 项真实 load 函数回归：首次超时无虚假旧数据、有旧快照时提示/对象保留、429 与保留数据后的 503 原行为不变。
- P58 组合与 CLI 表达式 5 项、已有 P57 C 组合 3 项，共 11 项定向单测；审核变换 SFC 编译、原业务脚本/校验保留、失败编号及可修正组织输入均覆盖。
- 实际 App 本地样例：390 / 1440 各检查 429、503、首次加载/超时/重试恢复/再次超时。6 组结果通过，最终回放 18 个 GET、0 写请求、0 pageerror。首次反馈不重复且包含请求编号；错误时组织输入可编辑；重新读取有蓝色焦点；恢复后仍有 `380 / 1050`，刷新超时后保留。
- 浏览器计时器推进 15001ms 验证原 15000ms 的超时；不是实测网络延迟、真实限流或服务故障验收。
- `npm run build:web` 包含 typecheck 和 Vite 构建，通过；构建产物仅本地保留。与本次无关的全库测试、真实 MySQL 与生产回归未执行。
- [10 张区域图与清单](../../output/playwright/p58-read-feedback-r1/README.md)。清单是 11 个选定交付源码的摘要，不宣称完整依赖图；未覆盖所有断点、主题、读屏、触控和模态状态。

## 使用与部署边界

默认 `node scripts/preview-platform-commercial-page.mjs` 启动已有本地 5174 审核宿主，Ctrl+C 停止。打开 `/platform-admin/commercial?organization_id=o1` 并读取页面快照后，可用缓存 Playwright CLI 的 `run-code --filename` 运行 `scripts/lib/platform-commercial-read-browser-checks.js`。默认不写文件，失败只拦截本地商业读取；429/503 错误封套复用原 E2E 样例。`o1` 是原测试值，不是有效业务 UUID。

首次采图可在执行前为当前本地 URL 添加 `#review-capture`，脚本将采图到固定 `output/playwright/p58-read-feedback-r1`。此标记只是本地验证入口，不是产品路由参数。当前图包已存在，不得再次用采图模式覆盖；复测使用无标记模式。新版本需单独命名并保留旧版。

本次没有部署或重启。未来批准发布后，两处生产文案随 Web 静态包生效，浏览器重新加载即可；无需 Node/Worker/Python/MySQL 重启或数据迁移。实际发布仍遵守宝塔发布门，不在本轮执行。原 P58 创建未知结果冻结策略与双工作区结构继续待确认。

## 审核及清理

已展示手机首次超时与保留快照超时两块区域并请求审核；未收到批准，不扩大既有局部批准。10 张图为永久审核交付物。手机 429/503 区域图底端包含少量原固定底栏边缘，不纳入反馈区审核，也不据此宣称完整页面无遮挡。

两个本地审核宿主先后停止、浏览器会话已关闭，5174 无监听。临时目录 `output/playwright/p58-read-cli-scratch` 内有 2 个 CLI 快照/日志；精确删除被工具策略拦截，保留且不提交，不换手段绕过。此前批次的受限临时目录未重试删除。本次提交仅包含当前生产文案、提案、回归与对应文档/图包，保留其他在途修改。
