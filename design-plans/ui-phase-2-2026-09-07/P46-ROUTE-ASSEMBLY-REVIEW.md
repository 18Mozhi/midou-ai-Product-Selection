# P46 真实路由 C 整体联动审核 r1

2026-09-11，基线 main / f2e3d775。用户本次答复“整体结构通过，继续完善交互”：仅批准所展示的桌面/手机导航、标题、概要/目录分区和手机编辑窗步骤/字段/底部操作排列。不是全部104图、全部状态、完整模态、真实保存/权限或生产验收。“创建并刷新成功”反馈仍待答。本批未修改生产源、未部署。

## 看图与设计变化

[完整104图浏览页](../../output/playwright/p46-route-assembly-review/index.html) · [原始证据](../../output/playwright/p46-route-assembly-review/evidence.json)

- 桌面：白色品牌/全局导航、页面标题和上下文条、蓝色来源内部导航、右侧概要/统计/白色筛选与目录。
- 手机：固定顶部入口、可展开全局导航、上下文折叠、蓝色内部导航、纵向内容；四步编辑和详情沿用各自 C 审核样式，在真实外壳组合。
- 修正组合暴露的标题竖排、审核标识与手机导航重叠，以及桌面返回工作台文字裁切。新增标题单行、菜单顶部边界和开关未被遮挡断言。
- 仅审核宿主去除旧装饰页码/视觉口号，桌面隐藏重复外壳标题（角色仍在上下文条），不改变权限或业务内容。外壳“已连接 · 可复核”为原静态文本，不是本轮真实后端健康结论。

宽度390/760/761/840/841/1440，高度1000。覆盖全页、目录、手机菜单、创建四步/校验、筛选、手机详情/技术信息/转编辑、编辑冲突、路由返回、刷新失败/恢复、导航拒绝/恢复。104张是此组场景图数，不是全部动作/状态或73页完成率。

## 实际链路与边界

`index.html → main.ts → router.ts → App.vue → NavigationShell.vue → KeepAlive → ProviderRuntimeSurface → ProviderRegistry`。实际跳转 P47 后浏览器返回 P46，保留 `?keep=p46-route` 和已读列表；P47只作往返锚点，不计页面设计验收。

独立 `provider-route-assembly-preview.css` 仅由验证宿主注入，复用已有page/editor/detail审核CSS。Registry仅复用既有预览函数的“操作”表头和重读主按钮文案替换，完整script不变。原预览CSS隐藏未绑定secondary，未接入生产。没有API、OpenAPI、环境配置、依赖、后端、Python、插件或数据库变更。

宿主强制内存API基址为本机相对路径并关闭代理，未知请求失败关闭，不修改.env。身份/导航/来源/适配器均为已有E2E样例；实际48次GET、6次PUT，PUT均409拒绝，无POST，无真实持久化。session-status仅在允许分支中，实际未请求，不能称真实登录验收。导航403验证前端不挂载来源页/不追加来源GET，不代替真实RBAC测试。

## 验证与未完成

- 六宽度232项浏览器断言通过；104PNG带尺寸/哈希，168个加载源文件绑定当前原始指纹。168是模块图文件数，不是已验页面数。
- 返回保留列表/query；详情转编辑解除App inert；关闭新建/编辑返回对应入口；冲突保留编辑窗；刷新恢复与导航拒绝已用本地样例回放。
- **未通过：六宽度均复现编辑窗最后一项按Tab移出窗口。** 保存为未解决观察，不计完整模态/无障碍通过；后续补焦点约束和真实外壳回归。
- 日期时区、只读字段残留、完整字段边界、真实成功保存/未知结果、真实账号权限、三主题密度、生产一致性及73页验收仍未闭合。本批不改变这些业务/数据边界。
- 本次所展示整体结构已获批准；未展示状态仍待审。之前两块反馈的原PNG与批准范围不变，均不扩大为完整弹窗或生产验收。

## 复现与交付

`node scripts/verify-ui-phase2-provider-route-assembly.mjs` 只回放，加 `--capture` 更新此目录正式图及清单。`node --test tests/unit/ui-phase2-provider-route-assembly.test.mjs` 核对源码绑定、图包、样例请求、返回与未解决焦点观察。复用现有依赖。

只提交审核CSS、验证器、永久合同测试、正式图包和定位文档。生产源未变，沿用f2e3d775已通过构建，不重复执行无关后端测试。浏览器/临时Vite已关闭；截图、HTML、JSON为正式交付保留，没有另建临时文件。

验证结果：新增4项永久合同测试通过，相关回归871/871通过，文档门与代码风格门通过。六宽度232浏览器检查通过与Tab逃逸观察并列记录，不能用通过计数掩盖未解决项。

本批无部署和重启需求。后续发布遵循 CURRENT-DELIVERY-PLAN：现有部署器并非纯静态热更新，会上传运行包、由宝塔停止Node并执行现有迁移白名单；发布前核验授权、迁移与恢复材料，不能据“仅UI”承诺无重启或数据库操作。
