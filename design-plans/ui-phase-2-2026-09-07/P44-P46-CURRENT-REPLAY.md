# P44/P46 当前图包：共享筛选关联修复后的重放

2026-09-12，main / af239b08。只更新当前验证产物和交接文档，不改运行源码、验证器或测试。
承接 [共享筛选关联修复](FILTER-DRAWER-ASSOCIATION-REVIEW.md)，不是新的页面视觉批准。

## 范围与实际执行

重放前逐个检查五包原清单和图片字节，唯一当前源码失配均为 ResponsiveFilterDrawer.vue。
先把五个 current 目录的 260 个文件复制到独立系统临时目录，保留实际前版字节用于比较。
历史 baseline 目录和原批准提案均不重建，不修改哈希来跳过运行。

使用 Playwright 技能的实际页面验证思路。技能 CLI 本地不存在，不安装新依赖，复用仓库
已有 Vite/Vue/Playwright 验证器；本机空闲端口、现有样例，所有外部和未列明请求拒绝。

| 当前包       | 执行命令（仓库根目录）                                                                         | 检查 | PNG | 源文件 |
| ------------ | ---------------------------------------------------------------------------------------------- | ---: | --: | -----: |
| P44 目录     | `node scripts/verify-ui-phase2-admin-mobile-directory-implementation.mjs --capture`            |  134 |  30 |     40 |
| P44 比较控件 | `node scripts/verify-ui-phase2-admin-mobile-controls-implementation.mjs --capture`             |  110 |  16 |     40 |
| P44 比较结果 | `node scripts/verify-ui-phase2-admin-mobile-results-implementation.mjs --capture`              |  234 |  48 |     40 |
| P44 角色资料 | `node scripts/verify-ui-phase2-admin-mobile-results-implementation.mjs --role-facts --capture` |  352 |  24 |     40 |
| P46 整体结构 | `node scripts/verify-ui-phase2-provider-route-assembly.mjs --structure --capture`              |  452 | 132 |    173 |

共 1282 项实际检查、250 PNG。P44 为实际父 Vue 隔离挂载：目录覆盖三路由，其他包两路由，
均四宽度 390/760/761/1440；既有详情样例缺少 organization_id 的限制保留，不能当真实授权。
P46 为实际 index/main/router/App/NavigationShell/KeepAlive 路径，六宽度
390/760/761/840/841/1440，检查导航、编辑步骤、键盘、失败恢复、主题及 P47 样式隔离。
P46 共 54 个本地请求，其中六个 PUT 均带原幂等键且被样例返回 409；没有真实保存。

## 前后字节、像素及观察值

全部当前源指纹、截图清单/字节重新核对。各包只有共享筛选抽屉的源码指纹变化，其他源码
及全部 observations（已记录样式、几何、请求、焦点）相同。1282 项检查值在仅归一化
三个具名 URL 检查的临时本机端口后相同：route stays after detail、restored URL、
admin comparison URL stays；host/protocol/path/query 仍严格比较，原清单没有被归一化。

首次内存比较直接比较完整 URL，因随机端口不同停止；补齐这三个实际字段后重新比较。
这是比较工具的运行环境差异，不是产品失败，不修改原验证器或它们的验收条件。

| 包           | 字节相同 | 有像素差异 | 单图最大变化像素 | 最大单通道差 |
| ------------ | -------: | ---------: | ---------------: | -----------: |
| P44 目录     |       25 |          5 |               26 |            2 |
| P44 控件     |       15 |          1 |                9 |            1 |
| P44 结果     |       34 |         14 |               18 |            2 |
| P44 角色资料 |       21 |          3 |               11 |            6 |
| P46 结构     |      120 |         12 |               71 |            2 |

总计 215 张字节相同，35 张差异；所有尺寸保持。通道差 6 仅出现在
P44 760-permissions-role-facts.png 的一个像素 (16,125)。已目视查看该前后对，
P44 390-admins-focus 前后对及 P46 840-direct-recovered 前后对；抽查变化位置位于
控件边缘，其他大量差异位于右侧滚动区域。不能据此推断所有差异的唯一成因。

[精确差异 JSON](P44-P46-CURRENT-REPLAY.json) 保留逐图前后 SHA、尺寸、变化像素数、通道差
与包围盒，以及每包来源变化和 observations 比较结果。它是本轮实测记录，**不是新增
统一像素容差**，不能用它自动放行后续变化、改写历史或扩大用户批准范围。

## 门禁及边界

当前六包指纹/图片单测 6/6 通过，其中 P46 独立反馈包源码未漂移，仅校验，没有重复重放。
另四个旧实施测试文件共 19 项、4 通过/15 失败；包括旧源码、旧 36 源数量以及旧样式范围
断言。此轮不修改或豁免这些失败，不把当前证据校验通过称为全部相关测试通过。
最终完整 Node 单测 1379 项：1180 通过、199 失败，292.62 秒，退出 1，无取消或跳过。
与前轮完整结果相比，仅消除本次五包当前绑定失败，没有新增失败名称。
仍未达到全库/发布门，不提交或部署，commit hash 不适用。
前一轮构建/类型/预算结果对应的源码及依赖未变，不重复构建。格式、文档/路由与运行文档
检查通过，差异 JSON 的当前图片哈希/尺寸亦重新校验通过。

P44 部分邻接路由保留原样的截图属于隔离对照，不意味着它们已完成 C 重设计；P46 的局部
结构批准也不扩展为全部主题、状态、真实权限、保存、全部弹窗或生产验收。
后续继续 P47 其余当前包、P58 当前包和共享按钮候选身份的显式合同登记。

本轮无 API/OpenAPI、配置、环境变量、数据库、依赖、业务、安全/权限或部署变更，
无重启要求。250 张正式当前图及本记录保留，五个捕获服务均已退出、端口无监听。

临时对照目录的删除命令被工具策略拒绝，未重试绕过；只读复核仍有 260 个复制文件，
并非已清理。剩余路径：
`C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`。
用户可在复核后手动删除这个临时副本目录，不影响 output/playwright 下正式当前图包；
本轮没有删除原始用户文件。全库测试自有临时夹具 scoutops-code-style-WViHCM 已由测试
自动删除并经收尾确认不存在，父子测试进程退出。前版对照目录是本批唯一未清理临时产物。
