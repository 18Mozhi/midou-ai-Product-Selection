# P47 当前C页：检查反馈交互核对

## 本批边界

承接全73页C重设计，按无障碍技能检查来源归属、等待焦点、追踪展开与关闭恢复。
本地UI CLI缺失；不安装依赖，使用已有App/Vite/Vue/Playwright驱动。
开始为main/af239b08、417项工作区变更、暂存为空。没有改生产Vue/CSS、业务、API、
OpenAPI、权限、配置、环境变量、数据库或依赖，也没有新增或扩大用户批准。

## 当前与历史分开

新增无参数 `node scripts/verify-provider-adapter-feedback-current.mjs`，以原驱动
SHA256 `4b2b39aeca9420f71264f1fd412e310d840e15d007c0d82aac352603f6374310` 失败关闭。
只执行current，不捕获图片、不执行旧baseline或提案review装配；当前Vue不转换、
不注入预览样式。保留原点击/键盘、接口拦截、反馈/追踪/归属、超时和finally清理。
宽度由390/760/1440补到390/760/761/1440。

只显式替换一项历史语义：旧反馈修复阶段要求检查期间旧GET显示“已刷新”，但后续
f2e87218读取保护已使它失效。当前入口等待刷新结束，断言页面不发布旧消息、窗内
仍显示检查等待、焦点保持status、检查按钮仍禁用；没有修改生产逻辑以迎合旧断言。
旧驱动和历史断言保持不变。新增单测逐字比较完整浏览器场景，只允许这一替换，
并拒绝未知原驱动/网络/焦点/替换块变化。

原44图反馈包完整清单与170个来源均核对到真实拍摄提交
004c3e0e5d9835488036a5052b31ed93a505f145，全部匹配。原单测不再串接后续C/读取
转换器，改为精确历史来源；原图片字节、尺寸、目录、122条观察、8次运行与网络门
保持。现行脚本行为仍单独从原始工作区Vue执行，不以历史源码替代。

## 结果

- 当前真实App四宽度、4组、54项检查通过，0截图。
- 手机：检查前空反馈、等待焦点、同来源重开、他来源等待说明、迟到结果不抢焦点，
  冲突反馈与追踪归属，后来GET保留结果，过期GET不发布，新检查通过、受阻原因，
  追踪目标至少44px、Tab仍在详情、关闭回原来源及背景解除inert。
- 761/1440：原桌面冲突反馈、检查禁用锁和无手机详情反馈保持。
- 390/760各4GET/3POST，761/1440各2GET/1POST，共12GET/8POST，无body，
  全部本地拦截，未知请求拒绝，浏览器错误为0；无真实服务、探针、采集、RBAC或写入。
- 两个完整相关单测文件14/14通过，0取消/跳过，39.81秒；包含原始当前Vue执行、
  缺失反馈变量反例、历史来源/图片，以及新增3项驱动边界测试。

来源集合172是继承静态清单与本次模块图的路径集合，包含本轮未执行的历史预览项；
不是172个真实加载文件，更不是新截图哈希包。仅默认外观/reduced-motion，未覆盖
全部主题、读屏器、卸载、缓存退出、未知写结果或生产验收。上一批读取顺序100项
检查与本批输入未互改，不重复运行；也不重复全库或推算最新失败数量。

## 使用、收尾与后续

从仓库根运行上述无参数入口；不接受capture，不覆盖旧图。无需配置调节、部署或
生产重启。原驱动、转换器、44图和批准不变，完整73页仍未验收，不提交、不部署，
commit hash不适用。下一步继续其他P47当前状态及剩余页面证据，不把历史归档当作当前通过。

浏览器与宿主已退出，61942无监听。无新临时文件、图片、日志或夹具；新增脚本、
测试和本记录为永久交付。文档门（73路由/60受保护/6角色/153必需文档）、运行文档门、
格式门（130变更代码文件/535生产文件/51仓储检查）及diff空白检查通过；工作区421项，
暂存为空。另实测--capture和--baseline都在启动浏览器/服务前以退出码1拒绝。
此前两处策略阻止清理的目录
仍待手动处理，本批不绕过重试：

- `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p47-c-e2e-temp`
- `C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`
