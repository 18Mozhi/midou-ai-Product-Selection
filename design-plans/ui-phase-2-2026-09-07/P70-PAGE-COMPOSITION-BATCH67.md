# P70 实际 Vue C 组合 · 批次67

P70 原有 SCHEDULER-C-r1 是离线稿。本批新增仅审核用的 Vite 转换：保留 `CrawlerSchedulerCenter.vue` 的 script、GET、15秒单飞、来源筛选/分页、权限、快照与两个原有确认弹窗，使用原组件 DTO 将总体结论、单机/资源、来源队列、回执/租约和24小时样本分区。审核版不进入生产构建，也不会点击任何恢复入口。

`tests/unit/scheduler-page-preview.test.mjs`通过；实际 Vue 在1440/390与两种动效下共144项检查、52次本地 GET，无外部请求、写请求、下载或页面错误。r1图册为16PNG/173来源指纹，适用于默认布局、读取状态和焦点审核，不代表真实调度、来源健康检查、恢复、权限、审计或生产。

批67复核既有读取语义：503保留上次成功的调度事实，显示“刷新未完成”及原有技术详情；403清空受保护事实，只显示“没有平台运维权限”和重新核验入口。真实 Tab 可到达顶部“刷新运行事实”与保留快照区“重新核验”，二者均呈现3px蓝色轮廓。两类恢复确认弹窗保持来源、处理函数和原有写入合同，但本地审核不打开或提交它们。

无需重启、部署或配置变更。待用户分别审核手机默认、读取状态与焦点组合后，再核验筛选分页、两个确认弹窗、操作结果和真实环境验收。
