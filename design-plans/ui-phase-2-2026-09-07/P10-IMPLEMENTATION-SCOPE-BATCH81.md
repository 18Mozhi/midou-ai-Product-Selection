# P10 实施范围 · 批次81

已从 `ThemeStudio.vue` 核对当前实现：读取 `GET /me/ui-preferences`，主题预览立即应用本地主题；仅主题变化可通过 `PUT /me/ui-preferences` 提交 `theme` 与 `expected_version`。标准/紧凑密度仅当前会话生效，不能在审核图或文案中描述为服务器已保存。

下一轮实际 Vue 审核转换将覆盖：读取中、已同步、主题脏状态、撤销、保存成功、版本冲突、权限拒绝与登录失效；保留三种兼容主题 ID，但不沿用旧纸张、朱砂或营销侧栏视觉。生产组件、令牌、API、版本并发规则和部署均未改。
