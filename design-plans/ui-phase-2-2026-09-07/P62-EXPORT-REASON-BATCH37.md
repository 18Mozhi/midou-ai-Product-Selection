# P62 导出原因与按钮层级 · 批次37

起点main/6025c198，536项在途变化，其中含上一批尚未提交改动；索引为空。上一批追踪分离已取得有效进展，本轮继续实际原因窗，不把自动继续当作API覆盖测试修复批准或部署批准。

## 本批实现与证据

使用ui-skills-root选择fixing-accessibility，核对原生dialog、字段标签、初焦点、Tab循环与取消返焦；使用baseline-ui处理实际发现的按钮层级问题。沿用已有Vue组件与C配色，不安装新依赖或引入另一套组件。

审核CSS原本在外层把原因窗按钮定义为白底、确认提交定义为蓝底，但更具体的页面级button选择器把取消/关闭也覆盖成蓝底。新增实际computed-style断言复现：取消背景实际rgb(23,72,181)，预期白色，失败后仅在P62审核根内补两条type=button规则：默认白底/蓝字，悬停浅蓝/蓝字。确认提交及禁用仍沿用原规则。只改审核CSS，没有改生产Vue、共享原因窗或点击/请求行为。悬停检查等待原有样式过渡结束，不加速时钟或修改产品动效。

## 实际页面验证

永久命令`node scripts/verify-log-export-reason.mjs`默认无文件；`--capture-review rN`只创建独占版本目录。真实App、原API客户端/共享原因窗、C审核宿主与原E2E日志夹具；先按URL query=trace-shared/source=crawler读取一条匹配事件，沿用source→GET status映射。1440×1000和390×844、reduce/no-preference四组，各54项，共216项。

覆盖默认原因、required/minlength与帮助关联、Tab/Shift+Tab四控件循环；空/空白/一字禁用，禁用确认从循环中跳过，两字可用；Escape/关闭/取消三条不发POST且返焦，重开恢复原默认原因。提交去首尾空白，原query/source/reason、text/csv及实际客户端生成的幂等头均核对；在途禁用按钮调用原生click不再派发，400失败后入口恢复可用，快照编号不受导出编号污染，人工重试只打开新原因窗，取消不二次POST。

四组仅4次本地日志GET与4次浏览器拦截POST。POST受控保持在途，再以本地400释放，没有请求真实服务、写审计或下载CSV；不使用伪造成功进度/耗时。二次点击只验证原生禁用后的激活保护，不证明handler本身单飞、原因等待重入或离页迟到安全。

新增200项初始检查通过，图r1每端6张。CSS颜色断言先失败，修复后216项通过；默认页面77项及6项审核结构/编译合同回归通过。上一批生产代码、依赖及配置没有再次变化，不重复构建或270项真实计时验证；这些沿用上一批结果，不宣称本轮重新执行。

## 正式图与待审范围

最新`output/playwright/p62-export-reason-r2`：双端12PNG、index.html、manifest.json、README.md，168项来源与12图SHA-256；捕获HEAD6025c198加当前混合工作树。每端包含默认原因、确认焦点、一字禁用、在途页面视口、失败提示区域、失败后重开原因。r1同样12图是修正前正式对照，保持原捕获清单不改；其CSS和验证脚本与当前不同，不作为最新样式证据。

本次申请审核仅限手机原因窗的白底取消/关闭、蓝色确认、键盘焦点与禁用层级。等待视口只是展示当前按钮位置；图内其余导航/筛选/全页不得视为本次通过。失败图只覆盖提示块，不证明完整交互、读屏、真机触控/软键盘或真实导出验收。

## 明确未修复的缺口

1. 实际输入301个“查”字后没有maxlength，提交仍可用；后端`PlatformDashboardService.exportLogs`明确去首尾空白后2–300字符。本轮只记录，未发送超长原因。现有测试中的limitGap是诊断断言，不是认可该行为；未来修复时应更新此断言。尚未实现原提案中的300字输入/错误关联。
2. 实际提交关闭原因窗后，原生禁用导出按钮不能接回焦点，四组document.activeElement均为BODY。pendingFocus记录该缺陷，不计为焦点通过；需后续修复提交等待/失败恢复的焦点连续性。
3. 未改变导出handler的原因等待重入、URL范围冻结、离页晚下载、独立读写消息；未实际验证CSV文件、服务端幂等/审计/权限。四组有幂等头不等于手动重试复用原键。

## 交付与运行边界

同步P62规格、当前计划、索引、feature map和运维命令说明。无生产代码/API/OpenAPI/env/数据库/依赖/权限/Worker/Python/插件合同变更，无配置项或重启要求，本轮未部署。审核CSS不进入生产构建；仅在本地审核宿主查看。

批次36发现的API覆盖测试223/225旧计数失败仍待用户决定，相关代码未变，按效率约束不重复相同失败测试，也不擅自修复。按失败不自动提交规则，本批继续未暂存/未提交，commit hash不适用；原529项和批次36在途工作保留。

没有一次性脚本、测试数据文件、下载或日志；两个版本图册和永久验证脚本作为正式交付保留，所有浏览器/Vite在finally关闭。既有受阻清理目录不触碰。全73页重设计实施与真实部署验收尚未完成。
