# P04 实际 Vue C 组合 · 批次75

本批以审核用 Vite 转换重构找回密码页面：保留 `LocalIdentity.vue` 的邮箱字段、`POST /auth/password-reset/request`、通用“如账号存在”反馈和返回登录动作；移除营销装饰，改为蓝色恢复边界、单字段工作区、受理或失败提示。

单元测试通过；实际 Vue 在1440/390与两种动效下40项检查、4个本地拦截POST通过。r1有双端默认/受理4 PNG、39来源指纹。请求只含email，202表示受理而非账号存在或邮件已投递；不代表真实邮箱、队列、限流、身份服务、权限或生产验收。

无需重启、部署或配置变更。待审核默认与受理组合；服务端错误/限流、真实投递、缩放、读屏和生产验收仍待完成。
