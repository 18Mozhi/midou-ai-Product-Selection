# P12 实际 Vue C 组合 · 批次84

本批新增审核用 Vite 转换，保留 `NavigationShell.vue` 的成员守卫、导航、路由与权限行为，以及 `HomeDashboard.vue`、`HomeAutomationOverview.vue` 的首页读取、规则读取、首次创建与首条暂停规则恢复。审核层同时把本页的壳层替换为蓝白 C 方向，不保留旧账页标题、米色背景、圆角卡片或营销式布局。

实际 Vue 在 1440/390 与两种动效偏好下通过 32 项检查，产生 8 次本地拦截写入：首次规则 POST 精确包含十市场合同的语言映射、默认名称、来源门槛和通知通道；恢复只 PATCH `pausedRules[0]` 的状态、版本、频率和来源门槛。r2 图册覆盖运行中、运行详情、未配置、创建成功、暂停恢复和读取失败的双端 12 PNG/5 个受控来源指纹。

无需重启、部署或配置变更。转换和图册仅供用户审核，未改生产首页组件、共享导航、API、规则、权限或采集任务。真实会话/RBAC、后端规则读取失败、并发与迟到响应、读屏、爬虫执行和生产验收仍待完成。
