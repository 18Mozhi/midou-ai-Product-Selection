# P47 C · 实际组件整合与交互核对

2026-09-12 交互增量：[空态焦点实施](P47-EMPTY-FOCUS-IMPLEMENTATION.md)。当前组件新增
局部清除回焦点保护；后文脚本保持描述仅对应原整合时点。原 C 不变量通过显式精确逆变换
继续校验，当前截图绑定未经转换的源码，不将本次修复隐藏进旧整合结论。

2026-09-12 后续配色与当前证据更新见 [P47 配色复核](P47-PALETTE-REVIEW.md)。下文源数量、
构建数量和起点描述原整合时点；图册现已重新生成并显式绑定调色板，不能将旧哈希当作当前。

日期：2026-09-11。起点：main / 18f7792f，工作树干净。本批只整合已展示的C构图并验证现有交互，未部署生产，不是P47或全73页验收完成。

## 1. 用户审核边界

本次答复：“手机默认布局通过，继续整合与交互验证”。仅覆盖展示的蓝色横向目录、四项概要、搜索与折叠筛选、来源列表。之前已批准桌面默认布局、手机详情分区及检查拒绝反馈组合；不推定全部按钮、错误状态、主题、真实探针或生产验收通过。

## 2. 实际接入

- `apps/web/src/components/ProviderAdapterCenter.vue`采用已审核页面/详情组合；保留后来实施的来源归属反馈及检查按钮事件对象。
- 三份`apps/web/src/provider-adapters-c-{page,detail,feedback}.css`由对应审核CSS机械缩小到`.adapter-center--c`活动页面标记。详情经Teleport显示时仍限定P47；P46不会继续命中这些规则。
- 除三个CSS导入外，原script的TypeScript token完全相同；原有全部事件、六个模型、条件、循环及显示事实受永久测试约束。既有scoped反馈样式不变。
- 不改接口、权限、探针判定、采集恢复规则、缓存策略、超时、依赖、环境变量、数据库或后端/Python。Feature Map只补交付状态；OpenAPI、环境示例和API消费方无需修改，因为运行契约未变。

## 3. 验证与图册

[打开20张实际组件图册](../../output/playwright/p47-c-integration/index.html)，[机器证据](../../output/playwright/p47-c-integration/evidence.json)。真实index/main/App/NavigationShell/KeepAlive/ProviderRuntimeSurface链路，无模板替换或审核样式注入。

| 验证 | 范围 |
| --- | --- |
| 36组、430项显式检查 | 390/760/1440 × 首次读取/主动重读/检查 × 成功/409失败 × 离页完成/返回后完成 |
| 20张PNG | 三宽度默认与展开筛选、手机缓存返回/检查反馈/首次读取失败 |
| 170个源指纹 | 当前实际加载源、入口/路由/Vite、运行器及E2E配置；不是170页覆盖 |
| 原M03-03共16用例 | 桌面与手机的显示、筛选、全控件重置、分页、空态/权限/依赖反馈 |

确认首次请求离页后不重复读取、在途返回按钮仍锁定、失败只能显式重读；P46新编辑窗输入/焦点不被迟到回执打断；返回保留筛选且详情不自行重开；检查不重放，显式重开显示本来源结果与追踪；手机详情双向Tab循环和Escape回入口；切回P46后body背景、标题颜色/字体回到进入P47前值。

原两行M03样例保持，另外显式构造同一来源v3及409反馈，仅为受控交互测试。API全部本地拦截，未匹配请求失败关闭，健康POST无body且没有到达真实服务。探针成功样例不能当作真实采集、熔断恢复或权限证明。

最终本地结果：相关Provider单元100/100，原M03-03功能16/16；`build:web`（含vue-tsc）、254资源包体预算、73路由/153项文档门和代码风格门均通过。人工核对桌面/手机默认图、手机详情拒绝及首次读取失败图；通用首次读取失败区仍有“操作未完成/没有写入成功”措辞，未单独细化或签收。用户随后明确通过新增展示的手机成功反馈组合，仅限浅蓝结果提示、本次检查追踪和展开编号，不代表真实探针、完整详情或生产验收；继续其他状态。

## 4. 复现与历史证据

```powershell
node scripts/verify-ui-phase2-provider-adapters-c-integration.mjs --capture --e2e
node --test tests/unit/ui-phase2-provider-adapters-c.test.mjs
npm run build:web
npm run verify:frontend-budget
npm run verify:docs
npm run verify:code-style
```

运行器自动分配本机端口，运行结束关闭Vite与浏览器。`--capture`更新本批正式图册，`--e2e`使用既有`PLAYWRIGHT_WEB_PORT`向子进程传递端口；不新增应用配置。测试配置产生的`output/playwright/p47-c-e2e-temp`是可删除临时结果，正式图册保留交付。

旧18份单元证据通过严格SHA关联读取18f7792f的不可变源，仅允许已知两文件精确变更，未知漂移不归一化；旧图和旧运行器保持原样。新的C测试直接校验当前原始组件、CSS、网络回放与全部PNG，不使用历史映射替代本次验收。源绑定使重新格式化运行器或被加载源码后必须重采本批图证。

## 5. 未覆盖与后续

仍需P47其他交互/错误/主题组合的逐项核对，卸载及缓存淘汰、未知写结果、屏幕阅读器/真实软键盘、真实权限/探针/恢复，以及全73页剩余设计实施与生产验收。不新增未知结果规则。本轮不用重启生产；最终上线需按既定本地构建及宝塔部署流程另行验证，不以本地图册代替部署。

技能：ui-skills-root选择最小上下文，frontend-design保持已批准C构图，fixing-accessibility落实焦点与隔离核对，playwright复用已有本地浏览器工具与真实组件验证；未安装依赖。
