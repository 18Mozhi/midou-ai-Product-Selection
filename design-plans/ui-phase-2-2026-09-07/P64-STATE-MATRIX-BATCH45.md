# P64 实际 Vue 状态矩阵 · 批次45

起点 main/6025c198，561项在途变化，索引为空。上一批焦点修复与验证属于有效进展；本批补结论、演练有效期、缺记录和长内容的真实组件展示。不将自动继续视为任何图稿批准或业务政策授权。

## 来源和范围

新增 `scripts/lib/backup-state-fixtures.mjs`：读取当前 `apps/api/src/backup-recovery-service.ts`，在内存转译并隔离执行，注入合成仓储和固定时间 2026-09-15T08:00:00Z；不复制判定函数，不直接指定返回 state。每个样例只调用一次合成 repository.read，无数据库、文件备份、网络或真实审计写入。

九种样例：已验证、剩1天、恰好今天到期、已过期1天、权限/审计/哈希未核验、缺演练、无记录、明确数值0、长区域名。输入字段来自当前 service 与原 M07-04 合同；长区域是48个汉字的本地样例，实际迁移的 region/primary_region/recovery_region 均为 varchar(64)，不改生产配置。有效期仍90天、RPO目标15分钟、RTO目标240分钟，零数值只是原服务接受的合成样例，不是声称真实零耗时恢复。

服务当前在精确第90天返回 verified / remaining=0；页面显示“今天到期”。第91天返回 stale / remaining=-1 与服务产生的阻断说明。本批只验证整数天样例和 UI 对事实的呈现；既有半天边界、null转换、负数、未来日期等风险仍见 P64 规格，不据此宣布恢复资格算法完整正确，也未擅自修改算法。

## 检查与图册

`node --test tests/unit/backup-state-fixtures.test.mjs` 的6项来源/样例检查通过；连同原7审核、14焦点及9个 M07-04 测试共36项通过。原加密自测只用内部合成临时文件，已自动清理。

`node scripts/verify-backup-state-matrix.mjs` 挂载实际 Vue/App + C 审核转换器，1440/390和减少/正常动效四组478项检查，36个本地GET。核对服务结论、RPO/RTO的null与0、权限/审计哈希、到期短语、全部阻断说明、全部资产数、唯一h1、页面不横溢、同机边界和长区域详情关闭返焦；不发生外部请求、写请求、下载或浏览器异常。

当前 `output/playwright/p64-state-matrix-r2` 为71张待审图/168来源：桌面34、手机37。每种状态都有结论/目标与实际/证据区域，存在阻断时追加阻断区域；无记录/零数值/长区域追加资产，手机长区域另有详情。为避免手机导航遮住717px长目标区域，r2将其拆成区域信息、RPO、RTO三张。捕获前逐一核对可见标题、字段、说明及控件的视口范围与中心命中，没有隐藏导航或改页面布局。r1/69图保留为旧取景对照，不作为当前长区域图。

截图仅在减少动效组生成；正常动效有交互检查但不另出图。日期渲染明确使用 Asia/Shanghai。manifest.json记录合成输入的服务输出、来源与PNG指纹、浏览器检查及捕获版本；index.html用于审核。上述都不是生产事实、真实权限、SQL查询/审计或恢复验收。

## 未改及交付

本批只增加审核样例、驱动、永久单测和文档/图册。没有改生产组件、CSS、API、服务算法、数据库、权限、依赖、配置或环境；后端、Worker/Python、插件消费者无需联动。此前焦点/默认/读取及类型/构建证据保留原范围，运行源码未变，不重复构建。无新调节项，复现默认不落文件；`--capture-review rN` 只创建尚不存在的新版本目录。

临时浏览器/Vite均finally关闭并核对55931/55984/56084无监听；自测目录无残留。图册、脚本、测试为永久交付，项目既有构建产物保留；既有来源不明/清理受阻路径未处理。本轮不部署、不迁移、不重启。

既有API覆盖测试计数问题输入仍与HEAD相同，决定尚未收到；未重跑相同失败、未擅自修复、未暂存/提交，commit hash不适用。P64本批与此前区域均按独立审核范围，保活/迟到响应、追踪归属、完整列控件/主题密度、读屏和真实恢复仍待完成；P62离页策略及全73页目标保留。
