# P47 · 空态清除后的局部焦点实施

后续刷新修复后的当前验证见[P47双焦点共存](P47-EMPTY-REFRESH-CURRENT-REVIEW.md)，使用
`verify-ui-phase2-provider-adapter-empty-refresh-current.mjs`。本文的`empty-current`命令及
176来源图包对应刷新修复前时点，保持原样，不再用于重拍当前源码。

## 范围与用户审核

用户确认“两种空态组合通过，继续其他状态”，仅覆盖已展示的手机浅灰区域、标题说明和蓝色按钮。
本次按继续完善交互的要求实施局部键盘修复，不将视觉确认写成焦点、整页或生产验收通过。

生产组件仅增加 `nextTick` 导入、`resetEmptyFilters` 函数及空结果清除按钮的一处绑定。
函数调用原 `resetFilters` 后，等待 Vue 更新；仅当原搜索框仍连接、未禁用、不在 inert 区域，
且焦点仍落在 BODY 时回到搜索框。用户新焦点不会被抢走，顶部重置保持原绑定。
这遵循 fixing-accessibility 的局部焦点保护要求，不添加新 ARIA、依赖或共享焦点机制。

未实施既有空态审核稿的文案/CSS，没有更改筛选、排序、分页、概要事实、请求或权限。
API、后端、Python、数据库、OpenAPI、环境变量均不适用本次改动；不部署，不重启生产。

## 可追溯验证

- [当前图册](../../output/playwright/p47-empty-current-implementation/index.html)：实际 App 的原样组件与既有视觉提案，390/760/1440 三种宽度，18 组、270 检查、30 PNG、176 源指纹。
- [原审核存档](../../output/playwright/p47-empty-review/index.html)保持不变；原基线清除后 BODY 失焦的负面证据及已展示区域图没有重拍覆盖。
- 当前原样组件及提案均验证搜索/组合无结果清除后回焦点，顶部重置保留焦点；六模型恢复，每组一次初始 GET，零写请求。空目录登记入口只验证进入 P46，没有真实登记。
- 实际函数经 AST 提取，连同原提案覆盖正常回焦点、新焦点、节点移除、disabled、inert、缺失输入六种条件。
- 原 C 脚本不变量测试通过显式 SHA 限定的逆变换检查修复前脚本；未知源码漂移不会逆变换。当前图包始终绑定实际原始源码，而非逆变换源码。

复验命令：`node --test tests/unit/ui-phase2-provider-adapter-empty.test.mjs`。
重拍当前图包：`node scripts/verify-ui-phase2-provider-adapter-empty-current.mjs --capture`。
原审核 runner/helper/CSS 和原 manifest/PNG 由独立测试固定，不用于刷新当前证据。

## 使用与交付边界

在 P47 输入无匹配搜索词，用键盘激活空结果区“清除筛选”，结果恢复后可直接继续输入搜索。
没有新增可调参数。未来随前端构建发布生效；本次没有发布，不需重启后端服务。
本地样例与浏览器测试不代表真实探针、读屏全链、完整权限或生产验收。
全项目验证仍有未解决失败，暂不自动提交；commit hash 不适用。全 73 页交付继续开放。

## 关联回归与收尾

前端类型检查及构建通过，资源预算通过（252 assets）；文档门通过（73 路由、153 必需文件）。
空态/C 整合/调色板证据的 17 项单测及读取失败的 5 项单测通过。
当前 P47 C 图包重拍后通过 36 组 430 检查、20 图、16 E2E；P46 结构 452 检查/132 图、
反馈 244 检查/40 图也已重拍并校验。P44 四包和标准 81 包不依赖本次组件，未无谓重拍。

P47 当前读取失败包首次像素比较出现 9 个差异像素、最大色阶差 1，未通过原 8 像素容差；
单独复测后 30 组 357 检查/30 图通过，六组邻近状态比较五组为 0，1440 blocked 为 4/1。
原容差和审核原图未调整，没有将不稳定的首轮写成通过。

最终 `npm run test:unit` 运行 1316 项：1118 通过、198 失败，退出码 1，约 225.3 秒。
相较上一轮 1314 项/187 失败，本轮新增两项空态测试，失败数增加 11；不能沿用上一轮数字。
失败清单包括生产验收预检、P47 旧访问/分页提案的脚本不变断言和多页历史源码/图证据绑定。
本轮没有为消除失败改写历史图片或放宽哈希检查；全部失败的归因与历史/当前绑定整理尚未完成，
不能断言这些都只是无害的旧测试。本次定向 22 项及浏览器回归通过不替代全库门禁。

验证宿主和浏览器已关闭；检查过本轮 61565、61658、61794、61853、62109、61956、62108、
62894、63040 端口无监听。临时 `output/playwright/p47-c-e2e-temp/.last-run.json`
及空目录已删除。当前图包是正式交付物，不是临时测试文件；现有构建产物按项目运行需求保留。
