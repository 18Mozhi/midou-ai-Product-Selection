# P33 C 页内导航 · 实际交互验证

2026-09-13，main / af239b08b69f7d97cd0372f9840a70009eeef0c7；起点634项既有工作区变更，
暂存为空。用户批准的P47两种手机空态仍仅限视觉区域，本轮不重新设计、不扩大批准。

## 改动与事实

ui-skills-root选择fixing-accessibility，重点验证新增原生链接的键盘顺序和焦点可见性。
沿用项目Playwright真实App审核驱动，不安装依赖。新增独立验证组合与永久回归测试，
不改r4的Vue变换、CSS、原驱动、截图或清单，布局待审状态不变。

首次手机只读探查：目录点击与协作链接Enter均到对应hash；此时activeElement为BODY，
但下一次Tab分别进入目录首个状态按钮与成员选择框。因此没有把BODY单独误判为失焦，
也没有擅自增加tabindex或劫持原生导航。浏览器历史还原可能受离开条目时聚焦目录链接
引起的滚动影响；本轮不定义“后退必定把目标区域置顶”的新政策。

## 验证范围

新入口在内存中组合固定SHA的原r4驱动，原夹具、全部原检查与GET白名单保持。
只运行review，禁用capture；不写报告或图片文件。未知参数和来源漂移在启动前拒绝。
已有网络拦截会拒绝所有非本机请求、未知API以及写请求。

- 手机预检49项通过；完整390/840/841/1440四组196项通过，每组原29项加20项导航检查。
- 目录鼠标点击、协作链接键盘Enter后，Tab进入目标区域首个控件；焦点轮廓可见，
  控件在视口内且中心点未被固定栏等元素遮挡。
- 两次跳转、后退、前进、回到初始历史条目后，pathname、组件DOM身份、团队选择、
  成员选择、搜索/排序和已填写创建草稿保持；实际浏览器未重载文档。
- 每组团队GET仍只有一次；全部API为无body GET，0意外网络、0浏览器异常、0写入。
- 新增四项定向单测覆盖驱动可逆组合、语法/导入解析、所测交互、漂移拒绝和禁止capture。
- 最终七份关联测试40/40通过，0跳过/取消，约32.12秒；原r4全部当前源及PNG保持一致。
  Prettier检查、文档门禁（153文件、73路由/60受保护/6角色）和运行文档一致性检查通过。

“sources:177”沿用原r4驱动的运行来源统计，不包含新增验证组合文件，不能当作新图包的
完整来源清单。本轮没有新图包；r4全部当前来源和原PNG仍由原捕获单测独立保护。

```powershell
node scripts/verify-ui-phase2-teams-anchors.mjs --smoke
node scripts/verify-ui-phase2-teams-anchors.mjs
node --test tests/unit/ui-phase2-teams-anchors.test.mjs tests/unit/ui-phase2-teams-vue-preview.test.mjs
```

## 未覆盖与收尾

不代表历史前进后退的滚动位置/焦点策略、所有Tab链、屏幕阅读器朗读、所有短屏、
真实写入/权限、OG-G02修复或完整页面验收。P33布局意见及共享原因窗重设计仍待，
P47其他原待审区域不自动通过。全73页实施与生产验收目标继续开放。

生产组件、router、后端/Worker/Python、API/OpenAPI、环境配置、权限、持久化数据、
依赖及宝塔服务均未变，无生产重启要求。没有创建临时文件、截图、日志或浏览器配置；
内存探查与正式验证均在finally关闭浏览器/Vite，端口56524、56716、56771另行核对。
既有正式图包保留，历史清理受限目录不触碰。当前工作区包含大量前序未提交实现，
本次验证依赖其中的r4来源；不将其包装为独立可部署提交，未暂存、提交或部署，
commit hash不适用。全库门禁未重跑，不推算现时全库失败数。
