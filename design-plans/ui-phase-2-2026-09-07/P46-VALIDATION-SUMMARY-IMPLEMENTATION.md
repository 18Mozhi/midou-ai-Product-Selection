# P46 错误汇总随字段修正更新

2026-09-11，基线 main / fa69ae75。上一批桌面编辑审核实际复现：字段错误已经清除，先前点击“下一步”产生的错误汇总仍显示原数量。本批按“继续完善交互”修复反馈一致性，桌面新稿仍待审，不接入新样式。

## 实施与依据

采用 fixing-accessibility 技能处理表单错误反馈。真实链路为`/platform-admin/providers` → App/NavigationShell/KeepAlive → ProviderRuntimeSurface → ProviderRegistry。沿用原formErrors和currentStepErrors，不新增或更改任何校验条件。

Registry仅增加26行：以`validationScope`明确区分当前步骤/全表校验汇总；监听既有错误数量与当前步骤。用户修正字段后汇总按实际数量更新，全部修正后清除；直接切换步骤时清除上一步的局部汇总，全表汇总仍按全表错误数更新。清除后的新错误不会仅因输入就重新生成“已尝试继续”的汇总，仍由原下一步/保存校验入口触发。

新建/关闭、应用技术模板、成功进入下一步、实际开始保存和收到当前保存失败回执时退出校验汇总模式。模板提示和API失败提示不会因随后编辑字段而被校验监听器误清除；失败回执即使晚于窗口内另一次校验，也重新持有反馈。新一次本地校验替代服务器错误时，清除已经不对应当前提示的旧请求关联号。

模板、CSS、23字段、原校验、步骤入口、按钮启停、保存序列、请求体、权限与日期处理不改。不存在API/DB/环境变量/依赖变更，OpenAPI/.env.example/后端/Python/插件不适用。当前Registry LF SHA256：`9822fd9a882dd61409420d414e1fcdaae4c8ce513d53c853c9bda9741c609c2c`。

## 验证与图包

[修改前52图](../../output/playwright/p46-validation-summary/baseline/index.html) · [修改后52图](../../output/playwright/p46-validation-summary/current/index.html)

真实App入口、生产CSS，无审核标记或布局替换。390/760/761/1440px，前后各136显式检查、52状态观察、52PNG和165个原始加载源指纹。每版8GET与4POST均本地拦截，POST全部409拒绝，前后请求序列和请求体相同；无真实写入。

- 第一步3→2→1→0的实际错误数量与汇总一致，旧版一直停留3；第三步修复也从1项自动清除。
- 直接选步骤不再带着上一页的错误；关闭重开清空反馈。
- 模板提示和服务器失败提示不受后续字段修改误清除；新校验提示不附带旧保存请求关联号。
- Escape后回新建入口，无新增页面错误/意外网络请求；截图不代表读屏器实际播报认证。

四项真实脚本单元测试补充：全表错误从3增至4再清零、跨步骤保留全表语义、模板后再次无效输入、保存待决期间触发校验后收到409的反馈归属。该层运行当前Vue脚本和Vue响应式，生命周期/DOM/API为显式替身；直接调用save只用于防御分支，不冒称通过禁用按钮完成了浏览器提交。

两项源/图包合同精确证明仅新增反馈更新代码，原模板/CSS/校验/请求保持，并绑定所有104PNG。历史图包及原批准证据不回写；仅对已知前后完整哈希增加历史关联，任意漂移不被归一化，当前原始源码另行校验。

复现：`node scripts/verify-ui-phase2-provider-validation-summary.mjs --capture`，加`--baseline`为原生产行为；定向测试为`node --test tests/unit/ui-phase2-provider-validation-summary.test.mjs`。

## 交付边界

本次没有部署，无可调配置。后续静态前端发布后刷新页面生效，本改动不要求Node/Python重启；不把这一点当作整个待发布计划的重启结论。

正式104PNG和两套HTML/JSON保留，共108文件。临时Vite端口54592/54631及本次Node宿主已核对关闭，浏览器由finally关闭；无一次性测试文件，正常构建产物保留。尚未覆盖完整读屏器、软键盘、首错误定位/必填宣告、未知保存结果、日期/只读字段、未审桌面视觉或全73页生产签收。

最终门禁：新增6项定向测试通过；P46相关63/63、第二阶段相关819/819回归通过（后者包含前者，不相加）。vue-tsc与前端生产构建通过，423模块；文档门153文件/73路由通过，代码样式门及git diff --check通过。本批已实现并本地验证，但全计划及生产验收仍未完成。
