# P33 团队审核来源边界修复

## 范围与根因

2026-09-13，main / af239b08b69f7d97cd0372f9840a70009eeef0c7，开始时已有622项工作区变更，
暂存为空。使用 ui-skills-root 选择诊断技能，先两次复现、再核对历史提交与当前调用方。
本轮只修测试的证据归属，不实施新视觉、不改变用户批准或生产行为。

`ui-phase2-teams-review.test.mjs` 在模块初始化时调用 builder，旧图记录的原因窗指纹
b58a89ec… 与当前3191e4ba…不一致，导致七项测试根本没有执行。控件和字段测试也各有
一项同根因失败。不是换行差异：指纹已经统一LF；也不是P33新增默认字数上限。

逐行差异仅为共享原因窗的可选 maximumLength prop、条件说明、原生 maxlength 绑定与
提示样式。实际 OrganizationAdminCenter 没有传上限；现有SSR合同验证默认无上限、
明确传500时生效、required和最少2字不变。没有改变P33内联创建原因原有500字上限。

## 修复与防护

- 新测试辅助文件固定 df4b7263b1684e94e4ecc8d46c856c202fd9ee7b 的三套完整清单。
  三套本地清单必须与固定Git版本精确相同；批量读取并验证全部15/20/20条历史源绑定。
- 原控件、字段、动作、容器及图片断言继续执行；574张原PNG全部按原指纹检查，未重拍、
  覆盖、调换指纹或调整批准状态。基础图包没有approval字段，严格保留原结构和非生产边界；
  控件/字段图包及动作登记仍是pending-user-review。
- 另外检查全部55条当前依赖绑定：仅允许原因窗的精确3191e4ba版本，其余必须与图包一致。
  对每个来源分别注入未知字符的55个反例均拒绝，LF/CRLF等价，不存在通用漂移豁免。
- 当前实际原因窗与组织调用方独立SSR验证；实际团队子组件、父函数及后端服务的既有
  惰性验证继续读取当前工作区，不以历史代码替代当前行为测试。
- builder本身没有修改：传当前源码配旧图仍必须报stale team source，不能生成看似
  当前的旧审核登记。历史读取器仅用于本地测试，不在生产或浏览器中导入。

原31源码位置、17语义组、12含本地动作、13模型、3容器、41业务控件变体等断言保持。
这些数字不代表页面已经实施。OG-G02等待成员写入时切团队的异常/错误反馈归属仍由
原提取验证如实复现，未把它改写为通过或决定新的草稿、迟到回执政策。

## 验证与使用

```powershell
node --test tests/unit/ui-phase2-teams-review.test.mjs tests/unit/ui-phase2-teams-controls.test.mjs tests/unit/ui-phase2-teams-fields.test.mjs tests/unit/ui-phase2-teams-capture-boundary.test.mjs tests/unit/ui-phase2-organization-reason-contract.test.mjs
```

原复现：登记文件启动失败；控件/字段9项7过2失败。新增测试首次发现基础图包没有
approval字段，按真实清单结构修正该新增断言，原文件与原元数据未改。
最小边界测试4/4；最终上述5文件29/29，0取消/跳过，约5.04秒。
没有改生产代码或依赖，不重复无影响的构建。只运行风险匹配的相关完整测试，不重跑
全库，不能从本轮结果推算新的全库通过率；既有全库失败仍需逐项处理。

## 交付与未完成项

本轮同步当前计划、进度、P33页规格和Feature Map。没有增加生产参数、环境变量、API、
路由、权限、持久化结构或依赖；OpenAPI、后端/Worker/Python消费者及生产重启不适用。
运行方式仍是上述本地测试命令，不需调节运行配置。

只新增永久测试/辅助文件和交付记录；没有临时图片、日志、夹具文件或浏览器服务。
本轮测试进程均正常退出，历史受清理策略限制的目录未触碰、未绕过重试。
工作区仍混有大量前序改动，全局发布门禁未通过，未暂存、提交或部署，commit hash不适用。
P47刷新失败视觉意见仍待答复；P33真实C组合、OG-G02、其他页面/状态、真实API/权限及
全73页生产交付仍未完成，不用本次证据修复代替完整实施。
