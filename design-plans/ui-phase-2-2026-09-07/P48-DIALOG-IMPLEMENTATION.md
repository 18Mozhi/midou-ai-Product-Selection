# P48 任务弹窗实施记录

2026-09-23，继续将已审核通过的 C 方向落实到来源管理的四个既有对话框：采集设置、版本与回滚、固定样本回放、解析兼容矩阵。保留 ProviderSourceCenter 对读取与写入的编排，子组件以显式 props/emits 接收现有状态；不改变 API、字段、权限、业务规则或持久化合同。

编辑设置/版本窗及样本/兼容窗采用蓝色任务身份与白色事实工作区；字段增加就近说明，版本/样本/兼容信息按任务顺序呈现，兼容矩阵在窄屏逐行标注字段名。新增共享 `useProviderSourceDialogFocus`，为这些本地弹窗提供标题首焦点、Tab/Shift+Tab 循环、Escape 关闭、背景 inert 与触发入口焦点归还；关闭控件在对应写操作进行时禁用。页面状态/request ID 同时在开启的弹窗内可见，避免背景被隔离时丢失反馈。

保持现有五项配置、公开来源“先停用保存→真实烟测→再次启用”、回滚目标版本及期望版本、不可变样本快照/已存快照回放/另一管理员复核、兼容矩阵只读等合同。未添加新的写入口，也没有在测试中访问真实外部来源。

验证：`npm run typecheck:web` 通过；`npm run test:e2e -- tests/e2e/m03-07-provider-sources.spec.ts` 桌面 Chromium 12 passed / 1 skipped、390px mobile 13 passed。测试写入由本地 API 夹具拦截，不证明生产权限、真实数据库写入、烟测或双人审批。发布门禁和线上检查在部署前单独记录。

范围边界：此批完成弹窗呈现和基础模态键盘交互，不等于 P48 全部异步迟到响应、写后读取失败的归属/恢复、SC48/PR-G01、真实权限与外部来源行为、MySQL或正式生产验收已完成。全 73 页实施目标继续开放。
