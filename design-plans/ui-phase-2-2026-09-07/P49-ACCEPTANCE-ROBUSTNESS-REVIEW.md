# P49 · 1688 启用检查稳健性 r1

## 本批范围

在 P49 实际 Vue 评审模板上核对项目现有三种主题、两种密度、长内容、失败内容、320px 最小宽度、844×390 手机横屏，以及桌面容器内组件级 200% CSS 缩放。审核宿主只在本地 Vite 中替换真实 `Alibaba1688AcceptanceCenter` 模板并追加隔离 CSS；**生产 SFC、ApiClient、接口、权限、数据库、环境、依赖和线上数据均未改，未部署。**

本批依照适配原则按可用内容宽度重排，而不是把桌面结构等比例缩小。200% 组件缩放最初暴露了桌面双栏、门禁卡片和四列表单的横向溢出，评审样式已在该状态切换为单栏，并重新跑完全部矩阵。

## 主题、密度与内容边界

- 主题：使用仓库真实 `deep-ocean / aurora-purple / cloud-white` 令牌，分别对应“信号纸 / 档案纸 / 净页白”；三者都是浅色纸张主题。仓库当前没有暗色主题，因此不宣称暗色模式通过。
- 密度：`standard / compact` 两组真实令牌均有运行证据；评审 CSS 使用 `--so-*` 语义变量，不在本批样式中硬编码主题色。
- 权威数据：基础主题/密度、320px、横屏和缩放使用 E2E 原始 2/3 门禁夹具。
- 合成长内容：责任人、门禁原因、待办、解析器版本、合同标识、错误分类及 200 字关键词均沿真实响应结构扩展，只用于排版压力测试，不代表生产事实。
- 合成失败内容：来源停用、门禁待验/阻断、覆盖 invalid/not_observed/not_exercised 和运行 failed 均沿真实枚举与响应结构生成，只用于状态组合评审。

## 实际证据

- [42 图总览](../../output/playwright/p49-acceptance-robustness-review/index.html)：24 张三主题×两密度×四宽度，4 张长内容，4 张失败内容，2 张组件级缩放，3 张 320px，3 张手机横屏，另含 2 张技术详情展开图。
- [机器证据](../../output/playwright/p49-acceptance-robustness-review/evidence.json)：40 次真实 App 路由运行、699 项检查、42 张 PNG 和实际加载源码哈希。
- 每次运行都验证页面与组件零水平溢出、可见按钮/链接/summary 文案未裁切、粗指针控件至少 44px、语义颜色令牌与密度令牌已解析、零页面异常、零控制台错误。
- 网络严格限制为导航、会话、启用检查、组织和工作区五类本地拦截 GET；没有 POST、来源启停、数据库写入、权限修改、凭证访问、外链或未知请求。
- 320/390/760 使用单栏内容结构；844×390 保留可读的双区桌面结构；1024/1440 的组件级 200% 压力状态使用单栏，避免将压缩后的有效内容宽度误当作桌面宽度。

复验：`node --test tests/unit/ui-phase2-1688-acceptance-robustness.test.mjs`。重建图包：`node scripts/verify-ui-phase2-1688-acceptance-robustness.mjs --capture`。随机端口和 Chromium 已关闭，正式图包保留。

## 未覆盖与待审

组件级 CSS `zoom: 2` 是确定性的容器压力测试，不等同于浏览器 UI 缩放、操作系统文字缩放或辅助技术认证。真实手机/平板、Firefox、Safari、Edge、SC49 生命周期、KeepAlive/迟到响应、真实 API/MySQL/权限/采集和生产实现仍待后续验证。

本批只申请 P49 主题、密度、长内容、失败内容、320px、横屏与组件级缩放的适配审核；不代表 P49 前三批、P48、生产实现或部署已经通过。
