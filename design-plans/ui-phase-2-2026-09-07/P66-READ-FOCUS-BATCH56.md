# P66 读取焦点交接 · 批次56

起点main/6025c198、606项在途变化、索引为空。上一批追踪编号修复与实际验证为实质进展。本批继续P66键盘交互，不把自动继续当作图稿批准；完整73页目标不缩减。

## 依据和实现

已读AGENTS、Feature Map runtimeTopology、P66读取/无障碍规格与总纲M08-01，再核对RuntimeTopologyCenter和runtime-topology.css。原首次重试按钮随loading替换，保留失败重试随refreshFailure清除；顶部按钮同时原生disabled。实际App四组均复现“首次重试后没有把焦点交给永久刷新入口”。

按ui-skills-root选择fixing-accessibility，并使用现有Node/Playwright实际App验证方式。复用项目已有的局部焦点交接模式：在原单飞guard之后、清除旧重试区域之前，只把当前持有焦点的retry/noticeRetry交给顶部刷新按钮。两端必须连接、可见且不在inert背景；焦点成功后，仅在目标越界或中心被遮盖时滚动。没有拥有焦点、用户已移走、目标不可用或焦点失败时不抢焦点/不额外滚动。

顶部按钮改为aria-disabled/aria-busy，保持原load与原controller防重；原生button仍能接受焦点，等待文字沿用“正在刷新…”。生产CSS只把现有等待外观扩展至该aria-disabled状态。C审核CSS单独设浅灰底、灰字及不透明蓝色焦点框，避免等待透明度淡化焦点。原两个重试入口的出现条件/禁用和其他控件不变。

未改首次加载/错误文案、反馈标题层级、调度、业务结论、15秒超时、共享重试、追踪归属、API、权限或保活离页策略；这些不是本轮顺带调整的内容。完整C布局仍审核专用，生产仅上述交互局部修复。

## 验证

- 新14项单元合同覆盖两个自有焦点来源、用户移焦、源/目标不可见或断开或inert、focus拒绝、目标上下越界或覆盖，以及单飞guard先于交接/移除；加原追踪、模板、M08-01与API客户端共49项通过。
- 实际App `node scripts/verify-topology-read-focus.mjs` 四组1440/390×减少/正常动效通过112项检查、28个本地GET。首次和保留失败重试均交接焦点，截图滚动之前已检查实际视口/中心命中；等待期间Enter/Space/程序点击无重复派发；成功/失败后焦点保留。用户转移到“告警与阻断”目录后，后续结果及无焦点程序重试均不抢焦点。
- 检查灰底/灰字、opacity=1、蓝色3px焦点。首次颜色读取命中过渡中间色，改为8秒有界等待实际最终计算样式，再断言；没有改待测DOM样式或使用固定sleep。截图前等待字体及双帧，所有组都检查字段和按钮可见边界/中心命中；无参数不产图。
- 追踪回归184项/40GET、默认组合304项/16GET通过，本轮最终合计600项浏览器检查/84GET。无外部请求、业务写入、下载或页面异常。既有追踪测试的15秒等待及剪贴板替身继续通过，不等于真实权限或审计。
- 前端类型与构建通过，资源预算253通过；Prettier检查通过。

## 图册和审核

正式 `output/playwright/p66-read-focus-r1` 共10PNG/167来源指纹：每端首次重试焦点、等待、完成、保留失败重试焦点、保留重试等待5图。只捕获减少动效组，正常动效同样验证。index.html提供图册，manifest绑定捕获时来源。

本次展示手机initial-retry-pending，仅请求审核底部浅灰等待按钮、文字与蓝色焦点框；上方说明、全状态、真机触控及生产不在范围。未答复保持待审，批54/55与其他局部批准范围不扩大。旧图原来源保持，不把旧捕获重新绑定当前Vue/CSS。

用户操作仍为Enter/Space/点击刷新或重新核验；等待时不重复发请求，返回后继续Tab即可。复验无参数；新正式图可加`--capture-review rN`，必须新目录、不可覆盖。

## 闭环和剩余

同步Feature Map、P66规格和运维说明。没有新增API/配置/env/依赖/数据库/权限/外部合同，OpenAPI无需更新；后端/Worker/Python消费者不受影响，无单独后端重启要求。未来发布仍走既定宝塔整体部署，本批未部署或重启。

正式图、验证器与永久测试保留；未创建验证专用临时文件，已有构建输出属必需项目产物。Vite/浏览器按finally关闭后复核，无常驻服务交接。前批 `output/playwright/p65-read-states-r1` 和 `output/playwright/p65-table-controls-r1` 的清理策略阻碍仍存在，未绕过。

HEAD既有API覆盖225/258与旧223/256断言不符的输入未改变，仍缺处理决定；不重复失败、不暂存/提交，commit hash不适用。P66完整读取状态、命名播报、保活、全主题/密度、真实运行权限审计及73页整体实施验收继续。

收尾复核：10PNG/167来源SHA无差异，Feature Map解析与本批diff空白检查通过；相对批55，仅RuntimeTopologyCenter和runtime-topology.css两个生产源文件发生预期变化。七个本次端口及关联测试/浏览器/构建进程无残留，索引为空，610项在途变化未暂存；两个前批受阻临时目录仍保留。
