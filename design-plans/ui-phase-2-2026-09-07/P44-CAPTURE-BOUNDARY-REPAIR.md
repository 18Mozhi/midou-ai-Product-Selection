# P44 历史图证、当前装配与状态重放

## 目标与范围

继续全73页C方向实施，不把局部图批准扩大为整页或生产验收。起点main /
af239b08b69f7d97cd0372f9840a70009eeef0c7，561项既有工作区变更、暂存为空。
P44目录、比较控件、同角色结果和角色资料的既有窄范围批准保持不变。

本轮使用UI技能选择Playwright既有驱动，局部装配冲突按baseline-ui沿用原间距与控件。
不安装依赖、不访问生产、不创建真实管理员。产品总纲4.3的平台超级管理员授权及审计
规则不变，角色矩阵仍读取后端目录样例，不从按钮推断真实权限。

## 历史与当前分开

最初5份定向测试19项13过6失败，失败点为ResponsiveDataView及比较样式的来源SHA漂移。
新增`ui-phase2-admin-review-historical-capture.mjs`固定六份完整原manifest、原提交与
所有262项来源绑定（跨包重复计数），通过严格Git批量blob解析读取；未知阶段/来源拒绝，
无当前源码兜底。原322张PNG、清单、请求记录、负面焦点观测及用户批准图哈希均保留。
五份测试只把捕获来源断言切换为明确历史来源，当前script/指令/原生控件检查独立保留。
详情包也保留原来源及变换结果指纹，不再要求已扩展的页面helper与旧helper逐字相等。

当前回放继续使用原五份完整SHA固定的浏览器驱动（边界驱动分别运行baseline/preview）。
只替换输出目录、解析静态模块导入，以及插入一个审核宿主CSS；原交互、网络白名单、
断言和finally清理不改。当前原始Vue/CSS进入已有审核模板转换，**不是完整原始App装配**。
跨轮比较只对已枚举URL检查的随机本机端口做归一；协议、127.0.0.1、合法端口、路径、
查询、无凭据/片段逐项检查。原运行期“URL不变化”断言仍原样执行，不删检查。

## 实际发现与修复

1. 去掉历史源码重构后，当前管理员标题使旧preview的nav/筛选紧邻锚点失效。
   页面helper临时抽出精确标题，完成旧装配后复用其文案及v-if、使用审核目录class，
   避免重复标题，也避免生产标题仅对直接子节点显示的CSS在嵌套审核容器中将其隐藏。
   旧输入产生的原变换SHA不变；当前完整script、动态指令与控件保持。
2. 手机已批比较CSS的负外边距与旧审核容器零padding叠加，真实390容器clientWidth356、
   scrollWidth376，标题左边界-3且文字被截断。保留[失败截图](../../output/playwright/p44-current-replay-diagnostics-r1/390-overflow.png)。
   新`admin-current-replay.css`仅在P44审核宿主、max760下复位六区margin并恢复字段/资料
   内边距，只有margin/padding声明，不改运行组件、颜色、动画、事件或业务条件。
3. 首次跨包检查因随机Vite端口不同失败；保留原实际URL，比较函数仅处理明确枚举的
   URL断言。反例覆盖协议、外部域、路由、查询及普通能力数量漂移，均必须拒绝。

## 当前图包与验证

[正式当前图包r3](../../output/playwright/p44-current-replay-r3/index.html) /
[完整当前清单](../../output/playwright/p44-current-replay-r3/evidence.json)：

| 组合 | 检查 | PNG | 与旧图字节不同 |
| --- | ---: | ---: | ---: |
| 角色比较 | 164 | 44 | 23 |
| 创建管理员 | 393 | 106 | 7 |
| 页面组合 | 282 | 68 | 28 |
| 整体装配 | 302 | 72 | 23 |
| 临界宽度原状 | 238 | 16 | 5 |
| 临界宽度焦点提案 | 274 | 16 | 5 |
| 合计 | 1653 | 322 | 91 |

先全部不截图回放通过，再正式捕获。前四组390/760/761/1440，边界两组1200/1201；
共20组宽度组合，原请求与检查一致（仅上述本机端口归一），276本地GET与12本地拦截POST。
无真实账号写入，临时密码只记录合成值匹配标记。63项聚合来源指纹含折入CSS与新审核
样式，分包指纹、所有新旧图片、尺寸、字节差异均独立核对。231张字节一致；91张
不一致，其中12张页面长图尺寸变化，不能宣称像素等同。新图也不自动获得旧图批准。

新保护测试覆盖当前标题/完整动态绑定、精确驱动逆向、完整历史来源、URL归一反例、
CSS仅审核手机范围、完成包拒绝重拍/续拍、当前图证来源校验。最终相关7文件30项通过；
另含详情/已批手机实现的6文件29项通过，两组有4项详情测试重叠，不能相加为59个唯一测试。

人工查看裁切诊断图及当前手机比较控件、目录、创建默认顶部/底部和提交等待图。
目录标题和比较文字现在可见；筛选入口右侧“调”经真实模板核对是原`aria-hidden`装饰字，
不是被裁切的“调整”文本，未擅自修改共享控件。创建默认两滚动位置的新视觉组合已提交
用户审核，尚未收到回复；不将默认可用按钮视作真实表单合法或真实创建成功证明。

创建时使用`node scripts/verify-ui-phase2-admin-review-current-replay.mjs --capture`；仅不完整
且已有完整分包时可`--resume`，逐包重新验证来源/图像/请求后复用，不覆盖未完成分包。
完整r3存在后入口拒绝再次运行或覆盖。后续变更需新版本图包，不重拍旧批准图。
日常校验使用`node --test tests/unit/ui-phase2-admin-review-capture-boundary.test.mjs`。

全库检查由`node scripts/verify-ui-phase2-admin-review-unit-gate.mjs`生成独立
`P44-CAPTURE-UNIT-RESULT.json`，复用既有完整TAP解析器；文件存在时拒绝再次执行。
最终完整CLI结果：[P44-CAPTURE-UNIT-RESULT.json](P44-CAPTURE-UNIT-RESULT.json)，268文件、
1539测试、1407通过、132失败、0取消/跳过，372920.5004ms，exit1；132项失败完整诊断与
footer一致，无截断。与P39报告139失败按名称多重集比较（仅Windows反斜杠转义归一），
移除7项、无新增名称：本批P44六处及前批P47刷新一处，不能把七处都归功于本轮。
此前P39报告之外新增的一项防重跑测试及本批七项新测试解释测试总数增加8。
全库失败仍需逐项处理，不假设全部无害。报告入口再次运行在启动子进程前拒绝，
原报告字节未变，该收尾检查通过但不计入上述1539项。

## 清理与边界

审核Vite与Chromium已由各驱动finally关闭；另行核对本轮21个端口均无监听：53989、
54120、54213、54364、54541、54642、54806、54851、54935、55102、55144、55199、55251、
55301、55341、55448、55503、55586、55773、55833、55857。全库子进程66180已退出，
现有格式测试创建的fixture自行清理，临时目录下没有本次启动后遗留的scoutops-code-style目录。
文档、运行说明一致性、相关代码格式及限定diff检查通过；生产构建不适用本轮审核辅助变更。
未完成的r1/r2图包仅为本轮调试临时输出，清理命令被工具策略拒绝，不能绕过重试：
`D:/项目工程文件/vue/curson/工具/智能选品/output/playwright/p44-current-replay-r1`；
`D:/项目工程文件/vue/curson/工具/智能选品/output/playwright/p44-current-replay-r2`。
它们不作为当前验收证据。正式r3及单张裁切诊断图属于审核交付物，不作临时文件删除。
只读核对r1/r2合计312个文件仍存在，清理命令在执行前被拒绝，未删除任何文件。

此前另两处清理遗留未重试：
`D:/项目工程文件/vue/curson/工具/智能选品/output/playwright/p47-c-e2e-temp`；
`C:/Users/23136/AppData/Local/Temp/scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`。

无生产Vue/CSS、API/OpenAPI、后端/插件/Python契约、数据库、环境变量、配置、依赖或
安全权限变化；这些生产消费链不适用于本轮审核辅助改动。不需要生产重启/迁移或新调节项。
未暂存、提交、部署；commit hash不适用。全库门禁、待审设计、真实账号/权限验收和
全73页完整重构交付仍未完成，不将这些本地样例图当作生产上线证明。
