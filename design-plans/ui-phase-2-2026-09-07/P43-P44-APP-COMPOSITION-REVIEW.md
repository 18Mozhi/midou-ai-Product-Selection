# P43 / P44 · 完整应用 C 组合 r2

2026-09-13。承接73页完全重设计。本轮将用户管理与管理员管理的既有 C 页面提案放入
真实 App、NavigationShell、router、同一个缓存账号组件中，补上从局部页面图到整页组合
的缺口。**这是新的整页审核提案，未生产接入，也不扩大此前局部批准。**

## 看图入口与本次请审范围

- [80张现有/新C组合对照图](../../output/playwright/account-pair-app-c-r2/index.html)
- [机器证据与来源清单](../../output/playwright/account-pair-app-c-r2/evidence.json)
- [P43桌面整页](../../output/playwright/account-pair-app-c-r2/review-1440-P43-whole-page.png)
- [P44桌面整页](../../output/playwright/account-pair-app-c-r2/review-1440-P44-whole-page.png)
- [P43手机整页](../../output/playwright/account-pair-app-c-r2/review-390-P43-whole-page.png)
- [P44手机整页](../../output/playwright/account-pair-app-c-r2/review-390-P44-whole-page.png)

本次请审核两页与 C 导航合在一起的整体层级：导航、标题、全平台汇总、账号目录、
管理员页权限比较；创建窗和详情图提供上下文，不把本次布局意见视为全部弹窗状态批准。
手机长页截图含真实固定快捷导航，它在整张长截图中位于拍摄视口底部，不代表页面中段新增
导航栏；详情/比较的长区域图也可能包含该固定层，不作为所有内容无遮挡的证明。

## 实现边界与发现

使用 UI 路由、frontend-design、Vue 与 Playwright 技能；复用原 C 蓝色目录、白色表单和
权限比较样式，不新增运行依赖、不换配色方向。不是新建静态假页面：真实菜单切到管理员页，
再用浏览器后退回用户页；共享账号实例不重建，标题随实际 tab 恢复。

新预览组合器保留账号父级、创建和详情的完整 script，以及原有全部 v-model/事件绑定。
壳层只复用此前明确的 C 导航预览转换。真实权限、五字段创建、角色默认值和写入规则不改。
P43 使用 buyer 样例、P44 使用原 admin 样例，管理员空组织/会话事实明确保留；不把普通用户
详情伪装成管理员。原 buyer 样例缺少 membership.organization_id 的限制也未暗中补全。

实际发现并修正两个装配问题（仅新预览）：

1. 旧管理员标题样式默认隐藏，只允许父级直接子元素显示；放入新目录后标题消失。
   改用独立组合标题类，保留原 admins 条件，并给用户标题加互斥条件。
2. 已批手机比较控件为原卡片内边距设置了负边距，放入整页后标题/字段贴边。
   复用已有 admin-page-assembly-preview.css 的精确覆盖，不修改原生产局部样式；
   加入标题范围、文字和字段至少15px内缩的真实几何检查。

## 验证

先手机无图最小验证，再四宽度现有/提案完整无图验证，最后 r2 完整采集：

| 维度 | 结果 |
| --- | --- |
| 实际视口 | 390 / 840 / 1200 / 1440 × 1000 |
| 上下文 | baseline/review各4组，共8组 |
| 命名浏览器检查 | 176项通过，含缓存DOM、两页标题、原生创建、默认角色、取消回焦点、详情身份、6/0权限比较、无横向溢出 |
| 请求 | 56次本地GET，0写入；未知API及外部请求拒绝 |
| 图 | P43整页/目录/创建/详情；P44另加比较/同角色，现有与新稿共80图 |
| 来源 | 181项原始加载/工具/CSS来源；4项变换后源码哈希独立记录 |

新8项永久测试与上一轮实际App归属、原P43历史/当前保护共3文件18项全部通过，0取消/跳过。
覆盖完整业务script/事件模型保留、原管理员身份、标题条件、全部来源及80PNG精确字节、
重复capture启动前拒绝。文档153项、运行说明一致性、6个当前代码/CSS/地图格式和定向diff
检查通过。没有改产品源码，不重复前端构建或已知失败全库门；旧完整报告不改。

主题偏好读取使用受控503，只验证已有读取失败回退；不是三主题、全密度、完整菜单和弹窗
状态、读屏器、真实后端/权限/审计、生产或全站验收。详情/创建只捕获当前可见窗口区域，
不能按图数宣称覆盖全部字段和状态。原48图功能证据、历史P43/P44/P47图和批准均未改。

## 使用、运行与清理

`node scripts/verify-ui-phase2-account-pair-app.mjs`无图双端与断点回放；`--smoke`仅手机新稿。
`--capture`独占创建 r2，已存在时在浏览器启动前拒绝，不得覆盖；修改来源后须新版本采集。
永久验证：`node --test tests/unit/ui-phase2-account-pair-preview.test.mjs`。

没有产品源码、API/OpenAPI、后端/Python/插件、数据库、环境变量、依赖、权限或宝塔变化，
所以生产运行合同同步及重启不适用；新CLI只用于本地审核，地图和说明已同步。
本轮新r2图、清单、工具、CSS与测试为审核交付物保留。r1是本轮产生的裁切中间包：对精确
路径的删除命令被工具策略在执行前拒绝，未删除、未重试或绕过。仍保留80图及2文件：
`D:/项目工程文件/vue/curson/工具/智能选品/output/playwright/account-pair-app-c-r1`。
如用户手动清理，只处理这一中间目录；正式r2和之前原图不能删除。此前4处策略拒绝目录
仍按[上一轮清理记录](P43-LIFECYCLE-CURRENT-REVIEW.md#使用及收尾边界)保留。
所有服务/浏览器由 finally 关闭，收尾确认65301/65407/49153/49301/49326/49363/49450/
49495/49756/49821/49878均无监听，无本轮残留进程。无其他新临时文件或日志。

全库126失败是此前完整快照，本轮不重算或宣称清零。工作区已有混合变更，未暂存/提交部署，
commit hash不适用。下一步由用户审核这一整页组合，其他页/状态和全部交付门继续，不能据此
认定73页完成。
收尾Git617项变更、暂存为空。本轮只新增6个永久文件及相关文档补充，不处理混合既有变更。
审核入口已请求在Codex文件面板展示，工具返回queued，不宣称用户已经看过或批准。
