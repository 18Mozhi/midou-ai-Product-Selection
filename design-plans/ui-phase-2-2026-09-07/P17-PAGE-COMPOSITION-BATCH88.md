# P17 实际 Vue C 组合 · 批次88

本批为 `ScoreRuleConsole.vue` 添加审核专用蓝白样式层，保留成员壳层、评分规则读取、五项配置投影、版本目录、创建/影响预览/生命周期原生弹窗与原有 API 调用。首屏重构为“当前启用版本与质量门覆盖 → 版本工作区”，不保留旧账页标题、米色背景、圆角卡片或营销式布局。

实际 Vue 在 1440/390 与两种动效偏好下通过 28 项检查，零写入：只有当前启用规则的市场、竞争、成本证据组和风险维度才显示为评分配置已覆盖；影响预览保持只读，不能改写规则、机会或历史评分。r3 移除了共享质量门及权重条遗留的浅米色，图册包含默认、影响预览和读取失败的双端 6 PNG/5 个受控来源指纹。

无需重启、部署或配置变更。审核层未修改生产评分组件、规则阈值、审批流、质量门、权限或 API。创建草稿与生命周期原因弹窗仍保留现有实现，但本批未提交写入；真实会话/RBAC、创建/审批/启用/回滚写入、预览分页、读屏与生产验收仍待完成。
