# P65 服务结论状态矩阵 · 批次52

起点 main/6025c198、588 项在途变化、索引为空。上一轮指标修正和验证属于实际进展；本轮补实际 Vue 的服务结论状态，不缩减全73页目标，不将未回复的图视为批准。

## 事实与改动

已先读 AGENTS、Feature Map 的 releaseRollout 映射、P65 规格和总纲 M07-05 固定单后端发布合同，再追到 ReleaseRolloutService。当前服务可由 release.status=failed 返回 stopped，即使不存在 automatic_stop 门；也可由 release.status=rolled_back 返回 rolled_back，即使不存在 rollback 门。旧实际 Vue 标题“发布已自动停止”“已回滚到稳定版本”和说明“回滚事实已审计”超过这些返回值能够证明的范围。

本轮只在 C 审核转换器将两标题改为“服务返回停止结论”“服务返回回滚结论”，并提示结合下方回滚记录和证据标记核对。未改生产 Vue、服务判定、状态优先级、布尔证据、API 或历史行。原业务 script、六列表格和全部手机详情插槽的字节等值合同仍通过。使用 fixing-accessibility 技能保证结论用明确文字表达；沿用原生技术披露、焦点圈定和返回，不新加执行按钮或 ARIA。

## 服务生成样例

新增 scripts/lib/release-state-fixtures.mjs，AST 提取当前 ReleaseRolloutService 整个类，编译到隔离 VM；仅注入内存 read 仓储与固定时钟 2026-09-15T08:00:00Z。不调用同文件签名或写探针类，不连接 SQL、不写审计、不申请发布资格。

25 组样例各执行一次当前服务 read：六种结论；当前无记录与历史较新但非当前版本；应用/配置/迁移身份不符与来源 SHA 不符；来源回退；缺25%门、无门、待观察门；只有发布状态与实际存在停止/回滚门的区别；身份优先于回滚、回滚优先于停止；错误率等于阈值、读取等于/超过阈值；指标 null 与0；证据恰好30分钟和超过1分钟。没有手动覆盖输出 state 或 flags。

合成记录仅使用现有门 enum、合法形状的36字符 UUID 和不同主键，没有额外 canary_50 或同类型重复门；未将旧原型的无效存储边界输入冒充真实数据库记录。服务移除 metadata、1250ms 计时以及最新匹配/历史分离有单测。原 E2E 的矛盾显示样例仍单独保留，不用它证明本轮服务计算。

## 验证与图册

- 新状态来源8项单测通过；结论文案合同先失败1项后修正通过。状态来源与实际 C 编译/脚本/插槽共16项通过，扩展读取焦点、追踪、指标和 M07-05 后70项通过。
- 新验证器 `node scripts/verify-release-state-matrix.mjs --capture-review r1`：1440/390 × 减少/正常动效，2674项检查，100个本地发布读取 GET。逐组核对服务结论、完整 SHA、当前与历史、证据标记、动作计时和五种阻断码；技术信息用 Enter 开/关，不重读。
- 桌面逐行四项数值；手机逐记录摘要、选定记录全部六字段、待观察门、初焦点、前后 Tab、Escape 与返焦。空集合不造门。所有取景的文字、字段、控件边界与中心命中检查在不出图模式也执行；无外部请求、写请求、下载、浏览器异常及页面横向溢出。
- 首次不出图验证为2414项，后补每条手机摘要和待观察门详情，正式捕获为2674项。不能把早期计数称为最终覆盖。
- 原默认实际页面102项/12个本地GET回归通过；本轮最终浏览器验证合计2776项。读取重试逻辑未改，不重复上批15秒与焦点全套；70项相关合同已回归。
- 输出 `output/playwright/p65-state-matrix-r1`：63PNG、169来源及图片SHA256，桌面30张/手机33张。截图来自减少动效组，正常动效完成同样交互。两张手机停止/回滚结论区域已展示待审，不含下方证据、整页或真实动作验收。

默认验证器不输出文件。需要新图时使用尚不存在的 `--capture-review rN`；目录禁止覆盖。旧P65图引用的审核转换器已漂移，保留原manifest，不改写旧来源或宣布旧图对应当前版本。

## 收尾与仍待办

仅审核转换器、永久验证器/样例/测试和对应文档/地图变化。生产脚本、Vue、CSS、API/OpenAPI、数据库、权限、配置/env、依赖、离页行为均未改，producer/consumer 运行合同联动不适用。已有上一批前端类型、构建和253资产预算证据未因审核专用改动失效，不重复生产构建、不部署、不重启后台。

本轮没有新建验证专用临时文件；63张图是用户要求的审核交付物，永久脚本/测试保留。测试 Vite/Chromium 按 finally 关闭，端口63640/63727/63768收尾复核。前一轮清理被策略拦截的 output/playwright/p65-read-states-r1 仍留存，未尝试绕过策略。

既有 API 覆盖断言223路径/256操作与实际 docs/openapi.yaml 的225/258不符，未获处理决定；本轮未改该测试或API，不重复运行已知失败、不暂存/提交，commit hash不适用。P65全控件/主题密度/保活策略与真实发布验收、其他页面完整实施和全73页签收仍未完成。P62/P64离页决定及此前各待审图仍待用户反馈，不据自动继续指令改变规则。
