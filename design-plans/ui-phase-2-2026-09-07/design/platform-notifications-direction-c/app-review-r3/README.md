# P57 实际 App 阅读与筛选组合 r3

2026-09-15：使用未改写的App、Router、NavigationShell及实际P57组件，沿用仓库platform-message-management E2E样例。当前12张PNG、186份加载来源指纹，390/1440宽度与两种动效偏好共4环境56项检查通过。手机筛选区域已申请局部审核，待答复；其他图片是诊断材料，不等于整页批准。

## 本次修复与范围

- 页面及Teleport筛选键盘焦点绑定既有蓝色token；阅读窗危险按钮hover保持既有红色。
- 筛选字段增加可见关联标签、单列间距、44px控件和白字蓝底提交。CSS仅限定通知portal；共享表单仅通知域增标签/实例ID，其他域原字段、选项及事件不变。
- 驱动检查焦点、阅读/编辑返回、筛选应用自动关闭、两类独立分页、无横向溢出、稳定后的危险颜色、筛选控件几何/颜色/标签。样例页2不代表后端分页事实；未测试写操作。

## 图片

每个宽度包含messages、reader、editor、filter、deliveries、system-facts。editor/filter及手机reader为1000px高视口图；其余是完整页面长图。长图中的固定顶栏/底栏可能位于截图的当前滚动位置，不能用于认定实际遮挡或证明无遮挡，尚须视口命中与滚动专验。手机筛选图只审核字段与操作区。

## 证据边界与下一步

真实完整App仍保留暖色旧外层导航及重复“通知管理”标题，与最终C方向不一致，是后续必须处理的整合项。本批不改全局导航；桌面筛选全屏及留白、全部交互、主题/角色/软键盘/真机/完整读屏均未签收。

所有请求仅本地GET样例：原E2E通知与导航数据、合成登录状态、UI偏好503回退；其他请求拦截，无后台服务、真实权限或发送。使用工作区当前字节，含其他未提交UI，不是干净生产发布包。来源清单包含Vite模块及递归本地CSS导入，不宣称所有构建输入完整穷举。旧r1/r2按原捕获身份保留，不能作为当前代码证据。

日常运行：`node scripts/verify-platform-notification-app.mjs`，不写文件。首次捕获：同命令加`--capture-review`，仅排他创建本app-review-r3目录，存在即拒绝覆盖；不要删除现有审核包以重跑。服务/浏览器在finally关闭，无新增生产配置、依赖或后端重启。采用现有在途CSS导入指纹helper及其2项单测作为验证器直接依赖一并接管。
