# P18 有利润的观察机会 · DETAIL-COST-CONTEXT-C-r1

具体稿待用户审核。C方向已选，不等于本页或行为改进已批准。

[交互提案](index.html) · [双端图册](gallery.html) · [来源与验证证据](evidence.json) · [P18规格](../../page-specs/P18.md)

## 本次补齐

同一机会444的结论与缺项、利润分项、当前输入、待复核记录、九字段提交连续查看。蓝色目录和统一对象身份常驻，手机目录默认收起，首先看到机会与结论；白色工作面区分评估、不可变利润运行与成本复核。切到结论再回到原成本区，来源展开、九字段草稿、失败提示和复核原因保留。忙碌时目录锁定，复核保持内联，不改成弹窗。

20个场景、4个往返核对状态及2个表单底部视口，1440×1000 / 390×844共52张永久PNG。全页图可以高于视口；底部图保持视口尺寸，并逐字段/按钮验证滚动到达和命中，不以长图代替手机首屏或按钮可达性验证。

## 为什么没有采纳

原始M04-04夹具为“户外净水杯利润机会”，id尾号444，v8：系统推荐observe、风险unknown、利润calculated，未提供selection_stage或quality_gates。历史导航允许cost:confirm但没有opportunity:decide。原DecisionPanel只有recommended且all_passed才允许采纳，因此这里不从69.4 USD净利润推导推荐或人工采纳能力，也不移入424的质量门、701的AI数据。

利润快照：含税售价100 USD，总成本30.6 USD，净利润69.4 USD、净利率69.4%，计算时间2026-08-08T12:00:00.000Z。页面只展示样本值，不计算利润或汇率。current_inputs为空，cost_input_reviews有一条40 CNY采购成本待复核，汇总证据3条而返回明细为空；分别保留，不补造成完整证据链。

## 复用与提案边界

复用未修改的[原利润稿](../profit-direction-c/README.md)数据、控制器和样式，只在新包添加统一上下文与结论工作面。审核场景中的失败、只读、忙碌和长文本是离线演示；原成本控制器成功反馈及记录也只存在内存，不是实际SQL写入。部分原型保护（本地时间默认值、忙碌锁、失败保留）仍是待审改进，不代表当前Vue已修复。

结论往返保留成本复核原因是新连续稿提案。真实Vue成本表单由父级保存，但复核子组件可能因分区v-if卸载而丢失；没有把这里的同文档显示/隐藏说成真实KeepAlive或路由生命周期通过。

原利润稿仍有模拟成功/失败操作；本次证据主要验证组合、字段/原因往返和事实隔离，没有重复声称验证事务成功。未知结果、跨机会/组织切换、权限刷新、完整十分区、全部动作六态、三主题密度、软键盘/读屏/实际200%缩放和真实Vue/API/SQL/生产仍待完成。原样本当前输入为空，因此“有生效输入且满足全部质量门的同一机会采纳链”仍未交付，不用这个图包抵扣。

## 验证与使用

本地打开交互提案，在顶部选择场景；左侧或手机目录切换五个核对区。核对成本失败提示后，可返回结论，再回原成本区检查草稿和原因。导航仅记录本地意图，不访问生产。

```powershell
node scripts/verify-ui-phase2-detail-cost-context-c.mjs --smoke
node scripts/verify-ui-phase2-detail-cost-context-c.mjs --capture
node scripts/verify-ui-phase2-detail-cost-context-c.mjs
```

最小检查后采集正式图，无参数校验当前源和PNG哈希并复验全部离线状态。使用现有Playwright及来源提取器；检查9字段、失败/复核原因往返、分项展开、唯一ID、触控尺寸、首屏标题、700/701/768/1024重排和零HTTP/存储。内联取消归焦与原生技术信息Enter展开单独验证，不把它们计为模态焦点环。

没有新增依赖、环境变量、API/OpenAPI、配置、权限或数据库变更；不部署、不重启。52图、图册及evidence为永久审核交付；不启动服务，浏览器上下文在finally关闭。全站G0–G5、逐页批准及全站语义分母不提升。
