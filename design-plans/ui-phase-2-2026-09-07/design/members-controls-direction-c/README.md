# P30 成员与邀请 · 按钮状态 C-r1 待审

[打开444张图册](gallery.html) · [操作交互稿](index.html) · [源码对应](../../MEMBERS-SEMANTIC-REVIEW.md) · [原94张上下文图](../members-direction-c/README.md)

本包是独立离线提案，不是生产页面、真实邀请或用户批准。P16独立C-r2布局批准不外推本页，也不批准任一新增业务规则。

## 本次细化

51个显式控件/变体、216个单端状态，在1440×1000和390×844两个视口生成432张控件图，另6反馈场景双端12张，共444张。原94图和其布局、数据、控制器保持原样；新HTML复用它们，只叠加本包样式与审核适配器。

| 分类 | 控件/变体 | 单端状态 | 边界 |
| --- | --- | --- | --- |
| 现有动作代表控件 | 19 | 84 | 映射精确selector/state；不代表全部实例或Vue通过 |
| 同一动作附加变体 | 22 | 92 | 加载/错误重试、选中页签、邀请阶段、恢复及原因窗按钮，不新增业务动作 |
| 仅设计提案 | 10 | 40 | 目录、折叠、空结果重置、反馈详情、中断尾部保留，不进入源动作分母 |
| 反馈上下文 | 6 | 不计控件槽 | 部分失败、中断、待投递、版本冲突、刷新失败、处理中 |

默认、悬停、键盘焦点、按下、禁用、处理中按实际适用控件展示，不统一补造六态。四类原因窗（禁用、恢复、分配角色、撤销邀请）分别列确认/取消/关闭；确认至少2字才可用，源行为先关窗后写，故没有伪造窗内提交busy。五个原生select只展示默认/hover/focus，不把浏览器原生选项浮层说成已覆盖。

frontend-design与fixing-accessibility技能用于本包：蓝色明显焦点环、蓝底目录白色焦点环、危险操作按下反馈、折叠入口悬停、错误/逐条结果16px和忙碌说明关联。没有引入依赖、图片生成服务或新的运行服务。

## 操作与事实

打开交互稿的“审核场景”，选择列明控件和适用状态；Tab看真实键盘焦点，点击检查本地意图。示例邮箱为隔离夹具，不联系HTTP、不存储数据、不发邮件。刷新保持两GET意图；成员/角色/邀请写入保留原方法、body与expected_version。恢复成员关系不解锁账号；角色分配是替换；创建待投递邀请不等于邮件发送成功。

动作登记的114代表槽中84绑定新图，30仍明确未映射；不能用22额外变体的92状态抵扣这30槽。全部字段、角色/主题/密度、原生缩放/软键盘及跨路由生命周期未完成。

## 仍需确认 / 实施

- 邀请遇401/403后的失败邮箱与未处理尾部分开保留、恢复登录或权限后手动重新提交，不自动重试：已问未答；当前图仅提案，现有源缺陷未修。
- 共享原因源无maxlength，原提案写500字符并非现行业务；本批没有实施此上限或取得批准。
- 原独立稿共用busy，而实际父组件refreshing与写busy分离。本批逐目标状态验证不代表整页并发禁用逻辑一致；迁入Vue必须按真实读写链处理。
- P30具体稿、全部输入/原因组合、真实C实现/异步归属、其余页面和宝塔生产验收继续待办。整页批准数量不增加。

## 验证与复核

```sh
node scripts/verify-ui-phase2-members-controls-c.mjs --smoke
node scripts/verify-ui-phase2-members-controls-c.mjs --capture
node scripts/verify-ui-phase2-members-controls-c.mjs
node scripts/build-ui-phase2-members-review.mjs --check
node --test tests/unit/ui-phase2-members-controls.test.mjs tests/unit/ui-phase2-members-review.test.mjs
```

smoke为102个焦点/禁用检查及100次交互；完整为432状态检查、100次交互，断言Tab焦点、焦点环对相邻面至少3:1、16px/44px、中心命中、无横向溢出、禁用点击零新增意图、源对象/表单/选择保持、准确意图与取消返焦。PNG和所有来源均绑定SHA256；上下文图不当精确控件证明。浏览器错误/HTTP/cookie/storage均为0，浏览器与上下文finally关闭。完整门禁结果见../../PROGRESS.md。

444 PNG、图册、HTML/CSS/JS、证据与永久测试均为用户要求的审核交付，不是临时截图。不修改apps/packages、API/OpenAPI、权限、env、配置、数据库、依赖或旧图，无新参数、无需重启、未部署。验证临时产物清理结果见PROGRESS。
