# P34 父级读取状态 · C-r1 待审核提案

2026-09-10，源基线 `2eafe040`。本包延续已选定的C方向，补齐父级加载、读取失败、刷新失败和权限失效的画面；不是生产Vue，也不把原筛选、空结果的局部批准扩展为整页通过。

[交互原型](index.html) · [74图图册](gallery.html) · [来源与逐项证据](evidence.json) · [真实父级行为依据](../../P34-PARENT-READ-STATES-REVIEW.md)

## 构图与状态

- 桌面：蓝色范围侧区与白色内容面；手机：紧凑蓝色范围顶区，不缩小桌面布局。正文16px、辅助13px，恢复按钮至少44px触控区，圆角和焦点沿C规范。
- 首次加载不显示审批数量或模板，使用静态占位线和明确等待说明；没有假进度或计时器。
- 首次失败用独立白色恢复区；错误不冒充“没有记录”。说明、追踪和恢复操作按阅读顺序排列。
- 普通后台失败在旧内容上方用浅暖色提示，明确“本轮未更新模板或摘要”；保留可编辑筛选。
- 401/403无论首次或后台都不展示审批子内容；不承诺清除浏览器内存、改变权限、自动登录或自动跳转。
- 重新加载为蓝色主动作，刷新忙碌为原生灰色禁用；焦点3px蓝环，悬停与按下状态另出图。减少动态偏好禁用过渡，没有脉冲/闪烁。
- 错误不是危险写入操作，不使用红色删除确认按钮。每个失败有文字身份，不仅靠颜色区分。

## 实际依据和差异

依据真实 OrganizationAdminCenter.load 的双读取Promise.all、mustReplacePage分支，以及api-client分类/重试；上批56实际App隔离HTTP场景验证过summary和approvals两个读取各自失败。当前设计的14失败状态逐一与这份证据的四个样本（双端×两读取）比对，不从旧提案文字推导行为。

503、429、网络失败在真实客户端已完成至多3次安全GET尝试后才进入最终失败状态；本图没有“禁止自动重试”或等待秒数。旧90图中的rate_limited说明属于历史提案，不再作为父级重试依据。409仅是既有客户端分类的人工响应演练，不声称该GET有新的生产冲突合同。12秒真实超时、畸形响应及组织切换仍未验证。

父级文案精简、错误只展示一处、追踪信息折叠、替换态不显示旧更新时间、范围栏排列均是**本批待审呈现提案**。生产仍保留原父级布局、两处错误说明及当前时间文本。追踪编号明确标为审核夹具示例；没有新增复制、申请权限、登录或返回按钮。

下方内容复用原C子页面离线renderer与字段装饰器，仅作保留内容的上下文；源文件及已批准PNG未改。不是当前真实Vue全页截图，子目录/详情没有因此获批；范围栏也不是新的生产导航替代方案。蓝色范围区按组织只读合同解释范围，不捏造组织名称或数量。

## 交互与验证

17状态（首次加载、成功、后台刷新、7类首次失败、7类后台失败）×双端34组合图；15可用操作状态×双端30焦点图；两动作×双端悬停/按下8图；双端追踪展开2图，共74图。两按钮位置为原父刷新和重试，不虚构业务弹窗。

338项浏览器检查覆盖与实际父级可见性对应、aria-busy、错误alert、原生禁用/焦点/按下、无溢出，以及刷新失败后编辑筛选、再次刷新保留输入。点击仅记录既有summary/approvals两项GET意图，不发请求；后续结果由独立审核工具切换，不用演示成功冒充实际返回。HTTP/外部请求全部阻断，零页面错误、零业务弹窗。

```powershell
node scripts/verify-ui-phase2-org-approvals-parent-c.mjs --smoke
node scripts/verify-ui-phase2-org-approvals-parent-c.mjs --capture
node scripts/verify-ui-phase2-org-approvals-parent-c.mjs
node --test tests/unit/ui-phase2-org-approvals-parent-c.test.mjs
```

默认命令校验现存源/PNG哈希并重跑交互，不改图片。capture只用于本待审包，不能覆盖此前获批PNG。原型用文件直接打开，无需服务、环境变量或新依赖。frontend-design用于父级状态分区和阅读层级，Playwright复用项目依赖验证。本目录HTML/CSS/JS、74PNG及清单均为永久设计交付。

## 审核边界

用户已[批准手机首次500失败白色区域](../../P34-MOBILE-FIRST-FAILURE-COMPOSITION-APPROVAL.md)：提示层级、说明文字、折叠追踪、底部重新加载组合，不含顶部、其他状态、整页或生产。采集清单的pending是批准前状态，不修改为整包获批。批准图固定SHA，后续不得覆盖；修改应另存修订图。手机权限拒绝白色区域被用户要求“语气温和一点”，已另存[四处措辞r2及12图](../../P34-PERMISSION-TONE-R2-REVIEW.md)，用户已单独批准r2语气，不能扩大为权限整区域通过；布局/按钮/焦点不变，原74图不改。此前灰色刷新按钮和浅蓝保留数据提示条的单独问题继续待答，没有被本轮代替。其他状态见图册，不能把一张通过推成17状态全通过。

未改生产源、API、SQL、鉴权、环境、依赖；无需重启，未部署。全73页真实C实施与生产验收仍未完成。
