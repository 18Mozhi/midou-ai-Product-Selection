# P18 自适应与长原因稿 · DETAIL-ADAPTIVE-C-r1

具体稿待用户审核。本包扩展已有[同一机会十分区连续稿](../detail-assembly-direction-c/README.md)，复用原数据与控制器，不重建组合壳层、不改旧图证据，也不替换生产 Vue。

共352张正式PNG：52个基础/扩展场景＋4个长窗操作区＋90个主题密度组合＋30个代表控件状态，分别1440×1000和390×844。全页图可高于视口，弹窗图保持视口尺寸。

- [打开交互提案](index.html)：顶部选择场景、三主题、两密度；挂起态可点“完成挂起演示”。这些仅为审核工具，不保存主题或用户偏好。
- [完整图集](gallery.html)：每幅图标明场景和1440/390视口；长原因弹窗另有操作区截图。
- [深色桌面](1440-overview-dark-comfortable.png)、[手机长原因失败](390-observe-dialog--long-error-footer.png)、[长复盘表单](390-feedback-long.png)。
- [来源/截图/检查清单](evidence.json)：以实际清单数量为准，不把图片数等同业务覆盖率。

## 本次设计变化

沿已选C保留蓝色范围目录、清晰身份和白色工作面；补8/12/20px圆角层次、深色与高对比语义色、可见按下反馈、两档间距与700/701px目录边界。明亮、深色、高对比只是离线审核名称，不映射或新增实际主题持久化ID。

十分区和四种可用原因弹窗逐一出三主题×两密度双端图；同一机会701仍是历史隔离样本。四种原因覆盖空值禁用、处理中只读、失败保留、结果未知锁写、1000字长原因及长原因失败；复盘补255字来源/1000字说明、非法退货量、忙碌态。长窗页头和底部操作区使用粘性定位，不把整个页面改成新业务弹窗。

十类代表控件以真实鼠标悬停、键盘焦点和鼠标按下采图；导航和关闭不强加busy/disabled。此代表性检查不等于OpportunityWorkspace全部源动作逐项映射；P18尚未进入P11那样的完整action-review侧表。

“完成挂起演示”只在实际产生本地请求意图且仍busy时启用；旧pending-elsewhere是静态状态示意，没有可完成的回调，因此按钮禁用，不伪造完成结果。

## 数据与权限边界

保留原M04-07机会701及其本身AI/利润响应，证据汇总1/明细0、无成本输入、缺血缘与复盘、无竞品/供应/成本权限均不补造。采纳所需质量门未提供，所以本包没有第五种“采纳”原因变体；核心稿的采纳提案仍独立，不能假称此处已串通。

所有提交只记录请求意图，不生成业务记录或模拟持久化成功。原型原因失败保留、未知锁写等仍是待审提案，不代表真实Vue修复。没有改变API、后端、权限、数据库、环境变量、依赖或生产服务，无需重启。

## 验证与使用

`node scripts/verify-ui-phase2-detail-adaptive-c.mjs --capture`生成本目录正式PNG/图集/证据；不带参数重新核对源/图指纹并执行浏览器检查。使用已有Playwright，退出时关闭浏览器，无测试服务器、HTTP、Cookie或浏览器存储写入。

检查所有出图状态的16px控件/13px辅助字、44px热区、唯一ID、整页横向溢出及不透明背景文字对比度至少4.5；对比检查不覆盖透明合成、边界线、读屏和完整无障碍。另检查768/1024/700/701/720px重排、长草稿跨十分区保留、query replace/from、四窗Tab/Shift+Tab与Escape、失败原因恢复、事实不变。720px重排不是实际浏览器200%缩放，未借此关闭缩放验收。

保留所有正式图稿与永久验证器；没有一次性临时产物。未完成：完整有数据/全权限成本与评分链、采纳链、全部源动作和动态变体映射、真实软键盘/读屏/缩放、跨机会/组织竞争、主站壳层/Vue/API/SQL/生产以及OP07–OP10。全73页目标和用户批准状态不变。
