# P47 已批空态与焦点整合接管 · 批次 17

状态：接管实际产品组件与必要样式依赖，非新增设计批准、整页验收或生产部署。此前用户批准的两种手机空态、详情反馈及桌面/手机默认布局仍按各自区域记录，全 73 页目标继续。

## 本批产品范围

依据 2026-09-14 的在途接管授权，将 `ProviderAdapterCenter.vue` 中已有的三处焦点修复、两种已批手机空态，以及 `provider-adapters-empty-mobile.css` 纳入本批。同步接管 C 页面/详情/反馈三份 CSS 的等值色板提取与 `design/provider-adapter-tokens.css`，避免遗漏实际生产导入依赖。

- 刷新：只有触发按钮持有焦点，才在禁用前把焦点移到本页常驻标题；原 `load()` 仍只调用一次。
- 清除筛选：保留原六项筛选重置，等 Vue 渲染后，仅在焦点落到 body 且本页搜索输入仍连接、可用、非 inert 时恢复到搜索；不覆盖用户后来移到其他控件的焦点。
- 分页：保持原 20 条分页和步进，只在持焦按钮即将进入首末页禁用边界时，先移到同一分页栏的常驻页码状态。中间页和其他控件持焦不抢焦点。
- 空态：不超过 760px 使用已批“尚无来源”和“筛选无结果”文案及浅灰区域/蓝色操作；宽屏原文案保留，隐藏副本文字不进入可访问名称。
- C 色板：原有页面、详情与反馈规则展开后保持有序 CSS AST 一致。新增手机空态颜色独立保留；作用域仍是当前挂载的 P47 页面，不改其他主题 ID 或偏好。

独立回归锁定 `883a533c` 中原业务脚本语句指纹：除三个焦点包装函数及必需导入外，读取/探针/筛选/排序/计数/超时/接口逻辑一致。没有修改 API、权限、数据库或真实探针行为。

## 本轮验证证据

- 两份独立测试共 32 项通过：`provider-adapter-pagination-focus.test.mjs`（15 项）、`ui-phase2-p47-takeover-contract.test.mjs`（17 项）。覆盖实际函数、Vue 模板编译、延迟渲染后的焦点归属、不可用目标、原脚本合同及三份 CSS 的有序等值检查。
- 工作区既有 `node scripts/verify-provider-adapter-pagination-focus.mjs`：四宽度、before/current 共 8 组 172 项通过；before 明确为旧焦点对照，不冒充当前结果。
- `node scripts/verify-provider-adapter-refresh-focus-current.mjs`：未替换的当前 App，四宽度、成功/失败共 8 组 152 项，40 个本地 GET、0 写请求。
- `node scripts/verify-provider-adapter-empty-mobile-current.mjs`：未替换的当前 App，四宽度、目录为空/搜索为空/组合筛选为空共 12 组 216 项，28 个本地 GET、0 写请求。
- 浏览器宽度均为 390/760/761/1440；三次共 540 项检查，当前组件 LF SHA-256 为 `24a3b27fca62f5331bd19c58ba501e2b6b9777d7e59a7b03248286d82a4962a2`。无页面错误、意外请求或新截图；旧图片及清单未覆盖。
- 产品文件本轮未再改字节：复用上一轮同一工作区成功的 `build:web`（含 Vue 类型检查）及 253 资源预算结果，不重复构建。本轮新增测试与文档另做格式/文档门检查。

三个历史/当前浏览器驱动的完整辅助依赖仍属于其他在途整理包，本批不将其全部暂存；这里如实记录当前工作区执行证据。新增独立测试不依赖这些历史重建辅助。局部绿色检查不替代尚未收口的全站测试、完整读屏、真机触控、真实权限或生产验收。

## 使用与收尾

使用入口不变：P47 来源适配器目录，刷新状态、清除筛选及上一页/下一页按原业务规则操作。没有新增配置、环境变量、依赖或可调业务参数；OpenAPI、后端消费者和数据库无需变更。

没有部署或重启。正式上线需随后续获准的本地前端构建包经既有宝塔流程发布；不能把本次 Git 接管当成生产更新。样式改值后须重新审核，不能直接刷新等值指纹使测试通过。

浏览器驱动均已退出，59184/59218/59239/59309 端口已关闭。本轮未创建一次性脚本、日志、导出或新截图；新增测试和本记录是永久交付。常规构建输出与依赖缓存保留，此前清理受阻的目录不重试删除。其他在途文件仍留在工作区。
