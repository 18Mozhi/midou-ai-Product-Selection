# P34 父级加载、刷新与拒绝链路验证

2026-09-10，基线 `d77ce47ba93ed91c2aaff4263f398e13cdfbfff9`。承接用户“空结果组合通过，继续其他状态”，本批补齐真实父页面状态证据；不扩大空结果批准范围，不实施仍待审核的顶部刷新视觉。

## 改动与使用

新增 `scripts/verify-ui-phase2-org-approvals-parent.mjs`、2项证据单测、[64张双端实图索引](P34-PARENT-READ-STATES-GALLERY.md)及来源/图片哈希清单。复用现有 Playwright、Vite 和组织后台 E2E 夹具，运行实际 App → NavigationShell → OrganizationAdminCenter → OrganizationApprovalPanel；不是只挂载子组件。

```powershell
node scripts/verify-ui-phase2-org-approvals-parent.mjs --smoke
node scripts/verify-ui-phase2-org-approvals-parent.mjs --capture
node scripts/verify-ui-phase2-org-approvals-parent.mjs
node --test tests/unit/ui-phase2-org-approvals-parent.test.mjs
```

smoke检查手机端500/403/网络失败，不生成文件；capture生成本批永久图和清单；默认模式重验当前源/已存图片哈希并重跑完整矩阵，不覆盖图片。不需要配置、凭证或新增依赖。服务探测空闲端口、仅绑定127.0.0.1；所有API由隔离夹具拦截，未匹配请求终止，finally关闭浏览器和Vite。

## 已证明的现有行为

| 阶段 | 当前行为 | 验证 |
| --- | --- | --- |
| 首次加载 | 不显示子页面，刷新按钮禁用 | 两个读取都受控等待 |
| 后台刷新 | 保留旧内容，刷新按钮禁用，模板筛选可编辑 | 成功后一起更新模板和摘要时间 |
| 首次读取失败 | 显示相应错误页和重新加载入口 | 不显示审批子页面 |
| 后台普通失败 | 保留旧模板和旧摘要，显示错误提示 | 成功的另一路读取不会单独覆盖页面 |
| 后台401/403 | 撤下审批子页面，显示失效/拒绝状态 | 不声称清除了内存数据；父标题和旧更新时间仍可能显示 |
| 恢复 | 重新加载或刷新后错误消失 | 模板查询及无关URL键保留 |

两宽度390/1440 × 两阶段首次/后台 × 两个失败目标summary/approvals × 七类500/503/409/429/401/403/网络失败，共56场景。503/429/网络失败按现有客户端安全GET规则尝试3次，其余1次；重试保持请求追踪关联，成功的另一路读取只有一次。另含首次加载、成功、后台刷新、刷新成功四状态各双端图，总64张。完整浏览器712项检查，smoke162项检查，2项哈希/矩阵单测通过；没有业务写入、外部/未匹配请求、页面脚本错误或虚构业务弹窗。

## 图与验收边界

frontend-design用于核对状态含义，Playwright用于真实父组件取证。当前实图仍含旧父级配色、布局和导航，不能当作C重设计交付或风格获批；已批准手机筛选/空结果局部与未改区域并存。错误中英文标记来自隔离夹具，不是新的产品文案。原90/244/150张C提案图及批准PNG不变。

本批不改变前端生产代码、按钮行为、接口、后端、数据库、权限、环境变量、依赖或上线配置。409仅人为响应验证既有客户端分类，不声称审批GET在生产会返回该业务冲突。没有真实服务中断、服务器鉴权/SQL、12秒超时、畸形响应、组织切换/KeepAlive生命周期、生产验证证据。

本批无需重启，未部署。输出目录 `output/playwright/p34-parent-read-states` 的64PNG及evidence.json是永久交付，不属于待删除临时文件。真实C父状态布局、顶部刷新批准、目录/详情的其余状态及全73页实施仍待继续，不宣称整页或项目完成。

提交前收尾：704/704完整单测通过；153文档/73路由与静态检查通过；原101包14995提案PNG审计零来源/图片漂移；审核入口桌面/手机通过并同步来源索引。未改生产源，沿用上批有效构建，不重复构建。隔离端口56054/56668已关闭，审核下载临时目录playwright-artifacts-eMlBih及单测临时fixture已自动删除，无本批遗留临时服务或临时文件。
