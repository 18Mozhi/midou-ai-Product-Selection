# P61 分批验证与提交收口 · 批次31

本批恢复29/30因既有测试失败而暂停的提交，不扩大设计批准范围。手机需核查区域、首次超时区域及其余图册继续待审；全73页重设计、生产接入和真实验收仍未完成。

## 测试阻碍的依据与修正

基线 main / 54438ad7，暂存区空，543项在途变化。实际 `PlatformDashboard.vue` 的load已有 `pending || disposed || !isDashboardRoute()` 三层前置保护；M06-02合同仍要求旧 `if (pending.value) return` 精确字符串。上轮失败不是实际保护缺失，本轮未修改首页生产代码。

本次将该合同改为核对实际三层保护，并在原Vue响应式测试中加入两项直接行为验证：并发直接load只发一个GET且只建一个计时器；离开首页后直接load不创建GET或计时器、不改变数据。原卸载后再次调用、晚响应、窗口切换、历史返回等9项继续保留。UI Skills路由选到Vue测试指南，但参考章节本地不可用，验证沿用项目既有真实Vue ref/reactive/watch与实际script执行方式，不安装新测试库。

完整运行以下本批相关组合，35项全部通过，无跳过或失败：

`node --test tests/m06-02/platform-dashboard.test.mjs tests/unit/ui-phase2-platform-window-sync.test.mjs tests/unit/platform-status-page-preview.test.mjs tests/unit/platform-status-read-feedback.test.mjs`

其中M06-02合同10项、首页窗口/保护11项、P61审核转换6项、P61读取反馈8项。没有删去保护断言、屏蔽失败或改写首页行为来换取通过。

## 收口范围

纳入批次29的P61四分区实际Vue审核宿主、CSS、原样例提取和审核验证；纳入批次30的实际首次超时/错误语义修正、读取验证和本批测试修正。审核组件与新C样式未接入生产。生产改动仅P61状态加载器的一处条件文案及PlatformManagementCenter中status专属的首次错误/忙状态呈现；其他domain逻辑不变。

图册保留原始捕获身份：默认r2/r3共46PNG、读取r1/r2共20PNG，共66PNG及各自manifest、index和README。默认r2及读取r1为明确已替代设计对照；默认r3与读取r2为各自区域当前审核入口。不得把66图合称66项已通过设计。

最新读取r2核对191来源、10图哈希全部匹配。上一批生产类型/前端构建与253资产预算、128项读取浏览器检查，以及98项默认组合回归均已有记录；本轮只改测试与文档，不重复执行未变的产品构建或15秒浏览器流程，也不将旧结果冒充本轮新运行。

Feature Map、计划、审核索引、P61规格及运维说明同步“测试阻碍解除”。旧29/30记录中的“未提交/既有失败未处理”保留为历史事实，由本批收口状态覆盖。API/OpenAPI、数据库、环境、权限、依赖及后端/Worker/Python/插件不适用变更，无生产重启或部署；未来正式Web发布后刷新加载文案修正。

## 归属与清理

仅暂存明确P61所有文件及两份本批测试，共享Feature Map/计划/索引只取29/30/31增量，其他既有工作区修改保持未暂存。不整体git add，不暂存真实环境、凭证或无关测试。截图来源指纹记录的是捕获工作树，不能据提交推断全部191项来源都是本批拥有或生产版本。

本轮没有创建临时文件、截图、服务或浏览器；正式图册保留。上轮 `output/playwright/p61-page-composition-r1` 四张不完整诊断图的清理被工具策略阻止，仍未重试或纳入提交；其余历史受阻目录未触碰。当前没有由本轮创建的长驻进程。

使用方式：默认组合 `node scripts/verify-status-page-preview.mjs`，读取链 `node scripts/verify-status-read-feedback.mjs`；均默认不输出文件，`--capture-review rN`需独占新版本目录。读取r2之后用r3、默认r3之后用r4，不能覆盖旧manifest。完整主题/密度/断点/真机/读屏、全部生命周期与真实运维权限/部署仍继续。

提交前收尾：文档、73路由/60受保护路由/6角色、153必需文件、运行文档及本批代码格式检查均通过；暂存区精确为98文件（17实现/测试/说明、3共享文档增量、78图册文件含66PNG），图像哈希逐一通过，诊断r1目录排除，cached diff检查通过。共享文档暂存内容与HEAD加29/30/31增量逐字比较，Feature Map移除这三键后与HEAD完全一致，不夹带其他在途修改。
