# P34 旧壳/样式与当前审核映射的来源闭环

日期：2026-09-14。接续查询归属修复后的六项来源门禁，不新增设计或扩大局部批准。

## 两项独立的共享源码差异

旧NavigationShell哈希对应`af239b08b69f7d97cd0372f9840a70009eeef0c7`中的文件。
当前文件只新增P57六个内部组件的分发排除与一行注释；旧signal-ledger样式对应
`7398aa8a6c38d498f888c64451c4345d4c726706`，后续`093d643b`新增四行
`.confirm-status`外观。两项均通过完整Git源字节比较确认，不凭注释推断相同。

新增`ui-phase2-org-approvals-shared-history.mjs`只在五个历史测试中精确去除上述两块，
再复用既有P34查询归属历史比较。任何缺失、重复、局部修改或额外变更仍失败。旧
manifest与图不改；新四项永久测试固定五份旧清单及其全部图片，并对比独立Git快照。
当前Vue、预览生成器与生产运行时都不导入历史还原，不声称共享变更对所有页面无影响。

## 审核映射不再混用两条来源

`build-ui-phase2-org-approvals-review.mjs`现在分别检查：

1. 四份旧视觉提案的组件依赖与固定历史Git源码一致。
2. 当前P34的三个实际组件与最新路由验证包的源哈希一致。
3. 当前Vue真实prop包含`owner-path=props.routePath`，动作、函数转发与字段仍按原契约校验。

生成器没有引用历史还原工具，也没有改写旧证据。其`sourceHashes`与结构依赖继续记录
当前真实源码；旧实施引用通过已有`asOfCommit`字段固定为正确提交，新增当前路由
证据单独列出。三个旧包完整源闭包均已逐文件比对所指Git提交，不只验证组件入口。
新增负向检查：任一当前组件变化、新哈希覆盖历史包，均拒绝生成。

重新生成P34动作清单，仍为29源位置、8个只读路由动作、1组转发、4组排除、25旧
控件变体、7历史提案控件、10字段/67状态、8字段组合与17旧父状态。未把计数改成
当前整页验收，也未增加业务写动作。补入用户已批准的手机实际Vue权限白区记录，
整体仍`pending-user-review`。旧过期的“权限仅文案通过”说明已纠正。

## 检查与复现

- `node --test tests/unit/ui-phase2-org-approvals-shared-history.test.mjs`：四项通过。
- 五个旧来源门禁定向通过；审核prop与双来源隔离两项定向通过。
- `node scripts/build-ui-phase2-org-approvals-review.mjs --check`：应与当前P34.json一致。
- 当前查询、路由与其他历史测试仍分别运行，不把旧图直接视为当前浏览器通过。

完整关联21文件97项全部通过；此前审核文件在初始化即失败，这轮其6项检查均实际
执行。动作清单`--check`、153文件文档、73路由/60受保护/6角色、运行文档和本轮
9个代码文件格式检查通过。Vue/CSS未变，复用上轮有效类型/浏览器结果，没有重跑
无变更的截图矩阵。尚未覆盖全73页、全部新断点/按钮/弹窗、
组织切换或真实权限、生产部署；当前2宽矩阵不能代替这些范围。

## 运行与产物

本轮未改Vue/CSS、API/OpenAPI、权限、数据、环境变量或依赖，无生产重启/部署。
旧图原样保留，不重复出图或请求相同组合审核。新增历史工具、永久测试与本文为
交付文件；未创建临时图片、文件、浏览器或服务，只有已明确启动的本地测试命令。
既有大量未提交的跨页设计和预览材料必须与本轮改动区分，不能整体暂存为P34提交。

## 提交边界需要确认

最新路由包202个源输入中有43项尚未提交，包括本页预览，也包括NavigationShell、
PlatformDataCenter、PlatformManagementCenter/Filter、PlatformMessageEditor/Workbench、
ProviderAdapterCenter、ResponsiveDataView/FilterDrawer及平台样式。它们在本轮开始前
已经存在，不能把整个依赖集当作本轮独立修改随意提交。

只提交当前映射会留下依赖于未提交输入的证据；整体纳入则涉及其他页面的现有改动。
因此暂存区保持为空，commit hash不适用，等待用户确认是否允许核查并将这批跨页
依赖与P34一起形成阶段检查点。此确认不授权部署或扩大页面批准。测试已结束，
无新临时产物、浏览器或服务；所有正式旧图保持原样。
