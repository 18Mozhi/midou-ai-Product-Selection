# P37 分页响应归属 · 实际 Vue 局部修复

2026-09-11；main / c380b995b3a6d55d7f1742dc42baf9479be0e8e7 起点。保留此前 P36/P37 未提交工作。本批仅关闭已复现的旧分页响应覆盖新读取结果问题，不代表完整 C 设计、真实权限或生产验收。

[40张当前实图](../../output/playwright/p37-parent-read-vue-r2/index.html) · [192项当前双端检查](../../output/playwright/p37-parent-read-vue-r2/evidence.json) · [逐项来源关联记录](P37-PAGINATION-SOURCE-ASSOCIATIONS.json)

## 改了什么

OrganizationAdminCenter.vue 仅在 loadAuditPage 内新增14行：记录当前读取代次、data对象、组织和路径；分页成功或失败返回时先判断是否仍属于该上下文。失效响应不能追加旧行、覆盖新状态或错误提示；非API程序错误继续抛出。finally仍释放原busy状态，没有新增取消请求、提前解锁、超时或重试。

当前正常分页仍50→55条、按原cursor请求且等待时不重复提交；普通筛选替换首页及当前分页失败处理均保持。模板、其他父函数、API客户端与后端未改。严格逆向移除这三处新增段后，与起点父组件逐字一致。

## 依据与验证

历史真实父子宿主曾复现：刷新已返回10条，旧页5条迟到，错误追加成15条。本批先5项实际函数直接回归，再手机96项最小浏览器验证和390/1440双端192项检查，保存40PNG。当前宿主不替换任何SFC，使用真实Vue父子、API客户端、Router及KeepAlive；测试数据来自既有合成夹具，仅允许隔离本地audit-events GET，其他请求失败关闭，不调用真实后端、生产、写接口或系统剪贴板。

六种交错覆盖旧页200/403/500、刷新200/500/403，以及旧响应先到/后到。MutationObserver同时检查中途行数，避免仅最终结果正确而曾短暂插入旧行。新成功保持10条，刷新500保留50条及错误提示，刷新403隐藏内容；旧分页结果均不覆盖。复制反馈修复亦在当前父链复验。当前组织/路径变化的函数级保护有直接测试，但不据此宣称跨组织完整生命周期通过。

本批另有3项证据测试，核对14行精准增量、当前源/图/请求与历史清单保留。第一次浏览器检查定位沿用“加载更多记录”，但等待时真实名称变为“正在加载…”；仅修正测试定位器后复验，没有改产品按钮。测试最初误写15行已按真实diff纠正为14行。

## 历史证据处理

新journal记录40份清单、5358张原PNG的逐字段前后值及原/current hash；不重写PNG，不改变审批、图中内容或历史结论。仅同步精确源关联和依赖清单hash，绝不把旧图当新运行证明。P34首次失败、P36父读取和P37旧父读取三份历史证据冻结保留；旧P37验证明确还原固定父子源，只用于回放原问题。当前实现必须用r2命令验证。

P34限流精确增量测试先严格移除独立P37增量，P35历史清单测试先验证并撤回journal层，再核对原P35变更；未删除断言或放宽原差异。共享壳层辅助父源hash同步，但70入口、语义、审批和验收状态不变。

## 使用与复验

```powershell
node --test tests/unit/ui-phase2-org-audit-pagination.test.mjs tests/unit/ui-phase2-org-audit-pagination-evidence.test.mjs
node scripts/verify-ui-phase2-org-audit-read-r2.mjs
node scripts/refresh-ui-phase2-audit-page-bindings.mjs
```

无参数运行不重拍或覆盖图；只有明确更新当前交付图时使用r2的`--capture`。旧token-filter和audit-copy关联命令仍可无参数校验历史层，但存在分页journal时其`--write`失败关闭，避免旧写入器撤销新关联；更新使用最新audit-page关联命令。没有新增可调环境变量、CLI业务参数或服务。

## 未改与未完成

URL同实例反向恢复、KeepAlive返回恢复及已编辑/已确认筛选区分仍待处理。读取离开再回到相同组织/路径且没有新load的A→B→A归属不能由本次局部保护证明。没有修改P35直接色值、其他页面生产逻辑、数据库/迁移、OpenAPI、权限、依赖或.env；feature map仅登记当前证据边界。

P37完整C、未答设计组合、全部73页实施/部署/审核仍未完成。既有P35全局颜色门失败及修复授权待答，不重复未变的失败测试，不提交或部署混合工作树；commit hash不适用。当前仅本地变更，无生产重启；以后正式发布仍按既定宝塔流程。

40PNG和index/evidence共42文件是永久交付，保留在`output/playwright/p37-parent-read-vue-r2/`。未创建临时脚本、下载或调试日志；验证器finally关闭浏览器与隔离服务，既有依赖缓存和项目正常构建输出保留。

最终复核：第二阶段UI测试562/562通过（最初559/562，三处历史关联断言按上述精确分层修复后全量复验）；当前父链192、复制62、历史父链116及P36父链178项均通过各自声明范围。Web类型与生产前端构建、73路由、153必需文档及运行文档检查通过。102主图包/15069PNG来源与图hash无漂移，整页批准仍0；三个新旧关联检查均通过，未重写图。格式检查通过，git diff --check通过。没有运行真实后端/生产/完整权限或全项目所有门禁。

本次构建更新正常项目产物`apps/web/dist/`与`apps/web/public/browser-helper/scoutops-browser-helper.zip`，不作为临时文件删除。隔离端口52533/52738/52937/53544/53859/53888/53938在收尾检查中均无监听，不终止其他任务进程。
