# P36 令牌记录与分页 · 实际 Vue 的 C 审核组合

从干净 `main/7e812f14` 继续。创建组合和一次性明文组合仍待审核；本批补齐列表结果区，不转授已有手机筛选批准。

[60 张四宽度图册](../../output/playwright/p36-list-vue-preview/index.html) · [手机正常记录](../../output/playwright/p36-list-vue-preview/390-active-record.png) · [桌面正常记录](../../output/playwright/p36-list-vue-preview/1440-active-record.png) · [证据](../../output/playwright/p36-list-vue-preview/evidence.json)

## 设计及本次审图

使用 `frontend-design` 将旧双列小卡改为纵向目录记录：名称/状态/前缀/范围组成身份区，三项时间独立成组，技术详情及两个操作位于底部。桌面时间三列与身份区并排，手机时间按标签/值排列；标题18px、普通时间和操作16px、辅助13px起，控件至少44px。真实状态保留文字，活动为绿、临期提醒为暖色，历史和未知状态为中性色。

筛选保持原节点和指令，不新增字段或覆盖已批筛选样式。结果标题移到筛选下方，避免计数与条件混排；本批结果图不包含筛选。审核宿主工作台暂改上下全宽排列，不能称为整页布局通过或筛选区域像素完全不变。

仅询问 **390px 单条正常使用记录的默认组合**：名称/状态、读取范围、三时间、技术详情入口和轮换/撤销按钮。其他状态、完整列表、原因窗、新创建与明文、权限/生产验收均不由该回答推定通过。

## 实现边界

`scripts/lib/ui-phase2-token-list-preview.mjs` 用 Vue AST 定位唯一 ledger/list/article，保留整个 scriptSetup、所有 directive/native 控件、原循环/状态条件/字段表达式；只重排标题、身份、时间与结果容器。未改变六条分页、排序、查询、技术详情原生展开及业务 handler。

`implementation/token-list-preview.css` 仅审核宿主加载。旧 desktop 创建表单 `position:sticky` 在上下预览排列中遮住列表，已在 `.p36-list-preview` 下改为 static，并增加每张截图三个可见采样点的遮挡检查；不修改生产创建布局。最初被遮挡的桌面图已在同一路径完整重拍，不保留为交付证据。

## 验证及样例

390 / 760 / 761 / 1440 四宽度，**264 检查、39 项源指纹、60 PNG**。15 状态包括：第一页、第二页、无匹配、正常记录、展开技术详情、两个操作的焦点/悬停/按下、等待禁用、组织无令牌、已过期、未知状态/范围和长名称。

- 从现有 E2E 的 `organizationTokens` 初始化表达式提取原8条测试记录，固定审核时间2026-08-26，保留原排序、6/2分页、从未调用、活动/轮换/撤销状态。不是生产数据或当前日期。
- 空数组、已过期克隆（到期时间改为审核时钟之前）与未知状态/范围/长名称克隆为**补充合成响应**，不冒称后端真实返回、长度策略或允许未知授权。未知状态继续用原文字降级且不出现写入按钮。
- 实际父子 Vue 初始 GET、分页/筛选不增加请求；刷新禁用活动记录操作，技术 ID 默认隐藏、原生展开后完整显示。
- 轮换/撤销点击打开真实父原因窗，核对目标名称和空原因，Escape 取消并恢复触发焦点；每宽度总计10个精确 GET，无 POST。未提交轮换/撤销、未做幂等/事务/审计写入验收。原原因窗本次不重设计或作为 C 图获批。
- 所有图检查横向溢出及可见部分遮挡，控件真实 Tab 焦点、原生按下及等待禁用；窄视口鼠标 hover 不等于触屏 hover。系统剪贴板拒绝调用，无外部/非预期请求、浏览器错误或 storage。
- 原原因窗使用 aria-label；初版验证器错误查找 aria-labelledby，已按真实源码修正，产品弹窗没有改动。

原 OG-G05 到期负零行为、完整App/主题/密度/缩放、真实授权、跨组织、写入生命周期和全73页实际C/宝塔交付仍未完成。

## 使用与收尾

```powershell
node scripts/verify-ui-phase2-token-list-preview.mjs
node --test tests/unit/ui-phase2-token-list-preview.test.mjs
# 仅需更新本批图片时添加 --capture；普通复测不写图片。
```

生产 Vue、CSS、API/OpenAPI、后端/数据库、环境、依赖和权限均未修改，不需要配置或重启，本批未部署。图册的60PNG及index/evidence是正式交付物；各轮同名中间截图被最终成功回放覆盖，无额外临时文件。Vite/浏览器 finally 关闭，验证端口57777/57860/57959/58031结束。

最终验证：4项新增定向测试及687项相关UI/归属/组件边界回归全部通过；73路由、153必需文档、样式门、diff空白检查通过。原102图包/15069PNG审计无漂移，新60图独立登记，不增加已批准页面计数。生产代码/配置/依赖未变，复用此前有效构建结果，不重复生产构建。
