# P47 历史图证与当前实现分层

从main/af239b08、457项已有变更、空暂存继续。上一轮审核入口补充是有效进展；本轮处理
仍阻止整体提交/部署的测试门禁，不重新设计已批准区域。全73页与真实验收目标不缩减。
先读项目总纲/Feature Map和实际测试，按UI技能保留焦点与实际界面的独立约束。

## 原因与边界

手机空态实施把P47组件推进到51c0ba阶段，色板由原37项增加5项局部角色。旧刷新8图、
两包空态各30图及两包P46邻页回归仍捕获于2f71a8或ea3eaec阶段，原断言把它们误当当前
完整源码。当前diff逐文件确认只有P47组件/色板不匹配；不据此判断其他失败全是历史问题。

新增仅供测试的精确阶段解析器：pre-mobile先调用既有完整手机逆向校验，pre-refresh再
调用既有完整刷新逆向校验。色板只接受已核对的完整新/旧SHA，精确删除五行手机色角色，
其余37项及全文必须还原为原SHA。只处理这两个明确文件，其余源码仍逐字核对。
未知阶段、业务/焦点/文案/标记/其他颜色漂移继续失败，不使用通配替换或哈希重绑。

旧测试明确改称历史/拍摄阶段，五个原manifest加完整固定指纹；保留全部来源清单、
图片哈希、尺寸、检查结果、请求、交互与基线负例断言。解析器不导入生产、不改变原
逆向文件或浏览器验证驱动。当前焦点VM测试仍读取原始当前组件；实际手机实施96图/
179来源/32严格比较和当前刷新入口继续单独验证，不被历史适配抵扣。

原图、原manifest、审核入口的历史标记和用户批准不改；旧图通过版本完整性检查不等于
当前像素、全页、真实权限、探针或生产验收通过。没有以图数或测试数替代全站交付。

## 已执行验证

首轮完整249文件1440项：1261通过/179失败，0取消/跳过，314601.7142ms，exit1。
结果保存至[P47-HISTORICAL-GATE-UNIT-RESULT.json](P47-HISTORICAL-GATE-UNIT-RESULT.json)，
含全部失败名称和位置；多行错误正文未保留，不把摘要写成完整堆栈。

新增阶段正反例4项，加原空态8、刷新8、色板证据6，共26项定向通过；随后当前手机3项
和当前刷新入口5项通过。检查生产目录/脚本引用，阶段解析器仅被上述永久测试使用。
当前浏览器152检查结果与96图手机实施包仍适用：生产源码、样式、依赖、驱动均未改，
本轮不重复截图或运行相同浏览器回归，也不生成新的视觉批准问题。

最终完整250文件1444项：1274通过/170失败，0取消/跳过，312560.8697ms，exit1。
同一JSON保留两轮全部失败名称/位置和对照；按去序号名称+位置比较，消除9项、无新增。
9项包括两P46旧图源、五P47旧空态边界、两P47旧刷新边界；不宣称其余170项都是历史问题。
文档153文件/73路由/60受保护/6角色、运行说明、5代码格式、定向diff和审核索引check通过。
索引仍显示3个历史包/各2处P47差异；没有用测试通过把它们重新标为当前。结果JSON按既有
格式规范排版，不改数据。全库仍失败，不提交部署。
未改Vue/CSS、业务/API/OpenAPI、权限、数据库、环境变量、运行配置或依赖，无生产重启。

## 使用与收尾

```text
node --test tests/unit/ui-phase2-adapter-historical-source.test.mjs tests/unit/ui-phase2-provider-adapter-empty.test.mjs tests/unit/ui-phase2-provider-adapter-refresh-focus.test.mjs tests/unit/ui-current-palette-evidence.test.mjs
node --test tests/unit/ui-phase2-provider-adapter-empty-mobile.test.mjs tests/unit/ui-adapter-refresh-focus-current-runner.test.mjs
```

新解析器只用于历史证据测试，不能用于替换当前源码或重拍历史图。当前浏览器刷新与手机
验证入口继续按runbook执行，审核台仍如实标出3个历史图包的来源变化。
无新临时脚本、截图、日志或服务；新增永久测试/解析器/结果为交付物。首轮全库进程
55452/26564已退出，临时scoutops-code-style-tMz1nd目录已确认不存在。
最终进程28012/45620也已退出，scoutops-code-style-p5BCbT不存在；没有新增浏览器或后台服务。
工作区461项变更、暂存仍为空；新结果/解析器/永久测试是交付物。下一核对P44捕获来源：
现有目录/控件current包各40来源，旧测试分别写死37/36；先逐项确认来源和版本，不直接改数。
全库仍失败时不提交部署；commit hash不适用。全73页及未答具体设计审核仍继续。

旧策略阻止清理的两目录保留、不绕过重试：
`D:/项目工程文件/vue/curson/工具/智能选品/output/playwright/p47-c-e2e-temp`；
`C:/Users/23136/AppData/Local/Temp/scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`。
