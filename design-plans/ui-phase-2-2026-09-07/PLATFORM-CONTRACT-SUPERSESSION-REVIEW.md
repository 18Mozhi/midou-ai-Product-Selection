# 平台账号当前合同：共享控件的明确接续

2026-09-12，main / af239b08。上一批 P58 重放完成后继续处理当前静态合同；本轮不改运行
组件、业务/API、权限、配置、依赖或审核图片，不把局部登记当作全站重构或生产验收。

## 已复现与方向纠正

原独立验证器首先失败于候选身份集合。实际历史表为 `Q#beb5f8d5846aa028.1`，当前扫描器
读带 `:aria-controls="panelId"` 的触发按钮，得到 `Q#7e0fa28eaeb1cc09.1`。只在内存移除
该属性便恢复历史签名。此前 FILTER-DRAWER-ASSOCIATION-REVIEW.md 末段误把 actual/expected
方向读反；本记录和当前合同明确纠正，原历史表及原记录不覆盖。

使用 ui-skills-root / fixing-accessibility，核对 ARIA 展开关联、键盘与减少动态效果边界。
技能 CLI 不可用，不安装依赖；本批验证的是已实施修复的静态接续，不重做浏览器修复。

## 修改验证器与合同

- Q 六个历史候选固定核对，只允许一条明确登记接续打开按钮；其余五项原样核对。
- 当前触发器必须包含准确关联属性，且仅移除它就能重建历史签名；修改点击处理器或其他
  按钮不能伪装成关联修复。原128候选和24模型分母不变。
- 32 源历史表保持。当前显式修订从五处增为七处，新增已存在的 use-modal-dialog 和
  NavigationShell 修订登记；其余未登记来源仍严格核对原指纹。
- 两个共享浮层的原色提取、P44 两个手机 CSS 的同类提取，实际导入两份调色板；当前
  原始来源从36增为38，不能漏掉被 PostCSS 折入所有者的样式依赖。
- 所有当前哈希均为真实原始源码的 LF SHA-256。共享详情旧补充表 b9e635a3 不改；之后
  P55/P56 已加入 appearance 外观，颜色提取前的 af239b08 中间版为6d3088d1。验证器只
  展开既有变量、移除精确 import，完整文本必须回到该中间版，不误称回到旧焦点版。

具体登记及复验入口见 [当前合同](platform-account-current-contract.md)。原始历史、当前
原文与可逆增量各自核对，不能用生成后的代码哈希冒充原始文件或随意接受旧版本。

## 来源差异依据

| 来源 | 实际既有差异 | 本轮没有实施的事情 |
| --- | --- | --- |
| ResponsiveFilterDrawer | P55/P56 外观、通知样式接入、原色提取、useId 关联及 scoped reduced-motion 首焦点修复 | 不新增 props 或改开关/筛选行为 |
| ResponsiveDataView | P55/P56 appearance/class 与原色提取，五个候选不变 | 不改选择、返焦、缓存停用或业务内容 |
| use-modal-dialog | 635e5538 仅增加/返回 discardReturnFocus，已有 CredentialAssetCenter 调用 | 不把此新增能力认定为全部模态已修复 |
| NavigationShell | 当前 diff 仅六个 P57 内部 Vue 的 glob 排除及说明注释，仍由父页面导入 | 不改路由、网络、权限或缓存 |
| P44 两份 CSS/两份调色板 | 已有颜色角色集中，关联图包已实际重放 | 不改原色、样式范围或批准区域 |

已逐项读取当前源码、Git 差异/635e5538 提交、原合同、颜色收口及浏览器关联报告；没有
从候选集合失败直接推断全部来源安全。补齐 Q 后暴露其他过期来源，第一次定向测试因此
仍25过/4失败，继续定位到 P44 两个 CSS 的真实新依赖后完整复测。

## 最小验证

`node scripts/verify-ui-phase2-platform-account-contract.mjs`：8页、128候选、24模型、38源、
32历史源、7显式修订、83本地链接通过。

`node --test tests/unit/ui-phase2-platform-current-contract.test.mjs tests/unit/ui-current-palette-evidence.test.mjs`：
36/36通过，其中当前合同30项、P44/P46现有六包来源/图片绑定6项。新增反例覆盖篡改历史、
缺失/重复/错误替代、复用旧身份、错误关联、额外点击行为、其他候选漂移、漏掉调色板与
调色板原值变化。原绑定、页面章节、链接、CRLF和依赖漂移门保留。

组件和浏览器输入本轮未变，复用已记录八组实际 Vue/16键盘场景，以及 P44/P46、P47、
P58 重放的对应当前来源证据；没有宣称本轮重新运行所有浏览器或新增视觉批准。

## 完整门禁与收尾

最小验证通过后运行完整 Node 单测（240 个 tests/unit 文件，沿用未变的既有构建）：1388项，
1218通过、170失败、0取消/跳过，283.98秒，退出1。并非全库通过，不自动提交或部署，
commit hash 不适用。上一份完整快照是1379项/1180通过/199失败；本次新增9项检查。
本轮完整失败名称的工具回传被截断，因此不声称逐名证明“没有新增失败”，也不把失败减少
推断为所有未通过项都是历史哈希。后续从实际失败的共享详情/原生模态历史与当前证据开始定位。

文档与运行文档检查通过（73路由、60保护、6角色、153必需文档），git diff --check通过；
最终格式检查通过（changed=113、production=535、repositories=51）。本批没有手工临时文件、图片或服务。全库格式测试自己的临时
夹具 `C:\Users\23136\AppData\Local\Temp\scoutops-code-style-H7XBGO` 已自动清理，收尾复核
不存在；父子测试进程34424/58800均退出，暂存区为空，未混合暂存其他变更。

之前被工具策略拒绝删除的两处仍待用户手动清理，本批不重试绕过：

- `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p47-c-e2e-temp`。
- `C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`。

无 API/OpenAPI、运行时参数、环境变量、数据库、权限或依赖变更，后台/Worker/Python 不涉及，
生产无需重启；本批不部署。原待审页面及未知创建结果规则保持待定，全73页目标继续。
