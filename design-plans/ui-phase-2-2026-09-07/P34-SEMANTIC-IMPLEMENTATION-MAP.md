# P34 审批治理 · 完整实施对应表

接续实施：手机首次429已按[P34-RATE-LIMIT-VUE-REVIEW](P34-RATE-LIMIT-VUE-REVIEW.md)接入，新增第二条原load转发。当前29源码入口、8本页动作/1接线组/4排除组不变；500旧证据固定于d2d566c2，当前500未变由新429四断点基线对比证明。

2026-09-10，main / 2841885e起点。依据蓝图M06-01、真实路由与父子组件、F04合同及现有C图；不是新的视觉批准或生产验收。

## 阅读任务与设计语言

本页是组织范围的只读观察，不是P25审批工作台。C方向用蓝色范围/阅读导航与白色内容面分开审批记录和模板差异；已获批的手机筛选、空结果及首次500白色区域局部进入Vue。其余C稿继续待实施、逐区域审核。`frontend-design`用于保持这一设计方向，不用旧界面外观替代全新设计目标。

真实路径：`/org-admin/approvals` → `OrganizationAdminCenter` → `OrganizationApprovalPanel`；首次无数据500另接`OrganizationApprovalFirstFailure`。父summary与approvals两读取保留原权限；子接收六个数据/格式化props，没有业务事件或busy prop。刷新期间普通失败保留内容、401/403撤下内容；服务端权限不能由菜单或本登记替代。

## 按动作实施与核对

| 动作 | 当前真实入口 | 对应图/实施边界 |
| --- | --- | --- |
| 刷新 | 父刷新按钮，loading/refreshing禁用 | 旧控件六态与新父17状态；顶部布局未通过 |
| 重新加载 | 父原按钮、新F手机500按钮 | 同一load；F只emit，父一条显式转发；不计第三个业务动作 |
| 记录/模板切换 | 两个原生按钮 | 独立筛选及URL保留，图中选中/未选不是四个业务动作 |
| 审批筛选/重置/分页 | 五字段、重置、上一/下一页 | 8行分页，最近100条与summary总量分开；空结果清除为提案新增 |
| 模板筛选/重置/分页 | 五字段、重置、上一/下一页、手机空结果清除 | 6行分页；手机筛选和空结果已实施，桌面新增清除未实施 |
| 模板选择 | 目录按钮 | 归档仍可读；详情可能跨页，不臆造新查询或当前页自动选中 |
| 技术信息 | 审批details、模板details、F请求追踪 | 原生折叠；没有复制动作或弹窗；F只有真实编号非空时出现 |
| 审批/审计导航 | 两个RouterLink | 固定入口，不切工作区、不带审批ID，不承诺精准直达 |

[机器登记](action-reviews/P34.json)覆盖29源码入口，13语义组＝8本页动作＋1接线组（500/429两转发）＋4排除组，写入动作0。父资料、成员、授权、其他页原因发起均排除。登记完成不代表29个按钮，也不代表P34整体已完成。

## 字段、结构和弹窗

10个本页v-model分别登记全部67字段状态、8双端组合。父资料另外6个模型明确排除，不算本页16字段。搜索没有输入maxlength；200是URL恢复截断。模板updated_desc按版本号排序；工作区按名称去重，不能悄悄改为ID或按更新时间。

结构扫描记录父资料form、共享AuditedReasonDialog与子只读说明aside。前两个没有P34发起路径，aside是持续说明而非抽屉；模板详情article、技术details也不是弹窗。**P34业务弹窗为0**；跨页已经打开的共享原因窗生命周期仍另验，不以“本页没有入口”证明所有时序均不存在窗口。

## 全套已有图与批准边界

- [页面45场景/90图](design/org-approvals-direction-c/README.md)：两个阅读视图、列表和版本差异。
- [32控件变体/244图](design/org-approvals-controls-direction-c/gallery.html)：25已有控件变体，7条原始提案新增项，不能都算已实现。原模板空结果清除提案另有手机实施绑定，原提案标记和图保持不变。
- [10字段/67状态及8组合/150图](design/org-approvals-fields-direction-c/gallery.html)：使用精确binding、selector、状态及视口关联，不用整页图代替字段证据。
- [17父状态/74图](design/org-approvals-parent-direction-c/gallery.html)：首次/后台、替换/保留分别核对；409是人工响应分类，不是新GET契约。
- [权限措辞r2](P34-PERMISSION-TONE-R2-APPROVAL.md)仅语气通过，未实施；[手机限流白区](P34-MOBILE-RATE-LIMIT-COMPOSITION-APPROVAL.md)仅组合通过，已局部接入真实Vue而未部署。网络白区问题继续待答，不重复询问。

每张原图保留。代表六态只对应指定控件，不扩大到新F、所有变体或完整适用性。手机返回目录/筛选折叠等提案不能被登记成现有功能。

## 实施顺序与验证

下一实施队列：父级其余状态/顶部审核与接入 → 记录阅读区和模板目录/详情 → 完整P34双端、缩放/键盘/历史与组织切换 → 后端权限、真实数据和宝塔上线验证。已批准限流白区本轮完成局部接入。维持既有读写/权限/URL合同；需要改变业务规则时仍须单独确认。

本批只新增可执行对应表、校验器与回归测试，未改运行组件、路由、API/OpenAPI、SQL、环境变量或依赖，不需重启，未部署。运行：

```powershell
node scripts/build-ui-phase2-org-approvals-review.mjs --check
node --test tests/unit/ui-phase2-org-approvals-review.test.mjs
node scripts/audit-ui-phase2-action-coverage.mjs
```

校验器拒绝漏源、错接线、遗漏控件、错selector、错字段状态、漏视口、手机提案扩大到桌面、错误保留权限内容以及整页批准提升。旧图和已有真实Vue证据仍分别保留各自验证边界。

验证结果：5项定向测试通过；除上一轮已通过且未变更的全库格式门用例外，其余单元测试全量重跑通过，新脚本/测试另经Prettier检查。总动作登记35/73页，整页批准仍0；102图包/15069PNG零漂移。文档153项、静态检查391文件、运行文档一致性通过；审核入口1440/390双端的筛选、焦点、来源/导出、无溢出与零页面错误通过。未重跑未改变的生产构建或真实服务测试，未宣称生产验证。审核临时服务和浏览器已关闭，临时下载目录已确认移除；新表、JSON、脚本和测试为永久交付，没有新增图片或遗留临时文件。
