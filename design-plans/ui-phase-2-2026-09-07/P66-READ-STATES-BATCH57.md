# P66 读取状态与命名区域 · 批次57

起点main/6025c198、610项在途变化、索引为空。上一批焦点修复和验证属于实质进展；本批补读取状态整组，不把继续指令当作图稿批准，完整73页目标保持。

## 依据与改动

按AGENTS→Feature Map runtimeTopology→P66规格/总纲M08-01定位实际Vue、读取客户端合同和审核转换器。原loading、首次错误和保留刷新失败使用b标题，section没有关联名称或busy状态。本批按fixing-accessibility技能将其改为关联h3标题的命名区域，动态aria-busy沿用refreshing；实际C版在h1之后使用h2，不跳级。原aria-live、所有文案、条件、请求/重试、15秒超时、401/403清除、429/依赖失败保留及追踪/焦点逻辑均未改变。

生产Vue仅上述模板语义，生产CSS保留对应18px标题和零默认外边距。审核版移除旧装饰性惊叹号/脉冲点，采用白色区域、蓝色左边线、明确标题/说明/追踪与底部操作；手机单列全宽主操作，桌面保留失败提示为说明/操作双列。并修正P66审核主色边框变量，去除从旧外壳继承的橙色边线；不改变生产配色或其他页面。

## 验证

- 三项区域语义/审核层级测试先失败后通过，再补主色边框约束；4项新增加原P66/客户端/M08-01合同共53项通过。C模板编译及与当前生产script逐字等值通过。
- `node scripts/verify-topology-read-states.mjs`使用当前实际App，1440/390×减少/正常动效。初稿432检查通过；人工看图发现蓝按钮的橙色边框后修正审核变量，修订稿436检查/84本地GET通过。
- 逐一核对初次loading及命名区域、真实15秒超时、不冒充保留旧事实；首次503/429按现有安全重试各3次，403/401各1次；403原重试按钮仍存在，401仍指向/login而不实际跳转。成功恢复原E2E快照，不重算结论；等待时保留事实且防重；第二次真实15秒超时、503/429保留快照及独立失败编号；403/401清除事实；最后恢复empty时只有节点为空，不将整份运行事实清空。
- 原E2E只是部分数据，保留其队列汇总不一致等既有内容；empty变体明确为本地显示样例。没有执行真实服务/探针/调度/SQL/权限审计，不用样例证明单机资格。
- 成功快照与失败追踪分离、复制成功/拒绝为页面内替身；无真实剪贴板写入。截图前等待字体与双帧，所有宽度/动效组均检查标题、文案、编号与控件的视口边界和中心命中，不仅出图组执行。超时没有加速时钟；实际请求数21/组含共享重试。无外部请求、业务写入、下载或页面异常。
- 最终焦点112/28GET、追踪184/40GET、默认304/16GET回归通过；加本批436/84GET，共1036项浏览器检查/168GET。类型/生产构建及253资源预算通过；随后仅审核边框变量与相应测试变化，不重复生产构建。格式检查通过。

## 审核图

当前 `output/playwright/p66-read-states-r2` 共34PNG/167来源指纹。每端17图：首次loading/超时/503/429/403/401、恢复原快照、保留等待/超时/快照追踪/复制成功/复制失败/503/429、清除后的403/401、恢复empty。减少动效组截图，正常动效同样验证；index.html为入口，manifest绑定来源和图像。

`p66-read-states-r1`为修订前正式设计迭代（同为34图），保留原manifest作历史对照，不作为当前稿；不是验证专用临时文件，不覆盖。r2除边框外还会有新的本地请求UUID，不能宣称两版仅一个像素差异。更早批54-56图也不重绑当前源码。

本次展示r2手机initial-timeout，提请仅审核白色区域的蓝色左边线、标题说明、独立追踪和底部主操作。未回复即待审，不包含其他错误状态、完整读屏、真实权限或生产；前述各局部批准和待审范围不变。

## 使用与闭环

点击原刷新/重新核验，等待时顶部保持焦点且防重；可展开对应追踪或复制。无参数命令复验不写文件，新正式图可加`--capture-review rN`，必须新目录，不覆盖。

同步Feature Map、P66规格和运维手册。没有API/环境变量/配置/依赖/数据库/业务规则/权限/超时或离页政策变化，OpenAPI无需改，后端/Worker/Python消费者无联动。无单独后端重启要求；未来发布仍须宝塔整体流程，本批未部署/重启。

本批没有验证专用临时文件，图册、永久测试/验证器保留；已有构建输出为项目必需产物。测试服务与浏览器按finally关闭并收尾复核。前批`output/playwright/p65-read-states-r1`和`output/playwright/p65-table-controls-r1`仍因工具策略不能清理，未绕过。

既有HEAD API覆盖225/258与旧223/256断言问题输入未变、处理决定未补，不重复失败、不暂存/提交，commit hash不适用。P66运行结论矩阵/完整告警与策略、保活、主题密度、完整读屏、真实鉴权审计和全73页实施验收继续。

收尾复核：r2的34图/167来源SHA及r1历史34图SHA均匹配，未重绑旧来源；Feature Map解析、格式和本批diff空白检查通过。相对批56只有RuntimeTopologyCenter与runtime-topology.css两个生产源文件变化。九个本轮端口及相关测试/浏览器/构建进程无残留，索引为空，613项在途变化未暂存；两个前批受阻临时目录仍存在。
