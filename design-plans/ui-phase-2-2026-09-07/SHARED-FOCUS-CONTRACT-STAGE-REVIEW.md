# 共享详情焦点包与当前颜色接续

## 范围

全73页 C 重设计目标继续。本批只改独立焦点验证与其公共合同辅助函数，不修改生产Vue、
颜色值、用户批准、历史图片、接口、权限、数据库、配置或依赖。使用 ui-skills-root 与
fixing-accessibility 区分焦点交接、缓存退出及证据归属；本地CLI不存在，未安装新依赖。
开始时main/af239b08、405项未提交变更、暂存区为空，保留所有原有工作。

## 原失败与实际依据

1. 原焦点包当前保存的清单不只来自093d643b的图片修订，还包括635e5538的模态辅助函数更新。
   以093d643b检查时，17个来源中use-modal-dialog.ts不匹配；改查完整
   635e5538a96493fcf62e95938c78fb07aebcc9b7后，17个来源及完整清单全部对应。
2. P50通用写入和编辑器焦点两包的完整清单也与635e5538一致；对应CredentialAssetCenter、
   ConfirmDialog和use-modal-dialog的指定源码绑定一致。这不是当前P50全部状态重新验收。
3. 原“其他20个调用者不变”断言属于P50当时的变更范围，不能要求后续重设计全部回退。
4. 原合同函数已经向contentAfter升级，但旧测试仍期望更早after值。现保持全部旧常量，
   显式登记当前颜色变量提取后的版本，不覆盖或重命名历史哈希。

## 变更方式

归档焦点测试按固定635e5538完整清单校验，17个源码、4张现存图片、10条观察与待审标记保持。
P50范围测试从该提交枚举当时20个调用者，仍与7398aa8a比较；两份P50清单保持整包精确一致，
原busy防护、discardReturnFocus、浮层样式和观察器断言针对其实际历史阶段执行。
当前交互没有转去执行历史代码：上轮的当前Vue72项交互与实际App4宽度输入未变，本批不重拍。

当前响应式详情合同新增：

- 当前Vue原文SHA256=8669cbd3…；颜色文件原文SHA256=92238301…，二者分别检查。
- 只展开明确导入的overlay颜色角色；完整展开源码必须等于6d3088d1…，并与固定
  af239b08b69f7d97cd0372f9840a70009eeef0c7中的文件一致。
- 返回值是当前原文SHA256，不是展开后的历史SHA；缺少导入、角色或任何未知源码变化拒绝。
- before/after/governanceAfter/contentAfter四个明确旧入口接续到当前原文；未知身份拒绝，
  无关文件仍由各调用验证器独立检查，不获得通用豁免。
- 原before入口对焦点图证的依赖改为其真实635e5538阶段，不声称历史图证明当前页面；
  其他源码与图证验证器继续保留各自的再验证责任。

收尾复核补强：before入口原有的当前依赖约束不能随着历史归档一起消失。现在17个依赖
同时校验固定历史指纹与当前原文指纹；除本批已证明的详情颜色提取、原因窗已有的可选
maximumLength扩展（当前3191e4ba…）之外，其余15个当前来源必须仍与归档相同。
原因窗差异已逐行核对，只含可选prop、条件文案/maxlength和对应提示样式；没有新增默认上限。
另实际运行既有9项组织原因合同：当前Vue默认无上限、明确传入500仍生效、minimum=2与required
保持，调用方不得静态/动态/不透明传入未约定上限。未知模态/原因/全局样式变更的反例均拒绝。

此辅助函数有12个设计验证脚本调用，故完成最小检查后运行完整单测观察整体影响。
不把单个共享组件的兼容证明扩大为这些页面整页通过。

## 验证与收尾

- 原三项失败定向检查3/3通过，29.67秒，无取消/跳过。
- 新颜色/依赖接续反例6/6通过：原文返回值、丢失导入、行为/选择器变更、颜色值/角色漂移、
  全17依赖保留与未知依赖拒绝。既有组织原因合同9/9通过。
- 第一轮全库1401项：1237过/164失败，312.72秒，0取消/跳过。随后补强当前依赖约束并新增2项
  测试，所以该数字不是最终源码的全库结果；最终复测另列。首轮夹具nY1znx已自动删除并验证不存在。
- 最终243文件全库1403项：1239过/164失败，299.48秒、0取消/跳过、退出1。与上批完整
  1397项/167失败清单逐名比较，无新增失败，移除三项本批修正的共享焦点历史验证失败。
  完整名单及前后对比见 [最终单测结果](SHARED-FOCUS-UNIT-RESULT.json)。其他失败不统称无害。
- 焦点文件完整4项、新颜色/依赖6项及既有原因合同9项在全库中通过；文档/运行文档/diff门通过，
  全库内真实工作树格式门通过。没有改图片、清单或批准标记来换取通过。
- 两次自有格式夹具nY1znx/qDekuv均已自动删除并验证Test-Path=False；最后父子进程40924/51520
  均退出，暂存为空。未提交部署，commit hash不适用，生产无需重启。

原有运行命令与参数不变：设计验证器继续调用公共合同函数；当前交互可使用
`node scripts/verify-responsive-data-view-current.mjs`。无生产重启或配置调整需求。
本批新增测试为永久交付，无新临时图片、日志或浏览器服务。原P58视觉及未知结果规则继续待定。

此前两处清理受工具策略阻止，仍待手动处理，本批不绕过重试：

- `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p47-c-e2e-temp`。
- `C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`。
