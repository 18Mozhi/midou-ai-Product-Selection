# 全库门禁复核与只读预检依赖隔离

2026-09-12，main / af239b08；保留既有未提交的第二阶段实现。本批不迁移旧图批准，不部署。

## 复现与证据边界

- 修改前 `npm run test:unit`：1355 项，1161 通过、194 失败，退出 1，286.96 秒。
  四个前置构建通过；失败包括生产预检。首轮流式输出未完整保留该断言详情，不能确认错误码。
- 同源单独运行生产验收合同：3/3 通过，预检约 0.43 秒。
- 为保留失败诊断，在代码未改、构建未变时重跑原 `node --test tests/unit/*.test.mjs`：
  1355 项，1162 通过、193 失败，退出 1，158.96 秒；预检通过。
  包装器只汇总子进程 TAP，不改变参数、并发或测试内容，不创建日志文件。
- 第二轮 193 个失败分布在 122 个测试文件。不能统称为格式问题或安全地批量改哈希：
  ResponsiveDataView 的旧源指纹不再对应当前源码；多个 P44 包的 36/37 源数量断言面对
  当前 40 源；原因窗旧测试仍要求不存在 maxlength；P47 探针旧 harness 读取到 undefined。
  这些分别需要核对历史证据、现行业务约束和当前实际装配，不删除断言来换取通过。

P47 的一个错误已进一步定位：`ui-phase2-provider-adapter-feedback.test.mjs` 用
`text === old` 决定是否导出 `probeFeedback`；历史转换器对未匹配修订返回原文，当前组合
确实出现 `old === source`，而当前源码仍声明该字段，故 harness 误省略它。
只读复核为 historicalEqualsCurrent=true / currentDeclaresFeedback=true /
harnessIncludesFeedback=false。这是已确认的夹具问题，不等于真实探针回归；本批不修改
该历史转换器、测试或其源码/图片绑定，下一步须区分历史验证与当前行为验证。

## 本次必要修正

实际只读入口在判断 `--production` 前静态加载 auth/config/database 运行包，其中 auth
加载原生 argon2，database 加载 mysql2。用进程内数据 URL 模块加载器阻断这三个包，
旧入口的只读预检和缺密码分支均在 auth 导入时报错。这证明存在不必要的运行依赖加载，
但不证明它是首轮预检失败的唯一根因，也不证明机器总体性能改善。

将三个包改为原预检退出分支和既有密码长度检查之后的动态加载。生产函数、配置解析、
数据库连接、种子/清理、请求和成功判断全部不改。没有延长原 10 秒超时，也没有新增参数。

新增永久测试 `tests/unit/production-acceptance-preflight-isolation.test.mjs`：

1. 阻断全部生产运行包时，只读入口仍输出原 225 路径/258 操作/60 受保护路由/6 角色基线。
2. 显式生产分支缺少密码时，先由原长度校验拒绝，不加载运行包。
3. 显式生产分支使用虚构有效长度密码时必须触发运行包阻断，不能绕过依赖继续执行。

加载器在依赖解析阶段即失败关闭；测试不读取真实运行环境或连接数据库，不执行生产写入。
旧代码下新增测试 2 失败/1 通过；修正后与原验收合同及 API 探针回归共 8/8 通过。
定向 Prettier 通过。最终复用未变的四个前置构建，执行原完整 Node 单测入口：1358 项，
1165 通过、193 失败，退出 1，188.70 秒，无取消或跳过。与修改前第二轮按用例名称对照，
无新增失败；新增三项通过，旧预检也通过。不能将首轮到末轮失败数减少 1 归为已证明修复，
因为改动前第二轮预检就已通过。历史失败仍阻止提交/部署，局部通过不解除其余门禁。

文档/路由门（73 路由、60 受保护、6 角色、153 必需文件）、运行文档一致性、
定向格式与差异检查通过。运行脚本仅三项导入改为生产分支内动态导入，不改 TypeScript
生产包，因此不重复执行已通过且输入未变的构建；没有执行真实生产验收。

## 使用与交接

本地只读入口保持 `npm run verify:production-acceptance`，不要添加 `--production`。
本批没有新增环境变量、配置项、端口或依赖；不需要调整设置或重启服务。
实际生产执行仍须通过原宝塔发布流程和全部验收门，不因只读输出通过获得发布许可。
UI、截图、已批准组合、API/OpenAPI、权限、数据库、后端业务及生产环境未改。
旧失败需继续逐项处理；未知写入结果规则与 P58 具体视觉仍待原问题答复。
尚未提交或部署，commit hash 不适用。

本批无临时脚本、截图、日志或服务。首轮格式样例 `scoutops-code-style-Wk8Lw5` 与最终
`scoutops-code-style-hwJIHZ` 位于 `C:\Users\23136\AppData\Local\Temp`，均确认已删除；
最终运行同时记录 fixture 和 fixture_removed。中间诊断未保留隔离库名称，但临时目录
中已无该前缀目录，不能把未记录名称冒充完整逐次审计。永久测试/文档保留，既有审核图不删。
