# P46 字段名称与错误关联实施

2026-09-11，基线 main / 52099bc3。接续“整体结构通过，继续完善交互”。本批修复真实编辑器字段语义，不改已批准结构，也不把结构批准扩大到所有状态或生产验收。

## 改动与边界

23个既有字段名称分别包入可引用的文字span，原生input/select以aria-labelledby关联；20个已有formErrors提示赋予唯一ID，输入以aria-describedby条件关联，并由原formErrors计算aria-invalid。错误消失时移除description引用，避免引用不存在节点。三个选择项没有独立formErrors，不伪造错误或校验。

完整script、CSS、模型、事件、校验、步骤跳转、模板、保存/序列化、权限及接口保持。前后Vue AST移除本次精确语义增量后相同；没有新增required约束、改变错误出现时机或移动焦点。公开来源批准条件原本可能映射到条款地址错误，该业务映射未改。

不改变API/OpenAPI、后端/Python/插件、环境变量、依赖或数据库，故这些配套面不适用。没有部署或重启生产；未来按既有宝塔发布流程交付，不在本批启动生产变更。只读字段残留、日期时区、未知保存结果、背景隔离、必填宣告、步骤首错误定位及完整读屏器测试仍待后续核对，不能宣称完整无障碍。

## 实际验证

真实index/main/router/App/NavigationShell/KeepAlive/ProviderRuntimeSurface/Registry链，保留已接入生产C结构，无审核body/CSS注入。旧版只在本地Vite内存载入52099bc3 Registry，新版使用当前文件；其余源码相同。每版390/760/761/1440四宽度，780条显式检查、276条逐字段初始/错误/修复观察、112PNG，164个原始加载源指纹。23字段分5/6/7/5四步，每宽度20个错误条件由真实输入操作触发后修正。

新版额外逐项断言浏览器可访问名称等于独立字段名称、可访问描述等于当前错误文字；同时核对DOM无重复ID、错误引用随修正移除、下一步保留原校验、修复全部错误后提交可用、Escape回新建入口。未实际点击提交。每版8次GET全部来自明确本地夹具，0写入、0异常网络/页面错误；不是实际登录、授权或保存验收。浏览器可访问树不是NVDA/系统读屏器真实语音输出，不声称播报实机通过。

[修改前112图](../../output/playwright/p46-field-semantics/baseline/index.html) · [修改后112图](../../output/playwright/p46-field-semantics/current/index.html)

111对PNG字节相同；760-step4-repaired.png仅50个边缘像素有最多2色阶差异，范围x33–726/y399–501，原图保留并精确断言该差异，不设全图宽松容差。截图包括四步错误/修正后窗口及20个错误字段局部；不是整个页面所有状态覆盖。

## 复现与证据管理

- `node scripts/verify-ui-phase2-provider-field-semantics.mjs --capture`：当前真实入口样例回放与正式图。
- 加`--baseline`：同链前版对照，只在内存替换Registry，不改磁盘/生产。
- `node --test tests/unit/ui-phase2-provider-field-semantics.test.mjs`：当前原始源/AST/语义观察/图片完整性和像素差合同。
- 历史结构/键盘/反馈证据不覆盖；新增精确源码适配器仅关联既有旧图，未知漂移拒绝。当前原始源由新增独立合同验证，不能用历史源替代当前验收。

当前Registry LF SHA256：`ebd8b3876f9911ca6d5a0be6bac7a9352d977fa2446f4407342e54cd92b1fe1c`。正式224PNG和两套HTML/JSON作为交付保留；无一次性测试脚本、日志或图片，回放服务/浏览器均在finally关闭，正常项目构建产物保留。回退仅涉及本次字段语义及证据版本，既有结构、焦点和异步修复保留；以受控Git提交处理，不使用硬重置。

最终验证：新增4项合同、相关884/884回归、153文件文档门和代码样式门通过；vue-tsc与生产构建通过（422模块）。真实扫描的22个动作候选及candidateId前后完全一致，无扫描警告；API/模型/控件事件未新增。临时宿主和浏览器均已关闭，端口及进程复核无残留。

全P46、其他未审组合及73页重构/宝塔/用户签收仍未完成，本批不缩减总目标。
