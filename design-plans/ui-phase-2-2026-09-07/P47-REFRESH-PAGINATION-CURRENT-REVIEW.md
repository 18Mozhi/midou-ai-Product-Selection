# P47 分页实施后的刷新状态复验

## 范围与依据

用户确认手机“尚无来源”与“筛选无结果”两块视觉区域通过，继续其他状态。
批准已记录于 P47-EMPTY-REVIEW.md，当前手机样式已实施；本轮不重复批准、不扩大为
焦点视觉、整页、真实登记或生产验收。按 UI 技能选择现有 Playwright 驱动，不安装依赖。

起点 main / af239b08b69f7d97cd0372f9840a70009eeef0c7，已有560项工作区变更，暂存为空。
定向复现刷新当前入口测试5项4过1失败：手机逆向仍要求旧2f71a84b，而分页修改后的
输入经该逆向得到24f21af2。它发生在来源校验阶段，不是页面刷新行为失败证据。

## 最小修改

仅当前回放构建器接入既有严格 beforeAdapterPaginationFocus，再执行原手机与刷新
逆向；两次完整分页源码SHA校验保留。逆向仅供来源核验，实际浏览器仍只运行未经
转换的当前 App / ProviderAdapterCenter。原驱动完整SHA仍固定ed261c30178dc65d251d87ffe4d4a4b01b446dc3da321f8bd63fc9da79ea5975。
原场景、传输白名单、焦点和清理断言逐字保留，禁止参数、强制不截图也保持。

测试增加对已知分页前后版本结果相等的检查，以及未知附加文本、分页方向、刷新焦点
和已批空态文案漂移均拒绝。没有把任意源码漂移归一化为通过，没有改旧源码边界或图包。

## 本轮结果

- 最小验证：原5项由4过1失败变为5项通过。
- 相关完整验证：7份测试40项通过，含分页、手机空态、原刷新与当前读取/反馈入口保护。
- 当前实际浏览器：390/760/761/1440 × success/failure，8组152检查通过；178来源路径，
  当前Vue SHA为24a3b27fca62f5331bd19c58ba501e2b6b9777d7e59a7b03248286d82a4962a2。
- 每组Enter、Space和鼠标三次显式刷新；等待按钮禁用、持久标题焦点、Tab跳过禁用按钮、
  返回响应不抢搜索焦点、保留搜索和筛选结果，成功及409失败反馈均按原断言验证。
- 40条本地拦截无body GET，0写入、0浏览器错误、0意外请求、0新图片。
  路径集合数量不是单独保存的当前全源指纹清单，不扩大为全源图证。
- 原当前结果P47-REFRESH-FOCUS-CURRENT-RESULT.json继续保留为51c0ba手机阶段记录，未覆盖。

复验：`node scripts/verify-provider-adapter-refresh-focus-current.mjs`，不带参数。
本地Vite/Chromium已退出，端口53159关闭；本轮没有临时文件、日志或截图。

## 交付边界

只改验证辅助文件、对应永久测试和直接相关文档/Feature Map，不改运行Vue/CSS、接口、
OpenAPI、权限、业务规则、数据库、依赖、环境变量；不需要生产重启或配置调整。
没有重跑未受影响的构建或全库测试；全库1531项1392过139失败仍是P39阶段快照，
不能从本轮局部通过推算新的失败总数。工作区混有此前变更且全局门禁未通过，
未暂存、提交或部署，commit hash不适用。全73页完整交付与原待审项仍未完成。

历史两处临时目录仍需单独处理，本轮未创建且不绕过先前清理限制：
`D:/项目工程文件/vue/curson/工具/智能选品/output/playwright/p47-c-e2e-temp`；
`C:/Users/23136/AppData/Local/Temp/scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`。
