# P47 探针测试的历史与当前边界

2026-09-12，main / af239b08。承接全库门禁复核，只修改测试装配和交接文档，不修改页面或业务。

## 已复现的两类问题

1. 反馈测试用 `text === old` 决定是否导出 `probeFeedback`。历史转换器对未知修订返回
   原文，当前源码未匹配旧版本时 `old === source`，实际字段存在却被 harness 省略。
2. 反馈与读取顺序测试将未知的当前源码误当作历史反例。旧反馈版允许早先发起的 GET
   修改页面消息，但之后的读取归属修复会拒绝这种回写；两种版本的要求不能混用。

修改前定向选择检查等待、读写交错共 5 项，全部失败：两项字段 undefined、一项错误期待
列表刷新覆盖检查消息、两项历史反例实际运行了当前保护代码。失败不是新增业务需求。

## 修正与保留

- 反馈历史版本明确读取 Git `004c3e0e`，校验 SHA-256 为
  `28d57b9d143bf19419bbc0eb88afdc1126967017d6f3cb421b6be83ad412b1c2`。
- 读取顺序修复版本明确读取 Git `f2e87218`，校验 SHA-256 为
  `f6352020e923cac9b96a54b358ee69e6b53c5f8c1b820016b2bc038f41ccb08e`。
  对应修复前源码仍由原精确历史转换器取得，不接纳未知哈希。
- 当前行为默认直接执行工作区原始 Vue script，不经 C 或历史转换器回退。反馈字段是
  harness 的必需导出，不再根据字符串相等关系省略。删除真实字段的内存变体必须失败。
- 等待单飞、成功/拒绝追踪归属、前一个来源反馈清空分别对历史反馈版和当前原始版验证。
  旧 GET 成功/失败交错覆盖修复前、修复时、当前三个明确版本；其余读取归属用例执行当前原文。
- 历史组件差异、原 12 秒定时器、请求方法/路径/无额外 body、失败提示及后续新读取不变。
  真实实现和此前读取归属合同是依据，不改产品去满足旧断言。
- 原 44+48 张 PNG、两个清单与采集脚本均不写入。将历史图片/尺寸/场景完整性检查与
  “当前源码可否复用旧图包”检查拆开，避免第一个旧依赖失败遮住全部图片检查。
  后者逐项源码比较完整保留，没有更新哈希、添加通配或跳过。

## 验证与剩余门禁

修正后最小选集 11/11 通过。最终两文件 25 项，23 通过、2 失败；通过部分包含 92 张
历史 PNG 的内容哈希、尺寸、清单与版本归属，以及当前脚本行为。两项失败均先报
`apps/web/src/components/AuditedReasonDialog.vue` 不符合旧采集依赖指纹，因此旧图包不能
宣称适用于当前全部依赖。后续需逐个核对依赖变化或提供独立当前证据，不能直接改旧指纹。

这些是受控 Vue script/VM 单元回放和历史文件校验，不是本轮实际浏览器、读屏、真实
探针/外发/API/数据库或生产验收。未扩大任何用户已批准区域。
完整 Node 单测入口最终 1364 项：1178 通过、186 失败，退出 1，250.55 秒，无取消或跳过。
相比上轮 1358 项/1165 通过/193 失败，增加 6 项明确边界检查，消除 7 项已定位的装配失配；
两项旧图复用门只是改名后继续失败，没有忽略。格式、文档/路由（73/60/6/153）、运行文档
与差异检查通过。生产构建输入未改，复用之前通过的构建，不重复编译或重跑浏览器。
其余 186 项失败仍阻止提交/部署，不称全库门禁已解决或页面已完成。

## 使用与收尾

定向入口：`node --test tests/unit/ui-phase2-provider-adapter-feedback.test.mjs tests/unit/ui-phase2-provider-adapter-read-order.test.mjs`。
该入口仍应如实报告两项证据复用失败，不将选集通过冒充整文件通过。

无需用户配置或服务重启。没有改变 API/OpenAPI、权限、环境变量、数据库、依赖、后端/
Worker/Python 或运行部署器。没有新增临时脚本、截图或开发服务；永久测试与本文保留。
全库格式单测创建并自行删除 `C:\Users\23136\AppData\Local\Temp\scoutops-code-style-UFfFbJ`，
已复核路径不存在，测试父子进程均退出。本批无临时文件或服务遗留，原正式图不删除。
既有未提交工作不暂存，commit hash 不适用。P58 视觉与未知结果策略继续等待原问题答复。
