# P08 组织与工作区选择 · 生产 Vue 实施记录

## 实施范围

将已审核的 C 方向直接接入 `/select-context` 的真实 `TenancyChooser.vue`：以蓝色范围说明区和白底工作区承载组织目录、当前组织上下文、工作区与团队计数；手机端按层级单列。当前账号仍为非交互文字，不添加账号菜单或弹窗。归档工作区显式标为不可进入；搜索只过滤服务端已返回的组织名称与 slug。

## 合同与状态

- 保留 GET `/org/memberships`，选择组织后并行 GET `/org/{id}/workspaces` 与 `/teams`。
- 组织选择不更新会话。只有 `status=active` 的工作区可 POST `/auth/context`，请求仍只包含该工作区所属 `organization_id` 与 `workspace_id`。
- 保留无组织时 POST `/me/personal-workspace`（无 body）及原有默认 `/home` / 安全 `return_to` 分支；本轮没有用真实账号触发创建。
- 保留错误、403、401、无组织、无工作区和完成状态语义；请求序号阻止已被后续组织选择或返回目录取代的响应改写页面状态。
- 未修改 API/OpenAPI、数据库、配置、依赖、权限规则或数据格式；无需迁移。部署按固定宝塔流程更新静态前端，并重启受管 Node 项目以满足部署器发布合同。

## 验证与发布

| 项目                                                             | 结果                                                                                                          |
| ---------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| `tests/m01-03/tenancy-contract.test.mjs` 与 P08 页面预览定向测试 | 6/6 通过                                                                                                      |
| M01-03 浏览器 E2E                                                | 桌面 4/4、390px 手机 4/4 通过；覆盖键盘选择、选组织零写入、归档禁用、精确范围请求、无组织创建路由、403 与 401 |
| P08/身份 setup 与陈旧响应隔离检查                                | 18 项通过；身份 C 方案核对 82 场景、188 张方案图、26 个动作 ID，0 页面错误/网络请求（仅设计原型证据）         |
| Vue 类型检查                                                     | 通过                                                                                                          |
| 前端生产构建                                                     | `npm run build:web` 通过                                                                                      |
| 文档、计划结构、代码格式、静态分析、发布矩阵、前端预算           | 通过；宝塔部署前会再执行固定发布门                                                                            |
| 宝塔健康、版本、深链及静态资源                                   | 部署后结果待填写                                                                                              |
| 提交 / 生产 build SHA                                            | 待填写                                                                                                        |

浏览器写请求使用本地 E2E 拦截夹具，不写真实租户或会话范围。以上不证明真实成员资格、生产 RBAC/SQL 审计、个人空间创建或正式 M07-03 验收。

## 运维及后续

代码通过项目固定 `python scripts/deploy-baota.py` 发布；无需环境变量或服务策略调整。该部署器按项目固定流程验证对象并管理发布重启。真实身份/成员资格、生产权限隔离与个人空间创建写入继续留在对应业务验收，不以页面上线替代。
