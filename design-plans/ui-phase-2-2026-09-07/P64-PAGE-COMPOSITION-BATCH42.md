# P64 实际Vue C方向组合 · 批次42

起点main/6025c198，546项在途变化，索引为空。上轮P62离页导出政策与既有API覆盖计数失败均未获处理决定；本批推进独立的P64实际Vue审核，不默认批准上述选择。全73页目标不缩减。

## 结构实施

新增审核转换器 backup-page-preview.mjs、审核CSS及原E2E夹具读取器；Vite内仅替换BackupRecoveryCenter模板与该路由壳层标题。业务script经换行规范化后完全一致，生产文件及共享ResponsiveDataView/TechnicalDetails未编辑。

按frontend-design技能接续既有BACKUP-RECOVERY-C方向：取消旧英雄卡背景与四张数字卡，蓝色同机边界说明和三段页内导航；白色内容按结论、目标与实际、恢复证据、资产、阻断排列。目标RPO/RTO与原实际表达式并列，null仍为未记录，不推断达标、不新增事实或判定。恢复证据移至资产之前，七列表格与summary/detail插槽原样保留。无条件“高强度加密”标题替换为“按实际记录核对”，加密和完整性仍以详情原字段显示。

fixing-accessibility指导标题层级、原生页内锚点和模态可达检查。新页内链接只指向现有审核区域，不发API请求、不新增执行动作。未改变全局主题/依赖；审核CSS高优先级仅限P64宿主，传送至body的详情也限定该宿主存在时使用蓝白样式。

## 样例与验证

默认原样复用tests/e2e/m07-04-backup-recovery.spec.ts的base和blocked状态：一个mysql_full主备份、实际RPO5、无演练、无已验证恢复副本。第二次读取使用明确合成的五种对象和主备份/恢复副本角色，其中mysql_binlog加密及完整性未核验；只用于字段展示，不是生产覆盖或服务判定证明。120天只是继承既有显示测试的本地策略变体，不授权修改生产90天发布门槛。

6项新合同测试通过：原业务脚本等价、模板编译、原七列及完整详情插槽一致、顺序/旧布局移除、实际值表达式保留、原部分夹具未伪造verified演练。加原M07-04相关9项，共15项通过。其中AES-GCM自测只使用临时合成文件，自带finally清理并返回cleanup=passed，不是宝塔真实恢复验收。

`node scripts/verify-backup-page-preview.mjs`两端1440/390、减少动效，97项通过：初始结论、目标/实际/缺失值、页内锚点无GET、五类手机详情六字段、完整对象/角色代码、Tab双向循环、Escape返焦、原七列表格、动态120天、资产为空及失败保留空快照；没有POST/外部请求/真实下载或浏览器异常。未覆盖全部列设置组合、复制消费者、真实权限、读取15秒超时、KeepAlive生命周期或全主题密度。

读取次数曾断言4而实际6。核对api-client的[0,150,400]安全重试：三次成功GET加末次503失败的三个尝试，每端6GET，共12；测试按真实合同计数，不改重试行为、不把自动重试说成重复用户点击。

## 正式图

最新版 `output/playwright/p64-page-composition-r3`：31PNG（桌面11、手机20）、index.html、manifest.json及README，165来源/31图SHA-256，HEAD6025c198加捕获时工作树。包含默认、目录、目标对照、恢复证据、原资产、技术/阻断、五对象、120天、空快照、刷新失败及五类详情底部续图。

r1/26图为旧主题覆盖残留的修正前对照；r2/26图已修正标题字体、米色导航/控件和橙色详情顶线，但尚无底部续图；均保留为版本化正式对照，不覆写其manifest。r3增加首屏外角色代码可见性和Tab循环，详情底部图不可当成另一张完整弹窗。已人工查看桌面默认、手机目标对照及增量日志详情；不把图片数量或脚本成功等同用户已批准。

## 使用与收尾

无参数命令不写文件；`--capture-review rN`只创建新的独占版本目录，已有目录即失败，不覆盖旧图。正式审核看r3/index.html。查看页面仅在本地宿主拦截请求；不使用生产环境，不执行备份/恢复/解密/迁移。

OpenAPI、API/鉴权/审计/SQL/业务判定、后端/Worker/Python、.env/配置/依赖均未改变；生产者/消费者接口联动不适用。产品源码与构建输入未变，复用前轮有效构建结果，不重建生产包；当前审核没有生产重启要求，也未部署。旧runbook路径与固定目录冲突仍不在本轮执行，不据旧文档迁移。

本轮浏览器/Vite全部finally关闭；加密自测临时文件自动清理，核查TEMP无scoutops-backup-selftest目录残留。新脚本/测试、三个版本图册为永久交付，未创建散落的临时截图/日志。既有清理受阻目录不处理。

按既有API覆盖失败未获处理决定的提交边界，未暂存/未提交，commit hash不适用。本批具体图待审；P64其他状态/全部交互/保活、真实权限与恢复演练，以及全站73页实施部署签收仍未完成。
