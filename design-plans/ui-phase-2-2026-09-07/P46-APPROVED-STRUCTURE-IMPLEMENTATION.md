# P46 已批准结构接入真实前端

2026-09-11，main / 717cc944基线。接续用户“整体结构通过，继续完善交互”，不扩大为全部按钮/状态、完整弹窗、真实保存权限或生产验收。本批完成的是已展示结构的生产源码接入，**尚未部署，P46和全73页仍未验收完成**。

## 接入范围与刻意保留项

ProviderRegistry仅新增独立 `provider-approved-structure.css` 引用，完整原script、template、旧样式引用逐字不变。没有新增路由、API、业务字段、权限、数据库、依赖或环境配置；无需变更OpenAPI/.env.example/后端/Python/插件。页面规格、feature map与源指纹同步。

CSS从已批准整体审图的page/shell及手机editor样式提取，使用局部 `--p46-*` 颜色令牌；所有规则均以文档中实际挂载的 `#app .provider-registry` 为条件。P46的白色外壳、标题/上下文、蓝色内部导航、概要/统计、目录筛选分区，以及<=760px手机编辑窗的步骤/字段/底部操作接入。删除审核宿主标记/提示与旧装饰页码、视觉口号的生产展示，保留角色、导航能力与业务说明。

未接入记录行/列控件的完整C样式、详情专属样式、错误/空状态专属布局及桌面编辑C布局。它们仍可能保留旧视觉；全局P46字体继承和共用控件样式会影响同页组合，不能称非目标区域像素完全不变。原预览里“操作”表头、重读文案和隐藏未绑定secondary的替换本批未接入；业务和原恢复行为未改。

三个兼容主题ID及切换逻辑不变，不写或强制切换任何用户偏好。已批准蓝白身份区域使用固定局部C令牌，其余区域仍沿用原主题；三套主题的完整重设计及命名/说明协调仍待后续核对。本批不宣称全页三主题已经完成。

## 真正运行的验证链

`index.html → main.ts → router.ts → App.vue → NavigationShell.vue → KeepAlive → ProviderRuntimeSurface → ProviderRegistry`。

两组均使用真实入口，**不注入审核body class、审核标识、审核CSS，不替换template或script**。基线只在内存移除新增style引用，当前完全使用原始组件；相同E2E样例、相同请求处理、相同操作。验证宿主仅将API基址约束在本机、禁用代理并拦截明确样例路径，不修改.env。身份与请求不是真实后端证据。

[接入前132图](../../output/playwright/p46-approved-structure-implementation/baseline/index.html) · [接入后132图](../../output/playwright/p46-approved-structure-implementation/current/index.html)

每版390/760/761/840/841/1440六宽度，132PNG。当前452项浏览器检查；基线320项检查外加132个独立宽度观察，不把基线缺陷当通过。基线164/当前165加载源文件绑定原始指纹，另记录移除style后的渲染源hash；文件数不是页面覆盖。

- 旧版761px内容宽776px，841px内容宽1009px；在列表/筛选/返回及主题状态出现。基线完整图如实保留超宽，不裁掉缺陷；新版这些截图均无横向溢出。
- 四步键盘正反循环、中间移动、保存中跳过禁用项、技术详情展开/折叠、Escape回入口、详情转编辑及浏览器返回继续验证。原保存/字段问题不因这些检查通过而关闭。
- 真实按钮选择三个主题ID，原语义主色变量与基线一致；不新增偏好写入。每版48GET/6PUT均本地拦截，PUT全部409拒绝，无POST或真实保存；请求及focus观察前后相同。
- 先挂载P46再切P47，六宽度P47背景、显示字体令牌、布局和导航计算值恢复原状；六张P47图前后SHA256完全相同。测试固定了P47的浏览器时钟以排除“最近刷新”秒数差异，并未隐藏该文字。只证明此相邻路由场景，不冒称所有路由均已回归。

## 复现、回退和证据管理

补充摄影与隔离证据：P47六张关闭菜单图及四张实际展开菜单图，前后共十对图SHA256逐字节一致；可见SVG的几何、路径、绘制样式及打开后的菜单SVG读值一致。关闭抽屉内部分屏外SVG的计算读值存在延迟更新，原始观察保留，不把它们当已绘制像素；必须实际打开菜单验证。截图等待有限CSS过渡结束并允许正常绘制，不再用禁用动画的摄影快进；取消/替换CSS过渡导致的finished AbortError只在动画等待处理，不吞接口错误。未掩盖、删除或修改截图中的图标/刷新时间。上下文绿色状态文字保留原色；“已连接 · 可复核”是原静态文案，不是真实后端健康证明。

- 当前：`node scripts/verify-ui-phase2-provider-route-assembly.mjs --structure`；加 `--capture` 写当前正式图包。
- 前版样式：在上条命令加 `--baseline-production`，只在内存移除新增引用，写baseline正式图包，不修改源文件。
- 合同：`node --test tests/unit/ui-phase2-provider-structure-implementation.test.mjs`。使用原始当前源，验证style-only差异、CSS作用域、完整图包、真实入口无审核注入、原溢出/新边界、请求一致与P47隔离。
- 历史keyboard/assembly/feedback图包和用户批准不回写。历史精确版本适配器仅用于关联旧图，未知源码漂移拒绝；当前源由新增测试独立绑定。旧capture模式遇到已接入结构CSS会失败关闭，防止覆盖旧审核证据。

当前Registry LF SHA256：`87728f892d895f3be4ecb0ecf016125745ea99fc9898f4d3c8c1845013b19a90`。只需移除本批新增style引用即可恢复接入前布局，键盘和异步修复仍保留；实际回退应以Git提交和受控发布包为准，不执行硬重置。

未部署、未重启生产。后续仍使用既有宝塔部署器，核对发布窗口、迁移白名单和恢复材料；不能把前端CSS改动宣称为整个部署流程零停机/无数据库操作。正式264PNG和各HTML/JSON保留；临时Vite/浏览器关闭，无一次性脚本/日志文件；正常web dist和浏览器插件构建包保留。

最终验证：新增结构合同5/5通过，相关UI/平台账号/组织动作/组件边界/响应式筛选回归880/880通过；当前真实入口浏览器452项检查通过，原版320项检查与132项宽度观察单独保留。前端类型检查与构建通过（422模块）；最后生产CSS调整后再次Vite构建通过。文档门通过（153文件）。这些是本地样例验证，不替代生产数据与权限验收。

余项：未审记录/详情/错误/桌面编辑/主题密度完整视觉，日期与只读字段残留，真实保存及未知结果，完整模态无障碍，生产验收与73页全量交付。该清单不缩减总目标。
