# P48 · 来源频道

主目录/筛选/页内详情与四个既有业务弹窗已按 C 方向迁入生产 Vue，分别见[主目录实施记录](../P48-PAGE-COMPOSITION-IMPLEMENTATION.md)和[弹窗实施记录](../P48-DIALOG-IMPLEMENTATION.md)。原规格中“待审/待交”段落保留为历史审计基线；页面异步竞争、写后重读恢复、真实权限/来源验收和生产签收仍未完成。

2026-09-11兼容矩阵窗增量：[解析兼容矩阵审核](../P48-COMPATIBILITY-REVIEW.md)。真实App/ProviderSourceCenter/ProviderCompatibilityMatrixDialog与本地合成读取夹具覆盖加载、兼容/不兼容/不一致/待验证、四状态组合、空矩阵、缺采集程序版本、缺来源观测和依赖失败；蓝色身份、证据边界、风险优先结论及连续六列台账取代两列重复卡片，手机按同DOM顺序显示每个字段名，完整SHA-256折叠。四宽度40组602检查、42图、59加载源；通常目录/适配器各1次GET，503保留现有3次安全GET，零写入。生产未改/未部署；本批待审，真实证据/API/MySQL/权限/生命周期、P48整页整合与SC48继续开放。

2026-09-11固定样本窗增量：[固定样本 / 回放 / 独立复核审核](../P48-PARSER-SAMPLES-REVIEW.md)。真实App/ProviderSourceCenter/ProviderParserSampleDialog/ProviderParserSampleReview与本地合成读取夹具覆盖加载、候选、双空态、可复核、自审受限、批准、驳回和回放通过/差异/失败；蓝色身份、三步启用门、候选/样本分区和折叠技术详情压缩判断路径，保留不可变样本、已存快照回放、另一管理员与2–1000字原因合同。四宽度40组564检查、46图、59加载源；每组目录/样本各1次GET，零写入。生产未改/未部署；本批待审，真实固定/回放/复核、冲突/失败/重读/迟到响应、兼容矩阵窗、SC48及真实权限/MySQL继续开放。

2026-09-11版本交互增量：[配置回滚交互审核](../P48-SOURCE-VERSION-ACTIONS-REVIEW.md)。真实App/ProviderSourceCenter/ProviderSourceConfigurationDialog与本地受控POST/GET覆盖提交中、成功、409、403、服务失败、写入后目录/历史重读失败，以及首次读取失败和安全恢复；拆分写入与重读事实，提交中禁用全窗写入口，恢复读取不重复POST，补终态焦点/手机回顶及操作代次保护。四宽度32组444检查、52图、61加载源；回滚场景各1次本地POST且请求体/幂等键通过。生产未改/未部署；本批待审，真实回滚/API/MySQL/权限/超时和独立卸载迟到响应证明、其余两任务窗及SC48待后续。

2026-09-11版本窗增量：[配置历史与回滚审核](../P48-SOURCE-VERSIONS-REVIEW.md)。真实App/ProviderSourceCenter/ProviderSourceConfigurationDialog与M03-07版本夹具，审核专属SFC变换/CSS覆盖读取中、完整历史/差异、短原因禁用、无可见差异和空历史；四安全字段转可读值，原因按需显示，单时间线取代多层卡片，补首焦点、Tab循环、Escape、背景隔离及触发点归还。四宽度20组276检查、26图、59加载源；每组目录/版本各1次GET、零写入。生产未改/未部署；本批待审，真实回滚/409/失败/迟到响应/卸载、其余两任务窗、SC48及真实权限/MySQL继续开放。

2026-09-11配置返回增量：[终态返回与目录重读审核](../P48-SOURCE-CONFIGURATION-RETURN-REVIEW.md)。真实App/ProviderSourceCenter/ProviderSourceConfigurationDialog与本地受控响应覆盖保存成功、部分保存、版本冲突确认后的目录重读中/成功/失败及显式恢复；拆分写入结果与目录同步，失败保留旧目录但不冒充最新配置，唯一动作“重新读取来源目录”，补窗→页焦点归属。四宽度32组456检查、35图、63加载源；生产未改/未部署。本批待审，真实权限/超时/卸载/并发、SC48及其余三任务窗待后续。

2026-09-11配置窗状态增量：[保存与烟测状态审核](../P48-SOURCE-CONFIGURATION-STATES-REVIEW.md)。真实App/ProviderSourceCenter/ProviderSourceConfigurationDialog与本地受控PUT/POST覆盖直接保存中/成功/失败/409，及公开来源先存停用、烟测中、烟测拒绝/不可用、二次启用中/失败和成功；保留两段写入/版本锁，以提交快照避免第二段表单漂移，窗内区分未写入与部分保存。四宽度44组632检查、49图、61加载源；所有写入仅到本地拦截层。生产未改/未部署；本批待审，终态后安全重读、迟到响应归属、真实权限/超时/SC48及其余三任务窗待后续。

2026-09-11配置窗字段增量：[编辑采集设置审核](../P48-SOURCE-CONFIGURATION-REVIEW.md)。真实App/ProviderSourceCenter/ProviderSourceConfigurationDialog与M03-07 fixture，审核专属SFC变换/CSS覆盖网页登录停用默认态和公开来源烟测启用态；保留五字段、同频/并发口径和两阶段启用合同，补持续标签/帮助关联、首焦点、Tab循环、Escape、背景隔离及触发点归还。四宽度8组120检查、12图、59加载源；每组1次GET，零写入。生产未改/未部署；本批待审，保存中/成功/烟测失败/PUT失败/版本冲突/重读失败、其余三任务窗及SC48/PR-G01待后续。

2026-09-11刷新保留增量：[刷新反馈审核](../P48-SOURCE-REFRESH-REVIEW.md)。真实App/ProviderSourceCenter与146条M03-07 fixture，审核专属SFC变换/CSS覆盖刷新中、成功、429、503、500和12秒超时；保留146条口径/当前20条目录，去除重复入口和旧请求号，补失败重试与标题焦点。四宽度20组328检查、27图、64加载源；429/503各3次安全GET，500/超时各1次，恢复再1次成功GET，零写入。生产未改/未部署；本批待审，401/403旧目录可见性因会话/权限边界继续开放，四任务窗及SC48/PR-G01待后续。

2026-09-11首次读取状态增量：[七状态审核](../P48-SOURCE-STATES-REVIEW.md)。真实App/ProviderSourceCenter与现有HTTP映射，审核专属SFC变换/CSS覆盖读取中、空目录、登录失效、权限不足、429、503和500；去除重复提示，技术编号折叠，新增明确恢复动作与标题焦点归还。四宽度28组388检查、34图、65加载源；429/503各3次安全GET，空/403/429/503/500恢复到146条目录，401只进已验证/login，零写入。生产未改/未部署；本批七状态待审，刷新保留/失败、四任务窗、SC48/PR-G01和真实权限/依赖继续开放。

2026-09-11手机实际Vue增量：[筛选与页内详情审核](../P48-MOBILE-DIRECTORY-DETAIL-REVIEW.md)。真实App/ProviderSourceCenter与146条M03-07 fixture，审核专属SFC变换和CSS保留搜索/结果数，六项次级筛选折叠并在440px内滚动；目录显示采集方式/最近成功摘要，页内详情保留七项事实与三个原动作。四宽度8组94检查、24图、172加载源，4组1024/1440桌面基线0像素差；每组1次GET、详情零额外读取、零写入。生产未改/未部署；本批手机筛选/摘要/详情待审，上一批默认目录与首条记录仍待审，其余状态、四任务窗、SC48/PR-G01和真实权限/烟测/复核继续开放。

2026-09-11实际Vue增量：[默认目录审核](../P48-PAGE-VUE-REVIEW.md)。真实App/ProviderSourceCenter与146条M03-07 fixture，审核专属CSS形成蓝色身份、连续四统计、白色七筛选、用途台账和分页；四宽度前后8组148检查、48图、62加载源。20条分页、第二页21–40、筛选/重置、五个44px代表控件与零额外GET/写入通过。生产未改/未部署，默认目录与首条记录待审；手机摘要详情、状态、四任务窗、SC48/PR-G01和真实权限/烟测/复核继续开放。

2026-09-09首轮C稿：[SOURCE-CHANNELS-C-r1](../design/source-channels-direction-c/README.md)，81场景双端162主图＋39连续长窗局部，共201PNG（含2审核工具图）。四任务窗、目录→详情、七筛选、原20/6分页与烟测三阶段/样本复核/矩阵状态有离线图与源函数检查；不是正式实现图或用户通过。源二次PUT表单漂移、旧保存关新窗和成功覆盖重读错误已复现，保护仅提案。具体图、SC48/PR-G01、真实Vue/API/MySQL/权限/烟测/双人复核及完整主题密度/生命周期待验；下一P49。以下“正式新图待交”是原事实规格时间点，design/desktop、design/mobile与implementation全站标准槽位未以本包替代验收。

状态：事实规格完成；正式新图、Vue重构与完整验收待交。起点main/b52fb56。逐控件、输入、弹窗与API依据见[来源频道、启用检查与凭证合同](../source-channel-credential-contract-review.md)，不将旧矩阵中的拟议动作当现有能力。

## 1. 角色、范围与路径

路由`/platform-admin/providers/sources`，ProviderRuntimeSurface → ProviderSourceCenter，平台范围。路由目录声明platform:operate、platform:superadmin；目录/配置/样本API为provider:configure，固定样本回放为collection:replay，不能以导航可见代替后端授权。上级与定义页为P46，凭证P50，1688检查P49；`provider_id`深链精确限定已登记来源。壳层五链接的后两项以platform:superadmin可见，三页没有改变该条件。

## 2. 主任务与真实数据

GET /api/v1/platform/provider-sources把代码目录与已登记记录合并；前端过滤、排序、每页20条后分成市场信号/商品竞品/供应链三组。显示来源、用途、目标、字段、市场/语言、接入模式、采集频率、最后成功/失败、负责人及策略。组总数按全部过滤结果，组内行只含当前页，不用组总数替代页内数。全局统计不随过滤变化。

amazon_product/1688_search的effectiveAvailability以provisioned.status是否enabled决定automatic或setup_required，其他来源沿原availability；因此automatic计数不是所有来源当前确已进入调度的证明。计划派生“≤频率分钟”不是实际测得SLA；缺并发快照回退配置limit和active=0，不能描述为实测空闲。新稿须区分这些口径，保留原业务规则不自行重算。

## 3. 待审桌面构图

以“定位来源→理解可用性/最近证据→调整或处理阻塞”重构。主区首先给来源身份、真实运行状态与最近证据；筛选收敛为有明确当前条件的工具区，技术字段/策略从主路径渐进展开。来源配置、版本历史、样本和兼容矩阵分别是任务面，不把所有按钮同权铺在每行。重新评估分组、列表与详情关系，不默认保留旧卡片墙、横向账页导航或大标题编号。

## 4. 待审移动构图

来源行先给名称、可用性、最近结果，次要字段进入详情；筛选与分页结果范围可见，保持provider_id定位。配置与长样本详情需独立可滚动操作面，标题/关闭/错误/底部动作不被软键盘覆盖。保留版本差异、审批限制与样本标识的可达性，不能把桌面隐藏列当删除能力。

## 5. 动作与按钮

SC48-LOAD目录刷新/空态重读；DEFINE跳P46；FILTER七个条件和重置；LINK打开HTTPS来源；PROBE匿名测试；CONFIG编辑/保存；VERSIONS历史/回滚；COMPAT兼容矩阵；LOGIN跳指定来源凭证；SAMPLES固定样本/回放/复核；ACCEPT跳P49；PAGE前后页。可见条件逐项见合同。API虽有provision，当前页没有独立登记按钮；未登记显示等待系统登记，不能据API补一个未授权产品动作。目录刷新是GET，不调用成员即时采集/provider-sources/refresh。

## 6. 弹窗与写入合同

四个本地role=dialog定义：配置编辑、配置历史、解析样本、兼容矩阵；后者只读，样本复核为窗内每样本行，不另算独立弹窗。配置五输入为schedule_minutes 1–10080整数、timeout_ms 1000–120000整数、retry_limit 0–10整数、status enabled/disabled、reason 2–500字符；服务端另校验expected_version正整数。

停用公开来源启用：PUT停用配置→POST真实健康烟测→ready后带新expected_version再PUT；失败保留已存的停用配置，不能说整次没有写入。历史回滚POST target_version/expected_version/reason生成新当前版本，不改历史。样本创建取真实browser_job_id与固定命名；回放只重解析已存快照并写差异记录，不等同外部浏览器重采；复核需另一管理员、原因和review_version。回放通过不代替审批通过，也不自动启用来源。

## 7. 加载、空态与异常

loading/ready/empty/expired/forbidden/blocked/error，目录为空与筛选无结果分开；12秒超时，有items时刷新失败保留目录，包括401/403当前实现的保留路径，是否符合会话边界待验。历史读取失败关闭历史窗；矩阵失败留窗显示错误；样本读取失败关闭样本窗。父级message承载部分写入反馈，配置窗内是否能感知错误是独立待验项，不因页面存在role=status就宣称通过。

## 8. 筛选与异步归属

初始化读取URL q/category/availability/market/language/access_mode/sort/page；默认business排序保持接口顺序，attention优先待设置和automatic未启用，再名称；name按中文名称，recent按最后成功时间。七条件变化回第1页，页数缩小夹紧；router.replace保留provider_id及其他query。重置不清provider_id，不能宣称返回全目录。没有URL历史回写本地过滤器的watch。

load每次独立controller，无单飞/代次/卸载清理；多个弹窗和写入使用共享ref，关闭、更换来源、KeepAlive离开、迟到响应、保存期间再改输入待专门复现。回滚与样本行仅禁用当前进行中的ID，不能推断全窗所有写入已互斥。翻页滚动尊重reduced-motion。

## 9. 无障碍与视口

四自建role=dialog没有统一useModalDialog或本地完整Tab/Escape/焦点归还代码。后续逐调用方验首焦点、循环、取消、遮罩、长表单与父级错误可感知；对字段设置真实错误关联，不只增加aria。执行PLAN的1440/390、768/1024、长中文、200%缩放、44px热区、三主题两密度；状态有文字，不能仅靠颜色或“自动”徽标。

## 10. 验收与图槽位

已有tests/e2e/m03-07-provider-sources.spec.ts覆盖目录、启用来源显示、URL分页、配置、矩阵、烟测、回滚、1688检查、刷新、拒绝及样本双人复核等局部场景；本批未把测试文件存在当重新执行。SC48-01–05补全部过滤/深链返回、版本冲突/烟测部分保存、样本结果与身份归属、只读矩阵失败、完整模态与真实权限。正式design/desktop/P48.png、design/mobile/P48.png、implementation对应双视口及四窗各状态图待交；无方向批准、生产或用户签收。
