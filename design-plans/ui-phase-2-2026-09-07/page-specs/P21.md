# P21 供应链与利润 · 报价事实和采购工作台

2026-09-20接续：[批92实际 Vue C 组合](../P21-PAGE-COMPOSITION-BATCH92.md)为现有找货路由提供蓝白审核层，覆盖找货记录、候选报价事实、保存对比和读取失败双端图。候选、对比与到岸成本状态仍独立，缺失不补造为确认报价；32项本地拦截检查/零写入通过。用户审核、真实 RBAC、找货/报价/采购/删除/成本复核写入、弹窗、读屏和生产验收仍待办，未修生产 Vue。

[当前状态适用性核对](../SOURCING-STATE-APPLICABILITY-REVIEW.md)：P21保留150个代表图状态映射，另20个disabled/busy槽以调用方／实际控件／源码指纹登记为当前源码无此呈现，未分类代表槽0；不是全字段、全变体或整页验收。31动作／7关联／54局部源位置和695PNG不变，不生成假禁用图、不自动批准。下一仍需字段／主题及真实生命周期与P22等同级核对；全站重构、部署和签收未完成。下方20待核对为历史批次。

[成本与复核控件审核](../SOURCING-COST-CONTROL-REVIEW.md)。2026-09-09 P21成本与复核：8控件变体新增80双端图；本包65主场景695PNG、59变体544双端状态实例。五语义组新增26代表槽，累计150映射、20待适用性核对；驳回额外变体不新增动作。成本busy与找货独立，机会/复核版本分别使用，取消/换结论不撤回已发请求；无源禁用条件不编造图。13组源隔离、smoke和完整capture通过，具体批准/真实Vue/部署仍待。

[导航、搜索与恢复审核](../SOURCING-NAVIGATION-RECOVERY-REVIEW.md)。2026-09-09 P21导航与恢复：新增230PNG（28控件变体双端四态224图＋3场景6图），包现65主场景615PNG、51控件变体464双端状态实例。8导航和2恢复代表组新增40槽，累计124已映射、46待补；额外来源、query和角色/主次恢复不增加动作分母。源恢复实际load与通用标签差异已明示，原型不伪造登录/权限/返回能力，加载/失败隐藏旧记录和删除入口。11组源隔离与完整capture通过，具体批准/真实Vue/部署未完成。

[主操作状态审核](../SOURCING-MAIN-STATE-REVIEW.md)。2026-09-09：主操作新增96张双端图；本包62主场景385PNG、23控件变体240双端状态实例。8语义组增加36代表槽，第二采购来源/其他记录/已勾选为3额外变体，不新增业务动作；累计84代表槽已绑定、86待补。局部/读取控件没有源码禁用或busy条件的，不编造图或减分母。仅离线提案，未提升批准、改生产或部署。

2026-09-09：[四窗关闭与取消](../SOURCING-SECONDARY-STATE-REVIEW.md)新增96张，现289PNG；4关闭组24代表槽新增，4取消变体不膨胀动作分母，剩122槽。源允许busy关闭，新稿父请求在途锁仍待审；精确回焦及重开草稿语义按窗口分别核对，不宣称撤回请求或真实Vue完成。

2026-09-09：[四提交按钮六态](../SOURCING-CONTROL-STATE-REVIEW.md)新增48张双端上下文图，包现193PNG。找货/报价/删除禁用即请求busy，采购另按MOQ/原因；按钮区灰底忙碌/独立危险色/深蓝焦点及手机底栏修正仅为离线提案。24代表槽已映射、146待补；真实Vue与具体批准未完成。

2026-09-09新增[逐动作与既有图稿对账](../SOURCING-SEMANTIC-REVIEW.md)：54源位置、38语义组（31动作/7关联），25输入/12结构/4自定义弹窗。现有145图只关联上下文，170代表状态槽未逐selector映射；恢复主次按真实load/resetQuery/openSearch，成本与找货权限和版本独立。原生模态/忙碌锁/独立降级仍是待审提案，本轮未改图或生产。

2026-09-09新增[SOURCING-C-r1整页待审图稿](../design/sourcing-direction-c/README.md)：62场景145PNG，覆盖货源/对比/机会成本、四类输入与四窗及内联双人成本复核。九组真实setup隔离检查不关闭SC-G01–08；独立降级、焦点与准确提示仍是提案，未改生产，P22另交。

状态：规格草案；W03；`/sourcing`，SourcingWorkspace及其三个子面板。依据见[供应与成本合同](../sourcing-cost-contract-review.md)。正式全新构图待审，不把现有四阶段条、侧栏或纸张样式作为必须继承项。

## 1. 用户与导航

member壳层、protected、sourcing:read、reset_on_scope。找货/报价/对比/删除/采购写入需supplier_quote:manage；机会成本提交/重算另需cost:confirm，不能互相代替。采集明细链接只对platform:operate或platform:superadmin显示；成本复核动作由服务端每条can_review控制，真实API仍独立鉴权。入口包括P18找货深链、导航及带create/opportunity_id的创建入口；返回费用规则传from=route.fullPath，机会详情链接目前仅携带from=/sourcing，不声称保留当前找货record。

## 2. 主任务与事实出处

主要任务是找到当前可追溯报价，并带明确数量创建采购任务；不是浏览器即时计算利润。GET /sourcing/searches读当前范围记录，GET /sourcing/searches/:id读候选、collection_progress和可选erp_reference，GET /sourcing/comparisons读同范围对比历史。名称优先display_name；机会ID仅用于追溯。当前QUOTE join覆盖候选展示的MOQ、价格、币种、规格、交期、所在地、可信度及观测/证据，旧采集事实不被界面编辑覆盖。对比历史引用保存时的报价ID，不保证今天仍是现行报价，现“现行报价”字样列SC-G06待核实。

ERP参考成本必须标非确认报价；缺到岸价显示待费用规则计算，缺候选字段逐项列出。机会成本子面板独立读取机会version、profit-analysis与有权限时的cost-input-reviewers；只有最新run.status=calculated显示金额与利润，否则按真实状态/缺项处理，不填示例值。

## 3. 桌面构图方向

待审新稿围绕当前找货记录组织“搜索条件和来源进度→候选事实→报价对比→采购提交”，弱化重复介绍与统计卡。记录切换和主任务清楚分离；候选卡保留价格/币种、MOQ、规格、交期、证据和时间，不能用一个可信度大数字替代事实。对比工作区同字段顺序且明确规格差异；成本复核仅在关联机会时出现，展示已生效输入、待复核输入与利润运行的不同身份。记录删除为次级危险动作，报价确认/采购是对象级主行动。

## 4. 移动构图方向

按记录选择→当前详情→报价操作渐进展开，长名称可换行；选中记录、返回路径和已选供应商数始终可理解。对比不能把五列缩成不可读小字，可按供应商切换且保留同字段顺序；最终交互须审稿后实现。底部选中栏留安全间距，不遮挡原页、弹窗或软键盘；多步骤成本表单与采购原因不得挤在狭窄横排。所有字段/业务能力保持，不新增后台分页或保存偏好。

## 5. 所有动作与层级

SC-S-OPEN/CLOSE/SUBMIT管理四类显式输入；SC-SEARCH修改本地query并同步q；SC-DETAIL读取record；SC-REFRESH POST当前记录/refresh；SC-DELETE-OPEN/CLOSE/SUBMIT为原因软删。SC-QUOTE-OPEN/CLOSE/SUBMIT确认新不可变版本；SC-SELECT最多五个quote ID；SC-COMPARE在2–5选中时保存。SC-PURCHASE-OPEN/CLOSE/SUBMIT以当前quote_id、数量及trim原因排队采购。SC-NAV区分供应入口、费用规则、机会、受权采集明细；SC-SOURCE/ERP打开真实外部证据页。共享UiStatePanel的主/次动作均按当前态记录，不凭按钮名称猜测登录或重试。

SC-COST-SUBMIT提交指定复核人、精确成本字段和expected_version；SC-COST-RECALCULATE只排队；SC-COST-REVIEW按reviewId、decision、trim原因、review.version提交；取消只收起本地内联表单。全部77个本地/共享候选的语义归属见合同，不自动把P18共享动作重复计入全站分母。

## 6. 表单、弹窗和失败

四个本地role=dialog：找货、报价、采购、删除；另有成本提交与复核内联表单，不冒充弹窗。找货input_type为keyword/image/opportunity/product_url，input_ref必填≤2048；取消清create query但当前草稿保留，重开不自动重置。输入类型切换仅改标签/inputmode，不上传图片，不新增URL解析或机会选择器。

报价提交candidate_id、MOQ≥1、规格、交期≥0、所在地、可信度0–100、stable/variable/unknown、low/medium/high/unknown、观测时间和证据ID。openQuote按候选重置，当前缺值预填MOQ=1/交期=7/可信度=80属于已有输入默认，不是采集事实；是否调整须单独明确，不默认为已确认。datetime-local按浏览器时区显示原观测时刻，再转UTC提交。datalist仅建议当前候选/ERP证据，用户仍可输入任意文本，最终证据归属由后端校验，不能说前端阻止了越权。

采购显示锁定版本、MOQ、证据；数量整数≥MOQ，原因trim至少2字。打开采购重置数量/原因；成功仅提示queued，不是已采购。删除原因当前原生required/≤500，handler trim后空值不提交；关闭不重置原因。四窗仅有初焦点及局部Escape监听，完整焦点圈/归还/错误可达未验，不能称无障碍通过。POST失败保留表单，但父级notice可能被模态层遮挡；关闭期间迟到写入、重复提交仍列SC-G03/SC-G04。

## 7. 状态与边界

必须覆盖加载、真空、搜索无结果、正常有/无报价、2–5对比、超限点击、来源受阻/空成功/失败、详情失败、401/403、限流、成本待复核/超时/已通过/驳回、利润不足与重算中。蓝图没有blocked_login找货状态，不添加登录续期入口。加载目前串行先列表再对比历史，历史GET失败会整页非ready，与Feature Map“可选降级”不一致；明确记SC-G02，未实施为独立降级。

本批修复SC-SELECT：点击第六个复选框后，原生checked立即同步到实际selectedQuotes；不再显示六个已勾选而只发送五个。仍可取消已有选择，最多五家不变。源服务条款、公开采集来源和采购消费不在本次产品修改范围。

## 8. 状态连续性

mounted读q/create/opportunity_id；load按record/当前选中/首条恢复并replace record，detail切对象清选中报价。query变化replace q且移除create；没有对record/q的完整路由反向监听，KeepAlive和异步读取归属仍待验。比较成功清选择并load；refresh/delete先设notice再load会清提示，成本提交/复核/重算同样有提示被load清除的边界。不凭文字“自动回填”推断轮询，当前组件无定时GET。范围切换依真实壳层键，但迟到读写仍需独立验证。

## 9. 响应式、主题与动效

默认1440×1000、390×844，补768/1024及520/820邻界、200%缩放、长字段/多条报价。共享三主题与两密度按正式方向重新设计；正文16px、元信息13px、44px热区、清晰键盘焦点，禁用有原因，不只变颜色。动效只做反馈且遵守reduced-motion。当前两项局部产品修复不等于本页新视觉实现。

## 10. 验收与审图

复用m04-06原9项；新增UI2-SC04四类输入的required/取消/重开草稿与准确两字段POST，SC05报价版本/MOQ/原因/queued，SC06选择1/2/5/超限与精确报价ID。SC06先复现第六框假勾选再修复；这只是隔离Vue行为，不证明真实报价有效性、审计/Outbox或采购消费。既有非UTC用例继续验证原报价时刻。

正式design/{desktop,mobile}/P21.png、implementation对应槽位及四窗/内联复核各适用态图仍待方向审核后交付；按PLAN关联SC动作及用户意见。真实后端须覆盖跨范围拒绝、证据归属、当前报价/小于MOQ拒绝、双人复核、任务消费和清理。SC-G01–SC-G08未关闭前不称整页验收完成。
