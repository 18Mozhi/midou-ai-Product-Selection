# P51 · 采集任务

状态：事实规格完成；[COLLECTION-TASKS-C-r1首轮图册](../design/collection-tasks-direction-c/README.md)已交待审；[详情可达性与重放归属](../P51-INTERACTION-OWNERSHIP-IMPLEMENTATION.md)已进入生产 Vue，C 视觉与完整验收待交。图稿基线main/b3039224；事实起点main/0ba784a；[采集三页合同](../collection-runtime-contract-review.md)记录局部修复、真实动作与未验项，不能据规格、交互修复或选C方向计为具体稿通过。

## 1. 角色、范围与路径

`/platform-admin/collection`，CollectionRuntimeSurface → CollectionTaskCenter；platform_admin壳层，路由capabilities列platform:operate/platform:superadmin，cachePolicy=preserve。列表、详情和重放API实际均要求collection:replay。菜单可达不等于API放行；不改当前权限。列表不自动传组织/工作区过滤，不能按成员当前范围推断平台列表已隔离。

## 2. 主任务与真实数据

GET `/platform/collection/tasks`传page、page_size=50及非all状态；meta.total为服务端总数。文本输入仅在当前响应页内按任务/组织/工作区ID及错误码大小写无关匹配，不请求全库。四项指标均从当前页tasks计算，文本筛选不改变指标；不能画成全站统计。

GET详情返回task/subqueries/attempts/events/dead_letter。逐来源展示必需性、状态、可用结果、缺失字段、真实开始/结束与耗时，result_kind由事件投影区分empty_success/no_new_content/parse_failed；缺元数据不猜测。robots只展示已返回的判定版本、依据、规则预览；不读取或展示原始target_json、租约令牌和凭证。

## 3. 待审桌面构图

围绕“定位异常任务→核对逐来源事实→进入正确恢复路径”组织工作面。筛选范围与服务端总数在队列旁，当前页摘要明确标范围；详情主体优先覆盖/缺失与下一步，其后尝试/事件/技术记录。列表与详情关系、主操作位置重新设计，不继承旧四卡加长表的默认构图；没有创建普通采集任务的现有按钮，不补造。

## 4. 待审移动构图

任务摘要保留状态、证据、覆盖与更新时间，完整标识进入技术详情。现有ResponsiveDataView先开记录详情，再点“打开完整任务详情”进入任务模态面；新稿必须解释两层关系及返回位置，不能仅把桌面宽表压小。长来源、缺失字段、robots预览和事件列表局部展开，操作与底部安全区不相互遮挡。

## 5. 动作与按钮

CL51-LOAD刷新/错误重读/状态面重试；FILTER当前页文本及服务端状态；PAGE前后页；DETAIL桌面查看/移动完整详情、失败重试、关闭/遮罩/Escape/URL返回；RECOVER状态相关恢复链接与重放锚点；TECH原生详情展开；REPLAY原因输入、预览确认和提交。共享采集三链接另列于合同，不重复算三页新增动作。按钮六态按适用性出图，不因只读页面而漏掉链接和移动记录入口。

## 6. 弹窗、输入与重放

本地一个role=dialog任务详情，含loading/error/loaded变体；共享ConfirmDialog为死信重放alertdialog，另有移动记录抽屉。三个v-model为query、status、replayReason。原因trim后至少2字、输入maxlength=500；后端同时检查原始长度≤500。此调用未传destructive，默认false，没有影响勾选；输入“确认重放”后，POST `/platform/collection/tasks/{id}/replay`仅reason，幂等键由共享API客户端提供，保存中单飞。此前“勾选影响”表述与真实源码及E2E不符，本次更正事实，不改变共享组件。

服务端只允许dead_letter且有子查询的任务，事务创建scheduled新任务、克隆原子查询目标与顺序、旧任务改manually_replayed、开放死信改replayed，并保留历史与审计。同actor/route/key返回既有结果；不是原地重新启动旧任务。成功切到新task URL并刷新列表；本批不真实重放外部采集。

## 7. 状态、错误与恢复

首次loading/empty/ready/expired/forbidden/blocked/error；列表及详情15秒超时。显式刷新已有列表失败保留快照并提示；重新首次加载失败走状态面。详情每次读取清旧对象，成功与失败都以detailSequence校验归属。UI2-CL51覆盖移除task查询后的迟到成功/非重试404失败不得重开详情。

dead_letter进入原因区；blocked_login/captcha去凭证；blocked_robots或source_changed去来源频道；failed_terminal/partial警告去总览。链接是恢复入口，不是依赖恢复成功证据。retry/rate-limit显示任务级available_at，不能伪造逐来源退避时间。后端automatically_replayed已支持，当前下拉缺该选项，列待补核对而不称全部状态已可筛。

## 8. URL、关闭与异步归属

page/status初始由URL读取，改变时replace且保留其他query；文本过滤不写URL。同路由back/forward现由路径受限watcher恢复控件并重新读取，不依赖组件重挂载；每次列表GET固定page/status和递增代次，新读取abort旧请求且旧分支无权提交。查看详情push task，浏览器返回删除task；task缺失/无效分支abort并递增详情序号，阻止旧读取重新占用详情。再次前进会重新读取；closeDetail从列表打开时back，直达时replace删除task。列表越界有数据总数时回退有效页。

KeepAlive离页若列表读取仍在等待，现会中止该客户端请求、隔离旧代次，并仅在返回任务页后恢复一次读取；已有快照保留，非中断返回不强制刷新。page/status同路由历史和快速状态切换的旧读取覆盖已由双端局部用例覆盖；空服务端结果已保留筛选和返回全部状态入口；详情关闭/切换中的重放POST归属与原因草稿清理已有双端局部覆盖。没有新增缓存、偏好持久化或统一丢弃草稿规则。

## 9. 无障碍与布局边界

详情三态现有常驻名称、描述和关闭初焦点，自定义Tab循环排除无布局框、hidden、aria-hidden和visibility:hidden元素，并保留body滚动锁。原因错误与字段已关联；嵌套重放确认打开时底层详情inert，Escape后返回原操作。移动记录转完整详情时以原任务记录按钮作为稳定返焦目标，不再指向即将卸载的二级按钮；opacity/clip等隐藏策略、完整读屏和缩放仍独立待验，不宣称全模态可访问性完成。

正式验收依PLAN覆盖1440/390、中间断点、200%缩放、长文本、44px热区、三主题两密度及reduced-motion；现有翻页smooth滚动也需核查。CL51只证明本次历史返回读取边界，不代替全部键盘或读屏验证。

## 10. 验收与图槽位

永久测试入口`tests/e2e/m03-05-collection-tasks.spec.ts`；现有用例涵盖详情、失败恢复、RSS分类、分页、状态与重放，新UI2-CL51以确切请求终态同步迟到两分支，再验forward/close。事实合同中保留真实API/数据库、权限和外部执行未覆盖项，结果见PROGRESS。

首轮C稿已交：63场景，1440/390队列、20任务状态、当前页无匹配、服务端空、刷新失败/超时；移动摘要/记录；详情loading/error/loaded、逐来源缺失/robots/重试、原因及确认校验/处理中/失败/成功新任务，长窗连续分图见图册。受控源函数已复现快速切状态丢请求、确认目标/原因漂移、旧重放覆盖新详情；锁定/快照/代次/未知结果及常驻关闭和焦点保护仅提案。原重放夹具保留旧尝试与死信的矛盾显式说明，不冒称真实新任务履历。初始深链重载、真实Vue与API/MySQL/Worker、完整主题/生命周期和具体稿用户审核待交；静态原型history测试不替代上述验收。

2026-09-12交互增量：生产 Vue 已补齐 `automatically_replayed` 状态筛选；详情三态使用常驻标题/描述/关闭入口，可见控件焦点循环与 reduced-motion；重放固定提交目标/原因，关闭或切换详情后迟到成功只刷新列表与页面提示，不重新打开详情，网络/超时/限流/网关类未知结果不再误称未执行。新增双端 E2E 覆盖上述边界。本批不改变 API/任务状态机/权限/数据库/配置，不部署；服务端空结果筛选、嵌套确认焦点、page/status 历史、完整 C 视觉与真实链路仍待。

同日追加：服务端0条仍显示队列范围及两项筛选；指定状态空结果可返回全部状态，全部状态为空只重读且不造创建入口。重放确认期间底层详情inert，Shift+Tab不落入禁用确认按钮，Escape取消后返回原操作。双端用例覆盖，API/确认短语/状态参数不变；page/status历史、移动二级入口和完整视觉/真实链路仍待。

同日再追加：同路由 page/status 历史变化通过路径受限 watcher 恢复；列表GET固定page/status快照并以代次隔离，快速切换时新请求abort旧请求，旧成功/失败/finally均不覆盖新状态。旧代码双红，修复后双端定向覆盖；URL/GET/状态/分页合同不变。KeepAlive离页读取恢复、移动二级入口、完整视觉和真实链路仍待。

同日继续：KeepAlive离页会中止尚未完成的列表GET并标记返回续读，回到任务页后仅恢复被打断的读取；手机记录抽屉转完整详情时，关闭后返焦原任务记录。旧代码双端定向3失败/1通过，修复后4/4；缓存策略、路由、共享抽屉合同、API和业务动作不变。完整视觉、真实链路、主题/缩放/读屏仍待。
