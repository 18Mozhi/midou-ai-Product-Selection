# P53 · 网页登录采集运行

状态（2026-09-24）：用户已授权剩余页面/状态视觉自动通过；C 方向已接入实际 Vue 并随线上构建 `06ae38230de2b5b7a35b89727aa22432387030d5` 发布。桌面/390px E2E 各 8/8，健康/深链与专属 JS/CSS 哈希通过，详见[P53实施记录](../P53-PAGE-COMPOSITION-IMPLEMENTATION.md)。真实权限/API/MySQL/Python/浏览器运行、租约回收与正式 M07-03 仍需独立验证。事实起点main/0ba784a，新稿起点main/5a0dc0b3；[采集三页合同](../collection-runtime-contract-review.md)区分浏览器运行、业务采集任务、凭证期限与真实登录。

## 1. 角色、范围与路径

`/platform-admin/collection/browser-runtime`，CollectionRuntimeSurface → CollectionRuntimeCenter；平台壳层、preserve，目录capabilities列platform:operate/platform:superadmin；GET与回收POST均核验collection:replay。不是成员浏览器管理器，页面无启动浏览器、下载档案或编辑凭证入口。

## 2. 主任务与真实数据

GET `/platform/crawler-runtime`传page、非空q、非all状态。service默认每页25；q trim后≤160且禁止控制字符，只搜索运行ID/error_code/request_id/trace_id，不搜组织、工作区或来源名称。状态为running/succeeded/succeeded_empty/blocked/failed/timed_out/cancelled。后端纠正超末页页码，前端同步URL。

profiles全量元数据与run_metrics全量统计不受运行列表q/status筛选；列表pagination.total才是当前筛选数。abnormal只数blocked/failed/timed_out，duplicate_risk按lease_expired；不是所有取消记录或已证实重复执行。活动租约统计所有lease对象，包含过期占用；过期风险按observed_at与lease.expires_at比较，不是直接检查OS僵尸进程。

login_status来自绑定资产expires_at与服务器时钟，不是即时登录探测；期限为空为unknown。前端到期预测以observed_at计算，ceil天数且≤7天提示即将到期；不能以“有效”宣称目标站已登录或可采集。last_failure是最近blocked/failed/timed_out/cancelled记录，不一定是当前仍未恢复故障。

## 3. 待审桌面构图

围绕“确认占用与观测时间→定位异常运行→核对安全恢复条件”组织。档案状态、凭证期限、租约占用三类事实分开展示，不用同一个绿标合并为健康。全局摘要与筛选运行结果附各自范围；回收入口贴近过期占用说明，技术ID按需展开。重新设计关系布局，不照搬任务队列或来源总览。

## 4. 待审移动构图

档案卡优先身份/站点/占用与到期，运行摘要优先状态/数量/时间/错误；详情展示完整数量、耗时、结束时间和技术标识。查询与分页就近，确认范围不被长档案名和移动键盘遮盖。不能仅将桌面表缩成横向滚动，也不能因小屏删掉恢复前的风险说明。

## 5. 动作与按钮

CL53-LOAD刷新及UiStatePanel重试；FILTER查询/状态/重置；PAGE前后页；DETAIL移动运行详情及技术展开；RENEW过期档案的“处理续期任务”跳P51?status=blocked_login，不自动选择特定任务；RECOVER预览/取消/确认过期租约回收。没有本页凭证上传、轮换、编辑档案或真正浏览器启停动作，不据旧概念图补造。

## 6. 输入、弹窗与真实回收

两个v-model为queryDraft、status；query仅提交时从draft复制。共享ConfirmDialog标题为“回收所有已过期租约？”，未传destructive，默认false，因此只输入“确认回收”，没有影响勾选（更正旧规格）；父saving防重复、无过期风险时主按钮禁用。共享ResponsiveDataView另有运行详情抽屉；本地无独立role=dialog。新稿桌面入口与两原生dialog的焦点保护属于提案，不是共享Vue已完成证明。

POST `/platform/crawler-runtime/recover-expired` body={}，Origin及幂等检查保留。服务器按当次时间锁定所有过期profile租约，不受当前搜索、页码或状态筛选限制；把其中running运行改timed_out/lease_expired，写事件、清过期租约并保存幂等结果。不会停止有效租约，不等于结束OS浏览器或重新执行任务。返回recovered后刷新，不删历史、凭证或档案；本批不执行真实回收。

## 7. 状态、错误与恢复

首次loading/empty/expired/forbidden/blocked/error；profiles存在或全量run_metrics.total非零就ready，所以筛选无结果可保留档案与全局摘要。读取单飞15秒，有observed_at时刷新失败保留快照并提示；未知/未设置期限/无记录不混同失败。

回收成功与后续GET失败现分成两个稳定通知，服务器返回数量与新快照是否取得不再互相覆盖。没有HTTP响应的回收现标为结果未知并锁定再次提交；明确HTTP失败继续展示服务端action_hint。租约过期仍只表示重复执行风险，应结合任务证据核查，不承诺回收后采集已恢复。

## 8. URL、内存与异步边界

初始及同路由历史q/status/page均恢复已提交范围；查询/重置/翻页用replace重建本页query，修正页码后再次同步。GET固定读取快照与代次，新读取abort旧请求；KeepAlive离页中止等待GET，返回才续读。回收POST离页不取消、不后台GET，结算暂存到返回页面再核对；不新增持久化。

回收是全局过期集合操作，不能从当前筛选记录伪造影响数量，也不自行增加选中档案参数、阈值、原因字段或业务规则。需真实执行时另核对隔离目标和授权。

## 9. 无障碍与断点

查询label与maxlength明确，分页nav命名，读/写通知使用两个稳定的polite状态区。档案可用/停用、期限和风险同时有文字，回收按钮就近说明禁用原因与全局执行范围。共享确认打开时底层P53原生inert，初焦点、Tab/Shift+Tab、Escape和关闭返焦已有双端回归；运行抽屉继续由共享组件合同覆盖，桌面表与移动详情的字段差异仍不能豁免。

正式图依PLAN验证1440/390及中间断点、200%缩放、长错误/档案名、44px热区、三主题两密度；不因本页本轮未改源码就宣称全部可访问性或视觉已通过。

## 10. 验收与图槽位

现有`tests/e2e/m03-04-playwright-crawler.spec.ts`为本页Vue入口，真实采集生命周期、SQL独占租约与Python链有独立模块/集成测试。本批只核对源合同，未变化不重复运行其业务测试；隔离API回收截图不能抵扣真实浏览器任务或生产恢复验收。

首轮正式图已提供：双视口全局监控与筛选列表、无档案/无运行/搜索无结果、有效/过期/未知期限、活动/过期租约、最近失败、移动运行详情、分页、回收确认/禁用/处理中/失败/成功但刷新失败、首次异常与旧快照提示。所有图源为原E2E与明确合成数据；凭证明文、Cookie、租约令牌不进入图、trace或日志。当前生产Vue新增五组双端回归，完整本页16/16；图册108张同步当前源桥接且零HTTP。真实SQL/回收/浏览器与用户具体视觉、全主题验收保持待办。
