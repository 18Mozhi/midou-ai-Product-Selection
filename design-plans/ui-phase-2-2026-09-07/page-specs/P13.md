# P13 今日工作 · W03实施规格

2026-09-09逐项语义：[P13清单](../action-reviews/P13.json)及[今日工作审核入口](../WORK-SEMANTIC-REVIEW.md)补48局部源位置/36组，6模型/7结构容器，3原生dialog展开7业务变体；复用66图。明确today入口排除导出/详情编辑，源confirmBatch等待间可变字段及缺busy函数防护已隔离复现但未修。174视觉槽未逐selector映射，具体批准/真实Vue与生产验收不提升。

状态：C方向已选，WORK-C-r1独立图稿待审核，非设计通过/实现完成。入口 `/work` → TaskWorkspace(mode=today) → TaskListPanel/TaskBatchActions；详情为独立P24。共用动作见 [任务合同复核](../task-contract-review.md)。

2026-09-08首轮稿：[33场景双端66图](../design/work-direction-c/README.md)，含2张非业务控件板。个人状态目录、七弹窗、权限/读取与合成分页已独立出图；原始列表2/汇总7差异明确。源码VM/SQL构造和原型验证不代表Vue/真实后端/用户批准。未获审的真实结果、全部主题/密度及全站链仍待补。

## 1. 用户、进入与离开

task:read成员在合法组织/工作区查看自己的任务。列表行跳 `/tasks/{id}?from=<完整/work URL>`，详情关闭还原原筛选。新建/更新/分配分别由task:create/update/assign控制，不从角色中文名称推断权限。

## 2. 主任务及字段来源

主任务是找到本人下一项需处理工作并进入详情。GET `/tasks?page=…&page_size=10&mine=true`，不是仅今天到期；可带status/query/sort。GET `/tasks/summary`在MySQL按当前actor统计本人当前工作区全部状态及逾期，不受当前页搜索过滤。GET `/tasks/member-options`为转交提供真实成员目录。任务标题、描述、优先级、状态、进度、期限及SLA来自任务事实；无期限明确未设置。

## 3. 桌面新构图要求

C方向采用蓝色本人状态目录、白色逐项任务队列，独立区分本人全部状态汇总和筛选结果；不继承旧账页的纸张/宋体/朱砂或横向导航。P13强调个人行动，P23强调工作区检索，不复用旧图只换标题。不自动推荐第一项，四种排序沿现有合同。WORK-C-r1为首轮正式待审提案，不是最终通过稿。

## 4. 移动新构图要求

状态/期限/下一步完整可读；查看与选择为不同热区；批量操作仅在选中后出现。筛选可渐进展开，主操作和弹窗按钮避开底栏及软键盘。具体布局随方向审核确定，不用缩字号掩盖空间问题。

## 5. 按钮及动作层级

查看任务为行主入口；新建按capability提供。状态六种、搜索应用/重置、排序四种、分页、本页/单行/清除选择、单行删除、五种批量操作全部纳入task合同表。today模式不显示业务/导出Tab，但不改变已有其他壳层入口。

## 6. 弹窗及字段

本页拥有新建、删除、五种批量变体；详情编辑及五种单项表单归P24。输入、版本、原因、日期与取消行为按合同复核第4节。新建采用TaskWorkspace内联表单，不建立不存在的TaskEditForm依赖。

## 7. 状态

加载、无任务、筛选无结果、读取失败、无权、过期、限流及成员目录不可用分别验收。不存在任务的详情归P24。未设置期限、逾期、进行中/暂停/终态、批量无可执行项不能省略。每种状态的动作可见/禁用和恢复路径需留独立结论，本轮仅部分验证。

## 8. 状态存储与连续性

status/query/sort/page沿URL；筛选/翻页清selectedIds；搜索框本地草稿不持久化。快捷创建query用于预填，关闭只清create/title/description，其他query保留。详情from接受/work或/tasks及其query；不能自建任意返回地址。跨组织状态仍需专门回归。

## 9. 响应式与视觉

WORK-C-r1已有1440/390双视口图、长中文、320/768/780/781/1024与390×667代表检查；三主题/两密度、原生200%缩放与软键盘仍待验。控件至少16px、元数据13px、选择标签/菜单热区44px；减少动态效果和焦点有原型检查。此证据不计P13已审，也不抵扣真实Vue。

## 10. 验收与证据

新增UI2-T05核对mine与返回URL；其他共享表单/批量见UI2-T01–T08及原m05-01测试。只证明隔离响应下的Vue合同，不是实时MySQL或生产验证。用户负责方向/图审定，实施者负责自检；本页仍待最终设计、全状态/角色和真实链路验收。
