# P37 组织审计 · 只读追踪

当前实际Vue整页C：[四宽度76图](../P37-PAGE-COMPOSITION-REVIEW.md)。左侧蓝查询、右侧数量/时间线/详情，手机纵向；用户已授权剩余视觉方向通过，但这不代表整页交互、真实后端/RBAC或生产验收。2026-09-25 修复同实例路由查询、浏览器历史和 KeepAlive 返回时的筛选恢复，详见 [查询恢复复核](../P37-QUERY-RESTORATION-REVIEW.md)；七条审计 E2E 桌面 7/7、390px 移动 6/6 通过，另有一条桌面专属超时场景按既有规则跳过。更早的“待修复”段落是修复前证据，不覆盖本次结果。完整无障碍/壳层/SQL/权限及73页交付仍继续。

2026-09-09独立稿：[ORG-AUDIT-C-r1](../design/org-audit-direction-c/README.md)，50场景双端100主图＋4局部图，共104PNG，零业务弹窗。蓝精确条件/白时间线和详情、手机折叠定位；原55条与独立40连接/10业务夹具不拼接。源函数/六搜索ID/七条件/50游标/键名脱敏及惰性SQL有离线证据，非实际Vue/API/MySQL/权限。复制归属及旧页追加新列表已复现；保护、旧事实查询标签和错误可读性只在提案。具体稿、OG-G及完整主题密度/生命周期/全站实现部署待办；下文“图待交”是原规格，首轮设计现已交付，不代表实现获验。

状态：W04事实规格；`/org-admin/audit`，OrganizationAdminCenter→OrganizationAuditPanel；[组织治理合同](../organization-governance-contract-review.md)。

## 1. 用户及范围

audit:read、当前组织。审计员可进入本页，父层特意不读organization:manage摘要，不让无关403覆盖合法审计。服务端组织ID与当前范围授权仍是边界，普通成员与跨组织请求拒绝。

## 2. 主任务与真实数据

GET /organizations/:organizationId/audit-events?limit=50，items/nextCursor。统一platform_audit_events与已提交audit_logs，后者outcome为succeeded；不是复制或迁移历史记录。字段id/action/resource_type/resource_id/outcome/actor_id/workspace_id/request_id/trace_id/metadata/occurred_at/schema_version；计数仅当前已加载，不是全库。

## 3. 桌面方向

以时间线和证据阅读为主，精确筛选与已加载搜索明确分区；选中记录展示结果、请求关联与脱敏上下文。系统连接记录折叠为次级，不删除其事实；重构不默认继续旧账页和大指标卡。

## 4. 移动方向

列表摘要先动作/结果/时间，再详情；高级请求/时间条件折叠且错误可达。JSON与长追踪号局部滚动，不撑整页；复制按钮标明字段，避免两枚无上下文“复制”。

## 5. 全动作

OG-AUD-QUERY已加载搜索；OG-AUD-ADVANCED原生details；OG-AUD-FILTER应用七项服务端精确条件；OG-AUD-RESET；OG-AUD-SYSTEM展开/收起连接记录；OG-AUD-SELECT；OG-AUD-MORE下一游标；OG-AUD-COPY请求或追踪ID；OG-TECH；父刷新/重试。无编辑/删除/导出/任务重放、本地dialog或写入按钮。

## 6. 条件及详情

action/outcome/resource_type/request_id/trace_id/occurred_from/occurred_to，文本trim、日期转ISO；开始晚于结束本地拒绝，不发GET。form novalidate，服务端仍校验非法条件。应用替换列表，loadMore追加items/nextCursor；页内搜索不发请求且会隐藏更多按钮。详情aside非模态，无关闭确认；metadata敏感键名递归脱敏，数组最多100、深度>8截断，不能将其宣称覆盖任意非敏感键下的秘密值。

## 7. 状态与反馈

成功/失败/blocked三结果及未知原值；真空、页内无匹配、仅系统记录、游标到底、错误/离线/超时/过期/无权。realtime.connected默认折叠但计数保留，页内搜索或精确action筛选时直接展示。复制失败与成功按字段反馈，不写审计事实。

## 8. URL和请求归属

org_audit_query/selected加action/outcome/resource/request/trace/from/to，原始cursor不存URL；刷新重读第一页。初始URL恢复和可控游标已有用例；watch主要本地→URL，完整后退、无效选中、刷新与loadMore并发、KeepAlive/组织切换仍待验。选择变化清复制状态，但异步复制完成归属待确认。

## 9. 无障碍与效果

选中行button使用role=listitem的语义待复核；系统展开需可访问展开状态/目标关联；高级日期报错关联字段、详情键盘可达、复制区字段可区分。四视口、200%缩放、深层JSON/长ID/仅连接事件、三主题两密度与44px热区；无写入窗记不适用，不新增危险恢复捷径。

## 10. 验收与图

复用m06-01服务端筛选/游标/URL、连接事件折叠、时间范围、审计员权限、空/HTTP/离线/12秒超时。真实双表合并顺序、跨组织游标拒绝、脱敏全链与生产权限仍另验。design及implementation各desktop/mobile/P37.png、列表/详情/高级筛选/各错误态图待交；现有局部测试不是全新视觉或生产通过。
