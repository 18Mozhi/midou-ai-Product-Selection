# P62 · 链路日志

2026-09-15 [读取键盘批次34](../P62-READ-KEYBOARD-BATCH34.md)：保留已有重试按钮经过loading；两个只读按钮用aria-disabled维持焦点和原单飞，成功移除重试区前仅交接仍归属自己的焦点，不抢搜索/筛选窗焦点。四组270检查、36合同、默认77及前端构建通过；r4双端22图/168来源待审，r2/r3为截图范围对照；下文“读取焦点未修复”为旧批次事实，完整读屏/生命周期/权限与导出仍待办。

2026-09-15 [读取反馈批次33](../P62-READ-FEEDBACK-BATCH33.md)：实际Vue首次超时不再误称保留旧日志，原15秒/重试/保留规则不变；读取区域命名与busy补齐。四组200检查、24合同和前端构建通过，r1双端18图/168来源，手机首次超时区域待审；默认77项回归通过。下文首次超时文案为旧事实，焦点连续性/生命周期/真实权限与导出仍未完成。

2026-09-15 [实际Vue组合批次32](../P62-PAGE-COMPOSITION-BATCH32.md)：四个共享组件与原业务script接入审核宿主，蓝色链目录/白色事件区及筛选、手机详情和原原因取消出图；r3双端21图/77项/6本地GET，16项合同通过。生产文件未改，手机筛选区域待审，原型中的冻结范围等提案尚未实施。当前共享详情已有Tab首尾处理代码，以下“未实现完整Tab圈定”为旧事实，本批仅验证关闭返焦；全生命周期/真实导出/权限/生产接入仍待办。

状态：事实规格完成；[LOGS-C-r1首轮具体稿](../design/logs-direction-c/README.md)已交61场景130双端PNG，C方向已选、具体稿待审；真实Vue/全生命周期/生产验收待交。原事实起点main/5695ae7，设计接续main/b5c39aab；[B3a合同](../log-backup-release-contract-review.md)为源码候选与未验项索引。不修改该页产品代码，不运行真实导出。下文“正式图待交”为原事实批次状态。

本轮蓝色链索引与白色选中链事件工作区，手机横向链目录和完整详情；七列/每链独立设置、trace/请求/记录技术展开、准确异常跳转、筛选与导出原因全状态。原E2E三事件双链单独保留；合成11事件、缺trace、200条及长字段降序窗口不冒充生产，组内按真实computed正序。七组源隔离检查、12相关历史hash绑定；记录范围、迟到ID、原因冻结/2–300字错误和手机焦点仅待审提案。模态/七列最少一列/三运行面/源码与图指纹/六断点/CSS200%为原型证据，不证明真实Vue、SQL、鉴权审计、浏览器缓存或用户已保存CSV。

## 1. 用户、范围与入口

/platform-admin/logs → PlatformLogCenter，platform_admin壳层，preserve缓存；实际GET和导出POST均要求会话及platform:operate。目录的superadmin声明不代替API能力验证。这里只读脱敏运营事件，不读取服务器原始日志文件或终端。

## 2. 数据与列表口径

GET /platform/management?domain=logs，query最长120，运行面URL用source而GET字段用status（api/worker/crawler或省略）。MySQL联合platform_audit_events、collection_task_events、crawler_browser_runs及providers/browser_collection_jobs关联，按occurred_at/id降序最多200条。不是分页、时间范围查询或全量日志搜索，summary只计实际返回条数及三运行面；原PAGES的时间范围/分页描述是拟议项，不作为已有能力。

前端按真实trace_id分组，缺失时用来源+记录ID形成独立临时键；每组发生时间正序。200条截断可能只含部分链，不能把组内首末时间当完整链耗时。API不猜task/provider；Worker有任务而无单一provider；Crawler须实际关联才产生任务/来源链接。

## 3. 待审桌面结构

新结构以“已应用条件→有限结果/链→事件证据→异常处理”为阅读顺序。结果旁明确最多200条和观测时间；检索输入草稿、当前请求和保留结果三个范围分开。链摘要解释仅为当前返回事件，不添加不存在的错误评分、完整链耗时或分页控件；技术编号可完整展开，避免以截短ID代替可核对证据。

## 4. 待审移动结构

筛选由ResponsiveFilterDrawer全屏承载；应用关闭，取消关闭但当前实现不回滚输入草稿。链摘要下为事件卡，详情保留时间、运行面、事件/状态、资源、错误和可用跳转。长编号局部换行；详情内跳转目标和返回入口明确，不保留旧桌面多列表格硬压到手机。

## 5. 按钮与输入

LG62-LOAD刷新/重新加载；LG62-APPLY form及检索；LG62-RESET清除两字段；LG62-EXPORT先填原因；LG62-TRACE/IDS/REQUEST原生details；LG62-TASK/PROVIDER桌面与移动异常链接，只在对应ID存在时显示。表格列设置、冻结首个可见列、标准/紧凑由每条链的共享TableViewControls提供，非后端筛选。

本地query/source两个绑定；原因共享组件第三字段。重置的activeFilterCount基于草稿，不代表已成功读取条件；检索以router.replace存query/source、保留其他URL参数，返回不为每次筛选新增历史项。无时间输入、页码、删除/重放日志按钮。

## 6. 导出与全部弹窗

LG62-EXPORT使用AuditedReasonDialog，默认原因为故障排查；原生dialog初焦点原因、取消/关闭/Escape返回null不POST；提交去首尾空白，前端至少2字，服务2–300字，当前textarea无maxlength300。提交后原因窗关闭，再POST /platform/management/logs/exports，字段query/source/reason。同源、Idempotency-Key和platform:operate服务检查；浏览器每次新调用由api-client生成键，不能称手动重试持久复用原键。

仓储先重新读同一条件最新200条，再对actor/route/key命名锁、事务查幂等结果；相同key不同条件409，重放返回保存结果，首次审计platform.logs.export及保存操作结果。不是导出当前DOM的不可变快照。CSV固定12列、UTF-8 BOM、引号转义及以=+-@开头时加单引号；下载文件名由浏览器日期生成，URL完成后revoke。原始metadata/payload/Cookie/stderr不导出。

其他模态是筛选抽屉和每条链的ResponsiveDataView移动事件详情；不能以局部没有dialog标签声称无弹窗。共享TableViewControls列菜单是details，不计成业务确认窗。

## 7. 状态与反馈

首次loading/ready/empty/error；成功零记录保留筛选和导出入口，空态重新加载。15秒读取超时，已有observedAt时保留记录及message；没有独立401/403状态分支，权限失败后的旧快照保留必须专门受控验证，不能沿用备份/发布页的清除结论。整个根aria-live=polite不等于各类状态均可读屏。首次超时仍说保留上次日志的文案适用性待验。

## 8. 生命周期与防重

load单飞、sequence检查、unmount取消；watch只看query/source未限定当前路径，preserve缓存但无deactivated/activated处理。URL切换发生在旧读取期间会被单飞早退，旧数据与新导出条件可能错配；待延迟场景复现。导出按钮在读取/导出中禁用，但等待原因时尚未exporting，提交后无额外handler单飞或AbortController；离开、取消后重开、同一原因重复提交和迟到下载归属均待验。本批不改变这些合同。

## 9. 无障碍与效果

输入有label，筛选抽屉已有初焦点、Tab循环、Escape和触发器返回；事件详情只已有开关/返回，未实现完整Tab圈定。原因窗300字限制、错误与原因字段关联、键盘取消及跨弹窗焦点需要回归。新稿包含四断点、200%缩放、长链/长ID、按钮六态、三主题/两密度和减少动效，不以静态扫描证明通过。

## 10. 验收与正式图

现有日志浏览器用例位于tests/e2e/m06-02-platform-dashboard.spec.ts；本批未重跑日志模块。后续图须覆盖三运行面/全部、无匹配/失败/保留旧链、事件技术展开/两类异常跳转、移动筛选/详情及原因取消/非法/提交/导出失败。真实CSV与幂等只在授权隔离对象验证，不用生产日志。正式风格F00待意见，零新增API/业务字段/生产服务。
