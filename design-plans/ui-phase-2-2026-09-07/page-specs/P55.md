# P55 · 质量与规则

状态：C方向已进入真实Vue；[批108当前Vue C组合](../P55-PAGE-COMPOSITION-BATCH108.md)整理既有实际双端证据待用户审核；[当前双端证据](../design/governance-direction-c/vue-implementation/)与[P55交互实现记录](../P55-GOVERNANCE-INTERACTION-IMPLEMENTATION.md)保持原始证据链，真实MySQL/RBAC/生产验收仍待交。[GOVERNANCE-C-r1静态提案](../design/governance-direction-c/README.md)继续按main/ce50835a冻结复现，不冒充当前实现。事实起点main/63a1ef0；[数据与治理合同](../data-governance-contract-review.md)为真实入口依据。路由标题虽为“质量与规则”，组件实际是跨组织治理目录，质量问题解决在P54，不能混为一个页面。

## 1. 角色、范围与路径

`/platform-admin/governance`，platform-governance-center → PlatformGovernanceCenter，平台壳层、preserve；目录capabilities为platform:operate/platform:superadmin，GET要求会话和platform:operate。跨组织读取不等于有原模块写入权限；详情后的跳转仍受目标页面/接口权限和当前范围限制。

## 2. 主任务与真实数据

GET `/platform/management?domain=governance&section=...&page=N&page_size=20`，非空query/status附加。五类score_rules/cost_rules/approval_templates/automation_rules/releases独立状态集合；query≤120、status≤40、page正整数、page_size1–100，页面固定20。后端COUNT筛选总量后收束页码，以updated_at/id倒序分页。

评分/费用以revision、version_code解释版本；审批优先current_version再revision；自动化使用version，含触发、严重度、动作、频控；发布app_version映射name、build_sha映射version_code、stage与status。评分搜索名称/版本/组织/工作区；费用另含市场/平台；审批含resource_type；自动化含trigger_event_type/action_title；发布按app_version/build_sha/stage。不能把全部搜索框说明当每分类完全相同SQL。

summary是五张业务表及provider_versions六个全量COUNT，不受当前列表筛选；provider_versions_latest_at是全局MAX(created_at)，observed_at为本次读取时刻。列表total不是摘要总量，不造综合健康分或容量指标。

## 3. 已实施桌面构图

以“选择治理对象→辨认版本/范围→查看依据→进入所属工作台”为阅读顺序。分类/版本与当前记录的组织归属是主信息，全局摘要低于任务焦点；来源配置历史是关联入口而不是第六种可编辑列表。重新设计导航、信息布局与详情结构，不沿用旧账页大标题加同款表格。

## 4. 已实施移动构图

分类切换可达，筛选抽屉显示已提交与草稿区别；每条摘要突出名称/状态/版本，详情补组织、类型、更新时间和特定分类字段。主操作为原模块入口，不虚构本地启停、审批或发布按钮。长版本/组织名、空结果、失败保留及返回状态都需独立移动图。

## 5. 动作与按钮

DG55-LOAD刷新/初错重试；SECTION五分类；FILTER应用/重置；PAGE前后页；DETAIL桌面原生dialog或移动记录抽屉、关闭/Escape；TECH三处技术展开；WORKBENCH五种分类入口及自动化编辑；PROVIDER进入来源版本管理。表格公共列显隐/冻结/密度和移动抽屉触发由共享组件供给，消费者仍独立验。

顶部入口按目标分类导航；行/详情入口绑定记录所属分类。本批修复等待/失败保留评分记录时错误生成自动化编辑链接的问题；正确链接不能以是否成功打开目标模块替代目标权限验证。

## 6. 输入、弹窗与原模块合同

局部仅queryDraft/statusDraft两个v-model。桌面查看详情直接使用已加载行，不发新GET；原生dialog展示对应版本、组织/工作区、状态、时间及存在的自动化字段。移动详情使用共享抽屉，现与桌面等价展示自动化触发、严重度、动作标题、动作类型、次数与窗口。`rate_limit_count=0`按返回事实显示为0，不推断为不限频率。

评分→`/opportunities/scoring-rules`，费用→`/sourcing/cost-rules`，审批→`/org-admin/approvals`，自动化顶部→`/automations`、行编辑→`/automations?rule=<id>&action=edit`，发布→`/platform-admin/releases`，来源版本→`/platform-admin/providers`。除自动化ID外不自行追加规则ID、组织或工作区参数；本页无POST/PATCH和保存弹窗。

## 7. 状态、失败与恢复

首次loading/expired/forbidden/blocked/error；成功observed_at作为保留事实依据。空列表仍显示全局摘要、分类、分页范围和来源入口；桌面表格与手机列表均有独立筛选无结果反馈。权限拒绝使用温和说明“当前无法查看治理目录”，不展示旧内容。

读取单飞15秒，失败保留原数据。本批新增成功快照范围：section/query/status及服务器实际page，仅成功时更新。旧记录表头、类型、版本、详情和行入口始终使用原分类；新范围等待或失败时通过role=status说明仍显示的原分类/搜索/状态/页码。刷新成功后切到新记录并清提示，顶部目标分类入口及筛选选项不改。没有改变SQL分页或任何原模块写入合同。

## 8. URL、内存与异步边界

初始section/q/status/page来自URL；非法分类/状态回默认，page仅1–4位数字；查询/分类/分页用replace重建query。分类切换清status和selected、page=1，搜索继续保留。成功页码以服务端纠正后回写。刷新失败时URL仍为请求目标，范围提示明确实际显示内容，不能误称目标已加载。

读取使用请求代次和AbortController；`onDeactivated`中止GET、关闭详情并保留成功快照，停用期间不发读取；`onActivated`返回后刷新且不重写URL，迟到响应不能覆盖新一代状态。请求中可编辑筛选草稿但不等于已应用条件；详情为行快照不宣称实时详情。跨组织自动化编辑落点、页面授权、浏览器长历史及更新后回到原筛选仍需真实链路验收，不通过本页自行切租户。

## 9. 无障碍与断点

筛选有label/maxlength，分类aria-current且读取时disabled，分页有命名nav。本地useModalDialog提供showModal、Escape取消和关闭后焦点返回；共享移动详情与筛选抽屉保留Tab/Shift+Tab圈定和返焦。当前双端夹具覆盖关键路径，但不代表真实读屏、软键盘或所有辅助技术完整通过。

当前Vue证据覆盖1440/390，静态提案另覆盖760邻界、中间断点、长版本/标题、CSS zoom2、三主题两密度和动效降级。P55局部以独立蓝白令牌隔离旧纸张/橙色组件味道；全局NavigationShell仍是共享旧外壳，须在后续批次单独重构，不能用本页局部通过替代全站视觉审核。

## 10. 验收与图槽位

2026-09-09静态首稿已交五分类列表/详情/技术字段/筛选/空结果、25分类状态组合、全局计数与来源历史、等待/失败/成功保留范围、20/21分页及长详情。该提案继续绑定ce50835a历史源码；服务和仓储分支只经隔离适配器运行，不是实际SQL/RBAC/导航验收。

当前真实Vue补交12张证据：治理目录、默认记录、自动化详情、筛选无结果、温和权限拒绝和读取失败保留快照，桌面1440与手机390各一张。UI2-DG55验证评分→自动化等待/失败期间原版本/详情/所属链接、Escape关闭，重试成功后自动化版本及精确编辑ID；新增夹具验证五分类、完整自动化字段、零频控、筛选空态、KeepAlive停用/激活与迟到响应隔离。模块m06-02-platform-dashboard、unit platform-governance为入口；原业务写入及真实后端另验。

当前23个局部候选已在[action-reviews/P55.json](../action-reviews/P55.json)归并为8组，包含每个页面按钮、路由入口、筛选表单、原生详情和技术展开；48个逐控件六态槽仍不以组合图冒充穷尽。工作台链接不是业务确认窗；真实目标权限、跨组织落点、读屏/软键盘、生产及用户具体审核仍pending。

## 2026-09-12 字体复核增量

[P54–P56 可读性复核](../P54-P56-TYPOGRAPHY-REVIEW.md)：治理 14 处辅助字号提高到 13px，实际 Vue 图已重拍；保持布局、字段和操作，当前图待审。
