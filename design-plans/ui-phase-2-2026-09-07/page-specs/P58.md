# P58 · 商业运营（配额管理）

2026-09-25 增量：[创建配额方案窗 C 方向真实 Vue 实施](../P58-CREATE-DIALOG-IMPLEMENTATION.md)。深蓝标题、桌面说明侧栏/白色七字段、手机单列，以及提交等待/拒绝的窗内反馈已接入真实组件；原请求/body/单飞/幂等/权限与后续流程保留。浏览器原生内部标识 pattern 修正为与服务端字符集等价。桌面/390px M06-06 E2E 各4/4，Web类型、两份历史图册合同8/8通过。提交 `69086515fbe0b3109075f0a7876ed7a9deb16330` 已部署；公开健康端点、深链与本页静态资源哈希复核通过。视觉沿用用户“剩余全部通过”的统一授权，不扩大为真实RBAC/写入/审计或整页验收。未知 POST 结果重提策略仍待产品决定；全项目收官继续。

2026-09-24 最新状态：按用户“剩余全部通过”授权，当前 C 方向工作区视觉通过。M06-06 商业 E2E 桌面与手机各2/2通过，相关单测13/13通过；生产 BUILD_SHA `84885c2e05f9d613999d76805b6b7812e37801ee` 下路由、父管理 JS/CSS 和 CommercialOperationsCenter JS/CSS 均 HTTP 200，SHA-256 与本地一致。此次只修复审核预览脚本对旧标题结构的定位，不改生产业务。真实会话/RBAC、真实配额写入和审计未验；创建请求结果未知后是否冻结原内容/幂等键影响写入行为，规格仍要求产品确认，不因视觉通过而推定。

2026-09-12 增量：[确认等待与拒绝](../P58-CONFIRM-WRITE-REVIEW.md)。前版 C 预览加入
operation 归属的窗内反馈、失败编号、等待关闭保护和键盘循环；提交才聚焦持续标题。
五类原写入路径、409/403、三宽度对照 60 组，各两次本地拒绝原键重试；原接口/内容不变。
反馈区域待审，真实写入/未知结果/生命周期/剪贴板与生产未验，不改变既有审核范围。

2026-09-12 增量：[十类确认窗实际 Vue 预览](../P58-CONFIRM-VUE-REVIEW.md)。方案三类、分配两类、
组织动作三类、人工调整/撤销共十入口，60 组三宽度原版/C 对照；保留原影响函数及提交行为，
只读展示真实 pending 原因/版本/方案资料；没有比较行时明确未提供，不伪称无影响。
仅 GET、预览后取消，真实执行/全交互/生产未验；本轮具体视觉待审，旧图包及旧审核不变。

2026-09-12 增量：[实际 Vue 编辑窗](../P58-EDIT-VUE-REVIEW.md)。版本/步骤与七字段分区，
按钮标为“预览修改影响”；原业务 script、验证属性及原确认窗不变。
6 组 168 检查、33 图、163 原始源，仅本地 GET；取消预览关闭修改，重开回原数据。
手机编辑组合待审，原确认窗设计/真实保存/完整无障碍/生产未验；创建未知结果策略仍待确认。

2026-09-12 风险：[创建重试内容/幂等键诊断](../P58-CREATE-RETRY-DIAGNOSTIC.md)。首次成功后丢响应，
改内容沿用原键会回放旧 id，生产与当前 C 预览仍显示新内容成功。6 项隔离诊断确认风险，
不是修复通过；未知结果冻结原提交策略待确认，后端合同与当前实现未改。

2026-09-12 增量：[创建窗键盘边界](../P58-CREATE-FOCUS-REVIEW.md)。同 C 预览加入局部 Tab 边界，
默认 10/错误折叠 11/展开 12 控件逐个双向循环；等待标题滚入可见位置，保留原生 H3 初焦点与返焦。
6 组 597 检查、30 图、168 原始源及 17 项联合单测通过；生产/共享模态/初始视觉不变，
其他弹窗与完整无障碍未验，此前具体视觉不自动通过。

2026-09-12 增量：[创建成功与目录核对](../P58-CREATE-OUTCOME-VUE-REVIEW.md)。独立实际 Vue 双区
反馈与 GET-only 重读，24 组 444 检查、66 图、167 原始源覆盖正常/409/403/503；
原 POST 表达式、查询及安全读取重试不变，13 项联合单测通过。
原错配提示/编号反例保留，截图增加导航遮挡检查；不代表生命周期、整页或生产修复。

2026-09-12 增量：[创建等待与拒绝反馈](../P58-CREATE-WRITE-VUE-REVIEW.md)。独立实际 Vue 预览，
12 组 216 检查、42 图、165 原始源；等待保护七字段/关闭、持续标题焦点、拒绝原因与编号放窗内。
原单飞/body/幂等键保留，409/403 全为本地模拟；生产未改，成功/未知/生命周期与完整验收未完成。

2026-09-12 增量：[实际 Vue 创建草稿窗](../P58-CREATE-VUE-REVIEW.md)。深蓝说明/白色七字段与
原因/底部操作，三宽度 6 组 114 检查、24 连续 PNG、162 原始源；全业务 script/原样式/指令不变。
仅预览等价修复 HTML pattern，生产未改。原生 Tab 的 BODY/滚动 dialog 中转如实记录，不宣称
严格焦点闭环。手机组合待审，保存/错误/编辑/确认/整页与生产验收继续；原 192 图不变。

状态：事实规格完成；[批111当前 Vue C 组合](../P58-PAGE-COMPOSITION-BATCH111.md)复验保留的 r1 双工作区 7 图及 5 项源码变换/编译检查，整体结构仍待审；[COMMERCIAL-C-r1正式审核图册](../design/commercial-direction-c/README.md)已交81场景192PNG，具体设计待审、Vue与全量验收未完成。事实起点main/75cb243，新图起点main/36917e5d。[B2c合同](../commercial-security-open-platform-contract-review.md)记录控件位置、请求与未验项。本批不修改该页产品代码，不把配额方案包装为收费套餐。

## 1. 用户、范围与入口

`/platform-admin/commercial` → CommercialOperationsCenter，platform_admin壳层、preserve；API会话及platform:operate。全部方案目录与选中组织的分配/周期/用量是两种范围；组织按现有UUID输入读取，未选择时只展示目录。保留真实入口，不新增组织选择API、计费、价格、币种或支付。

## 2. 数据与统计口径

GET `/platform/commercial`固定方案page_size=20、adjustment_page_size=10，传organization_id/query/status/page/adjustment_page。summary是全局方案数，不受搜索/状态/组织筛选；目录按active/draft/retired、updated_at/id倒序，服务端分页。assignment_count只计active/suspended组织分配。组织必须存在，否则404。

使用量分别来自collection_tasks.created_at、open_api_usage.occurred_at、report_exports.created_at，按组织与分配周期[start,end)COUNT，不按成功状态过滤。有效额度为当前方案基础值加本组织/本assignment的所有当前有效调整，按计量项合计后截零；不是可见历史页之和。历史调整按组织分页，可能含其他周期/已撤销/未生效记录；assignment本身状态与“当前有效调整”判定不可混用。

## 3. 待审桌面结构

方案目录与组织配额分成两条清楚的任务线：方案的创建→审核额度→启用；组织的读取→分配/周期→用量→调整记录。一个主行动随当前任务突出，其余分层；全局摘要不得暗示为当前组织统计。组织名称、周期、单位和已读取范围紧邻结果，方案目录筛选不挤占组织处置区。不默认保留账页构图。

## 4. 待审移动结构

组织范围和已读取标识先于分配/调整；方案行字段化显示三项额度、使用组织数、版本和动作。创建/编辑长表单单列，日期与原因可达；影响确认先对象、再前后值、最后执行。两套分页分别命名，不能把配额目录下一页当调整历史下一页；查询失败保留的旧结果必须与其原范围匹配。

## 5. 真实按钮与输入

CO58-LOAD刷新/重试；CO58-ORG读取/清除；CO58-FILTER查询/重置；CO58-PAGE与CO58-ADJ-PAGE独立分页。两处CO58-NEW打开同一草稿窗口；CO58-CREATE直接POST草稿；CO58-EDIT打开编辑后CO58-SAVE再转确认。行快捷启用只在draft、退役只在active；编辑状态select仍允许三状态，不把快捷按钮限制当服务状态机。

CO58-ASSIGN首次/变更都走版本化分配；CO58-SUSPEND/RESUME/END按active/suspended/非ended显示；CO58-ADJUST提交非零整数，CO58-REVOKE按调整原status=active显示，不代表当前时刻仍生效。

26处v-model：组织输入1、分配4、调整5、目录筛选2、创建7、编辑7。创建code1–80模式、name1–120、description≤500、三额度0–10亿整数、reason≤500；现有输入默认100/1000/20仅是草稿初值，不是已创建业务方案。分配select取当前页active方案并补当前assignment方案，不是全部方案远程搜索；date字段为datetime-local。调整delta±10亿且非0，effective空由服务使用当前时间、expires可空且有值时晚于effective。服务reason为trim后1–500，部分前端minlength=2，保持差异待核查。

## 6. 弹窗与实际写入

三个原生dialog：创建、编辑、影响确认，均经useModalDialog/showModal；编辑提交关闭编辑再打开确认。确认变体覆盖方案保存/启用/退役、分配/变更、暂停/恢复/结束、新增调整/撤销，不能只验一个标题。创建不额外二次确认，确认窗不另收原因，原因来自原表单或现有固定文案。

POST plans→201 draft；PATCH plans/{id}带完整名称/说明/三额度/status/expected_version/reason；POST assignments带organization_id/plan_id/period_start/period_end/expected_version/reason，新建或更新同一分配并变active；POST assignments/{id}/actions仅suspend/resume/end；POST adjustments→201，要求同组织未ended分配；POST adjustments/{id}/revoke要求版本和active。均同源、能力、幂等、事务审计；没有扣款或自动强制限额。关闭弹窗不取消已发事务。

## 7. 状态与反馈

首次loading/error/rate_limited/blocked；401/403在本页读错误均归error，不假称已区分专用状态。读取15秒、单飞/sequence，失败保留旧数据；首次超时也写保留旧数据，待复现。空目录区分全局无方案和筛选无匹配。mutating控制提交，创建顶部关闭/Escape仍可用；错误notice在父页，原生模态打开时错误是否可达待验。写成功后load吞错再覆盖成功提示、晚返回关闭新窗和旧组织快照归属属于未验项。

## 8. 连续性与预览边界

URL organization_id/query/status/page/adjustment_page；应用push、成功读取replace，popstate中止并重读；没有onDeactivated，preserve缓存离开待验。创建幂等键保留至成功后换新；确认prepare生成键并保留失败pending，但重新prepare会换键。影响预览是前端当前快照计算：当前方案被分页排除时目标可能找不到；未来/已过期调整、截零后的反推可能与服务有效配额不一致，需逐项复现再修，不改计量规则。新周期用量不预估。

## 9. 可访问性与效果

保持16px正文/输入、13px元信息、44px热区，390/768/1024/1440及邻界、200%缩放、三主题/两密度。native label/required已有；错误关联、模态首焦点/Tab/归还、编辑到确认、提交中Escape与移动键盘需完整验收。TechnicalDetails独立披露，不用技术UUID替代主要组织名称；不得为了凑图压缩影响说明。

## 10. 验收与全图

入口tests/e2e/m06-06-commercial.spec.ts与tests/m06-06/commercial.test.mjs；产品源码未变，不重跑或宣称其完整通过。COMMERCIAL-C-r1覆盖无组织/有组织、三方案状态、未分配/active/suspended/ended、双分页、创建/编辑/每类确认、无结果/读取失败/版本冲突/写成功读失败/表单错误及移动长表单。永久生成/复验入口scripts/verify-ui-phase2-commercial-c.mjs，隔离真实Vue/service/repository及本地原型检查，不替代真实MySQL/审计/幂等及授权生产写入。C方向已选不等于本页具体稿已审；精确影响算法、生命周期/软键盘/全部主题密度及全站部署签收未完成。
