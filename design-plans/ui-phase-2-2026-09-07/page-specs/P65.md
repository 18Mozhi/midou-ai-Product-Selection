# P65 · 发布管理

状态：2026-09-09补交[RELEASE-C-r1图册](../design/release-direction-c/README.md)，81场景169PNG，C方向具体稿待审，完整实施/验收/部署待办。图稿起点main/2f60527b；下文“本批”和main/5695ae7指历史事实规格批，不代表图稿已替换生产Vue。[B3a合同](../log-backup-release-contract-review.md)为控制/依据索引。

## 0. C方向具体稿与当前证据

蓝色页头和页内阅读目录，白色运行身份/部署捕获完整SHA对照、匹配记录、六列历史门实测、迁移/回滚计时和一条最近历史记录；替换旧七卡/三绿环结构。手机三比例门、空指标和长代码详情都有独立主图及续图；唯一业务模态为只读指标详情，不增加发布、回滚、批准、停止、迁移或探针执行能力。列显隐最少一列/首可见冻结/密度、刷新/重试/登录、两技术请求编号和超管覆盖入口保留；版本复制、完整披露和焦点保护仅提案。

46数据集由真实服务45组惰性输入输出和原始E2E独立组成。源函数证明来源SHA可回退；当前证据缺失并不会必然返回旧卡片使用的两个同源阻断码，不能称独立三方一致。单门条件函数直接从源码提取，严格错误率边界、时长/正样本流量、有限非空指标与其他包含边界逐值检查；负指标、门标签和流量百分比错配、无结束时间、未来时间未自动阻断，原型保持service.state并作字段核对提示，不擅自更改规则。

状态优先级、最新有效完成时间时效、首个重复门选择、额外门保留、timing_schema2合法0与旧/逆序/缺失区间null、metadata移除均有源执行证据。源Vue的15秒/单飞/保留和401/403清快照/unmount、仓库读审计提交与失败回滚、GET授权后14秒/依赖503/取消、原技术复制拒绝也以惰性依赖验证；不是挂载Vue、真实SQL/权限/剪贴板或生产发布。当前固定单后端manifest/config/bootstrap静态核对，旧建议折叠标明只作历史原文。

双端81场景169图、10相关历史源hash、六断点/CSS200%/字号热区、完整字段/Tab/Escape/遮罩/手机返焦、列设置/复制消费者/超管链接/刷新恢复均纳入永久验证器。当前稿不发布、不回滚、不迁移、不重启；具体审批、OP-G05/06、保活生命周期和全主题密度组合及全73页签收仍待办，下一P66服务拓扑。

## 1. 用户、范围与入口

/platform-admin/releases → ReleaseRolloutCenter，platform_admin/preserve。GET /platform/operations/releases要求platform:operate会话且no-store。查看接口覆盖证据链接仅capabilities含platform:superadmin时显示，目标/api-coverage重新授权。本页无发布、回滚、停止、批准、迁移或配置写入控件。

## 2. 版本与证据取数

仓储读取最近10条deployment_releases及其gates；同一可取消事务中写platform.release_rollout.read审计。service按currentBuildSha找当前release，最新历史release单独保留，门只取匹配release。历史记录不能证明当前构建；本地/远端SHA是部署捕获值，不是刷新时实时git查询。

可选来源字段缺失时服务会回退当前SHA、identity条件按已提供策略判断；UI不得将缺失独立证据叙述为确证三方一致。原始gate.metadata被移除，migration/rollback仅timing_schema=2且合法起止可得duration_ms，否则null显示尚无记录；0毫秒与未知不同。

## 3. 待审桌面结构

首屏区分运行身份、证据对应版本、观测时间与历史审计；“当前部署”和“历史5/25/100观察门”不得混为现行操作指令。版本标识需要完整可披露/复制方案，正文不只截十字符。下方按门类型列指标、阈值、阻断原因，保留真实业务字段，不制造发布操作或把环形绿色当完整通过。

## 4. 待审移动结构

先当前版本与可验证结论，再观测门卡/详情及停止阈值；长SHA、仓库、迁移名和阻断建议可完整查看。危险执行不属于此页，手机不会凭空多出确认按钮。API覆盖证据入口遵守相同能力检查。

## 5. 全部操作

RL65-LOAD顶部刷新；RL65-RETRY刷新失败/首次错误核验；RL65-LOGIN过期登录；RL65-COVERAGE超管查看接口覆盖证据；RL65-GATE详情、RL65-TECH桌面/移动门及阻断技术展开，共享请求ID复制/列设置/密度。局部无v-model业务输入。原PAGES“已有发布动作”以此真实清单纠偏。

## 6. 模态与关联写接口

一处ResponsiveDataView为canary门禁指标的移动详情，展示观察状态、时长/样本、错误率、读写P95、异步滞后；技术区门ID/代码/release ID。桌面列设置和原生details不是执行确认。TechnicalDetails在刷新失败和页脚显示请求编号。

/platform/operations/releases/write-probe是独立签名探针POST，202记录样本，不是浏览器页面按钮，也不是发布启动接口。本批不调用、不生成探针签名、不执行历史双槽脚本。固定生产入口仍为python scripts/deploy-baota.py；旧manifest候选拓扑只能作历史依据。

## 7. 结论与未知

服务优先empty、身份/来源不符blocked、rollback/rolled_back、automatic_stop/failed、healthy且所有required门通过后stale或verified。通过检查status、最少观察时长、正样本/流量、有限指标、错误率严格小于阈值与其他指标不超过阈值；最新有效完成时间用于时效。service判定不是前端环形status=passed单项的同义词。

需受控验证：版本同源卡只排除两个blocker时，缺失来源输入或无当前release是否误导；sha十字符无局部完整披露；UI仍用当前式“渐进观察门/自动停止”描述历史流程。正式新设计必须使该区别可见，不改变服务判定或恢复历史部署能力。

## 8. 读取恢复与缓存

浏览器15秒单飞超时、相关request/trace ID；服务授权后14秒边界，可取消读取和审计事务。429/依赖失败/超时保留成功快照提示；401/403清除数据，过期去登录。组件仅onMounted和onBeforeUnmount，preserve离开不等于卸载；返页新鲜度、迟到结果和请求ID归属仍待受控测证，不宣称已通过。

## 9. 无障碍与效果

按钮禁用/忙碌、live通知、原生披露、详情初焦点/关闭返回已有；完整Tab圈定、列/复制反馈、200%缩放、长版本、三主题/两密度和全按钮六态待验。停止/未知/过期应文字明确，不只凭颜色或百分比圆环推断。事实稀疏时不虚构时间线记录。

## 10. 验收与正式图

现有tests/e2e/m07-05-release-rollout.spec.ts及模块单测是历史基线，本批未运行或更新其图快照，不把旧截图视为当前正式新图。后续图包含无记录、当前版本缺证据、版本不一致、门缺失/指标超阈/过期、已停止/回滚、未知/零耗时、移动门详情和失败保留。真实生产验收需当前同提交与固定宝塔对象，未取得条件不连接/写入生产。F00仍待版本化审核。
