# P39 组织与账号概览

2026-09-11 整页组合增量：[28张当前Vue父级C组合图](../P39-PAGE-COMPOSITION-REVIEW.md)。蓝色全局目录/白色结果区，原刷新移至结果标题，1024及以下上方汇总与原760移动记录分界独立；复用筛选/创建样式，原146图保持。57浏览器检查/11项定向通过；视觉方向已按用户统一授权通过，不代表完整应用壳、真实接口/目的页/生产验收。

2026-09-11 创建弹窗增量：[42张双端与短屏实际Vue图](../P39-CREATE-PREVIEW-REVIEW.md)。原父子模板/脚本仅加独立审核CSS，四/五字段、角色、原生校验、等待、失败、重开及短屏底部；121浏览器检查、8项定向通过。原86图和18筛选图不变；该视觉组合纳入用户统一授权通过，不扩为真实创建或权限验收。

2026-09-11 筛选组合增量：[18张双端字段/按钮预览](../P39-FILTER-PREVIEW-REVIEW.md)。当前Vue脚本与原指令保留，仅审核宿主内增补label/帮助/操作容器和C样式；四宽度核对，非生产模板接入。原86图、后台筛选/权限和其余创建详情均不变；该视觉组合纳入用户统一授权通过，不扩大为真实筛选/权限验收。

状态：ACCOUNT-OVERVIEW-C-r1视觉方向已按用户统一授权通过；[86图及交互稿](../design/account-overview-direction-c/README.md)，40场景双端80主图＋6创建局部，含2非业务工具图。本批起点main/da750f6，核对真实源码/原夹具；不代表全部交互或生产验收。[W05合同](../platform-account-contract-review.md)记录父入口与共享动作，P41/P42目的页全变体仍分包待做。

## 1. 角色、范围与路径

`/platform-admin/accounts`→PlatformAccountCenter.vue，默认organizations页签。platform:superadmin的平台范围，不要求当前成员组织上下文；不能把概览当普通组织管理员后台。与P40–P45共享账号入口，路由缓存及返回需逐分支验。

## 2. 主要任务与事实来源

主任务是核对全平台组织/账号规模并进入相应管理。GET /api/v1/platform/accounts返回summary、organizations、users、admins；服务query最多120字符、status30字符，每个结果数组最多200，summary全局不受筛选影响。当前概览正文实际是组织记录，不是组织与用户混合搜索结果列表；不能按“搜索组织名称或用户邮箱”占位词承诺显示全部账号命中。

## 3. 桌面构图与信息层级

已批准编排：全局规模用紧凑摘要，与当前组织结果数量明确区分；组织、用户、管理员是清晰分流入口，结果区只呈当前对象字段。主任务入口和上下文范围优先，查询/刷新置于结果区；不保留旧巨幅标题或三块同权创建动作的默认布局。功能继续保留；路由、权限和真实数据行为仍按运行验证。

## 4. 移动构图与返回

全局摘要压缩、分流入口可达，组织名称/状态为首要字段，成员和工作区计数次之。现有移动筛选抽屉与记录预览继续作为待审事实；进入组织详情会换路径，关闭返回P40而非本概览。新图必须明确返回目的地，不画成未实现的保留原筛选返回。

## 5. 全部动作与控件

PA-ORG-CREATE、PA-USER-CREATE、PA-REFRESH；组织/用户/管理员导航；query/status搜索、重置、空结果清除筛选；组织详情桌面/移动入口、技术信息、共享列设置/冻结/密度/预览/筛选抽屉。当前无组织时页头仍可新建，但本概览空态没有P40的第二个新建按钮。默认组织tab下status选项包含active、disabled、archived，需按实际结果区验，不能把两个停用值合并更改业务。

## 6. 弹窗与输入

新建组织沿P41两步及三字段；新建用户沿P43五字段；组织详情沿P42资料和三个原因变体。概览入口打开用户创建窗口标题与P43可能不同，但请求合同相同。写入通过既有Origin、Idempotency-Key与鉴权；创建组织/用户现有body无人工reason，编辑/状态等有reason，不擅自统一新增字段。

## 7. 状态与反馈

初读loading、ready、空/筛选无结果、error/重试、后台refreshing和busy。列表已有data时任何错误均保留快照，401/403的当前行为待验证；角色目录不在概览普通load主链，失败不能被假定为账号失败。新建账号POST成功而随后的列表GET失败时，仍确认“账号已创建”，并明确提示列表未刷新、要求手动核对；不把写成功误报为列表已更新（2026-09-24桌面/手机E2E）。

## 8. 连续性与异步

URL初始化query/status；提交router.replace保留其他query，相同条件直接重读，重置只移除两键。路由过滤变更遇到在途读取时，旧响应不覆盖当前URL；在途结束后补读最新条件（P39桌面/手机E2E覆盖）。创建/详情使用无query的绝对路径，当前会丢失原筛选；不新增持久化或自动重放写入。

## 9. 可读性与效果

1440/390主视口，768/1024、759/760、200%缩放、长中文组织名和全零计数；16px正文、13px辅助、44px目标、键盘/焦点/错误关联、三主题两密度。普通账号/组织筛选的查询框现有可见label并与输入框原生关联。P39 当前可见页签、按钮、输入/选择控件及手机筛选窗控件已按页面作用域达到44px目标；桌面组织目录列设置/密度工具和展开列选项亦达标，均由双端实际浏览器测量。手机端使用移动记录预览，不渲染桌面列工具。详见P39实施记录“表格工具触控目标”。

## 10. 验收与图槽位

新增永久verify-ui-phase2-account-overview-c.mjs及数据辅助器，执行真实Vue函数/computed/显式watch及惰性SQL构造，核对120/30/200与LIKE转义、独立summary、精确跨页路径/五字段null、重复读取与12秒abort回调；路由在途竞态现由桌面/手机E2E覆盖；创建成功覆盖重读失败、旧创建成功关闭新窗已离线复现。原型演示范围提示及新窗归属，旧创建成功/失败、后台读回保新表单、五字段/校验/三平台角色与组织角色、移动筛选/预览及列设置等的真实浏览器结果见图包与PROGRESS，不当作挂载Vue/API/MySQL/鉴权通过。P41/P42全变体和实际跨页渲染仍待做；完整历史/KeepAlive、角色/主题/原生缩放/软键盘与生产仍待验。
