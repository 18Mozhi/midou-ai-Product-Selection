# P27 自动化规则：逐动作、字段与弹窗状态登记

r2收尾：定向登记7/7、最小状态64项、全部208图捕获及无参数208项复验通过；全量单测470/470。新增变体仍归既有14动作，不增加分母；86图包共11933 PNG，源码与PNG漂移0，审核台双端及文档/运行文档/静态检查通过。本轮未改变父包、生产源码、依赖或运行态，复用上一轮父包122图/业务合同验证，不重复构建或启动服务。单测临时fixture及两份审核下载自动清理，浏览器finally关闭，无本轮残留临时文件；208图及其审核材料均为永久交付物。旧轮受限清理路径未触碰。

2026-09-10 P27状态变体r2：[新增84图、累计208图](design/automation-controls-direction-c/README.md)。补恢复/编辑保存/另两模板/五类错误重载/编辑取消10变体，绑定42条扩展状态，不增加14个原动作。原C布局、数据和业务controller不改；r1基础审核与r2新增审核分开。以下“这些变体未补”为r1历史记录；字段/共享提案控件、全组合与真实Vue仍待。

2026-09-10，从干净main/e5a12d05继续。AGENTS → Feature Map automationRules → route-catalog /automations → NavigationShell的automation-rule-center → AutomationRuleCenter.vue，API各读写继续team:manage及组织/工作区范围。当前源码LF hash与既有合同2184a176…完全一致；本轮不改产品源码、接口或规则。

[P27完整C稿122图](design/automation-direction-c/README.md) · [新增逐按钮124图](design/automation-controls-direction-c/README.md) · [机器可核对登记](action-reviews/P27.json)

## 已核对的边界

19候选源位置合并为16组：14路由动作、2原生模态接线。真正写入语义只有AR-SAVE（创建POST/编辑PATCH）和AR-STATUS（暂停/恢复）；AR-PREVIEW的POST是只读。submit与保存按钮为同一动作，不重复计数。dialog定义及cancel转发分别指向关闭动作，不虚增两个关闭业务。

10处v-model包括条件任务负责人和编辑原因；2原生dialog/1内嵌form，对应执行记录/新建/编辑3业务变体。32场景引用是这些容器的状态展开，不是32个弹窗。模板是三个源对象的草稿合并，不新增接口description存储或额外删除、复制、手工运行能力。

| 动作组 | 实际语义 | 本批代表图 | 未完整覆盖 |
| --- | --- | --- | --- |
| AR-CREATE | 清路由及本地状态，打开emptyForm | 可用四态、全局busy | 各页面状态下入口组合 |
| AR-LOAD | 规则与成员并发重读，再恢复路由 | 通用错误四态 | 其他五类错误逐态及完整读取归属 |
| AR-DETAIL / AR-EDIT | rule深链后GET详情/按列表预填编辑 | 各四态及全局busy | 所有规则行、深链/历史/范围组合 |
| AR-STATUS | 原version及固定原因暂停/恢复 | 暂停四态和busy | 恢复逐态、真实防重入/事务/回执 |
| AR-DETAIL-CLOSE / AR-EDITOR-CLOSE | 清本地与rule/action；不取消在途请求 | 关闭/取消四态 | 全生命周期、额外提案关闭图标 |
| AR-TASK / AR-NOTIFICATION / AR-SAMPLE | 使用真实字段形成目标路由及from | 各四态 | 目标鉴权及全样本/真实跨页 |
| AR-TECH | 有错误码或资源时原生折叠 | 四态，入口16px | 全错误/资源组合；提案展字段与原源码不同 |
| AR-TEMPLATE | 非编辑时合并模板到form | 审批超时四态 | 竞品复核与审批驳回逐态 |
| AR-PREVIEW | 原生validity通过后只读POST；previewing禁用 | 四态及预览busy | 全触发×条件×成员、真实预览后台 |
| AR-SAVE | 原生form提交创建或编辑，busy禁用按钮 | 创建四态及busy | 编辑逐态、冲突、重复提交、跨窗回执 |

## 图证不是完成率

新增62场景×双端124PNG：14代表控件四态共56场景，6个在途场景同时绑定disabled/busy，共68条精确selector/状态引用。5个真实控件没有禁用/忙碌呈现，共10槽独立列明；3类链接的6槽沿导航不禁用记录。注册表0未映射代表槽，不等于所有按钮变体、字段、弹窗、共享新增控件、主题密度或生产已验收。

鼠标hover/按下与真实Tab逐控件验证，截图前核对中心命中、44px目标、16px字体和无横向溢出；完整原型state前后保持不变，未发HTTP或造写成功。父图122张及其五源body/预览SQL/Worker惰性合同验证仍单独重跑，不用本包伪类图代替业务验证。

frontend-design指导本包延续C蓝目录/当且则流程；只增加技术详情入口字号，未采用“保留旧身份”的improve-ui流程。旧原型controller和CSS主体直接引用，无复制分叉。查看样本默认捕获曾被固定底栏遮挡，改用实际滚动到中部后重新验证命中，没有force点击或修改业务规则。

## 仍待实施/核验的真实差异

- 原源码create/status没有原型的busy函数早退；previewing与busy互不禁用另一提交按钮。此处按原合同登记，不隐瞒差异也不自动改业务。
- 原源码action_failed一律承诺重试，C稿已按最终失败等状态区分；源执行技术折叠只显示错误码或资源ID，提案展开更多既有字段。真实Vue文字/结构未实施。
- AR-G01–G03继续待：错误位于模态外、列表/详情/保存后读取归属、切范围缓存生命周期、成员失效/版本冲突、完整输入组合；原型导航目录、分区和额外关闭入口仍需本身逐态证据。

## 验证与运行交接

5项永久登记测试验证当前范围，并拒绝漏模板、漏条件成员输入、错误cancel转发、用通用控件板充当精确状态证据、旧源码hash及假批准。最小图验证40项与完整124项通过，父图122项复验通过；各测试的适用范围见图册README。正式图稿/索引/验证脚本为交付物，不是临时产物。

无产品代码/API/OpenAPI/环境变量/依赖/数据库/Worker变化；无需部署或重启。这里只构建离线审核材料，不触发正常宝塔部署器的停服/迁移/重启流程。用户仍需审核P27具体布局与控件，不把P16布局批准扩展到本页；全73页目标、G0–G5和分母状态继续按总台账。

收尾结果：全量单测468/468通过；新包无参数复验124项、父包122图/合同复验、文档/运行文档/静态检查和双端审核台检查均通过。全局现28页显式语义登记、621源位置、584语义组、498路由动作、62接线，余45页未登记；86图包/11849 PNG，源码与图像漂移0。生产代码/依赖/运行态未变，不重复构建或执行上一轮同样的真实Vue测试。

本轮新增图包130个永久文件（124图＋HTML/JS/CSS/README/evidence/gallery）及永久登记/验证文件保留；失败捕获在同一路径成功重跑后全部进入124张清单，无游离截图。单测临时fixture和审核台两份临时下载已自动删除；浏览器/上下文关闭，未启动服务，4101/5173无监听。本轮无残留临时产物；旧轮被策略拒绝清理的路径不重试、不改动。
