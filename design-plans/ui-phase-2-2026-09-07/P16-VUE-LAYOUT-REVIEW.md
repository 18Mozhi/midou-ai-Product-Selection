# P16 C r2 · 真实 Vue 布局与按钮审核

2026-09-10，从干净 main/a4a30f78 继续。依据用户“整体布局通过，继续细化按钮状态”，本批落地本页已批准的蓝色阶段栏、候选/决策双区和五项质量门区。使用 ui-skills-root/frontend-design 确保新视觉在 P16 内隔离旧全局样式，Playwright 用于现有回归及真实挂载图核对。不是全站完成或生产发布。

## 审核入口

[打开24张真实页面图册](../../output/playwright/p16-c-r2-review/index.html)。每个状态含1440桌面、390手机图；浏览器真实挂载现有 NavigationShell → SelectionJourney，HTTP 全部拦截为原service隔离样例。悬停图使用保留指针命中的视口截图，其他状态为整页长图；390悬停是窄视口下鼠标验证，不冒充手机触控hover。顶部纸色导航/标题和手机底栏属于未改的全局壳层，仍可看到；不隐藏旧壳制造全站焕新的印象。长截图中的手机底栏固定在首屏下沿，是实际浏览器渲染，不是新按钮。

| 状态 | 核对重点 |
| --- | --- |
| 创建默认/悬停/键盘焦点 | 蓝色主操作，深蓝悬停与3px焦点；输入类型仍为原三种 |
| 创建忙碌/失败 | 原按钮禁用；不降低文字透明度；失败保留原输入，不自动重放 |
| 未选候选 | 五门显示待核对/未返回，不冒充五门失败 |
| 五门通过 | 5/5且原采纳资格满足时才可选择采纳；保留继续观察/驳回 |
| 时间轴展开焦点 | 原生details，Enter展开/Space收起，不发业务HTTP或写存储 |
| 保存焦点/忙碌/失败 | 提交与开始下一次在busy时禁用；失败保留原因，不清空草稿 |
| 成本门未通过 | 4/5，采纳禁用但观察/驳回可用，仍由服务端最终校验 |

不是所有按钮六态的新完整验收：本册24图仅12个真实挂载状态。原离线382图/102代表控件状态仍见[原型图册](design/journey-direction-c/README.md)；输入冻结、错误关联、重置草稿、写入晚到归属等提案没有随布局自动落地。新增summary的4个代表状态槽尚未纳入旧离线动作图映射规范，disabled/busy按实际无属性单列非适用；不编造0缺口。

## 实际改动与未改范围

- 重写 selection-journey.css，停止导入旧 enhancements（原文件保留）。局部选择器覆盖旧 html/#app 字体、圆角、单选框最小尺寸与禁用透明度规则，不修改公共CSS或其他路由。单选标记20px，承载label≥52px；主操作≥44px。
- 蓝色只读阶段栏，不是可跳过流程的按钮。宽屏候选/决策并列，手机顺排；任务报告数和本页返回数分别标注；时间轴默认收起。
- 质量门区只消费原五项boolean与既有adoptionHint/canAdopt，不计算新评分，不编造价格/利润/资格。当前5/5不替代recommended、关联ID、all_passed或服务器锁内复验。
- 原create/decide/reset/read处理器、三种输入、5个v-model、POST字段、原链接/共享恢复事件、活动ID格式、2秒轮询均不变。没有新增业务弹窗、数据库迁移、环境变量、API/OpenAPI、权限或依赖变化；后端/Worker/Python无相关改动。

## 验证与复验

现有42项真实Vue隔离回归通过：三类创建、五个缺门、换候选、409后重读、观察/驳回精确body、失败保留、轮询/离开/返回/晚到404/重置/恢复重试。

新验证器70项检查通过：24场景捕获过程、双区位置、阶段语义、未知/4门/5门、原采纳body、忙碌禁用、失败草稿、键盘时间轴零额外请求、C样式不被旧皮肤覆盖，以及768/1024/1280中间宽度无横向溢出。首次发现旧#app最小尺寸/透明度及旧全局字体覆盖，定向修复后重拍；等待resize重排稳定，不用隐藏溢出来通过。页面异常和未拦截外部/API请求为0；故意模拟503不算真实服务故障。

```powershell
npx --no-install playwright test tests/e2e/ui-phase2-journey-contracts.spec.ts tests/e2e/ui-phase2-journey-reads.spec.ts --workers=1 --output=output/playwright/p16-layout-20260910
node scripts/verify-ui-phase2-journey-vue.mjs
```

新验证器无参数先核对源/PNG哈希再运行；`--capture`重拍上述固定图册，不授予批准。复用现有依赖，临时Vite仅127.0.0.1:5175、strictPort；浏览器与服务finally关闭，不是新增生产服务。既有Playwright配置使用5173/4101，运行前先检查端口；新脚本5175占用则退出，不能接管别人的进程。进一步构建/预算/静态/文档/设计审计结果见[执行记录](PROGRESS.md)。

没有部署或重启生产服务。后续获准正式发布时只需走原本地前端构建及宝塔部署流程，不改服务命令、无需数据库迁移或后端/Python重启。没有新增业务可调参数。

## 等待审核及剩余工作

请审核本册按钮配色、禁用可读性、键盘焦点和五门呈现；整体布局已通过，不重复申请。完整按钮/字段/变体/主题密度、全局壳层和其他72路由、真实API/MySQL/RBAC/外部采集、写入生命周期及生产签收仍未全部完成。全页批准0及G0–G5原状态不提升。

图册、evidence.json和验证器属于永久审核交付物，保留并纳入本次提交。本轮临时output/playwright/p16-layout-20260910/.last-run.json删除调用被安全策略拒绝，未提交且不绕过。旧output/playwright/ui-phase2-competitor-races-20260909的playwright.config.ts及results/.last-run.json也仍保留；如需清理，可在资源管理器核对这三个测试文件后手动删除。无后台服务保留。
