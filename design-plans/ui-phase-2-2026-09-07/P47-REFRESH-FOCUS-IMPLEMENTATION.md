# P47 刷新按钮等待焦点修复

## 实现范围

承接用户“继续完善交互”，修复上一轮实际复现的刷新按钮禁用后焦点落到BODY。
开始main/af239b08，425项工作区变更、暂存为空。使用无障碍技能；本地UI CLI缺失，
复用已有Vue/Playwright依赖。没有新增依赖或常驻服务。

ProviderAdapterCenter只增加refreshFromButton包装、标题区域tabindex=-1和一个原生
click绑定。包装只在按钮当前拥有焦点时，把焦点移到同页持久标题区域，再调用原load；
断开、inert、目标缺失、焦点已经在别处时不移动。异步完成不处理焦点。
手机CSS隐藏h2文字，因此不把焦点设到隐藏h2。复用已有[tabindex]:focus-visible蓝色
轮廓，未改CSS、默认布局、按钮文案或待审刷新失败组合。

整个load、probe、空态重置函数、onMounted、错误面板重读、请求/响应/超时/重试、
过滤和排序均未改。没有新接口、API/OpenAPI字段、环境变量、配置、数据库、权限或
跨页锁策略。单测将本次三个精确变化逆向移除，必须还原完整前版SHA：
ea3eaecf5bb8a6ec5e8e701dd8743cac61a35c6806a8a0d35079ba64be3b5e40。
当前Vue SHA：2f71a84bc29b416ce9de84732993fa488ae856ac508d22297bdab8f7278b6e5f。
未知源码和不匹配的包装拒绝，不放宽旧历史转换器。

原刷新失败提案辅助函数只补兼容“标题原来已可聚焦”的情况，仍要求唯一已知标题；
没有把该提案实施到生产。旧提案的正文、状态和inline重试包装不变。

## 实测与图

新命令 `node scripts/verify-provider-adapter-refresh-focus.mjs --capture` 生成独立审核包：
`output/playwright/p47-refresh-focus/index.html`、8PNG及evidence.json。旧图没有覆盖。
不带参数执行完整回归但不写图。包绑定173个实际来源及补齐的本地CSS导入。

实际App/Vue前版与当前版在390/760/761/1440、成功/409、本机Enter/空格/鼠标点击：
16组280检查、48次显式刷新。共80个本地拦截GET，无body、POST或真实业务写入。
前版三种输入均复现BODY；当前停在标题区域，键盘可见焦点，原scroll不跳动；Tab
跳过禁用刷新到已有返回链接，主动移到搜索框后，响应不抢回。过滤结果/提示与请求数保持。
仅默认/reduced-motion，不代表全主题、读屏器、完整页面或生产验收。

最终格式后的新8项单测全部通过，含6类焦点所有权、完整源码/样式内容边界与8图/源绑定。
首次断言误比较了Vue源位置信息、格式后精确文本需要接续，各自已修正并复测；不称为运行时缺陷。
此前相关36项一轮35过/1格式接续失败，其中其余29项通过；修正后的8项独立文件通过，
全库最终结果另记，不把拼合成绩冒充那次36项运行全过。

`npm run build:web`通过：已有浏览器助手归档、vue-tsc、Vite440模块；产物保留于项目
正常构建目录，不是临时文件。修改后当前读取顺序100项、检查反馈54项回归通过，均未截图。
原刷新失败驱动的baseline还期待BODY，不能再作为当前版本通过依据；用新入口验证当前修复。
其他现行图证需按实际源码重放，不能改哈希当作通过。

首次全库结果见`P47-REFRESH-FOCUS-UNIT-RESULT.json`：246文件、1418项，1240通过、
178失败、0取消/跳过，319210.9106ms。与上一份完整快照逐名比较，新增21项失败、
移除7项；不把所有新增失败直接归因为图片过期。其中权限/筛选分页当前提案组合仍只
接受刷新修复前的完整源码，这是已确认的两项兼容问题。

随后接续当前组合器：先通过精确刷新焦点逆向辅助函数验证完整前版，再复用既有空态
转换；组合后保留两处实际焦点函数、原按钮绑定和可聚焦标题。对未知源码、改动焦点
函数的反例仍拒绝。当前预览入口同时记录新增辅助文件及刷新保护标记；原历史入口、
清单、截图和提案正文不改。两个文件12项定向测试通过；本段后续改动不倒算首次
全库178项失败数量。当前权限入口`--state=access`回归30组411检查；筛选分页入口
`--state=filter-pagination`回归6组162检查，均179来源、0截图、本地拦截场景。
保留原历史驱动的状态/路由/请求数量断言；当前页与提案不混称，没有真实权限验收。
两入口共四个端口50451/50600/50757/50785已核对无监听。

最终同246文件全库：1418项，1244通过、174失败、0取消/跳过，297441.1453ms，exit1。
相对首次完整结果无新增失败，四项权限/筛选分页当前组合及提案编译失败消除。完整
失败名称和逐名比较写入同一JSON；首次178失败的原结果完整保留在firstRun，不覆盖历史。
其余174项仍为失败，未豁免，不能宣称全站/部署通过。文档153文件、73路由/60受保护/
6角色及运行说明检查通过；先前完整格式门通过，最新修改的8个相关代码文件格式复验
通过，git diff --check通过。构建后的生产Vue未再改动，不重复构建。

手机新焦点画面已展示并单独请求审核，尚未收到回复；不扩大原成功/空态/默认布局批准。
跨页面检查锁、待审失败布局、全部状态与全73页重设计仍未完成。

## 发布与清理

本批未提交或部署，commit hash不适用。将来正常宝塔前端发布后刷新浏览器生效；
无本次新增配置/调节项或独立API/Worker/Python重启要求，正常部署器的既有检查不可跳过。
新8张图、清单、入口、辅助函数与测试为永久交付，不作临时文件删除。
本轮没有新临时脚本/截图夹具；三轮本批图包捕获均使用该新交付目录，最终包覆盖本批中间版，
没有覆盖用户已有图。宿主端口64669/64758、64978/65060、65292/65368已核对无监听。
首次全库自有临时夹具`C:\Users\23136\AppData\Local\Temp\scoutops-code-style-79g3qj`
已不存在，父27772/子31684进程已退出。最终全库夹具
`C:\Users\23136\AppData\Local\Temp\scoutops-code-style-zh4lJm`也已不存在；
父47528/子51472及本批测试子进程退出，没有新保留的临时服务。收尾工作区431项变更、
暂存为空；未暂存或覆盖无关变更。新图片是审核交付物，不是待删除的测试截图。

此前两处策略阻止删除的临时目录仍待手动清理，不绕过重试：

- `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p47-c-e2e-temp`
- `C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`
