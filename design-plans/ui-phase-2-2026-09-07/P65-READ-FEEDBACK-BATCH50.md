# P65 读取反馈、追踪与键盘 · 批次50

起点main/6025c198、579项在途变化、索引为空；本次ReleaseRolloutCenter.vue起点无本地修改。上一批已交付实际Vue C图册，为有效进展。本批不把自动继续或局部批准解释为P64/P62离页策略、发布政策或完整设计验收。

## 已修复

1. 成功快照编号与失败编号分开。原实现失败/超时会覆盖requestId，旧事实因此贴上另一请求的编号；11项新追踪合同在原实现全部失败。现在requestId只归成功快照，readFailureId归最近失败；开始重读清失败编号但不改旧快照，401/403清数据时同时清快照编号，超时使用本次实际发送的关联ID，非接口异常没有编号就不编造。
2. 首次失败增加既有TechnicalDetails入口；保留失败与首次失败均标“本次失败读取追踪”，页脚标“快照读取追踪”。复用原复制与拒绝反馈，不改共享复制组件、客户端返回或后端追踪协议。
3. 使用fixing-accessibility技能补读取/失败/保留告警的标题关联、aria-busy。生产组件用h3接原h2，C审核转换用h2接页面h1，互斥区域复用同一读取标题ID。首次超时明确“读取超过15秒，停止本次等待，重新核验”，不再笼统提示登录；不宣称中止已完成的服务审计。
4. 原重试区域移除前，仅在该按钮实际拥有焦点且源/目标可见、连接、非inert时，移交顶部稳定刷新按钮。顶部等待改aria-disabled而非native disabled，原controller单飞守卫先于焦点操作；Enter/Space/click不会重复读取。只有接到焦点且被遮挡时滚动到可见位置，完成/失败均不抢回用户移走的焦点。无公共焦点函数重构。

## 未改范围

GET、15秒浏览器/14秒服务超时、503/429重试、状态判定、401/403清快照、权限、缓存离页/返回、审计事务、发布/回滚规则均不变。生产仅上述P65反馈/键盘修正；完整蓝白C布局仍在本地审核插件，不能说全页已经接入生产。API/OpenAPI/SQL/.env/依赖/Worker/Python/插件及签名探针无变化，不执行发布、迁移、回滚或宝塔操作；没有配置或运行合同需联动。未来上线需重新构建前端静态包；本次不部署、不重启后台。

## 验证证据

- 7项模板/语义合同、14项焦点合同、11项追踪合同，加17项既有M07-05检查，共49项通过。11项追踪先红后绿；焦点与语义在实施后验证，不虚称全部先红后绿。M07-05包含静态与惰性依赖检查，不是真实SQL/签名探针或发布执行。
- `node scripts/verify-release-read-states.mjs --capture-review r2`：1440/390 × 减少/正常动效，364检查、84本地GET。首次加载、真实15秒超时、503/429三次与401/403一次、恢复原始样例、保留等待/超时/失败、清空、成功empty、快照/失败编号归属、复制成功/拒绝。复制使用浏览器内存替身，未写系统剪贴板。可见控件中心命中检查也在不出图模式运行；不是完整读屏或可访问性认证。
- `node scripts/verify-release-read-focus.mjs --capture-review r1`：同四组80检查、28本地GET，两个重试入口焦点移交、等待可聚焦/不可执行、完成与失败、不抢主动移走焦点、未聚焦程序化重试；取景命中断言在默认不出图模式同样运行。10张局部图有6px焦点留边。
- `node scripts/verify-release-page-preview.mjs --capture-review r3`：原102项双端默认/完整详情回归、12本地GET，36图；不是旧图重复绑定新来源。总计546项浏览器检查，124个本地P65 GET，没有生产/写请求。
- `npm run build:web` 包含vue-tsc并完成生产构建；`npm run verify:frontend-budget`通过253资产预算。现有依赖复用，无安装升级。构建及浏览器助手压缩包是项目所需产物，保留，不当作验证垃圾删除。

## 图册与待审

当前读取 `output/playwright/p65-read-states-r2` 34PNG，焦点 `output/playwright/p65-read-focus-r1` 10PNG，默认/详情 `output/playwright/p65-page-composition-r3` 36PNG；每组165来源，manifest中来源和图片SHA-256均已核对。r2读取首次超时手机白区已展示待审，不扩大为整页或真实发布验收；上一批身份区域仍待审，历史默认r1/r2的来源发生变化，不改其manifest。

读取r1是本轮修订前的34张临时测试图（另有index/manifest，共36文件）；其中恢复图片名称沿用了不准确的blocked标识，r2已改为recovered-original，原始E2E的verified/回滚门/未核验布尔矛盾仍保留。首次超时说明也以r2为准。r1清理命令被工具策略拒绝，尚未删除，未换工具绕过。残留的确切目录为 `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p65-read-states-r1`，不作为当前交付；仅该目录由本轮创建，已有用户文件与其他图册未删除。必要时用户可在确认后自行对该确切目录执行PowerShell Remove-Item，不需停止服务才能清理。

## 收尾与未完成

本轮测试浏览器/Vite均finally关闭，相关端口复核无监听；没有留给用户运行的服务。正式测试、驱动、图册与记录保留。生产服务未连接，真实权限/审计/发布、保活/迟到/全主题密度/全按钮状态与完整73页仍未验收。

既有API覆盖测试与OpenAPI输入仍同HEAD，未重复无变化的失败、未改计数；处理决定未到，未暂存、未提交，commit hash不适用。没有用本批49项通过覆盖掉既有失败。
