# 第二阶段 UI 在途改动接管与分批提交

## 授权与边界

2026-09-14，用户明确回复“允许”，授权接管当前工作区内第二阶段 UI 相关在途改动，核对归属、排除无关和敏感文件，验证通过后分批提交。本授权不等于设计批准、整页验收、生产发布或新增迁移授权；旧记录中的“接管范围待确认”由本节取代，其他未验事项继续有效。

接管基线：main，HEAD `af239b08b69f7d97cd0372f9840a70009eeef0c7`，暂存区为空；展开未跟踪目录后834个在途文件。目录归类只是整理起点，不据此批准全部文件。按依赖拆批，禁止整体暂存；原图、用户批注、来源指纹和局部批准范围不重写。

## 批次 01：P16 原生单选组的可访问名称

产品范围仅`apps/web/src/components/SelectionJourney.vue`两行模板属性：候选结果与决策方式容器添加`role="radiogroup"`和各自`aria-label`。实际脚本、CSS、接口、原生radio的name/value/checked/disabled、五项质量门与采纳规则不改。

永久回归：`tests/unit/ui-phase2-journey-radio-semantics.test.mjs`直接编译并SSR渲染当前实际模板；核对原输入类型组、两个命名组、原生radio数量/name、无自定义tabindex、采纳禁用/观察选中和来源链接安全属性。测试仅依赖已有Vue/TypeScript依赖，不依赖其他未提交审核辅助文件；合成状态不证明真实API、浏览器完整读屏或生产行为。

定向执行：`node --test tests/unit/ui-phase2-journey-radio-semantics.test.mjs`，3项通过；组件及测试格式检查、`npm run typecheck:web`、`verify:docs`和`verify:runtime-docs`通过。另将实际组件与接管基线作精确替换对照，确认差异仅为两处属性，没有脚本或样式改动；未重跑全站套件，不据此宣称其他批次通过。只将本批产品、测试、本记录及Feature Map/当前计划的对应增量纳入提交，其他在途内容留在工作区。

使用方式不变，无新增环境变量、配置、依赖或数据库迁移；OpenAPI、后端和其他消费者不涉及，本轮不部署、不重启。该修复随未来正式前端包发布。没有新增一次性验证文件、截图或长驻服务；已有清理受阻材料保持原处理边界。

## 批次 02：P34 查询归属与分页焦点

批次01已提交为`54a54303`。批次02生产范围为`OrganizationAdminCenter.vue`和`OrganizationApprovalPanel.vue`：父组件传入原所属routePath，子组件捕获ownerPath并保留原route.path回退；防止缓存期间晚创建的审批面板把目标路由当成自己的查询归属。分页仅在持焦按钮即将禁用时把焦点移到常驻页码，nextTick后复查连接、inert和焦点所有权，仅在越界或被遮挡时滚入视口。

原8/6条分页、查询键/筛选、选中模板、GET读取、权限与组织范围均不改。ownerPath是组件内部可选属性，不是后端API字段；OpenAPI、环境文件、数据库和后端消费者不适用。

配套`ui-phase2-org-approvals-query.test.mjs`9项与`ui-phase2-org-approvals-pagination-focus.test.mjs`49项，共58项通过；真实模板编译、查询回流/迟创建、分页边界和渲染后焦点所有权分支均覆盖。组件、测试和预览CSS格式检查通过；同次接管的Vue类型与文档门已通过，产品字节未变不重复运行。已有实际App图包只作为此前本地样例证据，不把未提交的整套审核驱动计为本批独立交付。

一并保存`implementation/org-approvals-vue-c.css`作为永久审核资产及测试输入，未被生产组件或生产入口导入；完整C模板组合、审核驱动和图册索引仍另批提交。保存该资产不批准其尚未审核的区域。正式生产视觉没有切换到整页C，两个已批审批记录区域与其余待审范围仍分开。

无新增外部依赖、服务或配置；本轮不部署、不重启。产品焦点修复需随后续正式前端包发布。未新建临时截图、日志或验证进程；本批提交后仍有其他工作包和完整73页验收待办。

## 批次 03：共享筛选抽屉关联、动效与通知色板

批次02已提交为`550808b3`。本批接管`ResponsiveFilterDrawer.vue`的每实例useId、触发按钮aria-controls与面板id关联、notifications外观和减少动效覆盖；沿用原responsive/dialog模式、760px断点、筛选字段、关闭/焦点回退及重置后焦点保护。新增共享局部色板，不改既有治理/内容颜色。实际Vue复现通知外观遗漏色板选择器：背景为透明、主色/边框变量为空；仅补notifications选择器后恢复白底和既定蓝色。

永久浏览器驱动`node scripts/verify-filter-drawer-association.mjs`运行真实Vue、合成插槽和本地只读资源，不访问业务API：390/760/761/1440宽度乘两种动效设置共8组，含24组色板检查、16组键盘检查、多实例唯一关联、视口/模式切换和卸载重建。全部通过，零页面错误与意外请求。该技能检查落实键盘语义和减少动效约束，不等同完整读屏、整页或生产验收。

筛选与重置测试当前工作区23项通过；其中平台调用方的1项依赖P57在途改动，单独留在工作区、不纳入本批暂存测试。本批对应22项共享回归，保留原重置行为及反向变异检查。历史重置截图只绑定原捕获Git版本，原图/manifest不变，不冒充当前截图。共享详情/色板等扩大检查40项通过，但相关数据详情源码、历史驱动和测试仍另批整理；其结果不是本批全部独立交付。

当前工作区`build:web`（含类型检查）、252资源体积预算、73条路由映射、153份文档门和运行文档门通过。构建包含其他在途UI，不是干净提交的生产发布包；本轮不部署、不重启。无新环境变量、外部API/OpenAPI、依赖、数据结构或权限变化；新增appearance值仅是组件内部样式选项，P57父调用方与导航分发排除规则另批提交。

使用方式不变；共享抽屉的改动随后续正式前端构建发布。浏览器与临时Vite服务均已关闭，未写入一次性截图、日志或测试文件；构建保留项目正常`apps/web/dist`及`apps/web/public/browser-helper/scoutops-browser-helper.zip`。原清理受阻的`output/playwright/p34-pagination-focus-r1`与`output/playwright/p34-pagination-focus-r2`未重试删除；仍按原记录人工清理。全73页与待审视觉范围继续开放。

## 批次 04：共享详情色板与历史/当前验证分离

批次03已提交为`cadc6e1f`。本批产品变更仅`ResponsiveDataView.vue`的治理/内容色板提取：使用已提交的局部overlay令牌，原颜色、阴影表达式、选择器、模板、脚本和生命周期不改。严格展开令牌后，完整组件字节必须等于原`af239b08`版本；未知颜色、选择器或行为漂移仍拒绝通过。默认外观和其他页面样式不改。

实际Vue驱动`node scripts/verify-responsive-data-view-current.mjs`保留原驱动全部交互断言且禁止截图写入，增加真实KeepAlive退出/返回与治理/内容计算颜色检查；限定本地GET资源、拦截其他请求。默认/治理/内容乘两种动效设置、各4宽度，6场景共80项通过。覆盖嵌套确认/原因窗、Tab边界、原有inert恢复、记录刷新/删除、缓存退出与重新打开、桌面表格；零截图、零业务写入，所有浏览器和服务关闭。这不是完整页面、真实权限或生产验收。

历史验证仍使用原Git捕获版本、原manifest和原PNG；当前源码独立绑定。生命周期图表只允许已登记的同一张P50后续截图回溯到原阶段：两端原始哈希、390×900尺寸、2像素/最大通道差3及精确坐标均校验，且先验证磁盘图片与当前manifest相符。其他路径、未知像素或版本替换均失败，不放宽为通用误差或视觉批准。

本轮15项色板/历史像素/当前驱动测试通过；上一批执行的共享详情18项中，11项焦点/生命周期/基础渲染所涉字节和运行条件未变，沿用其通过结果，共26项相关单测。格式检查通过。产品、依赖、配置与上一批构建时相同，复用已通过的前端构建、252资源预算及类型检查，不重复构建；文档同步后再执行文档与运行说明门。构建仍是含其他在途UI的工作区包，不是干净生产发布包。

没有新环境变量、接口/OpenAPI、业务数据、数据库、权限或依赖变动；用法不变，后续正式前端发布后浏览器刷新生效，无后端/Worker/Python重启要求。本轮未部署。无新一次性图片、日志或测试文件；原临时目录清理待办和全部待审视觉范围不变。只收口本批共享组件及其直接验证依赖，P57和其他页面继续。

## 批次 05：P57 C通知工作台与离页/分页修复

批次04已提交为`699ac3f3`。本批接管P57专用Center/Management/Facts/Operations/Pagination、消息工作台/编辑/动作窗、对应类型/三个composable和局部CSS/令牌；PlatformManagementCenter将notifications交给专用入口，NavigationShell只排除6个内部非路由组件。筛选调用方传notifications外观与dialog模式；原共享筛选测试中待提交的P57调用方项一并收口。没有改平台权限、后端API、版本/幂等字段、受众、邮件关闭或实际发送机制。

本轮先复现8个失败用例：离开后旧保存/动作成功触发读取，失败覆盖新提示，读取期间离开后的完成反馈污染，以及两个分页目标失败后同按钮失效。修复为独立页面生命周期代号和stop；组件停用/卸载统一失效化，写请求前后及刷新后均检查原归属。普通关闭仍保留原完成反馈；新访问正在提交时旧finally不得清除它的忙态。失败文案固定原publish/cancel语义，不借后来窗口的动作改名。分页防重复以成功快照页为准，保留20/10、请求范围和原URL成功后同步策略。

13项新生命周期/分页单测、7项平台通知合同/服务检查与8项共享筛选测试通过，共28项；实际Vue/router/KeepAlive双端12组通过，内存写样例不产生业务网络请求；原实际App E2E双端25通过、1按原设计跳过。新建/六受众编辑/发布/三状态正文/空配置/403/保留快照/冲突/双分页均保留；没有执行真实发送。旧源码行数门槛1050已在接管基线1068行处失效，本批验证父组件确实降为1061行且无关RecordList与基线字节一致，不把阈值放大到1100或改动无关列表。

Web构建（含类型检查）、252资源预算、文档门、运行说明门与25个代码文件的格式检查通过；配套规格、Feature Map和运行说明同步。生产接口、OpenAPI、环境文件、数据库和外部依赖不变；生产不部署、不重启。源级代码落地不等于整页视觉、完整读屏/触控/主题/角色、父包装器追踪归属或真实投递验收。历史静态提案及40张Vue图未重绘，当前源码已演进，不作为现版全量证据；这些审核资产/历史核验另批整理。

永久回归新增`tests/unit/platform-notification-lifecycle.test.mjs`与`scripts/verify-platform-notification-lifecycle.mjs`。E2E关闭SCOUTOPS_UI_PHASE2_CAPTURE，仅在OS临时目录生成测试运行状态；正常构建产物保留。对`C:\Users\23136\AppData\Local\Temp\scoutops-p57-e2e-06b1716f58dd44679808953f41833ab9`的受限路径校验清理调用被工具策略拒绝，未绕过或重试；该目录留待人工删除，不进入Git或发布包。只读检查确认其仅含`.last-run.json`，测试浏览器和服务已结束，4101/5173没有监听；原P34临时目录受阻记录不重试处理。全73页与P57剩余验收继续。

## 批次06：P57追踪归属与当前局部图

批次05提交`18cd046f`。实查父包装器的共享requestId会被任意读/写响应先行更新，不能代表P57当前显示的快照。P57改为接收原统一客户端的完整响应信封，列表只在已通过世代检查的成功读取中同时接受data/request_id；失败读取编号独立记录，重试先清空失败编号，缺少元数据时不沿用其他编号。写入使用局部拆信封适配器，保持原错误提示/cause/AbortError和payload，不再修改父包装器的读取编号。其他域的api包装器完全不改。

新增8项追踪用例，连同原13项生命周期/分页及7项通知合同共28项通过。真实父组件、统一API客户端和Vue/Router/KeepAlive的18组双端验证通过；默认不生成文件。独立App双端25通过/原1跳过，构建含类型检查及252资源预算通过。未访问业务后端、未真实发布。

固定审核参数`node scripts/verify-platform-notification-lifecycle.mjs --capture-review`仅生成新包且拒绝覆盖现有目录；默认复验无文件写入。本次保留[trace-review-r1](design/platform-notifications-direction-c/trace-review-r1/README.md)4PNG+manifest作为永久交付，核对175份来源和4张图片原始哈希均一致。用户只通过两张手机图底部的追踪标签、展开编号与蓝色焦点；桌面图、整页、完整无障碍/主题/角色、真实权限与投递仍开放。原40张P57图保留历史身份，不替换或自动扩大批准。

本批没有API/OpenAPI、环境变量、配置、数据库、权限或外部依赖变化；内部函数信封类型与父子消费者同步。未部署、不重启，正式前端发布后刷新生效。浏览器与测试服务已结束，4101/5173无监听；E2E新临时目录`C:\Users\23136\AppData\Local\Temp\scoutops-p57-trace-e2e-5c85f9bd75b728`仅含`.last-run.json`，路径校验后的删除仍被工具策略拒绝，未重试或绕过，待人工清理且不进入Git。其他原清理受阻目录保持原记录。全73页目标不收缩。

## 批次07：P57弹窗生命周期、字段关联与错误区

批次06提交`1a969fd5`。真实Vue先复现手机阅读窗Tab越界、缓存返回重开、变为桌面后仍打开，以及提交中关闭桌面动作窗的返回焦点丢失，分别在两种动效模式出现。阅读转编辑实测通过，保留原行为。只修改P57三个组件及两个局部辅助模块：阅读窗停用/卸载关闭且丢弃离页焦点、760px现有断点恢复；三个原生窗共享严格可见/可用Tab边界；仅原动作按钮被禁用时回到同一消息目录，其他窗口已聚焦时不夺焦。全局useModalDialog不改。

字段帮助与空受众说明用实例ID关联；动态标签名称不再拼入计数字样。表单aria-busy及dialog错误描述不更改required/min/max、受众和payload，也不将通用请求失败标成字段错误。32组双端/双动效实际组件回归、18组父组件/统一客户端回归和28项相关单测通过。实际图片再发现手机错误区被1fr拉伸，按baseline-ui局部布局规则修正align-self与footer网格行，并增加文本几何高度断言。原组件行数门槛和体积预算均未提高；最终Web构建含类型检查与253资源预算通过。

永久新增无文件默认验证器`verify-platform-notification-dialogs.mjs`；可选`--capture-review`只创建固定dialog-review-r2目录且拒绝覆盖。保留[修复前r1](design/platform-notifications-direction-c/dialog-review-r1/README.md)与[当前r2](design/platform-notifications-direction-c/dialog-review-r2/README.md)，共8张局部PNG、两个manifest及原驱动存档，不将对照图标为通过。r2四图中仅手机取消失败改变，另三张原始字节一致；用户通过手机错误提示区域的高度/留白/说明排列，不包含按钮全部状态、完整弹窗或真实取消。每包36份加载来源指纹不宣称完整构建来源清单；旧trace-review-r1保持原捕获身份，组件演进后不能再声称175份哈希全部匹配当前。

本批没有业务API/OpenAPI、环境变量、数据库、依赖或权限变化，未部署、不重启；未来正式Web包发布后刷新浏览器生效。新增文件均为永久产品、验证或审核交付，未新增一次性截图/日志/测试目录；所有本轮测试浏览器/服务已关闭，旧受策略阻止清理的目录未重试。仍有完整无障碍、主题/角色/软键盘、全控件复合状态、真实投递、完整73页与生产验收待办。

## 批次08：P57取消按钮复合状态

批次07已提交 `846ee29e`，用户仅批准手机取消失败 r2 错误区；手机保存中组合另待审核。本批按 fixing-accessibility 的状态与焦点规则，实测确认原因窗危险按钮 hover 变蓝及焦点受全局主题深色覆盖。只增加 `platform-notifications.css` 的 5 行局部声明，保留原红色、蓝色 token、禁用淡化和全部业务行为，不处理其他页面或全局焦点。

新增永久验证器 `verify-platform-notification-button-states.mjs`；24 个双端/三主题/双密度/模拟粗细指针环境、144 项状态通过，含媒体查询核对和本地一次提交；不是完整 App、真机或全无障碍验收。既有32组双动效弹窗和7项通知合同通过，Web构建含类型检查与253资源预算通过。[按钮审核图](design/platform-notifications-direction-c/button-review-r1/README.md)含8张局部PNG、18份加载来源指纹；手机四种底部按钮组合获用户局部通过，桌面、完整弹窗、真机触控和真实取消未批准。默认验证不写文件，首次捕获目录已存在即拒绝覆盖；旧dialog-review-r2源码指纹现不能宣称全部匹配当前。

没有API/OpenAPI、环境、依赖、权限、数据或生产变化，无需重启；未来正式前端发布后刷新生效。新增文件为永久测试与审核交付，没有新建一次性临时文件；测试服务和浏览器已关闭。原清理受阻项不重试，全73页及真实验收仍未完成。

## 批次09：P57保存与发布主按钮状态

批次08提交 `7aad0879`。继续实际组件复现保存 hover 时蓝字蓝底和编辑窗焦点被全局深色覆盖；仅在两份局部CSS加6行，恢复保存白字/现有深蓝悬停和草稿窗原蓝色焦点。发布原样式通过，不为统一而改动其规则；新建/编辑/发布/取消校验、事件、payload均未改。

扩展既有按钮验证器 `--primary`，不复制新一套驱动；新建/编辑/发布72环境432状态通过，取消144项、原双动效弹窗32组、通知合同7项通过。真实模拟媒体查询与可用主按钮文字对比度被检查；一次原生校验后滚动使按钮重新进入视口是验证器修正，不算产品修复。[主按钮审核](design/platform-notifications-direction-c/primary-button-review-r1/README.md)保存16张底部局部图，待审。旧取消图与其`7aad0879`捕获身份/用户批准保留，源码指纹因验证器和CSS演进不能再宣称全部匹配当前。

默认验证与`--primary`均不写文件，可选`--capture-review`分别使用两个固定、排他创建的目录。16张图与20份来源指纹逐一匹配，手机保存/发布两组已请求局部审核、待答复；Web构建含类型检查、253资源预算及文档/运行说明门通过。无新临时测试文件、无生产访问或部署；测试服务关闭，正常Web构建产物与正式审核图保留，旧受策略阻止清理的目录未重试。API/OpenAPI、环境、依赖、业务权限无变化；无需后端重启，未来前端包发布后刷新生效。全73页、真实保存/发布和整页审核继续。

## 批次10：P57实际App焦点与筛选组合

批次09提交`f7d061d1`。按fixing-accessibility局部焦点/标签/控件规则，从真实App入口验证P57；先复现31/40项焦点失败，修正局部变量后截图又暴露阅读窗危险hover蓝色（过渡前采样误报）和提交后误命名filter。r2稳定等待及正确捕获通过44项，但图片发现Teleport筛选表单失样式。最终局部CSS恢复蓝焦点/红色危险hover，新notification-filter样式只限通知portal；共享PlatformManagementFilter仅通知域加可见标签/实例ID，不改变字段、选项、事件及后台契约。

新增永久实际App验证器，复用原E2E通知/导航样例，拒绝外部及非允许请求，不发写操作。双宽/双动效56项通过，覆盖稳定色值、字段几何/44px目标/可见关联标签、两类分页URL、编辑/阅读返回及筛选应用关闭；32项相关单测通过。复用并接管现有未跟踪CSS-import指纹helper及其2项测试作为直接依赖，不触及其他在途预览。r1/r2各12图原样保留并明确撤回/替代，r3新增12图与186份来源指纹；中间源码没有完整存档，不宣称旧包可独立重放。手机筛选r3待审，取消按钮四组合旧批准范围不扩大。

实际完整App还存在暖色旧外层导航和重复标题，必须继续整合；长图固定导航位置有截屏限制，不视为遮挡检查完成。全73页、全状态、真实权限/发送和生产签收仍未完成。本批无API/OpenAPI、环境、依赖、数据库、权限或部署变化，不需后端重启；以后正式Web发布刷新生效。默认验证不写文件，可选capture只首次排他创建app-review-r3；本批新增均为永久验证/审核交付，正常构建保留，旧受策略阻止清理项不重试。

Web构建含类型检查通过，资源预算253项、73路由产物、153文档门及运行说明一致性通过。当前r3全部图片/186来源哈希复核匹配；r1/r2仅核对图片完整性。测试浏览器/服务已关闭，4101/5173/5174无本批残留监听；没有新增一次性临时文件。

## 批次11：P57与C导航实际Vue组合提案

批次10提交`19cf0f02`。继续解决其实际App混用旧外层导航/重复标题的问题；复用已有C壳层review transform/CSS/菜单composable作为直接依赖，新增P57专用review transform/CSS。仅审核宿主抑制P57外层重复页名、提升内部h1并移动原aside至标题后形成横向分区；业务script逐字不变（共享壳层仅既有两个明确review新增语句），事件及菜单目标保持。生产NavigationShell/P57源码不改，等待组合获审后继续实现；不是把截图当成生产部署。

首轮菜单查询错用textbox，核对原input type=search后修正验证器为searchbox，产品未改；两张未完成临时截图经确切路径核对清理。随后r1完整16图/68检查目检发现旧荧光边线和衬线标题，保留诊断不申请批准；r2修正提案字体/边线，16图/191来源哈希及4环境72检查通过。原通知样例10个授权菜单与真实辅助函数结果逐项相等，手机模态首尾Tab/搜索无结果/Escape返回和桌面非模态通过；并非所有角色/断点/主题/真实业务。3新测试+2来源测试+既有3共享源测试通过；旧共享整包历史哈希测试未用作当前证据。无参数原App56项继续通过，旧app-review-r3驱动指纹已漂移、旧图与批准保持原身份。

当前双端首屏及审核边界见[组合r2](../../output/playwright/p57-shell-composition-r2/README.md)，待用户审核；所有菜单/主题浮层/全状态及完整73页继续。没有生产源码、API/OpenAPI、环境、依赖、数据库、权限或部署变化，无需重启；本批SFC编译直接验证新变换，没有新生产字节因此不重复Web构建。默认不写文件，可选--shell-preview --capture-review仅创建固定r2，存在拒绝覆盖；全部服务/浏览器关闭，永久图包保留，历史受限清理待办不动。

## 批次12：P57跨断点焦点与窄桌面阅读

批次11提交e938f40d。本批沿用fixing-accessibility，在实际C审核App扩展--shell-preview --responsive：840/841双动效4环境；菜单展开转桌面、桌面菜单持焦转手机、390/1440往返、编辑跨宽保留输入/焦点、三个底部/主操作键盘目标三点命中及顶栏稳定性。初版96项通过但图揭示841阅读宽287px/草稿两行42px；补文本Range检测真实复现后，仅审核CSS加非手机命名容器规则，消息容器≤640px改上目录下阅读。没有改760/840原行为或生产文件。

最终112项、常规C72项、原App56项与5项定向单测通过。r1四图保留窄阅读诊断；r2四图/108项因首屏未绘出顶栏而替代，不认定为产品故障；r3增加两帧等待、顶栏可见/位置/命中并移开首屏指针，4图/192来源逐一核对。底部图目录悬停不代表第三项选中。当前[窄桌面审核](../../output/playwright/p57-shell-responsive-r3/README.md)待答复，旧首屏与筛选审核不扩大；其他宽度/主题/角色/所有控件/真实发送/全73页验收继续。

新CLI默认无产物、缺少shell/重复/未知标志在启动前拒绝；capture只首次创建固定responsive-r3。旧composition-r2图不覆盖，源码指纹因CSS/驱动演进不再全匹配当前。无生产源码、API/OpenAPI、环境、依赖、权限或部署变化，无需重启；新规则经浏览器和SFC编译验证，不重复生产Web构建。新增均为永久交付，无一次性临时文件，测试服务关闭；旧受策略阻止清理项不动。

## 继续整理

后续共享导航、平台各入口与审核材料按实际引用和测试依赖分别核对，未提交部分不计完成。产品变更与审核预览分开；只有相应必要验证通过才能提交。全73页的设计意见、完整交互、真实后端、质量门和生产签收尚未完成。
