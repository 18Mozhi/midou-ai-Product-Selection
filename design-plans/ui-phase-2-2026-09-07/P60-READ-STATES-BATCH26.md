# P60 读取、失败与恢复 · 批次26

审核入口：[r2实际Vue 20图](../../output/playwright/p60-read-states-r2/index.html)。本次申请手机首次超时白色区域的说明与“重试”按钮组合审核，不包含顶部导航、完整权限、真实取消请求或生产验收。上一批详情、创建区域仍按原范围待审；P57已批准的四种取消按钮不扩大为整窗通过。

## 改动与事实边界

实际 `OpenPlatformCenter.vue` 修正读取反馈：首次失败不再重复显示横幅和状态区，技术编号留在当前失败区域；只有已有快照时才补“当前仍显示上次成功结果”。只有 AbortSignal 的实际 reason 为 timeout 才称15秒超时，措辞为“已停止本次等待”，不声称服务端工作已安全取消。加载与刷新补命名标题、状态消息和忙碌语义；无真实进度百分比或估算时长。

原15000ms计时器、单飞、API重试、状态映射、查询/分页和权限保持。读取超时沿用blocked分类；401/403仍保留原“重试”入口，本批不新增登录或申请权限流程。首次没有数据时不虚构旧结果；保留数据后403的清理策略没有更改或验收。原 `call()` 在写成功后的刷新失败时可能覆盖读取提示，这一问题尚未修复，不因本批读取测试通过而视为解决。跨组织/跨页归属、KeepAlive和密钥清理仍待核对。

frontend-design用于C方向白底状态区、左对齐标题、独立浅灰追踪和手机全宽重试。fixing-accessibility用于标题关联、状态播报及键盘追踪展开；完整读屏不在本批证明范围。这些C样式及h3到h2的两处审核层级变换仅在审核宿主加载；生产源码只含上述反馈及语义修正，未把整页未审CSS部署。

## 验证与图册

12项定向测试先复现4项错误表达，再验证修正；与原页面、详情、M06-05合同合计32项通过。VM计时器断言15000，卸载中止测试只证明措辞，不证明完整卸载生命周期。前端类型检查/构建通过，253份资产预算通过。原双端页面/创建流程134项、6次本地GET回归通过。

10种场景：首次加载、401、403、429、503、400、保留数据刷新、保留数据503、首次超时、保留数据超时。1440/390双宽、reduce/no-preference双动效；每组27次本地模拟GET，429/503保留实际三次尝试。超时等待真正的应用15秒计时器，不替换为加速时钟。检查数据是否存在、单一提示、追踪归属、忙碌状态、恢复清除提示、原重试与无横溢出；未知/外部请求和写请求全部阻断。

初轮680项通过，但检查图揭示固定手机底栏遮住状态底部。r2在审核CSS增加96px底部滚动留白，并新增7处每组底部命中测试；滚动后检查区域底缘仍在视口且未被别的元素覆盖，不隐藏底栏、不改截图像素。完整r2为4组708项、108次本地GET及20图。原r1的20张检查图与索引/manifest删除命令被工具策略拦截，未重试绕过；不是正式审核交付，也不提交。滚动/鼠标命中不等于真机触控或完整焦点测试。

r2使用HEAD 3d4f3511加本批工作树，绑定169份原始来源和20张原始PNG。保留旧数据时的提示截取不包含下方全部记录；刷新长图和局部状态图不等同整页批准。详情旧r2仍有24图，169来源中166匹配，差异为组件、共享验证入口及上一批焦点helper；页面旧r2有26图，166来源中164匹配，差异为组件和共享入口。旧图全部哈希一致，不重绑历史manifest。

## 使用、部署与收尾

- `node scripts/verify-open-page-preview.mjs --read-states`：四组检查，不生成文件。
- `node scripts/verify-open-page-preview.mjs --capture-read-states rN`：相同检查并独占创建 `output/playwright/p60-read-states-rN`；拒绝已存在目录、不安全版本和混合模式。当前r2，后续用r3。
- 原无参/创建/详情模式保留。96px为P60手机首次状态审核留白，不是生产配置；15秒与HTTP重试未调整。

同步规格、地图、计划、索引和运维说明。未改API/OpenAPI、后端、Worker、Python、插件、数据库、依赖、环境或权限；这些消费面与本次纯前端反馈修正无契约变化。未部署、迁移或重启；未来批准发布前端时须经固定宝塔部署流程，浏览器刷新后加载新资源，不需本批额外后端重启。

正式r2为永久审核交付；测试服务和浏览器由finally关闭，无新日志、下载或一次性脚本。遗留检查包为 `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p60-read-states-r1`（22文件），用户可在资源管理器确认该精确目录后手动删除，不需停止服务。正常生产构建产物保留，其他批次遗留文件不动。整页、完整读屏/真机、写后刷新、独立行原因、密钥、真实权限/发送以及完整73页实施部署仍待完成。
