# P43 真实应用、路由与缓存下的创建/改密归属

2026-09-13。本批是当前未变换 App 的功能证据，不是新的 C 方向设计提案。
旧视觉批准、原设计图和历史来源包不改，不要求用户把当前原界面当作新设计重新审核。

## 实际覆盖

直接启动仓库 Vite 入口，加载 main.ts、App、真实 router、NavigationShell 与 KeepAlive；
没有替换模板、注入孤立父组件或修改 Vue 内部状态。通过可见导航链接建立历史，再用浏览器
前进/后退穿过两条真实路径：用户页与管理员页共享账号组件；用户页与平台概览间切换缓存组件。
离开时检查旧节点的连接状态，回来后检查同一个节点身份，避免把重新挂载误认成缓存恢复。

| 维度 | 覆盖 |
| --- | --- |
| 视口 | 390、1440 |
| 操作 | 创建用户、强制改密 |
| 路径 | 共享账号路由、缓存平台概览 |
| 旧请求结果 | 成功、失败 |

16 组、224 项命名检查通过，48 张截图，168 项实际加载及驱动来源哈希。
手机最小验证先运行 8 组112检查，无图；随后双端完整捕获通过。
已逐张抽看手机创建成功后新草稿和改密失败后替换详情，未发现旧提示进入替换窗口。

动作顺序：提交旧请求并暂缓本地回执 → 取消窗口（改密还关闭原详情）→ 前进/后退 →
打开新创建草稿或账号详情 → 释放旧成功/失败回执。验证新窗口不被关闭、创建字段保留、
错误不污染新窗口、成功通知仍保留、成功才重读列表。等待期间改密禁用，不强制点击禁用按钮。

## 数据、来源和验证入口

145 次 GET、16 次 POST 全部由本地浏览器拦截；未知 API/外部请求直接拒绝。
账号、角色、导航和平台概览从现有 m06-01 E2E 样例严格提取，重复概览事实不一致会拒绝。
原 Chrome 详情缺少 membership.organization_id 的已知限制明确保留，没有修补虚构业务字段。
可选主题读取采用本地503失败路径；未验证主题持久化。所有密码均为合成样例，不记录到证据字段。

- [48图功能证据入口](../../output/playwright/p43-actual-app-lifecycle-r1/index.html)
- [完整检查与来源](../../output/playwright/p43-actual-app-lifecycle-r1/evidence.json)
- 当前无图重放：`node scripts/verify-ui-phase2-account-app-lifecycle.mjs`
- 手机最小重放：同命令加 `--smoke`
- 永久门：`node --test tests/unit/ui-phase2-account-app-lifecycle.test.mjs`

5项新单测验证样例提取/失败关闭、16组合与48图字节、真实App来源、重复捕获防覆盖。
随后包含创建/改密实际归属函数、原历史包及当前回放保护的6文件46项关联测试全部通过。
文档门153项、运行文档一致性、新代码和地图格式、定向diff检查通过。产品源码未改，不重复
前端构建或已知失败的全库单测；上次完整报告仍保留原样，不据本轮局部结果改写全库结论。
`--capture` 当前在服务器/浏览器启动前以 EEXIST 拒绝；不得覆盖 r1，源码有变化须新版本重新
采集并保留旧包。完整清单 SHA256：
`742a2d8290fd0ae1ac6afce0197e7779c75187cb782cdb0f1e6a03f20d484aed`。

## 未覆盖和收尾

没有改产品 Vue/CSS、业务/权限规则、API/OpenAPI、数据库、后端/插件/Python消费方、环境变量
或依赖；不涉及运行契约同步、生产重启、迁移或部署。新增 CLI 仅是本地审核工具，命令已同步
功能地图与运行说明。没有将已取消弹窗场景扩写成原生弹窗保持打开时直接离页已通过；后者、
敏感字段清理策略、完整 C 装配、真实改密/审计/权限和全73页仍待完成。

本轮临时 Vite/浏览器已由 finally 关闭；收尾确认5173、64121无监听。48图、HTML、
来源清单和新测试是交付物，保留于上述入口，无新临时文件。此前四个清理被策略拒绝的目录
仍按[上一轮记录](P43-LIFECYCLE-CURRENT-REVIEW.md#使用及收尾边界)保留，不重试或绕过。
已有全库报告仍是272文件1558项1432通过126失败的历史快照，本轮不推算新总数。
工作区含混合既有变更、发布门未通过，未暂存/提交；commit hash 不适用。
收尾Git状态611项，暂存区为空；仅新增本轮4个永久源码/测试/记录文件并补充相关已有文档，
未处理或提交其他用户/任务变更。
