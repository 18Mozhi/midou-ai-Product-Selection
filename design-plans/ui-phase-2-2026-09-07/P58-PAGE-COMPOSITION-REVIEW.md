# P58 C 工作区结构审核 · 批次 13

状态：待用户审核；不是生产实现完成或全 73 页验收完成。

## 本次范围

实际 App / Router / NavigationShell / CommercialOperationsCenter 在本地审核宿主运行。仅审核转换增加 C 导航、单一 h1，以及“方案目录 / 组织配额”两个工作区。全局方案统计留在目录；组织读取、分配、周期用量和人工调整留在组织区。原 UUID 输入、业务脚本、字段校验、请求地址和三个 Teleport 弹窗保持。无启用方案时的“下方搜索”改为“在方案目录搜索”。读取入口独立于读取失败分支，避免失败后不能修正编号。

`frontend-design` 用于延续已选 C 方向的蓝色导航、白色内容区、无衬线标题和局部控件层级。`playwright` 使用缓存 CLI 做实际浏览器检查；没有安装或升级依赖。危险“退役”按钮保留红色；原弹窗未被本次页面 CSS 覆盖。

## 证据与边界

- [7 张图与来源清单](../../output/playwright/p58-page-composition-r1/README.md)：1440 / 390 两工作区全页、手机两首屏、手机未读取组织。
- 数据来自 `tests/e2e/m06-06-commercial.spec.ts`。无组织与 `no-match-review` 空目录为明确的本地派生样例；全局统计不变。`o1/p1/a1` 是原测试内部值，不是有效业务 UUID，不可作为用户真实输入示例。
- 4 个宽度/工作区组合：无横向页面溢出、单一 h1、可见按钮/输入/选择框高度至少 43px（样式目标 44px），方案统计与组织用量互斥显示。
- 切换不增加读取请求；未提交日期保留；组织区仍可打开并关闭原新建弹窗；蓝色键盘焦点 3px。
- 查询空结果不改变全局 1/1/0/0 统计；重置恢复目录；清除组织恢复未读取状态；无效 `o1` 提交被原生校验阻止、没有请求。
- 首轮控制台 error / warning 均为 0；请求清单仅 GET。第二次运行验证了默认无产物宿主与无截图回归脚本。
- 7 项相关单元测试：4 项 P58（原脚本/样式/校验、SFC 编译、弹窗与错误入口位置、CLI 表达式和参数拒绝），3 项已有 P57 C 组合回归。
- 未覆盖：全部断点/主题/角色、完整读屏、所有 hover/disabled 状态、真实浏览器触控、原弹窗完整状态、真实 API/权限/配额写入、离页生命周期与生产验收。创建响应丢失的冻结原内容/原幂等键策略仍等用户决定，未实现。

## 使用与运行边界

在仓库根目录执行 `node scripts/preview-platform-commercial-page.mjs`。固定监听 `http://127.0.0.1:5174/platform-admin/commercial`，端口占用则失败，不自动结束其他进程。默认不写图包；Ctrl+C 停止。全部 `/api/` 请求由本地固定样例处理，未知或写请求返回 405，不转发真实后端。没有环境变量、生产 API、配置或依赖调整，OpenAPI 与生产运行文档不需修改，不需要生产重启。

可打开 `/platform-admin/commercial?organization_id=o1` 检查原测试组织。CLI 的 `run-code --filename` 可运行 `scripts/lib/platform-commercial-page-browser-checks.js`；先读取该页快照，脚本使用已检查的页面控件。脚本不写文件。截图由显式 CLI screenshot 命令生成，不能覆盖已送审图包。

`--capture-review` 仅用于首次创建固定 r1 图包；目录已存在时失败关闭。当前已保留图包，因此重复审核请使用默认无产物模式。创建新版本图包需要单独明确新的审核版本与路径，不删除旧版。图包清单有 166 个读取来源摘要；捕获后宿主增加默认无产物模式和参数检查，仅宿主驱动摘要发生变化，其余 165 个来源与 7 张 PNG 均已核对。清单是本地来源记录，不是可独立重建的生产发布包。

## 当前审核与后续

已向用户展示桌面方案目录和手机组织读取首屏，询问两个工作区的结构；尚未收到批准。没有把此前 P57 四种取消按钮的局部批准扩大到 P58。整体计划继续，下一步依用户结构反馈细化 P58 其他状态，再整合经批准的弹窗。已有其他 P57 待审图与全 73 页任务不撤销。

本轮只暂存本批次文件以及 Feature Map / 当前计划的新增小段，保留其他在途修改；不部署、不操作数据库或 BaoTa。浏览器会话和 5174 审核宿主已停止。CLI 临时目录 `output/playwright/p58-cli-scratch-20260915` 的删除被工具策略拦截，6 个快照/日志仍保留、未暂存；不绕过拦截重试，可由用户在资源管理器中删除这一明确目录。正式 7 图和清单保留为审核交付物。
