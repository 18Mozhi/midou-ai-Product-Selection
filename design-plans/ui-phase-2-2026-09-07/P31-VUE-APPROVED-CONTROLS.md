# P31 已批准控件 · 真实 Vue 落地

最新增量：[延期组合已进入真实Vue](P31-VUE-EXTENSION-REVIEW.md)，当前同一路径扩展为双端54检查/22图；下文30检查/12图及“延期未移植”为本文件首批历史边界。

2026-09-10，从干净main/fb97d335开始。此前四项控件批准见P31-CONTROL-VISUAL-APPROVAL.md；手机延期组合另见P31-EXTEND-COMPOSITION-APPROVAL.md。本批不将局部批准扩大为整页批准。

[真实页面图册](../../output/playwright/p31-approved-controls-review/index.html) · [同源证据](../../output/playwright/p31-approved-controls-review/evidence.json)

## 实际改动

在apps/web/src/organization-admin.css增加P31范围规则，并按既有P16惯例通过design/roles-tokens.css延迟加载局部语义变量：蓝色3px键盘焦点与白色间隔、原生禁用按钮不透明灰底、已选且禁用状态筛选的2px边框与加粗、撤销原因确认的红底白字。共享弹窗通过父页存在直接子级org-role-page限定，不修改AuditedReasonDialog或其他调用方。使用#app局部作用域覆盖现有无障碍样式的禁用透明度，不修改全局规则。颜色门禁增加仅允许该精确P31作用域下声明变量的约束，普通CSS仍禁止原始颜色。

旧页面布局、字体、壳层、摘要、字段、校验、事件、API、权限、原因规则、UUID复制及异步生命周期不变。手机延期组合尚未移植；此图册仍可看到旧风格，不能当成C整页重构完成。

## 验证方法与边界

`node scripts/verify-ui-phase2-roles-vue-controls.mjs --smoke`先做手机15项检查；`--capture`生成1440/390双端30项检查与12张永久图；无参数核对来源及PNG指纹并重新验证。使用仓库既有TypeScript/Playwright/Vite，不安装依赖。通过AST只提取原m06-01测试顶层数据及setup，复用原接口夹具，挂载真实路由，不执行原测试文件中的测试注册。未知HTTP默认拒绝，不连接真实API、数据库或生产。

覆盖键盘focus-visible、原生禁用重置、持有真实刷新响应时的选中禁用标记、撤销窗正常/无效原因禁用、Escape取消返焦，以及P30撤销邀请共享窗没有变红。全程零业务写请求、未知HTTP与Vue错误。保留CSS过渡，等现有动画结束后检查终态，不将动画中间颜色误判为失败，也不改变产品来迎合测试。

Vite使用先探测空闲端口再严格绑定的本机临时服务；浏览器、上下文和服务finally关闭，无常驻服务。PNG、index.html、evidence.json属于用户要求的永久审核材料；本次无失败截图/日志临时文件。构建产物和已有依赖缓存保留。

## 使用与发布

最终验证：27项定向与638项完整单测全部通过；真实Vue双端30检查/12图采集及无参数源图复验通过。build:web（vue-tsc、Vite 407模块）、251资产体积门、390文件静态检查、153文档、运行文档和73路由/60受保护/6角色门通过。图源审计旧94提案包仍13723图、零漂移；本批12张真实Vue图独立登记，不混入提案包或用户整页批准数。审核台双端筛选/焦点/批注导出通过，临时下载目录已消失，测试夹具无残留。

无新环境变量、配置、依赖或操作方式；Tab焦点、现有刷新与撤销流程直接使用新样式。生产尚未部署，不需要当前重启；之后按完整发布门禁使用既有本地构建/宝塔部署命令，不能将本批本地检查当生产验收。

真实接口权限、跨组织生命周期、其他主题/密度/浏览器、全页C布局及剩余页面、全部组合审核和宝塔签收继续待办。原HTML图册和采集指纹不重写成真实Vue证据。
