# P16 C 原值语义令牌与验证闭环

## 决策边界

上一轮有实际进展：P26读取批次修复及双端18项回归通过，但全量445/446因既有P16直接色值门禁失败未提交。本轮以main/2b5a8898及上一轮未提交工作树为依据，保留全部已有改动。

核对后发现无需改变外观或业务：24个既有C颜色可以集中成只作用于 `html #app .selection-journey` 的 `--so-selection-*` 语义令牌。UI Skills/baseline-ui要求优先使用既有变量；旧全局令牌是朱砂纸张色，直接替换会改变已批准C外观，因此保留C原值与作用域，不改变旧主题或恢复旧布局。不新增依赖、动画、交互原语或配置。

## 改动

- `design/selection-tokens.css` 增加P16局部主色、表面、文字、字段/选择边框、等待禁用、焦点及成功/警告等颜色角色，原有全局 `design/tokens.css` 不变。文件仅随该页CSS导入，不增加首屏全局令牌体积。
- `selection-journey.css` 只替换颜色引用，保留既有局部别名；导入集中令牌文件保证独立挂载宿主也能解析颜色，不依赖全站main先行加载。
- `ui-phase2-journey-color-tokens.test.mjs` 将所有新引用还原为实际色值后，对整份CSS计算SHA256，必须等于修改前基线a848229c6c668111e08f2fdef9f63724b4b29dbb745622d1fac5eff480a1af08；包括选择器、布局、响应式、密度和按钮状态。另限制24变量仅声明于P16作用域。既有语义色门禁仍禁止普通CSS/Vue直接写颜色，仅对这个精确命名的令牌文件增加严格的整文件语法校验：只能包含注释和P16作用域颜色自定义属性，不能带普通CSS声明、其他选择器或额外规则；不是忽略整个目录。
- 壳层组合验证器新增令牌源指纹依赖；已有真实Vue、字段和壳层组合图册随源码重验。不增加批准页数。

## 验证与未覆盖

定向语义色/主题基线及CSS原值恢复6项已通过。首次集中到全局令牌时完整447项单测通过，但首屏CSS123425字节超122880预算545字节，因此改为上述局部加载文件，不扩大预算。最终完整单测、构建、147张既有真实/组合图证的结果按收尾补记；P26上一轮12项源测试与18项双端回归仍有独立证据，其五项写入/草稿边界未被本轮处理。

没有修改P16模板、业务handler、五质量门、API/字段/权限、主题选择与偏好保存、数据库、环境变量、Worker或Python；OpenAPI及运行配置无需同步。全站C重构与具体按钮/字段审核未完成。未部署；将来本地 `npm run build:web` 后按项目宝塔流程上传，浏览器刷新生效，无需后端/Worker/Python重启。

本轮正式截图/清单是审核交付物，保留；不重新删除上一轮被策略拒绝的 `output/playwright/p26-read-batch-20260910/.last-run.json`。当前回归使用验证器自管5175服务，结束时核对关闭。提交以前最终检查工作树并只暂存本任务文件。

## 最终验证记录

- 最终模块拆分后 `node --test tests/unit/*.test.mjs`：447/447通过。复用上一轮已构建且未修改的contracts/provider-sources/api/worker，不重新安装依赖。
- `npm run build:web`：类型检查及构建通过；`verify:frontend-budget`：251资产通过，原122880字节首屏CSS预算不变。
- `verify-ui-phase2-journey-vue.mjs --capture`：170检查、114图；`verify-ui-phase2-journey-fields.mjs --capture`：96检查、24图、业务写入0；`verify-ui-phase2-shell-journey-c.mjs --capture`：43检查、9图。均使用真实Vue与隔离数据，不证明真实后端/生产或用户审批。
- 文档、运行文档、静态分析检查通过；格式检查首次与单测临时夹具并发产生路径竞态，夹具由测试清理后单独重验，不改规则。
- 最终4101/5173/5175无监听；没有新建人工临时文件，单测自建code-style夹具已不存在。正式图证保留；上一轮被策略拒绝删除的路径仍按上文交接。
- 全站目标仍未完成：P16仅原布局获批，按钮/字段/组合未获全部批准，P26其余写入/草稿归属、其他页面及生产部署继续待办。
