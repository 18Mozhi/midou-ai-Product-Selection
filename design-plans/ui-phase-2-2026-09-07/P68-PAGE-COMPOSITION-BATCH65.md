# P68 实际 Vue C 组合 · 批次65

P68 原有 MYSQL-C-r1 是离线稿。本批新增的 Vite 转换曾用于审核；其 C 构图现已按用户授权接入真实 `MySqlResilienceCenter.vue`，保留页面脚本、GET、15秒、单飞、权限、快照与宝塔边界，以既有 DTO 展示蓝色运行边界、白色运行资源/累计计数、同机恢复证据、持久化实际/目标对照。生产部署、提交与线上核验见[P68实施记录](P68-PAGE-COMPOSITION-IMPLEMENTATION.md)。图册仍是设计证据，不替代真实 MySQL、恢复或生产探针证据。

`tests/unit/mysql-page-preview.test.mjs`通过；实际 Vue 在1440/390和两种动效下共140项检查、32次本地 GET，无外部请求、写请求、下载或页面错误。r7图册为8PNG/173来源指纹，适用于布局审核，不代表真实 MySQL、恢复、权限、审计或生产。数据盘容量判断使用原始字节数，展示时才格式化，避免把已知容量误作未知。

批65补充原有读取分支的手机状态审核：503 在三次安全 GET 重试后保留旧快照，并显示“刷新未完成”及本次请求详情；403 清空快照，只显示“没有平台运维权限”和重新核验入口。r13为局部状态图，390px/减弱动效共41项检查、13次本地 GET、6PNG/173来源指纹，无外部或写请求。局部截图只隐藏固定底部导航的视觉层，不移除节点或改变页面布局；完整导航仍见r7默认页。它不证明真实 MySQL、权限、审计、恢复或生产。

批65继续核验原生键盘路径：从无焦点状态用 Tab 到达顶部“刷新运行事实”与保留快照区“重新核验”，两者均实际拥有焦点并呈现3px蓝色轮廓。r15取代r13作为当前局部状态/焦点图册：390px/减弱动效45项检查、13次本地 GET、8PNG/173来源指纹。它不替代真机、读屏、真实权限或生产验证。

`output/playwright/p68-page-composition-r1`、r2、r4、r5为验证中断时创建的空目录，不能作为图册；r3是修复前的有效但已过时图册，r6因格式化后来源哈希变化而过时；r8/r9/r10为30秒窗口中断的部分目录，r11/r12是固定导航干扰局部状态取景的保留对照，r13因新增真实Tab焦点检查而过时，r14为直接focus不匹配键盘模式的中断目录；默认页只使用r7，局部状态/焦点只使用r15。清理策略不允许删除这些目录，未绕过。

无需新增配置；当前发布已完成宝塔部署、Node/Python 重启和公共健康检查。用户已确认设计审核通过，后续仍需逐项核验焦点、追踪和真实 MySQL/恢复证据。
