# P60 重新读取键盘连续性 · 批次28

[实际Vue r1六十图](../../output/playwright/p60-action-keyboard-r1/index.html)。本次申请手机“读取中 / 读取完成”按钮与所在反馈区域的焦点样式审核；下方错误、全部页面、完整读屏、真机与真实业务不随之通过。上一批操作结果和其他待审范围保持。

## 复现与修正

从main/89cd955a出发，实际Vue键盘回归复现：Tab到重新读取按钮、Enter触发后，原生disabled使该按钮失去焦点。保留DOM本身不能证明焦点连续。按fixing-accessibility，本批仅该只读按钮改为原生button加 `aria-disabled=refreshing`，保持可聚焦；点击仍走原load，首行既有single-flight返回阻止忙碌期间重复GET。没有自行实现按钮键盘角色、正tabindex或异步强制抢焦。

产品模板只改此按钮一行，增加专属类；生产CSS给该类的aria-disabled应用原灰度/禁止光标，hover排除aria-disabled。C审核CSS同样映射已有灰色禁用样式。其他原生禁用按钮不变；业务script与89cd955a按换行归一后逐字一致。GET/POST/PATCH、原因/版本/幂等/权限、15000ms、重试、密钥与写后双结果模型均未变。

## 验证

新增3项定向测试核对实际模板AST、仅此按钮aria-disabled/原生type/click入口、load单飞前置、Vue编译与生产/审核CSS映射；连同原五项双结果、读取、页面、详情和M06-05合同共40项通过。类型/前端构建及253资产预算通过。

实际Vue包含保存后读失败、排队后读失败、保存后读成功、409写冲突，另加长读取说明样例；四宽1440/768/390/320、两动效reduce/no-preference，共8组744项。每组从原确认入口进入，所有请求本地拦截；合计144GET、32PATCH、8POST，其中8次PATCH为模拟409。没有真实数据库、回调停用、密钥生成或外部投递。

恢复链用真实Tab/Enter/Space：进入按钮、等待中连续触发不增加GET/写入、焦点保持、灰色外观与可聚焦禁用语义正确，等待结束仍在同一按钮；所有截图区域上下缘命中原区域且无导航遮挡。灰色背景和蓝色focus-visible最多等待3秒绘制，超时失败，不更改产品计时器。最初焦点修正后720项通过，再加3处每组样式/语义断言后744项通过，不把旧720结果当成最终截图结果。

长文案为本地压力样例：原提示后重复四段说明；UUID形态36字符读取编号仅是测试追踪，非生产编号。核对完整文字、换行及区域/页面无横溢出，不定义服务端字段上限，也不建议把重复文案用于产品。390视口高844，其他三宽高1000，不是设备实测。

原双结果192项/52GET/16拦截写请求和默认页面134项/6GET回归通过。原读取脚本、计时和条件未变，定向读取测试通过，不重复运行上批真实15秒708项或旧详情1556项，也不宣称它们是本轮新运行。

## 图册与历史来源

r1为60张原始PNG、170来源，捕获HEAD 89cd955a加本批工作树；包含四宽默认反馈、长错误、读取中焦点及完成焦点。全部来源和图哈希匹配。图片只来自实际Vue，不改像素，分区图不等于整页批准。

历史包保留捕获身份：双结果r2来源165/170匹配、14图一致；读取r2为164/169、20图一致；默认页r2为162/166、26图一致；详情r2为164/169、24图一致。本批组件/生产CSS/页面审核CSS/验证入口与helper演进，叠加前批既有差异；四份README说明更新，manifest不重绑。

## 使用与收尾

`node scripts/verify-open-page-preview.mjs --action-keyboard`运行8组、不输出文件；`--capture-action-keyboard rN`独占新目录，当前r1，后续r2。原其他CLI模式不变，非法版本、缺参和混合模式在启动宿主前拒绝。三个样式断言的3秒仅测试等待，不是可调业务配置。

同步规格、Feature Map、计划、审核索引和运行说明。API/OpenAPI、后端/Worker/Python/插件、数据库、环境、依赖、权限均未改，无外部契约变化；未部署、迁移或重启。未来正式发布须经固定宝塔前端部署，刷新浏览器加载新资源，本批不要求额外后端重启。

本轮无临时文件、日志或下载，60图为永久审核交付。验证宿主/浏览器由finally关闭；旧读取r1和双结果r1删除受阻记录不重试、不改动。未提交其他在途变更。完整读屏、移动真机、所有主题/密度/缩放、跨组织/缓存/晚响应、独立原因、密钥、全73页审核与生产验收仍待继续。
