# P47 加载、刷新失败与卸载：当前回归及历史证据

## 范围

全73页C重设计继续，本批不改变生产、提案或用户批准。开始为main/af239b08，421项
工作区变更、暂存为空。按UI路由及无障碍技能核对等待、失败、键盘焦点和离开页面；
本地UI CLI缺失，不安装依赖，直接复用项目已有Vite/Vue/Playwright驱动。

## 当前实际运行

下列命令均从仓库根执行，不加capture，不重拍或覆盖旧图，原驱动没有改动：

| 命令 | 场景组 | 检查 | 范围 |
| --- | ---: | ---: | --- |
| `node scripts/verify-ui-phase2-provider-adapter-loading.mjs` | 24 | 330 | 390/760/1440，首次/刷新等待，成功/409，当前页面与待审加载提案 |
| `node scripts/verify-ui-phase2-provider-adapter-refresh-failure.mjs` | 18 | 387 | 三宽度，409/403/503后手动重试，当前页面与待审失败提案 |
| `node scripts/verify-ui-phase2-provider-adapter-unmount.mjs` | 24 | 636 | 390/1440，实际壳卸载/KeepAlive max12淘汰，首次读取/刷新/检查，成功/409 |

共66组1353检查、0图片。加载/刷新驱动的baseline是本轮实际未转换的C页，不是旧源码；
review仍是局部提案，加载只替换两个属性及局部CSS，刷新失败包含说明区、状态和重试
焦点包装，不能视作生产实现。卸载驱动完全使用实际App与路由，只读观察真实实例和缓存，
不手工篡改缓存；为测试淘汰而访问的其余12页接口GET被阻止，不作为它们功能通过。

加载时没有虚构概要，刷新保留原概要、搜索和筛选结果；主动移走焦点后，返回结果不抢回。
409/403各一次读取，503保留原三次安全GET重试，随后手动重试一次；失败提示及追踪保持。
卸载后的新实例重新读取、过滤条件初始化，旧结果不能覆盖新实例、打开旧详情或抢焦点，
没有自动重放POST。所有API是本地样例，非允许传输被阻止，无浏览器错误或真实业务写入。
只证明这些场景，不证明真实探针、权限、生产、GC/内存泄漏、全部主题/动态偏好/读屏器。

两项问题仍明确保留，不用绿色断言掩盖：

1. 当前刷新重试按钮禁用后焦点落到BODY；待审提案将焦点移到持久标题，但未实现到生产。
2. 旧检查仍等待时，新实例检查按钮可用。原服务/注册器隔离测试也证明不同幂等键可同时
   进入检查；这是局部合同证据，不是实际HTTP/数据库并发结论，跨页锁范围/释放规则仍待原决策。

本轮来源路径集合分别169/169/195，旧拍摄包分别170/170/185；当前运行不是重建旧源码
环境，也没有生成新的来源/图片哈希包。不从数量差推断唯一原因或完整依赖覆盖。

## 历史图证修正

三个旧单测不再要求旧图依赖等于后续当前源码，而分别固定完整拍摄提交：

- 加载：255f03f949ebbb27b7714818ee1ce0c458513877，170源、48图。
- 刷新失败：7691b4679bfe75e06c30f0999991c5ce8f83c734，170源、54图。
- 卸载：89a72609f914b58f3de71ef2b36d74a81e536889，185源、8图。

三份完整manifest与相应提交完全相同；所有来源逐个哈希匹配。保留原图片哈希、尺寸、
精确目录、请求/重试、焦点反例和缓存观察断言。当前Vue预览编译/原合同、CSS边界及
实际服务/注册器测试仍直接读取工作区，不用历史源码替换当前执行。
三个完整相关单测文件10/10通过、0取消/跳过，31.19秒。

仅改这些隔离测试和直接文档，生产与驱动输入没有变化；以三套完整相关浏览器矩阵及
三个完整单测文件验证，不重复全库，也不推算此前全库快照的最新失败数量。

## 使用及收尾

运行上表原命令即可复查，不添加capture覆盖历史图。无新CLI参数、配置、环境变量、
API/OpenAPI、数据库、权限或依赖，不需生产重启。全73页及整体发布门仍未通过，
不提交、不部署，commit hash不适用。继续其他状态与未完成页面，既有待审问题不重复发起。

五个宿主端口62759/62876/62913/63065/63189均已无监听，浏览器及测试进程结束。
无新临时文件、图片、日志或夹具；本记录为永久交付。文档门（73路由/60受保护/6角色/
153必需文档）、运行文档门、格式门（133变更代码文件/535生产文件/51仓储检查）及
diff空白检查通过；工作区425项，暂存仍为空。
以前两处工具策略阻止清理的目录仍待手动处理，不绕过重试：

- `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p47-c-e2e-temp`
- `C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`
