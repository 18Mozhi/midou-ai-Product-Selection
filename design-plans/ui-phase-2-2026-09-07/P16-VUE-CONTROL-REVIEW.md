# P16 · 真实按钮状态补齐与手机底栏修复

2026-09-10，基于干净main/f3b0cc25继续。整体布局沿用已批准C r2；具体控件仍待用户审核。使用ui-skills-root/frontend-design维护既定方向，Playwright实际鼠标/键盘/拦截请求验证。本批不是全站完成或上线。

[打开114图审核册](../../output/playwright/p16-c-r2-review/index.html)：原12场景双端24图，加10个代表控件45状态双端90图。新增图片都是挂载原Vue的可见视口，不是另画按钮或注入伪状态；窄屏390鼠标态不宣称手机触屏hover。点击缩略图可看原图，默认展开已修复的“开始下一次”。

## 逐控件边界

| 实际控件 | 已采集状态 | 明确不冒充的状态 |
| --- | --- | --- |
| 创建真实选品任务 | 默认、悬停、键盘焦点、按下、禁用、创建在途 | 原生required空值不是disabled；不自动冻结全部输入 |
| 保存审计决策 | 默认、悬停、键盘焦点、按下、禁用、保存在途 | 候选不足禁采纳沿原规则，不限制观察/驳回 |
| 开始下一次 | 默认、悬停、键盘焦点、按下、保存时禁用 | 无独立重置HTTP或忙碌文案，不画重置请求 |
| 时间轴summary | 默认、悬停、键盘焦点、按下 | 本地展开，无自身禁用/忙碌 |
| 重试读取进度 | 默认、悬停、键盘焦点、按下 | 恢复重试期间面板未渲染，不是按钮禁用；已有journey读取分支不据此代验 |
| 查看影响 | 默认、悬停、键盘焦点、按下 | 本轮仅blocked变体，无申请权限/返回等全变体声明 |
| 返回机会列表 | 默认、悬停、键盘焦点、按下 | 导航不伪造disabled或busy |
| 查看来源原文 | 默认、悬停、键盘焦点、按下 | 仅第一候选代表链接，不覆盖全部动态行/外站成功加载 |
| 查看机会记录 | 默认、悬停、键盘焦点、按下 | 从隔离GET的已保存决定返回ID渲染，不声称真实采纳成功 |
| 打开验证任务 | 默认、悬停、键盘焦点、按下 | 返回ID缺失时不渲染，不画禁用假入口 |

每态检查真实pseudo状态、至少44px触控区域、16px文本、中心命中；按下时尚未释放，随后在目标外释放，快照证明没有提交/导航/展开/重置、改变字段或活动ID，也无额外旅程HTTP。保存/创建在途由真实Vue等待拦截请求产生，原disabled和文字均复核。不向DOM手写busy/disabled或伪造hover/active class。

## 本轮实际修复

1. 手机底栏继承flex-wrap:wrap，再改为column时会换列；“开始下一次”实测x=266、right=596超过390视口，根容器裁剪使原文档scrollWidth检查仍通过。仅在P16≤520px底栏改nowrap并恢复提示span的自然flex尺寸；按钮完整330×44，真实点击回输入页并清活动ID，不发送HTTP。没有用overflow:hidden遮错或修改全局壳层。
2. blocked恢复失败时journey为空但resumeId存在，“查看影响”曾误说创建未确认。说明分支改为journey或resumeId任一存在即显示读取受阻；无活动ID的新建失败仍保留创建说明。精确两个分支均测零HTTP，不改变任务状态、重试方式、保存/重置草稿或五门规则。

## 验证、来源及使用

`node scripts/verify-ui-phase2-journey-vue.mjs --capture`重拍固定图册；无参数先核对源和PNG哈希，再跑同一真实Vue流程及45状态×双端完整矩阵，漏项/重复、遮挡、伪状态、意外字段/ID/HTTP变化均失败。验收器区分viewport width与controlWidth，不能把按钮宽度当截图视口。恢复请求受阻→副说明→重试原ID一次→终态恢复，以及实际reset点击均已加入。

170项检查通过，114PNG/90控件状态实例；完整构建、静态、文档与原离线证据重验结果见[PROGRESS](PROGRESS.md)。原42项业务回归是前批历史结果，本批不重复宣称新跑42项；本次实际运行证据由新验证器独立记录。原离线382图与旧40适用槽不被实际Vue图数量替代，时间轴旧4槽在离线登记仍保持未映射，真实四态另在P16.json actualVueLayoutEvidence登记。

无参数复验或capture均仅启动127.0.0.1:5175严格端口的临时Vite和独立浏览器，finally关闭。未增加生产服务、环境变量、接口/字段、数据库、权限或依赖；OpenAPI/后端/Worker/Python与本次CSS/说明分支无关，保持不变。没有部署或生产重启；后续需获准通过原宝塔前端发布流程生效，不需数据库迁移或后端/Python重启。

图册、图和验证器为永久交付物，纳入提交；本轮不新增一次性测试结果目录。前批output/playwright/p16-layout-20260910/.last-run.json及output/playwright/ui-phase2-competitor-races-20260909里的playwright.config.ts、results/.last-run.json曾被安全策略拒绝清理，保持不动，不再次尝试绕过。

## 尚待审核

请按图册审核本页具体控件呈现；全量字段/动态行/主次恢复文案/三主题两密度、真实外站/HTTP/MySQL/RBAC和完整生命周期仍待。全局壳层和其余页面继续按计划重构，完整页面批准数及G0–G5不提升，不宣布第二阶段完成。
