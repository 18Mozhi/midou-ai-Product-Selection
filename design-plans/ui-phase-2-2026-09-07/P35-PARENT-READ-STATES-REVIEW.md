# P35 父级读取、失败与恢复 · 实际 Vue 证据

本批是**当前界面技术证据，不是新 C 设计稿或视觉批准**。从main/76c1420e继续，在实际App、NavigationShell、OrganizationAdminCenter、OrganizationDataPanel路径运行，隔离拦截HTTP。保留当前尚未替换的旧外观，用于确定下一轮重构必须遵守的读取合同。

[128张整页图册](../../output/playwright/p35-parent-read-states/index.html) · [逐场景证据](../../output/playwright/p35-parent-read-states/evidence.json)

## 矩阵与真实行为

工作区比较/导出履历 × summary/data两个请求 × 首次/后台阶段 × 七类失败 × 1440/390，共112个失败与恢复场景；另有首次挂起、初读成功、后台挂起、刷新成功，两视图双端16张。共128PNG、1624项浏览器检查。

| 隔离响应       | 实际状态     | 失败请求尝试数 | 首次读取                 | 已有数据的后台刷新       |
| -------------- | ------------ | -------------- | ------------------------ | ------------------------ |
| 500            | error        | 1              | 隐藏子页面，显示恢复入口 | 保留旧内容及两处观测时间 |
| 503            | blocked      | 3              | 同上                     | 保留旧内容及两处观测时间 |
| 409            | conflict     | 1              | 同上                     | 保留旧内容及两处观测时间 |
| 429            | rate_limited | 3              | 同上                     | 保留旧内容及两处观测时间 |
| 401            | expired      | 1              | 隐藏子页面               | 隐藏子页面，不展示旧列表 |
| 403            | forbidden    | 1              | 隐藏子页面               | 隐藏子页面，不展示旧列表 |
| 浏览器请求中断 | blocked      | 3              | 显示网络恢复提示         | 保留旧内容及两处观测时间 |

次数来自实际api-client GET策略，不来自图稿：503/429/中断会尝试3次，关联ID在同次重试内保持。409只是现有客户端分类器的合成练习，不声明GET接口有新版本冲突契约。请求中断不等同真实系统断网；未模拟客户端真实超时或真实服务器故障。

普通后台错误时，整段旧子页面innerHTML和summary更新时间、data观测时间均保持；一侧成功不能部分更新页面。后台读取期间只验证刷新按钮禁用、搜索字段继续可用，未虚构其他busy字段规则。恢复按钮继续原调用：summary/data各读取一次，错误提示消失，当前视图搜索和无关URL参数保持。

401/403证明的是DOM隐藏，不是内存数据清除、服务端权限正确或跨组织隔离验收。未用这些结果宣称真实RBAC通过。

## 样例与范围

复用现有m06-01组织管理员导航/摘要样例及原12工作区、23导出数据。读取“更新”样例只改变新品决策名称及两个观测时间，以区分提交新事实与保留旧事实；不修造原全量/最近列表计数和时间差异。界面固定使用测试时钟、中文locale、上海时区。

全部业务API由本地路由夹具处理，外部请求阻断。零业务写入、零未匹配/外部请求、零浏览器页面错误、零打开的业务弹窗。图片截取实际整页，包括真实壳层；不是独立HTML原型，也不是生产截图。此前P35详情/字段/控件图和批准均不变。

## 验证命令

```powershell
node scripts/verify-ui-phase2-org-data-parent.mjs --smoke
node scripts/verify-ui-phase2-org-data-parent.mjs --capture
node scripts/verify-ui-phase2-org-data-parent.mjs
node --test tests/unit/ui-phase2-org-data-parent.test.mjs
```

最小模式手机双视图24场景/368检查；完整模式112场景/1624检查。--capture生成永久128PNG、index.html、evidence.json；默认模式核对源和图哈希并重跑完整矩阵，不重写图。两个单测检查全部组合、精确重试/恢复、保留内容与观测时间、源码和文件清单，不授予批准。

Playwright技能采用仓库现有依赖，未安装新工具。随机端口先做本机空闲探测，再由Vite strictPort绑定，所有浏览器/上下文和服务在finally关闭。首次元素截图因自动滚动遮住固定导航下的标题，已改为顶部整页截图并重采同名文件；这是验证器取图修正，不是产品布局修改。

## 未改与剩余工作

未改生产Vue/CSS、API/OpenAPI、后端/Worker/Python、权限、数据库、env、依赖或重试策略；无迁移、部署或生产重启要求，也无新可调运行参数。前端源码没有变化，不重复生产构建。

父级C错误/后台提示区的完整新稿、逐区域批准与实际实施仍待完成。真实服务端/SQL/RBAC、会话与组织切换、多实例/卸载/迟到响应、完整URL历史、原生缩放/主题密度、真实断网/超时及生产验收不在本矩阵结论内。P34特有白区和措辞批准不转授P35，全73页交付仍继续。

## 最终收尾

完整无参数复验1624检查/112场景通过；后续仅图册HTML生成字符串拆行以满足640行长门，再完整采集128图通过，并同步正式登记/审计/统一入口指纹。19项P35定向单测、全部ui-phase2单测、153文档/73路由、运行说明及格式检查通过；旧94/192/140/24图与批准保持。

本轮130个输出文件（128PNG、图册和清单）是永久交付。没有另外创建临时脚本、下载或夹具；首次取图与格式修正均覆盖本轮同名文件，清单检查无额外残留。Vite和浏览器已finally关闭；端口62285、62420、62582、62661、63099、63447、63878最终均无监听，验证器进程不存在。现有依赖缓存和项目所需构建输出不清理。本轮未部署，无生产服务遗留或重启事项。
