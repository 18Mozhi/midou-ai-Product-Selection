# P62 已应用条件与已显示日志 · 批次40

起点 main/6025c198，541 项在途变化，索引为空；保留原529项及批次36–39。本批延续条件归属说明，不扩大既有局部审核批准，也不视为API覆盖测试修复或部署许可。

## 实现与使用

PlatformLogCenter 新增内部 displayedScope，仅在归属当前激活路由的成功读取之后，与结果一起记录该次请求开始时的 query/source。appliedScope 取规范化路由条件，不取尚未应用的输入草稿；失败和迟到读取不会改写已显示结果的条件，成功空结果也记录其条件。

“查询与日志范围”使用原生 section/dl：蓝底已应用条件、白底已显示日志对应条件，桌面并列、手机上下排列。未成功读取时写“尚未取得日志”；两者不同时浅灰区说明保留上次读取结果。底部说明“导出会按提交时已应用的条件重新查询，最多200条，不等同于屏幕上的固定快照”。不提供不存在的快照导出开关，也不冻结原因窗打开时的条件。

依照 frontend-design 与 fixing-accessibility 技能，沿用 C 方向蓝白层级、使用语义字段和独立命名的条件变化 status；长条件完整换行，不以颜色单独表达差异。无新增交互控件、动画、字体或依赖；未宣称完整读屏验收。

使用现有检索/刷新/导出入口即可，无新设置。改变草稿不会改变这块已应用说明；应用后读取失败时对照两组条件，重试成功后两者一致。

## 验证与正式图

5项新增实际Vue脚本测试先失败后通过：初始/规范化成功条件、草稿隔离、有记录/空结果保留及恢复、迟到旧成功不能覆盖新条件。生命周期单元14项、相关单元与M06-02定向测试共68项通过。

页面驱动增加双端各18项，默认113项通过，共12次本地GET；覆盖默认/筛选成功、草稿不改范围、失败保留空结果条件、重试空结果更新、原输入120字上限和范围完整换行。手机第二次打开筛选时实际按钮名含“2项已选”，原新增定位超时，改为匹配实际完整名称后通过；没有改按钮或放宽断言。

`node scripts/verify-log-page-preview.mjs --capture-review r4` 独占新目录 `output/playwright/p62-page-composition-r4`：31PNG、index.html、manifest.json，以及本批补充README。保留原21张页面/控件场景，增加每端默认条件、筛选后条件、读取失败条件差异、空结果恢复和120字条件共10张图。168个源码/配置来源和31图有SHA-256；来源为HEAD6025c198加捕获时混合工作树。r3及其他历史图不回写。手机长条件图顶部可见相邻筛选按钮焦点的边缘，不属于范围区新控件，不能作为完整焦点图验收。

已直接查看双端条件差异、默认手机和长条件图，核对蓝白分区、文案换行和桌面并列；截图与本地样例仅为局部待审证据。首次失败无快照的条件值由单元覆盖，本次没有新增该状态的实际浏览器专图；实际页面失败场景保留的是成功空结果，不伪称已覆盖保留有记录的全部组合。

回归：读取生命周期140项、读取/超时/键盘270项（原真实15秒等待）、原因及导出等待272项、独立反馈/追踪192项，均通过。前端类型检查、构建和253项资源预算通过。页面截图使用C审核宿主，生产主题全面接入仍待交。

## 交付边界

仅产品组件内部范围说明、直接测试/审核脚本与相关文档改变。无API地址/参数/响应/权限、后端/Worker/Python/插件、数据库、OpenAPI、环境变量、配置、依赖、超时、重试、200条上限、CSV、审计或幂等规则改变；生产者/消费者接口联动不适用。未来前端静态包更新生效，无Node/Python重启，本轮未部署。

仍待：原因窗等待/离页与迟到下载归属、真实CSV/审计/权限/幂等/成功下载焦点、更多范围状态、全73页实施和生产验收。既有API覆盖测试223/225失败输入未变，仍待用户处理决定，不重复相同失败、不改无关测试；未暂存/未提交，commit hash不适用。

所有本轮浏览器/Vite在finally关闭；无一次性脚本、下载、日志或临时截图，31图为正式待审交付保留，项目必需构建输出保留。既有清理受阻目录不触碰。
