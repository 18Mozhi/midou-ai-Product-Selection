# P33 团队页 · 真实 Vue C 组合 r4

## 本轮交付

2026-09-13，main / af239b08b69f7d97cd0372f9840a70009eeef0c7，起点629项前序工作区变更、
暂存为空。本轮由离线原型推进到真实 App、router、组织父组件和 OrganizationTeamPanel，
不是复制数据的静态页面，也没有把审核预览部署到生产。

frontend-design 指导蓝色页内目录、团队目录/协作关系双分区、白色独立创建区及去卡片化
统计行；沿用C色板和既有字体，不新增依赖。Playwright复用项目真实Vue审核驱动模式，
接口全部本机拦截，不执行真实创建、分配或移除。

- [当前34图审核入口](../../output/playwright/p33-teams-vue-c-r4/index.html)
- [桌面首屏](../../output/playwright/p33-teams-vue-c-r4/review-1440-default.png)
- [手机目录](../../output/playwright/p33-teams-vue-c-r4/review-390-directory.png)
- [手机创建区](../../output/playwright/p33-teams-vue-c-r4/review-390-create.png)
- [手机协作关系](../../output/playwright/p33-teams-vue-c-r4/review-390-collaboration.png)
- [原始证据](../../output/playwright/p33-teams-vue-c-r4/evidence.json)

## 实现与边界

审核插件仅转换NavigationShell和团队子组件，组织父组件未经转换。团队完整script、全部
事件/v-model以及原创建form原文保持；局部模板增加页内导航与容器、去除重复标题，并把
覆盖按钮角色的role=listitem改为原生ul/li/button。所有变换均可精确逆回原源码，锚点
不唯一即拒绝。父请求、权限、八行分页、可选成员、归档团队可操作规则不改。

CSS仅在审核body及真实P33存在时生效。统计仍显示真实样例10/9/1/13/5/4；保留上层
summary中的24团队原样例差异，不捏造两份夹具一致。导航样例缺少组织显示名称，壳层
仍显示原“未命名组织”回退，这不是生产组织数据。

原因窗截图来自当前共享组件，**没有重设计该弹窗**；取消零写入已验证，不代表提交、
全部焦点链或真实审计通过。当前团队等待成员回执切换选择的OG-G02仍未修，原惰性
验证继续复现；草稿/迟到回执策略未在本轮决定。

## 可见问题与修订

1. r1六组144项功能检查通过，但目检发现米色背景、创建网格红底、状态和流程键旧边框。
   实际浏览器进一步确认红底属于create-grid而非label；r2显式隔离这些背景/边框，
   增加四项每宽度颜色检查。r1的34图/清单原样保留为缺陷证据，不宣称当前来源一致。
2. r2六组160项通过，但长区域截图中固定底栏遮挡创建底部操作。r3没有隐藏底栏，也没有
   编辑图片；区域捕获临时加高视口并显式滚到距顶部100px，避开底栏。每张图保存实际
   captureViewport，1000–1787px高；首屏始终1000px高，每次截图后交互视口恢复1000px。
3. r3首次只加高未显式定位时，新增遮挡断言抓住基线手机目录仍与底栏重叠，捕获停止。
   唯一不完整baseline-390-default.png及空目录经精确路径确认后删除，再生成正式r3；
   正式r3不是与失败文件混合。最终10项区域遮挡断言通过，手机创建按钮已完整目检。
4. r3同时复用组织C壳层的局部边框处理，移除本页上下文的旧亮色竖线和底栏旧分隔线。
   原P29/P47壳层预览文件、原图与批准记录未修改。

r1/r2各34图保留为正式修订证据，固定清单和原PNG哈希由测试保护；不应拿它们继续审批
当前版本。r3后格式检查发现Prettier第二遍仍调整一处链式调用换行，先稳定格式再生成
独立r4，视觉和交互不变，r3清单与34图另加不可变测试保留。r4为当前入口。
视口加高只改善区域证据，不能据此宣称所有短屏无遮挡。

## 验证

实际App：未变换baseline 390/1440；C审核390/840/841/1440，六组，最终捕获170检查、
34PNG、177原始源指纹（包含递归CSS导入）、30本地无body GET、0写入、0意外网络和
0浏览器异常。r4清单SHA256：f0ada1de1cab21587b213a3569990082ee2b06e547a8c3a261d82cdd01e114b7。

覆盖八行/二行分页、选择归档团队、原可操作状态、流程键搜索、重置、未选成员提示、
原三位活动成员选项、分配/移除原因窗取消、创建打开/原焦点/120及80及500字约束/取消、
筛选无结果后选择保留与清筛选。每组只有一次团队GET，不把筛选或分页变成后台请求。
页内链接核对了目标存在，尚未对点击后的历史栈、完整键盘焦点及短屏滚动做验收。

最小SFC/模型事件保持检查2/2；手机无图25项；r2及最终样式无图完整160项；最终捕获
增加10项遮挡检查为170。r3关联35项通过后，r4六份相关单测最终36/36、0取消/跳过，
包括全部当前源码/PNG、转换逆向、原图保护与禁止覆盖；约5.72秒。全库与生产构建未重跑，本轮无生产导入，
不据此推算全库剩余失败或宣布全站完成。

```powershell
# 当前真实Vue审核，无新图
node scripts/verify-ui-phase2-teams-vue-c.mjs
# 仅手机预检
node scripts/verify-ui-phase2-teams-vue-c.mjs --smoke
# 当前r4已存在，会在浏览器启动前拒绝覆盖
node scripts/verify-ui-phase2-teams-vue-c.mjs --capture
node --test tests/unit/ui-phase2-teams-vue-preview.test.mjs
```

## 收尾与待审

新增永久helper/CSS/验证入口/测试及本记录，同步计划、进度、P33规格、Feature Map和运行
说明。不改变生产环境变量、接口/OpenAPI、权限、持久化结构、依赖或运行服务；后端、
Worker、Python消费方不适用，没有生产重启需求。工作区混有前序变更且全局门禁仍未通过，
未暂存、提交、部署；commit hash不适用。

所有本轮Chromium/Vite在finally退出；17个本轮端口已独立核对无监听。仅一次失败捕获的
临时图片/空目录已删除，无新增遗留临时产物。四套正式图包保留交付，不属于待删草稿；
历史六处清理受限目录未触碰、不绕过重试。

本次只提请审核展示的团队目录、协作区和创建区布局；共享原因窗、全部按钮状态、主题
密度、长文本/200%缩放、真实写入/RBAC及生产验收继续待办。全73页完整C实施目标保持，
P47原待审问题不重复提问、不默认通过。
