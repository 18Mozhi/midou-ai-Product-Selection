# 第二阶段：全站逐页重设计、交互实现与真实验收计划

版本：1.21；修订日期：2026-09-08；状态：完整实施计划待审核；已提交事实规格56/73，剩17份；正式风格未获审，全站重构、部署和用户签收未完成。

当前核对起点：`main` / `5ad7b7b`，工作树干净。B1c采集三页规格、合同及两处局部修复已提交，不再列为未完编码；既有临时材料清理问题仍按PROGRESS保留。F00统一方向审核台已提交，等待具体版本意见。先读第2.12节和执行手册第3.16节，再按第3–10节及PAGES执行完整W00–W09。第2.1–2.11节为历史快照；本次更新当前卡，不新增一套竞争计划。本轮仅修订PLAN/EXECUTION，不实施产品改动、生成正式图、迁移或部署；计划完整性不等于阶段完成或生产复验。

历史基线索引：最初编制为3d3313a，1.6核对为181ab16，当时存在未跟踪的opportunity-candidate-map.md，旧清单绑定e1f7a92；这些都不是本版当前状态，详情保留在第2.1–2.3节与PROGRESS。本文件是UI第二阶段，不是项目原有P02阶段，也不改变P00–P08模块完成记录。本轮没有重新核验线上版本。

## 1. 目标与范围

以现有业务能力和历史 Signal Ledger 2.1 实现为功能基线，重新设计全部页面的布局、视觉、真实按钮和弹窗状态，完成 Vue 实现与可追溯验收，最终交付用户能够逐页审阅的完整审核包。历史部署记录不作为本次线上状态证明。

当前蓝图中的信号账页体系（纸张底色、墨色正文、朱砂主行动、零圆角、无装饰阴影与渐变、横向模块导航）是现有工程参考，不是用户对第二阶段设计的预先通过。按用户“以前所有 UI、布局都不要，重新设计”的要求，73 条路由全部重新评估信息架构与构图；保留业务功能与合同，不默认保留旧布局，也不因核心页已有测试而豁免重新设计。W01 必须先交付具有结构差异的代表方案及旧版对照，经用户选定后同步设计规范，再开展全站实施。不得将换色、覆盖 CSS 或所有页面套相同模板作为逐页设计完成依据；每页必须说明自己的主要任务、信息顺序和动作层级。

本次制定计划只调整计划文档。实施阶段范围为前端页面、组件、样式、交互测试、验收工具和相关文档。保持既有字段、请求方法、权限、业务规则、任务语义和持久化契约；拟议布局所需事实不存在时，记录数据缺口，不添加假指标或自行扩展后端。没有新增依赖、服务、数据库迁移或环境变量的默认需求。

页面逐项分工及验收见 [PAGES.md](PAGES.md)，具体领任务、审图、复测和交接见 [EXECUTION.md](EXECUTION.md)。已有产物以 [PROGRESS.md](PROGRESS.md) 和实际文件为准，其余仍待产出；不能引用计划或未验证草稿作为“实现完成”证据。

## 2. 核实后的起点

| 项目 | 已有事实 | 第二阶段处理 |
| --- | --- | --- |
| 路由 | `config/route-catalog.json` 共 73 项：11 public、60 protected、1 internal、1 fallback | 73 项全部入册；public 分类不代表无会话要求，保留 `sessionRequired` |
| 全站图 | 旧包 `screens/desktop` 与 `screens/mobile` 各 73 张 | 是 HTML 概念设计稿，不是生产实景图 |
| 图的生成 | 旧包 `build.mjs` 导航到 `screen.html?route=...`，`assets/screen.mjs` 用模板及示例数据渲染 | 不能直接拿其生成物证明 Vue 功能或生产数据正确 |
| 已有实现图 | `implementation-proof` 有 8 张核心工作流图及说明 | 说明为真实 Vue + 隔离 API 合同数据；属于本地实现证据 |
| 按钮、弹窗 | 旧清单 648 个 button、56 个 dialog/role=dialog 源码匹配项 | 仅为静态扫描基线；需补动态菜单、链接动作、共享确认框调用点、不同状态入口和内部页面分类 |
| 生产测试 | 已有 API、六角色路由与核心链路验收脚本 | 路由能打开与 API 通过不等于所有动作逐项执行；实际截图目录和数量按报告清点，不沿用“只有 4 张”的推断 |
| 内部页面 | `route-catalog.ts` 生产过滤 internal；`App.vue` 对 UiStateShowcase/VerificationFramework 使用 DEV 条件导入 | `/ui-states` 留在开发审核，生产只验不可达与无内部组件产物 |
| 文档问题 | 旧 IMPLEMENTATION 指向不存在的 `pages/desktop`、`pages/mobile` | W00 修正为真实 `screens` 路径，同时明确图的证据类型 |

旧按钮清单通过正则匹配生成，源码行号会随代码调整失效。不得把 648/56 当作不可变的业务动作总数，也不得为了达成旧数量保留重复按钮。重建清单时为每个旧项给出保留、合并、内部限定或删除理由，实际总数由新清单与调用链共同确定。

### 2.1 1.6复核时的剩余工作（历史）

以下来自本地清单、已提交产物及 PROGRESS，不是生产复验或用户通过结论。

| 当前成果 | 能证明什么 | 仍必须完成 |
| --- | --- | --- |
| 73 路由、1377 控件/事件候选、100 弹窗定义/调用候选，旧704项有源码对应；54个目录外候选已有独立语义复核 | 静态位置与历史归属可追踪；目录外42个语义动作及1个原生确认有记录 | 复用54项记录，不重做；继续目录内运行时动态菜单、权限与状态，核实目录外生产可达/打包边界；语义归并后冻结分母 |
| P01–P14、P17、P23、P24、P31、P61 共19份页面规格 | 已有身份/入驻38项、账号/首页51项、趋势60项、评分25项、任务71项局部候选语义记录；各族弹窗变体另有合同 | 其余54份独立规格；已有19份仍须最终设计、剩余行为验证与审核，不直接标完成 |
| 任务 A/B 原型12图；规则/权限/异常恢复及按钮板 A/B 原型76图；88图已统一共享样式并复核基本字号/控件尺寸 | 已有两种结构方向可供选择；局部排版指标有证据 | 用户选定方向、正式规范及三主题/两密度/断点完整验证；局部尺寸检查不等于完整无障碍；图数不是页数 |
| 任务18图及规则/权限/系统状态36图的 Vue 基线 | 既有夹具下的部分真实组件行为和布局 | 最终新布局实现图、真实后端/权限链、全部动作及生产证据；CSS研究图不得抵扣最终设计图 |
| 审核台、图源/哈希校验、局部空态与移动宽度修复 | 工具及部分局部问题已有验证记录 | 用户意见闭环、同版本全站回归、上线和逐页签收 |

当前 `coverage.json` 为 G0=in-progress、G1–G5=pending，`denominatorFrozen=false`、`userApprovedPages=0`。聚合清单内业务验证计数仍为0，不等于此前没有局部测试；局部证据尚不能充当冻结后全量覆盖。不得用88张方向图或19份规格推算阶段完成百分比。原型统一和目录外语义复核分别已有提交 `0421f7c`、`cc62f08`，不是正式生产重设计发布。54项在生成清单中仍为未映射，人工复核不通过改计数冒充全站覆盖。

### 2.2 1.6实施起点与证据刷新（历史）

本次为计划修订，不接续编码。1.4中的S00任务缓存读取修复已提交为e1f7a92，相关清单和Vue证据由4e5e8f5刷新；不再把它列为待实现。6c0e090、915ee71、47707c1补充评分、身份和账号/首页规格与局部合同，181ab16补充P14趋势规格、60项候选映射及8项永久合同测试，尚不证明这些页面完成重设计。已有机会候选草稿不等于P15/P18两页规格交付；下一轮先核对草稿来源、引用完整性及实际源码，不覆盖或直接计入已完成。

产品源稳定并形成明确提交后，再刷新生成清单、受影响的人工候选位置和真实Vue证据。保持原图的历史版本和类型；重新采证后才更新对应源码指纹，禁止只改哈希冒充复验。与产品源码无关联且源/样式/夹具哈希未变的独立A/B原型可以复用，无需重做全部88图。

第一批执行顺序为：复用有效基线及P14/P17 → 补P15/P18两页 → 补P16创建流程 → 按共享入口族补其余51份规格及全站语义分母 → 正式规范及代表Vue闭环 → W02–W08逐页交付 → W09发布签收。规格计数应依次为19→21→22→73，不用草稿、图数或测试数抵扣缺失页面。用户方向审核与合同盘点同步安排，不必等54份全部补齐才提交审核；未审核前不批量套用任一方案。这里的51份是三页交付后的剩余目标，当前仍缺54份。

已有局部回归按PROGRESS引用：任务桌面/移动各42项、评分各15项、账号各8项、趋势各16项；这些是历史隔离Vue结果，本轮不重新宣称运行。身份I01–I12、账号/首页A06–A12、趋势TR08–TR12及已有合同明确的焦点、错误关联、提交竞态、真实后端权限缺项仍需逐项落实，不得因有测试文件而注销。缺口处理顺序和可直接领取的工作卡见EXECUTION 3.4及3.6。

PAGES的P06确认密码、P07复制、P09持久化引导进度等旧矩阵描述已有源码纠偏，见 [identity-onboarding-contract-review.md](identity-onboarding-contract-review.md)。当前没有对应字段或动作，不据旧矩阵补造业务。PAGES文本参与全局源指纹，下一次相关证据刷新应连同矩阵、清单和受影响图源同步修正；本次不只改指纹冒充重采。

### 2.3 1.8起点、缺口与执行顺序（历史）

本节依据当前文件、路由目录、Git和PROGRESS复核。历史测试仅引用记录，本轮计划不重复运行产品测试，也不把尚未提交的证据写成已提交发布。

| 项目 | 当前可核实状态 | 后续动作 |
| --- | --- | --- |
| 页面规格 | P01–P18、P23、P24、P31、P61、P72、P73，共24份；缺49份 | 不重做已交付的E02；从P19/P20开始补W03八页，再补W04–W08的41页；已有24份仍须最终设计与全验 |
| 源码与清单 | 产品HEAD为060e0b5；工作树5个生成清单已绑定该版本，指纹caf0574f33b1c91a446e6a7784bc954fc6d926d44ba5b349df538190304eff8d；尚未提交 | ConfirmDialog遮罩焦点修复影响的候选映射、图源与审核证据须核对；仅清单版本更新不证明旧图有效。本次不替代其验证或提交 |
| 覆盖 | 73路由、1377控件/事件候选、100弹窗候选；G0未冻结，G1–G5待验，用户通过0 | 动态状态、权限、共享入口及语义动作仍需归并；不将静态对应计为行为通过 |
| 机会与创建 | P15/P18规格和局部合同已提交d0c1bb6，P16已提交42aee26；读取生命周期局部修复已提交16f524b | 不重复N02/N03；补J08剩余范围/多标签/写入归属、J09草稿与错误恢复、J10无障碍/正式图/真实验收 |
| 异常入口与共享确认 | P72/P73及永久合同测试、ConfirmDialog遮罩焦点修复已提交060e0b5；历史记录为本族双视口各19项、业务消费者各8项局部通过 | 复用有效用例，不称所有调用方、真实后端或生产已验；余下变体、叠加弹窗、辅助技术和生产不可达继续验证 |
| 图与审核 | 已有88张A/B研究图及局部Vue基线；本地PROGRESS记录关联证据刷新，尚非最终逐页图 | 提交方向审核；用户可选案、要求修改或全部重做；未答复不批量实施任一方向 |
| 矩阵事实 | f55942d已提交PAGES中P06单密码、P07无复制、P09非持久化引导等证据纠偏 | 按真实源码出图，不新增不存在的字段、按钮或保存行为；不重复计为新成果 |
| 业务冲突J07 | 旅程adopt与P18/蓝图五项质量门存在已记录差异 | 用户明确统一质量门或保留独立规则前，不改该业务判定；可继续无争议页面规格与设计研究 |

下一批：E01核对060e0b5证据收尾，同时尽早提交E05方向审核 → E03按P19/P20、P21/P22、P25/P26、P27/P28四个小批补八份规格 → E04按W04–W08补41份并冻结全站语义分母。E02规格交付已完成，不再作为下一步。规格进度应为24→26→28→30→32→40→48→56→64→73；这些只表示文档数量，不是正式页面完成率。

用户选定方向后E06规范及代表Vue闭环，随后沿W02–W08逐页审图、实现、验证，W09统一发布与签收。E05无需等待49份全部补齐；等待审核时可继续事实合同工作，不能擅自通过A/B。任务输入、产物、退出标准见EXECUTION 3.7。不能让规格盘点无限推迟审图，也不能在审核前把旧界面换色当正式成果。

本阶段不新增业务能力、数据库迁移或依赖；对于已记录缺口，先复现再确定必要修复。尤其J07必须独立决定：视觉审核不能替代采纳质量门业务授权。未解决时只阻塞对应链路，不将全站其他安全工作一并暂停。

### 2.4 1.9事实与安排（历史，当前见2.5）

本节以实际文件、route-catalog、coverage、Git和PROGRESS为依据。历史验证仅引用，未在本次计划编制中重新执行；文档中的“已交付规格”不表示相应页面已经完成新设计。

| 事项 | 当前核实结果 | 本版安排 |
| --- | --- | --- |
| 页面规格 | P01–P20、P23/P24、P31、P61、P72/P73，共26份；缺47份 | W03先补P21/P22、P25/P26、P27/P28六份；W04–W08补其余41份。已有规格复用并补未验项，不重新计成果 |
| E01源证据收尾 | 已随69d7b21提交；清单产品源060e0b5，指纹caf0574f33b1c91a446e6a7784bc954fc6d926d44ba5b349df538190304eff8d | 不再安排一次无变化的全套重采。只有源、样式、夹具或证据失效时按影响重新验证；不能只改版本字段 |
| E03竞品首批 | P19/P20规格、36候选/3弹窗/10绑定合同及7个新增局部回归实例已提交；PROGRESS记录该族双视口各18项通过 | 不重做两页规格；CP-G01–CP-G07仍是待关闭项，在对应页面实现前复现、必要修复与复验，不说竞品全验完成 |
| 全站覆盖 | 73路由、1377控件/事件候选、100弹窗候选；G0未冻结，G1–G5待验，用户通过0 | 继续候选→语义动作→调用方→状态/角色→证据映射；缺失与未执行保持可见，不能按静态数量算通过率 |
| 视觉方向 | 任务A/B及其他代表A/B仍为研究候选；没有正式选定记录 | 第一审核节点先定导航、首屏、列表/详情和移动操作结构。允许两案全部否定；不把旧底色、字体或布局作为默认保留项 |
| 业务冲突 | J07旅程采纳与机会五项质量门差异仍待独立决定 | 不借视觉重构改业务；只阻塞相关采纳链路，其他明确范围可继续 |
| 发布与历史清理 | 本次没有检查线上版本；PROGRESS仍有12批历史临时目录未清理记录 | 本轮不宣称生产已更新或旧目录已清理；发布时以实测版本为准，历史目录只在确认归属和允许的清理方式后处理 |

剩余规格的唯一领取顺序：26→28→30→32→40→48→56→64→73。最后47份规格补齐只是G0/G1的输入，绝不是阶段终点。正式图、Vue实现、逐项行为验证和用户意见仍覆盖全部73条路由。

执行分成两个可交错但不扩展权限的工作流：事实合同工作按EXECUTION 3.8的F01–F04推进；正式视觉工作在F00获得方向意见后进入F05，再按W02–W08滚动交付。全站G0未冻结前可以完成已核实代表页的隔离原型和闭环验证，但不能标记全站门通过或发布；共享壳层影响必须覆盖全部已知消费者。不要等47份文档全部写完才开始首个获审页面，也不要以首个页面通过豁免其他页面。

每个正式小批控制在一个共享入口族或2–4个页面，必须同时交主页面、实际按钮/菜单、全部适用弹窗、失败恢复图和测试；交付后记录实际耗时与审图意见，再领取下一批。不连续只增加盘点或研究截图：若没有方向意见，下一份进度明确列待审入口和所缺决定，安全合同任务仍可执行，但不自动选案。近期具体文件入口、测试入口和退出条件在EXECUTION 3.8。

本次计划完成标准：两份计划与73条逐页矩阵、26份已交规格及47份剩余任务一致，执行卡有输入/产物/依赖/验收，命令和链接有效；不要求本轮生成全套新图，也不将计划获准等同风格、J07或新迁移获准。

### 2.5 1.11完整计划的执行起点（历史，当前见2.6）

本节是本次用户“制定第二阶段的完整可实施计划”的起点更新，不另建一套竞争计划。第3–10节的交付物、73页W00–W09分包、按钮/弹窗验收、图片矩阵、G0–G5和宝塔发布边界全部保留；PAGES仍是逐页范围与入口矩阵。不是只安排剩余文档，也不是继续实施的授权。

| 当前事实 | 已交付与未完成的边界 | 下一步 |
| --- | --- | --- |
| 32份规格 | 实际文件为P01–P28、P31、P61、P72/P73；剩41份。已有规格也未免除正式设计、实现和验收 | W04的8份 → W05的8份 → W06的8份 → W07的8份 → W08的9份；32→40→48→56→64→73 |
| F01–F03已有交付 | a78d226供应/成本、4b83588审批/通知、fa09f39自动化/报表已提交。PROGRESS分别记录局部双视口各24、16、13项通过；本轮未重跑 | 不重复领取这些规格及已证实修复；SC-G、AN-G、AR/RP-G及其他族未关闭项仍随正式页面实施验收，不能据局部测试宣称重设计完成 |
| R00已完成一次，随后源有变化 | 3aaf061已提交绑定4b83588的清单和实际重采证据；fa09f39随后修改AutomationRuleCenter。清单仍保留原来源，不是当前全站有效证据 | 下一次稳定产品提交后核对增量影响，再刷新需要更新的清单与证据；不重做已完成的供应/审批证据收尾，不只改SHA，也不无变化重采独立A/B图 |
| 全站覆盖与风格 | 73路由；1377控件/事件、100弹窗仍为候选量，不是冻结语义分母。G0未冻结，G1–G5待验，用户通过0 | F00审定结构方向，F04补事实合同；用户选定后F05代表页闭环，随后F06覆盖全部73路由，最后F07真实验收/发布/签收 |
| 发布与清理 | 本轮没有连接生产；PROGRESS记载14批历史临时目录未完成清理，最近R00/F03各自新临时目录已清理 | 不推断线上SHA或运行状态。旧目录不删除、不重试被拒绝清理、不纳入本次提交；实施收尾按原归属及安全限制登记处理 |

实际优先级：F04组织治理8份 → 平台账号8份 → 来源采集8份 → 数据运营8份 → 运维9份；证据更新在产品源稳定后按影响安排，不以旧证据通过替代当前检查。F00方向审核与这条链交错安排；一旦获得设计意见即可进入F05，不等全部41份文档写完。W02–W08每小批必须交付“规格、桌面/移动正式图、全部实际动作/弹窗/异常态、Vue实现、测试、意见闭环”，不能连续以盘点替代视觉重构。没有风格意见只阻塞正式视觉实施，不阻塞无争议事实盘点。

本阶段坚持整体重设计：导航组织、首屏构图、列表/详情关系、字体与颜色、密度、按钮层级、弹窗布局和移动操作方式均重新评估；不默认保留旧风格，不以覆盖CSS或统一模板换标题通过。先审任务、规则、权限、异常恢复等代表结构，再锁定设计令牌；用户可以否定全部候选。技能指导用于明确结构差异和可实施性，不代替用户选案，也不据此引入装饰动效或新依赖。

计划批准、风格批准、J07业务决定和生产发布条件分别记录：批准本计划不等于批准尚未展示的设计，也不等于任意数据迁移授权。J07保留为独立决策，仅阻塞相应采纳规则链路。完整剩余工期在F05实测后重估；第5节23–40有效工作日仅为最初总量参考，不作为本次起的剩余承诺。

### 2.6 1.12计划起点（历史，当前见2.7）

本节以本次Git、实际目录、路由目录、Feature Map及PROGRESS核对为准。当前用户要求是制定计划，不是继续实施；既有在途变更保留原样，不把历史授权或测试摘要写成本轮执行结果。

| 状态 | 可核实事实 | 实施接续要求 |
| --- | --- | --- |
| 已提交 | HEAD为9828743（计划提交）；此前产品提交fa09f39。32份规格为P01–P28、P31、P61、P72/P73 | 复用有效成果，但所有页面仍须正式新图、实现、验收与签收 |
| 在途 | P29/P30/P32–P37八份规格、organization-governance-contract-review、OrganizationAdminCenter及组织E2E、Feature Map、架构和运行说明共14个变更路径 | 先核对归属和完整diff；合同末节源码指纹尚无表格，PROGRESS尚无本批收尾。完成可追溯验证、清理和独立提交前，不计正式交付 |
| 尚缺 | P38–P60、P62–P71，共33份规格；分属W05八份、W06八份、W07八份、W08九份 | 在途批交付后按40→48→56→64→73推进。不是仅补文档：已有页同样进入新设计→Vue全链路 |
| 待审核/待验 | coverage中G0=in-progress、G1–G5=pending、分母未冻结、用户通过0；原型和旧实现图不是最终新风格 | 方向审核与事实盘点交错安排；用户选案后优先完成代表页闭环，不等待73份文档全部齐全 |
| 生产及临时材料 | 本轮未连接生产；历史清理限制沿用PROGRESS。在途测试目录不属于本次计划产生的文件 | 不推断线上已部署；归属未核清不删除、不夹带提交，具体在途收尾见EXECUTION 3.10 |

**完整实施路径**：在途批收尾 → 增量证据核对与剩余规格 → 新方向审核 → 代表页正式图和真实Vue闭环 → W02–W08逐页重设计与交互验收 → W09全站回归、宝塔部署、生产复验 → 用户签收。方向审核可以提前；不要求把所有事实盘点做完才开始审图。尚未审核的视觉方向不能直接批量应用到生产。

**彻底重构的验收标准**：每页必须指出旧导航、首屏、任务操作顺序、列表/详情组织、移动布局哪些被重新设计；新旧对照不能只有字体、颜色或圆角变化。功能、权限、接口及持久化合同保持，视觉层不默认保留信号账页布局。方向评审同时比较五类代表结构；若现有A/B仍像旧版，返回W01重做结构方案，不强迫二选一。设计技能只用于建立差异化与细节标准，不替用户批准风格。

**交付边界**：第3–10节及PAGES共同构成完整73路由范围，不缩为剩余33份规格。最低146个正式页面设计图槽位、146个Vue实现图槽位，按钮状态板、每个实际弹窗/变体与适用异常状态另计。旧矩阵中的拟议动作必须先追真实实现，没有的能力标注差异，不能为完成图包补造业务接口。

**四项独立决定**：计划批准、具体图版本的风格批准、J07采纳规则决定、生产发布/迁移范围分别留证。完整剩余工作量在代表闭环实测后按页型重新估算；23–40有效工作日仅是原始阶段总量，不是本次起的承诺。

### 2.7 1.14完整可实施计划的起点（历史，当前见2.8）

本节替代历史进度，不替代第3–10节的完整交付合同。依据为实际Git、规格目录、coverage及PROGRESS；未重新连接生产，未在本轮执行产品测试。

| 核对项 | 当前事实 | 计划处理 |
| --- | --- | --- |
| 已提交规格 | P01–P37、P61、P72/P73，共40份；ba27096已完成组织八份规格及局部明文生命周期修复收尾 | 不再重复H01；规格完成不抵扣正式新图、Vue实现、全部状态与用户审核 |
| 缺失规格 | W05 P38–P45八份；W06 P46–P53八份；W07 P54–P60/P63八份；W08 P62/P64–P71九份 | 40→48→56→64→73；每份追真实组件/API，不照旧概念矩阵添加不存在的动作 |
| H02已交付 | 5192de2已提交用户详情GET的打开代次、用户/路由归属及关闭/缓存离开/卸载失效；PROGRESS记载最终桌面、移动各21项通过 | 不重复修复或无变化重跑；PA-D01写完成回调、PA-D02列表/筛选/密码、PA-D03其他生命周期/真实权限、PA-D04完整图与验收仍待关闭。历史测试不是本轮重新运行 |
| 证据状态 | 全局清单仍绑定4b83588；后续自动化、组织及账号读取组件有变化；G0未冻结，G1–G5待验，用户通过0 | R01按实际受影响源重新核对及采证，包含新增详情辅助模块；不只改指纹，不无变化重采独立研究图 |
| 发布与清理 | 没有本轮线上版本证据；PROGRESS已有16批历史临时目录交接，包含账号批24个文件 | 不宣称已部署或清理完成；新实施批核对确切归属，既有拒绝清理不重试、不绕过 |

**第一优先级是让用户审到新结构，而不是继续只增加文档。** 下一实施轮先提交F00代表方向审核入口；现有A/B若仍带旧版味道，重做导航、首屏、列表/详情关系及移动操作结构，允许全部否定。保留业务能力，不默认保留原视觉；不以换色、圆角或所有页面套同一模板满足重构要求。正式视觉意见必须关联具体图版本，不由代理替用户选案。

两条工作线交错推进：事实线从W05平台八页开始，再补W06–W08合同和G0语义分母；视觉线先提供可比较的新结构，在方向确认后优先完成P23/P24及弹窗的正式图→审核→Vue→测试闭环，再按W02–W08覆盖全站。默认单执行者，不启动子代理；“交错”不是并发修改共享文件。任一批只有规格或局部修复时必须如实标注，不能称该批重设计完成。

**首轮可领取范围**：F00审图与F04-A平台八页合同。不得重复只发旧A/B链接：若现有稿不足以评判完全重构，交付新的未批准结构样稿及双视口对照，说明导航组织、首屏任务优先级、列表/详情关系、操作布局、移动转换五项具体差异；未收到意见时保留待审，不猜用户已否定或选定某案。F04-A按P38/P39、P40–P42、P43/P44、P45四个入口族核对，每族附所有真实动作/弹窗与未验项；八份完成后规格为48，不是48页重构完成。文件入口和验收见EXECUTION 3.11。

**防止计划代替交付**：每次实施交接必须给出实际可打开的图/页面、变更清单、测试结果和下一张可领取卡；仅做合同盘点时明确本批没有视觉成果。用户方向意见到达后，先消化该意见并完成一个代表闭环，再继续新增页面；不存在必须先补齐33份文档才允许审图或实现的前置条件。研究稿、正式稿、Vue夹具图、生产图四种证据分别管理。

每个正式小批为2–4页或一个共享入口族，交付：逐页十项规格、桌面/移动正式图、按钮与菜单动作表、全部适用弹窗及异常态图、真实Vue对照、测试证据、未完成项和版本化审核意见。共享壳层按全部消费者回归。底线仍为73路由、至少146个正式页面图槽位与146个Vue图槽位，额外按钮/弹窗/过程图不设数量上限；生产证据按第7节区别正常页、跳转、fallback和internal。

**阶段结束条件**：G0清单冻结、G1逐页设计获审、G2实现一致、G3全站本地验证、G4宝塔同版本生产复验、G5用户逐页签收均通过。局部夹具测试不能替代真实后端，正式设计不能替代生产截图，生产可打开不能替代每个动作验收。缺乏授权的高影响动作保留具体未执行结论。

执行卡见EXECUTION 3.11。工期按第5节在代表闭环后重新估算，23–40有效工作日仅为原始总量。计划批准、图版本批准、J07业务规则决定和发布/迁移范围独立留证；本次只制定计划，不自动触发后三项。

### 2.8 1.15执行基线与完整剩余计划（历史，当前见2.9）

本节为当前入口，取代旧版本的数量及待领取状态，不取消第3–10节的逐页、逐动作、全套图、实现和发布合同。本次根据磁盘、Git、真实路由目录、Feature Map及PROGRESS制定计划；不将历史运行记录描述为本轮线上复验。

| 工作包 | 全量路由数 / 已有规格 | 接下来做什么 | 退出依据 |
| --- | --- | --- | --- |
| W00 基线与覆盖 | 全站73条；G0未冻结 | 补齐剩余事实规格，归并动态菜单/共享控件/业务弹窗变体；R01按变更源刷新证据及旧矩阵纠偏 | 每个候选有去向、每个语义动作有caseId；源和证据相符，不能只更新哈希 |
| W01 全新设计方向与底座 | 已有A/B研究及P43 C方向18图，均不抵扣正式逐页交付 | 审查具体版本；补跨页面代表构图、共享令牌、按钮六态、模态规范及审核台；完成一个获审Vue闭环 | 用户明确方向，代表正式稿与真实Vue一致；完整导航/主题/密度/状态有证据 |
| W02 身份与核心 | 19 / 19 | P01–P12、P14–P18、P72/P73全部重新设计，不豁免已有规格页 | 双视口正式图与Vue证据、所有适用动作/状态；内部页保持生产不可达 |
| W03 成员业务 | 11 / 11 | P13、P19–P28依真实任务链重构；P23/P24优先作为获审代表闭环 | 竞品/供应链及任务/审批/通知等跨页连续性、返回与范围归属通过 |
| W04 组织治理 | 9 / 9 | P29–P37逐页新构图，复用H00/H01有效证据，补未关闭的治理合同 | 多角色、组织/工作区边界、全部确认和共享入口消费者通过 |
| W05 平台账号 | 8 / 1 | 复用P43；补P38–P42、P44/P45七份；补父入口/共享组件清单与PA-D01–PA-D04剩余验收 | 规格41→48；八页正式设计/行为仍单独验收，P43研究稿不代表已实现 |
| W06 来源与采集 | 8 / 0 | 补P46–P53；来源/适配器/凭证/1688门禁与采集/浏览器运行分别成批 | 规格48→56；启用门槛、失败恢复、凭证脱敏与实际运行链有证据 |
| W07 平台治理与运营 | 8 / 0 | 补P54–P60/P63；按数据质量、运营、安全、开放平台分批 | 规格56→64；真实字段和权限来源可追踪，停用能力不伪装可用 |
| W08 运维 | 10 / 1 | 复用P61规格，补P62/P64–P71九份，按恢复发布、资源和容量拆批 | 规格64→73；单机边界、日志/恢复链与危险动作执行限制明确 |
| W09 整体验收与发布 | 不新增路由 | 汇总全站回归、真实后端、同SHA宝塔发布/复验和用户逐页签收 | G0–G5全部有证据；未审、未测及未授权事项不计通过 |

**从现在开始的顺序**：先交付现有C版本的明确审核入口并处理意见 → 无意见时继续W05剩余七份事实合同 → W06/W07/W08及R01；一旦方向获审，即优先完成P23/P24代表正式图→审阅→Vue→测试，再按W02–W08交付2–4页小批，W09统一发布。默认单执行者交错推进，不自动开启多代理，也不要求32份文档全部补齐才开始获审页面实现。

**C方向如何复用**：[可操作研究稿](design/account-direction-c/index.html)及[18场景图与说明](design/account-direction-c/README.md)已由22f9ad4提交；[P43规格](page-specs/P43.md)和[33个直接候选合同](platform-user-design-contract.md)可作为事实输入。C只包含合成账号、局部布局和演示交互，没有完整全局导航、全部原因变体/控件六态/异常态、三主题两密度，也不是生产Vue。未收到意见时保持pending-user-review；不反复重做未变化研究稿，不把再次发送链接算新增交付。若C获审，先补跨成员/组织/平台壳层的导航构图及其余代表场景，再扩展统一规范；不能将账号目录模板直接套全站。

**首个实施批的可验收输出**：七份独立十项规格、W05父入口及共享组件的完整局部候选归属、语义动作/弹窗/输入表、PA-D01等复现结果与明确未验项；复用P43而非覆盖其证据。该批若只有事实合同，就明确没有新视觉成果；发现需修复的产品缺陷，先复现再按原契约最小修复，不凭旧矩阵补API、分页或保存按钮。具体文件、用例及停止点见EXECUTION 3.12。

**全套图不是固定张数任务**：全73条路由仍各有桌面/移动正式设计和Vue槽位，分别至少146；菜单、按钮六态、业务弹窗各变体及适用异常态另计。共享按钮状态板允许复用，但必须关联每个调用页面和差异；不以一张通用弹窗图抵扣不同字段/权限/后果。每个场景绑定pageId/actionId/dialogId/caseId和图版本。研究图、正式稿、隔离Vue图、生产证据分开统计；G0未冻结、G1–G5待验、用户通过0，不计算虚假的总体完成率。

**需要用户决定与可继续项**：正式视觉及移动交互提案绑定具体图版本审核；J07质量门属于独立业务决定；生产发布窗口、隔离写入和实际迁移范围在发布前单独核对。计划批准不等于这三类决定同时批准。没有方向意见可继续事实和无争议验证，但不能宣布风格通过；没有业务决定只暂停受影响链；没有生产条件仍可完成本地设计和验证。

**工期与交付节奏**：沿用第5节23–40有效工作日的初始总量，不冒充剩余工期。首个代表闭环记录规格/出图/返工/编码/测试的实耗，再按剩余页面复杂度、共用组件影响面和审核等待重估。每个小批交付图与可运行页面、测试结果、未完成项、精确commit和下一卡，不以继续补计划代替实施。详细逐页路径/入口见[PAGES](PAGES.md)，拟议行为始终以真实源码合同纠偏。

本次不改旧全局清单、图源、用户审核记录或产品代码；无新的临时产物、服务、数据库迁移或重启要求。16批历史临时材料按PROGRESS既有交接保留，不重试被拒绝清理，也不声称已清理。计划验证记录见EXECUTION 3.12末尾。

### 2.9 1.16完整可实施计划（历史，当前见2.10）

本节取代2.8的数量、领取顺序和完成状态；以下第3–10节及PAGES仍是完整交付合同。不新建另一套路线图，也不把计划变更解释为实施或部署授权。

#### 当前事实与剩余范围

| 工作包 | 页面范围 | 规格已有 / 总量 | 下一项可验收交付 |
| --- | --- | --- | --- |
| W00 基线与覆盖 | 全73条路由及目录外共享入口 | 全局语义分母未冻结 | R01按真实变更刷新清单、矩阵与证据；每个候选有归属，每个动作和弹窗变体有验收项 |
| W01 设计与共享底座 | 三类壳层、代表页面、控件及审核台 | A/B/C仅为未获审研究 | 新结构方向审核；获审代表正式图与Vue闭环；统一主题、密度、状态与动效规范 |
| W02 身份与核心 | P01–P12、P14–P18、P72/P73 | 19 / 19 | 全部重新构图、出图、实现及逐项验证；内部页保持生产不可达 |
| W03 成员业务 | P13、P19–P28 | 11 / 11 | 任务代表闭环后补齐竞品、供应链、成本、审批、通知、自动化、报表的真实连续流程 |
| W04 组织治理 | P29–P37 | 9 / 9 | 九页正式新设计及所有共享消费者；保留多角色、范围与敏感材料边界 |
| W05 平台账号 | P38–P45 | 8 / 8 | 先收口在途PA-D01，再补密码/创建/组织与真实权限等未验项；八页正式重构仍未交付 |
| W06 来源与采集 | P46–P53 | 0 / 8 | B1补八页事实合同，再按来源/凭证/门禁与任务/浏览器两族设计实现 |
| W07 平台治理与运营 | P54–P60、P63 | 0 / 8 | B2补八页合同，按数据质量、运营、安全与开放平台逐页设计实现 |
| W08 运维 | P61/P62、P64–P71 | 1 / 10 | B3补九页合同，复用P61；日志、恢复发布、资源、调度容量分批重构 |
| W09 验收与发布 | 全站汇总，不增加路由 | G1–G5待验 | 本地全站与真实后端验收、同SHA宝塔部署及复验、用户逐页签收 |

磁盘48份规格与已提交文件一致。`coverage.json`仍是G0 in-progress、G1–G5 pending、用户通过0，旧证据绑定旧源，不因为存在新代码或研究图自动升级状态。W05只读合同检查的历史通过记录来自8503d74及PROGRESS；当前在途产品源已变化，须在归属明确并完成验证后刷新关联证据，不能把旧hash通过当作当前通过。本轮未连接生产，线上版本、健康、迁移结果均不作新结论。

#### 执行顺序与职责

默认一个实施者，用户负责正式视觉/交互与最终验收决定；实施者负责事实追踪、开发、测试、图包、提交和发布记录，不替用户签收。先按EXECUTION 3.13的S0核对在途文件归属与未完成验证；无需再次编写已交付的W05七份规格。

随后交错推进两条工作线，而非无限补文档：事实线为B1 → B2 → B3及R01；视觉线为具体版本审核 → F05代表正式图 → 用户审阅 → Vue → 定向/整体验证 → F06每批2–4页 → F07全站发布。两线由同一执行者串行切换，不自动启动子代理。相关页事实已清楚且方向获审时，立即优先代表闭环，不等待剩25份全部写完。

#### 风格完全重构的可检查要求

1. 现有布局、色板、圆角、导航形态、卡片结构没有默认保留权；旧代码只提供业务事实与交互合同，不作为新视觉模板。
2. 方向评审必须展示三类壳层、任务列表/详情、复杂规则表单、权限操作和运维恢复。逐项对照导航结构、首屏信息顺序、主次动作位置、列表详情关系、弹窗构图、移动流程及字体/色彩体系。
3. 用户若认为仍有旧风格，修改对应结构而非再覆盖一层颜色。A/B/C均可全部否定；不自动选择C，也不自动新增业务步骤来制造差异。
4. W01建立基础值、语义用途、组件状态三层令牌及组件规格；保持已有主题/密度偏好的兼容标识，但不沿用其旧视觉。先复用已有字体和图标资源，确需新增依赖、字体授权或外部服务时另行确认。
5. 页面有不同任务时必须有不同的信息组织；共享控件可以一致，不能将73页套同一仪表盘模板。用现有字段完成设计，没有依据的指标、排序、按钮或保存能力不得虚构。

#### 每批交付与完整验收

每页交十项规格、桌面/移动正式图、真实Vue对应图、全部动作/菜单/弹窗变体及适用状态、caseId与源码版本、用户意见与修订记录。按钮六态与共享弹窗可复用规范图，但每个调用方仍有业务行为证据；详情、长表单和危险确认不能只交首页截图。

全73项分别至少146个正式设计视口槽位、146个真实Vue视口槽位，额外状态与弹窗另计；生产146份路由证据按第7节区分正常页面、跳转、fallback和内部不可达，不称73页都生产可用。研究图、隔离Vue、真实后端和生产证据分别计数。G0–G5依第8节逐门验收，用户未回复一律pending，不用截图数或测试条数冒充总体完成率。

测试按最小定向 → 所属包桌面/390移动 → 共享消费者 → 构建/预算/文档等门 → 全站/真实后端 → 同SHA生产顺序。覆盖六角色、跨组织/工作区、返回与KeepAlive、迟到成功/失败、重复提交、错误恢复、键盘/焦点、长中文与200%缩放；三主题两密度及768/1024断点按适用性补齐。具体命令、退出证据和失败处理见EXECUTION 3.13、第6节；本轮不执行产品验证。

#### 排期、发布与待决策项

初始23–40有效工作日只作历史总量估算，不作为当前剩余承诺。先交一个完整代表闭环并记录事实核对、设计、审核返工、编码、测试实耗，再按剩余简单页/工作台/治理运维页及共享影响面重估，另列用户审核和生产窗口等待。每批结束报告具体交付与下一卡，避免以反复修订计划代替实施。

默认分包本地交付，W09统一发布。当前部署脚本会通过宝塔停止Node并执行既有迁移白名单；纯UI也不能承诺零停机。发布前读取当时线上SHA、迁移账本与完整发布区间，确认干净工作树、变更所有权、隔离身份、维护窗口和可恢复运行包，再用`python scripts/deploy-baota.py`；不虚构前端专用或自动回滚参数，不扩大历史迁移许可。

待决策项相互独立：具体图版本的风格/交互审核；J07质量采纳规则；当次发布窗口、生产写入隔离范围与实际迁移范围。缺少一项仅暂停受影响工作，不伪造通过。本轮不要求用户先回答才能拿到完整计划；实施时遇到对应门再提供准确图稿或影响说明供确认。

### 2.10 1.18起点与实施路径（历史，当前见2.11）

本节更新状态和领取顺序，不缩减第3–10节的全站交付目标，也不新建竞争计划。逐页范围仍以[PAGES.md](PAGES.md)的73个稳定ID为索引，实际能力由对应规格和代码纠偏。原项目`plans/phase-02-ui-shells.md`不是本次UI第二阶段。

#### 已核对的起点

| 事实 | 当前结论 | 对计划的影响 |
| --- | --- | --- |
| Git | main/ed4e8ed；工作树干净 | 本轮只提交两份计划，历史测试材料不接管、不清理 |
| S0 | 55a7439已提交四类账号写入反馈归属；PROGRESS记载该模块桌面/移动各46项通过 | 不重复实施或无变化重跑；密码/创建/组织资料及其他PA-D缺口仍在S1，不称平台账号全验 |
| 页面规格 | 已提交P01–P47、P61、P72/P73，共50份 | 补P48–P60、P62–P71共23份；P63只归W07，不重复计数，不按文件数算完成率 |
| B1a | ed4e8ed已提交P46/P47、provider-definition-contract-review及空结果完整清除；PROGRESS记录Adapter桌面/移动各8项通过 | 不重复B1a或无变化重跑；PR-G01规则冲突、PR-G02–04未验项和正式重设计仍保留，历史回归不代表全页已验 |
| 审核与证据 | 73路由、6验收角色；coverage分母未冻结、用户通过0；A/B/C研究不是正式获审图 | R01继续增量证据核对；F00提交具体版本供审核，不自动采用C，不把研究图数计入正式图 |
| 生产 | 本轮未连接线上 | 不声称线上SHA、健康或迁移最新；发布前重新实测 |

PROGRESS中的测试和采图结果是历史记录，本轮不把它们描述为新执行。未交付规格集合唯一为W06的P48–P53、W07的P54–P60/P63、W08的P62/P64–P71，合计6+8+9=23。50份已有规格仍要经过正式新图、Vue、行为与生产证据、用户审核，不直接进入“已完成”。

#### 全阶段工作包与依赖

| 包 | 范围 / 已提交规格 | 可实施结果与退出条件 |
| --- | --- | --- |
| W00 | 全73路由、共享入口及目录外候选 | 语义动作/弹窗/角色/状态逐一归属，旧证据纠偏，实际采证后冻结G0；不只换hash |
| W01 | 三类业务壳层及个人中心、代表页、共享控件、审核台 | F00结构方向获审；令牌/主题兼容、密度、排版、动效、按钮六态、弹窗和审核机制明确；F05至少一个正式图→Vue→测试闭环 |
| W02 | P01–P12/P14–P18/P72/P73，19/19 | 身份、范围、首页、趋势、机会及异常入口全部重新构图；内部页生产不可达 |
| W03 | P13/P19–P28，11/11 | 任务、竞品、供应/成本、审批通知、自动化报表，逐页新图与实际连续链验收 |
| W04 | P29–P37，9/9 | 组织治理九页分别设计；所有共享消费者、跨范围和敏感材料边界验证 |
| W05 | P38–P45，8/8 | 平台八页正式重构；复用S0，S1剩余疑点逐场景复现与收口 |
| W06 | P46–P53，2/8规格已交付 | 复用B1a；B1b来源频道/门禁/凭证3页→B1c采集任务/总览/浏览器3页；八页全部正式设计实现 |
| W07 | P54–P60/P63，0/8 | B2数据质量2页→内容通知及接口证据3页→配额安全开放平台3页；不包装停用能力 |
| W08 | P61/P62/P64–P71，1/10 | B3日志/备份发布3页→拓扑及三资源4页→调度容量2页；复用P61规格但仍重新设计 |
| W09 | 全站，不增加页数 | G0–G3全站汇总、真实后端、六角色、同SHA宝塔发布/复验、精确清理及G5逐页签收 |

规格线收口计数为50→53→56→58→61→64→67→71→73；只代表规格交付，不代表设计或上线。正式实施按W01底座→W02–W08逐包→W09；F05代表页可跨包先验证底座。两条线由同一实施者串行切换，不自动开代理；已核实页面一旦获审即优先正式闭环，不等23份全部补齐才出第一批正式图。

#### 首轮执行与审核节奏

1. 获准实施后先提交F00具体版本的方向审核包：旧版与新稿对照、桌面/移动代表页、按钮板和弹窗，逐项说明导航、首屏、列表/详情关系与移动操作的结构差异。已有A/B/C只是研究，不默认通过；仍像旧版则重做结构，允许全部否定。不能用又一份盘点文档替代这次可视审核交付。
2. 等待方向意见期间领取B1b，完成P48/P49/P50三份独立规格及来源配置/样本/验收任务/凭证合同；复用B1a事实与有效测试。每个事实批结束都明确“可审核材料、缺少的决定、下个交付物”，不循环重写计划。没有变化的研究图不重复生成凑进展。
3. 用户选定后进入F05，先任务列表/详情与进度弹窗，再规则/权限/恢复及壳层；每页正式图获审后再改Vue。缺少方向决定时继续B1b/B1c及事实线，不自行选案。
4. F06每批2–4页或一个明确共享入口族，同批交规格、图、实现、状态/动作/弹窗证据和意见闭环；共享样式改动回归全部消费者。每批末提供下一卡，不以持续写文档替代最终视觉交付。
5. W09统一发布。阶段初始23–40有效工作日仅为历史规划区间；F05记录设计/返工/实现/验证实际工时后，按剩余页面复杂度和共享影响重新估算，审核/凭证/发布窗口等待另列，不承诺未经实测的完成日期。

#### 不可降低的完成标准

- 全73项至少146个正式设计视口槽位、146个真实Vue对应槽位；每个实际菜单/按钮/弹窗变体和适用状态另计。完整图库与审核台按页可筛选，不能只交首页或用统一模板换标题。
- 逐项验证六态按钮、键盘/焦点/错误关联、长中文、390/768/1024/1440、200%缩放、三主题两密度、缓存/返回/迟到响应/重复提交；只保留有真实合同的数据和动作。
- 证据分开标注设计、隔离Vue、真实后端、生产；安全和外发最终执行未覆盖须明确，不能静默删分母。用户意见绑定页面/动作ID和版本，未回复保持待审。
- 正式视觉审核、J07采纳质量门业务决定、当次生产窗口及迁移/隔离范围分别确认，互不替代。UI默认无新迁移、依赖或运行服务；既有部署器会停Node并检查/执行白名单迁移，发布前必须核对，不承诺纯前端零停机或自动全量回退。

#### 设计重构与实施合同的分界

旧蓝图第3节锁定的信号账页、朱砂主行动、零圆角、横向导航是旧版本事实。本次按用户明确的全新设计要求重新选择导航、栅格、字形、颜色、留白、层次和弹窗构图；不把旧规范强行带入新方案。F00阶段只提出候选，F05在具体版本通过后同步蓝图、设计规范、令牌和共享组件，保留六角色、数据权限及所有真实业务行为。字体优先使用已有合法资源；新依赖、字体下载或外部服务另行核对授权。

每页用“旧问题→新结构→现有字段/动作出处→桌面/移动稿→交互验收”说明重构理由。列表、详情、表单、监控和治理页按任务分别构图，不能共用一种卡片墙换标题。动作合并只合并视觉入口，不偷偷删除业务能力；颜色不能成为状态的唯一表达，动效不改变请求或任务时序。只有真实存在的数据才进图；缺失、过期、受阻和未知保持明确。

J07采纳质量门与PR-G01来源准入/烟测规则差异是独立待决项，视觉设计不代为选择业务规则。对应最终行为验收被阻塞时，其他已明确页面继续推进；不借本轮计划改后端，也不将未执行高影响动作计为通过。

本轮计划交付的验收仅为范围、路由、工作包、链接和命令有效；未改变Vue/CSS、API、SQL、权限、配置、图片或审核状态。详细可领取文件、命令与停止条件见[EXECUTION.md](EXECUTION.md)第3.14节。

### 2.11 1.20执行基线与完整实施安排（历史，当前见2.12）

本次按用户“制定第二阶段的完整可实施计划”更新现有权威计划，不接续未完编码，不新建竞争方案。完整范围仍是全部73条路由、每个真实按钮/菜单/弹窗及适用状态，经过全新设计、Vue实现、真实验证、宝塔发布和用户逐页审核。旧风格全部重新评估；保留的是业务能力、字段和权限合同，不是旧导航、布局、字体、配色或弹窗构图。

#### 当前事实与首要处理

| 核对项 | 当前可证明的状态 | 下一步与退出条件 |
| --- | --- | --- |
| 已提交成果 | main/a728f81；P01–P50、P61、P72/P73共53份规格已跟踪提交；B1b来源/门禁/凭证合同与局部恢复提示已交付 | 复用有效成果，不重做、不将历史测试冒称本轮运行；53份仍须正式新图、Vue与完整验收 |
| F00方向包 | [统一审核入口](direction-review.html)及[DIRECTION-REVIEW](DIRECTION-REVIEW.md)已提交；F00-1.18-r1含106张研究图、56个历史Vue对照槽位；C仅P43 | 直接提交该版本供用户判断保留/修改/全部重做；未回复不能选案，研究图不是正式设计覆盖 |
| B1c未完材料 | 两份采集E2E有未提交改动，P51/P52/P53规格尚不存在；已启动的6项定向测试终态为4失败/2通过 | 先辨别产品问题与测试定位器问题；补三页真实合同，必要修复及红绿/双视口验证后才从53计为56，不能直接接着部署 |
| 全站覆盖 | 实际目录73路由、6验收角色；coverage仍为G0进行中、G1–G5待验，分母未冻结、用户通过0 | R01补语义映射与有效证据；不按规格、截图或测试数量推算重构完成率 |
| 线上与清理 | 本轮未连接生产；PROGRESS记录旧18批及B1b清理未完，B1c另有既有red材料 | 发布前实时预检；本轮不删除既有文件，不称迁移、线上健康或历史清理已验证 |

#### 完整工作包（不得只交剩余规格）

| 执行段 | 本段交付 | 完成后进入 |
| --- | --- | --- |
| W00 / R01 | 路由→页面→实际handler/API→语义动作/弹窗变体→角色/状态→用例及证据；补动态菜单、共享调用方并冻结G0 | 全站覆盖有可核验分母；代表研究可提前，但不能提前宣布全站通过 |
| W01 / F00、F05 | 方向意见、三业务壳层与个人中心、令牌/排版/主题兼容/密度/动效、按钮六态和弹窗规范；先P23/P24进度场景，再P17/P31/P61及壳层闭环 | 具体正式图获审、代表Vue和消费者验证通过；记录实际工作量 |
| W02–W05 / F06前半 | 19页账号与核心、11页成员业务、9页组织治理、8页平台账号，共47页，全部重新设计与实现 | 每批2–4页或一个共享入口族，规格/图/交互/实现/回归/意见同批闭环 |
| W06 / B1、F06 | P46–P53八页，来源定义、采集程序、来源频道、1688门禁、凭证、采集任务、总览、浏览器运行 | 先补事实合同，正式实施仍覆盖已有P46/P47；保存、检查通过、启用和实际执行分别验收 |
| W07 / B2、F06 | P54–P60/P63八页，数据质量、内容通知、配额安全、开放平台和接口证据 | 不启用停用能力；外发/密钥/治理有独立权限、失败与恢复证据 |
| W08 / B3、F06 | P61/P62/P64–P71十页，状态、日志、恢复发布、拓扑资源、调度容量 | 不把未知显示为健康，不新增阈值或灾备能力；危险操作隔离验证 |
| W09 / F07 | 全站G0–G3汇总、真实后端和六角色链、同SHA宝塔发布/复验、恢复与清理、完整图库和逐页签收 | G4生产证据和G5用户结论齐全才算第二阶段完成 |

逐页路径、入口、桌面/移动重点和最低验收继续由[PAGES.md](PAGES.md)覆盖73个稳定ID。第4节十项规格、第6节逐动作/弹窗合同、第7节图与测试矩阵、第8节六门、第9节发布回退均是上述工作包的强制退出条件。已有53份规格没有任何一页豁免正式重设计。

#### 全新风格的设计审查，不以换色代替重构

每个代表方案必须同时提交旧版与新稿，逐项说明导航组织、首屏信息顺序、列表/详情关系、主次操作位置、弹窗分组及移动操作方式的结构变化，再讨论字体、配色、圆角、层次与动效。旧Signal Ledger的横向导航、纸张配色、宋体标题和零圆角均不是保留前提；也不为了追求不同而添加无合同的指标、按钮或流程。对既有研究图不满意可全部否定，再按明确意见重做代表样本，不强行选择A/B/C。

F05选定方向后，先交任务列表/详情/进度弹窗的正式桌面与移动稿；该页获审才改Vue，再以规则、权限、系统异常页检验设计能否承载不同任务。每个后续小批必须展示新图和实现差异，不能把同一模板改标题计为独立设计。若用户尚未回复，只继续无争议事实盘点，不批量套用未审风格；阶段报告将“等待风格决定”与“仍可执行的合同工作”分开。

#### 领取顺序、节奏与决策点

1. 获准实施后先恢复B1c未完现场；F00已有入口直接供审核，不重复生成无变化研究图。实际实施前记录Git归属、核查在途测试终态，不能把未提交测试自动当作已完成修复。
2. 事实线剩20份依次为B1c P51–P53、B2a P54/P55、B2b P56/P57/P63、B2c P58–P60、B3a P62/P64/P65、B3b P66–P69、B3c P70/P71，交付计数53→56→58→61→64→67→71→73。B1b不再重复计工作量，已有P61复核也不算新增规格。
3. 方向反馈到达后，结束当前可安全收尾的小步即优先F05；不能为了补齐20份文档一直推迟正式可视交付。每张正式稿先展示桌面/移动及关键弹窗，再经用户审核进入Vue；方向获准不等于所有细节自动通过。
4. F05后按W02→W08滚动，每个小批只交付完整闭环，不连续只写规格或堆研究图。每批报告页面ID、可审图、实际通过/未验项、提交、下批输入；同一实施者串行切换，无默认代理。
5. 至少146个正式页面设计视口槽位、146个对应Vue视口；全部业务弹窗变体、菜单、按钮六态和适用异常/恢复过程另计。生产146份路由视口证据按第7节区分72个非internal条目与内部页不可达，不冒称73个正常业务页上线。
6. 工期仍引用阶段初始23–40有效工作日，而非剩余承诺。首个代表闭环后以简单页、复杂工作台、多角色治理/运维的实测设计/返工/实现/验证工时重估；共享组件、全站回归及上线单列，审核、凭证和维护窗口等待另列。

用户需要决定的事项分开处理：F00具体结构方向；J07采纳质量门与PR-G01来源准入/烟测业务冲突；当次发布窗口、隔离对象和未知待执行迁移。计划审核不能替代这些授权。无新业务/API/权限/数据迁移/依赖的默认需求，生产仍使用现有宝塔固定对象和本地部署脚本。本轮计划不部署、不重启、不改变审批记录。

### 2.12 1.21完整可实施计划：从56份事实规格到全站交付

本版根据实际Git、路由目录、页面规格、Feature Map和PROGRESS修订，未重新测试生产。P01–P53、P61、P72/P73共56份规格已提交；缺P54–P60及P62–P71共17份。B1c的定向6项、桌面19项、移动19项通过属于既有隔离Vue记录，不是本轮复验，也不能证明正式重设计完成。当前G0进行中、G1–G5待验、分母未冻结、用户通过0。

#### 全阶段路线与可领取里程碑

| 顺序 | 工作范围与实际交付 | 退出标准 |
| --- | --- | --- |
| 1：核实范围 / W00、R01 | 复用73页矩阵；逐页将控件候选归并成真实动作、弹窗变体、权限、状态及用例；按变更范围刷新证据 | 所有历史项有去向；动态菜单及共享调用方入册后冻结G0，不以静态数冒充行为覆盖 |
| 2：方向审核 / W01、F00 | 审阅现有F00-1.18-r1，明确导航、首屏、详情关系、移动结构及视觉语言；允许全部否定 | 用户意见绑定具体版本；未回复不能默认选A/B/C，C仍只代表P43 |
| 3：代表闭环 / W01、F05 | 先P23/P24任务列表、详情及进度弹窗，再P17规则、P31权限、P61异常恢复与三壳层/个人中心；正式新图→审核→Vue→验证 | 桌面/移动、按钮六态、弹窗焦点/失败状态及共享消费者通过；规范与源码一致，记录设计、实现、返工实耗 |
| 4：事实补齐 / B2、B3 | 六批17页，顺序见下表；独立规格、字段来源、动作/弹窗合同、权限与测试缺口 | 每批2–4页、证据可追溯；方向获审即优先代表闭环，不等待文档全部补完 |
| 5：全站实施 / W02–W08、F06 | 19页账号核心→11页成员业务→9页组织治理→8页平台账号→8页来源采集→8页平台运营治理→10页运维；共73项全部重新设计 | 每批交规格、新图、Vue对照、动作/弹窗/状态验证和用户意见；已交事实规格页同样重构 |
| 6：全站收口 / W09、F07 | 同版本全站回归、真实后端与六角色链、宝塔发布、生产路由及授权动作采证、清理、逐页签收 | G0–G5全部具备实际证据；用户未签收不得称第二阶段完成 |

第2、4项可交错安排，由同一执行者串行推进，不自动创建代理。方向未获审时只推进事实与经复现的范围内缺陷；方向获审后结束当前安全小步，立即优先第3项，不再连续仅交文档。W02–W08的分包归属、73条路径及十项页面规格继续以第4–5节、PAGES为准，不另建第二套页面编号。

| 事实批次 | 独立页面 | 规格累计 | 首要验证边界 |
| --- | --- | --- | --- |
| B2a | P54数据中心、P55质量与规则 | 56→58 | P54包含记录与DataQualityCenter两类子流程；P55是治理目录，不等同质量问题处置；数据口径、导出、证据、版本和原模块跳转分别追踪 |
| B2b | P56内容、P57通知、P63接口覆盖 | 58→61 | 共享PlatformManagementCenter按域拆清；交叉复核P61；外发、停用邮件及接口证据不能伪装已验证 |
| B2c | P58商业运营、P59安全、P60开放平台 | 61→64 | 真实配额、处置权限、密钥及停用能力；不以UI重构启用新业务 |
| B3a | P62日志、P64备份恢复、P65发布 | 64→67 | 日志筛选/审计导出与恢复/发布分开；不在盘点时执行生产恢复或回滚 |
| B3b | P66拓扑、P67 Redis、P68 MySQL、P69文件 | 67→71 | 单机依赖、过时/缺失观测与真实容量边界，不宣称异地灾备或未经验证的高可用 |
| B3c | P70采集调度、P71容量边界 | 71→73 | 真实调度规则、运行观测、容量证据；不修改阈值、并发或任务语义 |

#### 每页交付、审核与估算

每页必须交付十项规格、桌面1440×1000/移动390×844正式稿与对应Vue图、实际按钮/菜单及弹窗变体、异常/恢复状态、源码与测试证据和版本绑定审核意见。全站至少146个设计视口和146个Vue视口；弹窗、菜单及额外状态另计，不用106张研究图抵扣。生产146份路由证据按第7节区分正常入口、跳转、fallback及内部页不可达，不冒称73个正常业务页面。

“完全重构”按结构审查：导航与分组、首屏重点、列表/详情关系、主次操作、弹窗信息顺序、移动使用方式均重新设计；字体、颜色、圆角和动效由选定方向统一。既有业务字段、权限、请求、数据排序及持久化语义保持，不保留旧账页布局作为前提，也不添加无事实来源的指标。每批最多2–4页或一个共享入口族，先审图再实现，随后交设计/实现差异和待改项；最终逐页签收人是用户。

工期引用第5节阶段初始23–40有效工作日区间，尚不能当作当前剩余工期。F05完成后按简单页、复杂工作台、多角色治理/运维三类的实测耗时重新估算剩余页，单列共享底座、回归、生产窗口和返工；审核/凭证等待另列。不以56份规格或研究图数量线性扣减工期。

#### 发布、风险与本轮范围

默认W09统一宝塔发布；先核对当次线上SHA、发布归属、固定对象与目录、既有迁移白名单执行记录、受限恢复材料和维护窗口。只使用现有本地构建与deploy-baota.py，不承诺前端独立上传、零停机或数据库自动回退。生产高影响动作需对应授权与隔离目标；未执行的最终提交明确保留未覆盖，不能算通过。

F00方向、J07/PR-G01业务冲突及当次迁移/发布条件分别处理，只阻塞相关步骤。历史20批临时材料清理限制以PROGRESS为准，不能重试绕过既有拒绝，也不能把其保留状态写成清理完成。本轮只更新两份计划；不变更API、SQL、权限、配置、依赖、生产服务、全局清单hash或审核状态，不产生新图和临时测试产物，无需迁移或重启。直接执行卡见EXECUTION 3.16。

## 3. 交付物与进度口径

以下为完整阶段交付清单。实施进展以 [PROGRESS.md](PROGRESS.md) 与实际文件为准；源代码盘点产物的生成不等于相应行为已经验证。

| 交付物 | 内容及用途 |
| --- | --- |
| `baseline.json` | 基线 SHA、路由摘要、静态清单数、源码哈希、环境与字体信息 |
| `page-specs/P01.md` 至 `P73.md` | 每页用户任务、字段来源、布局、动作、状态、响应式和路由连续性合同 |
| `actions.json` | 稳定 actionId、旧清单映射、路由/角色、可见条件、handler/API、结果、验证方式、证据 |
| `dialogs.json` | 稳定 dialogId、触发 actionId、各变体、字段、提交契约、焦点和关闭行为 |
| `coverage.json` | 页面/动作/弹窗/状态/角色覆盖及阻塞项；不同环境分别统计 |
| `design/desktop`、`design/mobile` | 第二阶段最终逐页设计图，至少 73×2；包含开发内部页与跳转过程示意 |
| `implementation/desktop`、`implementation/mobile` | 真实 Vue 对应状态图，至少 73×2；注明隔离数据、开发内部页、重定向结果 |
| `production/<build-sha>/` | 生产路由及交互证据索引；敏感原图留在服务器受限目录，仓库只保存脱敏材料 |
| `boards/`、`dialogs/` | 全站控件状态板、每个业务弹窗及变体的桌面/移动图 |
| `review.html`、`review-decisions.json` | 本地可筛选审核台及用户审核结论；支持对照设计图/实现图、按页/动作筛选 |
| `ACCEPTANCE.md` | 自动化结果、人工视觉复核、明确排除项、部署版本、回滚与清理记录 |

页面状态固定为：待盘点 → 规格完成 → 设计待审核 → 设计通过 → 实现完成 → 本地验收通过 → 生产验证通过 → 用户验收通过。内部页的生产状态为“不可达验证通过”，不标成生产业务页成功。不得由代理自动把“待审核”改成“用户通过”。

覆盖率至少分开计算四项：路由状态覆盖、动作行为覆盖、弹窗变体覆盖、用户审核通过率。分母来自冻结后的清单；新增动作自动扩大分母；无法执行的项目明确标记 blocked 或 documented-exclusion，不能计为 passed。旧截图数不参与新版本覆盖率。

## 4. 每页必须先完成的设计规格

每页规格必须包含以下十项，缺少任一项不能进入该页编码：

1. 用户角色、当前业务范围、进入来源、离开/返回位置。
2. 一个主要任务及已有 API/字段出处；区分事实、计算值、缺失状态与辅助解释。
3. 桌面构图：标题与主行动、首屏重点、列表/详情/说明的顺序、滚动容器。
4. 移动构图：摘要折叠、主次操作、表格字段化、详情返回、固定操作区与安全间距。
5. 所有 actionId 的名称、层级、可见/禁用条件和结果反馈；页面级全局创建与局部创建避免重复突出。
6. 弹窗入口、字段及必填项、校验、取消、失败重试、提交中和成功后行为。
7. 加载、空、搜索无结果、错误、离线、过期、无权限、受阻、恢复等适用状态；不适用项写理由。
8. 搜索/筛选/分页/选中/排序的现有存储位置，刷新和跨组织切换的行为；不得擅自新增持久化。
9. 390、768、1024、1440px 的排版说明及断点邻界检查；三主题和两种密度的适用规则。
10. 可执行验收步骤、期望结果、图路径与审核负责人。

效果规范：正文和输入控件不小于 16px，元数据不小于 13px，可交互热区不小于 44×44px。颜色令牌、文本状态、焦点环、loading 和错误信息一致。动效仅用于动作反馈与层级切换，优先 opacity/transform，遵守 reduced-motion；不对长表格行逐条入场。计划提出的任何新动效时长在 W01 评估确认后写入统一令牌，不逐页随意添加。

布局不得因长中文、长商品名、零数据、多页数据、200% 缩放、移动软键盘或固定底栏发生遮挡。数据表可保留有标签的局部横向滚动，但整个页面不能溢出。信息排序只调整视觉位置；数据排序、推荐资格、价格与利润算法继续来自当前合同。

## 5. 工程实施方式

复用 `App.vue` → `NavigationShell.vue`/`AccountShell.vue` → surface 组件的真实装配链。共享入口只负责取数、权限及状态编排，逐页差异放在对应展示组件；先审查入口的全部消费者，再做公共样式调整。新增展示组件必须由真实 routePath/mode 驱动，不能复制一个页面再更换标题充当独立设计。

重点复用 `ResponsiveDataView`、`ResponsiveFilterDrawer`、`UiStatePanel`、`TechnicalDetails`、共享确认/原因弹窗及 `useModalDialog` 等实际已有组件；编码前确认名称、prop 和调用点。继续使用 `api-client.ts`，不增加裸 fetch、不把权限判定迁到 CSS。公共样式迁移与业务页改造分开提交，避免跨批次全局选择器影响。

现有组件行数门按 `tests/unit/frontend-component-boundary.test.mjs` 执行：例如 NavigationShell <700、OpportunityWorkspace <1000、SourcingWorkspace <700；不得通过压行或删除语义测试达标。生产内部页必须继续条件导入，不能仅从菜单隐藏。

### 批次与依赖

```mermaid
flowchart LR
  A["W00 清点与证据校正"] --> B["W01 共享设计与验收底座"]
  B --> C["W02 账号与核心重设计"]
  C --> D["W03 成员业务"]
  D --> E["W04 组织治理"]
  E --> F["W05 平台账号"]
  F --> G["W06 来源与采集"]
  G --> H["W07 平台治理与运营"]
  H --> I["W08 运维"]
  I --> J["W09 全站验收、发布、用户审核"]
```

默认串行推进，不自动启动其他任务或代理。各批次重复“规格与图 → 用户审阅 → Vue 实现 → 定向验证 → 收尾”。已有授权只按明确对象和范围复用，不推断其涵盖尚未展示的新稿、全局操作或新迁移；具体页面稿的审核结果必须保留。用户选定方向前可以做隔离原型，但不能把该原型当作正式生产实现。高影响且尚未获授权的业务变更另列待决策项。

| 包 | 交付范围 | 实施步骤与退出条件 | 主要依赖 |
| --- | --- | --- | --- |
| W00 | 全站基线、旧证据纠偏、清单 | 清点73路由、扫描+运行时补齐动作；校正旧文档路径和图类型；每个旧清单项有新归属；冻结覆盖分母 | 当前干净 SHA |
| W01 | 新方向、共享组件、审核台和证据机制 | 审阅已有五类代表 A/B 方案，用户选定后统一全部代表稿及令牌；补全按钮六态和弹窗合同；完善审核台及覆盖校验；至少一个真实页面/弹窗全链路跑通 | W00；原型研究可先行 |
| W02 | P01–P12、P14–P18、P72–P73，共19项 | 账号、核心及创建流程逐页重新设计并实现；登录与范围选择、筛选/返回、机会签认焦点验证；内部页生产不可达 | W01 |
| W03 | P13、P19–P28，共11项 | 竞品→供应链→利润、任务→审批→通知→自动化→报表；各页独立规格与桌面/移动图；对应真实业务链通过 | W02 |
| W04 | P29–P37，共9项 | 组织入口下的概览、成员、权限、工作区、团队、审批、数据、令牌和审计分别编排；共享入口所有子页回归 | W03 |
| W05 | P38–P45，共8项 | 平台概览、组织/用户/管理员记录、创建/详情、权限；不同平台角色与直达路由分别验证 | W04 |
| W06 | P46–P53，共8项 | 来源配置、解析器、登录档案、1688门禁、任务和浏览器运行；受阻原因及恢复入口可追踪，不能改变启用门槛 | W05 |
| W07 | P54–P60、P63，共8项 | 数据、质量、内容、通知、配额、安全、开放平台、API覆盖；已停用的能力明确显示，不设计为可用 | W06 |
| W08 | P61–P62、P64–P71，共10项 | 系统状态、日志、拓扑及存储/调度/发布/恢复；展示真实单机边界；危险动作仅按允许范围验收 | W07 |
| W09 | 全站跨页、全动作、全部弹窗、生产证据、用户审核 | 通过下述 G0–G5；同 SHA 生产身份与报告一致；清理记录通过；用户未签收前只称技术验收完成 | W02–W08 |

### 工作量与执行节奏

初步估算为一个熟悉仓库的前端实施人员、测试与用户评审配合时约 23–40 个有效工作日：W00 1–2、W01 3–5、W02 2–4、W03 4–6、W04 2–4、W05 2–3、W06 3–5、W07 2–4、W08 2–4、W09 2–3。此为规划区间，不是承诺日期，不含等待审核/凭证/生产窗口；自动化可能缩短执行时间，但在 W01 跑通代表页面后才依据实测修订。

该区间是阶段初始总量，不是从本次起的剩余工期，也不能按已有图片数量线性扣减。下一轮按EXECUTION 3.16领取当前卡片；记录每页规格、设计、返工、实现和验证的实际耗时，在G0冻结且代表页闭环通过后重估W02–W09。重估按简单表单/列表、复杂工作台、多角色治理/运维分组，分别记录剩余页数和代表实测工时，另列公共组件、真实后端、全站回归及上线窗口；未盘点前不承诺固定交付日。

每完成一个包更新其页面规格、动作/弹窗清单、失败/未验清单、审核图与真实测试结果。共享壳层、业务页、验收工具分成可回溯提交；每包不能以“文件已修改”作为完成条件。默认先分包本地交付，W09 统一生产发布，减少生产切换次数。

## 6. 按钮与弹窗逐项验收

### 动作清单

actionId 使用稳定语义键，例如 `task.detail.assign`；历史扫描编号保留为 sourceInventoryIds，源码行号只辅助定位。每个动作记录 routeId、组件、触发条件、角色/capability、真实 handler、现有 API 契约引用、输入/输出、成功与失败表现、是否有外部副作用、测试文件和结果证据。

步骤为：静态扫描 → 查 handler 与 API → 遍历动态菜单/批量态/空态 → 合并重复渲染 → 补 role=button、链接动作、菜单项 → 校对无孤立 handler、无漏记关键入口。纯跳转与写入分开。每个按钮均审核默认/悬停/聚焦/按下/禁用/处理中六态；没有异步行为时“处理中”记不适用理由。

同一个视觉组件只需一套六态放大图，但每个业务动作仍要独立证明名称、位置、可达性、禁用理由和调用结果。共享组件测试不能代替每个调用方的业务验收。危险动作不依据中文关键词推断，必须追踪真实 handler。

### 弹窗清单

每个弹窗及动态变体都要桌面/移动打开态图；补充校验失败、提交中、请求失败、成功关闭/保留等适用图。每个调用方核查：触发→首焦点→Tab/Shift+Tab循环→Escape/取消→焦点归还→重复提交保护→成功后的原页同步。复用 `useModalDialog` 已有行为，关闭时保留/丢弃草稿遵循当前页合同，不在视觉重构中统一改变。

弹窗长内容可滚动且标题、错误提示与操作区可达；移动键盘不能盖住当前输入或确认按钮。确认影响只使用已有事实，无明确影响人数、任务数时不编数字；不可逆操作、原因、范围及后果必须明确。密钥只展示已允许的掩码，图中不能出现原始凭证。

### 生产动作边界

| 类型 | 本地真实 Vue / 隔离测试 | 生产验证 |
| --- | --- | --- |
| 导航、筛选、展开、分页、详情 | 全动作执行及布局/状态验证 | 对隔离验收身份全量执行 |
| 工作区内可清理写入 | 创建明确归属的测试对象，验完整事务结果 | 复用生产验收隔离组织、runId和精确清理，执行后核对残留0 |
| 全局配置、真实凭证、第三方采集、发送邮件、运维恢复/回滚 | 在可控测试边界验证状态和调用契约 | 只验证可见/可达/确认/取消及拒绝边界；最终执行需对应隔离目标和已有明确授权，否则记录未执行原因 |
| 不可用能力及内部页面 | 验证诚实状态和DEV限定 | 验证未越权、不可达或明确禁用，不以演示成功代替 |

目标是每个动作100%有可核查结论。只有实际执行才可记“生产执行通过”；未执行高影响最终提交的动作作为明确生产覆盖限制，用户若要求全部真实执行，则在这些项目获得条件并通过前不能宣称全量生产验收完成。

## 7. 测试与图片矩阵

### 图片口径

所有73个路由条目都提供桌面/移动证据槽位。生产共有72个非internal条目，包括根路径跳转与fallback。针对这些条目提供144份生产视口证据；根路径记录请求URL、最终URL与跳转后的截图，动态路由用真实隔离ID，fallback用具体不存在URL，不能访问字面量路由模式。另对 `/ui-states` 保留两份生产不可达证据，可复用404画面但必须有独立URL/响应记录。因此目标为146份生产路由证据，不是73个正常业务页面的成功截图。

截图包含默认完成态之外的关键子页、Tab、详情、菜单及弹窗；实际图片总量会超过146，不以146封顶。截图等待字体加载、关键数据完成与布局稳定，使用固定隔离数据和可重复视口，不能仅凭 networkidle 对含SSE的页面判断完成。

每张证据元数据至少有 artifactId、kind（design/vue-isolated/production/internal-dev）、routeId、concreteUrl、角色、状态、主题、viewport、buildSha、sourceSha、capturedAt、浏览器/OS/字体、caseId、文件哈希。含登录token的URL、Cookie、凭证和客户数据禁止进入索引。Windows与Linux分别维护视觉基线，不跨平台直接比较像素。截图差异超阈值必须人工复核后更新基线，不直接批量接受。

### 验证层

1. 最小验证：改动组件的类型/交互用例，成功后才扩大到所属工作包；普通文案修正不新增镜像实现的无意义测试。
2. 页面验收：每页1440×1000和390×844；768/1024及759/760、839/840、1099/1100附近用于适用断点检查；200%缩放、长文案、空/满列表及输入错误。
3. 共享组件：三主题×标准/紧凑密度×关键视口；所有业务页至少验证默认主题的完整正常态，额外主题做令牌/溢出扫描和受影响页实景复核。
4. 权限与状态：六个现有验收角色、跨组织/工作区切换、返回/刷新、会话过期；错误状态按清单的expectedErrors断言，不能全局忽略401/403，也不能把预期拒绝混入正常链路故障。
5. 全量回归：沿用真实Fastify、Vue与已有Node/Python测试链；E2E桌面/移动按仓库脚本顺序执行，避免夹具污染。
6. 生产：扩展现有验收程序的逐页截图和动作覆盖，保留API与真实核心链验证；先验证外部副作用边界，再使用隔离数据。截图原件与报告保留中国境内，未脱敏材料不推送公开GitHub。

### 已有命令（实施时按风险执行）

```text
node scripts/locate_flow_v4.mjs "实际路由或功能名"
npm run typecheck:web
node --test tests/unit/frontend-component-boundary.test.mjs tests/unit/ui-governance.test.mjs
npx playwright test tests/e2e/m04-06-sourcing.spec.ts --project=desktop-chromium
npx playwright test tests/e2e/m04-06-sourcing.spec.ts --project=mobile-390
npm run test:unit
npm run test:e2e
npm run build
npm run verify:frontend-budget
npm run verify:static-analysis
npm run verify:docs
npm run verify:runtime-docs
npm run verify:release-matrix
npm run verify:e2e-realism
npm run format:check
git diff --check
```

上面的 sourcing 是定向示例，替换为 PAGES 对应模块测试；本计划没有执行这些业务测试。完整阶段收口优先采用现有 `npm run verify:functional` 聚合门，执行前查看其当前包含命令，避免重复跑已经有效的测试。端口使用现有默认4101/5173或已支持的测试端口配置，启动前核对占用，不停止未知进程。

生产已有 `scripts/run-baota-production-acceptance.mjs` 需 `--production`、宝塔受限环境以及隔离验收密码；裸执行 `npm run verify:production-acceptance` 仅预检，不得称作生产验收。扩展仍在该链条内使用现有脚本，不新增生产常驻服务。新增截图/清单参数若确有必要，同步文档与配置示例后才能实施。

## 8. 验收与审核门

| 门 | 必须满足 | 失败后的处理 |
| --- | --- | --- |
| G0 基线 | 73条路由全部归属，旧648/56项逐项有去向，新增动态动作入册，图类型校正 | 返回W00，不以旧数值推定覆盖 |
| G1 设计 | 每页十项规格齐全；桌面/移动图、按钮层级、弹窗变体可审阅；用户意见有记录 | 修改对应稿，不把未回复当通过 |
| G2 实现 | 真实Vue与该页设计一致；路由/权限/字段合同稳定；实际取数与状态可追踪 | 修复对应调用链，不追加假数据 |
| G3 本地 | 工作包定向用例、全站回归、类型/样式/文档/体积门通过，无未处理阻塞缺陷 | 定位→最小复测→受影响整体验证 |
| G4 生产 | 宝塔对象及路径正确、版本SHA一致；正常链路无非预期API/控制台错误；图与清单匹配；隔离数据残留0 | 停止晋级；按部署恢复方案处理并复核健康 |
| G5 用户 | 逐页及交互审核结果可检索；所有必须项通过，例外有用户明确结论 | 保留待验收状态及具体修改清单 |

本地资源预算采用现有门：入口JS ≤150KiB、路由chunk ≤50KiB、单CSS ≤120KiB（构建文件大小），不擅自上调阈值。W01记录同设备同数据下页面加载/交互/滚动表现，W09复测；出现明显退化必须定位，不能用不同机器的时间对比推断性能提升。

G1/G2/G3在执行中按页、按包判定，W09再汇总成全站门；不要求73页全部设计完成后才允许第一张已审页面进入实现，也不允许一个代表页通过就将全站门改为通过。W01退出只证明共享底座和代表闭环可用。

用户审核入口按工作包、页面、角色、动作、弹窗、未通过项筛选；展示前后对照与变更说明。意见最少记录 reviewer、目标ID、版本、通过/修改、理由、时间、复核证据。审核为未通过时，实施者修复并更新同一ID，而不是删除记录重新编号。最后集中审核全站一致性与实际业务使用。

## 9. 提交、部署与回退

本轮计划文件可以单独提交；实际编码时记录当时分支、HEAD和工作树，仅暂存该工作包文件。代码/API/配置没有改变的计划提交无需发布应用，也不需要重启。

发布时读取线上真实SHA作为base，不固定沿用本计划基线。确保本地main、origin/main、发布清单head一致，工作树干净；精确列出区间提交与路径，通过 `npm run verify:release-ownership` 后执行 `python scripts/deploy-baota.py`。沿用标准UTF-8路径，不再尝试GBK名称转换。

当前部署器并非纯前端上传：其 main 链会上传运行包、通过宝塔停止 Node、执行既有迁移白名单，再切换目录并公开验证。即使本阶段没有新增 SQL，也必须在发布前确认白名单每项的生产执行记录、是否存在待执行迁移及维护窗口；发现新增或未授权迁移先暂停，不把历史“允许迁移”扩大为任意迁移授权。不假设无服务中断，不虚构 `--frontend-only` 或 `--rollback` 参数。对应预检列入 EXECUTION 的发布任务。

部署前检查脚本当前回退边界：固定目录切换过程有恢复逻辑，但 `verify_public` 失败不等同于所有运行包、环境配置和数据库都自动恢复。必须为当前版本保留可恢复的运行包与受限配置备份，记录原SHA；在生产健康与关键页面通过前不得删除恢复材料。不得把前端回退描述成数据库自动回退。

如果部署公开验证失败，核对目录是否切换、环境是否变更、服务是否启动，再通过已授权宝塔管理能力恢复已知稳定运行包及必要配置，并检查live/ready/available/version。若当前部署器无法满足保留恢复材料的要求，在发布前补相应最小修复及测试；不能先发后补。本阶段正常不含新数据库迁移，现有白名单执行记录仍需核对。

Node（含API/Worker）与Python按部署器/宝塔实际变更重启，Nginx配置有变更才在语法检查后reload。不创建额外站点、candidate服务、current/releases目录或新的后台管理能力。图稿与测试材料不进入生产静态包，既有服务目录仍为frontend/backend/python/config/runtime/backups。

独立生产证据文件的每个字段必须来自本次实际检查：面板对象、MySQL版本与字符集、Redis监听、日志可见/轮转/脱敏、心跳及迁移记录分别采证。字段未验证就不得填true；格式校验通过不能替代事实采集。证据文件按既有运维要求验证后清理，长期报告保留在受限验收目录。

## 10. 风险、收尾与完整性标准

| 风险 | 处理 |
| --- | --- |
| 共享组件改动影响多条路由 | 使用PAGES和surface映射回归全部消费者；同入口各子页独立验收 |
| 原设计稿含源码没有的业务指标/动作 | 先做字段溯源；没有合同的设计元素删去或记录待议，不补接口 |
| 主题ID名字像旧风格 | 保留兼容ID以保护已存偏好，仅由既有新令牌呈现 |
| 长表/长标题/高密度/中文字体 | 固定测试数据覆盖，文字和操作可达优先，不压缩字号凑图 |
| 高影响生产操作无法隔离 | 记录生产最终执行未覆盖；不操作客户数据或真实全局设置凑覆盖率 |
| 运行资料与旧蓝图存在历史矛盾 | AGENTS、当前service-manifest及真实运行链优先；W00列差异并校正文档，涉及业务冲突需用户决策 |
| 测试辅助文件与审查图混杂 | 清单区分永久交付图、受限生产证据、临时文件；逐项清理自建临时目录和进程 |
| 用户审核滞后 | 可继续已获设计方向许可的独立工作，但不能把未审页记已通过 |

W09最终报告必须回答：改了哪些页、哪些共享合同、哪些保持不变；测试覆盖分母与未测项；部署SHA/健康/重启；生产副作用边界；用户通过数；临时数据与进程清理；保留图稿/报告/备份位置；提交hash。新永久测试与审核交付图保留，临时截图/日志/夹具/下载/浏览器profile清理，依赖缓存与不明归属旧文件不删除。

第二阶段完成的定义是：全部路由有正确环境下的规格、图、实现、覆盖结论及审核记录，所有必须行为已验证且没有隐瞒的例外；不是新增页面数、截图数或测试总数达到旧基线。若用户还未审核，则只报告“技术交付与验收通过，用户审核待完成”。

## 11. 最初计划评审记录（历史）

最初编制时已按源码对照修正了五项计划风险：概念图与真实图混淆、内部路由算正常生产页、静态按钮数当行为总数、全局危险动作批量实测、部署后失败误认为一定自动回退。下列技能限制和验证记录属于最初编制时的环境及结果，不作为本次重新执行的证明；最新状态见 PROGRESS 和 EXECUTION。

`plan-design-review` 与 `plan-eng-review` 的专用交互流程未运行完成。两者的 SKILL.md 要求："If no AskUserQuestion variant appears in your tool list, this skill is BLOCKED." 当前没有该专用工具，因此采用仓库原生计划评审，并非宣称通过了技能的自动审核或跨模型评审。本限制不阻止这份完整计划的制定；未新增遥测、全局配置或其他代理任务。

本计划的建议路线为W00→W09，审核对象是计划中的范围、页面布局方向和验证边界；实际页面设计稿与实现均在后续工作包中产出。

### 最初计划文件的校验记录（历史）

2026-09-07已用只读内联检查对照路由目录：PAGES中73项无遗漏、无重复，全部路径与ID顺序匹配；41个不同组件入口文件存在；两份计划引用的npm脚本及定向测试文件存在。`npm run verify:docs`通过，其中路由产物检查确认73路由、60受保护路由、6种验收角色。该文档门检查既有必需文档，不代表已经验证本计划未来交付图与业务动作；新矩阵完整性由上述专门检查补充。

本轮未运行UI业务回归、浏览器验收或生产发布，因为只编制计划、没有修改运行代码。未创建临时测试文件或启动服务；两份计划均为永久交付物。配置、OpenAPI、Feature Map和生产运行包没有变化，不需要数据库迁移或重启。

### 1.6本次计划校验与交付

本次只读内联校验通过：73个PAGES ID唯一并逐项匹配真实路由、41个入口组件存在、19份实际规格与54份待补相符、六角色及W02–W08的73项分包一致；两份计划9处相对链接和18次npm命令引用有效。`npm run verify:docs`通过（73路由、60受保护路由、6角色、153份必需文档），`git diff --check`通过。此结果只证明计划结构和引用，不证明全部图稿、运行行为、线上版本或用户验收。

本次未改产品代码、API、数据库、依赖、配置、Feature Map、PAGES、生成清单或用户审核记录；未运行构建/业务E2E或连接生产。没有创建临时文件、图片、测试服务或浏览器，无需迁移、部署或重启。已有机会候选草稿和PROGRESS中的历史临时目录保留，不纳入本次提交，也不宣称完成或清理。计划文件为永久交付物，实施前按EXECUTION 3.4重新检查当时工作树。
