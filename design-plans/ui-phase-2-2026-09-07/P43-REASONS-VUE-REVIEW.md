# P43 十类原因窗 · 实际 Vue C 组合

后续收尾更新：[当前合同接续](platform-account-current-contract.md) 已解决下文旧独立检查的历史漂移失败。“提交确认待答/暂不提交”是此前时点记录，不再需要豁免该检查；视觉审核仍待答，最终Git状态以本轮收尾为准。

2026-09-11，P43-REASONS-VUE-r1。补充改密之外的十类原因变体，不把280截图当作280个页面或业务动作。

[四宽度280图册](../../output/playwright/p43-reasons-vue-preview/index.html) · [手机授予](../../output/playwright/p43-reasons-vue-preview/390-grant-super-default.png) · [手机撤销](../../output/playwright/p43-reasons-vue-preview/390-revoke-super-default.png) · [恢复键盘焦点](../../output/playwright/p43-reasons-vue-preview/1440-restore-confirm-focus.png) · [证据](../../output/playwright/p43-reasons-vue-preview/evidence.json)

## 范围与设计

使用 `frontend-design`，沿用本页新C原因结构：蓝色操作说明、白色原因输入、就近帮助和双按钮。授予/恢复确认用蓝色，停用/撤销用红色；颜色只表达方向，不表示授予或恢复低风险，更不是权限判定。中文标题、按钮和原2–300字符约束不变，正文控件16px、热区至少44px；没有新增按钮或确认步骤。

| 真实动作 | 实际原因标题 | 样例状态 |
| --- | --- | --- |
| 停用登录 | 停用用户并撤销会话 | 原active用户 |
| 恢复登录 | 恢复用户 | 明确disabled且无活动会话的合成变体 |
| 授予/撤销运营角色 | 授予运营管理员 / 撤销运营管理员 | 原无角色 / 显式该角色变体 |
| 授予/撤销安全角色 | 授予安全管理员 / 撤销安全管理员 | 原无角色 / 显式该角色变体 |
| 授予/撤销超级角色 | 授予超级管理员 / 撤销超级管理员 | 原无角色 / 显式该角色变体 |
| 单会话撤销 | 撤销该会话 | 原Chrome活动会话 |
| 全部会话撤销 | 撤销全部活动会话 | 原Chrome活动会话 |

实际PlatformAccountCenter、PlatformUserDetailDialog、PlatformAccountDialogs、useModalDialog挂载；沿用已有目录/详情及密码/原因的审核转换，完整业务script/指令/字段不修改。独立审核CSS仅对四个已知原因标题调整确认颜色，不改变生产全局样式或以前图包；此标题选择器是隔离审核接线，不宣称已完成生产语义动作组件接入。

## 运行与图像证据

390/760/761/1440，每个宽度10种变体，40次实际挂载组合、1,244项浏览器检查、42项当前源指纹、三份转换指纹、280PNG。每种变体7图：默认、字段焦点、取消焦点、确认焦点、悬停、原生按下、长原因。按下后将指针移出按钮再释放，不发起提交；随后通过真实取消及重新打开后Escape验证焦点归还。

每个组合只有概览和同一用户详情两个拦截GET，共80GET、零POST；未知接口及外部请求一律拒绝。所有标题来自实际入口触发后的DOM，不在宿主中直接设置reasonTitle。样例只明确修改列表角色、账号状态和停用时的会话数量，不伪造登录角色或生产权限结果；停用样例的三个角色操作仍禁用。

每图验证原生modal、内容无横向溢出；每组合验证原默认原因、2–300约束、控件字号/尺寸、按钮顺序、确认颜色、按下取消和两种关闭后的原入口焦点。测试不写数据库、不授予权限、不停用用户、不撤销会话。没有制造不存在的“原因窗写入等待”：真实父逻辑先关原因窗再写入，写入生命周期仍另验。

## 未完成与审核边界

当前标题不显示账号邮箱和单会话设备详情，保留真实组件可用字段，不能把相同标题视为目标身份复核完成。真实self-protection/超级管理员保护、跨角色可见性、服务端鉴权、SQL/审计事务、迟到回执和完整App/KeepAlive仍未由本图组验证。蓝色授予超级管理员仍是高影响权限操作，不因颜色获得任何豁免。

本批补齐本页十种非改密原因的实际C局部图；加上此前改密原因图，也不等于全部11变体已获批、已上线或本页完成。其他错误/写入状态、三主题两密度、200%原生缩放、软键盘、完整无障碍及全73页仍需推进。前面的目录、创建、改密布局和提交确认未答不默认通过。

## 使用与收尾

```powershell
node scripts/verify-ui-phase2-user-reasons-preview.mjs
node --test tests/unit/ui-phase2-user-reasons-preview.test.mjs
# 更新本批正式截图才追加 --capture。
```

无本轮生产源码、API/OpenAPI、字段、环境、配置、依赖、数据库或权限变化，因此后端、Python和部署配置不变；生产构建复用此前未变源码结果，不部署、不重启。280PNG、图册、证据、审核CSS、场景表和永久验证属于交付物，不是临时文件；Vite/浏览器由finally关闭。

此前焦点修复及P43创建/改密未提交改动保留，旧独立合同检查失败的提交确认仍待答，本批不自动提交，commit hash不适用。完整目标继续，不宣称全套实施完成。

最终收尾：4项本批定向测试、733项相关回归、文档/格式门禁和diff检查通过。既有102图包源/PNG漂移为0，本批280张新图通过独立源/图片清单验证，不加入旧分母冒充全站验收。验证端口51796已无监听，无临时进程或非交付文件待清理。已展示授予/撤销超级管理员、恢复确认焦点和长会话原因并请求共用视觉审核，未答不视为通过。
