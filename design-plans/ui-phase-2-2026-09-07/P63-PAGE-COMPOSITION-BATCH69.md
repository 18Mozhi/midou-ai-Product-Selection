# P63 实际 Vue C 组合 · 批次69

P63 原有 API-COVERAGE-C-r1 是离线稿。本批新增仅审核用的 Vite 转换：保留 `ApiCoverageDashboard.vue` 的 script、父级 GET、平台超级管理员入口与原始目录 DTO，将目录关联状态、全目录统计、三类分布和逐操作五维证据重排为蓝色报告身份与白色证据阅读区。审核版不进入生产构建，不读取受限报告，也不发起接口探测。

`tests/unit/api-coverage-page-preview.test.mjs`通过；实际 Vue 在1440/390与两种动效下分四组共80项检查、24次本地 GET，无外部请求、写请求、下载或页面错误。r1图册为双端6 PNG/199来源指纹，适用于默认布局、报告状态与逐操作证据审核，不代表真实报告、API探测、六角色权限、SQL/RBAC或生产验收。手机长路径、operation_id和多角色字段都可换行，未展开的共享筛选抽屉不会扩展390px页面宽度。

本批按实际数据范围保留“当前目录证据可关联”的边界：它只表示schema、method/path计数、指纹和operation_id可关联，不等于构建版本一致、元数据或权限新鲜、更不表示五项证据维度都已通过。父级筛选、刷新和技术详情保持原有合同；本页没有新增执行、下载或验证按钮。

无需重启、部署或配置变更。待用户审核手机默认、报告状态和逐操作五维证据的组合后，再核验筛选抽屉、403/读取恢复、完整键盘/读屏、真实受限报告与生产验收。
