# P62 链路日志读取反馈 · 批次33

从main/e7e570a3开始，原529项在途变化保留。按实际PlatformLogCenter、API客户端、P62规格和M06-02总纲核对，不把上一轮图册视为用户批准。

## 实际改动

生产组件只改超时说明及读取区域语义。原文在首次超时也声称保留上次日志；现在按原hasSnapshot判断：无observedAt时提示“读取超过 15 秒，已停止本次等待。 尚未取得链路日志。”；存在成功观测时提示“读取超过 15 秒，已停止本次等待。 已保留上次成功日志。”。这里描述客户端停止等待，不声称服务端处理已取消。

首次loading/error及成功空结果的原区域新增aria-busy与aria-labelledby，原h3增加对应id。实际生产标题层级仍为h2→h3。C审核宿主改成h1→h2，沿用原蓝白样式，CSS只把该标题选择器改为id；C整页布局仍未接入生产。

不改GET/query/source/status映射、三运行面、200条、组内排序、原15秒、客户端150/400ms安全重试、单飞、sequence、卸载取消、权限或导出。无API字段、OpenAPI、数据库、环境、依赖、后端/Worker/Python/插件消费方变更；本次没有新增调节项。

## 验证与图册

8项新增测试直接执行实际Vue script，保留真实Vue响应式，模拟请求/计时：首次、有记录、成功空结果的超时与失败恢复；原单飞、卸载晚返回、命名读取区域和实际模板编译。修改前4通过/4失败，修改后全部通过；连同6项P62组合、10项M06-02合同共24项通过。

`node scripts/verify-log-read-feedback.mjs`默认无输出。实际App/Vue/原API客户端，1440×1000和390×844，各reduce/no-preference；四组200检查。每组九次逻辑读取、15次实际GET（包括三个503各三次安全重试），共60次本地拦截GET，零真实写入/导出/下载/未知或外部请求。保留真实15秒计时，没有改生产计时或使用加速时钟。

覆盖首次失败→重试等待→首次超时→恢复三事件双链；保留链的失败/超时→成功空结果；保留空结果的失败/等待/超时→恢复原三事件。验证区域只出现一次、busy值、原事件内容和观测时间、零记录不复活旧链、原按钮禁用及空结果重复Enter/Space不重复请求。首次超时结束后可通过Tab/Shift+Tab回到重试并Enter恢复，仅证明稳定状态键盘可操作，不证明加载时连续返焦或完整读屏。

新[读取反馈r1图册](../../output/playwright/p62-read-feedback-r1/index.html)为双端18张区域PNG，168项捕获来源，HEAD e7e570a3加本批工作树；所有视觉待审，当前只申请手机首次超时区域审核。首次无输出浏览器试跑误匹配嵌套原因窗footer，已限定主区域直接footer的时间span后通过，未更改产品以适配测试，也未产生诊断图。

原默认页77项回归通过；完整前端类型/构建及253资源预算通过。构建使用当前混合工作树，不代表干净发布包或生产验收。格式和文档门单独核对。

## 未完成与收尾

原刷新按钮native disabled可能失焦，首次重试按钮在loading被卸载；本批不冒充修复。查询变化与在途请求归属、KeepAlive、快照/失败/导出requestId分离、首次401/403/429、权限后旧数据处理、完整列/长数据/原因范围冻结/真实CSV、73页全量与生产验证仍待继续。空快照失败沿用“没有匹配事件”标题及原提示，这是保留事实，不代表新失败查询也已确认无结果。

复跑方式如上；正式出图用`--capture-review rN`，只允许新建独占目录并拒绝覆盖，当前r1。旧P62组合r1/r2/r3的原清单和图不重写，本次组件/审核转换/标题CSS变更使三项来源不再等于旧捕获工作树；历史图不能充当全量当前来源验收。

只有前端反馈随未来正式前端静态包发布生效，不需要Node/Python重启或配置迁移，本轮未部署。新脚本/测试/18图及清单为永久交付；没有新建一次性脚本、下载或日志，浏览器/Vite均finally关闭，既有清理受阻目录不触碰。仅暂存本批文件与共享文档增量，不提交其他529项在途变化。
