# P08 实际 Vue C 组合 · 批次79

P08 新增审核用 Vite 转换，保留 `TenancyChooser.vue` 的组织目录读取、本地名称/slug 搜索、组织后并行读取工作区与团队、活动工作区才可提交 `/auth/context`，及空组织入口。界面重构为蓝色范围边界、组织目录与当前组织工作区分区，已归档项有明确不可进入文字；“当前账号”保持无 handler 的文字占位。

实际 Vue 在1440/390与两种动效下40项检查、4个本地拦截范围写入通过。r1图册有目录、搜索无结果、工作区、范围已就绪、无组织和权限拒绝双端12 PNG/39来源。组织选择不写入；仅活动工作区写入精确的 organization_id/workspace_id。个人空间创建入口仅呈现，真实创建与跳转不在本轮执行。

无需重启、部署或配置变更。待用户审核六种状态；真实成员资格、个人空间写入、迟到响应、权限与生产验收仍待完成。
