# 待审 C 整页组合 · 创建与改密跨路由回执验证

2026-09-13。上一轮P43/P44真实App整页r2仍待用户审核。本轮不改这份设计，不把无回复视为
批准；继续为其补足运行证据。全73页布局、按钮、弹窗、真实后端与宝塔交付目标不变。

## 本批结果

[48张独立回放图](../../output/playwright/account-pair-c-lifecycle-r2/index.html)，
[来源及逐项检查](../../output/playwright/account-pair-c-lifecycle-r2/evidence.json)。

390/1440 × 创建/改密 × 共享账号页/缓存平台概览 × 成功/失败，共16组240检查通过。
每组保留原“提交并暂缓 → 取消原窗 → 前进/后退 → 重开替换窗口 → 旧回执返回”的全部
动作、请求体、成功/失败和忙碌锁判断。原未变换App的224项结果逐项相同；另加16项精确
导航状态检查。创建草稿不被覆盖、替换详情不被关闭、旧失败提示不污染新窗口，成功通知
及写后重读增量保持。改密等待时的禁用按钮不被强行点击，不制造第二次写入。

手机最小8组112检查先通过。首次完整捕获完成手机组后，在桌面第一个场景停止：原全页
`getByRole('dialog')`零数量断言包含了C导航的常驻非模态dialog。检查实际导航脚本确认
840px以上使用open非模态模式；新包装仅将账号关闭断言定位在.account-center，同时严格
要求全页其余dialog为唯一“工作台导航”、`:modal=false`、不在账号区域，手机则必须为零。
它不是忽略多余弹窗；身份或模态状态不符仍失败。随后完整无图16组240检查通过，最终完整
捕获再次通过，48图/183来源绑定。未修改原驱动、生产源码或原r2设计。

## 版本与接口证据

- 原驱动完整SHA：`ee649e31674d6fac6d4922aef07651b5c018504ee44771a40bf537bf14205ebd`。
- 复用整页r2清单SHA：`c50588c178dca13d8559c1ce2901fe44161262ceb19ed3f1bb42fe5d44f9ef24`。
- 本批清单SHA：`716fe28fc63aa9568558d79e754f56fecb63aa086750384e27d0ec463f42831d`。

4个变换组件、6份CSS、全部审核支持文件逐项匹配原r2；变化只在本轮独立驱动包装和报告。
包装通过内存模块执行，不复制或落盘临时脚本；只允许精确输出、预览插件、元数据及上述
导航感知断言替换。永久测试逆向还原完整原驱动，未知变化失败关闭。错误输出保留诊断和
行号，但不打印整个内存模块base64 URL。

147次GET、16次POST均为本地拦截，未知API/外部访问拒绝，合成密码不写入证据字段。
创建仍为原五字段，改密仍为原临时密码和原因；每个请求原幂等头检查保留。
原未变换App捕获为145次GET；4组累计account列表GET相差1次（1组少1、3组多1），并非
所有请求数量相同。生产loadAccounts已有refreshing单飞抑制，快速路由观察时点可能改变
累计次数；本批没有进行计时归因，也没有因此新增或调整重试/并发规则。每组请求方法/路径
集合相同，释放写请求后的列表增量仍严格为成功1、失败0，不把累计差异隐去。

## 验证与未覆盖

新增5项永久测试，连同原App与整页r2保护共3文件18项全部通过，0失败/取消/跳过。
覆盖驱动边界、精确r2插件、未知变更拒绝、全部183来源及48PNG、16组原结果逐项相同、
导航非模态库存及重复capture启动前拒绝。原App48图和整页r2的80图完整哈希仍通过。

没有验证原生窗口保持打开时直接离页、敏感字段清理策略、其他改密/原因设计组合、所有
字段/状态/主题、真实改密/审计/权限或生产。这里仍采用原r2中的改密和原因呈现，没有擅自
合并其他待审设计。只验证先取消窗口再进行路由往返，不宣布整页交互或全站完成。

## 使用与交付清理

`node scripts/verify-ui-phase2-account-pair-lifecycle.mjs`无图完整回放；`--smoke`手机；
`--capture`独占新r2目录，已存在时启动前拒绝，不覆盖旧图。源码变化后需另建明确新版本。
`node --test tests/unit/ui-phase2-account-pair-lifecycle.test.mjs`运行本批永久门。

没有产品Vue/CSS、API/OpenAPI、数据库、环境/配置、依赖、权限、后端/插件/Python变化；
不涉及运行契约同步、宝塔重启或部署。新本地CLI说明已同步地图及运维文档。
最终48图/清单/HTML、工具和测试是交付物保留；没有新临时脚本或日志。
4个本轮Vite端口50784/50944/51287/51472已无监听，所有浏览器和回放进程结束。

首次中断捕获留下25张中间PNG，没有完成清单。精确清理在执行前被策略拒绝；未删除，
不重试或绕过，仍在：
`D:/项目工程文件/vue/curson/工具/智能选品/output/playwright/account-pair-c-lifecycle-r1`。
原先5处策略拒绝清理目录继续保留，路径见[整页r2收尾](P43-P44-APP-COMPOSITION-REVIEW.md#使用运行与清理)
和[原P43收尾](P43-LIFECYCLE-CURRENT-REVIEW.md#使用及收尾边界)。正式审核入口不引用中间包。

全库126失败仍为此前完整快照，本轮不重跑或推算。未提交/部署，commit hash不适用；
工作区含混合既有变更，暂存区保持为空。原整页审核问题不重复发送，其他页面/状态继续。
