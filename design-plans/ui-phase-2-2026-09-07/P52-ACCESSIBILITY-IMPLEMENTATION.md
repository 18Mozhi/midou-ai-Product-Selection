# P52 · 焦点与字段语义实施

日期：2026-09-12

起点：`main/298952a7`

页面：`/platform-admin/collection/overview`

生产组件：`apps/web/src/components/CollectionOperationsConsole.vue`

## 已实施

- 批量重放确认打开后，P52 总览根节点进入原生 `inert`；共享确认层取得初始焦点并维持双向 Tab 闭环，Escape 取消后解除隔离并返焦“预览批量重放”。
- 来源超过 8 条时，“查看全部/收起”按钮通过 `aria-controls` 关联实际来源结果区，并继续用 `aria-expanded` 表达开合状态。
- 批量重放原因增加稳定帮助说明、已输入字数和字段级错误；无效提交设置 `aria-invalid`、将帮助/错误 ID 写入 `aria-describedby` 并聚焦 textarea，继续输入即清除该字段错误。
- 批次页面反馈改为预先存在的礼貌播报容器，异步文案变化不再依赖临时插入 live region。
- 已有手机来源/尝试记录抽屉不改代码；本批补证其打开后隔离 `#app`、初焦点、Shift+Tab/Tab 循环、Escape 关闭和原记录返焦均有效。

## 保持不变

- 批量原因仍按去除首尾空白后至少 2 字、原始长度最多 500 校验；预览按钮仍可提交并在字段处反馈错误，没有把无效态改成永久禁用。
- 来源排序、前 8 条折叠、死信选择上限、批量快照、串行 POST、幂等键和结算规则均不变。
- 不修改共享 `ConfirmDialog` 或 `ResponsiveDataView`，也不改变 API、权限、数据库、环境变量、依赖、路由和部署配置。

## 验证证据

- 新增三组永久 E2E；旧实现桌面/手机共 4 项失败、移动抽屉既有能力 2 项通过，缺口准确落在 P52 调用层。
- 修复后新增定向桌面/手机 6/6；完整 M06-03 桌面/手机 42/42。
- `npm run build:web` 通过，包含浏览器助手构建、`vue-tsc` 和 Vite 生产构建。
- P52 C 图册惰性提取器已适配当前读取/写入生命周期；重新采集 61 场景×2 视口共 124 张图，视觉 PNG 无 Git 差异，浏览器校验零控制台错误、零 HTTP 请求。
- 所有页面数据和请求均为本地夹具/拦截；以上不代表真实鉴权、API、MySQL、Worker 或生产验收。

## 仍待

- 401/403 刷新后旧快照的安全呈现策略与真实权限验证。
- P52 具体 C 视觉的用户逐组审核、完整读屏/主题/密度/200% 缩放人工签收。
- 真实 API、审计、MySQL、Worker、宝塔部署和生产签收。
