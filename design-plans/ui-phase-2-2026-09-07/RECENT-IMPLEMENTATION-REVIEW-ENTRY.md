# P44 / P46 / P47 实施图接入统一审核台

## 本轮进展

从main/af239b08、446项在途变更、空暂存继续。核对已收到批准的区域均已有对应实施，
本轮没有重复实现，也没有替未回复的图作决定。发现统一审核台原生成器仅从动作登记
的actualVue条目收录图册，P44/P46/P47后续实施图缺少入口；这是审核交付缺口。

在原review.html增加独立补充材料索引，不改旧review-data.js、review-evidence.js、
源码候选ID、草稿存储键、schemaVersion1导出格式、审批或生产代码。
沿用已选C方向审核台，以frontend-design组织实施/历史层级、无障碍技能约束原生展开、
可命名图片和焦点返回；本地UI CLI缺失，未安装依赖。

## 新增可用范围

8组材料覆盖P44目录/比较控件/同角色结果/角色资料、P46结构/反馈、P47整合/手机空态。
核对406张原图哈希后，挑选15张指定区域图在原审核台展开；完整图册和原尺寸图片仍可
单独打开。406包含对照和邻页回归场景，不等于406张本页批准图或新增设计图。
每组显示原证据清单版本、范围、局部批准边界与剩余项。

生成器逐项比较原sourceHashes与当前LF源码。5组登记时来源一致；3组历史包每组有2处
差异：P46结构、P46反馈、P47整合均涉及ProviderAdapterCenter.vue和P47色板后续变化。
不因其中包含邻页回归而隐藏差异，也不据差异否定此前获批视觉；原哈希不重绑。
来源一致材料优先显示，但明确只是生成索引时核对，不是实时、全页或生产验收。
未来来源再变化须复跑check；旧动作登记材料不被这8组补充项整体重新验收。

“记录这组图的意见”沿用原批注系统，target为pageId/materialId@原manifest完整SHA。
原页面/候选批注仍保留；图组版本变更不会自动接管旧批注。只记录修改/澄清，不新增
自动批准入口，不推定未回复意见。目标仍是73页全套C设计、实际实现和最终验收。

## 使用和验证命令

打开本目录[统一审核台](review.html)，搜索P44、P46或P47，进入“02 / 真实 Vue 对照”。
先读范围和来源状态，再展开区域图、打开原尺寸，必要时记录图组意见并导出。
所有意见仅存在当前浏览器；导出后交给实施者归档，不写业务服务。

```text
node scripts/build-ui-phase2-recent-review-materials.mjs --check
node --test tests/unit/ui-phase2-recent-review-materials.test.mjs
node scripts/verify-ui-phase2-review.mjs
```

明确核对源码差异后，可用生成器`--write`更新补充索引；它只写review-recent-materials.js，
不改变原图、原manifest或审批。未知图片、图片漂移、重复标识、无来源、越界路径等失败关闭。
审核台验证器新增`--capture-recent`，只写本次独立review-proof/recent-materials的6张审核台
截图和manifest；不能与旧`--capture`混用，不覆盖原review-proof两张图或产品图。
普通无参数运行不截图。无需生产构建、部署、重启、环境变量或新依赖。

## 已执行证据

新增12项单测通过，包含真实8组/406图、生成一致性及修改/缺失/越界等反例；旧审核入口
链接与脚本闭合保护1项定向测试通过。真实浏览器1440/390两端完整审核回归通过：73路由、
原P16/P36图册、15张新图加载、来源差异展开、键盘Space/Escape/返回焦点、图组版本意见、
刷新保留、旧批注、导出schema1、文本转义、筛选、44px控件与无横向溢出，浏览器错误0。
这些是审核工具验收，不是P44/P46/P47的真实后端或全部产品交互验收。

首轮生成时一处桌面图片文件名与清单不符，生成器拒绝；改为清单中真实文件名后通过，
未伪造新图。首轮审核截图带隔离测试批注，目视后将截图移到写测试批注前，优先展示
来源一致包并补原尺寸入口；第二轮完整双端通过，独立6图重拍，旧产品406图未改。
最终截图见[手机P47审核区域](review-proof/recent-materials/390-P47.png)；已目视手机P47与桌面P44。

两轮各4个临时下载均通过download.delete删除；浏览器/HTTP服务61545与61811退出。
playwright-artifacts-32ESRm与playwright-artifacts-0bjMMv临时目录已检查不存在。
6张审核台截图、manifest、补充索引、永久测试和脚本均为交付物，不作为临时文件删除。

文档153文件/73路由/60受保护/6角色、运行说明、7份相关代码格式与定向diff检查通过。
补充索引最终check仍通过，旧406图未改；最终工作区457项变更，暂存为空，不暂存其他改动。
完整全库上次快照仍为1423项1240通过/183失败；本轮不重复未受影响的全库/生产构建，
不推算剩余失败数或放宽历史门禁。没有API/OpenAPI/权限/数据库/业务运行配置变化。
全库门禁、其他具体设计审核、全73页实施及真实宝塔验收仍未完成；不提交部署，
commit hash不适用。本轮审核入口交付已验证，全站目标仍在继续。

历史两处策略阻止清理的目录仍保留，不绕过重试：
`D:/项目工程文件/vue/curson/工具/智能选品/output/playwright/p47-c-e2e-temp`；
`C:/Users/23136/AppData/Local/Temp/scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`。
