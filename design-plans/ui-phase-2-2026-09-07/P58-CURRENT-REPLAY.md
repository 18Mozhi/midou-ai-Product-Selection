# P58 当前图包：共享筛选关联修复后的重放

2026-09-12，main / af239b08。承接 [P47 当前重放](P47-CURRENT-REPLAY.md)，继续修复共享组件
已变更后当前审核图包的来源绑定。只重放现有七个图包及更新记录，不修改应用、预览、驱动、
测试或历史 COMMERCIAL-C-r1 图册，不将既有待审提案变为已批准。

## 实际执行与比较方法

使用 ui-skills-root 选择 Playwright 的实际浏览器验证。CLI 本地缺失，不安装新依赖，复用
项目已有 Vite/Vue/Playwright 驱动。原脚本的固定时间、三宽度 390/760/1440、原始 E2E
数据与精确源码锚点均保留，未缩减场景或改宽像素阈值。所有七包重放前唯一来源失配都是
ResponsiveFilterDrawer.vue，其他来源指纹及原图片清单先逐一验证。

前版 PNG 仅作为 Buffer 存在一个 Node 比较进程中，不写临时备份目录；驱动实际退出成功后
再核对新来源、图片字节/尺寸及逐像素差异。原清单保留真实请求键，不直接改写哈希来过门。
跨运行比较只将各 run.requests 中新生成的非空本地 idempotencyKey 按首次出现次序映射，
同一值在请求与检查中始终指向同一映射；显式 retry keeps key 检查必须引用已出现的请求键。
null/缺失不归一化；方法、路径、请求体、查询、版本、原因和检查值均不剔除。
映射不是实际修改 API 幂等键，也不能用它证明服务端幂等。原验证器继续严格比较本次运行的
原始两次拒绝请求，不放宽同键同内容要求。

| 当前包 | 执行命令（仓库根目录） |
| --- | --- |
| 创建默认 | `node scripts/verify-ui-phase2-commercial-create.mjs --capture` |
| 创建等待/拒绝 | `node scripts/verify-ui-phase2-commercial-create-write.mjs --capture` |
| 创建后目录双结果 | `node scripts/verify-ui-phase2-commercial-create-outcome.mjs --capture` |
| 创建键盘边界 | `node scripts/verify-ui-phase2-commercial-create-focus.mjs --capture` |
| 编辑 | `node scripts/verify-ui-phase2-commercial-edit.mjs --capture` |
| 十类影响确认 | `node scripts/verify-ui-phase2-commercial-confirm.mjs --capture` |
| 确认等待/拒绝 | `node scripts/verify-ui-phase2-commercial-confirm-write.mjs --capture` |

## 验证边界

默认创建、创建拒绝、创建双结果、编辑、十类确认的 baseline 是实际生产 Vue；review 是局部
审核转换。创建键盘和确认等待/拒绝包的 baseline 则是前版 C 预览，不冒充原生产页。
这些提案仍未实施生产，不能把本次重放称为业务或异步缺陷修复。

全部请求由本地 allowlist 拦截，未知外网与 API 拒绝。创建双结果的 201 是明确的本地样例；
其余提交样例只有 409/403 拒绝。没有真实成功保存、收费、配额改变、MySQL、权限或审计
验收，也不改变原请求重试次数。结果未知时冻结/重试规则继续等待用户决定，不作默认授权。

## 结果与收尾

| 当前包 | 组 | 检查 | PNG | 字节相同 | 像素变化 | 源文件 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 创建默认 | 6 | 114 | 24 | 21 | 3 | 162 |
| 创建等待/拒绝 | 12 | 216 | 42 | 37 | 5 | 165 |
| 创建后目录双结果 | 24 | 444 | 66 | 65 | 1 | 167 |
| 创建键盘边界 | 6 | 597 | 30 | 21 | 9 | 168 |
| 编辑 | 6 | 168 | 33 | 24 | 9 | 163 |
| 十类影响确认 | 60 | 894 | 76 | 76 | 0 | 163 |
| 确认等待/拒绝 | 60 | 2240 | 210 | 193 | 17 | 170 |

总计 174 组、4673 项浏览器检查、481 PNG；437 图字节相同、44 图变化，尺寸均保持。
按上述本地幂等键映射后，所有 runs 的检查、请求、事实、背景指纹及原有其他记录严格相同。
十类默认确认 76 图全部字节一致；不能把这句话扩大到全部编辑错误或提交反馈图。
[实测 JSON](P58-CURRENT-REPLAY.json) 保留逐张前后 SHA、像素数、通道差和包围盒。

### 三张编辑原生校验图的较大差异

编辑包 baseline 的 reason-invalid-part1 在 390/760/1440 分别变化 12781/25531/30093 像素，
最大通道差分别 33/184/151，不能当作细微抗锯齿或宣称全包视觉相同。当前 760 图可见浏览器
原生 minlength 气泡覆盖原因下方及按钮区；三张均为未经 C 转换的原生基线，不是新 C 稿。
其余 41 张变化单图不超过 207 像素；最大通道差 8 仅出现在创建键盘包桌面展开图的一个像素。

为核对时序，在原编辑驱动的**内存派生版本**仅保留 baseline，关闭 capture；每个宽度于
原原因校验位置连续截图三次，额外等待依次 0/120/500ms（不把截图耗时算作固定间隔）。
逐次断言原因仍为“审”、tooShort=true、validationMessage 原样、焦点仍在原因框、输入框
矩形相同；三宽度分别复现最多 15301/26839/31685 像素变化，位置与原生气泡区域相符。
这说明 native 气泡绘制时序可产生大面积像素变化，但不证明历史三图差异的唯一成因。
没有增加产品延时、改最小字数、关闭校验、修改截图驱动或放宽测试；不将原生气泡当作
已完成的中文产品错误区域。正式三张当前原生基线保留，不用提案图片覆盖它们。

定向复现沿用原编辑流程的 78 项检查通过，零落盘图片/日志/脚本，独立于上表 4673 项。
首次内存导入因错误选择已安装 Playwright 包的 CommonJS 入口而在启动浏览器前停止；按
该包 exports.import 改用既有 ESM 入口后成功，未改依赖或正式代码。

### 门禁与清理

七个相关单测文件共 29/29 通过：前五包 20 项、默认确认 4 项、确认等待/拒绝 5 项。
既有六项未知结果 DIAGNOSTIC 所涉及源码/预览未变，不重复运行，更不把风险复现当修复。
构建输入未变，不重复构建。未重跑全库：上一批全库 1180 通过/199 失败是历史结果，不能
凭本轮七项当前绑定转绿推算最新余数；整体提交/发布门尚未通过，commit hash 不适用。
收尾文档门通过（73 路由、60 受保护、6 角色、153 必需文档），运行文档一致性通过；
格式检查通过（111 个变更代码文件、535 生产文件、51 仓储），git diff --check 通过。
七包所有原始源码 LF 哈希、481 PNG 字节/尺寸、测量 JSON 的新图哈希均重新校验通过，
图包目录无额外文件。5173 无监听，捕获和诊断进程均正常退出；暂存区为空，391 个工作区
变更条目保持原归属，不混合暂存。旧两处临时目录只读复核仍为 1 文件/45 字节及 260 文件。

七个正式图包及本报告保留；前版图片和诊断截图仅在内存使用，比较/诊断进程退出后释放。
本轮无新增临时文件或目录，全部 Vite/Chromium 通过 finally 关闭，顺序使用本地 5173。
上两批被工具策略拒绝删除的临时目录仍保留，本轮不重试绕过，需用户手动清理：

- `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p47-c-e2e-temp`（1 个 45 字节状态文件）。
- `C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`（260 个前版副本）。

下一步为共享筛选按钮新旧候选身份的显式合同登记。当前只读定位确认验证器已有 S# 表格
替代机制，但 Q# 仍取历史表；不能通过覆盖历史身份或更新全部哈希来消除该失败。

无 API/OpenAPI、环境变量、配置、依赖、数据库、权限、运行源码、全局主题或部署变更；
生产无需重启。原待审问题不重复发起、不自动通过，全 73 页实施目标保持。
