# P47 当前状态图包：共享筛选关联修复后的重放

2026-09-12，main / af239b08。承接 [共享筛选关联修复](FILTER-DRAWER-ASSOCIATION-REVIEW.md)。
本批只重放四个当前图包并登记实测结果，不修改运行源码、验证器、测试、历史图或用户批准。

## 执行与边界

按 ui-skills-root 选择 Playwright 实际页面验证；本地技能 CLI 不可用，不安装新依赖，复用
仓库现有 Vite/Vue/Playwright 驱动。重放前逐图校验原 PNG 字节与清单，在 Node 进程内存中
保留前版 Buffer，重放后校验新清单、实际源文件、尺寸和像素；不创建临时对照目录。
四包重放前唯一源指纹失配均为 ResponsiveFilterDrawer.vue。原历史访问、分页、读取失败
图包及其驱动保持，当前重放不是通过直接改哈希绕过验证。

| 当前包 | 仓库根目录执行命令 |
| --- | --- |
| 访问状态 | `node scripts/verify-ui-phase2-provider-adapter-current-states.mjs --state=access --capture` |
| 筛选分页 | `node scripts/verify-ui-phase2-provider-adapter-current-states.mjs --state=filter-pagination --capture` |
| 首次读取失败 | `node scripts/verify-ui-phase2-provider-adapter-read-error-current.mjs --capture` |
| C 整合 | `node scripts/verify-ui-phase2-provider-adapters-c-integration.mjs --capture --e2e` |

前三包 baseline 为当前实际页面，review 为局部审核预览，不把预览的访问恢复或分页焦点
提案称为生产实现。C 整合包使用实际 index/main/router/App/NavigationShell/KeepAlive，
不注入页面提案，保留原 16 项 M03-03 端到端测试。所有接口均本地拦截，未知外网请求拒绝；
前三包只有 GET，C 整合的健康检查成功/失败也只是本地样例，无真实探针、数据库或线上写入。

## 结果

| 包 | 场景组 | 检查 | PNG | 字节相同 | 像素不同 | 源文件 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 访问状态 | 30 | 411 | 60 | 53 | 7 | 178 |
| 筛选分页 | 6 | 162 | 36 | 36 | 0 | 178 |
| 首次读取失败 | 30 | 357 | 30 | 29 | 1 | 176 |
| C 整合 | 36 | 430 | 20 | 18 | 2 | 174 |

共 102 组、1360 检查、146 PNG；所有 runs（包括检查值、请求及焦点记录）前后严格相同，
没有归一化字段。136 图字节相同，10 图像素不同，尺寸均保持。访问状态差异均在 baseline，
单图最多 27 像素、最大单通道差 2；读取失败唯一差异是桌面邻接 503 对照区的 4 像素、
通道差 1；C 整合两张手机返回图各 2 像素、通道差 1。所有筛选分页图、访问 review 图，
以及读取失败核心错误提案图保持字节一致。

[精确测量 JSON](P47-CURRENT-REPLAY.json) 记录十图前后 SHA、尺寸、变化数和包围盒。
测量仅说明本轮差异，不宣称唯一成因，不新设通用容差。现有驱动对邻接普通 500 的 64/2、
读取失败邻接状态的 8/1 原比较门保持，且均通过。目视查看手机筛选展开及末页图，保留
五字段、原生展开控件和持续页码焦点的实际画面；视觉仍按原待审范围审核。

C 整合原 16 项端到端测试全部通过（24.8 秒）；六个相关单测文件共 28/28 通过，包含
当前四包源/图片绑定、历史图不可变、既有空态焦点修复、局部预览边界和实际 C 运行合同。
分批执行命令为访问/分页/当前预览/空态四文件（18 项）、读取失败（5 项）、C 整合（5 项）。
本批无源码、依赖或构建输入变化，不重复构建。亦未重复全库单测：上一批实测是 1379 项
中 1180 通过/199 失败，不能根据四项当前绑定转绿推算最新全库总数，仍不满足整体发布门。
未提交、未部署，commit hash 不适用。格式检查通过（111 个变更代码文件，535 个生产文件、
51 个仓储检查）；文档门通过（73 路由、60 受保护、6 角色、153 必需文档），运行文档检查
及 git diff --check 通过。收尾重新校验四包全部当前源哈希、146 图字节/尺寸和差异 JSON，
均通过。暂存区为空，工作区 389 个既有及本次变更条目保持，不混合自动暂存或提交。

## 临时产物与进程

四个驱动的 Vite 和 Chromium 已关闭，七个实测端口 64125/64270/64441/64472/64518/64663/
64812 均无监听。对照图片只在内存保留，Node 比较进程退出后释放；146 图及测量记录为
正式交付保留。端到端测试新建的 `output/playwright/p47-c-e2e-temp/.last-run.json` 为 45 字节
临时状态文件。精确路径删除命令被工具策略拒绝，未执行、未重试绕过，因此该文件及目录
仍待用户手动清理，不声称已清理：
`D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p47-c-e2e-temp`。

上一批已有 260 文件临时对照目录同样尚待手动清理，本轮未再次尝试删除：
`C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`。
两者均不是正式审核图包；保留与阻塞原因透明交接，不删除用户原始文件。

## 未变更与后续

无 API/OpenAPI、参数、配置、环境变量、权限、业务规则、数据库、依赖或部署变更；无需
生产重启。待审状态保留原审核入口，不重复扩大空态、成功反馈或默认页的局部批准。
全 73 页交付、P58 当前证据和共享按钮候选身份显式登记继续，尚未达到整体完成或发布门。
