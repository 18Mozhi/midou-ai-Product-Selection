# P47 分页焦点接入当前预览

2026-09-13，main/af239b08，起始494项工作树变更、暂存为空。上一轮已改变实际 Vue 并生成
前后证据，属于进展；本轮继续实际修复与评审稿的衔接，完整73页C目标保持开放。

## 修改范围

使用 fixing-accessibility 技能约束焦点归属及原生控件，按仓库现有 Playwright 驱动验证，
无新依赖。本轮不改生产 Vue/CSS、接口、权限、环境变量、数据库或批准记录。

新测试辅助 `ui-phase2-adapter-pagination-focus-baseline.mjs` 仅接受完整24a3b2当前源码或
51c0ba修复前源码。逆向删除完整分页函数、还原完整分页nav后必须等于原全文SHA；未知
代码、筛选页长、焦点条件或任意尾部变化失败关闭。不将还原结果用于当前生产渲染。

当前访问/筛选分页组合器先沿已验证来源生成原提案，再逐字接回真实同步分页函数：访问
稿保留真实分页模板；筛选稿仅替换原异步分页处理器，保留它本来的待审布局/类名/单页
隐藏提案。刷新、空态清除函数与手机空态七处修改都保留，不能借后续预览撤回生产修复。
单测分别移除接回部分后必须全文等于上一修复阶段的预览，不能用局部关键词冒充保持。

当前重放入口只在确切24a3b2版本调整两处基线焦点期望：原BODY变为真实页码，与当前
实际修复一致。其他交互、读取次数、原脚本SHA固定及finally清理继续使用原驱动。
两处DOM匹配采用同一分页区span，兼容真实页码与原提案class，不放宽为任意焦点元素。
新增来源收集包含逆向辅助；这次仅无图运行，未生成新的完整来源哈希/图片包。

## 验证结果

| 检查 | 实际结果 |
| --- | --- |
| 最小源码/原焦点测试 | 20/20通过 |
| 增加覆盖保护后的四关联测试文件 | 30项、28通过、2失败，2083.6145ms |
| 当前访问状态实际App/提案 | 30组、411检查、182来源计数、0新图 |
| 当前筛选分页实际App/提案 | 6组、162检查、182来源计数、0新图 |
| 原访问/分页审核图 | 60+36张原PNG与完整原manifest/原驱动/原样式哈希保持 |
| 相关代码格式 | Prettier通过 |

浏览器保持原401/403/429/503/500读取/恢复、45条样例的20/20/5分页、双向边界、筛选回页、
空态清除与焦点恢复验证。合计36组573检查；没有新增真实GET、POST、凭证、探针或部署。
仍是隔离本地请求，不等于实网/权限/读屏器/完整状态或生产验收。

两项失败分别在 `ui-phase2-provider-adapter-access.test.mjs` 和
`ui-phase2-provider-adapter-filter-pagination.test.mjs` 的旧current-review包校验中，
首先命中驱动SHA：旧期望32756eba，当前5670fdbb。包中还包含其他早期来源，因此不能仅
更新这一个SHA便称全包通过。本轮保留失败与旧清单，不跳过或改写历史预期；其历史来源
分层和其他预览适配仍待处理。新实际脚本、模板编译、局部样式与焦点测试均通过。
最近完整全库仍是上一轮1478项1309通过/169失败，不从本轮定向结果推算新的全库失败数。
生产源未变，不重复前一轮已通过的build:web或实际分页原图捕获。

## 使用与收尾

无图复验：

```powershell
node scripts/verify-ui-phase2-provider-adapter-current-states.mjs --state=access
node scripts/verify-ui-phase2-provider-adapter-current-states.mjs --state=filter-pagination
```

在当前分页修复版本下，加 `--capture` 会在启动浏览器前明确拒绝，避免覆盖现有
`p47-access-current-review` / `p47-filter-pagination-current-review` 历史图片。两个负例
实际启动CLI并验证退出1、无浏览器启动输出、原manifest字节不变。要交新图，应建立新版本
目录和完整来源清单，不应删除旧包后复用旧名称。

61758/61826及61779/61955收尾均无监听，两个驱动已退出，无新增临时文件、图片或留用
进程。本轮新辅助、测试与本记录为正式交付。此前删除受限的
`output/playwright/p47-c-e2e-temp` 和
`C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`
仍保留，不绕过删除。未提交、未部署，无重启要求；完整图证门和全73页交付尚未完成。
