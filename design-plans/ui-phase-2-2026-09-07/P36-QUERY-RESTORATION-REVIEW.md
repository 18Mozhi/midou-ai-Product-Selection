# P36 查询历史恢复修复

2026-09-10，main / c380b995。保留此前未提交的手机筛选实装；此次仅修改同一个子组件的查询同步 script，模板、CSS、字段校验、创建/轮换/撤销及权限不变。原始完整 73 页 C 重构、审核和宝塔部署目标保持，不将此修复当作整页完成。

## 结果与对应证据

上一轮 [真实父子读取核对](P36-PARENT-READ-LIFECYCLE-REVIEW.md) 复现的 P36-HISTORY-01 / 02 已在本地实际 Vue 修复：同实例查询变化、浏览器前进/后退、KeepAlive 离页返回均恢复 URL 对应筛选；批量恢复四字段及明确第二页时，不再因筛选 watch 误重置到第一页。

- [修复后 28 张双端实际图](../../output/playwright/p36-parent-read-vue-r2/index.html)：178 项实际父子组件/Router/KeepAlive/API client 检查，两个已知问题转为明确成功断言，不再允许“记录问题但命令成功”作为恢复验证。
- [修复前 26 张问题图](../../output/playwright/p36-parent-read-vue/index.html) 与原证据原样保留，不回填为当前通过。
- [原手机筛选 10 张图](../../output/playwright/p36-mobile-filters-vue/index.html) 原 PNG 不变；真实子组件重新执行 290 项检查及 56 项非目标像素比较，随后才更新源关联。
- 原 112 / 354 / 196 提案图共 662 张、字段 capture-time 属性和局部批准保持。三份来源清单明确从“只改展示”升级为“只改展示与查询同步”，不再声称 script 完全不变。

这些是当前实际组件的核对图，不是未批准区域的新 C 稿。旧页头、创建区域和错误样式仍保留；修复后 JSON 的 `acceptanceComplete` 仍为 false，完整页面视觉/业务/生产验收未通过。

## 实际修改与保持项

| 修改                         | 边界                                                                                                                   |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 监听当前路径及 query         | 仅当前子组件所属路径恢复；离开时不把令牌筛选写进其他路径                                                               |
| 批量恢复标记和 nextTick 代次 | 四字段和 page 一起恢复，避免普通筛选重置 watch 干扰；连续恢复只释放当前批次                                            |
| query 指纹与待完成写入表     | 相同 query 不重复 replace；自己的回执不反向截断长输入或覆盖更新的本地输入；失败回执释放占用                            |
| 精确增量证明                 | `ui-phase2-token-query-delta.mjs` 将新增查询片段逐字反解，非查询业务 script 与 c380b995 一致；另保留原模板展示增量检查 |

仍用 `org_token_query/status/scope/sort/page`，未新增 query 键。URL 读取文本上限 200，非法选项回原默认值，页码为正安全整数；本地输入 220 字仍允许，其自身 URL 回执不截短。保留 6 条分页、现有排序与匹配字段、无关 query；默认筛选条件下点击重置仍不强制第一页，实际条件改变才按原 watch 回第一页。结果减少时保留原页数钳制行为。

未增加 API 读取或自动刷新策略，缓存数据是否重新读取不是本批决定；未改变任何请求路径、方法、body、权限、审计、幂等键或版本字段。创建草稿不会因查询恢复被清空。

## 验证

先运行 9 个直接回归测试，再运行 P36 相关 34 项测试；随后 web 类型检查及全部 UI 第二阶段专项单测通过。

9 项直接测试用真实 Vue reactivity/watch 执行真实 SFC script，不复制恢复逻辑：精确修改边界、四字段及第二页批量恢复、重置语义、非法/数组 query 与长 URL、220 字自身回执、迟到回执、失败回执释放、离页保护及草稿保留、连续恢复与页数缩减。它们不是浏览器历史的替代，因此另外保留 178 项真实浏览器回归；后者核对真实列表名称与 DOM 身份，不只看 URL 或结果数量。

```powershell
node --test tests/unit/ui-phase2-org-token-query-sync.test.mjs
node --test tests/unit/ui-phase2-org-token*.test.mjs
node scripts/verify-ui-phase2-org-token-parent-read.mjs
node scripts/verify-ui-phase2-org-token-mobile-filters.mjs
npm run typecheck:web
```

父子组件 verifier 的 `--capture` 只写新的 r2 目录，不覆盖修复前图片。手机 verifier 新增 `--refresh-source` 为此次来源迁移使用：先证明 child 只新增精确 query patch，核对其他原有源与 PNG，再完整重放检查，最后更新源清单而不重写 PNG。日常检查使用无参数命令。`refresh-ui-phase2-token-filter-bindings.mjs --write` 对原三提案清单只接受原始值、准确上一轮展示关联或本轮精确关联，不接受任意来源漂移。

原通用审核包重新核对：102 包 / 15069 PNG 来源与图片零漂移；该总数不重复加入独立实际图目录。动作仍为 37 页登记、757 唯一源位置，整页批准数仍 0。来源清单、P36 正式登记与统一审核索引同步，登记不等于页面实现完成。

## 使用、运行与剩余边界

使用时在组织令牌页改变查询链接或使用浏览器前进/后退，筛选和记录应跟随当前 URL；离开并从无 query 的令牌链接返回时恢复默认筛选。不需要新增设置、env 或依赖；没有可调新参数。未改后端/插件/Python/数据库，因此 OpenAPI、服务端合同和环境模板不需要变化。尚未部署；将来发布前端静态包后由浏览器重新加载，无 Node/Python 重启要求。

验证仅本地 GET 样例，禁止所有写入和外部请求，不读取生产令牌或系统剪贴板；完整 NavigationShell 鉴权、组织/工作区范围切换、真实 API/MySQL、并发写操作、全部 C 视觉仍待。

P35 基线已有颜色门失败仍未获准修复，未修改 P35 或放宽测试，因此未暂存、未提交、未部署，commit hash 不适用。创建组合审核仍待用户回复。本轮图片/证据为永久交付物；验证浏览器与服务在 finally 关闭，误用根目录不存在的 typecheck 命令产生的本次 npm 日志已删除，实际验证改用现有 `typecheck:web`。

收尾：默认178项父子浏览器重放通过，r2图册28图双端解码/无横向溢出/无浏览器错误；统一73路由审核入口双端过滤、焦点、意见导出及证据索引检查通过。审核导出的两份临时下载已删除；类型、文档/运行文档、格式和diff检查通过。未重复运行与本次query修改无关且来源未变的P35已知失败颜色门，也未将UI专项通过表述为全项目测试通过。
