# P32 恢复原因窗 · 已批准组合进入真实 Vue

2026-09-10，从干净main/07902a2e继续。依据[P32手机组合批准](P32-RESTORE-COMPOSITION-APPROVAL.md)，不扩大至桌面、其他状态、字段上限、整页或生产批准。

[真实Vue图册](../../output/playwright/p32-approved-restore-review/index.html) · [手机组合](../../output/playwright/p32-approved-restore-review/390-restore-deep-ocean.png) · [源与图指纹](../../output/playwright/p32-approved-restore-review/evidence.json)

## 实施范围

OrganizationAdminCenter仅在workspaceAction的restore分支给共享原因请求提供工作区名称、版本、明确范围成员数的只读展示快照。useAuditedReason保存该可选上下文，取消/提交清空当前请求，替换为其他请求时不残留。无上下文请求的对象结构与原默认值不变。

AuditedReasonDialog只在显式workspaceRestore上下文下呈现目标说明、原因区和蓝色确认；独立作用域令牌保证白色内容面、中文无衬线标题、蓝色输入焦点、44px操作区及16px帮助文字。既有全局标题/页脚/元数据样式以局部选择器及局部字体令牌处理，不改全局主题。归档/P30/P31等调用不启用该外观。

不复制提案中的500字前端截断，也不把原型技术说明带入生产表单。保留真实审计帮助文字、required、minimumLength默认2和trim提交。父层POST路径、action、expected_version、原因及提交后关闭/等待/失败重开默认原因的既有顺序不变；版本仍按原代码在原因确认后取item.version，展示快照不重新定义写入对象或并发政策。

## 验证与使用

`node scripts/verify-ui-phase2-workspaces-vue-restore.mjs --smoke`检查手机；`--capture`生成双端94项检查/20张正式图；无参数检查当前源/图指纹并重跑。真实Vite路由挂载，复用既有组织管理员E2E夹具；请求全部被浏览器隔离处理，不能当作MySQL/权限/生产审计验收。

覆盖三现有主题环境、目标/版本/成员数、至少两字、501字不截断但不提交、键盘循环、三种关闭返焦、等待禁用、精确一次版本化POST、409失败及成功后列表更新、归档不继承样式、P30撤销邀请保持原样。永久单测核对默认对象不变、上下文快照、替换/取消/提交清理和源图绑定。

用户仍从工作区详情点击“恢复工作区”，填写原因并确认，不需新配置。没有新API字段、路由、依赖、环境变量、权限或数据库迁移；OpenAPI、后端、Worker、Python及部署器不涉及。没有部署或当前重启；后续仍需完整宝塔发布门禁。

已批准原PNG固定保留；相关旧提案按共享来源变化执行原验证器复验，不能只改来源哈希或把旧图充当真实Vue。本批20张真实图为永久交付，临时Vite和浏览器由finally关闭。全P32重构、其他待审组合、跨页全站实现与生产签收继续待办。

## 收尾结果

最终完整单测661/661通过；定向父/子/相邻调用52项及主题/展示上下文9项通过。前端构建408模块、251资产体积门、153文档、73路由/60受保护/6角色、运行文档和390文件静态检查通过。首次全量发现共享合同旧行号及新令牌未登记的问题，已修正后完整复测；令牌门仅允许此精确dialog作用域的自定义属性，不放开普通CSS硬编码颜色。

受影响旧包按依赖顺序运行原浏览器验证器，26份来源清单更新；另复验数据质量包，P31真实Vue54检查/22图继续通过。新目标展示改变父组件三处调用签名及共享原因窗三处定义/表单签名，当前合同及P18/P29/P30/P31/P32/P54相关登记已按实际AST核对更新，旧基线仍保留。复采产生部分旧截图字节差异，不能称全部旧图未变；P32获批图640c22bd…7c49176和P31获批延期图a1f7d4f…fd11完整SHA256比对不变。

全站96图包14039PNG零来源/图片漂移；审核入口真实Vue关联从3增至4，不等于4页或全站验收完成。审核导出的playwright-artifacts-JejhHS、playwright-artifacts-U4NeOF目录已不存在；临时单测夹具已清理，已知本机验证端口无监听。正式20PNG及图册/证据保留并提交，不删除项目已有构建输出。当前没有运行服务需要用户手工停止。
