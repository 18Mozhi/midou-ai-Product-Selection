# 共享详情当前交互复核

## 范围与结论

沿用全73页 C 重设计目标。本批不修改生产 Vue、API、权限、依赖、数据库或运行配置；
不改历史图和批准范围，也没有解除现有失败门。使用 fixing-accessibility 检查焦点、
嵌套弹窗、关闭返回及缓存退出；本地 ui-skills CLI 不存在，未安装新依赖。

增加独立当前验证入口 `node scripts/verify-responsive-data-view-current.mjs`，不接受参数、
不截屏、不写业务数据。它以 SHA256 固定原驱动 f891a55f…，只适配测试宿主，实际导入当前
ResponsiveDataView、ConfirmDialog、AuditedReasonDialog 及现有样式。原驱动与旧图证保持原样。
当前证据不自动替换历史证据，不代表真实权限、全部20个消费者或整页视觉验收。

## 已实际执行

- 原驱动不带 capture：10组检查通过，0图片。
- 当前驱动：default/governance/content × reduce/no-preference × 390/760/761/1440，
  6组场景、24个宽度组合、72条聚合检查通过。桌面检查只覆盖表格显示及没有背景锁定，
  不冒充桌面详情弹窗验证。
- 手机覆盖初始焦点、双向 Tab、隐藏/禁用/折叠内容跳过、背景 inert、真实确认窗与原因窗、
  Escape/标题/遮罩/插槽关闭、焦点交接、同键刷新、移除记录回退、单按钮循环、卸载恢复。
- 新增真实 Vue KeepAlive 切出与返回：打开详情时切出，背景可操作，返回不幽灵重开、
  不抢夺背景焦点，再打开后双向 Tab 可循环。预先 inert 的元素仍保持 inert。
- 实际 App/NavigationShell/路由缓存的既有驱动也不带 capture 重跑：4个宽度、8个观察通过，
  每宽度5次本地样例 GET，0真实业务写入；离开及返回时 dialogs=0、appInert=false。
- 新增适配器单测3/3通过，验证六种适配保留原有 expect/assert.deepEqual、拒绝原驱动变化、
  拒绝未知参数、CRLF一致。它们是适配器测试，不冒充浏览器行为证明。

首次当前驱动运行在前4组通过后，content/reduce 等待初始列表入口超时，未得到完整场景结果。
随后添加挂载前置断言与页面错误/阻止请求的失败诊断；未延长超时、未自动重试、未更改 Vue。
第二次完整6组通过。首次失败原因未获充分证据，不能宣称已修复主题交互问题或证明永不偶发。
若再次发生，保留诊断并先查测试页挂载原因，不得继续延长等待掩盖问题。

## 仍未通过的历史门

`responsive-data-view-focus.test.mjs` 与 `responsive-data-view-lifecycle.test.mjs` 原样执行：
9项、3通过、6失败、0取消/跳过，221.68秒。已确认的边界：

1. 历史图证绑定 b9e635a3…，实际共享组件是8669cbd3…；旧接续函数仅允许6d3088d1…。
2. “20调用者全不变/只有 deactivation hook”断言已被后来页面修改违背。
3. 当前实际 App 回归通过不能使旧证据 sourceHashes 自动成为当前源码证据。
4. 旧 lifecycle 像素差异表中的 `output/playwright/responsive-data-view-focus/390-close-focus.png`
   记录 afterSha256=ed7b8531…；磁盘图与它自己的 manifest 均是 ce9b7f6c…。
   此图当前 Git 干净，最近修改提交093d643b（完善P50通用写入归属），不是本批截图造成。
   下一步核对这次显式后续修订及历史阶段图，不能扩大像素容差或覆盖旧差异清单。

完整全库没有重跑：运行时代码、原测试和原图证输入未变；本批完整相关验证为上述浏览器、
新单测与原9项。上一轮1388项/1218通过/170失败只是上一轮快照，不推算本轮全库数量。
相关历史门仍失败，不自动提交或部署，commit hash 不适用。

## 使用与收尾

复用已安装依赖，运行上述当前驱动即可检查本地实际 Vue。它以临时本机随机端口启动验证宿主，
每组通过或失败均走原 finally 关闭浏览器与服务；无后台重启或生产配置调整要求。
新脚本、适配器及单测属于永久回归交付，不是临时文件。没有新增截图、文件型日志或测试夹具。
收尾确认：所有浏览器/验证宿主退出，13个记录端口无监听。文档检查通过（73路由、60保护、
6角色、153必需文档），运行文档检查通过；格式门通过（changed=116、production=535、
repositories=51），git diff --check通过。暂存区为空，当前399项工作树变更未混合暂存。

以前被工具策略拒绝删除的两处不重试绕过，仍待手动清理：

- `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p47-c-e2e-temp`。
- `C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`。

原视觉待审及 P58 未知创建结果规则继续待定，本批无新增确认问题。
