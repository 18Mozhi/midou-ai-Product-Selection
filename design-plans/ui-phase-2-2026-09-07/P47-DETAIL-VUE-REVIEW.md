# P47 采集程序 · 实际 Vue 详情 C 审核

起点：main / fcdcacd5，工作树干净。页面整体结构仍待用户审核；本批继续详情设计，不把待审页面、旧离线稿或 P46 局部批准扩大为 P47 批准。

用户本批答复：“详情分区通过，继续完善交互”。仅签收实际展示的390px两张连续图中的蓝色标题、四组诊断及技术折叠排列；不扩大为760px、长文本/恢复样例、全部按钮状态、页面整体或真实健康验收通过。

## 交付与使用

[47张连续详情图](../../output/playwright/p47-adapters-detail-vue/index.html)，390/760px，高1000px。所有图由实际 App → NavigationShell → ProviderRuntimeSurface → ProviderAdapterCenter → ResponsiveDataView 的原手机入口打开，不是手写 HTML 模拟业务页。

独立 ProviderAdapterDetailPreview.vue 继承上一批页面审核稿，只重组 detail 插槽：来源身份3项、健康探针3项、24小时实际采集4项、暂停与恢复2项，技术详情7项仍折叠。完整 script、事件、条件、六个模型和原插值事实保持；detail 以外的模板与上一版逐字一致。原 probe 按钮放在探针分区，原恢复链接放在恢复分区，未新增桌面详情入口或恢复动作。

新增 provider-adapters-detail-preview.css，仅在审核 body 标记、当前挂载 P47 和含 p47-diagnostic-detail 的 Teleport 抽屉同时存在时生效。蓝色固定标题、白色诊断分区、蓝色检查按钮、灰色检查中和焦点框沿 C 方向；390px全宽，760px右侧最大560px。其他 ResponsiveDataView 消费方及生产导入不变，旧72张页面图不覆盖。

## 事实与测试边界

基于现有 M03-03 两条记录；零成功率/零P95/运行暂停与恢复门为显式合成显示样例，不能当服务端真实恢复条件已满足。长名称、长版本/代码和未检查时间也是合成样例。保持现有时间字符串展示，没有擅改时区或恢复算法。探针通过不代表采集成功，也不会自动解除暂停。

浏览器118项显式检查，47PNG和168份原始加载源指纹。源指纹数量不是页面覆盖数。四种样例分别验证关闭初始焦点、背景inert、12项主事实/7项技术事实、Tab/Shift+Tab循环、关闭/Escape回入口、44px控件和根文档/详情无横向溢出；另验检查中跳过禁用按钮、原条件恢复深链与无body的本地409探针。动态按实际固定标题高度确定连续截图步长，并验证首尾覆盖和片段间无缺口。

浏览器保持原共享详情实现，不替换焦点或隔离逻辑。只检查本批场景，不等于读屏器、200%缩放、软键盘、背景触摸滚动、完整模态或生产无障碍验收。761px起原列表不提供移动详情入口，本批不编造桌面弹窗场景。

本批再次确认：probe拒绝后的adapter-message位于inert背景，而详情没有归属明确的live结果。证据将此作为已知问题记录，不将红/绿样式替代反馈，不把关闭后能看到消息当打开详情时可感知。PR-G03中的并发GET覆盖probe、迟到结果、对象移出筛选和未知请求结果仍未修复，下一步需按真实请求归属完善交互。

## 验证与交付边界

复现：`node scripts/verify-ui-phase2-provider-adapters-detail-vue.mjs --capture`；定向：`node --test tests/unit/ui-phase2-provider-adapters-detail-vue.test.mjs`。复用已安装依赖，无新依赖、生产代码、API、配置、环境变量或数据库变更；OpenAPI/.env.example/后端/Python不适用。当前未部署，不需要重启；不得将审核组件导入生产当作用户已签收。

47PNG、index.html、evidence.json共49个正式交付文件保留。首版固定步长生成的5张多余末段截图已删除，均为本批新建，可运行永久审核脚本重新生成当前图包；不删除任何既有用户图或测试。审核浏览器及Vite由finally关闭。展示的详情分区已获上述有限批准；全部按钮/主题密度/真实权限/健康写入/恢复流程及73页总交付仍待完成。

本批收尾：新增定向合同4/4、相关provider合同71/71、文档153文件/73路由、代码风格检查通过。生产文件和运行配置无变化，不重复生产构建或部署。下一实施项是probe结果按来源归属并在打开的详情内呈现；需要覆盖同来源/换来源/关闭重开/并发刷新，不能直接把全局message复制到任意详情而造成错配。
