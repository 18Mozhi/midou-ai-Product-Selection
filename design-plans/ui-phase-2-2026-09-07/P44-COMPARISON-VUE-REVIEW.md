# P44 角色比较 · 实际 Vue C 组合

用户随后[批准同角色无差异底部结果](P44-MOBILE-NO-DIFFERENCE-APPROVAL.md)，只含0项数量/浅灰提示/无差异说明；角色资料和其他状态不包含。

后续用户已[批准手机比较控件组合](P44-MOBILE-COMPARISON-CONTROLS-APPROVAL.md)，不包含下方资料/权限结果或其他状态。以下待审描述保留制图时范围，不扩为整包通过。

2026-09-11，[手机账号目录已获局部批准](P44-MOBILE-DIRECTORY-APPROVAL.md)后继续其他区域。本批独立保存[44张四宽度图](../../output/playwright/p44-comparison-vue-preview/index.html)及[源/检查清单](../../output/playwright/p44-comparison-vue-preview/evidence.json)，不覆盖已批目录图或将批准转授比较区。

## 设计与保持

使用frontend-design细化C方向：蓝色说明与“只看差异”选择、白色角色/搜索/分组控件，去除权限结果的重复卡片背景，改为有分隔线的逐项事实。手机控件和左右角色事实单列，桌面左右对照；“拥有／无”和角色名保留，不依赖颜色推断权限。

只新增审核CSS，`PlatformRoleComparison.vue`原模板、脚本、默认比较角色、字段、选项、排序、差异与重置算法完全不改。宿主仍为真实P44父组件与之前隔离C容器。复选框视觉尺寸20px，label至少44px；修正原宿主全局最小尺寸叠加导致的大复选框，键盘选择焦点蓝色、蓝区复选框焦点白色、禁用重置中性灰。

## 实际验证

390/760/761/1440，164检查、44PNG、40源指纹；默认6项差异→同角色0项→取消“只看差异”显示3项；搜索空、重置6项、平台治理分组2项，均用原三角色测试样例。每宽度仅账号/角色2个拦截GET，所有选择/搜索/重置不发写入且不改URL。不是真实数据库或权限验收。

焦点采用实际Tab导航。最初立即读样式取到中间帧：DOM已聚焦、focus-visible成立且目标CSS已命中，但减弱动画的微短过渡尚未绘制；已改为等待真实计算样式稳定后截图，不修改权限行为或用新基线掩盖失败。初版一次禁用颜色检查同属过渡帧，修正后重拍。诊断输出已移除，没有额外临时文件。

每宽度11图：默认控件、已选焦点、角色焦点、角色事实、权限结果、同角色无差异、同角色全部、搜索无结果、重置焦点、重置禁用、分组结果。图册只证明所列区域/状态，未覆盖全部按下/悬停/长文本/短屏/主题密度、完整无障碍、完整App/缓存及真实授权；不能标为P44整页完成。

## 复验与交付

```powershell
node scripts/verify-ui-phase2-admin-comparison-preview.mjs
node --test tests/unit/ui-phase2-admin-comparison-preview.test.mjs
# 正式重拍另加 --capture，保持目录批准图不变。
```

生产、API/OpenAPI、路由、环境变量、依赖、数据库、权限和后端/Python/插件不变，无调参、部署或生产重启。图册/44PNG是正式交付物保留；宿主浏览器finally关闭，最终端口和测试结果见本轮收尾。比较区仍待用户审核，全部弹窗、完整C实施与全73页宝塔交付继续。

收尾验证：两包7项定向测试及794项相关UI回归通过，73路由/153必需文档、格式和diff检查通过。生产构建输入未变，复用上一提交的有效构建，不重复构建。全部本轮服务端口56237、56599、56698、56922、57644、57790、57975、58144、58187、58284、58352均无监听；一次性诊断代码已移除，无临时文件遗留。两包共112PNG及图册/清单是正式交付物，保留于各自output/playwright目录。
