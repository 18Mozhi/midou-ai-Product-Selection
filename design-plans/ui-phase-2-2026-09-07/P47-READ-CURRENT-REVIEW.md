# P47 当前 C 页面：读取顺序与旧图证分离

## 范围与依据

本批承接全73页 C 重设计，不改变原待审项或视觉批准。开始状态为main / af239b08，
412项工作区变更、暂存区为空。按UI路由技能选择无障碍与Playwright核查；本地UI CLI缺失，
不安装依赖，复用已有真实App/Vite/Vue浏览器驱动。

旧读取顺序包实际拍摄于f2e8721892a226346d0e6f33ea3a36636f0f4152，完整清单及166个来源
逐一与该提交匹配。它的current指当时修复后版本，不能代替后来的C版与共享控件。
原驱动包含旧缺陷对照，不直接执行它来冒充当前整包，更不覆盖原48张图。

## 改动

- 新增无参数入口 `node scripts/verify-provider-adapter-read-current.mjs`。
- 新适配器固定原驱动LF源码SHA256
  `86e73a7bc81bd71ed787d02ccdb8ce9c22b32e1e4eaceb333858560885848509`；未知驱动修改拒绝。
  仅关闭capture、选择current模式、补761px、输出观察与请求；原场景、断言、网络拦截、
  超时、模板装配和finally清理保持。current分支不转换生产Vue，不注入提案CSS。
- 原单测完整保留当前原始Vue的读取/检查行为执行和旧缺陷反例；只有最后一项错误的
  “旧截图复用当前依赖”改为核对完整历史清单和166个拍摄来源，不再串接C历史转换器。
  当前源码与旧包明确不同；历史来源不被当作当前浏览器证据。
- 三项新增单测验证模式/参数/截图限制、完整场景与清理字节不变、未知驱动修改拒绝。

## 实测

当前实际App在390/760/761/1440四宽度，四种场景共16次通过，100项检查，0截图。
覆盖检查前旧GET成功/失败，以及检查期间发起的GET先于/晚于检查返回；结果行、版本、
提示和手机详情检查反馈保持归属，新GET仍能恢复到新版本，关闭详情回到原来源按钮。
所有原生点击/键盘断言保持；不是通过手动修改页面状态跳过旧读取。

四宽度共64个样例GET、16个样例POST，全部无body并由本地拦截响应；未知请求拒绝，
无浏览器错误、真实探针、线上写入或数据库操作。驱动集合记录168个来源路径；它不是
新的截图源哈希包，不声称覆盖全部主题、动态偏好、未挂载代码或全部样式依赖。
本批只覆盖默认/reduced-motion；卸载、缓存退出、未知写结果和真实权限由独立门负责。

两个完整相关单测文件17/17通过、0取消/跳过（29.46秒）。没有改生产、公共验证库、
依赖或其他页面输入，因此不重复全库单测；上一轮1403/1239/164仅是历史快照，
不推算最新剩余数量，不把局部通过视为整站发布通过。

## 运行与收尾

从仓库根目录执行上述新命令；不接受参数，不能用它覆盖历史图。无新环境变量、API、
OpenAPI、配置、数据库、权限或依赖变化，无生产重启要求。原驱动、旧转换器、图片与批准
保持不变。本地浏览器/宿主已退出，61129无监听。没有新临时文件、截图、日志或夹具，
新增脚本、测试和本记录为永久交付。文档门（73路由/60受保护/6角色/153必需文档）、
运行文档门、格式门（127变更代码文件/535生产文件/51仓储检查）和diff空白检查通过；
收尾工作区417项，暂存区仍为空。格式后新增适配器三项单测复核通过。

整体验收未通过，不提交、不部署，commit hash不适用；继续P47检查反馈等剩余当前交互证据。
此前两处策略阻止清理的目录保持待手动处理，本批不绕过重试：

- `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p47-c-e2e-temp`
- `C:\Users\23136\AppData\Local\Temp\scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`
