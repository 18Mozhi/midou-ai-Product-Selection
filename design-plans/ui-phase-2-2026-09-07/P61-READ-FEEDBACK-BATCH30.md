# P61 首次与保留快照读取反馈 · 批次30（未提交）

提交状态续记：[批次31](P61-VERIFICATION-CLOSURE-BATCH31.md)已解除既有首页测试阻碍，35项组合验证通过；以下未提交和失败描述为本次捕获时历史，不再作为当前阻碍。手机首次超时区域仍待批准，未部署。

沿全73页C方向实施计划继续，上一批实际Vue图册是进展，待审不视为批准。本轮只修正P61读取事实表达，并出双端读取区域图；完整布局、全部状态、真实权限与生产验收仍未完成。

## 实际修正

基线 main / 54438ad7，开始时538项工作区变化、暂存区空，两个生产目标文件相对HEAD均无修改。源码证实首次15秒超时无条件声称保留成功数据，且首次失败同时显示message和错误区。先新增8项测试，5通过/3失败，明确复现两个超时文案及区域语义缺口，再修正实际实现。

`use-platform-status.ts`仅改一处超时文案条件：有status快照才说“已保留上次成功数据”，首次没有则说“尚未取得系统状态数据”；均描述“停止本次等待”，不承诺后台操作被取消。原15000ms、AbortController、sequence、GET、客户端重试和缓存归属不变。

`PlatformManagementCenter.vue`只调整status条件下的首次错误呈现：非ready时不再重复显示独立message；错误区加入依原refreshing的aria-busy、命名关联及系统状态标题。其他domain仍沿原message条件和h3文案。没有改变服务健康分类、权限、后台事务或增加加载百分比。

按fixing-accessibility保留原生重试按钮与已有反馈区域，不引入自定义键盘按钮。C审核CSS把初始状态区改为左对齐、蓝色无衬线标题、就近操作，仍然只被审核宿主加载，未作为生产样式接入。新稿：[读取反馈r2](../../output/playwright/p61-read-feedback-r2/index.html)，手机首次超时区域申请局部审核。

## 验证与范围

8项新定向测试核对有无快照的超时/失败、原错误与对象身份、单飞、成功重试、停止后的旧响应不可覆盖新读取、非status不请求、实际Vue编译/条件语义。加原6项审核合同与系统状态合同共15项通过。

实际Vue浏览器验证四组：1440×1000、390×844，各reduce/no-preference，共128项、40次浏览器拦截GET。每组含首次503（原3次尝试）→首次重试15秒超时→成功恢复→有快照503（3次尝试）→保留快照15秒超时→成功恢复。两种超时实际等待既有15秒，没有替换或推进浏览器计时器。每组10次GET，禁外部/未知API，无真实后台读取或写入。

核对首次提示仅一次、忙碌标记、无凭空快照、忙时不出现第二重试按钮、超时后原生重试键盘Enter恢复、保留原2项警告、管理刷新不覆盖浏览器会话指标、无横向溢出/页面脚本错误。此键盘检查不覆盖“刷新数据”原生disabled时的全部焦点连续性，也未做完整读屏、真机、晚响应或真实权限注入。

无产物验证128项通过；初次出图r1也128项通过，但视觉复核仍见旧居中衬线标题。仅审核CSS修正后重新生成r2，旧r1保留为设计迭代对照，不冒充当前图。两份均各10张区域PNG/191来源，reduce出图，normal只检查逻辑；不修改像素。每次捕获包含当前HEAD加工作树，并非已提交或生产版本。

原默认组合回归98项通过；`npm run build:web`包含Vue类型检查且通过，`verify:frontend-budget`通过253资产。上轮扩大M06-02回归的管理首页正则断言失败仍是已知未处理项，其首页及测试文件未变，本轮不重复运行不受影响的同一失败、不改无关断言。按“验证失败不自动提交”约束继续不提交，commit hash不适用；不能据定向通过称全模块全绿。

## 历史证据与交付

批次29默认组合r3的24图不变；本批实际组件、状态加载器和审核CSS演进，188/191来源仍匹配。该包保留原manifest，旧“首次超时待修”的文字仅描述当时状态，不覆盖本次修正。默认组合r2的22图不变，187/191来源匹配，额外差异来自上批默认验证入口。读取r1仅审核CSS有1项来源演进，其10图不变。所有旧图不重绑来源，也不扩大批准范围。

新命令 `node scripts/verify-status-read-feedback.mjs` 默认不写文件；`--capture-review rN` 独占新目录且拒绝非法参数或已有版本。后续改稿使用r3及更新版本。样式调节只在P61审核CSS内；本轮无新增业务可调参数或环境配置。

API/OpenAPI、数据库/迁移、后端/Worker/Python/插件、权限/密钥、依赖、超时与重试均未改变，不适用跨端契约或环境更新。生产文案修正尚未发布，审核布局仍未接入生产；将来按固定宝塔流程发布Web运行包后浏览器刷新生效，本次不部署或重启。

本轮未新增一次性临时文件；r1/r2为正式设计迭代材料，构建输出是项目运行所需材料，不清理。上轮`output/playwright/p61-page-composition-r1`四张诊断图仍因策略拒绝未清理，本轮未重试；未触碰其他旧受阻目录。新增验证宿主/浏览器均由finally关闭，无永久监听服务。

收尾核对：r2的191来源和10图SHA-256全部一致；r1仅上述审核CSS1项漂移、10图保持。端口59598、59678、59681、59827均无监听；格式、文档/73路由/153必需文件及运行文档检查通过。工作区543项，暂存区仍为空；未改动既有失败的首页/测试文件，未混入提交。手机首次超时白色区域已送审，等待用户确认，不将延续执行视为批准。
