# P16 采纳规则决定：统一 P18 五项质量门

2026-09-09，用户对“P16 是否统一遵守 P18 五项质量门”明确回复：“统一五项质量门”。

## 已授权范围

P16不再以“所选证据有topic_id且采集任务终态”作为充分采纳条件。实际实施复用P18的opportunity-selection-policy：机会处于pending、命中已启用监控规则并达到其来源门槛，且评分/市场/竞争/成本/风险五门全部通过，selection_stage=recommended才可采纳。前端说明/可用性和后端事务内最终校验必须一致。

缺少已评估机会或任何门未通过时拒绝采纳，不生成虚假分数、不自动补齐权重或资质、不把采纳偷偷替换为创建pending候选；允许的观察/驳回仍沿原合同。已采纳历史不自动撤销或回填。保持原始证据、评分/利润输入和历史快照，不因旅程采纳改成partial覆盖。

维持任务终态、当前旅程证据/主题和组织工作区归属检查、原有鉴权/幂等/审计与验证任务流程。不得通过调整180000ms期限、扩大权限或数据库迁移绕过规则。仅实现这项用户选择所必需的内部读模型/错误说明/调用方/测试/文档配套；不意味着所有新视觉稿已批准或现在允许发布未验版本。

## 实施与验证

以当前P18事务锁与evaluateOpportunitySelection为依据复用同一计算，拒绝无候选/缺门/过期状态/跨范围，允许同一对象真实门通过；并验证并发变化在提交时被重新判断。前端不能单凭旧读取或topic_id决定最终成功，错误保留原因并说明补齐质量门后重试。

新增最小定向测试覆盖五项逐一失败、规则门/非pending、无已有机会、全通过和原观察/驳回；然后验证相关Vue/API/OpenAPI与源码证据，最后提交。没有生产部署或历史数据更改；实施结果、剩余真实数据库/运行证据与重启要求以后续PROGRESS为准。

## 本地实现结果（2026-09-09）

已从独立审核提交 `a8cbf10f` 开始实施：候选 DTO 新增 `opportunity_id`、`selection_stage`、`quality_gates`；读取与事务内采纳共用 P18 判定。不存在合格既有机会时返回409且无业务写入；合格采纳保留评估事实并更新生命周期进入时间。前端逐门提示、禁用与提交函数双重保护，服务端冲突保留原因；保存成功清除先前错误态。

定向 Node 测试30项通过（含9类拒绝、合格写入、观察/驳回、历史幂等重放及读取投影）；真实 Vue 隔离响应回归桌面24项、390px 24项通过。浏览器覆盖逐门缺失、旧汇总标志、切换候选后的原采纳选择、服务端409后刷新及继续观察。API构建通过；其他收尾结果见 [PROGRESS](PROGRESS.md)。这些不是真实 MySQL 并发测试、真实采集、生产部署或视觉签收。

`action-reviews/P16.json`、合同第8节与 JOURNEY-C-r1 源绑定留作 `a8cbf10f` 改前快照；本次不改旧哈希来假装重验，不刷新旧PNG。相关完整图稿、动作分母及新规则成功/缺门状态需后续重新生成并交用户审核，全站G0–G5不提升。当前实现只落实用户选定的规则，不是全页重设计完成。
