# P47 已批准手机空态实施

## 授权与改动

从main/af239b08、434项工作区变更、空暂存继续。用户明确批准展示的390px两张空态
区域：浅灰区域、标题说明、蓝色操作按钮。只实施这两个手机区域，不把批准扩展到
桌面/整页/焦点/真实登记或生产验收。使用frontend-design落实原C方向，无障碍技能
检查响应式隐藏文案不重复出现在可访问名称中；本地UI CLI缺失，复用已有依赖。

`ProviderAdapterCenter.vue`只新增一个局部CSS导入、两处区域class及四处响应式文案。
沿用既有760px边界：手机显示原批准文案，宽屏保留原文案；display:none隐藏另一套
文字，不增加按钮/链接/ARIA/监听器。两处焦点函数、load/probe/reset、所有事件/条件/
字段/数据/排序/分页/接口/权限保持原文。旧scoped样式块不改。新增
`apps/web/src/provider-adapters-empty-mobile.css`只匹配当前P47的两处空态；除隐藏手机
文案的基础规则，其余均在max-width:760px。五组视觉声明解析颜色后与原审核CSS逐条相同。
全库发现直接色值违反项目语义颜色规范后，五色接入已有P47色板，原37项及激活选择器
保持不变，新增五项empty角色；不新增色板激活范围，不改全局主题或其他页面颜色。
新增单测移除精确五行后必须还原旧色板完整SHA，变量解析后与原五组声明严格等价。

独立逆向辅助函数只用于测试对照，移除七处精确改动后，完整源码必须还原
2f71a84bc29b416ce9de84732993fa488ae856ac508d22297bdab8f7278b6e5f。
实际新源码为51c0ba1f86fa1179fcb0d25b9cb327ef8a38f34b0cef53d5a916f0ef2f92a2ee。
未知文案/焦点函数/其他源码变更拒绝；没有放宽现有历史转换器或全站图证门禁。

## 实际Vue与图片

新入口`node scripts/verify-provider-adapter-empty-mobile.mjs --capture`使用原实际App、
原M03导航/目录样例及明确空数组，固定原驱动SHA，不改原脚本/审核CSS/旧图片。
三种模式：baseline精确实施前、review原空态审核参考、current未注入替换的实际组件。
390/760/761/1440 × 空目录/搜索无结果/组合无结果，36组576检查、96PNG、179原始来源
及CSS导入、32组逐文件SHA一致比较。当前手机空态比原参考，其他图片比baseline：
包括宽屏空态、非空目录、清除后的筛选区、登记链接到达的P46。新390空目录/搜索
空结果两图还与最初获批的原审核PNG完全相同；原审核完整清单SHA固定。

可见标题/帮助不重复、标题可访问名称正确、隐藏文案display:none；操作至少44px、
无横向溢出；六模型恢复，空态重置回搜索、顶部重置保留自身焦点，登记只到原P46。
每组仅一次适配器GET，共84次本地GET，无body/POST/真实登记或探针。
未覆盖读屏器实机、所有主题、极短屏、真实权限/数据、完整页或生产验收。

交付目录`output/playwright/p47-empty-mobile-implementation`含96PNG、evidence.json、
index.html。不带capture只执行交互，不写图，也不执行图片之间的文件比较；图片门禁
必须使用capture后执行对应单测，不能把无图运行写成32组图片比较通过。

## 验证过程

两项最小源码/CSS测试先通过。第一次截图比较发现390组合重置后固定顶部栏进入截图
上沿，因不同原生滚动位置导致取景不同。为所有模式统一截图前滚动位置，不改页面
样式/业务，不遮罩图片，不放宽SHA标准。第二次整包仍有两个微小差异：761空目录
按钮边缘1像素/最大色阶1，1440组合重置32像素/最大色阶2。未据此放行或改变容差；
代码和断言不变做一次完整复测，最终全部32组SHA一致。前两轮不称为通过；本批目录
中的中间产物已由同目录最终96图替代，没有覆盖原有用户图包。

最终新增单测3/3通过：完整源码逆向与未知变更拒绝、96图/179来源/32比较及两张原批准
图绑定、CSS手机范围与五组原声明逐字等价。`npm run build:web`通过，vue-tsc与Vite441
模块、15.65秒；资源预算通过252assets。本批5个代码文件格式检查通过。
首次全库247文件：1423项1239通过/184失败、0取消/跳过，335171.5723ms，exit1。
此次输出中的失败名称被截断，未宣称保存了完整清单。新增直接色值检查失败已定位并
按项目惯例修复，主题/色板两文件14项定向测试通过，原主题验证器未修改。颜色变量化后
重新完整捕获36组576检查/96图/32比较通过，新色板SHA为
ec274e4382f9b741d26d79f5cc8d5dd5b1602967000cb227320fca0e018fe214。
最终三个单测文件17/17通过，build:web再次通过441模块/13.50秒，252资产预算通过。
七个相关代码文件格式通过；文档153文件/73路由/60受保护/6角色、运行说明一致性和
git diff --check通过。最终同247文件全库：1423项1240通过/183失败、0取消/跳过，
325653.935ms，exit1。完整183个失败名称保存于`P47-EMPTY-MOBILE-UNIT-RESULT.json`。
与上次完整1418/174失败快照相比，失败名称新增11/移除2，其中包含期间测试重命名，
不能直接当作新增产品缺陷数。已确认颜色规范失败消除；旧源码/图证及提案组合门禁
仍未全部接续，没有豁免、降低验证或声称全站完成，因此不提交部署。

## 运维、清理与剩余范围

无API/OpenAPI、数据库/迁移、权限、依赖、环境变量或配置变化；后端/Python无对应改动。
没有新增用户调节项。未来正常宝塔前端发布后刷新浏览器生效，无独立后端重启需求；
现有部署器的验收不得跳过。本批尚未提交或部署，commit hash不适用。

96张图及索引/清单、辅助函数、永久测试是交付物。正常build:web输出和浏览器助手归档
按项目运行需求保留。本轮没有新临时脚本/截图夹具/日志；四轮浏览器宿主端口
53219/53357/53471、53755/53861/53965、54225/54343/54450、55467/55614/55720
已核对无监听，驱动进程已退出。首次全库父42560/子71152已退出，其自有临时夹具
`C:/Users/23136/AppData/Local/Temp/scoutops-code-style-XoE2bn`已不存在。
最终全库父64696/子52328及其已跟踪子进程退出，夹具
`C:/Users/23136/AppData/Local/Temp/scoutops-code-style-URUAq8`已不存在。
收尾440项工作区变更、暂存为空；没有暂存或覆盖无关变更。
旧两处策略阻止清理的目录保留，不绕过重试：
`output/playwright/p47-c-e2e-temp`和
`C:/Users/23136/AppData/Local/Temp/scoutops-p44-p46-replay-37f4e02d2ecd4dc5bf27ef9bffe270dd`。

其他状态、全73页与全库图证接续继续开放；旧图包的“current”名称仅代表其拍摄时点，
本次新源码不能直接重用它们的当前源码通过结论。上一轮刷新等待焦点画面和跨页锁等
待定问题不替用户作决定，不重复提出。已经批准且像素一致的两张手机空态无需重审。
