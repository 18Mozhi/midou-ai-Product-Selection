# P65 实际 Vue C 组合 · 批次49

起点 main/6025c198、572项在途变化、索引为空。上一批P64离页诊断取得新证据，属于有效进展；P64离页策略与P62导出离页策略仍待明确决定，本批没有以自动继续替代授权，转向独立的P65实现核对。

## 范围与设计依据

按AGENTS→Feature Map→总纲M07-05固定单后端合同→真实路由/组件→P65既有C提案核对。使用ui-skills-root选择frontend-design，复用锁定C方向的蓝色阅读目录、白色事实区和中文/版本等宽字体；没有重新选风格或引入新依赖。浏览器验证遵循Playwright技能的实页观察、交互和图像核对，沿用现有本地脚本与安装依赖。

原页面的七卡与三绿环在审核模板中移除，改为运行身份与部署捕获、当前构建匹配记录、历史观察策略与六列指标、动作计时与最近一条历史记录。三个页内目录为原生锚点，不是步骤或发布流程。运行/本地/远端SHA完整换行；配置指纹使用原生折叠。提示来源字段可能回退，返回文本相等不代表独立核验；移除根据两个阻断码缺失推导“版本一致”的审核显示。保留服务state，不重新计算资格。

迁移与回滚各自展示原duration函数的结果，明确0 ms与尚无记录；返回的automatic_stop_verified和rollback_verified分别展示，不从门状态推导。匹配当前构建的latest_release和latest_historical_release分区，后者仅一条记录、不称完整历史列表。刷新文案中的历史观察说明与固定宝塔部署边界对齐，不新增发布、停止、回滚、迁移、签名探针或版本复制动作。

## 实际接入边界

`scripts/lib/release-page-preview.mjs` 只在本地Vite审核插件中转换实际ReleaseRolloutCenter模板与壳层；原script字节等价（换行归一）、权限条件、请求方法、15秒/单飞/重试/状态处理不变。原ResponsiveDataView及六列表格、手机六字段和技术详情插槽逐字保留。生产Vue、共享组件、原样式、API、后端、SQL、审计、Worker、Python、插件、.env与依赖未改。

新布局与样式是实际Vue待审实现，不是已上线页面。真实来源、权限、读取审计、保活/迟到结果、追踪归属、所有控件/主题/密度/断点、空指标展示和全页批准仍未验收；未调用write-probe或历史双槽runner。没有运行合同变化，OpenAPI/环境样例同步与重启不适用。

## 验证与修正

最小验证6项新合同先通过；扩大至 `node --test tests/unit/release-page-preview.test.mjs tests/m07-05/release-rollout.test.mjs` 共23项通过。历史M07-05部分为静态合同和惰性依赖检查，不冒充真实MySQL、探针或生产发布。

`node scripts/verify-release-page-preview.mjs --capture-review r2`：1440/390、减少动效，桌面33项、手机69项，共102项，12个本地P65 GET。包括完整版本/配置披露、单H1、移除旧结构、C字体/蓝色目录、三锚点、普通运维不可见超管入口、三观察门详情六字段/技术ID/Tab循环/Escape返焦、独立证据布尔、不同最近历史、0/未知计时、空记录及503保留。每端3次成功和原3次503重试，所有业务API本地拦截，无外部/写请求、下载或浏览器异常。

取景检查在不输出文件时亦执行，对区域的可见标题、字段和控件检查视口边界/中心命中。手机初轮发现版本区宽度被压至18px：审核CSS基础选择器含:has，断点选择器优先级不足，桌面列仍生效；将两处断点限定为同等作用域后修复，补单列及来源区至少200px断言。没有用强制点击、隐藏导航或跳过遮挡来通过。脚本另纠正了两处测试定位假设（空态原文“暂无记录”、详情外有容器），不将它们记为产品故障。所有失败运行均未出图。

## 当前图册与收尾

当前 `output/playwright/p65-page-composition-r2`：36PNG、165来源，桌面14、手机22；含默认布局、目录、三个身份区、匹配与历史、配置展开、动作计时、0/未知、不同历史、空结论、保留失败，手机另有三门详情的蓝色标题/字段/技术三部分。index.html与manifest.json记录文件、像素、SHA-256、来源和检查范围。r1的33图为已完成的先前捕获对照（缺三张详情标题）；不覆盖旧manifest，不称r1绑定当前验证器。

底层基线来自原E2E：state=verified同时存在rolled_back门而rollback_verified=false，最新历史字段不完整。明确保留其矛盾，仅用于布局，不把绿色结论称作有效生产成功链。0/未知耗时、不同历史与empty是明确本地变体，不是服务生成的资格判定样例。手机版运行身份/配置区域已展示待审，不扩大为整页批准。

用原“刷新发布事实”、页内目录、技术折叠和手机记录详情入口查看；重现命令默认不出文件，出图需新的rN且拒绝覆盖。没有临时文件；正式脚本、合同测试、图册与文档保留。临时Vite/浏览器由finally关闭，端口复核无监听；未部署、迁移或重启。

既有API覆盖失败的输入仍同HEAD，处理决定未到；不改计数、不重复相同失败、不暂存或提交，commit hash不适用。完整73页目标、各局部审核及生产签收继续保留。
