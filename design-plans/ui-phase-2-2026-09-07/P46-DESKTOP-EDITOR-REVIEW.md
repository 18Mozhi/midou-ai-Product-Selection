# P46 桌面编辑窗 C 方向实际 Vue 审核

2026-09-11，基线 main / ba92cae2。接续“整体结构通过，继续完善交互”，此前批准仅覆盖所展示页面结构及手机编辑窗，不能视为桌面编辑窗已获批。本批为桌面补图和交互回放，待审核，不接入生产样式。

## 改动与边界

采用 frontend-design 技能，将既有 C 方向蓝白分区整理为桌面左侧步骤栏、右侧字段、底部操作。新审核 CSS 只在独立 body 标记且宽度至少761px时生效；761–1023px单列字段，1024px起双列，长字段跨列。窗口高度随内容伸缩并保留视口上限，长内容可滚动。390/760px仍直接使用已批准生产手机样式。

真实 index/main/router/App/NavigationShell/KeepAlive/ProviderRuntimeSurface/Registry 均不替换。仅本地审核宿主添加 CSS 链接及 body 标记，基线与新稿都加载同一份当前 Vue。Registry LF SHA256仍为 `b217ac9898675371d6a6b0406a2574784a7321fac93c233347299991289b0f8e`。没有生产代码、业务规则、23字段、模板、校验、API、权限、数据库、配置、依赖或日期转换变更；后端/Python/插件/OpenAPI/.env.example不适用，无重启或部署。

## 图包与验证

[当前75图](../../output/playwright/p46-desktop-editor/current/index.html) · [生产样式基线75图](../../output/playwright/p46-desktop-editor/baseline/index.html)

五个宽度390/760/761/1024/1440，高1000px。每版75PNG：四步字段错误、修正后、正常已填，以及前三步首个错误字段焦点局部。150PNG都是本次正式审核交付，不覆盖原有已批准图片。

- 每版345条逐字段观察：23字段×初始/错误/修正×5宽度。名称、错误关联、输入值和无障碍快照前后相同。
- 基线955、新稿967项显式检查；166个原始加载源指纹各自绑定，不是页面覆盖数。新稿额外核对桌面三宽度四步的单/双列。
- 原“下一步”校验仍阻止错误步骤继续；全部修正后提交按钮可用，但不点击提交。正常填写状态的字段Tab仍在窗内，窗口边界在视口内，Escape关闭后回新建入口。
- 每版10次GET，前后请求序列一致，全部来自既有测试样例并在本地拦截；意外请求一律中止，没有真实保存或权限验收。
- 30对手机截图中17对字节完全一致，13对存在14–101个像素、最大2色阶差异，主要在边框和背景边缘。单元测试精确绑定每张差异数量及坐标范围，不使用宽泛百分比豁免，不声称手机图全像素一致；所有审核CSS规则的桌面作用域另以CSS语法树检查。

## 保留的现有交互问题

修正字段后，字段错误已清除，但先前点击“下一步”生成的“当前步骤还有N项需要修正”汇总仍暂时保留。`*-repaired.png`如实保存这一现象，不算修复通过。随后通过原有成功的“下一步”清除汇总，再返回四个步骤拍摄`*-ready.png`，没有修改DOM或Vue状态伪造正常状态。

该旧汇总更新、首错误定位/必填宣告、完整模态/读屏器/软键盘、未知保存结果、原日期/只读字段问题及全73页验收仍未完成。仅展示1440px执行策略和合规发布两张组合征求本次批准；其他步骤与状态不自动获得批准。

## 复现与交付

`node scripts/verify-ui-phase2-provider-desktop-editor.mjs --capture`运行当前稿；加`--baseline`运行现有生产样式对照。始终在本地临时端口运行并在finally关闭浏览器/Vite，不对外请求或修改生产。正式HTML/JSON和PNG保留，无一次性测试文件；运行端口关闭另在收尾核对。

定向命令：`node --test tests/unit/ui-phase2-provider-desktop-editor.test.mjs`。本轮未改生产文件/依赖/配置，沿用上一批有效的类型检查与构建，不重复构建；本轮补跑P46相关回归、文档和代码样式门。没有新增用户可调配置，审核请先看当前图包，再按范围确认。

最终门禁：新增3项定向合同通过，P46相关57/57回归通过，文档门153文件与73路由检查通过，代码样式门通过，git diff --check通过。六个曾使用的本地端口52374/52487/52734/52784/53085/53120均已关闭，未残留本次Node宿主；浏览器由finally关闭。正式154个图包文件保留，没有需要删除的一次性文件。仍待用户本批局部视觉审核，不能把本批交付描述为整页或全站完成。
