# P35 组织数据 · 按钮与操作控件 C 方向细化

状态：待用户审核，非真实 Vue 或生产截图。本批基于 main/e2eef28a，生产源及原94张布局图不改。

## 审核入口

[192图控件图册](../../output/playwright/p35-controls-review/index.html) · [可操作原型](design/org-data-controls/index.html) · [逐项来源与验证](../../output/playwright/p35-controls-review/evidence.json)。

使用 ui-skills-root / frontend-design 沿用已选 C 方向，进一步区分蓝色键盘焦点、灰色禁用、深蓝主操作按下及视图选中下划线；技术详情文字从14px提高为16px。原94图保持不变，新增样式仅在独立审核入口加载，不修改其他页面。Playwright 使用仓库已有依赖与隔离 file 页面，不安装 CLI 或启动服务。

## 全部动作与状态

| 组 | 本批覆盖 | 不扩大为 |
| --- | --- | --- |
| 父刷新 | 默认、悬停、焦点、按下、后台忙碌；首次读取禁用 | 真实后端读取成功、重试策略修改 |
| 父重新加载 | error/forbidden/expired/rate_limited 各四态 | 全部父异常、真实权限验收 |
| 两个视图 | 各已选/未选×四态 | 新路由或切换会话工作区 |
| 两组重置 | 四态；仅恢复当前视图条件 | 清除另一视图条件或记录 |
| 两组分页 | 上一页/下一页四态与边界禁用；8/10分页 | 服务端游标、写入业务数据 |
| 技术详情 | 展开/收起×四态；键盘Space切换 | 下载、秘密详情、业务弹窗 |
| 报表入口 | 四态；仍指向 /reports | 自动生成或下载文件 |
| 新增提案 | 两组空结果清除、手机两组折叠/展开筛选，共6变体 | 当前生产已有控件或用户已批准 |

共25控件/变体，其中21项双端、4项仅手机。10个真实子组件动作位置逐一映射，原生summary算一个重复渲染源位置，不把每条履历复制成新业务动作。188张局部状态图、2张工作区整页与2张完整导出详情组合图。390px悬停/按下采用鼠标验证，是窄视口鼠标态，不声称手机触屏存在hover；截图保留焦点框周围空间。

禁用和忙碌只展示源逻辑确有的条件。重置、视图、技术详情、报表链接没有异步提交，不虚构禁用/忙碌槽。没有业务弹窗；原型的“控件审核工具”不属于业务功能。8个筛选字段的原生选项、输入边界、全部组合和其他父异常尚未在本批完成。

## 验证依据与边界

真实 OrganizationDataPanel 的10个动作通过源码扫描并核对处理器、原路由、分页禁用条件；用已有 m06-01 数据驱动的 C 原型逐个点击。源匹配是语义对应，不是挂载真实 Vue。原型演示零HTTP、零外部请求、零存储和业务写入。新入口的限流文字去除旧稿“不自动重试”承诺，改为“本轮读取未成功，请稍后重新加载。”；未改变真实客户端策略，未覆盖旧图。

188项原生状态检查涵盖44px目标、16px控件文字、3px键盘焦点、命中与无横向溢出、选中/展开和禁用无副作用；44项点击检查涵盖精确双GET意图、当前视图重置、两组分页、技术详情键盘切换、仅/reports导航及数据不变。四项单测核对源码映射、状态/视口/图片一一对应、提案边界与来源/图片哈希。

首次完整采集发现截图坐标错误（把文档滚动偏移重复加入视口裁剪），已修正为视口坐标并重跑定向检查；首次失败的同名图由成功全量采集覆盖，单测核对输出中没有额外PNG。不是产品布局或Chrome连接故障。

```powershell
node scripts/verify-ui-phase2-org-data-controls.mjs --smoke
node scripts/verify-ui-phase2-org-data-controls.mjs --capture
node scripts/verify-ui-phase2-org-data-controls.mjs
node --test tests/unit/ui-phase2-org-data-controls.test.mjs
```

默认命令重验来源/图片并重新执行双端交互；--capture更新独立图册，不授予批准。修改样式用 design/org-data-controls/controls.css，修改状态编目用 controls.js。图片/evidence/index属于永久审核交付；无临时服务器，浏览器context与进程在finally关闭。

## 实施与部署未完成项

本批只细化P35控件设计，不改生产Vue/CSS、API/OpenAPI、权限、SQL、URL参数、env、依赖或部署配置；后端/Worker/Python无相关消费方变化，无当前重启要求，未部署。P34温和文案目前只有措辞批准，现有生产结构缺少对应四槽，本批未擅自套用未批准布局；既有登录失效/服务不可用等审核问题仍保留。

P35整页、字段组合、主题/密度/角色、原生200%缩放、真实Vue/完整URL历史/后台及生命周期验证仍未完成，全部73页真实C重构与宝塔发布目标保持不变。请审核控件配色、焦点、禁用和技术详情组合；此项通过也不代表整页或生产验收。

## 收尾复核

192图采集后默认模式同源复验188状态/44点击通过；原P35的47场景双端、11排序来源、94原图复验通过。图册1440/390实际打开，188个控件图片入口与所有链接可读取，场景选择器定位导出末页禁用下一页，无横向溢出/页面错误。文档153文件/73路由、静态391文件、运行说明与本批格式检查通过。全站原102包15069图源图零漂移；P35规格新增审核链接使输入哈希过期，审查差异后仅更新该哈希及统一入口的依赖哈希，不提升完成度或把本批192图冒充旧审计计数。本批192图由独立证据与四项单测负责。

本轮输出精确194文件（192PNG、图册、evidence），均为请求的永久审核交付。没有临时脚本、下载或服务遗留；验证浏览器均已关闭。生产构建源未变，不重复产品构建/发布或真实API测试。

最后执行 tests/unit/ui-phase2*.test.mjs 全部第二阶段单测（dot报告、退出码0）通过；设计审计与统一入口依赖默认检查再次通过。Git提交只包含本批原型/脚本/测试/说明、192图和上述两个输入哈希更新，不暂存其他来源文件。
