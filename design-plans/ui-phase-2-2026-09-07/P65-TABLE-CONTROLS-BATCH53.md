# P65 列设置、密度与冻结 · 批次53

起点 main/6025c198、592项在途变化、索引为空。上一批25组服务结论与实际Vue证据属于进展。本批继续P65控件，未将自动继续视为视觉批准或生产验收，完整73页目标保持。

## 依据与本次范围

已读 AGENTS、Feature Map releaseRollout 路由、P65规格及总纲M07-05固定单后端合同，追到 ReleaseRolloutCenter → ResponsiveDataView → TableViewControls。控件现有规则为至少显示一列、冻结首个可见列、标准/紧凑密度；变化仅是本地呈现，不存在偏好写接口。

采用 fixing-accessibility 技能的原生标签、键盘可达和可见焦点要求。只在P65 C审核转换器中把列名span替换成关联input的label，保留原input的名称、说明ID、checked、disabled和change；增加“至少保留一列”说明，不改变允许隐藏的规则。共享生产组件和业务script完全不变，编译与字节等值合同通过。

审核CSS改为白底菜单、20px勾选图形与44px整行标签；最后一个已选列保持原生禁用，文字淡化。P65原C表格样式将紧凑模式覆盖为12px，实页六组均复现；新增仅匹配compact的6px上下间距，恢复共享组件既有两密度效果。焦点规则受审核外壳`:has(#app)`的较高优先级覆盖，新增仅P65表格工具栏的同级选择器，使勾选、列设置、冻结和密度焦点统一为#1249b8，不改变外壳或其他页面。

## 验证经过

- 初次实际App四宽双动效：手机原详情正常，六组桌面在整行native label断言失败。修正标签后，紧凑模式实际12px而非6px被确认；修正CSS后恢复6px。
- 取样须等Vue渲染与样式稳定。保留8秒有界等待验证真实6px和当前输入焦点/蓝色outline，不以固定sleep代替条件，也不修改待测DOM样式。首次出图r1因取景后的焦点采样未稳定，六组中两组未通过；不将部分捕获列为最终图册。
- 最终 `node scripts/verify-release-table-controls.mjs --capture-review r2`：1440/1024/768/390 × 减少/正常动效，共358项检查、14个本地GET。每桌面组58项/2次GET；手机组5项/1次GET。所有数据来自当前服务计算的verified内存样例，不使用原矛盾E2E判定。
- 核对六列实际名称和描述关联、20px勾选/44pxlabel、Space隐藏首列及冻结跟随、点击列名仅切换一次、最后一列已选且禁用、恢复其他列后重新启用、Enter开关原生披露、6/12px实际密度、冻结aria-pressed/横向滚动后首列可见、完整门ID/类型/发布ID。隐藏一列+紧凑+取消冻结后刷新仍保留选择，随后可恢复；仅明确刷新产生额外读取，没有写、外部请求、下载或浏览器异常。
- 菜单、说明、标签、勾选的边界和中心命中，以及技术详情全部字段可见检查在不出图/正常动效也执行；截图前等待字体与双帧。手机无桌面工具栏、三条详情入口和无页面横溢出；本批不声称重新覆盖手机完整详情生命周期。
- 10项P65实际模板/样式合同通过，扩大状态、指标、焦点、追踪与M07-05至72项通过。默认页面102项/12GET及服务状态矩阵2674项/100GET回归通过，本轮最终浏览器合计3134项/126GET。

## 审核图与使用

`output/playwright/p65-table-controls-r2` 共25张图/167来源指纹：三种桌面宽度各8图（工具栏、全选、首列隐藏、仅最后一列、紧凑、取消冻结、冻结横滚、技术信息），手机1张历史观察区域。PNG为减少动效组；正常动效同样验证。

已展示768px全选和最后一列两张菜单区域，请求只审核六列名称、说明、勾选与禁用的组合；不包含整个页面、其他控件、真机触控或真实发布权限。未回复即待审，之前P65结论和指标图批准范围不变。

默认命令不出文件；新图使用未存在的`--capture-review rN`，禁止覆盖目录。表格密度在原下拉框选择，列设置可用Enter展开、Tab和Space操作；至少保留一列，冻结跟随首个可见列。只有审核插件载入时能看到本轮呈现，生产尚未采用C布局。旧P65图的转换器/CSS来源已漂移，保留原manifest，不把旧捕获重新绑定当前代码。

## 收尾与未完成

未改生产Vue/共享组件、服务/规则、API/OpenAPI、数据库、权限、配置/env、依赖、离页策略、发布或探针；运行合同联动不适用。原前端生产构建与预算不因审核专用代码改变而失效，本批不重复构建、不部署、不重启。

新r1失败捕获目录为验证专用临时产物，共13PNG，无完整manifest。已核对确切路径 `D:\项目工程文件\vue\curson\工具\智能选品\output\playwright\p65-table-controls-r1` 后尝试PowerShell删除，但工具策略在命令执行前拒绝；未删除、未绕过、仍保留。正式r2图、验证器与测试是交付物，保留。前批p65-read-states-r1既有清理阻碍未处理。全部本次Vite/浏览器按finally关闭并复核；没有交接常驻服务。

既有API覆盖225/258与旧223/256断言问题未获处理决定，相关输入未改；未重复该失败、未暂存或提交，commit hash不适用。P65保活/全主题密度/完整控件验收、真实权限发布和73页完整实施仍待完成。P62/P64待定离页规则没有被本轮更改。
