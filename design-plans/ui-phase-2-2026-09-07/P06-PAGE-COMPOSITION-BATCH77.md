# P06 实际 Vue C 组合 · 批次77

P06 原有身份草图只作方向参考。本批新增审核用 Vite 转换：完整保留 `LocalIdentity.vue` 的单一新密码字段、`token + new_password` 到 `POST /auth/password-reset/confirm`、缺 token 仍提交空值交由后端拒绝、query `state=expired`、204 成功后的“密码已更新，请重新登录”及显式返回登录。界面改为蓝色密码重置边界、白色单字段工作区和明确成功/失效/失败反馈；不显示 token，不新增确认密码字段，也不自动登录。

单元合同通过；实际 Vue 在 1440/390 与两种动效下共 48 项检查、12 个本地拦截 POST 通过。r1 图册为缺 token、query 过期、204 成功与 503 失败的双端 8 PNG/39 来源指纹。用于路由拦截的合成 token 不渲染；缺 token 的请求体、成功 204 的无自动登录、失败关联编号均有检查。

无需重启、部署或配置变更。待用户审核四种状态；真实重置链接、实际过期响应、慢请求去重、读屏、专用隔离账号与生产验收仍待完成。

## 后续交付状态

本批是隔离审核原型的历史记录。视觉方向后由用户“剩下全部通过”的授权覆盖；实际 Vue 重构、定向测试、提交与线上证据见[P06实施记录](P06-PAGE-COMPOSITION-IMPLEMENTATION.md)。原型数据仍不证明真实邮件/密码重置或生产权限。
