# P47 · 首次读取与重新读取 r1

## 范围与设计

起点 `main/7481ff3b`、工作树干净；沿用已经通过的 C 方向，在独立预览中仅向原 UiStatePanel 增加 loading 专用标题和说明，并注入仅匹配本审核 body / 活跃 P47 / loading 的 CSS。**生产组件、完整 script、事件、计时器、请求和权限均未改，未部署。**

首次读取使用「正在读取采集状态」「正在获取来源目录与运行状态，请稍候。」；紧凑白色区域、左对齐标题与说明、浅灰三条骨架，无进度百分比、完成时间或虚构数据。隐藏装饰眉题、移除本区域多余最小高度及骨架动画；骨架原有 aria-hidden 保留，原 polite / aria-busy 保留。frontend-design 用于延续 C 构图；fixing-accessibility 用于检查等待公告、原生禁用按钮及异步焦点。

重新读取不是初次空白：保持原目录、概要、搜索条件和筛选结果，仅保留现有「刷新中…」禁用按钮及最近刷新时间。本批没有重画或改写标题操作区；截图用于核对现状。用户已通过的两种空态组合独立记载于 [空态审核](P47-EMPTY-REVIEW.md)，不是本批加载状态或焦点提案的批准。

## 实际 Vue 验证

- 基线/审核 × 390/760/1440 × 首次读取/重新读取 × 成功/409，共 24 组、330 项检查、48 张 PNG，170 个当前原始源指纹。
- 从原 M03-03 fixture AST 提取两条目录数据与导航，当前真实 App 路由加载 ProviderAdapterCenter；所有请求仅允许本机白名单 GET，未知请求中止，无 POST、真实探针或登记写入。
- 本地 gate 控制待返回请求；首次请求期间无概要/假数据/无效操作；刷新期间保留 2/1/1/1 四项概要及「公开趋势」搜索的 1 项结果。
- 刷新按钮原生 disabled，读取成功和失败后解除；无自动额外读取，每组首次场景 1 次、刷新场景 2 次 GET。未修改安全重试、12 秒超时或缓存策略。
- 用户在等待期间将焦点移动到返回链接或搜索框，成功/失败完成均不抢焦点。刷新失败仍保留目录与搜索，并显示原失败提示；首次失败转原错误面板且显示测试关联编号。
- 本地时钟冻结以稳定截图，本批不覆盖真实耗时、超时、网络断开、完整读屏或短屏验收。其他错误/结算图只作过程证据，不将其措辞或布局算成新批准。

图像检查后收紧首次读取区域的继承留白，重新运行上述完整矩阵；不修改共享 UiStatePanel 或其他页面。每次 Vite 与 Chromium 均在 finally 关闭。

## 图包与复验

- [48 图总览](../../output/playwright/p47-loading-review/index.html)
- [逐项证据及源/图片指纹](../../output/playwright/p47-loading-review/evidence.json)
- 定向测试：`node --test tests/unit/ui-phase2-provider-adapter-loading.test.mjs`
- 重新生成正式图包：`node scripts/verify-ui-phase2-provider-adapter-loading.mjs --capture`

审核 CSS 与转换 helper 是本批唯一构图调节入口；生产无新配置、环境变量、接口或重启要求，OpenAPI 不适用。`output/playwright/p47-loading-review` 保留为用户要求的正式图包，不是临时垃圾；未创建临时脚本/日志文件，验证服务已关闭。

## 未完成边界

本批首次读取区域待用户审核；实际生产未接入此预览。空态焦点提案、首次读取失败措辞、跨页在途检查锁仍未扩权采用。全 73 页、全部状态、真实探针/权限、未知结果策略与生产部署验收继续开放。
