import test from'node:test';import assert from'node:assert/strict';import{readFile,access}from'node:fs/promises';const read=path=>readFile(new URL(`../../${path}`,import.meta.url),'utf8');
test('M01-04.A03/A13 MySQL57 migrations persist roles scopes decisions and reverse order',async()=>{const names=['0011a_roles_m01_04','0011b_capabilities_m01_04','0011c_role_capabilities_m01_04','0011d_membership_roles_m01_04','0011e_membership_scopes_m01_04','0011f_platform_roles_m01_04','0011g_authorization_decisions_m01_04','0011h_seed_roles_m01_04','0011i_seed_capabilities_m01_04','0011j_seed_role_capabilities_m01_04'];for(const name of names){await access(new URL(`../../database/migrations/${name}.up.sql`,import.meta.url));await access(new URL(`../../database/migrations/${name}.down.sql`,import.meta.url));assert.doesNotMatch(await read(`database/migrations/${name}.up.sql`),/JSON_TABLE|WITH\s+RECURSIVE|CHECK\s*\(/i);}const scopes=await read('database/migrations/0011e_membership_scopes_m01_04.up.sql');for(const value of ['own','team','workspace','organization'])assert.match(scopes,new RegExp(`'${value}'`));const decisions=await read('database/migrations/0011g_authorization_decisions_m01_04.up.sql');for(const surface of ['api','worker','export','file','event','sse'])assert.match(decisions,new RegExp(`'${surface}'`));});
test('M01-04.A02/A05/A09 role and guard contract separates six surfaces and platform scope',async()=>{const source=await read('packages/authorization/src/index.ts');for(const role of ['member','selection_manager','procurement_member','organization_admin','platform_operations_admin','platform_security_admin','platform_super_admin','auditor'])assert.match(source,new RegExp(`code:'${role}'`));for(const scope of ['own','team','workspace','organization','platform'])assert.match(source,new RegExp(`'${scope}'`));for(const surface of ['api','worker','export','file','event','sse'])assert.match(source,new RegExp(`'${surface}'`));assert.match(source,/permission_denied/);assert.match(source,/scopeMatches/);});
test('M01-04.A06/A13 OpenAPI DTO and API use server session without client actor fields',async()=>{const[openapi,contracts,routes]=await Promise.all([read('docs/openapi.yaml'),read('packages/contracts/src/index.ts'),read('apps/api/src/authorization-routes.ts')]);assert.match(openapi,/\/me\/authorization:/);assert.match(openapi,/\/org\/\{organizationId\}\/roles:/);for(const dto of ['CurrentAuthorizationSummary','RoleCapabilitySummary','AuthorizationScopeSummary'])assert.match(contracts,new RegExp(`interface ${dto}`));assert.match(routes,/options\.auth\.authenticate/);assert.match(routes,/capability:'role:read'/);assert.doesNotMatch(routes,/actor_id|membership_id|platform_role_assignments/);});
test('M01-04.A07/A08/A15 UI contains full state and responsive read-only role matrix',async()=>{const[ui,styles,e2e]=await Promise.all([read('apps/web/src/components/AuthorizationCenter.vue'),read('apps/web/src/styles.css'),read('tests/e2e/m01-04-rbac.spec.ts')]);for(const state of ["'loading'","'ready'","'empty'","'error'","'forbidden'","'expired'"])assert.match(ui,new RegExp(state));assert.match(ui,/只读权限预览/);assert.match(ui,/credentials:'include'/);assert.match(styles,/@media\(max-width:720px\)/);assert.match(e2e,/toHaveScreenshot/);assert.match(e2e,/keyboard\.press\('Enter'\)/);});
test('M01-04.A01/A10/A11/A17 docs maps and env lock exact boundaries',async()=>{const[architecture,runbook,map,env,blueprint,gate,registry]=await Promise.all([read('docs/architecture/m01-04-rbac-data-scope.md'),read('docs/runbooks/m01-04-rbac-data-scope.md'),read('docs/feature-map.json'),read('config/env.example'),read('new-product-enterprise-blueprint.md'),read('scripts/verify-docs.mjs'),read('verification/modules/M01-04.json')]);assert.match(architecture,/动作族/);assert.match(architecture,/M01-05/);assert.match(runbook,/0011j.*0011i.*0011h.*0011g.*0011f.*0011e.*0011d.*0011c.*0011b.*0011a/s);assert.match(map,/"authorization"/);assert.match(blueprint,/M01-04 RBAC 与数据范围基线/);assert.match(gate,/m01-04-rbac-data-scope/);assert.doesNotMatch(env,/RBAC_|AUTHORIZATION_|ROLE_DEFAULT/);for(let i=1;i<=17;i++)assert.match(registry,new RegExp(`M01-04\\.A${String(i).padStart(2,'0')}`));});
