import{test,expect}from'@playwright/test';const org='00000000-0000-4000-8000-000000000601',workspace='00000000-0000-4000-8000-000000000602',navigation={shell:'member',organization_id:org,workspace_id:workspace,roles:['member'],capabilities:['task:read','task:create','trend:read','opportunity:read','competitor:read','sourcing:read','notification:read'],platform_roles:[],platform_capabilities:[],guard_reason:'navigation_member_allowed'},at='2026-08-07T16:30:00.000Z',item=(id:string,kind:string,title:string,route:string,extra={})=>({id,kind,title,reason:'来自当前工作区的已验证投影',route,priority:null,owner_label:null,due_at:null,source_count:null,observed_at:at,severity:'info',source_version:1,...extra});async function nav(page:any){await page.route('**/api/v1/me/navigation?**',route=>route.fulfill({status:200,contentType:'application/json',body:JSON.stringify({data:navigation,request_id:'m02-06-nav',trace_id:'m02-06-nav'})}));}
test('M02-06.A07/A08/A15 verified home dashboard is responsive and visual',async({page})=>{await nav(page);await page.route('**/api/v1/me/home-dashboard',route=>route.fulfill({status:200,contentType:'application/json',body:JSON.stringify({data:{actions:[item('00000000-0000-4000-8000-000000000610','action','处理逾期采集复核','/tasks/1',{priority:'overdue',owner_label:'陈宇航',due_at:'2026-08-07T18:00:00.000Z'})],changes:[item('00000000-0000-4000-8000-000000000611','change','户外照明热度变化','/trends/1',{source_count:3})],follows:[item('00000000-0000-4000-8000-000000000612','follow','关注竞品出现变动','/competitors/1')],health:[item('00000000-0000-4000-8000-000000000613','health','竞品来源延迟','/platform-admin/collection',{severity:'warning'})],scope:{organization_id:org,workspace_id:workspace},generated_at:at},request_id:'m02-06-home',trace_id:'m02-06-home'})}));await page.goto('/home');await expect(page.getByRole('heading',{name:'今天最值得做什么？'})).toBeVisible();await expect(page.getByText('处理逾期采集复核')).toBeVisible();await expect(page.getByText('竞品来源延迟')).toBeVisible();await expect(page).toHaveScreenshot('m02-06-home.png',{fullPage:true});});
test('M02-06.A07/A15 390 opportunity detail route is an honest skeleton',async({page})=>{await page.setViewportSize({width:390,height:844});await nav(page);await page.goto('/opportunities/00000000-0000-4000-8000-000000000620');await expect(page.getByText('等待 P04 真实 API')).toBeVisible();await expect(page.getByRole('button',{name:'市场'})).toBeDisabled();await expect(page).toHaveScreenshot('m02-06-opportunity-mobile.png',{fullPage:true});});
test('M02-06.A08/A16 empty then blocked recovery never fabricates metrics',async({page})=>{await nav(page);let blocked=false;await page.route('**/api/v1/me/home-dashboard',route=>route.fulfill(blocked?{status:503,contentType:'application/json',body:JSON.stringify({error:{code:'dependency_unavailable',message:'依赖不可用',action_hint:'稍后重试'},request_id:'m02-06-blocked',trace_id:'m02-06-blocked'})}:{status:200,contentType:'application/json',body:JSON.stringify({data:{actions:[],changes:[],follows:[],health:[],scope:{organization_id:org,workspace_id:workspace},generated_at:at},request_id:'m02-06-empty',trace_id:'m02-06-empty'})}));await page.goto('/home');await expect(page.getByRole('heading',{name:'当前范围还没有首页数据'})).toBeVisible();await expect(page.getByText('现在不展示模拟指标')).toBeVisible();blocked=true;await page.reload();await expect(page.getByRole('heading',{name:'依赖暂时受阻'})).toBeVisible();await expect(page.getByText('request_id')).toBeVisible();});
