import{expect,test,type Page}from'@playwright/test';
const org='00000000-0000-4000-8000-000000000221',workspace='00000000-0000-4000-8000-000000000222';
const envelope=(data:unknown)=>({data,request_id:'theme-e2e-request',trace_id:'theme-e2e-trace'});
async function ready(page:Page){let preference={theme:'deep-ocean',source:'saved',organization_id:org,workspace_id:workspace,version:1,updated_at:'2026-08-07T14:00:00.000Z'};await page.route('**/api/v1/me/ui-preferences',async route=>{if(route.request().method()==='PUT'){const body=route.request().postDataJSON();preference={...preference,theme:body.theme,version:preference.version+1,updated_at:'2026-08-07T14:05:00.000Z'};}await route.fulfill({json:envelope(preference)});});}
test('M02-01.A07/A08/A15 theme studio previews and saves by keyboard without changing semantics',async({page})=>{await ready(page);await page.goto('/?view=theme');await expect(page.getByRole('heading',{name:'主题与视觉语义'})).toBeVisible();const aurora=page.getByRole('radio',{name:/极光紫/});await aurora.focus();await page.keyboard.press('Enter');await expect(aurora).toHaveAttribute('aria-checked','true');await expect(page.locator('html')).toHaveAttribute('data-theme','aurora-purple');await page.getByRole('button',{name:'保存主题'}).click();await expect(page.getByText('✓ 已保存')).toBeVisible();await expect(page.getByText('需求变化')).toBeVisible();await expect(page.getByText('近 30 天 · 来源：公开市场信号')).toBeVisible();await expect(page).toHaveScreenshot('m02-01-theme-studio.png',{fullPage:true,maxDiffPixels:120});});
test('M02-01.A08/A15/A16 blocked scope has an explicit 390px recovery path',async({page})=>{await page.route('**/api/v1/me/ui-preferences',route=>route.fulfill({status:409,json:{error:{code:'preference_scope_required'},request_id:'theme-blocked-request',trace_id:'theme-blocked-trace'}}));await page.goto('/?view=theme');await expect(page.getByText('尚未选择组织与工作区')).toBeVisible();await expect(page.getByRole('link',{name:'选择工作区'})).toHaveAttribute('href','/?view=tenancy');await expect(page.getByText('请求标识：theme-blocked-request')).toBeVisible();if(page.viewportSize()?.width===390)await expect(page).toHaveScreenshot('m02-01-theme-blocked-390.png',{fullPage:true});});
