import{test,expect}from'@playwright/test';const provider={id:'00000000-0000-4000-8000-000000000802',code:'browser_source',name:'登录页来源'},asset={id:'00000000-0000-4000-8000-000000000803',provider_id:provider.id,name:'北美浏览器档案',kind:'browser_profile',status:'active',key_version:'v1',fingerprint:'0123456789abcdef',expires_at:null,rotated_at:'2026-08-07T18:00:00.000Z',version:2,updated_at:'2026-08-07T18:00:00.000Z'},profile={id:'00000000-0000-4000-8000-000000000804',provider_id:provider.id,credential_asset_id:asset.id,code:'browser_us_a',name:'北美采集档案 A',browser_family:'chromium',locale:'en-US',timezone:'America/Los_Angeles',status:'disabled',version:1,updated_at:'2026-08-07T18:00:00.000Z'},navigation={shell:'platform_admin',organization_id:null,workspace_id:null,roles:[],capabilities:[],platform_roles:['platform_security_admin'],platform_capabilities:['platform:secure','key_rotation:manage'],guard_reason:'navigation_platform_admin_allowed'};async function nav(page:any){await page.route('**/api/v1/me/navigation?**',r=>r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({data:navigation,request_id:'m03-02-nav',trace_id:'m03-02-nav'})}));await page.route('**/api/v1/platform/credential-provider-options',r=>r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({data:[provider],request_id:'m03-02-providers',trace_id:'m03-02-providers'})}));await page.route('**/api/v1/platform/crawler-profiles',r=>r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({data:[profile],request_id:'m03-02-profiles',trace_id:'m03-02-profiles'})}));}
test('M03-02.A07/A08/A15 masked credential vault is responsive and visual',async({page})=>{await nav(page);await page.route('**/api/v1/platform/credential-assets',r=>r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({data:[asset],request_id:'m03-02-assets',trace_id:'m03-02-assets'})}));await page.goto('/platform-admin/credentials');await expect(page.getByRole('heading',{name:'凭证与浏览器档案',level:2})).toBeVisible();await expect(page.getByText('0123456789abcdef')).toBeVisible();await expect(page.getByText(/secret-never|cookie-value|payload_ciphertext/)).toHaveCount(0);await page.getByRole('button',{name:'凭证资产'}).click();await expect(page.getByRole('heading',{name:'创建凭证资产'})).toBeVisible();await expect(page.getByLabel('需要加密保存的内容')).toHaveAttribute('type','password');await expect(page).toHaveScreenshot('m03-02-credential-assets.png',{fullPage:true});});
test('M03-02.A08/A09/A16 revoke is explicit and fail closed',async({page})=>{await nav(page);await page.route('**/api/v1/platform/credential-assets',r=>r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({data:[asset],request_id:'m03-02-assets',trace_id:'m03-02-assets'})}));await page.goto('/platform-admin/credentials');await page.getByRole('button',{name:'撤销'}).click();const dialog=page.getByRole('alertdialog');await expect(dialog).toBeVisible();const confirm=dialog.getByRole('button',{name:'撤销资产'});await expect(confirm).toBeDisabled();await dialog.getByRole('checkbox').check();await dialog.getByPlaceholder('确认撤销').fill('确认撤销');await expect(confirm).toBeEnabled();await page.keyboard.press('Escape');await expect(dialog).toBeHidden();});
test('M03-02.A08/A16 empty, forbidden and dependency states are actionable',async({page})=>{await nav(page);let status=200;await page.route('**/api/v1/platform/credential-assets',r=>r.fulfill(status===200?{status,contentType:'application/json',body:JSON.stringify({data:[],request_id:'m03-02-empty',trace_id:'m03-02-empty'})}:{status,contentType:'application/json',body:JSON.stringify({error:{code:status===403?'authorization_denied':'dependency_unavailable',message:'请求失败',action_hint:'按状态恢复'},request_id:`m03-02-${status}`,trace_id:`m03-02-${status}`})}));await page.route('**/api/v1/platform/crawler-profiles',r=>r.fulfill({status:200,contentType:'application/json',body:JSON.stringify({data:[],request_id:'profiles',trace_id:'profiles'})}));await page.goto('/platform-admin/credentials');await expect(page.getByRole('heading',{name:'还没有平台凭证资产'})).toBeVisible();status=403;await page.reload();await expect(page.getByRole('heading',{name:'你没有此项权限'})).toBeVisible();status=503;await page.reload();await expect(page.getByRole('heading',{name:'依赖暂时受阻'})).toBeVisible();});
