import{test,expect,type Page}from'@playwright/test';
const opportunityId='00000000-0000-4000-8000-000000000434',ruleId='00000000-0000-4000-8000-000000000435',draftId='00000000-0000-4000-8000-000000000436',actor='00000000-0000-4000-8000-000000000437',envelope=(data:unknown)=>({data,request_id:'m04-03-e2e-request',trace_id:'m04-03-e2e-trace'}),dimensions=[{code:'market_demand',label:'市场需求',weight:40,required:true,evidence_group:'market'},{code:'competition',label:'竞争',weight:30,required:true,evidence_group:'competition'},{code:'profit',label:'利润',weight:30,required:true,evidence_group:'cost'}];
async function navigation(page:Page){await page.route('**/api/v1/me/navigation?shell=member',route=>route.fulfill({json:envelope({shell:'member',organization_id:'00000000-0000-4000-8000-000000000421',workspace_id:'00000000-0000-4000-8000-000000000422',roles:['selection_manager'],capabilities:['task:read','trend:read','opportunity:read','opportunity:decide','opportunity:approve'],platform_roles:[],platform_capabilities:[],guard_reason:'navigation_member_allowed'})}));}
test('M04-03.A07/A08/A09/A15 score rule versions support audited responsive workflow',async({page})=>{await navigation(page);let draftStatus='draft',draftRevision=1;const rules=()=>[{id:ruleId,version_code:'org-v1',name:'当前生产评分规则',status:'active',dimensions,thresholds:{recommend_min:75,observe_min:55},revision:4,submitted_at:'2026-08-07T10:00:00.000Z',approved_at:'2026-08-07T10:10:00.000Z',activated_at:'2026-08-07T10:20:00.000Z',updated_at:'2026-08-07T10:20:00.000Z'},{id:draftId,version_code:'org-v2',name:'候选评分规则',status:draftStatus,dimensions,thresholds:{recommend_min:78,observe_min:58},revision:draftRevision,submitted_at:null,approved_at:null,activated_at:null,updated_at:'2026-08-07T11:00:00.000Z'}];await page.route('**/api/v1/opportunity-score-rules',route=>route.request().method()==='POST'?route.fulfill({status:201,json:envelope(rules()[1])}):route.fulfill({json:envelope(rules())}));await page.route(`**/api/v1/opportunity-score-rules/${draftId}/actions`,route=>{draftStatus='pending_approval';draftRevision=2;return route.fulfill({json:envelope(rules()[1])});});await page.goto('/opportunities/scoring-rules');await expect(page.getByRole('heading',{name:'评分规则引擎'})).toBeVisible();await expect(page.getByText('当前生产评分规则')).toBeVisible();await page.getByRole('button',{name:'提交'}).click();const dialog=page.getByRole('dialog');await dialog.getByLabel('原因（必填）').fill('提交候选权重等待审批');await dialog.getByRole('button',{name:'确认 submit'}).click();await expect(page.getByText('规则动作 submit 已审计记录。')).toBeVisible();await expect(page.getByText('pending_approval',{exact:true})).toBeVisible();await page.evaluate(()=>window.scrollTo(0,0));await expect(page).toHaveScreenshot('m04-03-score-rules.png',{fullPage:true});});
test('M04-03.A07/A08/A15 opportunity score explanation exposes inputs evidence missing and confidence',async({page})=>{await navigation(page);const detail={id:opportunityId,name:'便携式智能净水杯机会',market:'US',category:'outdoor',source_type:'trend_topic',source_ref_id:null,owner_id:actor,lifecycle_status:'ready',recommendation_status:'recommend',overall_score:80.2,trend_score:82,competition_score:70,profit_status:'insufficient_data',risk_level:'unknown',confidence:{status:'measured',score:100},evidence_count:8,source_count:3,coverage_status:'complete',decision_status:'pending',version:7,updated_at:'2026-08-07T12:30:00.000Z',score_rule_version:'org-v1',scored_at:'2026-08-07T12:30:00.000Z',latest_score_run:{id:ruleId,status:'calculated',coverage_percent:100,confidence_score:100,recommendation_status:'recommend',missing_fields:[],scored_at:'2026-08-07T12:30:00.000Z'},score_components:[{dimension_code:'market_demand',weight_percent:40,input_score:82,weighted_score:32.8,evidence_ids:[ruleId],missing_fields:[]},{dimension_code:'competition',weight_percent:30,input_score:70,weighted_score:21,evidence_ids:[draftId],missing_fields:[]},{dimension_code:'profit',weight_percent:30,input_score:88,weighted_score:26.4,evidence_ids:[opportunityId],missing_fields:[]}],evidence:[],decisions:[],section_status:{market:'covered',competition:'covered',profit:'insufficient_data',risk:'insufficient_data',execution:'not_available'}};await page.route(`**/api/v1/opportunities/${opportunityId}`,route=>route.fulfill({json:envelope(detail)}));await page.route(`**/api/v1/opportunities/${opportunityId}/profit-analysis`,route=>route.fulfill({json:envelope({latest_run:null,current_inputs:[]})}));await page.route(`**/api/v1/opportunities/${opportunityId}/score-runs`,route=>route.fulfill({status:202,json:envelope({job_id:draftId,opportunity_id:opportunityId,version:8})}));await page.goto(`/opportunities/${opportunityId}`);await expect(page.getByRole('heading',{name:'便携式智能净水杯机会'})).toBeVisible();await expect(page.getByText('规则 org-v1 · 覆盖 100%')).toBeVisible();await expect(page.getByText('market_demand · 40%')).toBeVisible();await expect(page.getByRole('link',{name:'管理规则版本'})).toBeVisible();await page.evaluate(()=>window.scrollTo(0,0));await expect(page).toHaveScreenshot('m04-03-score-explanation.png',{fullPage:true});});
