import{expect,test,type Page}from'@playwright/test';
const org='00000000-0000-4000-8000-000000000231',workspace='00000000-0000-4000-8000-000000000232';const envelope=(data:unknown)=>({data,request_id:'onboarding-e2e-request',trace_id:'onboarding-e2e-trace'});
test('M02-02.A07/A08/A15 login uses the real contract and continues to tenancy by keyboard',async({page})=>{await page.route('**/api/v1/auth/login',route=>route.fulfill({json:envelope({user:{id:'u',email:'member@example.test',status:'active'},session:{id:'s'},security_setup:{required:false,must_change_password:false,must_enroll_mfa:false}})}));await page.route('**/api/v1/me/landing',route=>route.fulfill({json:envelope({shell:'select_context',route:'/select-context',reason:'landing_context_required'})}));await page.route('**/api/v1/org/memberships',route=>route.fulfill({json:envelope([])}));await page.goto('/login');await page.getByLabel('邮箱').fill('member@example.test');await page.getByLabel('密码').fill('Long-enough-password-123!');await page.getByRole('button',{name:'登录',exact:true}).focus();await page.keyboard.press('Enter');await expect(page).toHaveURL(/\/select-context$/);await expect(page.getByRole('heading',{name:'选择组织'})).toBeVisible();await expect(page.getByText('暂无可用组织')).toBeVisible();await expect(page).toHaveScreenshot('m02-02-login.png',{fullPage:true,maxDiffPixels:140});});
test('M02-02.A07/A08 registration and email confirmation preserve single-use truth',async({page})=>{await page.route('**/api/v1/auth/register',route=>route.fulfill({status:201,json:envelope({id:'u',email:'new@example.test',status:'pending_verification'})}));await page.goto('/register');await page.getByLabel('邮箱').fill('new@example.test');await page.getByLabel('密码',{exact:true}).fill('Long-enough-password-123!');await page.getByLabel('确认密码').fill('Long-enough-password-123!');await page.getByRole('button',{name:'创建账号'}).click();await expect(page.getByRole('heading',{name:'检查验证邮件'})).toBeVisible();await expect(page).toHaveScreenshot('m02-02-register-verify.png',{fullPage:true,maxDiffPixels:140});await page.route('**/api/v1/auth/email-verification/confirm',route=>route.fulfill({status:204}));await page.goto('/verify-email?token=single-use-token');await expect(page.getByRole('heading',{name:'邮箱验证完成'})).toBeVisible();});
test('M02-02.A08/A16 password recovery covers generic request reset and rate-limit recovery',async({page})=>{await page.route('**/api/v1/auth/password-reset/request',route=>route.fulfill({status:202,json:envelope({accepted:true})}));await page.goto('/forgot-password');await page.getByLabel('邮箱').fill('unknown@example.test');await page.getByRole('button',{name:'发送重置说明'}).click();await expect(page.getByText('如账号存在，重置邮件会进入受控投递队列。')).toBeVisible();await page.route('**/api/v1/auth/password-reset/confirm',route=>route.fulfill({status:204}));await page.goto('/reset-password?token=reset-token');await page.getByLabel('新密码').fill('New-long-password-456!');await page.getByRole('button',{name:'更新密码'}).click();await expect(page.getByText('密码已更新，请重新登录。')).toBeVisible();await page.unroute('**/api/v1/auth/password-reset/request');await page.route('**/api/v1/auth/password-reset/request',route=>route.fulfill({status:429,json:{error:{code:'rate_limited',message:'请求过于频繁。',action_hint:'稍后重试。'},request_id:'rate-request',trace_id:'rate-trace'}}));await page.goto('/forgot-password');await page.getByLabel('邮箱').fill('member@example.test');await page.getByRole('button',{name:'发送重置说明'}).click();await expect(page.getByText('请求标识：rate-request')).toBeVisible();});
test('M02-02.A07/A08/A15 tenancy hands off to all three onboarding steps at desktop and 390px',async({page})=>{await page.route('**/api/v1/org/memberships',route=>route.fulfill({json:envelope([{id:org,name:'华南增长中心',slug:'south-growth',status:'active',timezone:'Asia/Shanghai',default_workspace_id:workspace,membership_status:'active'}])}));await page.route(`**/api/v1/org/${org}/workspaces`,route=>route.fulfill({json:envelope([{id:workspace,organization_id:org,name:'新品决策工作区',slug:'new-products',status:'active',version:1}])}));await page.route(`**/api/v1/org/${org}/teams`,route=>route.fulfill({json:envelope([])}));await page.route('**/api/v1/auth/context',route=>route.fulfill({json:envelope({organization:{id:org,name:'华南增长中心'},workspace:{id:workspace,organization_id:org,name:'新品决策工作区',slug:'new-products',status:'active',version:1}})}));await page.goto('/select-context');await page.getByRole('button',{name:/华南增长中心/}).click();await page.getByRole('button',{name:/新品决策工作区/}).click();await page.getByRole('link',{name:'继续快速引导'}).click();await expect(page.getByRole('heading',{name:'把市场变化，变成今天的行动'})).toBeVisible();await page.getByRole('button',{name:'下一步'}).focus();await page.keyboard.press('Enter');await expect(page.getByRole('heading',{name:'让协作围绕同一份证据展开'})).toBeVisible();await page.keyboard.press('Enter');await expect(page.getByRole('heading',{name:'先看事实，再做可解释的决定'})).toBeVisible();await expect(page.getByRole('link',{name:'进入智能选品'})).toBeVisible();await expect(page).toHaveScreenshot('m02-02-onboarding.png',{fullPage:true,maxDiffPixels:140});});
