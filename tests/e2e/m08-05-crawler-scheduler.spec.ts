import{expect,test,type Page}from"@playwright/test";const envelope=(data:unknown)=>({data,request_id:"m08-05-e2e",trace_id:"m08-05-e2e"}),base={state:"ready",topology:{mode:"single_host",worker_instances:1,crawler_instances:1,maximum_workers:1,maximum_crawlers:1},leases:{active_worker:0,active_crawler:0,duplicate_count:0},providers:[{id:"00000000-0000-4000-8000-000000000851",code:"google_news_search",configured_concurrency:3,effective_concurrency:1,active_leases:0},{id:"00000000-0000-4000-8000-000000000852",code:"authorized_market",configured_concurrency:1,effective_concurrency:1,active_leases:0}],profiles:[{id:"00000000-0000-4000-8000-000000000853",active_leases:0}],resource:{load_basis_points:3240,available_memory_mb:6144,free_disk_mb:245760,observed_at:"2026-08-15T08:00:00.000Z"},findings:[],observed_at:"2026-08-15T08:00:00.000Z",capacity_claim:"unverified"};async function navigation(page:Page){await page.route("**/api/v1/me/navigation?shell=platform_admin",route=>route.fulfill({json:envelope({shell:"platform_admin",organization_id:null,workspace_id:null,roles:[],capabilities:[],platform_roles:["platform_operations_admin"],platform_capabilities:["platform:operate"],guard_reason:"allowed"})}));}test.beforeEach(async({page})=>navigation(page));
test("M08-05.A07/A08/A15 desktop and 390 single-host scheduler truth",async({page})=>{await page.route("**/api/v1/platform/operations/crawler-scheduler",route=>route.fulfill({json:envelope(base)}));await page.goto("/platform-admin/crawler-scheduler");await expect(page.getByRole("heading",{name:"Crawler 单机调度"})).toBeVisible();await expect(page.getByText("Crawler 单机调度门已满足")).toBeVisible();await expect(page.getByText("1 / 1")).toHaveCount(2);await expect(page).toHaveScreenshot("m08-05-crawler-scheduler-desktop.png",{fullPage:true});await page.setViewportSize({width:390,height:844});await page.reload();await expect(page.getByText("惠州单机固定一个 Node Worker、一个 Python Crawler；来源并发上限 1，不启用负载均衡或备用服务器。",{exact:true})).toBeVisible();await expect(page).toHaveScreenshot("m08-05-crawler-scheduler-mobile-390.png",{fullPage:true});});
test("M08-05.A08/A09/A16 warning blocked empty forbidden expired rate limited unavailable and recovering",async({page})=>{let status=200,response:unknown={...base,state:"warning",findings:[{code:"crawler_resource_warning",severity:"warning",action_hint:"持续观察。"}]};await page.route("**/api/v1/platform/operations/crawler-scheduler",route=>status===200?route.fulfill({json:envelope(response)}):route.fulfill({status,json:{error:{action_hint:"按权限或频率门恢复。"},request_id:"m08-05-state",trace_id:"m08-05-state"}}));await page.goto("/platform-admin/crawler-scheduler");await expect(page.getByText("单机资源接近停止线")).toBeVisible();response={...base,state:"blocked",findings:[{code:"crawler_lease_duplicate",severity:"blocked",action_hint:"通过宝塔回收。"}]};await page.reload();await expect(page.getByText("Crawler 单机调度已阻断")).toBeVisible();response=null;await page.reload();await expect(page.getByText("尚无调度观测")).toBeVisible();for(const[next,label]of[[403,"没有平台运维权限"],[401,"登录已失效"],[429,"刷新过于频繁"],[503,"单机调度事实暂不可用"]]as const){status=next;await page.reload();await expect(page.getByText(label)).toBeVisible();}await page.goto("/platform-admin/crawler-scheduler?state=recovering");await expect(page.getByText("正在回收过期租约")).toBeVisible();});
