import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";import{previewEmailVerificationPage}from"../../scripts/lib/email-verification-page-preview.mjs";test("P05 keeps automatic token confirmation and hides token",async()=>{const s=previewEmailVerificationPage(await readFile("apps/web/src/components/LocalIdentity.vue","utf8"));assert.match(s,/confirmEmail\(\)[\s\S]*?email-verification\/confirm/);assert.match(s,/mode\.value === "verify"[\s\S]*?params\.get\("token"\)/);assert.match(s,/token 不展示/);assert.doesNotMatch(s,/\{\{\s*params\.get\('token'\)\s*\}\}/)})
