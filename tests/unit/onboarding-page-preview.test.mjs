import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";import{previewOnboardingPage}from"../../scripts/lib/onboarding-page-preview.mjs";test("P09 keeps local three step navigation",async()=>{const s=previewOnboardingPage(await readFile("apps/web/src/components/OnboardingGuide.vue","utf8"));assert.match(s,/step=index/);assert.match(s,/@click="previous"/);assert.match(s,/@click="next"/);assert.match(s,/RouterLink v-else class="p09-primary" to="\/"/);});
