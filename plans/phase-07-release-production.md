# P07：S0 发布、生产验收与恢复详细执行计划

> 状态：in_progress（P00 至 P06 已通过阶段门禁；P06 run_id/trace_id：`e1ab135d-20f6-450a-90c0-de9746ef6575`；M07-01 至 M07-05 已通过，M07-05 最新同提交生产验收 run_id/trace_id：`cfadd66f-b1d4-4a3e-ac0f-faa1efac2b1a`，当前执行 M07-06）
> 上游总纲：`new-product-enterprise-blueprint.md`
> 阶段索引：`plans/README.md`
> 最低行数：1,000；`npm run verify:plans` 必须自动校验。

## 1. 阶段使命、边界与完成证据

本阶段负责：S0 发布、生产验收与恢复。必须输出下游可复用的稳定数据、API、事件、页面和运行合同。

- 完成要求：迁移、服务、API、权限、页面、全状态、配置、审计、测试、运行和回滚同时齐全。
- 不完成：只有页面、只有接口、手工确认、缺少异常状态、未同步合同或配置。
- 上游输入：通过报告、迁移版本、环境 schema、Feature Map 和 OpenAPI。
- 下游只能消费公开 API/DTO/事件，不得复制内部实现或绕开权限。

## 2. 总计划配置继承（每个模块必须执行）

### C01
- 规则：宝塔管理所有生产服务，禁止 systemd、独立 PM2、宿主 crontab、屏外 Docker 与外部核心托管服务。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C02
- 规则：惠州是 S0 主机房，真实数据与主文件不出中国境内；当前备份恢复只使用现有主机内的宝塔受控加密副本和隔离库，不配置备用服务器，不声明整机或异地灾备能力。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C03
- 规则：Vue 3 + TypeScript 仅运行 UI；浏览器不访问 DB、Redis、AI、Cookie 或密钥。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C04
- 规则：Node API、Node Worker、Python Crawler 是后端边界；MySQL 固定 5.7 utf8mb4。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C05
- 规则：Redis 只用于缓存、队列、限流、SSE 协调，不是权限或事件真相。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C06
- 规则：组织资源、缓存、队列、文件、SSE、导出和审计必须有 organization_id。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C07
- 规则：工作区资源必须有 workspace_id；组织级对象可为空但始终组织过滤。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C08
- 规则：凭证和浏览器档案 AES-256-GCM 加密；主密钥仅在宝塔受限环境。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C09
- 规则：账号、API Key、Cookie、档案是平台资产，组织只有使用权。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C10
- 规则：Playwright 档案仅在授权任务期间解密到受限临时目录，结束立即清理。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C11
- 规则：来源开发不以前置授权书、合同、法务或 robots 检查阻塞；禁止绕过登录和验证码。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C12
- 规则：任务使用 succeeded、succeeded_empty、completed_with_warnings、failed_terminal、dead_letter 与 coverage_status。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C13
- 规则：数据质量遵循总计划 13.2；任务成功不替代字段准确率和证据覆盖。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C14
- 规则：AI 仅做摘要、分类、解释和缺失提示，不能自动替代事实或决策。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C15
- 规则：利润缺项为 insufficient_data；市场、费用、汇率和规则版本化。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C16
- 规则：API、Worker、导出、文件、事件、SSE 都执行 RBAC 与范围 Guard。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C17
- 规则：文件按组织放入宝塔受控目录，下载短时授权且不可公开。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C18
- 规则：敏感读取、导出、重放、审批、轮换、撤销和失败全部审计。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C19
- 规则：健康检查分 live、ready、async、crawlers、providers、version。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C20
- 规则：日志有 request_id/trace_id，不记录密码、Cookie、Token 或主密钥。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C21
- 规则：邮件 Provider 未确认，只保留适配层。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C22
- 规则：UI 实现前必须阅读 images-html 全部说明、概念图、高清图和总览图。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C23
- 规则：成功、失败、风险不只依赖颜色；图表有文字、数值、时间、来源、新鲜度。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C24
- 规则：模块脚本先失败再通过，人工确认不替代自动验收。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C25
- 规则：字段、路由、事件、配置、权限同步总纲、OpenAPI、Feature Map、env.example、Runbook。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

### C26
- 规则：S1/S2 必须先完成共享文件、Redis Sentinel、MySQL 副本、负载均衡和演练。
- 实施：代码、测试、env.example、宝塔受限配置、日志、部署和验收同步。
- 验收：适用项缺失或不符合必须使模块脚本失败；不适用要有理由。

## 3. 页面、图片、布局与状态合同

- 路由范围：全部生产路由和健康检查。
- 图片基准：核心图 01、04–10；概念图 53、54、72。
- 桌面：顶部组织/工作区/搜索/创建/通知/个人；左侧按成员、组织、平台角色分离；主区为标题、筛选、内容、状态、CTA。
- 详情：先结论和 CTA，再显示证据、数据、公式、时间线、任务、审批和审计；图片示例不作生产事实。
- 表格：筛选在前、分页在后；500 行服务端分页，2,000 行虚拟滚动。
- 移动：390px 折叠导航/筛选，表格转卡片或横滚，关键动作和风险不隐藏。
- 无障碍：键盘可用；状态不只依赖颜色；图表有文字、数值、时间、来源。

## 4. 数据、接口、事件与配置合同

- 数据：全业务表、迁移、备份清单、审计链。每个对象明确 organization_id、workspace_id、时间、版本、索引、审计、保留。
- API：全部 `/api/v1/*`、`/open/v1/*`、`/health/*`。编码前写 OpenAPI；编码后契约测试 DTO、权限、错误、分页和幂等。
- 事件：事务写业务、审计、Outbox；消费者 event_id 幂等；SSE 可重放。
- 配置：schema、env.example、宝塔受限环境、启动校验与文档同步；真实值不进入 Git。

## 5. 模块依赖与验收顺序

| 模块 | 目标 | 前置 | 自动验收 |
|---|---|---|---|
| M07-01 | 全链路测试矩阵 | P00–P06 | `npm run verify:module -- M07-01` |
| M07-02 | 安全门禁 | M07-01 | `npm run verify:module -- M07-02` |
| M07-03 | S0 宝塔部署 | P00-08、P01–P06 | `npm run verify:module -- M07-03` |
| M07-04 | 备份与恢复 | M07-03 | `npm run verify:module -- M07-04` |
| M07-05 | 发布与回滚 | M07-01–M07-04 | `npm run verify:module -- M07-05` |
| M07-06 | 真实选品验收 | P00–P06 | `npm run verify:module -- M07-06` |

## 6. 逐模块详细实施卡
## 7. 阶段验收、交接与回滚

1. 按依赖顺序执行全部 verify:module；失败即阻断下游。
2. 执行 `npm run verify:phase -- P07`，校验模块、迁移、OpenAPI、Feature Map、env.example、质量、视觉/E2E、审计、临时产物。
3. 执行 `npm run verify:docs` 与 `npm run verify:plans`，再执行适用安全、性能、恢复和宝塔检查。
4. 交接：版本、变更、通过命令、迁移编号、环境变量、宝塔重启、回滚、限制。
5. 回滚：先关闭新增入口，再回滚应用，最后按迁移说明处理数据；不删除审计或证据掩盖失败。

## 8. 原子任务卡（阶段计划的执行粒度）

本节将每个模块拆成 17 个原子任务。任务完成必须有代码/配置/文档证据和自动验收结果；不得因同一模块其他任务完成而跳过。

### M07-01 原子任务索引
- 模块目标：全链路测试矩阵。
- 业务说明：角色、来源、任务、质量、移动端、性能与安全场景自动化。
- 图片依据：按本阶段第 3 节映射读取；页面、布局和交互均须保留可验证截图。
- 数据边界：任何组织可见记录、文件、队列、事件和导出均执行 organization_id/workspace_id 约束。
- 模块命令：`npm run verify:module -- M07-01`。

#### M07-01.A01 范围冻结
- 交付目标：为“全链路测试矩阵”完成 范围冻结。
- 实现动作：锁定功能、非目标、依赖、输入、输出和失败条件。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A01。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A02 领域对象
- 交付目标：为“全链路测试矩阵”完成 领域对象。
- 实现动作：定义实体、字段、枚举、状态、来源、组织归属与版本。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A02。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A03 数据库迁移
- 交付目标：为“全链路测试矩阵”完成 数据库迁移。
- 实现动作：实现 MySQL 5.7 迁移、索引、约束、回滚和隔离数据。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A03。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A04 服务实现
- 交付目标：为“全链路测试矩阵”完成 服务实现。
- 实现动作：实现领域服务、事务、错误映射、幂等和审计。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A04。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A05 异步处理
- 交付目标：为“全链路测试矩阵”完成 异步处理。
- 实现动作：实现 Worker/Crawler/Outbox、租约、重试、限流、死信和恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A05。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A06 API 合同
- 交付目标：为“全链路测试矩阵”完成 API 合同。
- 实现动作：更新 OpenAPI、DTO、分页、错误码、权限、Idempotency-Key 与版本锁。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A06。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A07 前端布局
- 交付目标：为“全链路测试矩阵”完成 前端布局。
- 实现动作：按图片基准实现桌面、390px、筛选、表格、详情、CTA 和状态区。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A07。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A08 前端交互
- 交付目标：为“全链路测试矩阵”完成 前端交互。
- 实现动作：实现表单、筛选、确认、加载、空、错误、无权、过期、受阻与恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A08。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A09 权限审查
- 交付目标：为“全链路测试矩阵”完成 权限审查。
- 实现动作：在路由、API、Worker、导出、文件、事件和 SSE 执行范围 Guard。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A09。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A10 配置注入
- 交付目标：为“全链路测试矩阵”完成 配置注入。
- 实现动作：更新 schema、env.example、宝塔说明、启动检查和密钥泄露扫描。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A10。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A11 可观测性
- 交付目标：为“全链路测试矩阵”完成 可观测性。
- 实现动作：输出 request_id、trace_id、结构化事件、指标、健康影响和告警。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A11。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A12 单元测试
- 交付目标：为“全链路测试矩阵”完成 单元测试。
- 实现动作：覆盖规则、边界、缺失、状态、错误映射和权限分支。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A12。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A13 契约测试
- 交付目标：为“全链路测试矩阵”完成 契约测试。
- 实现动作：覆盖 OpenAPI、DTO、事件、Provider 输入输出和客户端兼容。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A13。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A14 集成测试
- 交付目标：为“全链路测试矩阵”完成 集成测试。
- 实现动作：覆盖 MySQL、Redis、文件、队列、事务、重试和组织隔离。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A14。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A15 E2E 视觉
- 交付目标：为“全链路测试矩阵”完成 E2E 视觉。
- 实现动作：覆盖用户路径、键盘、无障碍、390px 和图片视觉回归。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A15。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A16 故障演练
- 交付目标：为“全链路测试矩阵”完成 故障演练。
- 实现动作：注入超时、限流、登录失效、依赖失败、回滚与恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A16。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A17 文档交付
- 交付目标：为“全链路测试矩阵”完成 文档交付。
- 实现动作：同步总纲、Feature Map、配置、Runbook、验收输出和回滚说明。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-01` 必须覆盖 M07-01.A17。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-01.A99 模块关卡
- [x] 原子任务 A01–A17 全部有实现证据和测试名称。
- [x] 组织隔离、权限、审计、配置、错误状态、图片/响应式（适用时）已覆盖。
- [x] `npm run verify:module -- M07-01` 返回 0；run_id/trace_id 为 `f12a7160-90ba-437c-9e6f-fc23149b9077`。

### M07-02 原子任务索引
- 模块目标：安全门禁。
- 业务说明：依赖、密钥、SQL/XSS/CSRF/SSRF、上传、越权和日志扫描。
- 图片依据：按本阶段第 3 节映射读取；页面、布局和交互均须保留可验证截图。
- 数据边界：任何组织可见记录、文件、队列、事件和导出均执行 organization_id/workspace_id 约束。
- 模块命令：`npm run verify:module -- M07-02`。

#### M07-02.A01 范围冻结
- 交付目标：为“安全门禁”完成 范围冻结。
- 实现动作：锁定功能、非目标、依赖、输入、输出和失败条件。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A01。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A02 领域对象
- 交付目标：为“安全门禁”完成 领域对象。
- 实现动作：定义实体、字段、枚举、状态、来源、组织归属与版本。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A02。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A03 数据库迁移
- 交付目标：为“安全门禁”完成 数据库迁移。
- 实现动作：实现 MySQL 5.7 迁移、索引、约束、回滚和隔离数据。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A03。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A04 服务实现
- 交付目标：为“安全门禁”完成 服务实现。
- 实现动作：实现领域服务、事务、错误映射、幂等和审计。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A04。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A05 异步处理
- 交付目标：为“安全门禁”完成 异步处理。
- 实现动作：实现 Worker/Crawler/Outbox、租约、重试、限流、死信和恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A05。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A06 API 合同
- 交付目标：为“安全门禁”完成 API 合同。
- 实现动作：更新 OpenAPI、DTO、分页、错误码、权限、Idempotency-Key 与版本锁。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A06。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A07 前端布局
- 交付目标：为“安全门禁”完成 前端布局。
- 实现动作：按图片基准实现桌面、390px、筛选、表格、详情、CTA 和状态区。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A07。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A08 前端交互
- 交付目标：为“安全门禁”完成 前端交互。
- 实现动作：实现表单、筛选、确认、加载、空、错误、无权、过期、受阻与恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A08。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A09 权限审查
- 交付目标：为“安全门禁”完成 权限审查。
- 实现动作：在路由、API、Worker、导出、文件、事件和 SSE 执行范围 Guard。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A09。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A10 配置注入
- 交付目标：为“安全门禁”完成 配置注入。
- 实现动作：更新 schema、env.example、宝塔说明、启动检查和密钥泄露扫描。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A10。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A11 可观测性
- 交付目标：为“安全门禁”完成 可观测性。
- 实现动作：输出 request_id、trace_id、结构化事件、指标、健康影响和告警。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A11。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A12 单元测试
- 交付目标：为“安全门禁”完成 单元测试。
- 实现动作：覆盖规则、边界、缺失、状态、错误映射和权限分支。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A12。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A13 契约测试
- 交付目标：为“安全门禁”完成 契约测试。
- 实现动作：覆盖 OpenAPI、DTO、事件、Provider 输入输出和客户端兼容。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A13。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A14 集成测试
- 交付目标：为“安全门禁”完成 集成测试。
- 实现动作：覆盖 MySQL、Redis、文件、队列、事务、重试和组织隔离。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A14。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A15 E2E 视觉
- 交付目标：为“安全门禁”完成 E2E 视觉。
- 实现动作：覆盖用户路径、键盘、无障碍、390px 和图片视觉回归。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A15。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A16 故障演练
- 交付目标：为“安全门禁”完成 故障演练。
- 实现动作：注入超时、限流、登录失效、依赖失败、回滚与恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A16。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A17 文档交付
- 交付目标：为“安全门禁”完成 文档交付。
- 实现动作：同步总纲、Feature Map、配置、Runbook、验收输出和回滚说明。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-02` 必须覆盖 M07-02.A17。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-02.A99 模块关卡
- [x] 原子任务 A01–A17 全部有实现证据和测试名称。
- [x] 组织隔离、权限、审计、配置、错误状态已覆盖；模块不新增页面，图片/响应式不适用并有明确证据。
- [x] `npm run verify:module -- M07-02` 返回 0；run_id/trace_id 为 `3bd453d5-0016-418e-8c33-e42132341def`。

### M07-03 原子任务索引
- 模块目标：S0 宝塔部署。
- 业务说明：网站反代、四类项目服务、MySQL、Redis、日志、计划任务与受限配置。
- 图片依据：按本阶段第 3 节映射读取；页面、布局和交互均须保留可验证截图。
- 数据边界：任何组织可见记录、文件、队列、事件和导出均执行 organization_id/workspace_id 约束。
- 模块命令：`npm run verify:module -- M07-03`。

#### M07-03.A01 范围冻结
- 交付目标：为“S0 宝塔部署”完成 范围冻结。
- 实现动作：锁定功能、非目标、依赖、输入、输出和失败条件。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A01。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A02 领域对象
- 交付目标：为“S0 宝塔部署”完成 领域对象。
- 实现动作：定义实体、字段、枚举、状态、来源、组织归属与版本。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A02。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A03 数据库迁移
- 交付目标：为“S0 宝塔部署”完成 数据库迁移。
- 实现动作：实现 MySQL 5.7 迁移、索引、约束、回滚和隔离数据。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A03。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A04 服务实现
- 交付目标：为“S0 宝塔部署”完成 服务实现。
- 实现动作：实现领域服务、事务、错误映射、幂等和审计。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A04。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A05 异步处理
- 交付目标：为“S0 宝塔部署”完成 异步处理。
- 实现动作：实现 Worker/Crawler/Outbox、租约、重试、限流、死信和恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A05。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A06 API 合同
- 交付目标：为“S0 宝塔部署”完成 API 合同。
- 实现动作：更新 OpenAPI、DTO、分页、错误码、权限、Idempotency-Key 与版本锁。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A06。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A07 前端布局
- 交付目标：为“S0 宝塔部署”完成 前端布局。
- 实现动作：按图片基准实现桌面、390px、筛选、表格、详情、CTA 和状态区。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A07。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A08 前端交互
- 交付目标：为“S0 宝塔部署”完成 前端交互。
- 实现动作：实现表单、筛选、确认、加载、空、错误、无权、过期、受阻与恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A08。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A09 权限审查
- 交付目标：为“S0 宝塔部署”完成 权限审查。
- 实现动作：在路由、API、Worker、导出、文件、事件和 SSE 执行范围 Guard。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A09。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A10 配置注入
- 交付目标：为“S0 宝塔部署”完成 配置注入。
- 实现动作：更新 schema、env.example、宝塔说明、启动检查和密钥泄露扫描。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A10。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A11 可观测性
- 交付目标：为“S0 宝塔部署”完成 可观测性。
- 实现动作：输出 request_id、trace_id、结构化事件、指标、健康影响和告警。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A11。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A12 单元测试
- 交付目标：为“S0 宝塔部署”完成 单元测试。
- 实现动作：覆盖规则、边界、缺失、状态、错误映射和权限分支。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A12。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A13 契约测试
- 交付目标：为“S0 宝塔部署”完成 契约测试。
- 实现动作：覆盖 OpenAPI、DTO、事件、Provider 输入输出和客户端兼容。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A13。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A14 集成测试
- 交付目标：为“S0 宝塔部署”完成 集成测试。
- 实现动作：覆盖 MySQL、Redis、文件、队列、事务、重试和组织隔离。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A14。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A15 E2E 视觉
- 交付目标：为“S0 宝塔部署”完成 E2E 视觉。
- 实现动作：覆盖用户路径、键盘、无障碍、390px 和图片视觉回归。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A15。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A16 故障演练
- 交付目标：为“S0 宝塔部署”完成 故障演练。
- 实现动作：注入超时、限流、登录失效、依赖失败、回滚与恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A16。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A17 文档交付
- 交付目标：为“S0 宝塔部署”完成 文档交付。
- 实现动作：同步总纲、Feature Map、配置、Runbook、验收输出和回滚说明。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-03` 必须覆盖 M07-03.A17。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-03.A99 模块关卡
- [x] 原子任务 A01–A17 全部有实现证据和测试名称。
- [x] 组织隔离、权限、审计、配置、错误状态、图片/响应式（适用时）已覆盖。
- [x] `npm run verify:module -- M07-03` 返回 0；run_id/trace_id：`8cb425c1-1606-4e4c-a736-8d29e27caf32`。

### M07-04 原子任务索引
- 模块目标：数据备份恢复。
- 业务说明：MySQL、文件、配置备份与隔离恢复演练。
- 图片依据：按本阶段第 3 节映射读取；页面、布局和交互均须保留可验证截图。
- 数据边界：任何组织可见记录、文件、队列、事件和导出均执行 organization_id/workspace_id 约束。
- 模块命令：`npm run verify:module -- M07-04`。

#### M07-04.A01 范围冻结
- 交付目标：为“数据备份恢复”完成 范围冻结。
- 实现动作：锁定功能、非目标、依赖、输入、输出和失败条件。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A01。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A02 领域对象
- 交付目标：为“数据备份恢复”完成 领域对象。
- 实现动作：定义实体、字段、枚举、状态、来源、组织归属与版本。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A02。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A03 数据库迁移
- 交付目标：为“数据备份恢复”完成 数据库迁移。
- 实现动作：实现 MySQL 5.7 迁移、索引、约束、回滚和隔离数据。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A03。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A04 服务实现
- 交付目标：为“数据备份恢复”完成 服务实现。
- 实现动作：实现领域服务、事务、错误映射、幂等和审计。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A04。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A05 异步处理
- 交付目标：为“数据备份恢复”完成 异步处理。
- 实现动作：实现 Worker/Crawler/Outbox、租约、重试、限流、死信和恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A05。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A06 API 合同
- 交付目标：为“数据备份恢复”完成 API 合同。
- 实现动作：更新 OpenAPI、DTO、分页、错误码、权限、Idempotency-Key 与版本锁。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A06。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A07 前端布局
- 交付目标：为“数据备份恢复”完成 前端布局。
- 实现动作：按图片基准实现桌面、390px、筛选、表格、详情、CTA 和状态区。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A07。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A08 前端交互
- 交付目标：为“数据备份恢复”完成 前端交互。
- 实现动作：实现表单、筛选、确认、加载、空、错误、无权、过期、受阻与恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A08。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A09 权限审查
- 交付目标：为“数据备份恢复”完成 权限审查。
- 实现动作：在路由、API、Worker、导出、文件、事件和 SSE 执行范围 Guard。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A09。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A10 配置注入
- 交付目标：为“数据备份恢复”完成 配置注入。
- 实现动作：更新 schema、env.example、宝塔说明、启动检查和密钥泄露扫描。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；管理员备份连接通过 `BACKUP_MYSQL_SOCKET` 使用现有 `root@localhost`，业务连接继续使用 `product_scout@127.0.0.1`，禁止新增 TCP root 账号；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A10。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A11 可观测性
- 交付目标：为“数据备份恢复”完成 可观测性。
- 实现动作：输出 request_id、trace_id、结构化事件、指标、健康影响和告警。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A11。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A12 单元测试
- 交付目标：为“数据备份恢复”完成 单元测试。
- 实现动作：覆盖规则、边界、缺失、状态、错误映射和权限分支。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A12。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A13 契约测试
- 交付目标：为“数据备份恢复”完成 契约测试。
- 实现动作：覆盖 OpenAPI、DTO、事件、Provider 输入输出和客户端兼容。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A13。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A14 集成测试
- 交付目标：为“数据备份恢复”完成 集成测试。
- 实现动作：覆盖 MySQL、Redis、文件、队列、事务、重试和组织隔离。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A14。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A15 E2E 视觉
- 交付目标：为“数据备份恢复”完成 E2E 视觉。
- 实现动作：覆盖用户路径、键盘、无障碍、390px 和图片视觉回归。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A15。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A16 故障演练
- 交付目标：为“数据备份恢复”完成 故障演练。
- 实现动作：注入超时、限流、登录失效、依赖失败、回滚与恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A16。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A17 文档交付
- 交付目标：为“数据备份恢复”完成 文档交付。
- 实现动作：同步总纲、Feature Map、配置、Runbook、验收输出和回滚说明。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-04` 必须覆盖 M07-04.A17。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-04.A99 模块关卡
- [x] 原子任务 A01–A17 全部有实现证据和测试名称。
- [x] 组织隔离、权限、审计、配置、错误状态、图片/响应式（适用时）已覆盖。
- [x] `npm run verify:module -- M07-04` 返回 0；run_id/trace_id：`d1ede17d-0f7b-4f42-b80c-2d5dac118651`。

### M07-05 原子任务索引
- 模块目标：发布与回滚。
- 业务说明：版本、迁移、灰度、监控、自动停止与回滚 Runbook。
- 生产写门：使用宝塔受限 HMAC 签名的 `/api/v1/platform/operations/releases/write-probe`；canonical 固定为 timestamp/nonce/release_id/sample_id，避免宝塔 Nginx 重写 request_id 破坏签名，同时保存代理实际 request/trace 审计。每个唯一样本只执行一次 MySQL 持久化提交；仅 Nginx 499 且没有 upstream_status 的到达上游前中断可用同一 sample_id 重试一次，证据单独计数，重试仍未送达即失败；不得重试候选拒绝。候选已到达上游的响应必须为 202，且候选 build 的持久化增量必须与候选已到达上游的 Nginx 写样本完全一致；默认 1 秒采样提高低流量 S0 的 P95 样本分辨率，不得用采样调整、幂等回放、密码重置多提交链、放宽 600ms 阈值或降低 1,800 秒观察替代。
- 图片依据：按本阶段第 3 节映射读取；页面、布局和交互均须保留可验证截图。
- 数据边界：任何组织可见记录、文件、队列、事件和导出均执行 organization_id/workspace_id 约束。
- 模块命令：`npm run verify:module -- M07-05`。

#### M07-05.A01 范围冻结
- 交付目标：为“发布与回滚”完成 范围冻结。
- 实现动作：锁定功能、非目标、依赖、输入、输出和失败条件。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A01。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A02 领域对象
- 交付目标：为“发布与回滚”完成 领域对象。
- 实现动作：定义实体、字段、枚举、状态、来源、组织归属与版本。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A02。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A03 数据库迁移
- 交付目标：为“发布与回滚”完成 数据库迁移。
- 实现动作：实现 MySQL 5.7 迁移、索引、约束、回滚和隔离数据。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A03。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A04 服务实现
- 交付目标：为“发布与回滚”完成 服务实现。
- 实现动作：实现领域服务、事务、错误映射、幂等和审计。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A04。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A05 异步处理
- 交付目标：为“发布与回滚”完成 异步处理。
- 实现动作：实现 Worker/Crawler/Outbox、租约、重试、限流、死信和恢复。
- 发布门定义：60 秒异步延迟只覆盖宝塔 Node Worker 实际领取的显式队列表、到期工作与过期租约；没有当前消费者的领域审计/后续投递 Outbox 保留事实但不冒充可执行队列。清单同时锁定在发布 manifest、实现和自动测试中；队列 `DATETIME(3)` 与 Worker 统一使用 MySQL 会话 `NOW(3)` 判断到期，禁止用 `UTC_TIMESTAMP(3)` 在非 UTC MySQL 系统时区下制造固定时差假积压，也禁止动态扫描任意 `status + available_at` 表、删除审计数据或放宽阈值。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A05。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A06 API 合同
- 交付目标：为“发布与回滚”完成 API 合同。
- 实现动作：更新 OpenAPI、DTO、分页、错误码、权限、Idempotency-Key 与版本锁。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A06。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A07 前端布局
- 交付目标：为“发布与回滚”完成 前端布局。
- 实现动作：按图片基准实现桌面、390px、筛选、表格、详情、CTA 和状态区。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A07。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A08 前端交互
- 交付目标：为“发布与回滚”完成 前端交互。
- 实现动作：实现表单、筛选、确认、加载、空、错误、无权、过期、受阻与恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A08。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A09 权限审查
- 交付目标：为“发布与回滚”完成 权限审查。
- 实现动作：在路由、API、Worker、导出、文件、事件和 SSE 执行范围 Guard。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A09。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A10 配置注入
- 交付目标：为“发布与回滚”完成 配置注入。
- 实现动作：更新 schema、env.example、宝塔说明、启动检查和密钥泄露扫描。
- E2E 运行合同：`PLAYWRIGHT_API_PORT`、`PLAYWRIGHT_WEB_PORT` 默认保持 4101/5173；宝塔验收时与 `APP_PORT` 配合改用未占用的隔离临时端口，不得复用生产 API。隔离 API/Web 启动等待固定为 300 秒，覆盖 Debian 单机真实构建耗时；超时仍失败关闭且不得据此放宽生产门禁。`PLAYWRIGHT_CHROMIUM_EXECUTABLE_PATH` 仅接受可选绝对路径，用于复用宝塔主机已验证的系统 Chromium；空值保持 Playwright 默认浏览器。模块命令在截图前验证 Linux Chromium 可执行和中文字体存在，任一缺失都失败关闭，不得用方框字建立视觉基线。惠州 Debian 11 主机按明确授权通过宝塔有限任务安装固定版本 Debian 官方 `fonts-noto-cjk`，保留 0600 安装前证据；四张 Linux 基线必须经中文渲染人工检查，回滚通过宝塔删除字体包并刷新缓存。
- MySQL 持久性合同：惠州当前共享 MySQL 经明确授权使用 `innodb_flush_log_at_trx_commit=2`、`sync_binlog=1`、`binlog-ignore-db=product_scout`；主机或操作系统故障最多可能丢失约 1 秒事务，且 ScoutOps 不具备 binlog/PITR。现有 `product_scout@127.0.0.1` 账号经明确授权增加全局只读 `REPLICATION CLIENT`，只用于发布任务读取 `SHOW MASTER STATUS`，不得授予 `SUPER`。生产证据必须携带该合同，漂移时失败关闭。配置和权限仅由宝塔受限路径留存快照并回滚，不新增面板外服务。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A10。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A11 可观测性
- 交付目标：为“发布与回滚”完成 可观测性。
- 实现动作：输出 request_id、trace_id、结构化事件、指标、健康影响和告警。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A11。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A12 单元测试
- 交付目标：为“发布与回滚”完成 单元测试。
- 实现动作：覆盖规则、边界、缺失、状态、错误映射和权限分支。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A12。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A13 契约测试
- 交付目标：为“发布与回滚”完成 契约测试。
- 实现动作：覆盖 OpenAPI、DTO、事件、Provider 输入输出和客户端兼容。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A13。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A14 集成测试
- 交付目标：为“发布与回滚”完成 集成测试。
- 实现动作：覆盖 MySQL、Redis、文件、队列、事务、重试和组织隔离。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A14。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A15 E2E 视觉
- 交付目标：为“发布与回滚”完成 E2E 视觉。
- 实现动作：覆盖用户路径、键盘、无障碍、390px 和图片视觉回归。
- 运行边界：Playwright 临时 API/Web 使用可配置隔离端口，结束自动关闭，不创建宝塔面板外生产服务。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A15。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A16 故障演练
- 交付目标：为“发布与回滚”完成 故障演练。
- 实现动作：注入超时、限流、登录失效、依赖失败、回滚与恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A16。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A17 文档交付
- 交付目标：为“发布与回滚”完成 文档交付。
- 实现动作：同步总纲、Feature Map、配置、Runbook、验收输出和回滚说明。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-05` 必须覆盖 M07-05.A17。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-05.A99 模块关卡
- [x] 原子任务 A01–A17 全部有实现证据和测试名称。
- [x] 组织隔离、权限、审计、配置、错误状态、图片/响应式（适用时）已覆盖。
- [x] `npm run verify:module -- M07-05` 在构建 `e22968896ab087647ad171f00a6f107c6824acf9` 的同提交隔离工作区返回 0，11/11 命令通过；run_id/trace_id：`cfadd66f-b1d4-4a3e-ac0f-faa1efac2b1a`。生产发布 `8b6114fe-fafa-4c6c-8a5d-ebd52ffe65ae` 的 5%/25%/100% 均完整观察 1,800 秒并通过原阈值。

### M07-06 原子任务索引
- 模块目标：真实选品验收。
- 业务说明：新成员三分钟内完成真实输入、结果/空/受阻、决策与证据查看。
- 图片依据：按本阶段第 3 节映射读取；页面、布局和交互均须保留可验证截图。
- 数据边界：任何组织可见记录、文件、队列、事件和导出均执行 organization_id/workspace_id 约束。
- 模块命令：`npm run verify:module -- M07-06`。

#### M07-06.A01 范围冻结
- 交付目标：为“真实选品验收”完成 范围冻结。
- 实现动作：锁定功能、非目标、依赖、输入、输出和失败条件。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A01。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A02 领域对象
- 交付目标：为“真实选品验收”完成 领域对象。
- 实现动作：定义实体、字段、枚举、状态、来源、组织归属与版本。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A02。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A03 数据库迁移
- 交付目标：为“真实选品验收”完成 数据库迁移。
- 实现动作：实现 MySQL 5.7 迁移、索引、约束、回滚和隔离数据。
- 实现记录：`0028_selection_journeys_m07_06` 保存旅程领域对象；`0029_collection_task_evidence_links_m07_06` 为首采与去重复用证据建立组织/工作区/任务/子查询/Provider 范围关联并回填既有首采关系，回滚时必须先切回旧代码再删除关联表。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A03。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A04 服务实现
- 交付目标：为“真实选品验收”完成 服务实现。
- 实现动作：实现领域服务、事务、错误映射、幂等和审计。
- 实现记录：证据命中去重键时不得直接返回；Worker 必须在同一事务中验证当前任务范围、写 `deduplicated` 关联及事件/Outbox，旅程和机会决策只从当前任务关联读取证据。Google RSS 同 GUID 仅未消费 XML 包装变化且规范 URL、Parser、Adapter、Schema、规范载荷完全相同时复用旧不可变证据，审计 `content_changed` 与新旧内容哈希；规范事实变化继续失败关闭。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A04。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A05 异步处理
- 交付目标：为“真实选品验收”完成 异步处理。
- 实现动作：实现 Worker/Crawler/Outbox、租约、重试、限流、死信和恢复。
- 实现记录：保留既有宝塔 Node Worker/Python Crawler 和原始证据去重模型，不新增 daemon；重复采集通过任务—证据多对多关联恢复当前任务可见性，供应链投影同步按关联读取。RSS 包装哈希变化不覆盖原文，只有规范事实逐项相同才允许关联并写 `evidence.linked` 变化审计。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A05。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A06 API 合同
- 交付目标：为“真实选品验收”完成 API 合同。
- 实现动作：更新 OpenAPI、DTO、分页、错误码、权限、Idempotency-Key 与版本锁。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A06。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A07 前端布局
- 交付目标：为“真实选品验收”完成 前端布局。
- 实现动作：按图片基准实现桌面、390px、筛选、表格、详情、CTA 和状态区。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A07。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A08 前端交互
- 交付目标：为“真实选品验收”完成 前端交互。
- 实现动作：实现表单、筛选、确认、加载、空、错误、无权、过期、受阻与恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A08。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A09 权限审查
- 交付目标：为“真实选品验收”完成 权限审查。
- 实现动作：在路由、API、Worker、导出、文件、事件和 SSE 执行范围 Guard。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A09。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A10 配置注入
- 交付目标：为“真实选品验收”完成 配置注入。
- 实现动作：更新 schema、env.example、宝塔说明、启动检查和密钥泄露扫描。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 项目代理规则：如固定 Google News 来源需要局域网出口，只在 ScoutOps API、Worker 和来源有限任务配置成组 `PROVIDER_PROXY_*`；禁止全局 `HTTP_PROXY`/`HTTPS_PROXY`，禁止向成员或浏览器暴露，且不得放宽 10000 ms 来源健康门和 180000 ms 旅程门。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A10。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A11 可观测性
- 交付目标：为“真实选品验收”完成 可观测性。
- 实现动作：输出 request_id、trace_id、结构化事件、指标、健康影响和告警。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A11。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A12 单元测试
- 交付目标：为“真实选品验收”完成 单元测试。
- 实现动作：覆盖规则、边界、缺失、状态、错误映射和权限分支。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A12。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A13 契约测试
- 交付目标：为“真实选品验收”完成 契约测试。
- 实现动作：覆盖 OpenAPI、DTO、事件、Provider 输入输出和客户端兼容。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A13。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A14 集成测试
- 交付目标：为“真实选品验收”完成 集成测试。
- 实现动作：覆盖 MySQL、Redis、文件、队列、事务、重试和组织隔离。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A14。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A15 E2E 视觉
- 交付目标：为“真实选品验收”完成 E2E 视觉。
- 实现动作：覆盖用户路径、键盘、无障碍、390px 和图片视觉回归。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A15。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A16 故障演练
- 交付目标：为“真实选品验收”完成 故障演练。
- 实现动作：注入超时、限流、登录失效、依赖失败、回滚与恢复。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A16。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A17 文档交付
- 交付目标：为“真实选品验收”完成 文档交付。
- 实现动作：同步总纲、Feature Map、配置、Runbook、验收输出和回滚说明。
- 前置输入：模块已声明的依赖、总计划、OpenAPI、Feature Map、环境 schema 和图片包。
- 数据规则：字段保留来源、时间、版本、审计和组织范围；禁止跨组织查询和静默覆盖。
- 页面规则：提供桌面与 390px 状态；不得显示内部凭证、Cookie、队列细节或其他组织数据。
- 安全规则：所有写入、导出、下载、事件和后台操作都使用最小权限和可审计身份。
- 异常规则：空结果、缺字段、冲突、超时、限流、登录失效、无权和依赖失败必须有错误码、文案与下一步。
- 配置规则：真实值仅在宝塔受限环境；同步 env.example 和启动校验，但不得提交秘密。
- 自动测试：增加至少一个可失败的定向测试；失败输出模块 ID、最小复现和 request_id/trace_id。
- 自动验收：`npm run verify:module -- M07-06` 必须覆盖 M07-06.A17。
- 通过标准：实现、迁移/API/页面（适用时）、文档、配置、测试和审计全部同步。

#### M07-06.A99 模块关卡
- [ ] 原子任务 A01–A17 全部有实现证据和测试名称。
- [ ] 组织隔离、权限、审计、配置、错误状态、图片/响应式（适用时）已覆盖。
- [ ] `npm run verify:module -- 模块 ID` 返回 0；失败报告可定位到原子任务。

## 9. 阶段交付证据目录

- 模块报告：每个模块产出 verify:module 输出。
- 阶段报告：verify:phase 输出包含通过模块、阻断项、迁移、OpenAPI、Feature Map、配置与运行状态。
- 视觉报告：引用 images-html 的具体图号、桌面截图、390px 截图和状态截图。
- 数据报告：迁移编号、schema 校验、组织隔离、质量指标和回滚演练。
- 运维报告：宝塔对象名、日志路径、健康检查、重启要求、备份与回滚。

## 10. 模块依赖、输入与输出关系

| 顺序 | 模块 | 前置输入 | 本模块输出 | 下游使用方 | 自动门禁 |
| 1 | M07-01 全链路测试矩阵 | P00–P06 | 角色、来源、任务、质量、移动端、性能与安全场景自动化 | 本阶段后续模块及下游阶段 | npm run verify:module -- M07-01 |
| 2 | M07-02 安全门禁 | P00–P06 | 依赖、密钥、SQL/XSS/CSRF/SSRF、上传、越权和日志扫描 | 本阶段后续模块及下游阶段 | npm run verify:module -- M07-02 |
| 3 | M07-03 S0 宝塔部署 | P00-08、P01–P06 | 网站反代、四类项目服务、MySQL、Redis、日志、计划任务与受限配置 | 本阶段后续模块及下游阶段 | npm run verify:module -- M07-03 |
| 4 | M07-04 数据备份恢复 | M07-03 | MySQL、文件、配置备份与隔离恢复演练 | 本阶段后续模块及下游阶段 | npm run verify:module -- M07-04 |
| 5 | M07-05 发布与回滚 | M07-01–M07-04 | 版本、迁移、灰度、监控、自动停止与回滚 Runbook | 本阶段后续模块及下游阶段 | npm run verify:module -- M07-05 |
| 6 | M07-06 真实选品验收 | P00–P06 | 新成员三分钟内完成真实输入、结果/空/受阻、决策与证据查看 | 本阶段后续模块及下游阶段 | npm run verify:module -- M07-06 |

### 10.1 依赖执行规则
- 阶段 P07 中任何模块不得在其前置模块未通过自动验收时宣称完成。
- 模块只能通过公开 API、DTO、迁移、事件与配置 schema 交付给下游；禁止复制服务内部逻辑或绕过权限。
- 上游字段、状态、路由、事件、配置变化时，依赖模块必须重新执行定向验收和阶段验收。
- 依赖外部来源、AI、邮件或服务器状态时，自动验收必须返回通过、失败或 blocked；blocked 不得伪装为通过。
